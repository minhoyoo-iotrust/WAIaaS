---
phase: 244-core-design-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/objectives/m28-00-defi-basic-protocol-design.md
  - internal/objectives/m28-02-0x-evm-swap.md
autonomous: true
requirements:
  - PKGS-01
  - PKGS-02
  - PKGS-03
  - PKGS-04
  - APIC-01
  - APIC-02
  - APIC-03
  - APIC-04
  - APIC-05

must_haves:
  truths:
    - "packages/actions/ 디렉토리 트리와 모듈 구조가 확정되어 m28-01에서 패키지 스캐폴딩을 즉시 생성할 수 있다"
    - "내장 프로바이더 등록/해제 라이프사이클이 설계되어 데몬 시작 시 enabled 프로바이더만 로딩하는 방식이 명확하다"
    - "config.toml [actions.*] 공통 스키마 패턴이 정의되어 프로바이더별 설정 추가 시 일관된 구조를 따른다"
    - "Admin Settings 런타임 변경 가능 설정 항목이 정의되어 config.toml vs Admin Settings 경계가 명확하다"
    - "ActionApiClient 베이스 패턴이 설계되어 4개 프로바이더 API 클라이언트의 공통 기반으로 사용할 수 있다"
    - "외부 REST API 응답 -> ContractCallRequest 변환 매핑이 Solana/EVM 양쪽에 정의된다"
    - "DeFi 에러 코드가 기존 에러 체계에 추가 설계된다"
    - "슬리피지 제어 공통 로직이 프로바이더별 단위를 포함하여 설계된다"
    - "0x AllowanceHolder 토큰 승인 플로우가 기존 APPROVE 파이프라인과의 연동이 명확하다"
  artifacts:
    - path: "internal/objectives/m28-00-defi-basic-protocol-design.md"
      provides: "DEFI-01 패키지 구조 설계 + DEFI-02 API 변환 패턴 설계 확정"
      contains: "DEFI-01"
    - path: "internal/objectives/m28-02-0x-evm-swap.md"
      provides: "AllowanceHolder 기반 설계로 업데이트"
      contains: "AllowanceHolder"
  key_links:
    - from: "DEFI-01 패키지 구조"
      to: "DEFI-02 API 변환 패턴"
      via: "ActionApiClient가 packages/actions/src/common/에 위치"
      pattern: "action-api-client"
    - from: "DEFI-02 슬리피지 제어"
      to: "config.toml [actions.*]"
      via: "프로바이더별 단위(_bps/_pct)를 config 키에 반영"
      pattern: "slippage"
---

<objective>
m28-00 설계 문서에 DEFI-01(패키지 구조)과 DEFI-02(API 변환 패턴) 설계를 확정하여 작성한다.

Purpose: m28-01~m28-05 구현 시 패키지 스캐폴딩, API 클라이언트, 에러 처리, 슬리피지 제어가 동일한 설계 기반 위에서 진행되도록 한다.
Output: m28-00 objective 파일에 DEFI-01/DEFI-02 설계 섹션 확정 + m28-02 objective AllowanceHolder 업데이트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/m28-defi-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/m28-defi-ARCHITECTURE.md
@.planning/research/m28-defi-protocol-PITFALLS.md
@internal/objectives/m28-00-defi-basic-protocol-design.md
@internal/objectives/m28-01-jupiter-swap.md
@internal/objectives/m28-02-0x-evm-swap.md
@packages/core/src/enums/transaction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEFI-01 패키지 구조 설계 확정</name>
  <files>internal/objectives/m28-00-defi-basic-protocol-design.md</files>
  <action>
m28-00 설계 문서의 "1. packages/actions/ 패키지 구조" 섹션을 아래 내용으로 확정하여 교체한다. 기존 "설계 범위" / "설계 산출물" 구조를 **확정 설계**로 전환.

**DEFI-01 확정 내용 (4개 요구사항 대응):**

**PKGS-01: 디렉토리 구조 확정**
아래 디렉토리 트리를 확정 설계로 작성한다:
```
packages/actions/
  package.json                    # @waiaas/actions, workspace dependencies only
  tsconfig.json                   # extends root, ES2022, NodeNext
  src/
    index.ts                      # 내장 프로바이더 export + registerBuiltInProviders()
    common/
      action-api-client.ts        # ActionApiClient base (fetch + AbortController + Zod)
      slippage.ts                 # SlippageHelper (clamp, bps/pct 변환, branded types)
      errors.ts                   # DeFi 에러 코드 정의 (ACTION_API_ERROR 등)
    providers/
      jupiter-swap/               # m28-01
        index.ts                  # JupiterSwapActionProvider : IActionProvider
        jupiter-api-client.ts     # Jupiter REST API (extends ActionApiClient)
        schemas.ts                # QuoteResponse, SwapInstructionsResponse Zod
        config.ts                 # JupiterSwapConfig type + defaults
      0x-swap/                    # m28-02
        index.ts                  # ZeroExSwapActionProvider : IActionProvider
        0x-api-client.ts          # 0x REST API (extends ActionApiClient)
        schemas.ts                # PriceResponse, QuoteResponse Zod
        config.ts                 # ZeroExSwapConfig type + defaults
        allowance-holder.ts       # AllowanceHolder approve helper (NOT Permit2)
      lifi/                       # m28-03
        index.ts                  # LiFiActionProvider : IActionProvider
        lifi-api-client.ts        # LI.FI REST API (extends ActionApiClient)
        schemas.ts                # QuoteResponse, StatusResponse Zod
        config.ts                 # LiFiConfig type + defaults
      lido/                       # m28-04
        index.ts                  # LidoStakingActionProvider : IActionProvider
        lido-contract.ts          # ABI encodings (submit, requestWithdrawals)
        schemas.ts                # Input Zod schemas
        config.ts                 # LidoConfig type + defaults
      jito/                       # m28-04
        index.ts                  # JitoStakingActionProvider : IActionProvider
        jito-stake-pool.ts        # SPL Stake Pool instruction builder
        schemas.ts                # Input Zod schemas
        config.ts                 # JitoConfig type + defaults
```

package.json 확정 내용:
- name: "@waiaas/actions"
- dependencies: @waiaas/core (workspace:*), viem (^2.21.0), @solana/kit (^6.0.1), @solana-program/token (^0.10.0), @solana-program/system (^0.11.0), zod (^3.24.0)
- devDependencies: @types/node
- 신규 npm 의존성: 0개 (모두 기존 모노레포 패키지)

daemon package.json에 추가: "@waiaas/actions": "workspace:*"

**PKGS-02: 내장 프로바이더 등록/해제 라이프사이클 설계**
registerBuiltInProviders() 함수의 설계를 확정한다:
1. 데몬 시작 시 Step 4 (DaemonLifecycle)에서 호출
2. config.actions.{provider_name}.enabled 플래그 확인
3. enabled=true인 프로바이더만 factory() 호출 후 registry.register()
4. 등록 실패 시 warn 로그 + skip (데몬 전체 실패 방지)
5. 반환: { loaded: string[], skipped: string[] } -- 데몬 시작 로그에 출력
6. 데몬 종료 시: 별도 해제 불필요 (프로바이더는 stateless, GC 처리)

**PKGS-03: config.toml [actions.*] 공통 스키마 패턴 확정**
모든 프로바이더 공통 필드:
- enabled: boolean (필수, 기본 false)
- api_base_url: string (REST API 프로바이더만)
- api_key: string (0x 필수, Jupiter/LI.FI 선택)

프로바이더별 슬리피지 필드:
- Jupiter: default_slippage_bps (integer), max_slippage_bps (integer) -- API 네이티브 bps 단위
- 0x: default_slippage_pct (decimal), max_slippage_pct (decimal) -- API 네이티브 pct 단위
- LI.FI: default_slippage_pct (decimal), max_slippage_pct (decimal) -- API 네이티브 pct 단위

config.toml Zod 검증 스키마: ActionsConfigSchema를 core에 추가.
환경변수 오버라이드: WAIAAS_ACTIONS_{PROVIDER}_{KEY} 패턴 (기존 패턴 일관).

각 프로바이더의 전체 config.toml 섹션 예시를 포함한다 (연구 STACK.md의 config.toml 패턴 참조).

**PKGS-04: Admin Settings 런타임 변경 가능 설정 항목 확정**
config.toml (정적, 재시작 필요):
- enabled, api_base_url, 컨트랙트 주소 (steth_address, withdrawal_queue_address, stake_pool, jitosol_mint)

Admin Settings (런타임, hot-reload 가능):
- api_key (Jupiter, 0x, LI.FI)
- default_slippage_bps / default_slippage_pct
- max_slippage_bps / max_slippage_pct
- max_price_impact_pct (Jupiter)
- jito_tip_lamports (Jupiter)
- status_poll_interval_sec, status_poll_max_attempts (LI.FI)

경계 원칙: "보안 자격 증명과 인프라 설정 = config.toml only, 운영 파라미터 = Admin Settings"
Settings snapshot 패턴: resolve() 진입 시 설정 스냅샷 획득, 파이프라인 완료까지 스냅샷 사용 (Pitfall P19 방지).
  </action>
  <verify>
m28-00 문서에서 "DEFI-01" 키워드 검색 시 확정 설계 섹션이 존재하고, 디렉토리 트리, package.json, registerBuiltInProviders(), config.toml 패턴, Admin Settings 경계가 모두 포함되어 있는지 확인한다. grep "DEFI-01" internal/objectives/m28-00-defi-basic-protocol-design.md로 확인.
  </verify>
  <done>
PKGS-01~04 요구사항 모두 충족: 디렉토리 트리 확정, 프로바이더 등록 라이프사이클 설계, config.toml 패턴 정의, Admin Settings 경계 명확화. m28-01에서 설계 문서만 보고 패키지 스캐폴딩을 즉시 생성할 수 있다.
  </done>
</task>

<task type="auto">
  <name>Task 2: DEFI-02 API 변환 패턴 설계 확정 + m28-02 AllowanceHolder 업데이트</name>
  <files>internal/objectives/m28-00-defi-basic-protocol-design.md, internal/objectives/m28-02-0x-evm-swap.md</files>
  <action>
**Part A: m28-00 설계 문서 "2. REST API -> calldata 변환 공통 패턴" 섹션을 확정 설계로 교체.**

**APIC-01: ActionApiClient 베이스 패턴 확정**
ActionApiClient 클래스 설계를 코드 수준까지 확정한다:
```typescript
// packages/actions/src/common/action-api-client.ts
export class ActionApiClient {
  constructor(
    private readonly baseUrl: string,
    private readonly timeoutMs: number = 10_000,
    private readonly headers: Record<string, string> = {},
  ) {}

  async get<T>(path: string, schema: z.ZodType<T>, params?: Record<string, string>): Promise<T>
  async post<T>(path: string, body: unknown, schema: z.ZodType<T>): Promise<T>
}
```

핵심 설계 결정:
- native fetch + AbortController (SDK 미사용, 신규 의존성 0개)
- 모든 응답에 Zod schema.parse() 적용 -- API drift 런타임 감지
- 실패 시 ACTION_API_ERROR 에러 코드 반환
- 타임아웃 기본 10초, 크로스체인(LI.FI)은 15초
- resolve()는 파이프라인 진입 전에 호출되므로 파이프라인 타임아웃과 독립 (Pitfall P10 방지)
- 응답 로깅: 첫 성공 시 + Zod 실패 시 raw response 로깅

**APIC-02: ContractCallRequest 변환 매핑 확정**
Solana 매핑:
```
외부 API instruction -> ContractCallRequest:
  type: 'CONTRACT_CALL'
  to: programId (Jupiter v6 program)
  programId: instruction.programId
  instructionData: instruction.data (base64)
  accounts: instruction.accounts (AccountMeta[])
```

EVM 매핑:
```
외부 API calldata -> ContractCallRequest:
  type: 'CONTRACT_CALL'
  to: quote.to (ExchangeProxy / LI.FI router / stETH contract)
  calldata: quote.data (hex)
  value: quote.value (ETH amount, 0 for token swaps)
```

두 매핑의 공통점/차이점 테이블을 명시한다.

**APIC-03: DeFi 에러 코드 확정**
기존 WAIaaSError 체계에 추가할 DeFi 에러 코드:
| 코드 | HTTP | 설명 | 사용처 |
|------|------|------|--------|
| ACTION_API_ERROR | 502 | 외부 DeFi API 호출 실패 (non-200 응답) | 모든 REST API 프로바이더 |
| ACTION_API_TIMEOUT | 504 | 외부 DeFi API 타임아웃 | 모든 REST API 프로바이더 |
| ACTION_RATE_LIMITED | 429 | 외부 API rate limit 초과 | Jupiter, 0x, LI.FI |
| PRICE_IMPACT_TOO_HIGH | 422 | 가격 영향이 설정 상한 초과 | Jupiter, 0x |
| ACTION_REQUIRES_APPROVAL | 409 | ERC-20 토큰 승인 필요 (AllowanceHolder) | 0x |
| BRIDGE_ROUTE_NOT_FOUND | 404 | 크로스체인 경로 없음 | LI.FI |
| JITO_UNAVAILABLE | 503 | Jito 블록 엔진 사용 불가 (fail-closed) | Jupiter |
| QUOTE_EXPIRED | 410 | 견적 TTL 만료 | Gas Condition 재실행 시 |

각 에러 코드의 ChainError -> WAIaaSError 변환 경로를 명시한다.

**APIC-04: 슬리피지 제어 공통 로직 확정**
SlippageHelper 설계:
```typescript
// 브랜디드 타입으로 단위 혼동 방지
type SlippageBps = number & { __brand: 'bps' };
type SlippagePct = number & { __brand: 'pct' };

function clampSlippageBps(input: number, default_: SlippageBps, max: SlippageBps): SlippageBps
function clampSlippagePct(input: number, default_: SlippagePct, max: SlippagePct): SlippagePct
function bpsToSlippagePct(bps: SlippageBps): SlippagePct  // 50 -> 0.005
function pctToSlippageBps(pct: SlippagePct): SlippageBps  // 0.005 -> 50
```

프로바이더별 단위 매핑 테이블:
| 프로바이더 | API 파라미터 | 단위 | config 키 | 기본값 | 상한 |
|-----------|-------------|------|-----------|--------|------|
| Jupiter | slippageBps | bps(integer) | default_slippage_bps / max_slippage_bps | 50 (0.5%) | 500 (5%) |
| 0x | slippagePercentage | pct(decimal) | default_slippage_pct / max_slippage_pct | 0.01 (1%) | 0.05 (5%) |
| LI.FI | slippage | pct(decimal) | default_slippage_pct / max_slippage_pct | 0.03 (3%) | 0.05 (5%) |

config 검증 바운드: Jupiter bps 1~10000, 0x/LI.FI pct 0.001~1.0. 범위 초과 시 config 로딩에서 즉시 에러.

**APIC-05: 0x AllowanceHolder 토큰 승인 플로우 확정**
Research에서 확인된 바와 같이 AllowanceHolder를 사용한다 (Permit2 대신).
AllowanceHolder 플로우:
1. 0x /swap/allowance-holder/quote 호출 (NOT /swap/permit2/quote)
2. ERC-20 판매 시: AllowanceHolder 컨트랙트에 대한 standard ERC-20 approve 필요
3. approve는 기존 APPROVE 파이프라인 타입으로 실행 (별도 파이프라인)
4. approve 완료 후 swap 파이프라인 실행

AllowanceHolder vs Permit2 비교 테이블:
| 항목 | AllowanceHolder (채택) | Permit2 (미채택) |
|------|----------------------|-----------------|
| 서명 | 1회 (트랜잭션) | 2회 (EIP-712 + 트랜잭션) |
| 가스 | 낮음 | 높음 |
| 복잡도 | 낮음 (standard approve) | 높음 (EIP-712 서명 + 시그니처 조합) |
| 적합 환경 | 서버사이드 (WAIaaS) | 브라우저 지갑 |

policy 평가: approve 트랜잭션은 $0 지출로 평가 (승인은 지출이 아님). swap 금액만 SPENDING_LIMIT 대상.

**Part B: m28-02 objective 파일 업데이트**
internal/objectives/m28-02-0x-evm-swap.md 파일을 수정한다:
1. "Permit2" 언급을 "AllowanceHolder"로 변경
2. API 엔드포인트를 /swap/permit2/* 에서 /swap/allowance-holder/* 로 변경
3. EIP-712 서명 관련 내용 제거
4. 기술 결정 #1을 "Swap API v2 (AllowanceHolder 기반)"으로 변경
5. 기술 결정 #4를 "AllowanceHolder approve 처리"로 변경 -- standard ERC-20 approve + swap 순차 실행
6. 파일/모듈 구조에서 permit2.ts -> allowance-holder.ts 변경
7. E2E 검증 시나리오에서 Permit2 관련 항목을 AllowanceHolder로 수정
8. 변경 근거를 추가: "Research에서 AllowanceHolder가 서버사이드 환경에 적합하다고 확인됨 (0x 공식 권장)"
  </action>
  <verify>
1. m28-00 문서에서 "DEFI-02" 키워드 검색 시 확정 설계 섹션이 존재
2. m28-00 문서에 ActionApiClient, ContractCallRequest 매핑, 에러 코드 테이블, SlippageHelper, AllowanceHolder 플로우가 모두 포함
3. m28-02 문서에서 "Permit2" 키워드가 비교 테이블 이외에는 없고, "AllowanceHolder"가 주요 키워드로 사용됨
grep "Permit2" internal/objectives/m28-02-0x-evm-swap.md | wc -l 로 비교 테이블 항목만 남았는지 확인
grep "AllowanceHolder" internal/objectives/m28-02-0x-evm-swap.md | wc -l 로 업데이트 확인
  </verify>
  <done>
APIC-01~05 요구사항 모두 충족: ActionApiClient 베이스 패턴 확정, Solana/EVM ContractCallRequest 매핑 완성, DeFi 에러 코드 8개 추가 설계, 슬리피지 branded types + 프로바이더별 단위 매핑, AllowanceHolder 플로우 확정. m28-02 objective가 AllowanceHolder 기반으로 업데이트됨.
  </done>
</task>

</tasks>

<verification>
1. m28-00 설계 문서에 DEFI-01/DEFI-02 확정 섹션이 존재하고, "설계 범위"/"설계 산출물"이 "확정 설계"로 전환됨
2. packages/actions/ 디렉토리 트리가 5개 프로바이더 모두 포함
3. registerBuiltInProviders() 라이프사이클이 6단계로 문서화
4. config.toml 패턴이 5개 프로바이더 전체에 대해 일관된 구조
5. Admin Settings 경계가 config.toml vs Admin Settings 매트릭스로 명시
6. ActionApiClient의 get/post 시그니처와 타임아웃/Zod 검증이 코드 수준으로 확정
7. ContractCallRequest 변환 매핑이 Solana/EVM 양쪽에 대해 필드 수준으로 정의
8. 에러 코드 8개가 HTTP 상태코드와 사용처와 함께 테이블로 확정
9. 슬리피지 branded types, clamp 함수, 프로바이더별 단위 매핑 테이블이 확정
10. m28-02 objective에서 Permit2 -> AllowanceHolder 업데이트 완료
</verification>

<success_criteria>
- m28-00 DEFI-01/DEFI-02 설계가 확정되어, 구현 시 코드 레벨 참조로 즉시 사용 가능
- m28-02 objective가 AllowanceHolder 기반으로 수정되어 Research 결정과 일치
- 설계 문서만 보고 m28-01에서 packages/actions/ 패키지를 생성하고, ActionApiClient를 구현하고, 슬리피지 제어를 적용할 수 있다
</success_criteria>

<output>
After completion, create `.planning/phases/244-core-design-foundation/244-01-SUMMARY.md`
</output>
