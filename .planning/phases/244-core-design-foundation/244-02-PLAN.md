---
phase: 244-core-design-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/objectives/m28-00-defi-basic-protocol-design.md
autonomous: true
requirements:
  - PLCY-01
  - PLCY-02
  - PLCY-03
  - PLCY-04

must_haves:
  truths:
    - "ActionProvider -> 정책 평가 연동 플로우가 다이어그램으로 정의되어 resolve() 결과가 파이프라인 Stage 2에 도달하는 경로가 명확하다"
    - "4개 프로토콜의 CONTRACT_WHITELIST 등록 대상 주소가 확정되어 구현 시 화이트리스트 설정을 바로 생성할 수 있다"
    - "크로스체인 정책 평가 규칙이 확정되어 출발 체인 월렛의 정책으로 평가하는 방식이 명문화된다"
    - "도착 주소 변조 방지 검증 설계가 완료되어 목적지 주소 변조 공격을 방지할 수 있다"
  artifacts:
    - path: "internal/objectives/m28-00-defi-basic-protocol-design.md"
      provides: "DEFI-03 정책 연동 설계 확정"
      contains: "DEFI-03"
  key_links:
    - from: "DEFI-03 정책 연동 플로우"
      to: "파이프라인 Stage 2 (policy)"
      via: "resolve() -> ContractCallRequest -> Stage 1 -> Stage 2 Policy 경로"
      pattern: "CONTRACT_WHITELIST"
    - from: "DEFI-03 크로스체인 정책"
      to: "LI.FI toAddress 검증"
      via: "도착 주소를 LI.FI quote에서 추출하여 월렛 레지스트리와 대조"
      pattern: "destination.*address.*validation"
---

<objective>
m28-00 설계 문서에 DEFI-03(정책 연동 설계)를 확정하여 작성한다. ActionProvider에서 기존 정책 엔진으로 이어지는 플로우 다이어그램, 프로토콜별 CONTRACT_WHITELIST 주소 목록, 크로스체인 정책 규칙, 도착 주소 변조 방지를 설계한다.

Purpose: 4개 DeFi 프로토콜 구현 시 정책 연동에 대한 설계 불확실성을 제거하여, 프로바이더 간 일관된 정책 적용이 가능하게 한다.
Output: m28-00 objective 파일에 DEFI-03 정책 연동 설계 확정 섹션
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/m28-defi-SUMMARY.md
@.planning/research/m28-defi-ARCHITECTURE.md
@.planning/research/m28-defi-protocol-PITFALLS.md
@.planning/research/m28-defi-FEATURES.md
@internal/objectives/m28-00-defi-basic-protocol-design.md
@internal/objectives/m28-03-lifi-crosschain-bridge.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 정책 연동 플로우 다이어그램 + CONTRACT_WHITELIST 확정</name>
  <files>internal/objectives/m28-00-defi-basic-protocol-design.md</files>
  <action>
m28-00 설계 문서의 "3. 정책 연동 설계" 섹션을 아래 내용으로 확정하여 교체한다. 기존 "설계 범위"/"설계 산출물" 구조를 **확정 설계**로 전환.

**PLCY-01: ActionProvider -> 정책 평가 연동 플로우 확정**

아스키 아트 다이어그램으로 전체 플로우를 작성한다:

```
AI Agent / MCP / SDK
        |
        v
POST /v1/actions/:provider/:action { params, walletId }
        |
        v
ActionProviderRegistry.executeResolve(actionKey, params, context)
  1. inputSchema.parse(params) -- 입력 검증
  2. provider.resolve(actionName, params, context)
     - 외부 API 호출 또는 ABI 인코딩
     - Settings snapshot 획득 (resolve 시작 시점)
     -> 반환: ContractCallRequest
  3. ContractCallRequestSchema.parse(result) -- 반환값 재검증
        |
        v
executeSend(walletId, contractCallRequest) -- 기존 파이프라인 진입
        |
        v
Stage 1: Validate + DB INSERT (PENDING)
  - ContractCallRequest 필드 검증
  - transactions 테이블에 INSERT
        |
        v
Stage 2: Auth (sessionId 검증)
        |
        v
Stage 3: Policy Evaluation ← ★ 정책 평가 핵심 지점
  3a. CONTRACT_WHITELIST 검사
      - contractCallRequest.to 주소가 화이트리스트에 등록되었는지 확인
      - 미등록 시: POLICY_VIOLATION (CONTRACT_WHITELIST) 에러 → 즉시 거부
  3b. SPENDING_LIMIT 평가
      - 금액 추출: value (native) 또는 amount (token)
      - IPriceOracle로 USD 환산
      - 기존 4-tier 평가: ALLOW / DELAY / APPROVAL / DENY
  3c. APPROVED_SPENDERS 검사 (0x AllowanceHolder approve 시)
      - approve 대상이 APPROVED_SPENDERS에 등록되었는지 확인
        |
        v
[Stage 3.5: Gas Condition -- m28-05에서 추가]
        |
        v
Stage 4: Wait (DELAY/APPROVAL tier에 따라 대기/승인)
        |
        v
Stage 5: Execute (build -> simulate -> sign -> submit)
        |
        v
Stage 6: Confirm (waitForConfirmation)
```

핵심 원칙:
1. resolve()는 순수 함수 -- 정책 평가는 파이프라인 Stage 3에서만 수행
2. 정책 평가 시점에서 ContractCallRequest의 `to` 주소가 CONTRACT_WHITELIST에 등록되어야 함
3. SPENDING_LIMIT은 resolve()가 반환한 금액(value/amount)을 기준으로 평가
4. approve 트랜잭션은 $0 지출로 평가 (승인 ≠ 지출)
5. Settings snapshot은 resolve() 진입 시 획득, 파이프라인 완료까지 유지

**PLCY-02: 4개 프로토콜의 CONTRACT_WHITELIST 등록 대상 확정**

아래 주소 목록을 프로토콜별로 확정한다. 구현 시 이 목록을 참조하여 화이트리스트 번들을 자동 생성한다.

| 프로토콜 | 체인 | 주소 | 설명 | 화이트리스트 번들 이름 |
|---------|------|------|------|----------------------|
| Jupiter | Solana | JUP6LkbZbjS1jKKwapdHNy74zcZ3tLUZoi5QNyVTaV4 | Jupiter Aggregator v6 | jupiter_swap |
| 0x | All EVM | (quote.to에서 동적 획득) | 0x Settlement / ExchangeProxy | 0x_swap |
| 0x | All EVM | AllowanceHolder 주소 (0x API response에서 획득) | AllowanceHolder approve 대상 | 0x_swap |
| LI.FI | EVM | (quote.transactionRequest.to에서 동적 획득) | LI.FI diamond proxy | lifi |
| LI.FI | Solana | (quote response에서 동적 획득) | Bridge program | lifi |
| Lido | Ethereum | 0xae7ab96520DE3A18E5e111B5EaAb095312D7fE84 | stETH / Lido contract | lido |
| Lido | Ethereum | 0x889edC2eDab5f40e902b864aD4d7AdE8E412F9B1 | WithdrawalQueueERC721 | lido |
| Jito | Solana | SPoo1Ku8WFXoNDMHPsrGSTSG1Y47rzgn41SLUNakuHy | SPL Stake Pool program | jito |

정적 주소 (Lido, Jito, Jupiter): 프로바이더의 config.ts에 하드코딩. 프로바이더 등록 시 화이트리스트 번들로 제공.
동적 주소 (0x, LI.FI): quote response의 `to` 필드에서 획득. 프로바이더가 resolve()에서 반환한 ContractCallRequest.to가 화이트리스트에 등록되어야 함.

**프로바이더 화이트리스트 번들 설계:**
각 ActionProvider에 `getRequiredContracts(chain): ContractAddress[]` 메서드 추가.
프로바이더 활성화 시 Admin UI에서 "이 프로바이더가 필요한 컨트랙트 주소를 화이트리스트에 추가하시겠습니까?" 안내.
에러 메시지 개선: "CONTRACT_WHITELIST violation for 0xae75..." 대신 "Lido stETH contract가 화이트리스트에 미등록. Admin > Policies에서 Lido 번들을 활성화하세요" 형태.

Pitfall P13 (CONTRACT_WHITELIST 파편화) 방지: 프로바이더별 번들로 묶어 한 번에 등록/해제.
  </action>
  <verify>
m28-00 문서에서 "PLCY-01" 검색 시 플로우 다이어그램이 존재하고, "PLCY-02" 검색 시 CONTRACT_WHITELIST 주소 테이블이 존재하는지 확인.
grep "PLCY-01\|PLCY-02\|CONTRACT_WHITELIST" internal/objectives/m28-00-defi-basic-protocol-design.md | wc -l 로 확인.
  </verify>
  <done>
PLCY-01 충족: resolve() -> Stage 2 경로가 아스키 아트 다이어그램과 5개 핵심 원칙으로 확정됨.
PLCY-02 충족: 4개 프로토콜 8개 주소의 CONTRACT_WHITELIST 등록 대상이 테이블로 확정되고, 프로바이더 화이트리스트 번들 설계 포함.
  </done>
</task>

<task type="auto">
  <name>Task 2: 크로스체인 정책 평가 규칙 + 도착 주소 변조 방지 설계</name>
  <files>internal/objectives/m28-00-defi-basic-protocol-design.md</files>
  <action>
m28-00 설계 문서의 "3. 정책 연동 설계" 섹션에 크로스체인 정책 서브섹션을 추가한다.

**PLCY-03: 크로스체인 정책 평가 규칙 확정**

크로스체인 브릿지(LI.FI)에서의 정책 평가 규칙:

1. **출발 체인 정책으로 평가** -- 자금이 나가는 쪽의 월렛 정책을 적용
   - walletId = 출발 체인의 월렛 ID
   - CONTRACT_WHITELIST: 출발 체인의 브릿지 컨트랙트가 등록되어야 함
   - SPENDING_LIMIT: 브릿지 금액(fromAmount)을 USD 환산하여 평가

2. **도착 체인은 수신이므로 정책 미적용** -- 도착 체인에서 자산이 수신되는 것은 incoming TX와 동일
   - 도착 체인 월렛의 SPENDING_LIMIT은 소비하지 않음
   - 다만 도착 주소 검증은 별도 수행 (PLCY-04)

3. **cross-chain swap의 정책 평가**
   - "SOL -> Base USDC" 같은 크로스체인 스왑도 출발 체인(Solana) 정책으로 평가
   - 도착 체인(Base)에서의 DEX 스왑은 LI.FI가 내부적으로 수행 (WAIaaS 정책 범위 밖)

4. **SPENDING_LIMIT 예약 유지 규칙**
   - 브릿지 제출 시 SPENDING_LIMIT 예약 생성
   - bridge_status가 COMPLETED 또는 REFUNDED가 될 때까지 예약 유지
   - TIMEOUT/BRIDGE_MONITORING 상태에서는 예약 해제하지 않음 (자금이 limbo에 있을 수 있음)
   - Pitfall P4 방지: 예약 조기 해제 방지로 과지출 차단

플로우 다이어그램:
```
크로스체인 브릿지 플로우 (정책 관점):

출발 체인 월렛 (Solana)
  |
  v
resolve('cross_swap', { fromChain: 'solana', toChain: 'base', ... })
  |                     ← 출발 체인 context (walletId, policies)
  v
ContractCallRequest (출발 체인 트랜잭션)
  |
  v
Stage 3: Policy (출발 체인 월렛의 정책)
  - CONTRACT_WHITELIST: LI.FI Solana bridge program ✓
  - SPENDING_LIMIT: fromAmount USD 환산 → 4-tier 평가
  - toAddress 검증 (PLCY-04) ← 추가 검증
  |
  v
Stage 4-6: 실행 (출발 체인 트랜잭션 전송)
  |
  v
BridgeStatusWorker: /status 폴링
  → COMPLETED: SPENDING_LIMIT 예약 해제
  → FAILED: SPENDING_LIMIT 예약 해제 + 알림
  → TIMEOUT: 예약 유지 + BRIDGE_MONITORING 전환
```

**PLCY-04: 도착 주소 변조 방지 검증 설계**

Pitfall P7 (크로스체인 도착 주소 정책 바이패스) 방지를 위한 3단계 검증:

**위협 모델:**
AI 에이전트가 브릿지 요청 시 도착 주소를 공격자 주소로 지정할 수 있음.
정책 엔진은 출발 체인 트랜잭션만 평가하므로, 도착 주소가 브릿지 calldata 내부에 숨겨져 있어 정책 평가에서 보이지 않음.

**3단계 도착 주소 검증:**

1단계 - LI.FI quote에서 toAddress 추출 및 검증:
```
LiFiActionProvider.resolve() 내부:
  const quote = await lifiClient.getQuote({ ..., fromAddress: ctx.walletAddress });

  // toAddress 추출
  const toAddress = quote.action.toAddress;

  // 검증 1: toAddress가 반드시 존재
  if (!toAddress) throw new WAIaaSError('BRIDGE_MISSING_DESTINATION');

  // 검증 2: toAddress가 동일 Owner의 월렛인지 확인
  const isOwnWallet = await walletService.isOwnedBySameOwner(
    ctx.walletId,  // 출발 월렛
    toAddress,      // 도착 주소
    quote.action.toChainId  // 도착 체인
  );
```

2단계 - 정책 기반 분기:
```
if (isOwnWallet) {
  // 자기 월렛으로 브릿지 → 자동 허용
  // 이것이 기본 동작 (self-bridge)
} else {
  // 외부 주소로 브릿지 → APPROVAL 필요
  // resolve() 결과에 requiresApproval: true 플래그 설정
  // Stage 4에서 Owner 승인 대기
}
```

3단계 - calldata 내 도착 주소 일치 검증:
```
// resolve()가 반환한 ContractCallRequest의 metadata에 toAddress 저장
// Stage 5 (execute) 전에 calldata 내 도착 주소와 metadata.toAddress 일치 확인
// (선택적 심층 검증 -- 구현 복잡도에 따라 Phase 2에서 결정)
```

**기본 정책: self-bridge only**
- 기본적으로 도착 주소 = 동일 Owner의 도착 체인 월렛 주소로 자동 설정
- LI.FI /quote 호출 시 fromAddress와 동일 Owner의 도착 체인 월렛을 toAddress로 지정
- 에이전트가 명시적으로 다른 주소를 지정할 경우 APPROVAL 티어로 격상
- Admin Settings에서 "allow_external_bridge_destination" (기본: false) 설정 가능

**에러 메시지 설계:**
- BRIDGE_DESTINATION_NOT_OWNED: "도착 주소가 이 Owner의 월렛이 아닙니다. Owner 승인이 필요합니다."
- BRIDGE_DESTINATION_UNKNOWN: "도착 주소를 확인할 수 없습니다. self-bridge를 사용하세요."
  </action>
  <verify>
m28-00 문서에서 "PLCY-03" 검색 시 크로스체인 정책 규칙이 존재하고, "PLCY-04" 검색 시 도착 주소 검증 3단계가 존재하는지 확인.
grep "PLCY-03\|PLCY-04\|toAddress\|self-bridge" internal/objectives/m28-00-defi-basic-protocol-design.md | wc -l 로 확인.
  </verify>
  <done>
PLCY-03 충족: 크로스체인 정책 평가 규칙이 4개 항목 + 플로우 다이어그램 + SPENDING_LIMIT 예약 유지 규칙으로 확정됨. "출발 체인 월렛의 정책으로 평가" 명문화.
PLCY-04 충족: 3단계 도착 주소 검증 설계 완료 (toAddress 추출, 정책 분기, calldata 일치 검증). self-bridge 기본 정책 + 외부 주소 APPROVAL 격상 규칙 확정. Pitfall P7 방지.
  </done>
</task>

</tasks>

<verification>
1. m28-00 설계 문서에 DEFI-03 확정 섹션이 존재하고 "설계 범위"/"설계 산출물"이 "확정 설계"로 전환됨
2. ActionProvider -> 파이프라인 Stage 3 연동 플로우가 아스키 아트 다이어그램으로 문서화
3. resolve() 순수성, Settings snapshot, approve $0 평가 등 5개 핵심 원칙이 명시
4. CONTRACT_WHITELIST 등록 대상이 4개 프로토콜 8개 주소로 테이블 확정
5. 프로바이더 화이트리스트 번들 설계가 getRequiredContracts() 메서드와 함께 문서화
6. 크로스체인 정책: "출발 체인 월렛 정책으로 평가" 규칙이 4개 항목 + 다이어그램으로 확정
7. SPENDING_LIMIT 예약 유지 규칙이 bridge_status 상태별로 명시 (COMPLETED/REFUNDED에서만 해제)
8. 도착 주소 변조 방지: 3단계 검증 + self-bridge 기본 정책 + APPROVAL 격상 규칙 확정
9. 위협 모델이 명시되고 BRIDGE_DESTINATION_NOT_OWNED 등 에러 코드가 정의
</verification>

<success_criteria>
- m28-00 DEFI-03 정책 연동 설계가 확정되어, 4개 프로토콜 구현 시 정책 관련 설계 판단이 불필요
- CONTRACT_WHITELIST 주소 목록이 확정되어 m28-01~04 구현 시 즉시 화이트리스트 번들 생성 가능
- 크로스체인 정책 규칙이 명문화되어 m28-03 LI.FI 브릿지 구현 시 정책 연동 모호함 없음
- 도착 주소 변조 방지 3단계 검증이 확정되어 Pitfall P7이 설계 수준에서 해소
</success_criteria>

<output>
After completion, create `.planning/phases/244-core-design-foundation/244-02-SUMMARY.md`
</output>
