---
phase: 02-xrpldexprovider-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/xrpl-dex/schemas.ts
  - packages/actions/src/providers/xrpl-dex/offer-builder.ts
  - packages/actions/src/providers/xrpl-dex/orderbook-client.ts
  - packages/actions/src/providers/xrpl-dex/__tests__/offer-builder.test.ts
  - packages/actions/src/providers/xrpl-dex/__tests__/orderbook-client.test.ts
autonomous: true
requirements: [DEX-06, DEX-08]

must_haves:
  truths:
    - "XRP drops, IOU object, XRP-IOU/IOU-XRP/IOU-IOU 3가지 조합의 금액이 올바른 XRPL Amount 포맷으로 변환된다"
    - "Offer 생성 전 계정 reserve 충족 여부를 사전 검증하여 부족 시 명확한 에러를 반환한다"
    - "오더북 호가 조회와 활성 주문 조회가 xrpl.Client RPC를 통해 수행된다"
  artifacts:
    - path: "packages/actions/src/providers/xrpl-dex/schemas.ts"
      provides: "Zod input schemas for 5 actions (swap, limit_order, cancel_order, get_orderbook, get_offers)"
      exports: ["SwapInputSchema", "LimitOrderInputSchema", "CancelOrderInputSchema", "GetOrderbookInputSchema", "GetOffersInputSchema"]
    - path: "packages/actions/src/providers/xrpl-dex/offer-builder.ts"
      provides: "OfferCreate/OfferCancel param builders + currency amount conversion + reserve validation"
      exports: ["buildSwapParams", "buildLimitOrderParams", "buildCancelParams", "formatXrplAmount", "validateReserve"]
    - path: "packages/actions/src/providers/xrpl-dex/orderbook-client.ts"
      provides: "book_offers/account_offers RPC wrapper with connection lifecycle"
      exports: ["XrplOrderbookClient"]
  key_links:
    - from: "packages/actions/src/providers/xrpl-dex/offer-builder.ts"
      to: "@waiaas/adapter-ripple currency-utils"
      via: "import smallestUnitToIou, iouToSmallestUnit, IOU_DECIMALS, parseTrustLineToken"
      pattern: "smallestUnitToIou|iouToSmallestUnit"
    - from: "packages/actions/src/providers/xrpl-dex/orderbook-client.ts"
      to: "xrpl.Client"
      via: "client.request({ command: 'book_offers' | 'account_offers' })"
      pattern: "client\\.request"
---

<objective>
Build the foundational modules for XrplDexProvider: Zod input schemas, OfferBuilder (currency amount conversion + calldata JSON construction + reserve validation), and XrplOrderbookClient (book_offers/account_offers RPC wrapper).

Purpose: These three modules are consumed by Plans 02 and 03. Establishing them first prevents the "scavenger hunt" anti-pattern where executors must explore codebase to understand contracts.
Output: 3 new source files + 2 test files in packages/actions/src/providers/xrpl-dex/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

@packages/actions/src/providers/jupiter-swap/schemas.ts (reference: Zod schema pattern)
@packages/actions/src/common/slippage.ts (reference: slippage utilities)
@packages/adapters/ripple/src/currency-utils.ts (reference: IOU_DECIMALS, parseTrustLineToken, smallestUnitToIou, iouToSmallestUnit)

<interfaces>
<!-- From @waiaas/adapter-ripple currency-utils.ts -->
export const IOU_DECIMALS = 15;
export function parseTrustLineToken(tokenAddress: string): { currency: string; issuer: string };
export function iouToSmallestUnit(value: string, decimals: number): bigint;
export function smallestUnitToIou(amount: bigint, decimals: number): string;

<!-- From xrpl types (verified in xrpl@4.6.0) -->
type Amount = IssuedCurrencyAmount | string;
interface IssuedCurrencyAmount { currency: string; issuer: string; value: string; }
interface BookOfferCurrency { currency: string; issuer?: string; }

<!-- From @waiaas/core action-provider.types.ts -->
export interface ApiDirectResult {
  __apiDirect: true;
  externalId: string;
  status: string;
  provider: string;
  action: string;
  data: Record<string, unknown>;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod input schemas for 5 DEX actions</name>
  <files>packages/actions/src/providers/xrpl-dex/schemas.ts</files>
  <behavior>
    - SwapInputSchema: takerGets (string, "XRP" or "USD.rIssuer..."), takerGetsAmount (string, required), takerPays (string), takerPaysAmount (string), slippageBps (number, optional, 1-500 range)
    - LimitOrderInputSchema: takerGets (string), takerGetsAmount (string), takerPays (string), takerPaysAmount (string), expirationSeconds (number, optional, default 86400)
    - CancelOrderInputSchema: offerSequence (number, positive integer)
    - GetOrderbookInputSchema: base (string, e.g. "XRP"), counter (string, e.g. "USD.rIssuer"), limit (number, optional, 1-400, default 20)
    - GetOffersInputSchema: empty or limit (number, optional, 1-400, default 50)
    - Token format: "XRP" for native, "CURRENCY.rISSUER" (dot-separated) for IOU (reuse parseTrustLineToken format from adapter-ripple)
    - Invalid inputs rejected with Zod errors
  </behavior>
  <action>
    Create `packages/actions/src/providers/xrpl-dex/schemas.ts` with 5 Zod schemas. Token format uses dot separator: "XRP" for native, "{currency}.{issuer}" for IOU (matches existing parseTrustLineToken convention). Export all schemas and their inferred types.

    slippageBps for swap: default 50 (0.5%), max 500 (5%). Validate integer and range in schema.
    expirationSeconds for limit_order: default 86400 (24h), min 60, max 2592000 (30 days).
    offerSequence: z.number().int().positive().
    limit for orderbook/offers: z.number().int().min(1).max(400).default(20 or 50).

    No test file needed for pure schema validation -- Zod schemas are self-validating.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit --project packages/actions/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>5 Zod schemas exported with TypeScript types, type-check passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OfferBuilder -- currency amount conversion + calldata JSON + reserve validation</name>
  <files>packages/actions/src/providers/xrpl-dex/offer-builder.ts, packages/actions/src/providers/xrpl-dex/__tests__/offer-builder.test.ts</files>
  <behavior>
    - formatXrplAmount("XRP", "1000000") returns "1000000" (string drops)
    - formatXrplAmount("USD.rIssuer", "100") returns { currency: "USD", issuer: "rIssuer", value: "100" }
    - formatXrplAmount("XRP", "0") throws error (zero amount)
    - buildSwapParams(takerGets, takerGetsAmt, takerPays, takerPaysAmt, slippageBps) returns calldata JSON with xrplTxType='OfferCreate', Flags=tfImmediateOrCancel(0x00020000), TakerGets/TakerPays in correct XRPL format
    - buildSwapParams applies slippage to TakerPays (reduce expected receive by slippageBps)
    - buildLimitOrderParams returns calldata JSON without tfImmediateOrCancel, with Expiration in Ripple epoch
    - buildCancelParams(offerSequence) returns calldata JSON with xrplTxType='OfferCancel', OfferSequence
    - validateReserve(availableBalance, existingOfferCount) throws if balance < ownerReserve (200000 drops) for new offer
    - XRP-IOU, IOU-XRP, IOU-IOU 3 combinations all produce valid XRPL Amount formats
  </behavior>
  <action>
    Create `packages/actions/src/providers/xrpl-dex/offer-builder.ts`:

    1. `formatXrplAmount(token: string, amount: string)` -- if token is "XRP", return amount as drops string. If token is "CURRENCY.ISSUER", split by dot and return IssuedCurrencyAmount object { currency, issuer, value: amount }. Throw on zero or negative.

    2. `parseTokenToBookOfferCurrency(token: string)` -- returns { currency: "XRP" } or { currency, issuer } for book_offers RPC query.

    3. `buildSwapParams(input)` returns `{ xrplTxType: 'OfferCreate', TakerGets, TakerPays, Flags: 0x00020000 }`. Slippage applied to TakerPays: if buying IOU, reduce TakerPays value by slippageBps (what you're willing to pay can be up to slippage% more). Actually for IoC swaps: TakerGets is what you offer (fixed), TakerPays is what you want (minimum). Slippage reduces TakerPays (accept getting less).

    4. `buildLimitOrderParams(input)` returns calldata JSON with no tfImmediateOrCancel, optional Expiration converted to Ripple epoch (unix seconds - 946684800).

    5. `buildCancelParams(offerSequence: number)` returns `{ xrplTxType: 'OfferCancel', OfferSequence }`.

    6. `validateReserve(availableXrpDrops: string, existingOfferCount: number)` -- checks if available XRP >= ownerReserve (200000 drops per object). Throws ChainError if insufficient.

    RIPPLE_EPOCH constant = 946684800 (2000-01-01 UTC unix seconds).

    Create test file with cases for all 3 currency pair combinations, slippage math, Ripple epoch conversion, reserve edge cases.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx vitest run packages/actions/src/providers/xrpl-dex/__tests__/offer-builder.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>formatXrplAmount handles XRP/IOU correctly, buildSwapParams applies slippage and tfImmediateOrCancel, buildLimitOrderParams sets Ripple epoch expiration, validateReserve rejects insufficient balance, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: XrplOrderbookClient -- book_offers/account_offers RPC wrapper</name>
  <files>packages/actions/src/providers/xrpl-dex/orderbook-client.ts, packages/actions/src/providers/xrpl-dex/__tests__/orderbook-client.test.ts</files>
  <behavior>
    - constructor(rpcUrl: string) stores URL, does not connect immediately
    - connect() establishes xrpl.Client WebSocket connection
    - disconnect() closes connection
    - getOrderbook(base, counter, limit) calls book_offers for both sides (asks + bids) and returns normalized { asks: [...], bids: [...], spread }
    - asks = book_offers where taker_gets=base, taker_pays=counter (offers selling base for counter)
    - bids = book_offers where taker_gets=counter, taker_pays=base (offers buying base with counter)
    - Each offer includes: price (counter/base), amount (base units), total (counter units), owner_funds (funded amount), sequence
    - getAccountOffers(account, limit) calls account_offers and returns normalized list with seq, takerGets, takerPays, flags
    - checkTrustLine(account, currency, issuer) calls account_lines and returns boolean
    - getAccountReserve(account) calls account_info and returns { balance, ownerCount, baseReserve, ownerReserve, availableBalance }
  </behavior>
  <action>
    Create `packages/actions/src/providers/xrpl-dex/orderbook-client.ts`:

    Class `XrplOrderbookClient` with:
    - `private client: Client | null = null` (xrpl.Client, lazy connect)
    - `constructor(private readonly rpcUrl: string)`
    - `async ensureConnected()` -- creates and connects client if not connected
    - `async disconnect()` -- closes client
    - `async getOrderbook(base: BookOfferCurrency, counter: BookOfferCurrency, limit: number)` -- two book_offers calls (asks side: taker_gets=base, taker_pays=counter; bids side: taker_gets=counter, taker_pays=base). Normalize each offer to `{ price, amount, total, ownerFunds, sequence }`. Calculate spread from best ask/bid.
    - `async getAccountOffers(account: string, limit: number)` -- account_offers RPC, return array of `{ seq, takerGets, takerPays, flags, expiration? }`.
    - `async checkTrustLine(account: string, currency: string, issuer: string)` -- account_lines RPC filtered by peer=issuer, check if matching currency exists. Return boolean.
    - `async getAccountReserve(account: string)` -- account_info RPC, extract Balance, OwnerCount. Use server_info for reserve values (base=1000000, owner=200000 drops). Return `{ balance, ownerCount, baseReserve, ownerReserve, availableBalance }`.

    Normalize XRPL Amount to numeric: XRP drops -> number (drops/1000000), IOU -> parseFloat(value).

    Create test file that mocks xrpl.Client.request() for each RPC method. Test funded amounts (owner_funds, taker_gets_funded), trust line presence/absence, reserve calculation.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx vitest run packages/actions/src/providers/xrpl-dex/__tests__/orderbook-client.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>XrplOrderbookClient correctly queries book_offers (asks+bids), account_offers, account_lines (trust line check), account_info (reserve), all tests pass with mocked xrpl.Client</done>
</task>

</tasks>

<verification>
All 3 source files exist and export their contracts:
- schemas.ts: 5 Zod schemas + 5 inferred types
- offer-builder.ts: formatXrplAmount, buildSwapParams, buildLimitOrderParams, buildCancelParams, validateReserve
- orderbook-client.ts: XrplOrderbookClient class

Type check: `npx tsc --noEmit --project packages/actions/tsconfig.json` passes
Tests: `npx vitest run packages/actions/src/providers/xrpl-dex/ --reporter=verbose` all pass
</verification>

<success_criteria>
1. 3 source files + 2 test files created in packages/actions/src/providers/xrpl-dex/
2. XRP/IOU/IOU-IOU 3 combinations correctly format XRPL Amount
3. Slippage applied to swap TakerPays (IoC minimum receive)
4. Reserve validation catches insufficient balance for new offers
5. Orderbook client returns normalized asks/bids with funded amounts
6. Type check and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-xrpldexprovider-core/02-01-SUMMARY.md`
</output>
