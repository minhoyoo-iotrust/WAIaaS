---
phase: 02-xrpldexprovider-core
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/daemon/src/infrastructure/action/builtin-metadata.ts
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/actions/src/__tests__/xrpl-dex-integration.test.ts
autonomous: true
requirements: [BOOK-01, BOOK-02, BOOK-03, DEX-01, DEX-02, DEX-03, DEX-04, DEX-05, DEX-06, DEX-07, DEX-08]

must_haves:
  truths:
    - "XrplDexProvider가 builtin-metadata에 등록되어 Admin Settings에서 활성화/비활성화 가능하다"
    - "actions.xrpl_dex_enabled 및 actions.xrpl_dex_rpc_url 설정 키가 정의되어 있다"
    - "통합 테스트가 전체 resolve -> ContractCallRequest/ApiDirectResult 흐름을 검증한다"
  artifacts:
    - path: "packages/daemon/src/infrastructure/action/builtin-metadata.ts"
      provides: "xrpl_dex provider metadata for Admin Settings display"
      contains: "xrpl_dex"
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "actions.xrpl_dex_enabled and actions.xrpl_dex_rpc_url setting keys"
      contains: "xrpl_dex_enabled"
    - path: "packages/actions/src/__tests__/xrpl-dex-integration.test.ts"
      provides: "Integration tests covering all 5 actions end-to-end"
      min_lines: 80
  key_links:
    - from: "packages/daemon/src/infrastructure/action/builtin-metadata.ts"
      to: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      via: "enabledKey references setting key"
      pattern: "xrpl_dex_enabled"
    - from: "packages/actions/src/__tests__/xrpl-dex-integration.test.ts"
      to: "packages/actions/src/providers/xrpl-dex/index.ts"
      via: "import XrplDexProvider, test resolve()"
      pattern: "XrplDexProvider"
---

<objective>
Register XrplDexProvider in daemon infrastructure (builtin-metadata, setting keys) and create integration tests validating all 5 actions produce correct output types with proper calldata structure.

Purpose: Ensures the provider is discoverable by Admin Settings and MCP auto-exposure, and validates the full resolve -> ContractCallRequest/ApiDirectResult contract.
Output: Updated daemon infrastructure files + integration test suite
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-xrpldexprovider-core/02-01-SUMMARY.md
@.planning/phases/02-xrpldexprovider-core/02-02-SUMMARY.md

@packages/daemon/src/infrastructure/action/builtin-metadata.ts (reference: existing metadata entries)
@packages/daemon/src/infrastructure/settings/setting-keys.ts (reference: existing setting keys)

<interfaces>
<!-- From Plan 02 XrplDexProvider -->
export class XrplDexProvider implements IActionProvider {
  readonly metadata: ActionProviderMetadata; // name='xrpl_dex', chains=['ripple'], mcpExpose=true
  readonly actions: readonly ActionDefinition[]; // swap, limit_order, cancel_order, get_orderbook, get_offers
  constructor(orderbookClient: XrplOrderbookClient);
  resolve(actionName: string, params: Record<string, unknown>, context: ActionContext): Promise<ContractCallRequest | ApiDirectResult | ContractCallRequest[]>;
}

<!-- From Plan 01 -->
export class XrplOrderbookClient {
  constructor(rpcUrl: string);
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register xrpl_dex in builtin-metadata and setting keys</name>
  <files>packages/daemon/src/infrastructure/action/builtin-metadata.ts, packages/daemon/src/infrastructure/settings/setting-keys.ts</files>
  <action>
    1. In `packages/daemon/src/infrastructure/action/builtin-metadata.ts`, add xrpl_dex entry following existing pattern (look at jupiter_swap or zerox_swap as reference):
    ```typescript
    {
      name: 'xrpl_dex',
      displayName: 'XRPL DEX',
      description: 'XRPL native orderbook DEX for token swaps and limit orders',
      chains: ['ripple'],
      settingKeys: {
        enabled: 'actions.xrpl_dex_enabled',
        rpcUrl: 'actions.xrpl_dex_rpc_url',
      },
    },
    ```

    2. In `packages/daemon/src/infrastructure/settings/setting-keys.ts`, add setting key definitions:
    ```typescript
    'actions.xrpl_dex_enabled': { type: 'boolean', default: 'false', description: 'Enable XRPL DEX action provider' },
    'actions.xrpl_dex_rpc_url': { type: 'string', default: 'wss://xrplcluster.com', description: 'XRPL WebSocket RPC URL for DEX orderbook queries' },
    ```

    Follow existing patterns exactly -- check the file structure and add entries in alphabetical order within the actions section.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit --project packages/daemon/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>xrpl_dex registered in builtin-metadata with displayName and setting keys, setting keys defined with defaults, type-check passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for all 5 XrplDexProvider actions</name>
  <files>packages/actions/src/__tests__/xrpl-dex-integration.test.ts</files>
  <behavior>
    - swap XRP->IOU: ContractCallRequest with type='CONTRACT_CALL', calldata contains xrplTxType='OfferCreate', Flags includes tfImmediateOrCancel (0x00020000), TakerGets is drops string, TakerPays is IOU object
    - swap IOU->IOU: ContractCallRequest with both TakerGets and TakerPays as IOU objects
    - swap with missing trust line: returns array of length 2, first element has TrustSet calldata, second has OfferCreate calldata
    - limit_order: ContractCallRequest without tfImmediateOrCancel, has Expiration field
    - cancel_order: ContractCallRequest with xrplTxType='OfferCancel' and OfferSequence
    - get_orderbook: ApiDirectResult with __apiDirect=true, data.bids and data.asks arrays, data.spread
    - get_offers: ApiDirectResult with data.offers array, each with seq field
    - registerBuiltInProviders includes xrpl_dex when enabled
    - registerBuiltInProviders skips xrpl_dex when disabled
  </behavior>
  <action>
    Create `packages/actions/src/__tests__/xrpl-dex-integration.test.ts`:

    Mock XrplOrderbookClient with:
    - checkTrustLine: returns true by default, false when testing auto-setup
    - getAccountReserve: returns sufficient reserve by default
    - getOrderbook: returns mock { bids: [...], asks: [...], spread: 0.01 }
    - getAccountOffers: returns mock offers with seq/takerGets/takerPays

    Create XrplDexProvider instance with mocked client.

    Test each action via resolve() with mock ActionContext (walletAddress, chain='ripple', networkType='xrpl-mainnet').

    For calldata assertions, JSON.parse the calldata string and verify:
    - xrplTxType field matches expected
    - TakerGets/TakerPays formats are correct for XRP (string) and IOU (object)
    - Flags include/exclude tfImmediateOrCancel as expected
    - Expiration is present for limit_order
    - OfferSequence is present for cancel_order

    For ApiDirectResult assertions:
    - __apiDirect === true
    - provider === 'xrpl_dex'
    - data contains expected fields

    For registerBuiltInProviders test:
    - Mock SettingsReader to return 'true' for actions.xrpl_dex_enabled
    - Verify xrpl_dex appears in loaded array
    - Mock SettingsReader to return 'false'
    - Verify xrpl_dex appears in skipped array
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx vitest run packages/actions/src/__tests__/xrpl-dex-integration.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Integration tests cover all 5 actions with correct ContractCallRequest/ApiDirectResult output, trust line auto-setup 2-step flow, reserve validation, registerBuiltInProviders gate, all tests pass</done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/actions/src/__tests__/xrpl-dex-integration.test.ts` -- all pass
2. `npx vitest run packages/actions/src/providers/xrpl-dex/` -- all pass
3. `npx tsc --noEmit --project packages/daemon/tsconfig.json` -- no type errors
4. `grep -c 'xrpl_dex' packages/daemon/src/infrastructure/action/builtin-metadata.ts` returns >= 1
5. `grep -c 'xrpl_dex_enabled' packages/daemon/src/infrastructure/settings/setting-keys.ts` returns >= 1
</verification>

<success_criteria>
1. xrpl_dex appears in builtin-metadata and setting keys
2. Integration tests validate all 5 actions produce correct output types
3. Trust line auto-setup flow tested (2-step array return)
4. Reserve validation tested (throws on insufficient)
5. registerBuiltInProviders gate tested (enabled/disabled)
6. All tests and type checks pass across actions and daemon packages
</success_criteria>

<output>
After completion, create `.planning/phases/02-xrpldexprovider-core/02-03-SUMMARY.md`
</output>
