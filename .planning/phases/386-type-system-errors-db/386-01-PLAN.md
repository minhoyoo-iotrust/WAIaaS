---
phase: 386-type-system-errors-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/schemas/resolved-action.schema.ts
  - packages/core/src/enums/signing-scheme.ts
  - packages/core/src/errors/error-codes.ts
  - packages/core/src/index.ts
  - packages/core/src/__tests__/resolved-action.test.ts
autonomous: true
requirements: [RTYPE-01, RTYPE-02, RTYPE-03, RTYPE-04, RTYPE-06, ERR-01, ERR-02, ERR-03]

must_haves:
  truths:
    - "ResolvedActionSchema.parse()로 contractCall/signedData/signedHttp 3종을 kind 필드로 분기할 수 있다"
    - "kind 없는 ContractCallRequest 객체를 normalizeResolvedAction()에 전달하면 kind:'contractCall'이 추가되어 정상 파싱된다"
    - "CREDENTIAL_NOT_FOUND, CREDENTIAL_EXPIRED, SIGNING_SCHEME_UNSUPPORTED, CAPABILITY_NOT_FOUND, VENUE_NOT_ALLOWED, EXTERNAL_ACTION_FAILED 6종 에러 코드가 ERROR_CODES에 등록되어 있다"
  artifacts:
    - path: "packages/core/src/schemas/resolved-action.schema.ts"
      provides: "ResolvedAction 3-kind Zod discriminatedUnion + normalize utilities"
      exports: ["ResolvedActionSchema", "SignedDataActionSchema", "SignedHttpActionSchema", "NormalizedContractCallSchema", "normalizeResolvedAction", "normalizeResolvedActions"]
    - path: "packages/core/src/enums/signing-scheme.ts"
      provides: "SigningSchemeEnum 7-value Zod enum"
      exports: ["SigningSchemeEnum", "SigningScheme", "SIGNING_SCHEMES"]
    - path: "packages/core/src/errors/error-codes.ts"
      provides: "6 new error codes in EXTERNAL_ACTION domain"
      contains: "CREDENTIAL_NOT_FOUND"
    - path: "packages/core/src/__tests__/resolved-action.test.ts"
      provides: "Unit tests for schema parsing and normalization"
      min_lines: 80
  key_links:
    - from: "packages/core/src/schemas/resolved-action.schema.ts"
      to: "packages/core/src/enums/signing-scheme.ts"
      via: "import SigningSchemeEnum"
      pattern: "import.*SigningSchemeEnum.*signing-scheme"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/schemas/resolved-action.schema.ts"
      via: "re-export"
      pattern: "export.*resolved-action"
---

<objective>
ResolvedAction 3-kind Zod discriminatedUnion 타입 시스템과 External Action 에러 코드 6종을 구현한다.

Purpose: 후속 Phase(387~392)의 모든 구현이 이 타입과 에러 코드에 의존하므로 가장 먼저 확립해야 한다.
Output: resolved-action.schema.ts, signing-scheme.ts enum, error-codes.ts 6종 추가, 단위 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/design/81-external-action-framework.md (sections D1.1~D1.9)

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/core/src/schemas/transaction.schema.ts:
```typescript
export const ContractCallRequestSchema = z.object({
  type: z.literal('CONTRACT_CALL'),
  to: z.string().min(1),
  calldata: z.string().optional(),
  abi: z.array(z.record(z.unknown())).optional(),
  value: z.string().regex(/^\d+$/).optional(),
  programId: z.string().optional(),
  instructionData: z.string().optional(),
  accounts: z.array(z.object({ pubkey: z.string(), isSigner: z.boolean(), isWritable: z.boolean() })).optional(),
  preInstructions: z.array(z.object({ programId: z.string(), data: z.string(), accounts: z.array(z.object({ pubkey: z.string(), isSigner: z.boolean(), isWritable: z.boolean() })) })).optional(),
  network: NetworkTypeEnumWithLegacy.optional(),
  actionProvider: z.string().optional(),
  actionName: z.string().optional(),
  ...gasConditionField,
});
export type ContractCallRequest = z.infer<typeof ContractCallRequestSchema>;
```

From packages/core/src/interfaces/action-provider.types.ts:
```typescript
export interface ApiDirectResult {
  readonly __apiDirect: true;
  externalId: string;
  status: 'success' | 'partial' | 'pending';
  provider: string;
  action: string;
  data: Record<string, unknown>;
  metadata?: { market?: string; side?: string; size?: string; price?: string; [key: string]: unknown };
}
export function isApiDirectResult(result: unknown): result is ApiDirectResult;
```

From packages/core/src/errors/error-codes.ts:
```typescript
export type ErrorDomain = 'AUTH' | 'SESSION' | 'TX' | 'POLICY' | 'OWNER' | 'SYSTEM' | 'WALLET' | 'WITHDRAW' | 'ACTION' | 'ADMIN' | 'X402' | 'SIGNING' | 'CHAIN' | 'ERC8128' | 'NFT' | 'USEROP';
export const ERROR_CODES = { ... } as const satisfies Record<string, ErrorCodeEntry>;
export type ErrorCode = keyof typeof ERROR_CODES;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SigningSchemeEnum + ResolvedAction Zod schemas + normalize utilities</name>
  <files>packages/core/src/enums/signing-scheme.ts, packages/core/src/schemas/resolved-action.schema.ts, packages/core/src/__tests__/resolved-action.test.ts, packages/core/src/index.ts</files>
  <behavior>
    - Test: SignedDataActionSchema.parse({ kind: 'signedData', signingScheme: 'eip712', payload: {...}, venue: 'hyperliquid', operation: 'place_order' }) succeeds
    - Test: SignedHttpActionSchema.parse({ kind: 'signedHttp', method: 'GET', url: 'https://...', headers: {}, signingScheme: 'erc8128', venue: 'external-api', operation: 'fetch' }) succeeds
    - Test: NormalizedContractCallSchema.parse({ kind: 'contractCall', type: 'CONTRACT_CALL', to: '0x...' }) succeeds
    - Test: ResolvedActionSchema.parse() correctly discriminates on kind field for all 3 variants
    - Test: normalizeResolvedAction({ type: 'CONTRACT_CALL', to: '0x...' }) returns object with kind:'contractCall'
    - Test: normalizeResolvedAction({ kind: 'signedData', ... }) passes through unchanged
    - Test: normalizeResolvedActions([...]) normalizes array items
    - Test: normalizeResolvedAction({}) throws error for missing kind and wrong type
    - Test: SigningSchemeEnum has exactly 7 values: eip712, personal, hmac-sha256, rsa-pss, ecdsa-secp256k1, ed25519, erc8128
  </behavior>
  <action>
1. Create `packages/core/src/enums/signing-scheme.ts`:
   - `SigningSchemeEnum` = z.enum(['eip712', 'personal', 'hmac-sha256', 'rsa-pss', 'ecdsa-secp256k1', 'ed25519', 'erc8128'])
   - Export `SigningScheme` type and `SIGNING_SCHEMES` readonly array (from .options)

2. Create `packages/core/src/schemas/resolved-action.schema.ts`:
   - Import SigningSchemeEnum from enums
   - Define `SignedDataActionTrackingSchema` (trackerName: string, metadata: record)
   - Define `SignedDataActionPolicyContextSchema` (actionCategory enum, notionalUsd, leverage, expiry, hasWithdrawCapability)
   - Define `SignedDataActionSchema` per doc-81 D1.3 (kind: literal 'signedData', signingScheme, payload, venue, operation, credentialRef?, tracking?, policyContext?, actionProvider?, actionName?) with .strict()
   - Define `SignedHttpActionSchema` per doc-81 D1.4 (kind: literal 'signedHttp', method, url, headers, body?, signingScheme subset, coveredComponents?, preset?, ttlSec?, nonce?, venue, operation, credentialRef?, tracking?, policyContext?, actionProvider?, actionName?) with .strict()
   - Define `NormalizedContractCallSchema` by extending ContractCallRequestSchema with kind: z.literal('contractCall') -- import from transaction.schema.ts. Note: ContractCallRequestSchema does NOT currently have kind field, so use `.extend({ kind: z.literal('contractCall') })`. Do NOT modify ContractCallRequestSchema itself (backward compat).
   - Define `ResolvedActionSchema` = z.discriminatedUnion('kind', [NormalizedContractCallSchema, SignedDataActionSchema, SignedHttpActionSchema])
   - Define `RawResolvedAction` type union
   - Implement `normalizeResolvedAction(raw)`: if has kind -> parse via ResolvedActionSchema; if has type=CONTRACT_CALL -> add kind:'contractCall' and parse; else throw
   - Implement `normalizeResolvedActions(raw | raw[])`: wrap in array and map

3. Update `packages/core/src/index.ts`:
   - Add re-export from `./enums/signing-scheme.js`
   - Add re-export from `./schemas/resolved-action.schema.js`

4. Write test file `packages/core/src/__tests__/resolved-action.test.ts` covering all behavior items.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core test -- --run resolved-action</automated>
  </verify>
  <done>ResolvedAction 3-kind discriminatedUnion이 Zod 파싱으로 올바르게 분기되고, kind 없는 ContractCallRequest가 contractCall로 정규화된다</done>
</task>

<task type="auto">
  <name>Task 2: External Action 에러 코드 6종 등록</name>
  <files>packages/core/src/errors/error-codes.ts</files>
  <action>
기존 `packages/core/src/errors/error-codes.ts`의 ERROR_CODES 객체에 새 에러 코드 6종을 추가한다. ErrorDomain 타입에 'CREDENTIAL' 도메인을 추가한다.

추가할 에러 코드:

```typescript
// --- CREDENTIAL domain (2) ---
CREDENTIAL_NOT_FOUND: {
  code: 'CREDENTIAL_NOT_FOUND',
  domain: 'CREDENTIAL',
  httpStatus: 404,
  retryable: false,
  message: 'Credential not found',
},
CREDENTIAL_EXPIRED: {
  code: 'CREDENTIAL_EXPIRED',
  domain: 'CREDENTIAL',
  httpStatus: 400,
  retryable: false,
  message: 'Credential has expired',
},

// --- ACTION domain (external action extensions) ---
SIGNING_SCHEME_UNSUPPORTED: {
  code: 'SIGNING_SCHEME_UNSUPPORTED',
  domain: 'ACTION',
  httpStatus: 400,
  retryable: false,
  message: 'Signing scheme not supported',
},
CAPABILITY_NOT_FOUND: {
  code: 'CAPABILITY_NOT_FOUND',
  domain: 'ACTION',
  httpStatus: 400,
  retryable: false,
  message: 'Signer capability not found for the requested signing scheme',
},
VENUE_NOT_ALLOWED: {
  code: 'VENUE_NOT_ALLOWED',
  domain: 'POLICY',
  httpStatus: 403,
  retryable: false,
  message: 'Venue not in allowed list',
},
EXTERNAL_ACTION_FAILED: {
  code: 'EXTERNAL_ACTION_FAILED',
  domain: 'ACTION',
  httpStatus: 500,
  retryable: true,
  message: 'External action execution failed',
},
```

ErrorDomain 타입에 `'CREDENTIAL'` 추가. 파일 상단 JSDoc 주석의 에러 코드 총 개수 업데이트 (110 -> 116).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core run typecheck</automated>
  </verify>
  <done>ERROR_CODES에 6종 에러 코드가 추가되어 WAIaaSError('CREDENTIAL_NOT_FOUND') 등으로 사용 가능하다</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/core test -- --run` 전체 통과
2. `pnpm --filter @waiaas/core run typecheck` 통과
3. ResolvedActionSchema.parse()로 3종 kind를 각각 파싱 가능
4. normalizeResolvedAction()이 kind 없는 ContractCallRequest를 contractCall로 정규화
5. ERROR_CODES에서 6종 신규 코드를 정상 참조 가능
</verification>

<success_criteria>
- ResolvedAction Zod 스키마 3종이 kind 필드로 올바르게 분기된다
- kind 없는 ContractCallRequest가 contractCall로 정규화된다
- 6종 에러 코드가 등록되어 타입 안전하게 참조 가능하다
- 모든 테스트와 타입체크가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/386-type-system-errors-db/386-01-SUMMARY.md`
</output>
