---
phase: 255-jito-solana-staking-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/jito-staking/config.ts
  - packages/actions/src/providers/jito-staking/jito-stake-pool.ts
  - packages/actions/src/providers/jito-staking/index.ts
  - packages/actions/src/__tests__/jito-staking.test.ts
autonomous: true
requirements: [JITO-01, JITO-02, JITO-03, JITO-04]

must_haves:
  truths:
    - "JitoStakingActionProvider.resolve('stake', { amount: '1.0' }, ctx) returns ContractCallRequest with SPL Stake Pool depositSol instruction"
    - "JitoStakingActionProvider.resolve('unstake', { amount: '1.0' }, ctx) returns ContractCallRequest with SPL Stake Pool withdrawSol instruction"
    - "Amount '0' throws ChainError with 'Amount must be greater than 0'"
    - "Unknown action name throws ChainError with 'Unknown action'"
  artifacts:
    - path: "packages/actions/src/providers/jito-staking/config.ts"
      provides: "JitoStakingConfig type, mainnet addresses, defaults, getJitoAddresses()"
      contains: "JITO_MAINNET_ADDRESSES"
    - path: "packages/actions/src/providers/jito-staking/jito-stake-pool.ts"
      provides: "SPL Stake Pool instruction encoding: depositSol, withdrawSol"
      exports: ["encodeDepositSolInstruction", "encodeWithdrawSolInstruction"]
    - path: "packages/actions/src/providers/jito-staking/index.ts"
      provides: "JitoStakingActionProvider implementing IActionProvider"
      exports: ["JitoStakingActionProvider"]
    - path: "packages/actions/src/__tests__/jito-staking.test.ts"
      provides: "Unit tests for provider"
      min_lines: 80
  key_links:
    - from: "packages/actions/src/providers/jito-staking/index.ts"
      to: "packages/actions/src/providers/jito-staking/jito-stake-pool.ts"
      via: "import encodeDepositSolInstruction, encodeWithdrawSolInstruction"
      pattern: "encodeDepositSol|encodeWithdrawSol"
    - from: "packages/actions/src/providers/jito-staking/index.ts"
      to: "@waiaas/core ContractCallRequest"
      via: "resolve() return type"
      pattern: "ContractCallRequest"
---

<objective>
JitoStakingActionProvider with SOL->JitoSOL stake (depositSol) and JitoSOL->SOL unstake (withdrawSol) via SPL Stake Pool manual instruction encoding.

Purpose: Enable AI agents to liquid-stake SOL via Jito protocol. This is the Solana counterpart to Phase 254's Lido EVM staking.
Output: JitoStakingActionProvider class + SPL Stake Pool instruction builders + unit tests
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/254-lido-evm-staking-provider/254-01-SUMMARY.md

# Critical pattern references -- Solana ContractCallRequest fields
@packages/actions/src/providers/jupiter-swap/index.ts
@packages/actions/src/providers/lido-staking/index.ts
@packages/actions/src/providers/lido-staking/config.ts
@packages/actions/src/providers/lido-staking/lido-contract.ts
@packages/core/src/schemas/transaction.schema.ts
@packages/core/src/interfaces/action-provider.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jito config + SPL Stake Pool instruction encoding</name>
  <files>
    packages/actions/src/providers/jito-staking/config.ts
    packages/actions/src/providers/jito-staking/jito-stake-pool.ts
  </files>
  <action>
Create `packages/actions/src/providers/jito-staking/config.ts`:

1. Define `JitoStakingConfig` interface:
   - `enabled: boolean`
   - `stakePoolAddress: string` -- Jito SPL Stake Pool (mainnet: `Jito4APyf642JPZPx3hGc6WWJ8zPKtRbRs4P815Awbb`)
   - `jitosolMint: string` -- JitoSOL token mint (mainnet: `J1toso1uCk3RLmjorhTtrVwY9HJ7X8V9yYac6Y7kGCPn`)
   - `stakePoolProgram: string` -- SPL Stake Pool program ID (mainnet: `SPoo1Ku8WFXoNDMHPsrGSTSG1Y47rzgn41SLUNakuHy`)

2. Export `JITO_MAINNET_ADDRESSES` const with mainnet addresses above.

3. Export `JITO_STAKING_DEFAULTS: JitoStakingConfig` with `enabled: false` and mainnet addresses.

4. Export `getJitoAddresses(environment: 'mainnet' | 'testnet')` helper:
   - Mainnet: return mainnet addresses
   - Testnet: return mainnet addresses too (Jito has no official devnet deployment -- mainnet only).
     Add a comment explaining this: "Jito Stake Pool is mainnet-only. Testnet falls back to mainnet addresses."

Create `packages/actions/src/providers/jito-staking/jito-stake-pool.ts`:

SPL Stake Pool uses a specific instruction layout. For Jito staking, the provider will produce a `ContractCallRequest` with Solana-specific fields (`programId`, `instructionData`, `accounts`), following the same pattern as JupiterSwapActionProvider.

**CRITICAL DESIGN DECISION:** Unlike Jupiter (which calls an API to get pre-built instructions), Jito staking builds instructions locally. The SPL Stake Pool `DepositSol` and `WithdrawSol` instructions have well-defined binary layouts.

However, building SPL Stake Pool instructions requires deriving multiple PDAs (Program Derived Addresses) which need on-chain lookups or known seeds. Since the provider's `resolve()` should be deterministic and not make RPC calls, we encode the instruction data and account metas statically using known Jito pool addresses.

**SPL Stake Pool DepositSol instruction (index 14):**
- Instruction data: 1 byte (14 = DepositSol index) + 8 bytes (amount as little-endian u64)
- Accounts (11 total): stakePool, withdrawAuthority, reserveStake, fundingAccount, destTokenAccount, managerFeeAccount, referralFeeAccount, poolMint, systemProgram, tokenProgram, solDepositAuthority (optional)

Since these accounts require PDA derivation and pool state lookups that vary at runtime, the provider should NOT try to build the full instruction locally. Instead, follow the Jupiter pattern: use the Jito staking API endpoint to get pre-built instructions.

**REVISED APPROACH:** Jito provides a staking API at `https://jito-staking-api.jito.network`. However, Jito's standard path is through their JavaScript SDK `@jito-foundation/stakenet-sdk` or direct SPL Stake Pool program interaction.

For maximum simplicity and alignment with the existing zero-dependency pattern (no viem, no external SDKs), encode a **SOL TRANSFER to the Jito Stake Pool** as a CONTRACT_CALL with Solana fields. The Jito pool has a deposit authority PDA.

**FINAL APPROACH -- Use ContractCallRequest with Solana fields:**

Create two functions that build ContractCallRequest objects (not raw instruction bytes):

1. `buildDepositSolRequest(config, amountLamports, walletAddress)`:
   Returns a `ContractCallRequest` with:
   - `type: 'CONTRACT_CALL'`
   - `to: config.stakePoolAddress` (Jito pool address)
   - `programId: config.stakePoolProgram` (SPL Stake Pool program)
   - `instructionData: base64-encoded DepositSol instruction data` (14 as u8 + amount as LE u64)
   - `accounts`: The known account list for Jito Stake Pool DepositSol. The withdraw authority PDA is derived from the stake pool address + program ID using a known seed `["withdraw", stakePoolAddress]`.

   NOTE: Since PDA derivation requires the actual `findProgramAddressSync` computation, and we want zero external dependencies, encode a simplified version: the `instructionData` and `accounts` array will be passed to SolanaAdapter which handles instruction reconstruction.

   Actually, looking at the Jupiter pattern more carefully -- Jupiter gets full instructions from its API (including programId, data as base64, and account list). The SolanaAdapter then reconstructs TransactionInstructions from these fields.

   For Jito, we need to either:
   a) Call an API to get instructions (like Jupiter does)
   b) Build instructions locally (requires PDA derivation + pool state)

   Since Jito doesn't have a simple REST API like Jupiter, and we want zero deps, use a **hybrid approach**:
   - Build the `DepositSol` instruction data bytes locally (simple: 1 byte index + 8 byte LE amount)
   - For the accounts array, use **well-known Jito pool account addresses** that are derivable from the pool address

   Actually, the simplest valid approach that works: encode the amount in lamports and return a CONTRACT_CALL that the pipeline can process. The key Jito pool accounts are fixed and known:

   **Jito Mainnet Pool Accounts:**
   - Stake Pool: `Jito4APyf642JPZPx3hGc6WWJ8zPKtRbRs4P815Awbb`
   - Pool Mint (JitoSOL): `J1toso1uCk3RLmjorhTtrVwY9HJ7X8V9yYac6Y7kGCPn`
   - Withdraw Authority (PDA): `6iQKfEyhr3bZMotVkW6beNZz5CPAkiwvgV2CTje9pVSS`
   - Reserve Stake: `BgKUXdS2jFbByBaPQRf6eSCjMH3mRLDPH1MCVLhEAD3c`
   - Manager Fee Account: `B1aLzaNMeFVAyQ6f3XbbUyKcH2YPHu2fqiEagmiF23VR`
   - SOL Deposit Authority (none -- permissionless deposit)

   Create helper functions:
   - `encodeDepositSolData(amountLamports: bigint): string` -- returns base64 of [14, ...LE_u64(amount)]
   - `encodeWithdrawSolData(amountLamports: bigint): string` -- returns base64 of [16, ...LE_u64(amount)]
   - `buildDepositSolAccounts(walletAddress: string, config: JitoStakingConfig): AccountMeta[]` -- returns the 10 account metas for DepositSol
   - `buildWithdrawSolAccounts(walletAddress: string, config: JitoStakingConfig): AccountMeta[]` -- returns the account metas for WithdrawSol

   **Important:** Export the well-known PDA addresses as constants in config.ts:
   - `JITO_WITHDRAW_AUTHORITY: '6iQKfEyhr3bZMotVkW6beNZz5CPAkiwvgV2CTje9pVSS'`
   - `JITO_RESERVE_STAKE: 'BgKUXdS2jFbByBaPQRf6eSCjMH3mRLDPH1MCVLhEAD3c'`
   - `JITO_MANAGER_FEE: 'B1aLzaNMeFVAyQ6f3XbbUyKcH2YPHu2fqiEagmiF23VR'`
   - `SPL_TOKEN_PROGRAM: 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'`
   - `SYSTEM_PROGRAM: '11111111111111111111111111111111'`

   The wallet's JitoSOL ATA (Associated Token Account) address will need to be computed or passed. Since ATA derivation requires on-chain data, use the convention that the pipeline's SolanaAdapter creates the ATA if needed. For the accounts array, use a **placeholder** for the destination token account that the adapter will resolve, OR compute it deterministically.

   Actually, for clean design that matches the Jupiter pattern: The `accounts` array in ContractCallRequest contains ALL account metas. The SolanaAdapter reconstructs a TransactionInstruction from `programId`, `instructionData` (base64), and `accounts`. It does NOT do PDA derivation -- the provider must supply all resolved addresses.

   **ATA derivation is deterministic** (no RPC needed): `findProgramAddressSync([walletPubkey, TOKEN_PROGRAM, mintPubkey], ATA_PROGRAM)`. Implement a lightweight `findAssociatedTokenAddress(wallet, mint)` using SHA-256 based PDA derivation -- but this requires the ed25519 curve point check which is complex without a library.

   **SIMPLEST WORKING APPROACH:** Since Jito deposits via `DepositSol` instruction actually just transfers SOL (native) into the pool and mints JitoSOL to the user's ATA, we can express this as:

   The `DepositSol` instruction data is simple binary. The accounts are well-known except for:
   - `fundingAccount` (wallet address -- the SOL payer)
   - `destTokenAccount` (wallet's JitoSOL ATA -- needs derivation)

   For the ATA, implement a simple deterministic derivation using Node.js `crypto.createHash('sha256')` for the PDA bump search. This is a pure function with no external deps.

   Implement in `jito-stake-pool.ts`:

   ```typescript
   import { createHash } from 'node:crypto';

   // ATA derivation constants
   const ATA_PROGRAM_ID = 'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL';

   // findAssociatedTokenAddress(wallet, mint) -> deterministic PDA
   function findAssociatedTokenAddress(wallet: string, mint: string): string {
     // Seeds: [wallet, TOKEN_PROGRAM, mint] under ATA_PROGRAM
     // ... PDA derivation using SHA-256 loop
   }
   ```

   NOTE: PDA derivation requires checking if the result is on the ed25519 curve, which requires the `@noble/ed25519` or similar library. Since we want zero deps, and the @solana/kit already exists in the project (used by SolanaAdapter), we could import `getAssociatedTokenAddressSync` from a solana library if available.

   **CHECK:** The project uses `@solana/kit 6.x` (from MEMORY.md). `@solana/kit` v6 has `getAssociatedTokenAddress` utilities.

   **REVISED FINAL APPROACH:** Since computing ATAs reliably requires either a crypto library for PDA derivation or the @solana/kit dependency, and since the `@waiaas/actions` package currently has ZERO Solana dependencies (Jupiter gets full instructions from its API), take the pragmatic approach:

   Make the Jito provider's `resolve()` method return a simpler representation:
   - For **stake (depositSol)**: Return ContractCallRequest with `programId`, `instructionData` (DepositSol binary), and `accounts` array where the destination ATA is derived. Use Node.js `crypto` module only (available in all Node.js environments) for PDA derivation.
   - For **unstake (withdrawSol)**: Return ContractCallRequest with `programId`, `instructionData` (WithdrawSol binary), and `accounts` array.

   Implement PDA derivation using only `node:crypto`:
   ```typescript
   function findProgramAddress(seeds: Uint8Array[], programId: Uint8Array): [Uint8Array, number] {
     for (let bump = 255; bump >= 0; bump--) {
       const hash = createHash('sha256');
       for (const seed of seeds) hash.update(seed);
       hash.update(Buffer.from([bump]));
       hash.update(programId);
       hash.update(Buffer.from('ProgramDerivedAddress'));
       const result = hash.digest();
       // Check if NOT on curve (PDA must not be a valid pubkey on ed25519)
       // Simplified: just return the first hash -- in practice this works 99.6% of the time with bump=255
       // For correctness, need ed25519 curve check. Since this is deterministic and the Jito pool
       // addresses are fixed, we can PRE-COMPUTE and hardcode the known ATAs... but ATAs depend on
       // the wallet address which varies per user.
     }
   }
   ```

   The curve check is the blocker. Without it, PDA derivation is unreliable.

   **FINAL DECISION:** Use the same approach as Jupiter -- provide instruction data and account metas as a "Solana instruction template". The SolanaAdapter already handles reconstructing TransactionInstructions. But require the ATA to be pre-created or have the adapter create it.

   Actually, after careful analysis, the cleanest approach for this provider that maintains zero external dependencies while being correct is:

   1. Encode `DepositSol` and `WithdrawSol` instruction data (simple binary -- no PDA needed for data)
   2. For the accounts array, include a special marker for accounts that need runtime resolution (like the wallet's JitoSOL ATA)
   3. Alternatively, since the SolanaAdapter already handles TOKEN_TRANSFER with automatic ATA creation, we can structure the Jito stake as a two-step process where the adapter handles ATA management

   **ACTUALLY SIMPLEST CORRECT APPROACH:**

   Looking back at how the pipeline works: `resolve()` returns `ContractCallRequest`, which goes through the 6-stage pipeline. The SolanaAdapter's `executeContractCall()` method takes the `programId`, `instructionData`, and `accounts` and builds a `TransactionInstruction`.

   For the accounts array to be complete, we need the ATA address. Since we can't compute it without an ed25519 curve check, we have two options:

   a) Add a lightweight ed25519 check using `node:crypto` -- Node.js 22 supports `crypto.subtle` with Ed25519
   b) Use `@solana/kit` as a devDependency and import the PDA function

   Option (a) is complex. Option (b) adds a Solana dependency to @waiaas/actions.

   **OPTION C (recommended):** Pre-compute the well-known accounts for the Jito pool and encode the wallet-specific accounts (funding account = wallet pubkey, dest token account = needs PDA). For the ATA, implement a minimal base58 + SHA-256 + Ed25519 point-on-curve check using Node.js built-in `crypto.createPublicKey` with Ed25519.

   Actually, Node.js 22 has `crypto.subtle.importKey` with `Ed25519` which can validate if a point is on the curve by trying to import it as a public key. This is a clean zero-dependency approach!

   ```typescript
   async function isOnCurve(point: Uint8Array): Promise<boolean> {
     try {
       await crypto.subtle.importKey('raw', point, { name: 'Ed25519' }, false, ['verify']);
       return true;
     } catch {
       return false;
     }
   }
   ```

   This makes `findProgramAddress` async but that's fine since `resolve()` is already async.

   **IMPLEMENT THIS APPROACH.** Full implementation details:

   In `jito-stake-pool.ts`:

   a) Base58 encoder/decoder (lightweight, ~30 lines -- encode pubkey bytes to/from base58 string). Use the standard Bitcoin base58 alphabet.

   b) `findProgramAddress(seeds, programId)` using `node:crypto`:
      - Loop bump from 255 to 0
      - SHA-256 hash of [seeds..., bump_byte, programId_bytes, "ProgramDerivedAddress"]
      - Check if result is NOT on Ed25519 curve via `crypto.subtle.importKey`
      - Return [address_bytes, bump]

   c) `getAssociatedTokenAddress(wallet, mint, tokenProgram)` -- derives ATA PDA:
      - Seeds: [wallet_bytes, token_program_bytes, mint_bytes]
      - Program: ATA_PROGRAM_ID

   d) `encodeDepositSolData(amountLamports)` -- returns base64 string:
      - Buffer: [14 (u8), amount (LE u64)]

   e) `encodeWithdrawSolData(amountLamports)` -- returns base64 string:
      - Buffer: [16 (u8), amount (LE u64)]

   f) `buildDepositSolRequest(config, amountLamports, walletAddress)` -- returns ContractCallRequest:
      - Derives wallet's JitoSOL ATA
      - Builds all 10 account metas for DepositSol
      - Returns { type: 'CONTRACT_CALL', to: stakePoolAddress, programId, instructionData, accounts }

   g) `buildWithdrawSolRequest(config, amountLamports, walletAddress)` -- returns ContractCallRequest:
      - Derives wallet's JitoSOL ATA
      - Builds account metas for WithdrawSol
      - Returns { type: 'CONTRACT_CALL', to: stakePoolAddress, programId, instructionData, accounts }

   **SPL Stake Pool DepositSol accounts (in order):**
   0. stakePool (writable)
   1. withdrawAuthority (PDA, read-only)
   2. reserveStake (writable)
   3. fundingAccount (signer, writable) = wallet
   4. destTokenAccount (writable) = wallet's JitoSOL ATA
   5. managerFeeAccount (writable)
   6. referralFeeAccount (writable) = same as managerFeeAccount for no referral
   7. poolMint (writable) = JitoSOL mint
   8. systemProgram (read-only) = 11111111111111111111111111111111
   9. tokenProgram (read-only) = TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA

   **SPL Stake Pool WithdrawSol accounts (in order):**
   0. stakePool (writable)
   1. withdrawAuthority (PDA, read-only)
   2. userTransferAuthority (signer) = wallet
   3. sourceTokenAccount (writable) = wallet's JitoSOL ATA
   4. reserveStake (writable)
   5. destSolAccount (writable) = wallet (receives SOL)
   6. managerFeeAccount (writable)
   7. poolMint (writable) = JitoSOL mint
   8. clock (read-only) = SysvarC1ock11111111111111111111111111111111
   9. stakeHistory (read-only) = SysvarStakeHistory1111111111111111111111111
   10. stakeProgram (read-only) = Stake11111111111111111111111111111111111111
   11. tokenProgram (read-only) = TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA

   For parseSolAmount helper: SOL has 9 decimals (not 18 like ETH). Implement similar to parseEthAmount but with 9 decimal places.
   `parseSolAmount(amount: string): bigint` -- "1.5" -> 1500000000n (1.5 SOL in lamports)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit -p packages/actions/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify config.ts has JitoStakingConfig, JITO_MAINNET_ADDRESSES, JITO_STAKING_DEFAULTS exports. Verify jito-stake-pool.ts exports encodeDepositSolData, encodeWithdrawSolData, buildDepositSolRequest, buildWithdrawSolRequest.</manual>
  </verify>
  <done>config.ts exports JitoStakingConfig type + mainnet addresses + defaults + getJitoAddresses(). jito-stake-pool.ts exports DepositSol/WithdrawSol instruction builders with base58/PDA utilities. parseSolAmount converts "1.5" to 1500000000n lamports. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: JitoStakingActionProvider + unit tests</name>
  <files>
    packages/actions/src/providers/jito-staking/index.ts
    packages/actions/src/__tests__/jito-staking.test.ts
  </files>
  <action>
Create `packages/actions/src/providers/jito-staking/index.ts`:

Follow EXACTLY the same structure as LidoStakingActionProvider (packages/actions/src/providers/lido-staking/index.ts).

1. Import dependencies: `z` from zod, `ChainError` from @waiaas/core, IActionProvider types, config, jito-stake-pool builders.

2. Define Zod input schemas:
   - `JitoStakeInputSchema`: `{ amount: z.string().min(1, 'amount is required (SOL, e.g. "1.0")') }`
   - `JitoUnstakeInputSchema`: `{ amount: z.string().min(1, 'amount is required (JitoSOL, e.g. "1.0")') }`

3. Use parseSolAmount from jito-stake-pool.ts (or define locally like Lido's parseEthAmount).

4. Implement `JitoStakingActionProvider implements IActionProvider`:
   - `metadata`: name='jito_staking', description about Jito liquid staking for SOL to JitoSOL, version='1.0.0', chains=['solana'], mcpExpose=true, requiresApiKey=false, requiredApis=[]
   - `actions`: two ActionDefinition entries:
     - stake: description='Stake SOL to receive JitoSOL via Jito Stake Pool (DepositSol). Immediate, no lock-up.', chain='solana', riskLevel='medium', defaultTier='DELAY'
     - unstake: description='Withdraw SOL from Jito Stake Pool by burning JitoSOL (WithdrawSol). Epoch boundary delay.', chain='solana', riskLevel='medium', defaultTier='DELAY'
   - `resolve(actionName, params, context)`:
     - 'stake' -> call `buildDepositSolRequest(config, amountLamports, context.walletAddress)`
     - 'unstake' -> call `buildWithdrawSolRequest(config, amountLamports, context.walletAddress)`
     - unknown -> throw ChainError('INVALID_INSTRUCTION', 'solana', ...)

5. The constructor takes optional `Partial<JitoStakingConfig>` and merges with JITO_STAKING_DEFAULTS.

Create `packages/actions/src/__tests__/jito-staking.test.ts`:

Follow same structure as lido-staking.test.ts. Test cases (minimum 10):

1. **stake returns ContractCallRequest with Solana fields** -- type=CONTRACT_CALL, programId=SPL_STAKE_POOL_PROGRAM, has instructionData (base64), has accounts array
2. **stake instructionData starts with DepositSol index (14)** -- decode base64 and check first byte is 14
3. **unstake returns ContractCallRequest with Solana fields** -- type=CONTRACT_CALL, programId, instructionData, accounts
4. **unstake instructionData starts with WithdrawSol index (16)** -- decode base64 and check first byte is 16
5. **stake amount "1.0" encodes 1000000000 lamports in instructionData** -- decode LE u64 from bytes 1-8
6. **zero amount throws** -- amount "0" throws 'Amount must be greater than 0'
7. **unknown action throws** -- 'unknown_action' throws 'Unknown action'
8. **metadata has correct name, chains, mcpExpose** -- name='jito_staking', chains=['solana'], mcpExpose=true
9. **stake decimal "0.5" SOL encodes 500000000 lamports** -- verify precise conversion
10. **stake accounts include wallet as signer** -- check accounts array has wallet address as signer
11. **unstake accounts include wallet as signer** -- check accounts array has wallet address

Context for tests:
```typescript
const CONTEXT: ActionContext = {
  walletAddress: 'So11111111111111111111111111111111111111112', // Example Solana address
  chain: 'solana',
  walletId: '00000000-0000-0000-0000-000000000001',
};
```

Use a real-looking Solana base58 address for walletAddress in tests.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx vitest run packages/actions/src/__tests__/jito-staking.test.ts --reporter=verbose 2>&1 | tail -30</automated>
    <manual>Verify all 10+ tests pass. Verify provider metadata, resolve() return shape, and error handling match the Lido pattern.</manual>
  </verify>
  <done>JitoStakingActionProvider implements IActionProvider with stake/unstake actions returning ContractCallRequest with Solana-specific fields (programId, instructionData, accounts). All 10+ unit tests pass covering stake, unstake, amounts, errors, metadata, and account structure.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/actions/tsconfig.json` -- TypeScript compiles
2. `npx vitest run packages/actions/src/__tests__/jito-staking.test.ts` -- All tests pass
3. JitoStakingActionProvider has exactly 2 actions (stake, unstake)
4. resolve('stake') returns ContractCallRequest with programId, instructionData, accounts (Solana fields)
5. resolve('unstake') returns ContractCallRequest with programId, instructionData, accounts
6. parseSolAmount("1.0") == 1000000000n (9 decimal places, not 18)
7. Amount "0" throws ChainError
</verification>

<success_criteria>
- JitoStakingActionProvider resolves stake action to DepositSol ContractCallRequest with SPL Stake Pool program fields
- JitoStakingActionProvider resolves unstake action to WithdrawSol ContractCallRequest
- All unit tests pass
- TypeScript compiles without errors
- Zero external Solana SDK dependencies (pure Node.js crypto for PDA derivation)
</success_criteria>

<output>
After completion, create `.planning/phases/255-jito-solana-staking-provider/255-01-SUMMARY.md`
</output>
