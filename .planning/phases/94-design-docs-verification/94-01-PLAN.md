---
phase: 94-design-docs-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/56-token-transfer-extension-spec.md
  - docs/57-asset-query-fee-estimation-spec.md
  - docs/58-contract-call-spec.md
  - docs/59-approve-management-spec.md
  - docs/60-batch-transaction-spec.md
  - docs/61-price-oracle-spec.md
  - docs/62-action-provider-architecture.md
  - docs/67-admin-web-ui-spec.md
  - docs/v0.4/42-mock-boundaries-interfaces-contracts.md
  - docs/v0.4/43-layer1-session-auth-attacks.md
  - docs/v0.4/44-layer2-policy-bypass-attacks.md
  - docs/v0.4/45-layer3-killswitch-recovery-attacks.md
  - docs/v0.4/46-keystore-external-security-scenarios.md
  - docs/v0.4/47-boundary-value-chain-scenarios.md
  - docs/v0.4/49-enum-config-consistency-verification.md
  - README.md
autonomous: true

must_haves:
  truths:
    - "Design docs 15 files use wallet terminology for code identifiers, table names, and schema names"
    - "README.md API examples use /v1/wallets path, walletId field, and --wallet flag"
    - "AI agent concept references (describing what AI agents do) remain unchanged"
    - "Code snippets in docs reflect current codebase reality (walletId, wallet_id, WalletSchema)"
  artifacts:
    - path: "docs/56-token-transfer-extension-spec.md"
      provides: "Token transfer spec with wallet terminology"
      contains: "walletId"
    - path: "docs/67-admin-web-ui-spec.md"
      provides: "Admin UI spec with wallet terminology"
      contains: "walletId"
    - path: "README.md"
      provides: "Project README with /v1/wallets examples"
      contains: "/v1/wallets"
  key_links:
    - from: "docs/*.md code snippets"
      to: "packages/ source code"
      via: "consistent field names and API paths"
      pattern: "walletId|wallet_id|/v1/wallets"
---

<objective>
Update all 15 design documents and README.md to replace agent terminology with wallet terminology, matching the v1.4.2 codebase changes completed in Phases 89-93.

Purpose: Design documentation must reflect the actual codebase. After the agent->wallet rename across DB, core, daemon, MCP/CLI/SDK, and Admin UI, the docs still reference agentId, agent_id, agents table, /v1/agents, AgentSchema etc. This creates confusion for anyone reading the docs alongside the code.

Output: 15 updated design docs + 1 updated README.md with ~220 agent->wallet substitutions, preserving AI agent concept references.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 15 design docs with wallet terminology</name>
  <files>
    docs/56-token-transfer-extension-spec.md
    docs/57-asset-query-fee-estimation-spec.md
    docs/58-contract-call-spec.md
    docs/59-approve-management-spec.md
    docs/60-batch-transaction-spec.md
    docs/61-price-oracle-spec.md
    docs/62-action-provider-architecture.md
    docs/67-admin-web-ui-spec.md
    docs/v0.4/42-mock-boundaries-interfaces-contracts.md
    docs/v0.4/43-layer1-session-auth-attacks.md
    docs/v0.4/44-layer2-policy-bypass-attacks.md
    docs/v0.4/45-layer3-killswitch-recovery-attacks.md
    docs/v0.4/46-keystore-external-security-scenarios.md
    docs/v0.4/47-boundary-value-chain-scenarios.md
    docs/v0.4/49-enum-config-consistency-verification.md
  </files>
  <action>
    For each of the 15 design doc files, apply context-aware substitutions:

    **Code identifier replacements (always rename):**
    - `agentId` -> `walletId` (code variable/field names, JSON fields, URL params)
    - `agent_id` -> `wallet_id` (DB column names, SQL references)
    - `agentService` -> `walletService` / `getAgent` -> `getWallet` (function/method names)
    - `AgentSchema` / `AgentStatusEnum` -> `WalletSchema` / `WalletStatusEnum` (Zod types)
    - `agents` table -> `wallets` table (DB table references)
    - `/v1/agents` -> `/v1/wallets` (API paths)
    - `AGENT_NOT_FOUND` / `AGENT_SUSPENDED` / `AGENT_TERMINATED` -> `WALLET_NOT_FOUND` / `WALLET_SUSPENDED` / `WALLET_TERMINATED` (error codes)
    - `agent-001` / `agent-002` etc. -> `wallet-001` / `wallet-002` (fixture IDs in test scenarios)
    - `agentsSuspended` -> `walletsSuspended` (result field names)
    - `agentTokenPolicy` -> `walletTokenPolicy` (variable names)
    - `agentKeyPair` -> `walletKeyPair` (variable names)
    - `agentChain` -> `walletChain` (parameter names)
    - `agent.chain` / `agent.publicKey` -> `wallet.chain` / `wallet.publicKey` (object access)
    - `agentCount` -> `walletCount` (field names)
    - `sessionContext.agent` -> `sessionContext.wallet` (pipeline context)
    - `sessionContext.agentId` -> `sessionContext.walletId`
    - `idx_*_agent_*` -> `idx_*_wallet_*` (index names)
    - `AGENT_CREATED` / `AGENT_SUSPENDED` event types -> `WALLET_CREATED` / `WALLET_SUSPENDED`
    - `@param agent` -> `@param wallet` (JSDoc in code snippets)
    - `agent-{id}.json` -> `wallet-{id}.json` (file paths)

    **Concept references to PRESERVE (do NOT rename):**
    - "AI agent" / "AI 에이전트" (describing the AI agent as a concept/actor)
    - "에이전트가 ..." (describing what an AI agent does)
    - "에이전트 지갑" / "agent wallet" (concept)
    - "에이전트별 정책" -> rename to "지갑별 정책" only where it refers to per-wallet policies
    - "에이전트당" -> rename to "지갑당" only where it refers to per-wallet limits

    **Context-dependent judgment:**
    - In test scenario descriptions: "에이전트 Agent A" or similar conceptual references to AI agents should stay
    - In DB/code context: "에이전트" referring to the managed entity should become "지갑"
    - Korean text "에이전트" in error messages/templates should become "지갑" if it refers to the wallet entity

    **Per-file approximate counts:**
    - 56-token-transfer-extension-spec.md: ~14 occurrences
    - 57-asset-query-fee-estimation-spec.md: ~6 occurrences
    - 58-contract-call-spec.md: ~8 occurrences
    - 59-approve-management-spec.md: ~5 occurrences
    - 60-batch-transaction-spec.md: ~10 occurrences
    - 61-price-oracle-spec.md: ~1 occurrence
    - 62-action-provider-architecture.md: ~6 occurrences
    - 67-admin-web-ui-spec.md: ~52 occurrences (largest file -- Admin UI spec)
    - v0.4/42-mock-boundaries-interfaces-contracts.md: ~35 occurrences
    - v0.4/43-layer1-session-auth-attacks.md: ~11 occurrences
    - v0.4/44-layer2-policy-bypass-attacks.md: ~7 occurrences
    - v0.4/45-layer3-killswitch-recovery-attacks.md: ~12 occurrences
    - v0.4/46-keystore-external-security-scenarios.md: ~26 occurrences
    - v0.4/47-boundary-value-chain-scenarios.md: ~16 occurrences
    - v0.4/49-enum-config-consistency-verification.md: ~5 occurrences
  </action>
  <verify>
    Run: `grep -rc 'agentId\|agent_id\|AgentSchema\|/v1/agents\|AGENT_NOT_FOUND\|AGENT_SUSPENDED\|AGENT_TERMINATED\|agentService\|agents\.id\|agentsSuspended\|agent-001' docs/ | grep -v ':0$'`
    Should return 0 lines (no code identifier agent references remain).
    Then: `grep -c 'walletId\|wallet_id\|WalletSchema\|/v1/wallets' docs/56-token-transfer-extension-spec.md docs/67-admin-web-ui-spec.md`
    Should show positive counts.
  </verify>
  <done>All 15 design docs have code identifiers updated to wallet terminology. AI agent concept references preserved unchanged. ~214 substitutions applied with context-aware judgment.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with wallet terminology and v1.4.2 status</name>
  <files>README.md</files>
  <action>
    Update README.md with the following specific changes:

    **Section "3. 에이전트 생성 + 세션 발급" (lines ~155-169):**
    - Heading: "에이전트 생성" -> "지갑 생성"
    - Comment: "# 에이전트 생성" -> "# 지갑 생성"
    - API path: `/v1/agents` -> `/v1/wallets`
    - JSON body: `"name": "my-agent"` -> `"name": "my-wallet"`
    - Session body: `"agentId": "<agent-id-from-above>"` -> `"walletId": "<wallet-id-from-above>"`

    **Section "CLI 명령어" (lines ~366-369):**
    - `--agent <agent-id>` -> `--wallet <wallet-id>`
    - Comment: "특정 에이전트" -> "특정 지갑"
    - Comment: "모든 에이전트 일괄 설정" -> "모든 지갑 일괄 설정"

    **Section "MCP 연동" (line ~443):**
    - "에이전트별 세션 토큰" -> "지갑별 세션 토큰"
    - "모든 에이전트를 일괄 설정" -> "모든 지갑을 일괄 설정"

    **Section "config.toml" (line ~224):**
    - `max_sessions_per_agent = 5` -> `max_sessions_per_wallet = 5`
    - Comment: "에이전트당 최대 세션 수" -> "지갑당 최대 세션 수"

    **Section "Admin Web UI" (lines ~494-495):**
    - "**Agents** | 에이전트 목록/생성, 상세 정보, 키 재생성" -> "**Wallets** | 지갑 목록/생성, 상세 정보, 키 재생성"

    **Section "인증 수준" (line ~510):**
    - "에이전트 생성, 정책 설정, 세션 관리" stays (this describes what master auth does)

    **Section "인터페이스" table (line ~59):**
    - "에이전트/세션/정책/알림 관리" -> "지갑/세션/정책/알림 관리"

    **Section "프로젝트 상태" (lines ~562-590):**
    - Update LOC/test counts to v1.4.2 actuals: 65,074 LOC, 1,313+ tests, 9 packages
    - Update plan/req/phase/milestone counts: 197+ plans, 552+ reqs, 88+ phases, 21 milestones
    - Add v1.4 and v1.4.1 milestones to the table
    - Fix milestone description for v1.3.3: "에이전트별 MCP 토큰 격리" -> "지갑별 MCP 토큰 격리"

    **Preserve AI agent concept references:**
    - "AI 에이전트가 안전하게 온체인 거래를 수행하는" (line 2) -- keep
    - "AI 에이전트 전용 지갑 시스템" (line 5) -- keep
    - "에이전트가 자율적으로 거래를 수행" (line 11) -- keep
    - "에이전트의 자율적 거래를 보장" (line 23) -- keep
    - "에이전트 접근 제어" (line 37) -- keep
    - "AI 에이전트 | JWT Bearer 토큰" (line 512) -- keep
  </action>
  <verify>
    Run: `grep -n 'agentId\|/v1/agents\|--agent\|max_sessions_per_agent\|my-agent' README.md`
    Should return 0 lines.
    Run: `grep -c 'walletId\|/v1/wallets\|--wallet\|max_sessions_per_wallet' README.md`
    Should show positive count.
  </verify>
  <done>README.md has API examples, CLI flags, config keys, Admin UI table, and milestone history updated to wallet terminology. v1.4.2 project status reflected. AI agent concept references preserved.</done>
</task>

</tasks>

<verification>
1. `grep -rc 'agentId\|agent_id\|AgentSchema\|/v1/agents\|AGENT_NOT_FOUND\|agentService' docs/ | grep -v ':0$'` returns 0 lines
2. `grep -n 'agentId\|/v1/agents\|--agent ' README.md` returns 0 lines
3. `grep -c 'walletId\|wallet_id' docs/67-admin-web-ui-spec.md` shows positive count (largest file)
4. `grep -c '/v1/wallets' README.md` shows positive count
</verification>

<success_criteria>
- 15 design docs updated with wallet terminology (code identifiers only)
- README.md updated with /v1/wallets, walletId, --wallet, max_sessions_per_wallet
- AI agent concept references preserved in all files
- 0 stale code identifier agent references in docs/ and README.md
</success_criteria>

<output>
After completion, create `.planning/phases/94-design-docs-verification/94-01-SUMMARY.md`
</output>
