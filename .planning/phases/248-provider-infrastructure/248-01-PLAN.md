---
phase: 248-provider-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/enums/notification.ts
  - packages/core/src/__tests__/enums.test.ts
  - packages/core/src/i18n/en.ts
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/daemon/src/infrastructure/config/loader.ts
  - packages/actions/src/index.ts
  - packages/daemon/src/lifecycle/daemon.ts
  - packages/daemon/src/api/routes/actions.ts
  - packages/daemon/src/__tests__/settings-service.test.ts
autonomous: true
requirements:
  - PINF-01
  - PINF-02
  - PINF-03
  - PINF-05

must_haves:
  truths:
    - "SettingsService.get('actions.jupiter_swap_enabled') returns the correct value from DB or default"
    - "SettingsService.get('actions.zerox_swap_enabled') returns the correct value from DB or default"
    - "registerBuiltInProviders reads settings from SettingsService instead of config.toml [actions]"
    - "ACTION_API_KEY_REQUIRED notification event is defined and has an English i18n template with adminUrl field"
    - "API key missing on swap attempt fires ACTION_API_KEY_REQUIRED notification instead of just throwing"
  artifacts:
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "actions category with jupiter_swap and zerox_swap settings"
      contains: "actions"
    - path: "packages/core/src/enums/notification.ts"
      provides: "ACTION_API_KEY_REQUIRED event type"
      contains: "ACTION_API_KEY_REQUIRED"
    - path: "packages/core/src/i18n/en.ts"
      provides: "English notification template for ACTION_API_KEY_REQUIRED"
      contains: "ACTION_API_KEY_REQUIRED"
    - path: "packages/actions/src/index.ts"
      provides: "registerBuiltInProviders accepting SettingsService"
      contains: "SettingsService"
  key_links:
    - from: "packages/daemon/src/lifecycle/daemon.ts"
      to: "packages/actions/src/index.ts"
      via: "registerBuiltInProviders call with SettingsService"
      pattern: "registerBuiltInProviders.*settingsService"
    - from: "packages/daemon/src/api/routes/actions.ts"
      to: "packages/daemon/src/notifications/notification-service.ts"
      via: "notify ACTION_API_KEY_REQUIRED on missing key"
      pattern: "ACTION_API_KEY_REQUIRED"
    - from: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      to: "packages/daemon/src/infrastructure/settings/settings-service.ts"
      via: "SETTING_DEFINITIONS lookup"
      pattern: "actions\\."
---

<objective>
Add `actions` settings category to Admin Settings (SettingsService SSoT), migrate config.toml [actions] to Settings-based config, and add ACTION_API_KEY_REQUIRED notification event.

Purpose: Phase 248 foundation -- all subsequent plans depend on providers being managed via Admin Settings instead of config.toml. The notification event provides actionable guidance when API keys are missing.

Output: Updated setting-keys.ts with actions category, updated registerBuiltInProviders reading from SettingsService, ACTION_API_KEY_REQUIRED notification event with i18n template, config.toml [actions] section preserved for backward compat but settings take priority.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/daemon/src/infrastructure/settings/setting-keys.ts
@packages/daemon/src/infrastructure/settings/settings-service.ts
@packages/actions/src/index.ts
@packages/daemon/src/lifecycle/daemon.ts
@packages/daemon/src/api/routes/actions.ts
@packages/core/src/enums/notification.ts
@packages/core/src/i18n/en.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add actions settings category + ACTION_API_KEY_REQUIRED notification event</name>
  <files>
    packages/core/src/enums/notification.ts
    packages/core/src/__tests__/enums.test.ts
    packages/core/src/i18n/en.ts
    packages/daemon/src/infrastructure/settings/setting-keys.ts
  </files>
  <action>
1. In `packages/core/src/enums/notification.ts`:
   - Add `'ACTION_API_KEY_REQUIRED'` to the NOTIFICATION_EVENT_TYPES array (after 'TX_INCOMING_SUSPICIOUS').

2. In `packages/core/src/__tests__/enums.test.ts`:
   - Update the expected count for NOTIFICATION_EVENT_TYPES to include the new event (was 30, now 31).

3. In `packages/core/src/i18n/en.ts`:
   - Add notification template for ACTION_API_KEY_REQUIRED. Pattern from existing templates. Include `provider`, `action`, `adminUrl` fields.
   - Template: `"[WAIaaS] API key required for {provider}. Configure at {adminUrl} > System > API Keys"`

4. In `packages/daemon/src/infrastructure/settings/setting-keys.ts`:
   - Add `'actions'` to the SETTING_CATEGORIES array.
   - Add SettingDefinition entries for the `actions` category. Each provider gets 8 config keys matching the existing DaemonConfigSchema pattern:
     ```
     actions.jupiter_swap_enabled       (default: 'false', isCredential: false, configPath: 'actions.jupiter_swap_enabled')
     actions.jupiter_swap_api_base_url  (default: 'https://api.jup.ag/swap/v1', isCredential: false)
     actions.jupiter_swap_api_key       (default: '', isCredential: true)
     actions.jupiter_swap_default_slippage_bps (default: '50', isCredential: false)
     actions.jupiter_swap_max_slippage_bps     (default: '500', isCredential: false)
     actions.jupiter_swap_max_price_impact_pct (default: '1', isCredential: false)
     actions.jupiter_swap_jito_tip_lamports    (default: '1000', isCredential: false)
     actions.jupiter_swap_request_timeout_ms   (default: '10000', isCredential: false)
     actions.zerox_swap_enabled         (default: 'false', isCredential: false, configPath: 'actions.zerox_swap_enabled')
     actions.zerox_swap_api_key         (default: '', isCredential: true, configPath: 'actions.zerox_swap_api_key')
     actions.zerox_swap_default_slippage_bps (default: '100', isCredential: false)
     actions.zerox_swap_max_slippage_bps     (default: '500', isCredential: false)
     actions.zerox_swap_request_timeout_ms   (default: '10000', isCredential: false)
     ```
   - configPath follows the existing pattern: `'actions.{field_name}'` matching DaemonConfigSchema field names.
  </action>
  <verify>
    Run `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run build --filter=@waiaas/core` to verify core builds.
    Run `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test --filter=@waiaas/core -- --run` to verify core tests pass (especially enums count test).
    Grep for 'ACTION_API_KEY_REQUIRED' in notification.ts confirms it's in the array.
    Grep for 'actions' in setting-keys.ts confirms category is registered.
  </verify>
  <done>
    - NOTIFICATION_EVENT_TYPES has 31 events including ACTION_API_KEY_REQUIRED
    - en.ts has template for ACTION_API_KEY_REQUIRED with provider and adminUrl fields
    - SETTING_CATEGORIES includes 'actions'
    - SETTING_DEFINITIONS has 13 entries for actions.jupiter_swap_* and actions.zerox_swap_*
    - All core tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate registerBuiltInProviders to read from SettingsService + fire ACTION_API_KEY_REQUIRED notification</name>
  <files>
    packages/actions/src/index.ts
    packages/daemon/src/lifecycle/daemon.ts
    packages/daemon/src/api/routes/actions.ts
    packages/daemon/src/infrastructure/config/loader.ts
    packages/daemon/src/__tests__/settings-service.test.ts
  </files>
  <action>
1. In `packages/actions/src/index.ts`:
   - Change `registerBuiltInProviders` signature to accept a `SettingsReader` interface instead of raw `ActionsConfig`:
     ```typescript
     interface SettingsReader {
       get(key: string): string;
     }
     ```
   - Update the function to read config from SettingsReader:
     ```typescript
     export function registerBuiltInProviders(
       registry: ProviderRegistry,
       settingsReader: SettingsReader,
     ): { loaded: string[]; skipped: string[] }
     ```
   - For Jupiter, read keys like `settingsReader.get('actions.jupiter_swap_enabled')` (returns string 'true'/'false') and convert to the JupiterSwapConfig type.
   - Provider is registered when `enabled === 'true'` (string comparison, since SettingsService always returns strings).
   - For 0x, add a placeholder entry in the providers array with key 'zerox_swap' that reads `actions.zerox_swap_enabled`. Skip registration for now (factory returns null/skips) since ZeroExSwapActionProvider doesn't exist yet -- just push to `skipped` array.

2. In `packages/daemon/src/lifecycle/daemon.ts`:
   - Update the Step 4f section that calls registerBuiltInProviders.
   - Instead of passing `actionsConfig` object built from `this._config!.actions.*`, pass `this._settingsService!` (which implements the SettingsReader interface since it has a `get(key)` method).
   - Remove the `actionsConfig` object construction entirely.
   - The first boot will use SettingsService fallback chain: DB -> config.toml -> default. So existing [actions] config.toml values are automatically honored via `importFromConfig()` which runs during daemon startup.

3. In `packages/daemon/src/api/routes/actions.ts`:
   - In the `executeActionRoute` handler, step 2 (API key check), after throwing WAIaaSError, also fire a notification BEFORE the throw:
     ```typescript
     if (entry.provider.metadata.requiresApiKey && !deps.apiKeyStore.has(entry.provider.metadata.name)) {
       // Fire notification with adminUrl
       const adminPort = deps.config.daemon?.port ?? 3000;
       const adminUrl = `http://localhost:${adminPort}/admin`;
       void deps.notificationService?.notify('ACTION_API_KEY_REQUIRED', walletId, {
         provider: entry.provider.metadata.name,
         action: actionKey,
         adminUrl,
       });
       throw new WAIaaSError('API_KEY_REQUIRED', { ... });
     }
     ```
   - Note: walletId resolution happens AFTER the API key check in current code. Move the API key check to AFTER walletId resolution (step 3 in current code), so we have a walletId for the notification. Reorder: step 1 (action lookup) -> step 2 (resolve walletId) -> step 3 (API key check with notification) -> step 4 (lookup wallet) -> ...

4. In `packages/daemon/src/infrastructure/config/loader.ts`:
   - Keep the `[actions]` section in DaemonConfigSchema for backward compatibility -- existing config.toml files will still parse correctly.
   - Add the 0x-related config keys to the actions section of DaemonConfigSchema for forward compatibility:
     ```
     zerox_swap_enabled: z.boolean().default(false)
     zerox_swap_api_key: z.string().default('')
     zerox_swap_default_slippage_bps: z.number().int().min(1).max(10000).default(100)
     zerox_swap_max_slippage_bps: z.number().int().min(1).max(10000).default(500)
     zerox_swap_request_timeout_ms: z.number().int().min(1000).max(60000).default(10000)
     ```

5. In `packages/daemon/src/__tests__/settings-service.test.ts`:
   - Add test that verifies actions category settings are accessible via SettingsService.get().
   - Test that 'actions.jupiter_swap_enabled' returns 'false' by default.
   - Test that 'actions.zerox_swap_api_key' returns '' by default.
  </action>
  <verify>
    Run `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run build --filter=@waiaas/actions --filter=@waiaas/daemon` to verify both packages build.
    Run `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test --filter=@waiaas/daemon -- --run -t "settings"` to verify settings tests pass.
    Grep for 'SettingsReader' in packages/actions/src/index.ts confirms new interface.
    Grep for 'ACTION_API_KEY_REQUIRED' in packages/daemon/src/api/routes/actions.ts confirms notification is fired.
  </verify>
  <done>
    - registerBuiltInProviders accepts SettingsReader (SettingsService-compatible interface)
    - daemon.ts passes this._settingsService to registerBuiltInProviders
    - Actions route fires ACTION_API_KEY_REQUIRED notification before throwing API_KEY_REQUIRED error
    - DaemonConfigSchema has zerox_swap_* fields for forward compat
    - Settings service test confirms actions category keys are accessible
    - All affected packages build and pass tests
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run build` -- all packages compile
2. `pnpm turbo run test --filter=@waiaas/core -- --run` -- notification enum test passes
3. `pnpm turbo run test --filter=@waiaas/daemon -- --run -t "settings"` -- settings tests pass
4. Grep confirms: 'actions' in SETTING_CATEGORIES, ACTION_API_KEY_REQUIRED in notification.ts, SettingsReader in actions/index.ts
</verification>

<success_criteria>
- SettingsService can manage all action provider settings (13 keys across jupiter_swap and zerox_swap)
- registerBuiltInProviders reads from SettingsService, not from config.toml directly
- Existing config.toml [actions] values are honored via SettingsService fallback chain
- ACTION_API_KEY_REQUIRED notification fires when API key is missing on swap attempt
- All tests pass, no build errors
</success_criteria>

<output>
After completion, create `.planning/phases/248-provider-infrastructure/248-01-SUMMARY.md`
</output>
