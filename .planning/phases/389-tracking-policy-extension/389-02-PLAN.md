---
phase: 389-tracking-policy-extension
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/pipeline/database-policy-engine.ts
  - packages/daemon/src/__tests__/database-policy-engine.test.ts
  - packages/daemon/src/__tests__/venue-whitelist-policy.test.ts
  - packages/daemon/src/__tests__/action-category-limit-policy.test.ts
  - packages/core/src/schemas/resolved-action.schema.ts
autonomous: true
requirements: [POLICY-01, POLICY-02, POLICY-03, POLICY-04, POLICY-05, POLICY-06]

must_haves:
  truths:
    - "TransactionParam에 venue/actionCategory/notionalUsd/leverage/expiry/hasWithdrawCapability 필드가 추가된다"
    - "VENUE_WHITELIST 정책이 활성화되면 허용 목록에 없는 venue가 VENUE_NOT_ALLOWED로 거부된다"
    - "venue_whitelist_enabled Admin Setting으로 정책 비활성 가능 (기본값 false)"
    - "contractCall (venue 없음)은 VENUE_WHITELIST 평가를 항상 통과한다"
    - "ACTION_CATEGORY_LIMIT 정책이 카테고리별 USD 한도를 daily/monthly/per_action 단위로 평가한다"
    - "ACTION_CATEGORY_LIMIT 초과 시 tier_on_exceed(기본 DELAY) 동작이 반환된다"
    - "riskLevel 4등급이 defaultTier에 자동 매핑된다 (low->INSTANT, medium->NOTIFY, high->DELAY, critical->APPROVAL)"
    - "기존 SPENDING_LIMIT/provider-trust/CONTRACT_WHITELIST 정책과 충돌 없이 공존한다"
  artifacts:
    - path: "packages/daemon/src/pipeline/database-policy-engine.ts"
      provides: "VENUE_WHITELIST + ACTION_CATEGORY_LIMIT 평가 로직"
      contains: "evaluateVenueWhitelist"
    - path: "packages/daemon/src/__tests__/venue-whitelist-policy.test.ts"
      provides: "VENUE_WHITELIST 정책 테스트"
      contains: "VENUE_NOT_ALLOWED"
    - path: "packages/daemon/src/__tests__/action-category-limit-policy.test.ts"
      provides: "ACTION_CATEGORY_LIMIT 정책 테스트"
      contains: "daily_limit_usd"
    - path: "packages/core/src/schemas/resolved-action.schema.ts"
      provides: "ActionCategoryEnum Zod enum"
      contains: "ActionCategoryEnum"
  key_links:
    - from: "packages/daemon/src/pipeline/database-policy-engine.ts"
      to: "packages/core/src/schemas/resolved-action.schema.ts"
      via: "ActionCategoryEnum import"
      pattern: "ActionCategoryEnum"
    - from: "packages/daemon/src/pipeline/database-policy-engine.ts"
      to: "packages/daemon/src/infrastructure/settings/settings-service.ts"
      via: "venue_whitelist_enabled setting"
      pattern: "venue_whitelist_enabled"
---

<objective>
TransactionParam 확장 + VENUE_WHITELIST 정책 + ACTION_CATEGORY_LIMIT 정책 + riskLevel 자동 매핑

Purpose: off-chain action에 venue 화이트리스트(default-deny)와 카테고리별 USD 한도 정책을 적용하여 AI 에이전트의 off-chain 거래를 통제한다.
Output: 2 new policy types in DatabasePolicyEngine, ActionCategoryEnum, riskLevel mapping
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/design/81-external-action-framework.md (D5 section)

@.planning/phases/386-type-system-errors-db/386-01-SUMMARY.md
@.planning/phases/386-type-system-errors-db/386-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/daemon/src/pipeline/database-policy-engine.ts:
```typescript
interface TransactionParam {
  type: string;
  amount: string;
  toAddress: string;
  chain: string;
  network?: string;
  tokenAddress?: string;
  assetId?: string;
  contractAddress?: string;
  selector?: string;
  spenderAddress?: string;
  approveAmount?: string;
  tokenDecimals?: number;
  actionProvider?: string;
  actionName?: string;
  perpLeverage?: number;
  perpSizeUsd?: number;
}

// 평가 체인 순서: 4a. ALLOWED_TOKENS -> 4b. CONTRACT_WHITELIST -> ... -> 4i-c. PERP_MAX_POSITION_USD
// 신규: 4j. VENUE_WHITELIST, 4k. ACTION_CATEGORY_LIMIT

export class DatabasePolicyEngine implements IPolicyEngine {
  constructor(db, sqlite?, settingsService?, reputationCacheService?)
  async evaluate(walletId: string, transaction: TransactionParam): Promise<PolicyEvaluation>
  async evaluateBatch(walletId: string, instructions: TransactionParam[], batchUsdAmount?: number): Promise<PolicyEvaluation>
  evaluateAndReserve(walletId: string, transaction: TransactionParam, txId: string, usdAmount?, reputationFloorTier?): PolicyEvaluation
  // ... 기존 private 메서드: evaluateAllowedTokens, evaluateContractWhitelist, evaluateMethodWhitelist, evaluateApprovedSpenders, ...
}
```

From packages/core/src/errors/error-codes.ts:
```typescript
// Already registered in Phase 386:
// VENUE_NOT_ALLOWED (403) - POLICY domain
```

From packages/core/src/interfaces/action-provider.types.ts:
```typescript
export const ActionDefinitionSchema = z.object({
  name: z.string()...,
  description: z.string()...,
  chain: ChainTypeEnum,
  inputSchema: z.any(),
  riskLevel: z.enum(['low', 'medium', 'high', 'critical']),
  defaultTier: z.enum(['INSTANT', 'NOTIFY', 'DELAY', 'APPROVAL']),
});
```

From existing CONTRACT_WHITELIST pattern in database-policy-engine.ts:
```typescript
// Policies table: type='CONTRACT_WHITELIST', rules JSON = { contracts: [{address, name?}] }
// evaluateContractWhitelist(): CONTRACT_CALL/NFT_TRANSFER only, default-deny when policy exists
```

From SettingsService pattern:
```typescript
// Admin Settings: settingsService.get('venue_whitelist_enabled') -> boolean
// 기본값 false (비활성), true일 때만 VENUE_WHITELIST 평가
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TransactionParam 확장 + ActionCategoryEnum + VENUE_WHITELIST 정책</name>
  <files>
    packages/core/src/schemas/resolved-action.schema.ts,
    packages/core/src/index.ts,
    packages/daemon/src/pipeline/database-policy-engine.ts,
    packages/daemon/src/__tests__/venue-whitelist-policy.test.ts
  </files>
  <behavior>
    - TransactionParam에 venue?: string, actionCategory?: string, notionalUsd?: number, leverage?: number, expiry?: string, hasWithdrawCapability?: boolean 추가
    - ActionCategoryEnum = z.enum(['trade', 'withdraw', 'transfer', 'sign', 'deposit']) 추가 + export
    - evaluateVenueWhitelist(): venue 없음(contractCall) -> null (통과), venue 있음 + VENUE_WHITELIST 정책 있음 -> 허용 목록 체크, venue 미허용 -> DENY (VENUE_NOT_ALLOWED)
    - venue_whitelist_enabled Admin Setting false(기본) -> 정책 비활성 (venue 있어도 통과)
    - venue_whitelist_enabled Admin Setting true + VENUE_WHITELIST 미등록 + venue 있음 -> DENY (default-deny)
    - 기존 정책 평가 순서 유지, 4j 위치에 VENUE_WHITELIST 삽입
  </behavior>
  <action>
    1. packages/core/src/schemas/resolved-action.schema.ts에 ActionCategoryEnum 추가:
       ```typescript
       export const ActionCategoryEnum = z.enum(['trade', 'withdraw', 'transfer', 'sign', 'deposit']);
       export type ActionCategory = z.infer<typeof ActionCategoryEnum>;
       ```
    2. packages/core/src/index.ts에서 ActionCategoryEnum, ActionCategory export 추가

    3. RED: packages/daemon/src/__tests__/venue-whitelist-policy.test.ts 생성:
       - venue 없는 TransactionParam -> VENUE_WHITELIST 미평가 (null)
       - venue_whitelist_enabled=false -> VENUE_WHITELIST 미평가
       - venue_whitelist_enabled=true + VENUE_WHITELIST 정책 없음 + venue 있음 -> DENY (default-deny)
       - venue_whitelist_enabled=true + VENUE_WHITELIST 정책 있음 + venue 허용 -> null (통과)
       - venue_whitelist_enabled=true + VENUE_WHITELIST 정책 있음 + venue 미허용 -> DENY (VENUE_NOT_ALLOWED)
       - venue 대소문자 무시 (transform lowercase 검증)
       - 기존 SPENDING_LIMIT 정책과 함께 동작 (순서 확인)

    4. GREEN: packages/daemon/src/pipeline/database-policy-engine.ts 수정:
       a. TransactionParam 인터페이스에 신규 필드 추가:
          - venue?: string
          - actionCategory?: string (ActionCategory 타입)
          - notionalUsd?: number
          - leverage?: number
          - expiry?: string
          - hasWithdrawCapability?: boolean
       b. VenueWhitelistRules 인터페이스 추가:
          ```typescript
          interface VenueWhitelistRules {
            venues: Array<{ id: string; name?: string }>;
          }
          ```
       c. evaluateVenueWhitelist() private 메서드 추가:
          - transaction.venue 없음 -> return null (contractCall, 통과)
          - settingsService.get('venue_whitelist_enabled')가 falsy -> return null (비활성)
          - type='VENUE_WHITELIST' 정책 조회
          - 정책 없음 + venue 있음 -> DENY (default-deny, errorCode: VENUE_NOT_ALLOWED)
          - 정책 있음 + venues.some(v => v.id === transaction.venue.toLowerCase()) -> null (통과)
          - 정책 있음 + 미일치 -> DENY (VENUE_NOT_ALLOWED)
       d. evaluate() 메서드의 평가 체인에서 기존 4i-c(PERP_MAX_POSITION_USD) 이후, SPENDING_LIMIT 이전에 evaluateVenueWhitelist() 호출 삽입

    주의: CONTRACT_WHITELIST 패턴을 그대로 따르되, Admin Settings 토글(venue_whitelist_enabled)을 추가로 체크한다.
    주의: evaluateAndReserve()에서도 동일하게 evaluateVenueWhitelist() 호출 필요.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/venue-whitelist-policy.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>TransactionParam 6필드 확장, ActionCategoryEnum 5값, VENUE_WHITELIST default-deny 정책 동작, Admin Settings 토글 연동, 기존 정책 무간섭</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ACTION_CATEGORY_LIMIT 정책 + riskLevel 자동 매핑 + 기존 정책 공존 검증</name>
  <files>
    packages/daemon/src/pipeline/database-policy-engine.ts,
    packages/daemon/src/__tests__/action-category-limit-policy.test.ts,
    packages/daemon/src/__tests__/database-policy-engine.test.ts
  </files>
  <behavior>
    - ACTION_CATEGORY_LIMIT 정책이 카테고리별 per_action_limit_usd 초과 시 tier_on_exceed 반환
    - ACTION_CATEGORY_LIMIT 정책이 카테고리별 daily_limit_usd 초과 시 tier_on_exceed 반환 (누적 합산 쿼리)
    - ACTION_CATEGORY_LIMIT 정책이 카테고리별 monthly_limit_usd 초과 시 tier_on_exceed 반환 (누적 합산 쿼리)
    - tier_on_exceed 기본값 DELAY
    - notionalUsd 없는 transaction -> ACTION_CATEGORY_LIMIT 미평가
    - actionCategory 없는 transaction -> ACTION_CATEGORY_LIMIT 미평가
    - 다수 카테고리 정책 중 해당 카테고리만 평가
    - SPENDING_LIMIT과 완전 독립 (이중 차감 없음, maxTier로 합산)
    - riskLevel 없는 기존 ActionDefinition은 기존 동작 유지 (무변경)
    - 기존 database-policy-engine.test.ts 전체 통과
  </behavior>
  <action>
    1. RED: packages/daemon/src/__tests__/action-category-limit-policy.test.ts 생성:
       - actionCategory 없음 -> ACTION_CATEGORY_LIMIT 미평가
       - notionalUsd 없음 -> ACTION_CATEGORY_LIMIT 미평가
       - per_action_limit_usd=1000 + notionalUsd=500 -> null (통과)
       - per_action_limit_usd=1000 + notionalUsd=1500 -> tier_on_exceed (DELAY)
       - daily_limit_usd=5000 + 기존 누적 3000 + notionalUsd=2500 -> tier_on_exceed (초과)
       - daily_limit_usd=5000 + 기존 누적 3000 + notionalUsd=1500 -> null (미초과)
       - monthly_limit_usd=50000 + 기존 누적 45000 + notionalUsd=6000 -> tier_on_exceed
       - tier_on_exceed=APPROVAL 커스텀 값 테스트
       - trade 카테고리 정책이 withdraw 카테고리 transaction에 미적용
       - SPENDING_LIMIT + ACTION_CATEGORY_LIMIT 동시 적용: 각각 독립 평가, maxTier 합산

    2. GREEN: packages/daemon/src/pipeline/database-policy-engine.ts에 추가:
       a. ActionCategoryLimitRules 인터페이스:
          ```typescript
          interface ActionCategoryLimitRules {
            category: string;
            daily_limit_usd?: number;
            monthly_limit_usd?: number;
            per_action_limit_usd?: number;
            tier_on_exceed?: string; // PolicyTier, default 'DELAY'
          }
          ```
       b. evaluateActionCategoryLimit() private 메서드:
          - transaction.actionCategory 없음 또는 transaction.notionalUsd 없음 -> return null
          - type='ACTION_CATEGORY_LIMIT' 정책 조회, rules.category === transaction.actionCategory 필터
          - per_action_limit_usd 체크: notionalUsd > per_action_limit_usd -> tier_on_exceed 반환
          - daily_limit_usd 체크: 누적 합산 쿼리 (json_extract metadata $.notionalUsd, action_kind IN signedData/signedHttp, $.actionCategory=category, created_at >= today 00:00 UTC, status != FAILED) + notionalUsd > daily_limit_usd -> tier_on_exceed
          - monthly_limit_usd 체크: 동일 패턴, created_at >= month start
          - tier_on_exceed 기본값 'DELAY'
       c. evaluate() 평가 체인에서 4j(VENUE_WHITELIST) 이후, SPENDING_LIMIT 이전에 evaluateActionCategoryLimit() 호출 삽입
       d. evaluateAndReserve()에서도 동일

    3. POLICY-06 (riskLevel mapping): 이 매핑은 ActionDefinitionSchema에 이미 riskLevel+defaultTier가 정의되어 있고, ActionProviderRegistry에서 resolve 시점에 적용된다. 별도 정책 엔진 로직 불필요 -- 이미 Phase 386에서 구현 완료 (riskLevel 4등급 + defaultTier). 기존 동작 확인 테스트만 추가.

    4. packages/daemon/src/__tests__/database-policy-engine.test.ts 기존 테스트 전체 실행하여 regression 확인.

    주의: 누적 합산 쿼리는 raw SQL(json_extract)을 사용한다. Drizzle ORM의 json_extract 지원이 제한적이므로 this.sqlite?.prepare() 또는 sql.raw() 패턴 사용. 기존 cumulative-spending 패턴(packages/daemon/src/__tests__/cumulative-spending.test.ts) 참고.
    주의: ACTION_CATEGORY_LIMIT과 SPENDING_LIMIT은 완전 독립. on-chain amount(SPENDING_LIMIT) vs off-chain notionalUsd(ACTION_CATEGORY_LIMIT). maxTier로 최종 합산만.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/action-category-limit-policy.test.ts packages/daemon/src/__tests__/database-policy-engine.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>ACTION_CATEGORY_LIMIT daily/monthly/per_action 한도 평가 동작, tier_on_exceed 기본 DELAY, SPENDING_LIMIT과 독립 공존, riskLevel 4등급 매핑 확인, 기존 정책 테스트 전체 통과</done>
</task>

</tasks>

<verification>
1. pnpm vitest run packages/daemon/src/__tests__/venue-whitelist-policy.test.ts -- VENUE_WHITELIST 정책
2. pnpm vitest run packages/daemon/src/__tests__/action-category-limit-policy.test.ts -- ACTION_CATEGORY_LIMIT 정책
3. pnpm vitest run packages/daemon/src/__tests__/database-policy-engine.test.ts -- 기존 정책 regression
4. pnpm turbo run typecheck -- 전체 타입 체크
5. pnpm vitest run packages/daemon/src/__tests__/policy-engine-coverage-audit.test.ts -- 정책 커버리지 감사
</verification>

<success_criteria>
- TransactionParam에 6개 off-chain 필드 추가
- ActionCategoryEnum 5값 Zod enum export
- VENUE_WHITELIST default-deny 정책 동작, Admin Settings 토글 연동
- ACTION_CATEGORY_LIMIT daily/monthly/per_action 한도 평가
- tier_on_exceed 커스텀 가능 (기본 DELAY)
- SPENDING_LIMIT과 완전 독립 공존
- 기존 정책 평가 체인 무간섭
- 전체 typecheck 통과
</success_criteria>

<output>
After completion, create `.planning/phases/389-tracking-policy-extension/389-02-SUMMARY.md`
</output>
