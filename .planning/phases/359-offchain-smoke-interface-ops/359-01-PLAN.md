---
phase: 359-offchain-smoke-interface-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/e2e-tests/src/scenarios/interface-admin-mcp-sdk.ts
  - packages/e2e-tests/src/__tests__/interface-admin-mcp-sdk.e2e.test.ts
autonomous: true
requirements: [IFACE-01, IFACE-02, IFACE-03]
must_haves:
  truths:
    - "Admin UI serves HTTP 200 on root path"
    - "Admin Settings CRUD (GET/PUT) works via REST API"
    - "MCP server starts on stdio, lists tools, and responds to a basic tool call"
    - "SDK creates session, calls getWalletInfo, and calls getConnectInfo successfully"
  artifacts:
    - path: "packages/e2e-tests/src/scenarios/interface-admin-mcp-sdk.ts"
      provides: "Scenario registration for 3 interface E2E scenarios"
    - path: "packages/e2e-tests/src/__tests__/interface-admin-mcp-sdk.e2e.test.ts"
      provides: "E2E test file with Admin UI, MCP, SDK tests"
      min_lines: 100
  key_links:
    - from: "packages/e2e-tests/src/__tests__/interface-admin-mcp-sdk.e2e.test.ts"
      to: "daemon REST API /admin/settings"
      via: "E2EHttpClient GET/PUT"
      pattern: "admin/settings"
    - from: "packages/e2e-tests/src/__tests__/interface-admin-mcp-sdk.e2e.test.ts"
      to: "packages/mcp/src/index.ts"
      via: "child_process spawn stdio"
      pattern: "spawn.*mcp"
---

<objective>
Admin UI, MCP server, SDK interface E2E tests -- verify that non-REST interfaces are accessible and functional.

Purpose: Ensures the 3 primary consumer interfaces (Admin UI, MCP for Claude, SDK for programmatic access) work end-to-end against a real daemon.
Output: 1 scenario registration file + 1 test file with 3 describe blocks.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/e2e-tests/src/scenarios/core-auth-wallet-session.ts
@packages/e2e-tests/src/__tests__/core-auth-wallet-session.e2e.test.ts
@packages/e2e-tests/src/helpers/index.ts
@packages/e2e-tests/src/helpers/session.ts
@packages/e2e-tests/src/helpers/http-client.ts
@packages/e2e-tests/src/types.ts
@packages/mcp/src/index.ts

<interfaces>
<!-- Key types and contracts from existing E2E infrastructure -->

From packages/e2e-tests/src/types.ts:
```typescript
export interface E2EScenario {
  id: string; name: string; track: Track; category: string; description: string;
  networks?: string[]; protocols?: string[];
}
export const registry = new ScenarioRegistry();
```

From packages/e2e-tests/src/helpers/index.ts:
```typescript
export { DaemonManager, type DaemonInstance } from './daemon-lifecycle.js';
export { PushRelayManager, type PushRelayInstance } from './push-relay-lifecycle.js';
export { E2EHttpClient, type HttpResponse } from './http-client.js';
export { SessionManager, setupDaemonSession } from './session.js';
```

From packages/e2e-tests/src/helpers/session.ts:
```typescript
export async function setupDaemonSession(baseUrl: string, masterPassword?: string):
  Promise<{ session: SessionManager; http: E2EHttpClient; token: string }>;
```

Admin Settings API:
- GET /v1/admin/settings (masterAuth) -> returns all settings object
- PUT /v1/admin/settings (masterAuth, body: { key: value, ... }) -> updates settings

MCP server:
- packages/mcp/src/index.ts runs via stdio transport
- Env vars: WAIAAS_BASE_URL, WAIAAS_SESSION_TOKEN
- Uses @modelcontextprotocol/sdk, responds to tools/list and tools/call
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 3 interface scenarios + create E2E test file</name>
  <files>packages/e2e-tests/src/scenarios/interface-admin-mcp-sdk.ts, packages/e2e-tests/src/__tests__/interface-admin-mcp-sdk.e2e.test.ts</files>
  <action>
1. Create scenario registration file `packages/e2e-tests/src/scenarios/interface-admin-mcp-sdk.ts`:
   - Register 3 scenarios in global registry (follow core-auth-wallet-session.ts pattern):
     - id: 'admin-ui-settings', name: 'Admin UI + Settings', track: 'offchain', category: 'interface', description: 'Admin UI HTTP 200 access + Settings GET/PUT CRUD'
     - id: 'mcp-stdio-tools', name: 'MCP stdio Tools', track: 'offchain', category: 'interface', description: 'MCP server stdio connection -> tools/list -> basic tool call'
     - id: 'sdk-connectivity', name: 'SDK Connectivity', track: 'offchain', category: 'interface', description: 'SDK createSession -> getWalletInfo -> getConnectInfo'

2. Create test file `packages/e2e-tests/src/__tests__/interface-admin-mcp-sdk.e2e.test.ts`:
   - Import and use DaemonManager, setupDaemonSession, E2EHttpClient from helpers
   - Import scenario registration (side-effect)
   - beforeAll: start daemon via DaemonManager
   - afterAll: stop daemon

   **describe('admin-ui-settings')** (IFACE-01):
   - it('serves Admin UI at root path'): GET / to daemon baseUrl, expect status 200, expect content-type to include 'text/html'
   - it('reads current settings via GET /v1/admin/settings'): use adminClient with X-Master-Password header, GET /v1/admin/settings, expect 200, expect body to be an object
   - it('updates a setting via PUT and reads back'): PUT /v1/admin/settings with body { display_currency: 'KRW' }, expect 200. Then GET /v1/admin/settings, expect display_currency to be 'KRW'. Reset to 'USD' afterward.

   **describe('mcp-stdio-tools')** (IFACE-02):
   - it('connects via stdio, lists tools, and calls get-balance'):
     Use @modelcontextprotocol/sdk Client class with StdioClientTransport to spawn the MCP server as a child process.
     - Create a session first (via setupDaemonSession) to get a token
     - Spawn `node packages/mcp/dist/index.js` with env WAIAAS_BASE_URL=daemon.baseUrl, WAIAAS_SESSION_TOKEN=token
     - Use `import { Client } from '@modelcontextprotocol/sdk/client/index.js'` and `import { StdioClientTransport } from '@modelcontextprotocol/sdk/client/stdio.js'`
     - Create StdioClientTransport with command: 'node', args: ['packages/mcp/dist/index.js'], env vars
     - Create Client({ name: 'e2e-test', version: '1.0.0' }), call client.connect(transport)
     - Call client.listTools() -- expect result.tools to be an array with length > 0
     - Call client.callTool({ name: 'get-balance', arguments: {} }) -- expect a result (may be error about no wallet but should not crash)
     - Close client and transport in finally block
     NOTE: If @modelcontextprotocol/sdk is not available in e2e-tests package, add it as a devDependency. Check packages/mcp/package.json for the version.

   **describe('sdk-connectivity')** (IFACE-03):
   - it('creates session via SDK and calls getWalletInfo + getConnectInfo'):
     - First create a wallet + session via REST (setupDaemonSession)
     - Import WAIaaSClient from '@waiaas/sdk' (or use the path packages/sdk)
     - Create WAIaaSClient({ baseUrl: daemon.baseUrl, sessionToken: token })
     - Call client.getWalletInfo() -- expect result to have walletId
     - Call client.getConnectInfo() -- expect result to have session info and wallets array
     NOTE: If @waiaas/sdk is not available in e2e-tests package, add it as a devDependency.

   IMPORTANT: Check e2e-tests package.json for existing dependencies. If @modelcontextprotocol/sdk or @waiaas/sdk are not listed, add them. Use workspace protocol for @waiaas/sdk: "workspace:*". For @modelcontextprotocol/sdk, check packages/mcp/package.json for the exact version.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run build --filter=@waiaas/e2e-tests && node -e "require('./packages/e2e-tests/dist/scenarios/interface-admin-mcp-sdk.js')"</automated>
  </verify>
  <done>3 scenarios registered, test file compiles. Admin UI test checks HTTP 200 + Settings CRUD. MCP test spawns stdio server, lists tools, calls get-balance. SDK test creates WAIaaSClient, calls getWalletInfo + getConnectInfo.</done>
</task>

</tasks>

<verification>
- `pnpm turbo run build --filter=@waiaas/e2e-tests` succeeds
- Scenario file exports 3 registered scenarios (admin-ui-settings, mcp-stdio-tools, sdk-connectivity)
- Test file follows established pattern (DaemonManager beforeAll/afterAll, setupDaemonSession, adminHeaders)
</verification>

<success_criteria>
- Admin UI test verifies HTTP 200 response with HTML content-type
- Admin Settings test verifies GET/PUT CRUD cycle
- MCP test spawns stdio server, connects via Client, lists tools (>0), calls a tool
- SDK test creates WAIaaSClient with session token, calls getWalletInfo and getConnectInfo
</success_criteria>

<output>
After completion, create `.planning/phases/359-offchain-smoke-interface-ops/359-01-SUMMARY.md`
</output>
