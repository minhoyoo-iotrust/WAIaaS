---
phase: 256-staking-api-async-tracking-interface-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/lido-staking/withdrawal-tracker.ts
  - packages/actions/src/providers/jito-staking/epoch-tracker.ts
  - packages/actions/src/index.ts
  - packages/daemon/src/lifecycle/daemon.ts
  - packages/daemon/src/services/async-polling-service.ts
  - packages/actions/src/__tests__/staking-trackers.test.ts
  - packages/core/src/enums/notification.ts
  - packages/core/src/i18n/en.ts
  - packages/core/src/i18n/ko.ts
  - packages/daemon/src/__tests__/async-polling-service.test.ts
autonomous: true
requirements: [ASYNC-01, ASYNC-02, ASYNC-03, ASYNC-04]

must_haves:
  truths:
    - "Lido unstake 후 Withdrawal Queue 상태가 AsyncPollingService로 폴링된다"
    - "Jito unstake 후 에포크 경계 완료가 AsyncPollingService로 폴링된다"
    - "unstake 완료 시 STAKING_UNSTAKE_COMPLETED 알림이 발행된다"
    - "unstake 타임아웃 시 STAKING_UNSTAKE_TIMEOUT 알림이 발행된다"
  artifacts:
    - path: "packages/actions/src/providers/lido-staking/withdrawal-tracker.ts"
      provides: "LidoWithdrawalTracker implementing IAsyncStatusTracker"
      exports: ["LidoWithdrawalTracker"]
    - path: "packages/actions/src/providers/jito-staking/epoch-tracker.ts"
      provides: "JitoEpochTracker implementing IAsyncStatusTracker"
      exports: ["JitoEpochTracker"]
    - path: "packages/actions/src/__tests__/staking-trackers.test.ts"
      provides: "Unit tests for both staking trackers"
      min_lines: 80
    - path: "packages/core/src/enums/notification.ts"
      provides: "STAKING_UNSTAKE_COMPLETED and STAKING_UNSTAKE_TIMEOUT in NOTIFICATION_EVENT_TYPES"
      contains: "STAKING_UNSTAKE_COMPLETED"
    - path: "packages/daemon/src/__tests__/async-polling-service.test.ts"
      provides: "Tests for dynamic eventType dispatching"
      contains: "STAKING_UNSTAKE_COMPLETED"
  key_links:
    - from: "packages/daemon/src/lifecycle/daemon.ts"
      to: "packages/actions/src/providers/lido-staking/withdrawal-tracker.ts"
      via: "registerTracker(new LidoWithdrawalTracker(...))"
      pattern: "registerTracker.*LidoWithdrawalTracker"
    - from: "packages/daemon/src/lifecycle/daemon.ts"
      to: "packages/actions/src/providers/jito-staking/epoch-tracker.ts"
      via: "registerTracker(new JitoEpochTracker(...))"
      pattern: "registerTracker.*JitoEpochTracker"
    - from: "packages/daemon/src/services/async-polling-service.ts"
      to: "notification callbacks"
      via: "emitNotification('STAKING_UNSTAKE_COMPLETED'|'STAKING_UNSTAKE_TIMEOUT')"
      pattern: "STAKING_UNSTAKE_"
    - from: "packages/core/src/enums/notification.ts"
      to: "packages/daemon/src/services/async-polling-service.ts"
      via: "NotificationEventType includes STAKING_UNSTAKE_* values"
      pattern: "STAKING_UNSTAKE_"
---

<objective>
Lido Withdrawal Queue 폴러와 Jito Epoch 폴러를 IAsyncStatusTracker 구현체로 생성하고, 데몬 라이프사이클에 등록하여 unstake 비동기 완료를 추적한다. 완료/타임아웃 시 알림 이벤트를 발행한다.

Purpose: unstake 작업이 블록체인에서 완료되기까지 수 시간~수 일 소요되므로, 자동 폴링으로 상태를 추적하고 사용자에게 알림을 보낸다.
Output: LidoWithdrawalTracker, JitoEpochTracker, daemon registration, notification enum registration, unit tests
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/actions/src/common/async-status-tracker.ts
@packages/actions/src/providers/lifi/bridge-status-tracker.ts
@packages/daemon/src/services/async-polling-service.ts
@packages/daemon/src/lifecycle/daemon.ts
@packages/actions/src/index.ts
@packages/core/src/enums/notification.ts
@packages/core/src/i18n/en.ts
@packages/core/src/i18n/ko.ts
@packages/daemon/src/__tests__/async-polling-service.test.ts

@.planning/phases/254-lido-evm-staking-provider/254-01-SUMMARY.md
@.planning/phases/255-jito-solana-staking-provider/255-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LidoWithdrawalTracker + JitoEpochTracker 구현</name>
  <files>
    packages/actions/src/providers/lido-staking/withdrawal-tracker.ts
    packages/actions/src/providers/jito-staking/epoch-tracker.ts
    packages/actions/src/index.ts
    packages/actions/src/__tests__/staking-trackers.test.ts
  </files>
  <action>
두 개의 IAsyncStatusTracker 구현체를 생성한다.

**LidoWithdrawalTracker** (`packages/actions/src/providers/lido-staking/withdrawal-tracker.ts`):
- `implements IAsyncStatusTracker` (import from `../../common/async-status-tracker.js`)
- `name = 'lido-withdrawal'`
- `maxAttempts = 480` (480 x 30s = 4 hours active polling; Lido withdrawal takes 1-5 days, but active phase polls aggressively)
- `pollIntervalMs = 30_000` (30초)
- `timeoutTransition = 'TIMEOUT'` (terminal -- no two-phase monitoring for staking unlike bridges)
- `checkStatus(txId, metadata)`: Lido Withdrawal Queue는 on-chain 상태이므로, metadata에서 `requestId`와 `withdrawalQueueAddress`를 읽어 EVM RPC로 `isFinalized` 상태를 조회한다. 단, 외부 RPC 호출은 실제 구현이 복잡하므로 metadata-based 간이 방식을 사용: metadata.status가 이미 'claimable'이면 COMPLETED 반환, metadata.withdrawalRequestedAt 기반으로 예상 시간 5일 경과 시 TIMEOUT hint를 details에 포함하며 PENDING 유지. 이는 checkStatus가 아직 체인 조회 없이 메타데이터 기반으로 동작하는 v1 구현이다.
- 실제로는 metadata에 `tracker: 'lido-withdrawal'`가 기록되어야 AsyncPollingService가 이 tracker를 resolve한다.
- COMPLETED 시 details에 `{ completedAt: Date.now(), protocol: 'lido', notificationEvent: 'STAKING_UNSTAKE_COMPLETED' }` 포함
- PENDING 시 details에 `{ estimatedDaysRemaining: number, protocol: 'lido' }` 포함

**JitoEpochTracker** (`packages/actions/src/providers/jito-staking/epoch-tracker.ts`):
- `implements IAsyncStatusTracker`
- `name = 'jito-epoch'`
- `maxAttempts = 240` (240 x 30s = 2 hours; Solana epoch boundary ~2-3일이지만 active polling은 2시간)
- `pollIntervalMs = 30_000` (30초)
- `timeoutTransition = 'TIMEOUT'` (terminal)
- `checkStatus(txId, metadata)`: metadata에서 `targetEpoch`를 읽고, 현재 에포크와 비교하여 경과 여부를 판단. metadata.status가 'deactivated'이면 COMPLETED. metadata.unstakeRequestedAt 기반으로 예상 시간(~3일) 경과 시 정보를 details에 포함. v1은 metadata 기반 추적.
- COMPLETED 시 details에 `{ completedAt: Date.now(), protocol: 'jito', notificationEvent: 'STAKING_UNSTAKE_COMPLETED' }` 포함

**packages/actions/src/index.ts**:
- `export { LidoWithdrawalTracker } from './providers/lido-staking/withdrawal-tracker.js'`
- `export { JitoEpochTracker } from './providers/jito-staking/epoch-tracker.js'`

**Unit tests** (`packages/actions/src/__tests__/staking-trackers.test.ts`):
BridgeStatusTracker 테스트 패턴을 따라 최소 10개 테스트:
- LidoWithdrawalTracker: name/maxAttempts/pollIntervalMs/timeoutTransition 값 확인, checkStatus PENDING 반환 (기본 metadata), checkStatus COMPLETED 반환 (metadata.status='claimable'), checkStatus details에 protocol:'lido' 포함, COMPLETED details에 notificationEvent:'STAKING_UNSTAKE_COMPLETED' 포함
- JitoEpochTracker: name/maxAttempts/pollIntervalMs/timeoutTransition 값 확인, checkStatus PENDING 반환 (기본 metadata), checkStatus COMPLETED 반환 (metadata.status='deactivated'), checkStatus details에 protocol:'jito' 포함, COMPLETED details에 notificationEvent:'STAKING_UNSTAKE_COMPLETED' 포함
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions run test -- --run staking-trackers 2>&1 | tail -20</automated>
  </verify>
  <done>LidoWithdrawalTracker와 JitoEpochTracker가 IAsyncStatusTracker를 구현하고, 10개 이상 unit test가 통과한다. @waiaas/actions에서 export된다.</done>
</task>

<task type="auto">
  <name>Task 2: 알림 이벤트 등록 + 데몬 라이프사이클 tracker 등록 + 동적 이벤트 연동 + 테스트</name>
  <files>
    packages/core/src/enums/notification.ts
    packages/core/src/i18n/en.ts
    packages/core/src/i18n/ko.ts
    packages/daemon/src/lifecycle/daemon.ts
    packages/daemon/src/services/async-polling-service.ts
    packages/daemon/src/__tests__/async-polling-service.test.ts
  </files>
  <action>
**1. NotificationEventType enum 확장** (`packages/core/src/enums/notification.ts`):
NOTIFICATION_EVENT_TYPES 배열의 `'BRIDGE_REFUNDED'` 항목 뒤에 2개 추가:
```typescript
'STAKING_UNSTAKE_COMPLETED',
'STAKING_UNSTAKE_TIMEOUT',
```
이렇게 하면 NotificationEventType 유니온 타입에 자동 포함되어 emitNotification 호출 시 TypeScript 컴파일 에러가 발생하지 않는다.

**2. i18n 템플릿 추가**:

`packages/core/src/i18n/en.ts` notifications 섹션에 (BRIDGE_REFUNDED 항목 뒤):
```typescript
STAKING_UNSTAKE_COMPLETED: { title: 'Staking Unstake Completed', body: 'Unstake request {txId} has been completed. Protocol: {protocol}. Funds are now available for withdrawal.' },
STAKING_UNSTAKE_TIMEOUT: { title: 'Staking Unstake Timeout', body: 'Unstake request {txId} timed out after monitoring. Protocol: {protocol}. Manual verification may be required.' },
```

`packages/core/src/i18n/ko.ts` notifications 섹션에 (BRIDGE_REFUNDED 항목 뒤):
```typescript
STAKING_UNSTAKE_COMPLETED: { title: '스테이킹 언스테이크 완료', body: '언스테이크 요청 {txId}이(가) 완료되었습니다. 프로토콜: {protocol}. 출금이 가능합니다.' },
STAKING_UNSTAKE_TIMEOUT: { title: '스테이킹 언스테이크 시간 초과', body: '언스테이크 요청 {txId}이(가) 모니터링 후 시간 초과되었습니다. 프로토콜: {protocol}. 수동 확인이 필요할 수 있습니다.' },
```

**3. daemon.ts tracker 등록** — 기존 패턴(Step 4f-2 Bridge tracker registration)을 따라 staking tracker를 등록:

**Step 4f-3: Register staking status trackers** (Step 4f-2 다음에 추가):

```typescript
// Step 4f-3: Register staking status trackers when lido/jito is enabled
// Settings keys defined in Phase 254 (LidoStakingActionProvider) and Phase 255 (JitoStakingActionProvider)
// @see packages/daemon/src/infrastructure/settings/default-settings.ts — 'actions.lido_staking_enabled', 'actions.jito_staking_enabled'
if (this._asyncPollingService) {
  try {
    if (this._settingsService?.get('actions.lido_staking_enabled') === 'true') {
      const { LidoWithdrawalTracker } = await import('@waiaas/actions');
      this._asyncPollingService.registerTracker(new LidoWithdrawalTracker());
      console.debug('Step 4f-3: Lido withdrawal tracker registered');
    }
    if (this._settingsService?.get('actions.jito_staking_enabled') === 'true') {
      const { JitoEpochTracker } = await import('@waiaas/actions');
      this._asyncPollingService.registerTracker(new JitoEpochTracker());
      console.debug('Step 4f-3: Jito epoch tracker registered');
    }
  } catch (err) {
    console.warn('Step 4f-3 (fail-soft): Staking tracker registration failed:', err);
  }
}
```

**4. AsyncPollingService 동적 이벤트 타입 결정** (`packages/daemon/src/services/async-polling-service.ts`):

스테이킹 전용 이벤트를 위해 tracker의 checkStatus details에 포함된 `notificationEvent` 키를 우선 사용하도록 processResult와 handleTimeout을 최소 수정한다. 기존 bridge 동작은 notificationEvent를 설정하지 않으므로 기존 BRIDGE_* 이벤트를 그대로 사용한다.

processResult의 COMPLETED 케이스 (line ~271):
```typescript
// Determine notification event type — staking trackers set notificationEvent in details
const eventType = (updatedMetadata.notificationEvent as string) ?? (isRefunded ? 'BRIDGE_REFUNDED' : 'BRIDGE_COMPLETED');
```

processResult의 FAILED 케이스 (line ~296):
```typescript
const eventType = (updatedMetadata.notificationEvent as string) ?? 'BRIDGE_FAILED';
```

handleTimeout의 TIMEOUT 케이스 (line ~226, else 블록 내부):
```typescript
// Staking trackers include notificationEvent in metadata (e.g., 'STAKING_UNSTAKE_TIMEOUT')
const eventType = (metadata.notificationEvent as string) ?? 'BRIDGE_TIMEOUT';
```

각 케이스에서 하드코딩된 이벤트명 대신 `eventType` 변수를 사용하여 `emitNotification`을 호출한다.

LidoWithdrawalTracker와 JitoEpochTracker의 checkStatus는 COMPLETED 시 `details.notificationEvent = 'STAKING_UNSTAKE_COMPLETED'`를 반환하고, 초기 metadata에 `notificationEvent: 'STAKING_UNSTAKE_TIMEOUT'`을 포함하여 timeout 시 해당 이벤트가 사용되도록 한다.

**5. 동적 eventType 로직 테스트** (`packages/daemon/src/__tests__/async-polling-service.test.ts`):

기존 테스트 파일에 새 describe 블록 추가 — 'dynamic notification event type':
- **Test: staking tracker COMPLETED emits custom notificationEvent** — tracker.checkStatus가 `{ state: 'COMPLETED', details: { notificationEvent: 'STAKING_UNSTAKE_COMPLETED', protocol: 'lido' } }`를 반환할 때, emitNotification 콜백이 `'STAKING_UNSTAKE_COMPLETED'`로 호출되는지 검증. 기존 테스트 패턴 따라 in-memory DB + mock tracker + spy callbacks 사용.
- **Test: staking tracker TIMEOUT emits custom notificationEvent** — metadata에 `notificationEvent: 'STAKING_UNSTAKE_TIMEOUT'`이 포함된 TX가 maxAttempts 초과 시, emitNotification이 `'STAKING_UNSTAKE_TIMEOUT'`으로 호출되는지 검증.
- **Test: bridge tracker without notificationEvent falls back to BRIDGE_COMPLETED** — notificationEvent가 없는 기존 bridge tracker는 여전히 `'BRIDGE_COMPLETED'`로 emitNotification이 호출되는지 검증 (회귀 테스트).

**수정 대상 파일**: notification.ts (enum), en.ts/ko.ts (i18n), daemon.ts (tracker 등록), async-polling-service.ts (동적 이벤트), async-polling-service.test.ts (새 테스트)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon run test -- --run async-polling-service 2>&1 | tail -30 && pnpm --filter @waiaas/core run typecheck 2>&1 | tail -10 && pnpm --filter @waiaas/daemon run typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>STAKING_UNSTAKE_COMPLETED와 STAKING_UNSTAKE_TIMEOUT이 NotificationEventType에 등록되고 i18n 템플릿이 en/ko 모두 추가됨. lido_staking_enabled/jito_staking_enabled 설정이 true일 때 LidoWithdrawalTracker/JitoEpochTracker가 daemon 시작 시 등록된다. AsyncPollingService가 staking tracker의 notificationEvent를 사용하여 STAKING_UNSTAKE_COMPLETED/TIMEOUT 알림을 발행한다. 동적 이벤트 로직 테스트 3개가 통과하고, 기존 bridge 테스트에 회귀 없음.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/actions run test -- --run staking-trackers` - 10개+ 테스트 통과
2. `pnpm --filter @waiaas/daemon run test -- --run async-polling-service` - 기존 테스트 + 동적 이벤트 테스트 3개 통과
3. `pnpm --filter @waiaas/core run typecheck` - STAKING_UNSTAKE_* 타입 에러 없음
4. `pnpm --filter @waiaas/actions run typecheck` - 타입 에러 없음
5. `pnpm --filter @waiaas/daemon run typecheck` - 타입 에러 없음
</verification>

<success_criteria>
- STAKING_UNSTAKE_COMPLETED와 STAKING_UNSTAKE_TIMEOUT이 NOTIFICATION_EVENT_TYPES에 등록됨
- en.ts/ko.ts에 해당 이벤트 i18n 템플릿이 존재
- LidoWithdrawalTracker가 IAsyncStatusTracker를 구현하고 name='lido-withdrawal'
- JitoEpochTracker가 IAsyncStatusTracker를 구현하고 name='jito-epoch'
- 두 tracker가 @waiaas/actions에서 export됨
- daemon.ts에서 settings에 따라 조건부 등록 (settings keys from Phase 254/255)
- STAKING_UNSTAKE_COMPLETED/TIMEOUT 알림 이벤트가 동적으로 발행됨
- 동적 eventType 로직 테스트 3개 통과
- 기존 bridge tracker 동작에 영향 없음 (회귀 테스트 통과)
</success_criteria>

<output>
After completion, create `.planning/phases/256-staking-api-async-tracking-interface-integration/256-01-SUMMARY.md`
</output>
