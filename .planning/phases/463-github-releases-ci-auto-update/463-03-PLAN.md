---
phase: 463-github-releases-ci-auto-update
plan: 03
type: execute
wave: 2
depends_on: [463-01]
files_modified:
  - packages/admin-ui/src/desktop/update-checker.ts
  - packages/admin-ui/src/desktop/UpdateBanner.tsx
  - packages/admin-ui/src/app.tsx
  - packages/admin-ui/package.json
autonomous: false
requirements: [UPDT-01, UPDT-02, UPDT-03]

must_haves:
  truths:
    - "앱 시작 시 GitHub Releases latest.json에서 최신 버전을 자동 확인한다"
    - "새 버전이 있으면 사용자에게 업데이트 배너를 표시하고, 확인 후 다운로드 + 설치한다"
    - "변조된 바이너리의 업데이트가 Ed25519 서명 검증 실패로 거부된다"
  artifacts:
    - path: "packages/admin-ui/src/desktop/update-checker.ts"
      provides: "Tauri updater API 래퍼 — check + download + install"
      min_lines: 30
    - path: "packages/admin-ui/src/desktop/UpdateBanner.tsx"
      provides: "업데이트 알림 UI 컴포넌트"
      min_lines: 25
  key_links:
    - from: "packages/admin-ui/src/desktop/update-checker.ts"
      to: "@tauri-apps/plugin-updater"
      via: "dynamic import (tree-shaking)"
      pattern: "import.*plugin-updater"
    - from: "packages/admin-ui/src/desktop/UpdateBanner.tsx"
      to: "packages/admin-ui/src/desktop/update-checker.ts"
      via: "checkForUpdate + installUpdate"
      pattern: "checkForUpdate|installUpdate"
    - from: "packages/admin-ui/src/app.tsx"
      to: "packages/admin-ui/src/desktop/UpdateBanner.tsx"
      via: "dynamic import + isDesktop() guard"
      pattern: "UpdateBanner"
---

<objective>
자동 업데이트 UI를 Admin Web UI에 통합하여, Desktop 앱이 시작 시 새 버전을 확인하고 사용자 확인 후 업데이트를 적용한다.

Purpose: 사용자가 수동 다운로드 없이 앱 내에서 업데이트 확인 + 설치 가능
Output: update-checker.ts + UpdateBanner.tsx + App.tsx 통합
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@apps/desktop/src-tauri/tauri.conf.json
@packages/admin-ui/src/app.tsx

<interfaces>
From Phase 461 (IPC Bridge):
- packages/admin-ui/src/desktop/ipc-bridge.ts: isDesktop() 함수, getInvoke() 래퍼
- 4-layer tree-shaking: dynamic import + rollup externals + __DESKTOP__ define + CI verification
- Desktop 전용 코드는 packages/admin-ui/src/desktop/ 디렉토리에 위치

Tauri updater plugin JS API (@tauri-apps/plugin-updater):
- check(): Promise<Update | null> — latest.json에서 버전 확인
- Update.download(): 업데이트 다운로드
- Update.install(): 재시작 + 설치
- Ed25519 서명 검증은 Rust 측에서 자동 수행 (pubkey는 tauri.conf.json에 설정)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: update-checker.ts + UpdateBanner.tsx + App.tsx 통합</name>
  <files>
    packages/admin-ui/src/desktop/update-checker.ts,
    packages/admin-ui/src/desktop/UpdateBanner.tsx,
    packages/admin-ui/src/app.tsx,
    packages/admin-ui/package.json
  </files>
  <action>
1. **package.json**: `@tauri-apps/plugin-updater` optional peer dependency 추가 (tree-shaking 패턴 — Phase 461과 동일)

2. **update-checker.ts** 작성 (desktop/ 디렉토리):
   ```typescript
   // Dynamic import for tree-shaking — only loads in Desktop environment
   import { isDesktop } from './ipc-bridge';

   export interface UpdateInfo {
     version: string;
     date?: string;
     body?: string;
   }

   export interface UpdateState {
     available: boolean;
     info: UpdateInfo | null;
     downloading: boolean;
     progress: number;
     error: string | null;
   }

   let cachedUpdate: any = null;

   export async function checkForUpdate(): Promise<UpdateInfo | null> {
     if (!isDesktop()) return null;
     try {
       const { check } = await import('@tauri-apps/plugin-updater');
       const update = await check();
       if (update) {
         cachedUpdate = update;
         return {
           version: update.version,
           date: update.date,
           body: update.body,
         };
       }
       return null;
     } catch (e) {
       console.error('[updater] check failed:', e);
       return null;
     }
   }

   export async function installUpdate(
     onProgress?: (progress: number) => void
   ): Promise<void> {
     if (!cachedUpdate) throw new Error('No update available');
     let downloaded = 0;
     let contentLength = 0;
     await cachedUpdate.downloadAndInstall((event: any) => {
       if (event.event === 'Started') {
         contentLength = event.data.contentLength ?? 0;
       } else if (event.event === 'Progress') {
         downloaded += event.data.chunkLength;
         if (contentLength > 0 && onProgress) {
           onProgress(Math.round((downloaded / contentLength) * 100));
         }
       } else if (event.event === 'Finished') {
         onProgress?.(100);
       }
     });
     // App will restart automatically after install
   }
   ```

3. **UpdateBanner.tsx** 작성:
   Preact 컴포넌트. 앱 상단에 업데이트 알림 배너를 표시.
   - 상태: `idle` -> `available` -> `downloading` -> `installing`
   - "available" 상태에서 "Update to v{version}" 버튼 + "Later" dismiss 링크
   - "downloading" 상태에서 프로그레스 바 (%) 표시
   - 스타일: 기존 Admin UI와 일관된 배너 (배경 파란색, 흰색 텍스트)
   - `useEffect`로 마운트 시 `checkForUpdate()` 호출
   - tree-shaking: isDesktop() guard 내부에서만 렌더링
   ```tsx
   import { h } from 'preact';
   import { useState, useEffect } from 'preact/hooks';
   import { checkForUpdate, installUpdate, UpdateInfo } from './update-checker';

   export function UpdateBanner() {
     const [info, setInfo] = useState<UpdateInfo | null>(null);
     const [status, setStatus] = useState<'idle' | 'available' | 'downloading' | 'done'>('idle');
     const [progress, setProgress] = useState(0);
     const [dismissed, setDismissed] = useState(false);

     useEffect(() => {
       checkForUpdate().then(update => {
         if (update) {
           setInfo(update);
           setStatus('available');
         }
       });
     }, []);

     if (status === 'idle' || dismissed || !info) return null;

     const handleUpdate = async () => {
       setStatus('downloading');
       try {
         await installUpdate(p => setProgress(p));
         setStatus('done');
       } catch (e) {
         console.error('[updater] install failed:', e);
         setStatus('available');
       }
     };

     return (
       <div style={{
         background: '#2563eb', color: 'white', padding: '8px 16px',
         display: 'flex', alignItems: 'center', justifyContent: 'space-between',
         fontSize: '14px'
       }}>
         {status === 'available' && (
           <>
             <span>New version {info.version} is available</span>
             <div>
               <button onClick={handleUpdate} style={{ /* white bg blue text */ }}>
                 Update now
               </button>
               <button onClick={() => setDismissed(true)} style={{ /* transparent */ }}>
                 Later
               </button>
             </div>
           </>
         )}
         {status === 'downloading' && (
           <>
             <span>Downloading update... {progress}%</span>
             <div style={{ width: '200px', height: '4px', background: 'rgba(255,255,255,0.3)', borderRadius: '2px' }}>
               <div style={{ width: `${progress}%`, height: '100%', background: 'white', borderRadius: '2px' }} />
             </div>
           </>
         )}
         {status === 'done' && <span>Update installed. Restarting...</span>}
       </div>
     );
   }
   ```

4. **app.tsx** 수정:
   Desktop 환경에서만 UpdateBanner를 동적 임포트하여 렌더링.
   기존 Phase 462의 동적 임포트 패턴을 따른다:
   ```tsx
   // In app.tsx, inside component body:
   const [UpdateBannerComp, setUpdateBanner] = useState<ComponentType | null>(null);

   useEffect(() => {
     if (isDesktop()) {
       import('./desktop/UpdateBanner').then(m => setUpdateBanner(() => m.UpdateBanner));
     }
   }, []);

   // In render, before main content:
   {UpdateBannerComp && <UpdateBannerComp />}
   ```
   주의: app.tsx의 기존 구조를 유지하며, Wizard 동적 임포트와 동일한 패턴 사용. 정확한 기존 코드 패턴을 먼저 읽은 후 통합할 것.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && test -f packages/admin-ui/src/desktop/update-checker.ts && test -f packages/admin-ui/src/desktop/UpdateBanner.tsx && grep -q "UpdateBanner" packages/admin-ui/src/app.tsx && grep -q "checkForUpdate" packages/admin-ui/src/desktop/update-checker.ts && grep -q "installUpdate" packages/admin-ui/src/desktop/update-checker.ts && echo "PASS"</automated>
  </verify>
  <done>
    - update-checker.ts가 @tauri-apps/plugin-updater를 dynamic import로 로드 (UPDT-01)
    - UpdateBanner.tsx가 버전 확인 -> 사용자 확인 -> 다운로드 + 설치 플로우 제공 (UPDT-02)
    - Ed25519 서명 검증은 Rust updater 플러그인이 자동 수행 — pubkey 불일치 시 다운로드 거부 (UPDT-03)
    - App.tsx에서 isDesktop() guard + dynamic import로 tree-shaking 유지
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 463 전체 인프라 검증</name>
  <files>
    .github/workflows/desktop-release.yml,
    apps/desktop/src-tauri/tauri.conf.json,
    packages/admin-ui/src/desktop/update-checker.ts,
    packages/admin-ui/src/desktop/UpdateBanner.tsx
  </files>
  <action>
    Desktop 앱 자동 업데이트 전체 인프라 검증:
    1. Tauri updater 플러그인 통합 (Rust + JS)
    2. GitHub Actions 3-platform 빌드 매트릭스 (desktop-release.yml)
    3. 업데이트 확인 + 다운로드 UI (UpdateBanner)
  </action>
  <verify>
    1. `.github/workflows/desktop-release.yml` 워크플로우 파일 확인 — 4개 매트릭스(macOS arm64/x64, Windows, Linux) 구성 확인
    2. `apps/desktop/src-tauri/tauri.conf.json`에서 plugins.updater 설정 확인 (endpoints, pubkey)
    3. `packages/admin-ui/src/desktop/update-checker.ts` 코드 리뷰 — dynamic import 패턴 확인
    4. `packages/admin-ui/src/desktop/UpdateBanner.tsx` — UI 렌더링 확인
    5. (선택) `pnpm tauri build` 로컬 빌드 가능 여부 확인 (Rust 컴파일 필요)

    **GitHub Secrets 설정 필요 (실제 배포 시):**
    - `TAURI_SIGNING_PRIVATE_KEY`: Ed25519 비밀키 (generate-updater-key.sh로 생성)
    - `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`: 키 패스워드 (설정한 경우)
    - macOS 사이닝: `APPLE_CERTIFICATE`, `APPLE_CERTIFICATE_PASSWORD`, `APPLE_SIGNING_IDENTITY`, `APPLE_ID`, `APPLE_PASSWORD`, `APPLE_TEAM_ID`
  </verify>
  <done>사용자가 전체 인프라를 검토하고 approved 또는 수정 사항을 제시한다</done>
</task>

</tasks>

<verification>
- update-checker.ts가 dynamic import로 @tauri-apps/plugin-updater 로드
- UpdateBanner.tsx가 check -> download -> install 플로우 제공
- App.tsx에서 isDesktop() guard 내에서만 UpdateBanner 렌더링
- 브라우저 빌드에 updater 코드가 포함되지 않음 (tree-shaking)
</verification>

<success_criteria>
Desktop 앱이 시작 시 GitHub Releases의 latest.json에서 최신 버전을 확인하고, 새 버전이 있으면 배너를 표시하여 사용자 확인 후 다운로드 + 설치 + 재시작을 수행한다. 변조된 바이너리는 Ed25519 서명 검증 실패로 거부된다.
</success_criteria>

<output>
After completion, create `.planning/phases/463-github-releases-ci-auto-update/463-03-SUMMARY.md`
</output>
