---
phase: 196-readme-sdk-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/README.md
  - packages/cli/package.json
  - packages/sdk/README.md
  - packages/sdk/package.json
autonomous: true
requirements:
  - SDK-01
  - SDK-02

must_haves:
  truths:
    - "CLI npm package page shows installation command and quickstart usage"
    - "SDK npm package page shows installation command, client setup, and basic API call example"
    - "CLI package.json files field includes README.md"
    - "SDK package.json files field includes README.md"
  artifacts:
    - path: "packages/cli/README.md"
      provides: "CLI npm package README with install + quickstart"
      min_lines: 20
    - path: "packages/sdk/README.md"
      provides: "SDK npm package README with install + usage example"
      min_lines: 30
  key_links:
    - from: "packages/cli/package.json"
      to: "packages/cli/README.md"
      via: "files field includes README.md for npm publish"
      pattern: "README"
    - from: "packages/sdk/package.json"
      to: "packages/sdk/README.md"
      via: "files field includes README.md for npm publish"
      pattern: "README"
---

<objective>
CLI/SDK 패키지 npm README 작성

Purpose: npm 레지스트리 패키지 페이지(@waiaas/cli, @waiaas/sdk)에서 설치 방법과 기본 사용법을 바로 확인할 수 있다.
Output: packages/cli/README.md, packages/sdk/README.md (신규 생성), package.json files 필드 업데이트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@README.md
@packages/sdk/package.json
@packages/cli/package.json
@packages/sdk/src/types.ts
@packages/sdk/src/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI package npm README</name>
  <files>
    packages/cli/README.md
    packages/cli/package.json
  </files>
  <action>
Create `packages/cli/README.md` for the @waiaas/cli npm package page. This is what users see on npmjs.com when they visit the CLI package.

README structure (keep concise -- npm README, not full docs):

```markdown
# @waiaas/cli

Command-line interface for WAIaaS (Wallet-as-a-Service for AI Agents).

## Install

\```bash
npm install -g @waiaas/cli
\```

## Quick Start

\```bash
waiaas init                        # Create data directory + config
waiaas start                       # Start daemon
waiaas quickset --mode testnet     # Create wallets + MCP sessions
\```

## Commands

| Command | Description |
|---------|-------------|
| `waiaas init` | Initialize data directory and config.toml |
| `waiaas start` | Start the wallet daemon |
| `waiaas quickset` | Create wallets and MCP sessions in one step |
| `waiaas mcp setup` | Configure MCP server for Claude Desktop |
| `waiaas --help` | Show all available commands |

## Requirements

- Node.js >= 22.0.0

## Documentation

Full documentation: [github.com/minhoyoo-iotrust/WAIaaS](https://github.com/minhoyoo-iotrust/WAIaaS)

## License

MIT
```

Then update `packages/cli/package.json` to add "README.md" to the `files` array. npm always includes README.md automatically, but explicit listing is cleaner for monorepo packages. Change `"files": ["dist", "bin"]` to `"files": ["dist", "bin", "README.md"]`.
  </action>
  <verify>
1. File exists: `ls packages/cli/README.md`
2. Contains install command: `grep 'npm install -g @waiaas/cli' packages/cli/README.md`
3. Contains quickset usage: `grep 'quickset' packages/cli/README.md`
4. package.json files includes README: `grep 'README' packages/cli/package.json`
  </verify>
  <done>packages/cli/README.md exists with install + quickstart + commands table, and package.json files field includes README.md</done>
</task>

<task type="auto">
  <name>Task 2: Create SDK package npm README</name>
  <files>
    packages/sdk/README.md
    packages/sdk/package.json
  </files>
  <action>
Create `packages/sdk/README.md` for the @waiaas/sdk npm package page. This is what users see on npmjs.com when they visit the SDK package.

IMPORTANT: Use the CORRECT field names from the actual SDK types (packages/sdk/src/types.ts):
- BalanceResponse: `balance` field (not `amount`), `symbol` field
- SendTokenResponse: `id` field (not `signature`)

README structure:

```markdown
# @waiaas/sdk

TypeScript SDK for WAIaaS (Wallet-as-a-Service for AI Agents).

## Install

\```bash
npm install @waiaas/sdk
\```

## Quick Start

\```typescript
import { WAIaaSClient } from '@waiaas/sdk';

const client = new WAIaaSClient({
  baseUrl: 'http://127.0.0.1:3100',
  sessionToken: process.env.WAIAAS_SESSION_TOKEN,
});

// Check balance
const balance = await client.getBalance();
console.log(`${balance.balance} ${balance.symbol}`);

// Send native token
const tx = await client.sendToken({
  to: 'recipient-address...',
  amount: '0.1',
});
console.log(`Transaction: ${tx.id}`);
\```

## API Methods

| Method | Description |
|--------|-------------|
| `getBalance()` | Get wallet native token balance |
| `getAddress()` | Get wallet address |
| `getAssets()` | Get all token balances |
| `sendToken()` | Send native or token transfer |
| `getTransaction()` | Get transaction by ID |
| `listTransactions()` | List transaction history |
| `signTransaction()` | Sign arbitrary transaction |
| `x402Fetch()` | HTTP fetch with automatic 402 payment |

## Requirements

- Node.js >= 22.0.0
- A running WAIaaS daemon with a session token

## Documentation

Full documentation: [github.com/minhoyoo-iotrust/WAIaaS](https://github.com/minhoyoo-iotrust/WAIaaS)

## License

MIT
```

Then update `packages/sdk/package.json` to add "README.md" to the `files` array. Change `"files": ["dist"]` to `"files": ["dist", "README.md"]`.
  </action>
  <verify>
1. File exists: `ls packages/sdk/README.md`
2. Contains install command: `grep 'npm install @waiaas/sdk' packages/sdk/README.md`
3. Uses correct field: `grep 'balance.balance' packages/sdk/README.md` (NOT balance.amount)
4. Uses correct field: `grep 'tx.id' packages/sdk/README.md` (NOT tx.signature)
5. package.json files includes README: `grep 'README' packages/sdk/package.json`
  </verify>
  <done>packages/sdk/README.md exists with install + correct code example + API methods table, and package.json files field includes README.md</done>
</task>

</tasks>

<verification>
1. `ls packages/cli/README.md packages/sdk/README.md` -- both exist
2. `grep 'npm install' packages/cli/README.md packages/sdk/README.md` -- both have install commands
3. `grep 'balance.balance' packages/sdk/README.md` -- uses correct field name
4. `grep 'tx.id' packages/sdk/README.md` -- uses correct field name
5. `grep -c 'balance.amount\|tx.signature' packages/sdk/README.md` -- returns 0 (no wrong fields)
6. `node -e "const p=require('./packages/sdk/package.json'); console.log(p.files.includes('README.md'))"` -- true
7. `node -e "const p=require('./packages/cli/package.json'); console.log(p.files.includes('README.md'))"` -- true
</verification>

<success_criteria>
- CLI npm package has a README with install + quickstart + commands table
- SDK npm package has a README with install + correct code example + API methods
- Both package.json files field includes README.md
- No incorrect field names (balance.amount, tx.signature) in any README
</success_criteria>

<output>
After completion, create `.planning/phases/196-readme-sdk-docs/196-02-SUMMARY.md`
</output>
