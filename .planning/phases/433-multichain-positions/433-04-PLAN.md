---
phase: 433-multichain-positions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/jito-staking/index.ts
  - packages/actions/src/providers/kamino/index.ts
  - packages/actions/src/providers/drift/index.ts
  - packages/actions/src/providers/hyperliquid/perp-provider.ts
  - packages/actions/src/providers/hyperliquid/spot-provider.ts
  - packages/actions/src/__tests__/jito-staking.test.ts
  - packages/actions/src/__tests__/kamino-lending.test.ts
  - packages/actions/src/__tests__/drift-perp.test.ts
  - packages/actions/src/__tests__/hyperliquid-perp-provider.test.ts
  - packages/actions/src/__tests__/hyperliquid-spot-provider.test.ts
autonomous: true
requirements: [MCHN-08, MCHN-09]

must_haves:
  truths:
    - "Jito/Kamino/Drift extract network from ctx.networks instead of hardcoding 'solana-mainnet'"
    - "Hyperliquid Perp/Spot return empty array when ctx.chain is not ethereum (already done in 432, verify MCHN-09)"
    - "Solana providers use the first Solana network from ctx.networks for the network field in positions"
  artifacts:
    - path: "packages/actions/src/providers/jito-staking/index.ts"
      provides: "Dynamic network from ctx.networks"
      contains: "ctx.networks"
    - path: "packages/actions/src/providers/kamino/index.ts"
      provides: "Dynamic network from ctx.networks"
      contains: "ctx.networks"
    - path: "packages/actions/src/providers/drift/index.ts"
      provides: "Dynamic network from ctx.networks"
      contains: "ctx.networks"
  key_links:
    - from: "packages/actions/src/providers/jito-staking/index.ts"
      to: "ctx.networks[0]"
      via: "Solana network extraction"
      pattern: "ctx\\.networks"
    - from: "packages/actions/src/providers/kamino/index.ts"
      to: "ctx.networks[0]"
      via: "Solana network extraction"
      pattern: "ctx\\.networks"
---

<objective>
Solana 프로바이더(Jito/Kamino/Drift)의 하드코딩된 'solana-mainnet' 네트워크를 ctx.networks에서 추출하도록 변경하고, Hyperliquid가 MCHN-09를 충족하는지 확인한다.

Purpose: MCHN-08 Solana 프로바이더 네트워크 동적 추출 + MCHN-09 Hyperliquid 가드 확인
Output: 5개 프로바이더가 ctx.networks 기반으로 동작
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From packages/core/src/interfaces/position-provider.types.ts:
```typescript
export interface PositionQueryContext {
  walletId: string;
  chain: ChainType;
  networks: readonly NetworkType[];
  environment: EnvironmentType;
  rpcUrls: Record<string, string>;
}
```

Current Solana provider patterns (all 3 identical):
- Jito: hardcoded `network: 'solana-mainnet'` and `chain: 'solana'` in PositionUpdate
- Kamino: hardcoded `network: 'solana-mainnet'`
- Drift: hardcoded `network: 'solana-mainnet'`

All 3 have chain guard: `if (ctx.chain !== 'solana') return [];` (added in Phase 432)

Hyperliquid Perp/Spot: already has `if (ctx.chain !== 'ethereum') return [];` chain guard from Phase 432. MCHN-09 is already satisfied. Just need a test to confirm.

Solana has single network per environment (solana-mainnet for mainnet, solana-devnet for testnet). ctx.networks for a solana wallet will always have exactly one element. So: `const network = ctx.networks[0]` is the correct pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Solana 프로바이더 3개 네트워크 동적 추출 + Hyperliquid MCHN-09 테스트</name>
  <files>
    packages/actions/src/providers/jito-staking/index.ts
    packages/actions/src/providers/kamino/index.ts
    packages/actions/src/providers/drift/index.ts
    packages/actions/src/__tests__/jito-staking.test.ts
    packages/actions/src/__tests__/kamino-lending.test.ts
    packages/actions/src/__tests__/drift-perp.test.ts
  </files>
  <action>
1. In each of the 3 Solana providers (jito-staking/index.ts, kamino/index.ts, drift/index.ts), update `getPositions(ctx)`:
   - After the chain guard (`if (ctx.chain !== 'solana') return []`), add: `const network = ctx.networks[0] ?? 'solana-mainnet';`
   - Replace all hardcoded `'solana-mainnet'` string literals in PositionUpdate objects with the `network` variable
   - For Jito: update the CAIP-19 formatCaip19 call to also use the correct Solana CAIP-2 from ctx. Currently hardcoded `'solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp'`. For devnet/testnet, use the correct CAIP-2 identifier. Import `networkToCaip2` from `@waiaas/core` and use `networkToCaip2(network as NetworkType)` for the chainId parameter.
   - For Kamino: the assetId is currently just `deposit.mintAddress` (not CAIP-19 formatted). Keep as-is since it's Solana SPL token address format.
   - For Drift: assetId is null. Keep as-is.

2. Update tests:
   - Jito test: add test with ctx.networks=['solana-devnet'] to verify network field is 'solana-devnet' (not hardcoded 'solana-mainnet')
   - Kamino test: add test with ctx.networks=['solana-devnet'] to verify network field
   - Drift test: add test with ctx.networks=['solana-devnet'] to verify network field

3. For Hyperliquid MCHN-09, verify the existing chain guard tests from Phase 432 are sufficient. The chain guard `if (ctx.chain !== 'ethereum') return []` already satisfies MCHN-09. If no explicit test exists with the name pattern "returns [] for non-ethereum chain" or similar, add one to both perp-provider and spot-provider tests. (Phase 432 already added these -- just verify they exist.)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/actions/src/__tests__/jito-staking.test.ts packages/actions/src/__tests__/kamino-lending.test.ts packages/actions/src/__tests__/drift-perp.test.ts</automated>
  </verify>
  <done>
    - Jito/Kamino/Drift use ctx.networks[0] instead of hardcoded 'solana-mainnet'
    - Position network field reflects the actual network from context
    - Jito CAIP-19 uses correct Solana CAIP-2 for the network
    - Tests verify dynamic network extraction with devnet context
    - Hyperliquid chain guards confirmed (Phase 432 tests exist)
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run packages/actions/src/__tests__/jito-staking.test.ts packages/actions/src/__tests__/kamino-lending.test.ts packages/actions/src/__tests__/drift-perp.test.ts` -- all tests pass
- `pnpm turbo run typecheck --filter=@waiaas/actions` -- no type errors
- Grep for hardcoded 'solana-mainnet' in getPositions methods of the 3 providers: should find none
</verification>

<success_criteria>
- No hardcoded 'solana-mainnet' in Solana provider getPositions methods
- ctx.networks[0] drives the network field in all Solana position outputs
- Existing Hyperliquid chain guard tests confirm MCHN-09
</success_criteria>

<output>
After completion, create `.planning/phases/433-multichain-positions/433-04-SUMMARY.md`
</output>
