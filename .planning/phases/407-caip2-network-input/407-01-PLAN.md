---
phase: 407-caip2-network-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/enums/chain.ts
  - packages/core/src/__tests__/enums.test.ts
autonomous: true
requirements: [NET-01, NET-02, NET-03, NET-04, NET-05, TST-01, TST-02]

must_haves:
  truths:
    - "eip155:1을 network 파라미터에 전달하면 ethereum-mainnet과 동일하게 동작한다"
    - "solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp 등 15개 네트워크 CAIP-2 매핑이 전수 검증된다"
    - "기존 plain string(ethereum-mainnet)과 legacy(mainnet) 입력은 변경 없이 동작한다"
    - "미등록 CAIP-2 문자열(eip155:99999)은 validation error를 반환한다"
  artifacts:
    - path: "packages/core/src/enums/chain.ts"
      provides: "normalizeNetworkInput CAIP-2 확장 + NetworkTypeEnumWithLegacy z.preprocess"
      contains: "CAIP2_TO_NETWORK"
    - path: "packages/core/src/__tests__/enums.test.ts"
      provides: "15개 네트워크 CAIP-2 매핑 전수 테스트 + 우선순위 테스트"
      min_lines: 30
  key_links:
    - from: "packages/core/src/enums/chain.ts"
      to: "packages/core/src/caip/network-map.ts"
      via: "import CAIP2_TO_NETWORK"
      pattern: "CAIP2_TO_NETWORK"
    - from: "packages/core/src/enums/chain.ts"
      to: "all network parameter consumers"
      via: "NetworkTypeEnumWithLegacy z.preprocess"
      pattern: "normalizeNetworkInput"
---

<objective>
normalizeNetworkInput()을 확장하여 CAIP-2 문자열을 NetworkType으로 자동 변환하고, NetworkTypeEnumWithLegacy의 z.preprocess에서 일괄 적용한다.

Purpose: 모든 네트워크 파라미터(REST body/query, MCP, SDK)에서 CAIP-2 형식(eip155:1, solana:5eykt...)을 기존 plain string과 동일하게 수용하여 AI 에이전트의 표준 식별자 사용을 지원한다.
Output: 확장된 normalizeNetworkInput() + 15개 네트워크 CAIP-2 전수 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/core/src/enums/chain.ts
@packages/core/src/caip/network-map.ts
@packages/core/src/__tests__/enums.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/core/src/caip/network-map.ts:
```typescript
export const CAIP2_TO_NETWORK: Record<string, { chain: ChainType; network: NetworkType }> = {
  'eip155:1':        { chain: 'ethereum', network: 'ethereum-mainnet' },
  'eip155:11155111': { chain: 'ethereum', network: 'ethereum-sepolia' },
  'eip155:137':      { chain: 'ethereum', network: 'polygon-mainnet' },
  'eip155:80002':    { chain: 'ethereum', network: 'polygon-amoy' },
  'eip155:42161':    { chain: 'ethereum', network: 'arbitrum-mainnet' },
  'eip155:421614':   { chain: 'ethereum', network: 'arbitrum-sepolia' },
  'eip155:10':       { chain: 'ethereum', network: 'optimism-mainnet' },
  'eip155:11155420': { chain: 'ethereum', network: 'optimism-sepolia' },
  'eip155:8453':     { chain: 'ethereum', network: 'base-mainnet' },
  'eip155:84532':    { chain: 'ethereum', network: 'base-sepolia' },
  'eip155:999':      { chain: 'ethereum', network: 'hyperevm-mainnet' },
  'eip155:998':      { chain: 'ethereum', network: 'hyperevm-testnet' },
  'solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp': { chain: 'solana', network: 'solana-mainnet' },
  'solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1':  { chain: 'solana', network: 'solana-devnet' },
  'solana:4uhcVJyU9pJkvQyS88uRDiswHXSCkY3z':  { chain: 'solana', network: 'solana-testnet' },
};
export function caip2ToNetwork(caip2: string): { chain: ChainType; network: NetworkType };
```

From packages/core/src/enums/chain.ts (current normalizeNetworkInput):
```typescript
export function normalizeNetworkInput(network: string): string {
  const mapped = LEGACY_SOLANA_NETWORK_MAP[network];
  if (mapped) { /* legacy warning + return mapped */ }
  return network;
}

export const NetworkTypeEnumWithLegacy = z.preprocess(
  (val) => (typeof val === 'string' ? normalizeNetworkInput(val) : val),
  NetworkTypeEnum,
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: normalizeNetworkInput CAIP-2 확장 + z.preprocess 통합</name>
  <files>packages/core/src/enums/chain.ts</files>
  <behavior>
    - CAIP-2 입력 변환: `normalizeNetworkInput('eip155:1')` === `'ethereum-mainnet'`
    - CAIP-2 입력 변환: `normalizeNetworkInput('solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp')` === `'solana-mainnet'`
    - 12개 EVM CAIP-2 + 3개 Solana CAIP-2 = 15개 전수 변환 정상
    - 변환 순서: (1) CAIP-2 매핑 먼저 -> (2) legacy 매핑 -> (3) 그대로 통과
    - 기존 plain string 통과: `normalizeNetworkInput('ethereum-mainnet')` === `'ethereum-mainnet'`
    - 기존 legacy 통과: `normalizeNetworkInput('mainnet')` === `'solana-mainnet'`
    - 미등록 CAIP-2: `normalizeNetworkInput('eip155:99999')` === `'eip155:99999'` (통과 후 NetworkTypeEnum에서 reject)
    - NetworkTypeEnumWithLegacy.parse('eip155:1') === 'ethereum-mainnet'
    - NetworkTypeEnumWithLegacy.parse('eip155:99999') throws ZodError
  </behavior>
  <action>
    packages/core/src/enums/chain.ts에서 normalizeNetworkInput()을 확장한다.

    1. chain.ts 상단에 `import { CAIP2_TO_NETWORK } from '../caip/network-map.js';` 추가
    2. normalizeNetworkInput() 함수 내부 변환 순서 변경:
       - 먼저 CAIP2_TO_NETWORK[network]을 조회하여 매핑이 있으면 .network를 반환
       - 없으면 기존 LEGACY_SOLANA_NETWORK_MAP[network] 조회
       - 없으면 input 그대로 반환
    3. CAIP-2 매핑 시에는 별도의 deprecation warning을 출력하지 않는다 (CAIP-2는 표준 형식이므로 deprecated가 아님)
    4. NetworkTypeEnumWithLegacy는 이미 normalizeNetworkInput()을 z.preprocess에서 호출하므로, normalizeNetworkInput 확장만으로 모든 네트워크 파라미터에 자동 적용된다 (NET-05)

    CAIP-2 문자열은 `namespace:reference` 형식이므로 `:` 포함 여부로 CAIP-2인지 판별할 수 있으나, CAIP2_TO_NETWORK 맵 조회만으로 충분하다 (이미 15개 전수 매핑).
    미등록 CAIP-2 (eip155:99999)는 CAIP2_TO_NETWORK에 없으므로 그대로 통과 -> NetworkTypeEnum.parse()에서 validation error 발생 (NET-04).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/core/src/__tests__/enums.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>normalizeNetworkInput()이 CAIP-2 -> legacy -> passthrough 순서로 변환하며, NetworkTypeEnumWithLegacy가 모든 CAIP-2 입력을 수용한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CAIP-2 전수 테스트 + 우선순위 검증 테스트</name>
  <files>packages/core/src/__tests__/enums.test.ts</files>
  <behavior>
    - 15개 CAIP-2 -> NetworkType 매핑 전수 검증 (TST-01)
    - CAIP-2 + plain string + legacy 혼용 입력 우선순위 테스트 (TST-02)
    - NetworkTypeEnumWithLegacy가 CAIP-2 입력 수용 확인
    - 미등록 CAIP-2 (eip155:99999) rejection 확인
    - 기존 normalizeNetworkInput/NetworkTypeEnumWithLegacy 테스트 유지
  </behavior>
  <action>
    packages/core/src/__tests__/enums.test.ts에 CAIP-2 테스트 추가:

    1. `describe('normalizeNetworkInput CAIP-2')` 블록 추가:
       - 15개 CAIP-2 문자열 전수 테스트 (it.each 또는 개별 it):
         - 'eip155:1' -> 'ethereum-mainnet'
         - 'eip155:11155111' -> 'ethereum-sepolia'
         - 'eip155:137' -> 'polygon-mainnet'
         - 'eip155:80002' -> 'polygon-amoy'
         - 'eip155:42161' -> 'arbitrum-mainnet'
         - 'eip155:421614' -> 'arbitrum-sepolia'
         - 'eip155:10' -> 'optimism-mainnet'
         - 'eip155:11155420' -> 'optimism-sepolia'
         - 'eip155:8453' -> 'base-mainnet'
         - 'eip155:84532' -> 'base-sepolia'
         - 'eip155:999' -> 'hyperevm-mainnet'
         - 'eip155:998' -> 'hyperevm-testnet'
         - 'solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp' -> 'solana-mainnet'
         - 'solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1' -> 'solana-devnet'
         - 'solana:4uhcVJyU9pJkvQyS88uRDiswHXSCkY3z' -> 'solana-testnet'
       - 미등록 CAIP-2 통과 테스트: normalizeNetworkInput('eip155:99999') === 'eip155:99999'

    2. `describe('normalizeNetworkInput priority order')` 블록 추가:
       - CAIP-2가 legacy보다 먼저 적용되는 것 확인 (실제로 충돌 케이스는 없지만, 순서 검증)
       - plain string은 그대로 통과
       - legacy 'mainnet' -> 'solana-mainnet' 여전히 동작
       - CAIP-2 'eip155:1' -> 'ethereum-mainnet' 동작

    3. `describe('NetworkTypeEnumWithLegacy CAIP-2')` 블록 추가:
       - NetworkTypeEnumWithLegacy.parse('eip155:1') === 'ethereum-mainnet'
       - NetworkTypeEnumWithLegacy.parse('solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp') === 'solana-mainnet'
       - NetworkTypeEnumWithLegacy.parse('eip155:99999') throws (Zod validation error)
       - 기존 테스트(canonical + legacy) 모두 유지 확인

    기존 테스트 코드는 절대 수정하지 않는다. 새 describe 블록만 추가한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/core/src/__tests__/enums.test.ts --reporter=verbose 2>&1 | tail -50</automated>
  </verify>
  <done>15개 CAIP-2 매핑 전수 테스트 통과, 우선순위 검증 통과, 미등록 CAIP-2 rejection 확인, 기존 테스트 전체 통과</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run packages/core/src/__tests__/enums.test.ts` -- 기존 + 신규 테스트 전체 통과
2. `pnpm vitest run packages/core/src/__tests__/caip.test.ts` -- 기존 CAIP 테스트 regression 없음
3. `pnpm turbo run typecheck --filter=@waiaas/core` -- 타입 체크 통과
</verification>

<success_criteria>
1. normalizeNetworkInput('eip155:1') === 'ethereum-mainnet' (15개 전수 통과)
2. normalizeNetworkInput('ethereum-mainnet') === 'ethereum-mainnet' (기존 plain string 유지)
3. normalizeNetworkInput('mainnet') === 'solana-mainnet' (기존 legacy 유지)
4. NetworkTypeEnumWithLegacy.parse('eip155:99999') throws ZodError (미등록 rejection)
5. 변환 순서: CAIP-2 -> legacy -> passthrough
6. 기존 테스트 전체 통과 (regression 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/407-caip2-network-input/407-01-SUMMARY.md`
</output>
