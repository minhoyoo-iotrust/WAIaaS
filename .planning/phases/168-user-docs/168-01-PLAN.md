---
phase: 168-user-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs-internal/
  - docs/why-waiaas/001-ai-agent-wallet-security-crisis.md
  - docs/why-waiaas/002-ai-agent-wallet-models-compared.md
autonomous: true
requirements:
  - DOC-01
  - DOC-08

must_haves:
  truths:
    - "docs/ 디렉토리에는 사용자 문서만 존재한다 (설계 문서 없음)"
    - "docs-internal/ 디렉토리에 기존 설계 문서(56-72)와 v0.4 하위 디렉토리가 모두 존재한다"
    - "why-waiaas/ 루트 디렉토리가 삭제되고 docs/why-waiaas/로 이동되었다"
    - "docs/why-waiaas/에 영문 Why WAIaaS 문서 2개가 존재한다"
  artifacts:
    - path: "docs-internal/"
      provides: "내부 설계 문서 디렉토리"
      contains: "56-token-transfer-extension-spec.md"
    - path: "docs/why-waiaas/001-ai-agent-wallet-security-crisis.md"
      provides: "AI 에이전트 지갑 보안 위기 문서"
    - path: "docs/why-waiaas/002-ai-agent-wallet-models-compared.md"
      provides: "AI 에이전트 지갑 모델 비교 문서"
  key_links:
    - from: "docs-internal/"
      to: "docs/"
      via: "directory separation"
      pattern: "docs-internal.*설계.*문서"
---

<objective>
docs/ 디렉토리를 사용자 문서 전용으로 재편성하고, 기존 설계 문서를 docs-internal/로 이동하며, why-waiaas/ 문서를 docs/why-waiaas/로 이동한다.

Purpose: 오픈소스 공개 시 외부 사용자가 docs/에서 사용자 문서만 찾을 수 있고, 내부 설계 문서는 별도 디렉토리에서 관리된다.
Output: docs/ (사용자 문서), docs-internal/ (설계 문서) 분리 완료
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@objectives/v2.0-release.md (섹션 3.1 문서 디렉토리 재편성 참조)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 설계 문서 docs/ -> docs-internal/ 이동</name>
  <files>
    docs-internal/
    docs/
  </files>
  <action>
1. `docs-internal/` 디렉토리를 생성한다.
2. `docs/` 내의 모든 설계 문서를 `docs-internal/`로 이동한다:
   - `docs/56-token-transfer-extension-spec.md` ~ `docs/72-api-interface-dx-design.md` (모든 번호 달린 설계 문서)
   - `docs/67-admin-web-ui-spec.md`
   - `docs/v0.4/` 하위 디렉토리 전체 (41~51번 문서들)
3. `git mv`를 사용하여 git 이력을 보존한다.
4. 이동 후 `docs/` 디렉토리가 비어 있거나 존재하지 않으면 `docs/` 디렉토리를 다시 생성한다 (`.gitkeep` 불필요, Plan 02/03에서 파일 생성 예정).
5. 코드베이스 내에서 `docs/` 경로를 참조하는 파일이 있는지 grep으로 확인한다. 발견 시:
   - `.planning/` 내부 참조는 업데이트하지 않는다 (내부 계획 문서).
   - `CLAUDE.md`, `README.md`, `objectives/` 등 프로젝트 루트 파일의 참조만 `docs-internal/`로 업데이트한다.
   - 단, `docs/` 사용자 문서 경로를 가리키는 참조(배포 가이드, API 레퍼런스 등)는 유지한다.
  </action>
  <verify>
    - `ls docs-internal/` 실행 시 설계 문서(56~72번)와 v0.4/ 디렉토리가 모두 존재
    - `ls docs/` 실행 시 설계 문서가 없음 (번호 달린 .md 파일 없음)
    - `git status`에서 renamed 파일로 표시됨
  </verify>
  <done>
    docs/ 내의 모든 설계 문서(56-72, v0.4/)가 docs-internal/로 이동되고, git 이력이 보존된다
  </done>
</task>

<task type="auto">
  <name>Task 2: why-waiaas/ -> docs/why-waiaas/ 이동</name>
  <files>
    docs/why-waiaas/001-ai-agent-wallet-security-crisis.md
    docs/why-waiaas/002-ai-agent-wallet-models-compared.md
  </files>
  <action>
1. `docs/why-waiaas/` 디렉토리를 생성한다.
2. `git mv why-waiaas/001-ai-agent-wallet-security-crisis.md docs/why-waiaas/`
3. `git mv why-waiaas/002-ai-agent-wallet-models-compared.md docs/why-waiaas/`
4. `why-waiaas/` 루트 디렉토리를 삭제한다 (비어 있으면 git이 자동 삭제).
5. 코드베이스 내에서 `why-waiaas/` 경로를 참조하는 파일을 grep으로 찾아 `docs/why-waiaas/`로 업데이트한다.
  </action>
  <verify>
    - `ls docs/why-waiaas/` 실행 시 001, 002 문서 2개 존재
    - `ls why-waiaas/` 실행 시 디렉토리가 존재하지 않음
    - `grep -r "why-waiaas/" --include="*.md" .` 실행 시 `docs/why-waiaas/` 경로만 표시됨 (루트 `why-waiaas/` 참조 없음)
  </verify>
  <done>
    why-waiaas/ 루트 디렉토리가 삭제되고, 문서 2개가 docs/why-waiaas/로 이동되며, 모든 참조가 업데이트된다
  </done>
</task>

</tasks>

<verification>
1. `ls docs-internal/ | wc -l` >= 기존 docs/ 파일 수
2. `ls docs/` 에 번호 달린 설계 문서 없음
3. `ls docs/why-waiaas/` 에 001, 002 문서 존재
4. `test -d why-waiaas && echo "FAIL" || echo "PASS"` -> PASS
5. `git diff --stat HEAD` 에서 rename 이력 확인
</verification>

<success_criteria>
- docs/ 디렉토리에 설계 문서가 없고, 사용자 문서만 존재할 수 있는 빈 구조 (또는 why-waiaas/ 하위만 존재)
- docs-internal/ 디렉토리에 기존 설계 문서 전체가 git 이력과 함께 보존
- why-waiaas/ 루트 디렉토리가 삭제되고 docs/why-waiaas/로 이동 완료
- 코드베이스 내 경로 참조가 모두 업데이트됨
</success_criteria>

<output>
After completion, create `.planning/phases/168-user-docs/168-01-SUMMARY.md`
</output>
