---
phase: 453-skills-cleanup-admin-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/admin-manual/README.md
  - docs/admin-manual/setup-guide.md
  - docs/admin-manual/daemon-operations.md
  - docs/admin-manual/wallet-management.md
  - docs/admin-manual/policy-management.md
  - docs/admin-manual/defi-providers.md
  - docs/admin-manual/credentials.md
  - docs/admin-manual/erc8004-setup.md
  - docs/admin-manual/erc8128-setup.md
  - site/build.mjs
autonomous: true
requirements:
  - DOC-05
  - DOC-06
  - DOC-07
  - DOC-08
  - DOC-09
  - DOC-10
  - DOC-11
  - DOC-12
  - DOC-13
  - DOC-14
  - DOC-15
must_haves:
  truths:
    - "docs/admin-manual/ 에 README.md + 8개 매뉴얼 파일이 존재한다"
    - "각 매뉴얼 파일에 frontmatter(title, description, keywords)가 있다"
    - "site/build.mjs EXCLUDE_DIRS 에서 admin-manual 이 제거되어 빌드 대상에 포함된다"
    - "매뉴얼 내용이 masterAuth 엔드포인트 전체를 안내한다"
  artifacts:
    - path: "docs/admin-manual/README.md"
      provides: "인덱스 페이지 — 8개 매뉴얼 링크"
      contains: "setup-guide"
    - path: "docs/admin-manual/setup-guide.md"
      provides: "설치/초기화/첫 시작 안내"
      contains: "waiaas init"
    - path: "docs/admin-manual/daemon-operations.md"
      provides: "데몬 운영 안내"
      contains: "kill-switch"
    - path: "docs/admin-manual/wallet-management.md"
      provides: "지갑 CRUD/세션/Owner 안내"
      contains: "POST /v1/wallets"
    - path: "docs/admin-manual/policy-management.md"
      provides: "정책 CRUD 16타입 안내"
      contains: "POST /v1/policies"
    - path: "docs/admin-manual/defi-providers.md"
      provides: "DeFi Provider 설정 안내"
      contains: "actions."
    - path: "docs/admin-manual/credentials.md"
      provides: "Credential 관리 안내"
      contains: "POST /v1/wallets/:id/credentials"
    - path: "docs/admin-manual/erc8004-setup.md"
      provides: "ERC-8004 Provider 설정 안내"
      contains: "erc8004_agent_enabled"
    - path: "docs/admin-manual/erc8128-setup.md"
      provides: "ERC-8128 기능 활성화 안내"
      contains: "erc8128.enabled"
    - path: "site/build.mjs"
      provides: "admin-manual 빌드 포함"
  key_links:
    - from: "docs/admin-manual/README.md"
      to: "docs/admin-manual/*.md"
      via: "relative links"
      pattern: "\\./[a-z-]+\\.md"
    - from: "site/build.mjs"
      to: "docs/admin-manual/"
      via: "EXCLUDE_DIRS removal"
      pattern: "EXCLUDE_DIRS"
---

<objective>
docs/admin-manual/ 디렉토리에 README.md 인덱스 + 8개 masterAuth 관리자 매뉴얼 파일을 생성하고, site/build.mjs 에서 admin-manual 을 빌드 대상에 포함시킨다.

Purpose: skills/ 에서 제거될 masterAuth 콘텐츠의 새 보금자리를 만든다. 이 매뉴얼은 Operator(관리자)가 데몬을 설정하고 운영하기 위한 참조 문서다.
Output: docs/admin-manual/ 9개 파일 + site/build.mjs 수정
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/admin-manual/ 9개 파일 생성 (README + 8 매뉴얼)</name>
  <files>
    docs/admin-manual/README.md
    docs/admin-manual/setup-guide.md
    docs/admin-manual/daemon-operations.md
    docs/admin-manual/wallet-management.md
    docs/admin-manual/policy-management.md
    docs/admin-manual/defi-providers.md
    docs/admin-manual/credentials.md
    docs/admin-manual/erc8004-setup.md
    docs/admin-manual/erc8128-setup.md
  </files>
  <read_first>
    skills/admin.skill.md
    skills/setup.skill.md
    skills/wallet.skill.md
    skills/policies.skill.md
    skills/actions.skill.md
    skills/external-actions.skill.md
    skills/erc8004.skill.md
    skills/erc8128.skill.md
    docs/admin-manual/telegram-setup.md
  </read_first>
  <action>
각 파일에 YAML frontmatter 블록을 포함한다. 포맷:

```yaml
---
title: "문서 제목"
description: "한 줄 설명"
keywords: ["keyword1", "keyword2", ...]
date: "2026-03-18"
---
```

**README.md** (DOC-05):
- title: "WAIaaS Admin Manual"
- 한국어로 작성. 8개 매뉴얼 페이지 목차 + 간단 설명 + 상대 링크(`./setup-guide.md` 등).
- telegram-setup.md 도 목차에 포함 (기존 파일).
- "이 문서는 Operator(관리자)를 위한 것입니다. AI 에이전트는 skills/ 파일을 참조하세요." 안내 문구.

**setup-guide.md** (DOC-06, SKL-02 소스):
- title: "Setup Guide"
- skills/setup.skill.md 전체 내용을 이전. 설치(npm install -g @waiaas/cli), 초기화(waiaas init / --auto-provision), 데몬 시작(waiaas start), 지갑+세션 생성(waiaas quickset), 환경변수 설정, 연결 검증, 마스터 패스워드 강화, 스킬 파일 설치 안내.
- Troubleshooting 섹션 포함 (command not found, port conflict, quickset auth error).

**daemon-operations.md** (DOC-07, SKL-01 소스):
- title: "Daemon Operations"
- skills/admin.skill.md 에서 추출: Admin UI 네비게이션, 세션 생성 POST /v1/sessions (masterAuth), 세션-월렛 관리 (POST/DELETE/PATCH/GET), Agent Self-Discovery POST /admin/agent-prompt (masterAuth), Health/Status, Kill Switch (GET+POST /v1/admin/kill-switch), Shutdown (POST /v1/admin/shutdown), JWT 시크릿 갱신 (POST /v1/admin/jwt/rotate), Settings CRUD (GET/PUT /v1/admin/settings), API Key 관리 (GET/POST/DELETE /v1/admin/api-keys), Notification 설정, Audit Logs (GET /v1/admin/audit-logs), Backup/Restore (POST /v1/admin/backup, POST /v1/admin/restore), Webhook 관리 (GET/POST/PUT/DELETE /v1/admin/webhooks), Stats (GET /v1/admin/stats), AutoStop (GET/PUT /v1/admin/autostop), Oracle Status (GET /v1/admin/oracle-status).

**wallet-management.md** (DOC-08):
- title: "Wallet Management"
- skills/wallet.skill.md 에서 masterAuth 부분 추출: 지갑 CRUD (POST/GET/PUT/DELETE /v1/wallets), 세션 CRUD (POST/GET/DELETE /v1/sessions), MCP 토큰 관리 (POST/DELETE /v1/mcp-tokens), 토큰 레지스트리 (POST/DELETE /v1/tokens), Owner 설정 (PUT /v1/wallets/:id/owner), 네트워크 관리, WalletConnect 페어링, Smart Account Provider 설정 (PUT /v1/wallets/:id/provider), 관리자용 NFT 조회 (GET /v1/wallets/:id/nfts).

**policy-management.md** (DOC-09):
- title: "Policy Management"
- skills/policies.skill.md 에서 추출: POST /v1/policies (생성), PUT /v1/policies/:id (수정), DELETE /v1/policies/:id (삭제), 16개 정책 타입 전체 (SPENDING_LIMIT, DAILY_LIMIT, WHITELIST, TIME_WINDOW, RATE_LIMIT, ALLOWED_TOKENS, CONTRACT_WHITELIST, APPROVED_SPENDERS, NETWORK_RESTRICTION, X402_DOMAIN, ERC8128_ALLOWED_DOMAINS, REPUTATION_THRESHOLD, VENUE_WHITELIST, ACTION_CATEGORY_LIMIT, TOKEN_SPENDING_LIMIT, CUMULATIVE_SPENDING_LIMIT), 정책 우선순위 규칙.

**defi-providers.md** (DOC-10):
- title: "DeFi Provider Configuration"
- skills/actions.skill.md 에서 masterAuth 부분 추출: Provider 활성화 방법 (Admin Settings via PUT /v1/admin/settings), API 키 등록 (0x, LI.FI 등), CONTRACT_WHITELIST 정책 설정, provider-trust bypass 설정. 각 프로바이더별 필요 설정 요약 테이블 (Jupiter, 0x, LI.FI, Lido, Jito, Aave, Kamino, Pendle, Drift, DCent Swap, Hyperliquid, Across, Polymarket).

**credentials.md** (DOC-11):
- title: "Credential Management"
- skills/external-actions.skill.md 에서 masterAuth 부분 추출: Credential CRUD (POST/DELETE/PUT /v1/wallets/:id/credentials, Global credentials /v1/admin/credentials), 지원 타입 (apiKey, apiSecret, rsaPrivateKey, ed25519PrivateKey), 벤치마크 정책 (VENUE_WHITELIST, ACTION_CATEGORY_LIMIT).

**erc8004-setup.md** (DOC-12):
- title: "ERC-8004 Trustless Agents Setup"
- skills/erc8004.skill.md 에서 masterAuth 부분 추출: Provider 활성화 (actions.erc8004_agent_enabled=true, 기본값), 레지스트리 주소 설정, 등록 파일 base URL 설정, REPUTATION_THRESHOLD 정책 생성.

**erc8128-setup.md** (DOC-13):
- title: "ERC-8128 Signed HTTP Requests Setup"
- skills/erc8128.skill.md 에서 masterAuth 부분 추출: 기능 활성화 (erc8128.enabled=true in Admin Settings), ERC8128_ALLOWED_DOMAINS 정책 생성, 프리셋/TTL/nonce/알고리즘/rate limit 설정.

모든 파일은 한국어로 작성하되, API 엔드포인트/curl 예시/코드 블록은 영어로 유지한다. 각 파일 상단에 "이 문서는 Operator(관리자)를 위한 문서입니다. AI 에이전트 접근은 sessionAuth 로 제한됩니다." 안내 문구를 포함한다.
  </action>
  <acceptance_criteria>
    - `ls docs/admin-manual/*.md | wc -l` 출력이 10 이상 (README + 8 신규 + telegram-setup 기존)
    - `grep -l "^---" docs/admin-manual/setup-guide.md docs/admin-manual/daemon-operations.md docs/admin-manual/wallet-management.md docs/admin-manual/policy-management.md docs/admin-manual/defi-providers.md docs/admin-manual/credentials.md docs/admin-manual/erc8004-setup.md docs/admin-manual/erc8128-setup.md docs/admin-manual/README.md | wc -l` 이 9
    - `grep -c "title:" docs/admin-manual/setup-guide.md` 이 1 이상
    - `grep -c "description:" docs/admin-manual/daemon-operations.md` 이 1 이상
    - `grep -c "keywords:" docs/admin-manual/wallet-management.md` 이 1 이상
    - `grep "setup-guide" docs/admin-manual/README.md` 이 매치
    - `grep "daemon-operations" docs/admin-manual/README.md` 이 매치
    - `grep "waiaas init" docs/admin-manual/setup-guide.md` 이 매치
    - `grep "kill-switch" docs/admin-manual/daemon-operations.md` 이 매치
    - `grep "POST /v1/wallets" docs/admin-manual/wallet-management.md` 이 매치
    - `grep "POST /v1/policies" docs/admin-manual/policy-management.md` 이 매치
    - `grep "0x\|LI.FI\|Jupiter" docs/admin-manual/defi-providers.md` 이 매치
    - `grep "credentials" docs/admin-manual/credentials.md` 이 매치
    - `grep "erc8004_agent_enabled" docs/admin-manual/erc8004-setup.md` 이 매치
    - `grep "erc8128.enabled" docs/admin-manual/erc8128-setup.md` 이 매치
  </acceptance_criteria>
  <verify>
    <automated>ls docs/admin-manual/*.md | wc -l && grep -l "^title:" docs/admin-manual/*.md | wc -l</automated>
  </verify>
  <done>docs/admin-manual/ 에 README.md + 8개 매뉴얼 파일이 존재하고 각 파일에 frontmatter(title, description, keywords)가 포함되어 있다</done>
</task>

<task type="auto">
  <name>Task 2: site/build.mjs EXCLUDE_DIRS 에서 admin-manual 제거</name>
  <files>site/build.mjs</files>
  <read_first>site/build.mjs</read_first>
  <action>
site/build.mjs 30번째 줄의 `const EXCLUDE_DIRS = ['admin-manual'];` 를 `const EXCLUDE_DIRS = [];` 로 변경한다. 이렇게 하면 docs/admin-manual/ 파일들이 site 빌드 대상에 포함된다.

isExcluded 함수(324줄 부근)는 그대로 두되, EXCLUDE_DIRS 가 빈 배열이므로 아무것도 제외하지 않게 된다.
  </action>
  <acceptance_criteria>
    - `grep "EXCLUDE_DIRS = \[\]" site/build.mjs` 가 매치
    - `grep -c "admin-manual" site/build.mjs` 가 0 (EXCLUDE_DIRS 에서 제거됨)
  </acceptance_criteria>
  <verify>
    <automated>grep "EXCLUDE_DIRS" site/build.mjs</automated>
  </verify>
  <done>site/build.mjs EXCLUDE_DIRS 에서 admin-manual 이 제거되어 빌드 대상에 포함된다</done>
</task>

</tasks>

<verification>
- `ls docs/admin-manual/*.md | wc -l` >= 10
- `grep -rn "^title:" docs/admin-manual/*.md | wc -l` >= 9
- `grep "EXCLUDE_DIRS = \[\]" site/build.mjs` 매치
- `node site/build.mjs` 실행 시 admin-manual 페이지가 빌드된다
</verification>

<success_criteria>
1. docs/admin-manual/ 에 README.md 인덱스 + 8개 매뉴얼 파일이 존재한다
2. 각 파일에 frontmatter(title, description, keywords)가 있다
3. site/build.mjs 에서 admin-manual 이 빌드 대상에 포함된다
</success_criteria>

<output>
After completion, create `.planning/phases/453-skills-cleanup-admin-manual/453-01-SUMMARY.md`
</output>
