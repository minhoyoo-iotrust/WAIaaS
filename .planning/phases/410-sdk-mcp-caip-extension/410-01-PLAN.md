---
phase: 410-sdk-mcp-caip-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sdk/src/types.ts
  - packages/sdk/src/validation.ts
  - packages/sdk/src/index.ts
  - packages/sdk/src/__tests__/client.test.ts
  - packages/sdk/src/__tests__/validation.test.ts
autonomous: true
requirements: [SDK-01, SDK-02, SDK-03, SDK-04, SDK-05, TST-08]

must_haves:
  truths:
    - "SDK network 파라미터에 'eip155:1' 등 CAIP-2 문자열 전달이 타입 에러 없이 가능하다"
    - "sendToken()에 { assetId } 단독 전달이 가능하며 기존 address+decimals+symbol 시그니처도 유지된다"
    - "SDK 타입 export에 Caip2ChainId, Caip19AssetId 타입 alias가 포함된다"
    - "응답 타입에 chainId, assetId 필드가 반영된다"
  artifacts:
    - path: "packages/sdk/src/types.ts"
      provides: "Caip2ChainId, Caip19AssetId 타입 alias + union SendTokenParams + 응답 타입 chainId/assetId"
      contains: "Caip2ChainId"
    - path: "packages/sdk/src/validation.ts"
      provides: "assetId-only TokenInfo 허용 (address/decimals/symbol 없이)"
      contains: "assetId"
    - path: "packages/sdk/src/index.ts"
      provides: "Caip2ChainId, Caip19AssetId export"
      contains: "Caip2ChainId"
    - path: "packages/sdk/src/__tests__/validation.test.ts"
      provides: "assetId-only validation 테스트"
      contains: "assetId"
  key_links:
    - from: "packages/sdk/src/types.ts"
      to: "packages/sdk/src/index.ts"
      via: "export type"
      pattern: "export.*Caip2ChainId"
    - from: "packages/sdk/src/types.ts"
      to: "packages/sdk/src/validation.ts"
      via: "TokenInfo assetId-only path"
      pattern: "assetId"
---

<objective>
SDK CAIP 타입 확장: Caip2ChainId/Caip19AssetId 타입 alias, network 파라미터 string 유지(CAIP-2 허용 명시), TokenInfo union(assetId-only), 응답 타입 chainId/assetId 추가, validation 완화

Purpose: SDK 사용자가 CAIP-2/19 식별자를 네이티브로 사용할 수 있도록 타입 시스템 확장
Output: types.ts 확장 + validation.ts 완화 + 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/sdk/src/types.ts
@packages/sdk/src/validation.ts
@packages/sdk/src/index.ts
@packages/sdk/src/__tests__/validation.test.ts
@packages/sdk/src/__tests__/client.test.ts

<interfaces>
<!-- Key types the executor needs. -->

From packages/sdk/src/types.ts (current):
```typescript
export interface TokenInfo {
  address: string;
  decimals: number;
  symbol: string;
  assetId?: string;
}

export interface SendTokenParams {
  to?: string;
  amount?: string;
  humanAmount?: string;
  // ...
  token?: TokenInfo;
  network?: string;
  // ...
}

export interface BalanceResponse {
  walletId: string; chain: string; network: string;
  address: string; balance: string; decimals: number; symbol: string;
}
```

From packages/sdk/src/validation.ts (current):
```typescript
function validateTokenInfo(p: Record<string, unknown>): void {
  // Requires token.address (string), token.decimals (number), token.symbol (string)
  // Does NOT handle assetId-only case
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CAIP 타입 alias + TokenInfo union + 응답 타입 확장</name>
  <files>packages/sdk/src/types.ts, packages/sdk/src/index.ts</files>
  <behavior>
    - Test 1: Caip2ChainId 타입이 string alias로 export된다
    - Test 2: Caip19AssetId 타입이 string alias로 export된다
    - Test 3: TokenInfo에 assetId만 있는 객체가 타입 호환된다 (컴파일 타임 검증)
    - Test 4: 기존 TokenInfo {address, decimals, symbol} 객체도 여전히 타입 호환된다
    - Test 5: BalanceResponse에 chainId 필드가 optional로 존재한다
  </behavior>
  <action>
1. `types.ts` 상단에 CAIP 타입 alias 추가:
   ```typescript
   /** CAIP-2 chain identifier (e.g., "eip155:1", "solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp") */
   export type Caip2ChainId = string;
   /** CAIP-19 asset identifier (e.g., "eip155:1/erc20:0xa0b8...", "eip155:1/slip44:60") */
   export type Caip19AssetId = string;
   ```

2. `TokenInfo` 인터페이스를 union 타입으로 변경. 기존 시그니처 유지하면서 assetId-only 패턴 추가:
   ```typescript
   /** Token identification: provide either full metadata OR assetId alone (daemon resolves). */
   export type TokenInfo = TokenInfoFull | TokenInfoByAssetId;

   /** Full token metadata (existing pattern). */
   export interface TokenInfoFull {
     address: string;
     decimals: number;
     symbol: string;
     assetId?: Caip19AssetId;
   }

   /** CAIP-19 only -- daemon resolves address/decimals/symbol from registry. */
   export interface TokenInfoByAssetId {
     assetId: Caip19AssetId;
     address?: string;
     decimals?: number;
     symbol?: string;
   }
   ```

3. 모든 `network?: string` 파라미터에 JSDoc으로 CAIP-2 허용 명시 (타입은 string 유지 -- 하위 호환). 대표 파라미터들:
   - `SendTokenParams.network`: `/** Target network (e.g., 'polygon-mainnet' or CAIP-2 'eip155:137'). */`
   - `BalanceOptions.network`, `AssetsOptions.network`, `SignTransactionParams.network` 등

4. 응답 타입에 CAIP 필드 추가 (additive, optional):
   - `BalanceResponse`: `chainId?: Caip2ChainId`
   - `AssetsResponse`: `chainId?: Caip2ChainId`
   - `AssetInfo`: `chainId?: Caip2ChainId` (assetId는 이미 있음)
   - `TransactionResponse`: `chainId?: Caip2ChainId`
   - `IncomingTransactionItem`: `chainId?: Caip2ChainId`, `assetId?: Caip19AssetId`
   - `ConnectInfoResponse`: `supportedChainIds?: Caip2ChainId[]`
   - `NftItemResponse`: `chainId?: Caip2ChainId` (assetId 이미 있음)
   - `NftMetadataResponse`: `chainId?: Caip2ChainId`
   - `DeFiPosition`: `chainId?: Caip2ChainId`
   - `MultiNetworkBalanceEntry`: `chainId?: Caip2ChainId`
   - `MultiNetworkAssetsEntry`: `chainId?: Caip2ChainId`

5. `index.ts`에 타입 export 추가:
   ```typescript
   export type { Caip2ChainId, Caip19AssetId, TokenInfoFull, TokenInfoByAssetId } from './types.js';
   ```
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/sdk</automated>
  </verify>
  <done>Caip2ChainId, Caip19AssetId 타입이 export되고, TokenInfo union이 기존 시그니처와 assetId-only 모두 수용하며, 응답 타입에 chainId/assetId optional 필드가 추가됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validation.ts assetId-only 허용 + SDK 테스트</name>
  <files>packages/sdk/src/validation.ts, packages/sdk/src/__tests__/validation.test.ts, packages/sdk/src/__tests__/client.test.ts</files>
  <behavior>
    - Test 1: TOKEN_TRANSFER + token={assetId: "eip155:1/erc20:0xa0b8..."} 단독 전달 -> validation 통과
    - Test 2: APPROVE + token={assetId: "eip155:1/erc20:0xa0b8..."} 단독 전달 -> validation 통과
    - Test 3: TOKEN_TRANSFER + token={address, decimals, symbol} (기존) -> validation 통과 (하위 호환)
    - Test 4: TOKEN_TRANSFER + token={} (빈 객체) -> validation 실패
    - Test 5: TOKEN_TRANSFER + token 미제공 -> validation 실패
    - Test 6: network에 CAIP-2 문자열 전달 -> validation 통과 (string이므로 추가 검증 없음)
    - Test 7: sendToken()에 assetId-only token으로 API 호출 성공 (mock)
  </behavior>
  <action>
1. `validation.ts`의 `validateTokenInfo()` 함수를 수정하여 assetId-only 패턴 허용:
   ```typescript
   function validateTokenInfo(p: Record<string, unknown>): void {
     const token = p['token'];
     if (!token || typeof token !== 'object') {
       throw new WAIaaSError({
         code: 'VALIDATION_ERROR',
         message: 'sendToken: "token" must be an object with address/decimals/symbol or assetId',
         status: 0,
         retryable: false,
       });
     }
     const t = token as Record<string, unknown>;

     // assetId-only mode: daemon resolves address/decimals/symbol
     if (typeof t['assetId'] === 'string' && (t['assetId'] as string).length > 0) {
       return; // Valid -- daemon will resolve from registry
     }

     // Full mode: require address + decimals + symbol
     if (typeof t['address'] !== 'string' || (t['address'] as string).length === 0) {
       throw new WAIaaSError({ ... });
     }
     if (typeof t['decimals'] !== 'number') {
       throw new WAIaaSError({ ... });
     }
     if (typeof t['symbol'] !== 'string' || (t['symbol'] as string).length === 0) {
       throw new WAIaaSError({ ... });
     }
   }
   ```

2. `validation.test.ts`에 테스트 추가:
   - assetId-only TOKEN_TRANSFER 통과
   - assetId-only APPROVE 통과
   - 빈 token 객체 실패
   - 기존 full token 통과 (회귀 확인)

3. `client.test.ts`에 SDK 통합 테스트 추가:
   - sendToken({..., token: {assetId: 'eip155:1/erc20:0xa0b8...'}, type: 'TOKEN_TRANSFER'}) -> POST /v1/transactions/send 호출 확인
   - network: 'eip155:1' 전달 -> body에 그대로 전달 확인
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/sdk/src/__tests__/validation.test.ts packages/sdk/src/__tests__/client.test.ts --reporter=verbose</automated>
  </verify>
  <done>assetId-only TokenInfo가 validation을 통과하고, CAIP-2 network 문자열이 SDK를 통해 daemon에 전달되며, 기존 시그니처가 그대로 동작함</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/sdk` -- 타입 에러 없음
2. `pnpm vitest run packages/sdk/src/__tests__/ --reporter=verbose` -- 모든 SDK 테스트 통과
3. `Caip2ChainId`, `Caip19AssetId`가 index.ts에서 export됨
4. `TokenInfo` union이 assetId-only와 full 모두 수용
</verification>

<success_criteria>
- SDK에서 `network: 'eip155:1'` 형태의 CAIP-2 문자열 전달 가능 (string 타입 유지)
- sendToken({token: {assetId: 'eip155:1/erc20:0xa0b8...'}}) 형태의 assetId-only 전달 가능
- 기존 sendToken({token: {address, decimals, symbol}}) 시그니처 유지
- Caip2ChainId, Caip19AssetId 타입이 SDK에서 export
- 응답 타입에 chainId, assetId optional 필드 추가
- 모든 기존 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/410-sdk-mcp-caip-extension/410-01-SUMMARY.md`
</output>
