---
phase: 445-daemon-infra-admin-notification-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/__tests__/admin-wallets-coverage.test.ts
  - packages/daemon/src/__tests__/admin-settings-coverage.test.ts
  - packages/daemon/src/__tests__/admin-auth-coverage.test.ts
  - packages/daemon/src/__tests__/admin-monitoring-coverage.test.ts
  - packages/daemon/src/__tests__/wallet-apps-coverage.test.ts
autonomous: true
requirements: [DINF-04]

must_haves:
  truths:
    - "Admin wallets CRUD 엣지 케이스(삭제 시 활성 세션 정리, 네트워크 설정 변경)가 테스트로 검증된다"
    - "Admin settings 라우트의 partial update, validation 실패, 카테고리별 조회가 테스트로 검증된다"
    - "Admin auth 라우트의 SIWE/SIWS 검증 실패, owner 상태 전이, grace period 만료가 테스트로 검증된다"
    - "Admin monitoring 라우트의 health-factor/margin/balance 조회 엣지 케이스가 테스트로 검증된다"
    - "Wallet-apps 라우트의 앱 등록/해제, sign topic 설정 엣지 케이스가 테스트로 검증된다"
  artifacts:
    - path: "packages/daemon/src/__tests__/admin-wallets-coverage.test.ts"
      provides: "Admin wallets 라우트 미커버 경로 테스트"
    - path: "packages/daemon/src/__tests__/admin-settings-coverage.test.ts"
      provides: "Admin settings 라우트 미커버 경로 테스트"
    - path: "packages/daemon/src/__tests__/admin-auth-coverage.test.ts"
      provides: "Admin auth 라우트 미커버 경로 테스트"
  key_links:
    - from: "packages/daemon/src/__tests__/admin-wallets-coverage.test.ts"
      to: "packages/daemon/src/api/routes/admin-wallets.ts"
      via: "Hono app.request() mock"
      pattern: "createAdminWalletRoutes"
    - from: "packages/daemon/src/__tests__/admin-auth-coverage.test.ts"
      to: "packages/daemon/src/api/routes/admin-auth.ts"
      via: "Hono app.request() mock"
      pattern: "createAdminAuthRoutes"
---

<objective>
Admin API 라우트(wallets/settings/auth/monitoring)와 wallet-apps 라우트의 미커버 경로를 테스트로 커버한다.

Purpose: daemon 패키지 90/85/95 달성을 위해 Admin API routes 미커버 497줄 + wallet-apps 65줄 해소
Output: 5개 테스트 파일, ~562 uncovered lines + ~96 branches 커버
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Wallets + Settings + Auth 라우트 미커버 경로 테스트</name>
  <files>
    packages/daemon/src/__tests__/admin-wallets-coverage.test.ts
    packages/daemon/src/__tests__/admin-settings-coverage.test.ts
    packages/daemon/src/__tests__/admin-auth-coverage.test.ts
  </files>
  <action>
기존 테스트 패턴(`admin-settings-api.test.ts`, `api-admin-endpoints.test.ts`)을 참조하여 Hono app.request() 또는 in-memory DB 기반 테스트를 작성한다.

1. **Admin Wallets** (`admin-wallets-coverage.test.ts`, 소스: 701줄, 148줄 미커버):
   - 기존: `admin-cross-wallet-api.test.ts` 참조
   - 미커버 경로:
     a. DELETE /v1/admin/wallets/:id — 지갑 삭제 시 관련 세션/정책 cascade 삭제
     b. PUT /v1/admin/wallets/:id/networks — 네트워크 활성화/비활성화 토글
     c. GET /v1/admin/wallets — pagination(limit/offset), 필터링(chainType)
     d. 존재하지 않는 walletId → 404
     e. smart account 지갑의 특수 필드(accountType, aaProvider) 반환
     f. wallet credential 연결 상태 조회

2. **Admin Settings** (`admin-settings-coverage.test.ts`, 소스: 623줄, 141줄 미커버):
   - 기존: `admin-settings-api.test.ts` 참조
   - 미커버 경로:
     a. PUT /v1/admin/settings — partial update (일부 키만 변경)
     b. validation 실패: 잘못된 타입, 범위 초과 값 → 400
     c. GET /v1/admin/settings?category=notification — 카테고리 필터링
     d. hot-reload 트리거 확인: 설정 변경 후 HotReloadOrchestrator 호출
     e. 읽기 전용 설정(master_password_hash) 변경 시도 → 거부
     f. 설정 초기화(DELETE) → 기본값 복원

3. **Admin Auth** (`admin-auth-coverage.test.ts`, 소스: 577줄, 109줄 미커버):
   - 기존: `admin-serving.test.ts` 참조
   - 미커버 경로:
     a. SIWE 검증 실패 — 잘못된 서명, 만료된 nonce → 401
     b. SIWS 검증 실패 — Solana 주소 불일치 → 401
     c. Owner 상태 GRACE → LOCKED 전이 (grace period 만료)
     d. Owner LOCKED 상태에서 인증 시도 → 403
     e. Owner 등록 해제 후 재등록 흐름
     f. masterAuth + ownerAuth 동시 적용 미들웨어 분기
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon exec vitest run src/__tests__/admin-wallets-coverage.test.ts src/__tests__/admin-settings-coverage.test.ts src/__tests__/admin-auth-coverage.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Admin wallets/settings/auth 라우트 미커버 경로 테스트 전체 통과, admin-wallets.ts 85%+, admin-settings.ts 85%+, admin-auth.ts 85%+ 달성</done>
</task>

<task type="auto">
  <name>Task 2: Admin Monitoring + Wallet Apps 라우트 미커버 경로 테스트</name>
  <files>
    packages/daemon/src/__tests__/admin-monitoring-coverage.test.ts
    packages/daemon/src/__tests__/wallet-apps-coverage.test.ts
  </files>
  <action>
1. **Admin Monitoring** (`admin-monitoring-coverage.test.ts`, 소스: 967줄, 71줄 미커버, B: 68.8%):
   - 기존: `admin-stats.test.ts`, `balance-monitor-integration.test.ts` 참조
   - 미커버 경로:
     a. GET /v1/admin/monitoring/health-factors — 빈 결과, 단일 지갑, 다중 지갑
     b. GET /v1/admin/monitoring/margins — Drift/Hyperliquid 마진 조회 실패 시 graceful fallback
     c. GET /v1/admin/monitoring/balances — 특정 체인 필터, 임계값 이하만 필터
     d. PUT /v1/admin/monitoring/thresholds — 임계값 업데이트, validation 실패
     e. monitoring 비활성화 상태에서 조회 시도 → 503 또는 빈 결과
     f. branches: 조건부 monitor 존재 여부 체크, environment 필터

2. **Wallet Apps** (`wallet-apps-coverage.test.ts`, 소스: ~250줄, 65줄 미커버):
   - 기존: `wallet-app-service.test.ts`, `wallet-app-test-notification.test.ts` 참조
   - 미커버 경로:
     a. POST /v1/admin/wallets/:id/apps — 앱 등록 (sign_topic, notify_topic 설정)
     b. DELETE /v1/admin/wallets/:id/apps/:appId — 앱 해제
     c. PUT /v1/admin/wallets/:id/apps/:appId — sign_topic 변경
     d. 존재하지 않는 walletId/appId → 404
     e. 중복 앱 등록 시도 → 409 또는 기존 반환
     f. wallet_apps 테이블 연동 검증
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon exec vitest run src/__tests__/admin-monitoring-coverage.test.ts src/__tests__/wallet-apps-coverage.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Admin monitoring/wallet-apps 라우트 미커버 경로 테스트 전체 통과, admin-monitoring.ts B:80%+, wallet-apps.ts 80%+ 달성</done>
</task>

</tasks>

<verification>
전체 테스트 통과:
```bash
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon exec vitest run src/__tests__/admin-wallets-coverage.test.ts src/__tests__/admin-settings-coverage.test.ts src/__tests__/admin-auth-coverage.test.ts src/__tests__/admin-monitoring-coverage.test.ts src/__tests__/wallet-apps-coverage.test.ts --reporter=verbose
```
</verification>

<success_criteria>
1. Admin wallets 삭제/네트워크설정/페이지네이션/404 경로 테스트 통과
2. Admin settings partial update/validation/카테고리필터/hot-reload 테스트 통과
3. Admin auth SIWE/SIWS 실패, owner 상태 전이, LOCKED 차단 테스트 통과
4. Admin monitoring health-factor/margin/balance 조회 + 비활성화 경로 테스트 통과
5. Wallet-apps 등록/해제/변경/404/409 경로 테스트 통과
6. 신규 테스트 전체 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/445-daemon-infra-admin-notification-tests/445-02-SUMMARY.md`
</output>
