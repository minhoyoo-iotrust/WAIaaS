---
phase: 445-daemon-infra-admin-notification-tests
plan: 03
type: execute
wave: 2
depends_on: [445-01, 445-02]
files_modified:
  - packages/daemon/src/__tests__/api-routes-coverage-sweep.test.ts
  - packages/daemon/src/__tests__/pipeline-branches-coverage.test.ts
  - packages/daemon/src/__tests__/infra-misc-coverage.test.ts
  - packages/daemon/vitest.config.ts
autonomous: true
requirements: [DCOV-01, DCOV-02, DCOV-03]

must_haves:
  truths:
    - "daemon 패키지 Lines >= 90% 달성"
    - "daemon 패키지 Branches >= 85% 달성"
    - "daemon 패키지 Functions >= 95% 달성"
    - "pnpm --filter daemon run test:unit 실행 시 임계값 검증을 통과한다"
  artifacts:
    - path: "packages/daemon/src/__tests__/api-routes-coverage-sweep.test.ts"
      provides: "API 라우트 잔여 갭 해소 테스트"
    - path: "packages/daemon/src/__tests__/pipeline-branches-coverage.test.ts"
      provides: "Pipeline 분기 갭 해소 테스트"
    - path: "packages/daemon/vitest.config.ts"
      provides: "통일 임계값 적용 (L:90/B:85/F:95)"
  key_links:
    - from: "packages/daemon/vitest.config.ts"
      to: "pnpm test:unit"
      via: "vitest coverage thresholds"
      pattern: "thresholds.*lines.*90"
---

<objective>
Plan 01/02에서 해소하지 못한 잔여 커버리지 갭을 sweep하고, daemon vitest.config.ts 임계값을 90/85/95로 인상한다.

Purpose: daemon 패키지가 Lines 90% / Branches 85% / Functions 95% 통일 기준을 공식적으로 달성 + 하락 방지
Output: 3개 테스트 파일 + vitest.config.ts 임계값 인상, daemon 90/85/95 게이트 통과
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/445-daemon-infra-admin-notification-tests/445-01-SUMMARY.md
@.planning/phases/445-daemon-infra-admin-notification-tests/445-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API 라우트 + Pipeline + Infra 잔여 갭 커버리지 sweep</name>
  <files>
    packages/daemon/src/__tests__/api-routes-coverage-sweep.test.ts
    packages/daemon/src/__tests__/pipeline-branches-coverage.test.ts
    packages/daemon/src/__tests__/infra-misc-coverage.test.ts
  </files>
  <action>
Plan 01/02 완료 후 `pnpm --filter daemon exec vitest run --coverage` 실행하여 현재 커버리지를 측정한다. 90/85/95에 부족한 영역을 파악하고 아래 우선순위로 테스트를 추가한다.

**커버리지 갭 측정 후 우선순위 (예상 잔여 갭 ~500-800줄):**

1. **API 라우트 sweep** (`api-routes-coverage-sweep.test.ts`):
   - `api/routes/wallets.ts` (316줄 미커버): wallet 생성 시 chainType별 분기(evm/solana), smart account 생성, credential 연결, 네트워크 할당
   - `api/routes/actions.ts` (218줄 미커버): action 조회/실행 API, provider별 분기, dry-run 모드
   - `api/routes/transactions.ts` (204줄 미커버): tx 조회 필터(status/type/network), pagination, 상세 조회 시 enrichment
   - `api/routes/sessions.ts` (85줄 미커버): 세션 갱신 실패, 만료 세션 조회, pagination offset
   - `api/routes/wc.ts` (129줄 미커버): WC pairing 생성/삭제, 세션 승인/거부
   - 가장 미커버 줄 수가 큰 파일부터 차례로 커버하되, 90% 달성에 필요한 만큼만 작성

2. **Pipeline 분기 sweep** (`pipeline-branches-coverage.test.ts`):
   - `pipeline/stage5-execute.ts` (232줄, B:51.1%): 체인별 실행 분기, 배치 트랜잭션, NFT 전송, contract deploy
   - `pipeline/external-action-pipeline.ts` (84줄, B:40.7%): external action resolve, credential vault 조회, 실행 실패
   - `pipeline/dry-run.ts` (72줄, B:67.7%): dry-run 시뮬레이션 실패, 가스 추정, 결과 포맷팅
   - Branches 85% 달성에 필요한 분기만 선택적으로 커버

3. **Infra 잡 sweep** (`infra-misc-coverage.test.ts`):
   - `infrastructure/settings/hot-reload.ts` (86줄 미커버): 설정 변경 감지, 서비스별 reload 핸들러 호출
   - `infrastructure/nft/nft-indexer-client.ts` (27줄): retry 실패, cache miss
   - `infrastructure/oracle/coingecko-oracle.ts` (26줄): API 에러, rate limit, fallback 가격
   - Functions 95% 달성에 필요한 미커버 함수(현재 8개 부족)를 최소 1개 테스트로 커버

**실행 전략:**
- 먼저 `pnpm --filter daemon exec vitest run --coverage` 실행하여 Plan 01/02 이후 정확한 갭을 측정
- coverage-summary.json에서 Lines/Branches/Functions 현재값 확인
- 90/85/95 달성에 필요한 최소 커버리지를 역산하여 테스트 범위 결정
- 테스트는 목킹 기반으로 작성하되, 기존 테스트 패턴(in-memory SQLite, Hono app.request())을 따름
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon exec vitest run --coverage 2>&1 | grep -E "(Lines|Branches|Functions|Statements|All files)" | tail -5</automated>
  </verify>
  <done>daemon 패키지 커버리지가 Lines >= 90%, Branches >= 85%, Functions >= 95%를 달성한다</done>
</task>

<task type="auto">
  <name>Task 2: daemon vitest.config.ts 임계값 인상 + 전체 테스트 통과 확인</name>
  <files>
    packages/daemon/vitest.config.ts
  </files>
  <action>
1. `packages/daemon/vitest.config.ts`의 `thresholds` 섹션을 다음으로 인상:
   ```
   thresholds: {
     branches: 85,
     functions: 95,
     lines: 90,
     statements: 90,
   }
   ```
   - 기존: branches 80, functions 87, lines 85, statements 85
   - CLAUDE.md 규칙: 임계값 하향 절대 금지 (모든 값이 올라감을 확인)

2. `pnpm --filter daemon run test:unit` 전체 실행하여 임계값 통과 확인
   - 실패 시 Task 1로 돌아가 부족분 추가 테스트 작성
   - 통과 시 완료

3. 임계값 인상 후 기존 테스트가 깨지지 않음을 확인 (regression 없음)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon run test:unit 2>&1 | tail -15</automated>
  </verify>
  <done>daemon vitest.config.ts 임계값이 L:90/B:85/F:95/S:90으로 인상되고, `pnpm --filter daemon run test:unit` 전체 통과(0 failures, 임계값 충족)</done>
</task>

</tasks>

<verification>
최종 검증:
```bash
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon run test:unit
```
출력에 "Coverage threshold" 관련 에러 없이 전체 통과해야 한다.
</verification>

<success_criteria>
1. daemon Lines >= 90% (현재 85.31% → 90%+)
2. daemon Branches >= 85% (현재 81.36% → 85%+)
3. daemon Functions >= 95% (현재 94.26% → 95%+)
4. vitest.config.ts 임계값 L:90/B:85/F:95/S:90 적용
5. `pnpm --filter daemon run test:unit` 전체 0 failures + 임계값 통과
6. 기존 테스트 regression 없음
</success_criteria>

<output>
After completion, create `.planning/phases/445-daemon-infra-admin-notification-tests/445-03-SUMMARY.md`
</output>
