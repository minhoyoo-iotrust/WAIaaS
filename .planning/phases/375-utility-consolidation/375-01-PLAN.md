---
phase: 375-utility-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/common/amount-parser.ts
  - packages/actions/src/providers/aave-v3/index.ts
  - packages/actions/src/providers/kamino/index.ts
  - packages/actions/src/providers/lido-staking/index.ts
  - packages/actions/src/providers/jito-staking/jito-stake-pool.ts
  - packages/actions/src/providers/jito-staking/index.ts
  - packages/actions/src/providers/hyperliquid/perp-provider.ts
  - packages/actions/src/providers/hyperliquid/spot-provider.ts
  - packages/actions/src/providers/hyperliquid/sub-account-provider.ts
autonomous: true
requirements: [UTIL-01, UTIL-03, UTIL-04]

must_haves:
  truths:
    - "parseTokenAmount(amount, decimals) 호출 시 모든 프로바이더가 동일한 공통 구현을 사용한다"
    - "프로바이더 파일에 로컬 parseTokenAmount/parseEthAmount/parseSolAmount/parseUsdcAmount 함수 정의가 0개이다"
    - "기존 테스트가 100% 통과하며 행위 변경이 없다"
  artifacts:
    - path: "packages/actions/src/common/amount-parser.ts"
      provides: "parseTokenAmount 공통 함수 + chain-specific convenience wrappers"
      exports: ["parseTokenAmount"]
    - path: "packages/actions/src/common/amount-parser.test.ts"
      provides: "parseTokenAmount 유닛 테스트"
      contains: "parseTokenAmount"
  key_links:
    - from: "packages/actions/src/providers/aave-v3/index.ts"
      to: "packages/actions/src/common/amount-parser.ts"
      via: "import { parseTokenAmount }"
      pattern: "from.*common/amount-parser"
    - from: "packages/actions/src/providers/kamino/index.ts"
      to: "packages/actions/src/common/amount-parser.ts"
      via: "import { parseTokenAmount }"
      pattern: "from.*common/amount-parser"
    - from: "packages/actions/src/providers/lido-staking/index.ts"
      to: "packages/actions/src/common/amount-parser.ts"
      via: "import { parseTokenAmount }"
      pattern: "from.*common/amount-parser"
---

<objective>
parseTokenAmount 공통 모듈을 추출하고 7곳의 중복 구현을 교체한다.

Purpose: 프로바이더 전반에 동일한 금액 파싱 로직이 7곳에 복사되어 있다. 통합하면 ~250줄 중복을 제거하고 버그 수정 시 단일 지점만 수정하면 된다.
Output: `packages/actions/src/common/amount-parser.ts` 공통 모듈 + 7개 프로바이더 교체
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 7곳의 중복 패턴. 모두 동일한 split('.') + padEnd + BigInt 패턴이며 차이는 decimals 기본값과 chain 이름뿐. -->

From packages/actions/src/providers/aave-v3/index.ts (line 65):
```typescript
function parseTokenAmount(amount: string, decimals: number = 18): bigint {
  const parts = amount.split('.');
  const whole = BigInt(parts[0] || '0');
  const fractional = (parts[1] || '').padEnd(decimals, '0').slice(0, decimals);
  const result = whole * 10n ** BigInt(decimals) + BigInt(fractional);
  if (result <= 0n) {
    throw new ChainError('INVALID_INSTRUCTION', 'ethereum', { message: 'Amount must be greater than 0' });
  }
  return result;
}
```

From packages/actions/src/providers/kamino/index.ts (line 57): 동일, decimals=6, chain='solana'
From packages/actions/src/providers/lido-staking/index.ts (line 49): parseEthAmount, hardcoded 18 decimals, chain='ethereum'
From packages/actions/src/providers/jito-staking/jito-stake-pool.ts (line 309): parseSolAmount, hardcoded 9 decimals, async (lazy ChainError import), chain='solana'
From packages/actions/src/providers/hyperliquid/perp-provider.ts (line 61): parseUsdcAmount, hardcoded 6 decimals (USDC_DECIMALS const), NO validation (no ChainError throw)
From packages/actions/src/providers/hyperliquid/spot-provider.ts (line 42): 동일 parseUsdcAmount
From packages/actions/src/providers/hyperliquid/sub-account-provider.ts (line 37): 동일 parseUsdcAmount

Import patterns:
- aave-v3, kamino, lido-staking: `import { ChainError } from '@waiaas/core';`
- jito-staking: lazy `async function getChainError()` 패턴 (동적 import)
- hyperliquid 3곳: `import { ChainError } from '@waiaas/core';` 있지만 parseUsdcAmount에서는 사용 안 함
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseTokenAmount 공통 모듈 생성 및 테스트</name>
  <files>packages/actions/src/common/amount-parser.ts, packages/actions/src/common/amount-parser.test.ts</files>
  <behavior>
    - parseTokenAmount("100.5", 18) returns 100500000000000000000n (100.5 ETH in wei)
    - parseTokenAmount("100.5", 6) returns 100500000n (100.5 USDC)
    - parseTokenAmount("1.5", 9) returns 1500000000n (1.5 SOL in lamports)
    - parseTokenAmount("0", 18) throws ChainError('INVALID_INSTRUCTION')
    - parseTokenAmount("-1", 18) throws ChainError('INVALID_INSTRUCTION')
    - parseTokenAmount("1000", 18) returns 1000000000000000000000n (정수 입력)
    - parseTokenAmount("0.000001", 6) returns 1n (최소 단위)
    - parseTokenAmount("1.123456789", 6) returns 1123456n (초과 소수점 truncation)
  </behavior>
  <action>
    1. `packages/actions/src/common/amount-parser.ts` 생성:
       - `import { ChainError } from '@waiaas/core';`
       - `export function parseTokenAmount(amount: string, decimals: number): bigint` 구현
         - chain 파라미터 불필요: ChainError의 chain은 generic 값 'evm' 사용 (이 함수는 chain-agnostic)
         - 기존 구현과 동일한 로직: split('.'), padEnd, BigInt 변환
         - result <= 0n 시 `throw new ChainError('INVALID_INSTRUCTION', 'evm', { message: 'Amount must be greater than 0' })`
         - decimals 파라미터에 기본값 없음 (호출자가 명시적으로 지정해야 중복 방지)
       - Hyperliquid 호환: hyperliquid의 parseUsdcAmount는 validation이 없었으나, 통합 후에도 동일하게 동작하도록 별도 `parseTokenAmountUnchecked(amount, decimals): bigint` 함수를 export하지 않음. 대신 모든 곳에서 validation을 적용한다 (0이나 음수 금액은 어차피 유효하지 않음).

    2. `packages/actions/src/common/amount-parser.test.ts` 생성:
       - behavior에 명시된 케이스 모두 테스트
       - edge case: 정수만 ("100"), 소수점만 (".5"), 긴 소수 ("1.123456789012345678")

    테스트 먼저 작성 후 구현 (RED -> GREEN).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/common/amount-parser.test.ts</automated>
  </verify>
  <done>amount-parser.ts가 존재하고, 모든 테스트 케이스가 통과하며, parseTokenAmount가 export된다</done>
</task>

<task type="auto">
  <name>Task 2: 7개 프로바이더의 로컬 구현을 공통 모듈 import으로 교체</name>
  <files>
    packages/actions/src/providers/aave-v3/index.ts,
    packages/actions/src/providers/kamino/index.ts,
    packages/actions/src/providers/lido-staking/index.ts,
    packages/actions/src/providers/jito-staking/jito-stake-pool.ts,
    packages/actions/src/providers/jito-staking/index.ts,
    packages/actions/src/providers/hyperliquid/perp-provider.ts,
    packages/actions/src/providers/hyperliquid/spot-provider.ts,
    packages/actions/src/providers/hyperliquid/sub-account-provider.ts
  </files>
  <action>
    각 프로바이더에서 로컬 amount 파싱 함수를 제거하고 공통 모듈에서 import한다.

    **1. aave-v3/index.ts:**
    - 라인 65-78의 `function parseTokenAmount` 전체 삭제 (JSDoc 포함 ~18줄)
    - 파일 상단에 `import { parseTokenAmount } from '../../common/amount-parser.js';` 추가
    - 기존 호출부 `parseTokenAmount(input.amount)` -> `parseTokenAmount(input.amount, 18)` (기본값 제거됨)
    - 4곳 호출: 라인 176, 208, 235, 267

    **2. kamino/index.ts:**
    - 라인 57-70의 `function parseTokenAmount` 전체 삭제 (~18줄)
    - `import { parseTokenAmount } from '../../common/amount-parser.js';` 추가
    - 기존 호출부 `parseTokenAmount(input.amount)` -> `parseTokenAmount(input.amount, 6)` (기본값 제거됨)
    - 4곳 호출: 라인 191, 213, 240, 262

    **3. lido-staking/index.ts:**
    - 라인 49-62의 `function parseEthAmount` 전체 삭제 (~15줄)
    - `import { parseTokenAmount } from '../../common/amount-parser.js';` 추가
    - `parseEthAmount(input.amount)` -> `parseTokenAmount(input.amount, 18)` (2곳: 라인 132, 151)

    **4. jito-staking/jito-stake-pool.ts:**
    - 라인 309-323의 `export async function parseSolAmount` 전체 삭제 (~15줄)
    - 라인 29의 `async function getChainError()` 삭제 가능 (parseSolAmount만 사용했는지 확인 필요 -- 다른 곳에서도 사용하면 유지)
    - `import { parseTokenAmount } from '../../common/amount-parser.js';` 추가
    - `parseSolAmount` 대신 `parseTokenAmount`을 직접 사용하는 래퍼로 교체:
      `export function parseSolAmount(amount: string): bigint { return parseTokenAmount(amount, 9); }`
      (기존에 async였지만 공통 모듈은 sync이므로 sync로 변경 가능. 단, export signature 호환을 위해 확인 필요)

    **4b. jito-staking/index.ts:**
    - 라인 24의 `import { parseSolAmount } from './jito-stake-pool.js';` 유지 (래퍼가 여전히 export됨)
    - 호출부 `await parseSolAmount(input.amount)` -> sync가 되면 `parseSolAmount(input.amount)` (await는 sync에 무해하므로 그대로 둬도 됨)

    **5. hyperliquid/perp-provider.ts:**
    - 라인 61-67의 `function parseUsdcAmount` 삭제 (~7줄)
    - `import { parseTokenAmount } from '../../common/amount-parser.js';` 추가
    - `parseUsdcAmount(...)` -> `parseTokenAmount(..., 6)` (3곳: 라인 714, 723, 728)
    - USDC_DECIMALS 상수는 다른 곳에서도 사용하므로 유지

    **6. hyperliquid/spot-provider.ts:**
    - 라인 42-48의 `function parseUsdcAmount` 삭제 (~7줄)
    - `import { parseTokenAmount } from '../../common/amount-parser.js';` 추가
    - `parseUsdcAmount(...)` -> `parseTokenAmount(..., 6)` (1곳: 라인 350)

    **7. hyperliquid/sub-account-provider.ts:**
    - 라인 37-43의 `function parseUsdcAmount` 삭제 (~7줄)
    - `import { parseTokenAmount } from '../../common/amount-parser.js';` 추가
    - `parseUsdcAmount(...)` -> `parseTokenAmount(..., 6)` (1곳: 라인 96)

    **주의사항:**
    - ChainError import는 각 파일에서 다른 용도로도 사용하므로 제거하지 않는다
    - import 경로는 `.js` 확장자 포함 (ESM 프로젝트 규칙)
    - 행위 변경 없음: 동일한 입력에 동일한 출력. 유일한 차이는 ChainError의 chain 인자가 'evm'으로 통일되나, 이 에러는 사용자에게 "Amount must be greater than 0" 메시지만 전달하므로 chain 값 차이는 무관
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run lint && pnpm turbo run typecheck && pnpm turbo run test</automated>
  </verify>
  <done>7개 프로바이더 파일에 로컬 parseTokenAmount/parseEthAmount/parseSolAmount/parseUsdcAmount 함수 정의가 0개이고, 모두 common/amount-parser.ts에서 import하며, 전체 테스트 통과</done>
</task>

</tasks>

<verification>
1. `grep -rn "function parseTokenAmount\|function parseEthAmount\|function parseSolAmount\|function parseUsdcAmount" packages/actions/src/providers/` 결과가 0줄
2. `grep -rn "from.*common/amount-parser" packages/actions/src/providers/ | wc -l` 결과가 7 이상
3. `pnpm turbo run lint && pnpm turbo run typecheck && pnpm turbo run test` 전체 통과
</verification>

<success_criteria>
- amount-parser.ts가 존재하고 parseTokenAmount를 export한다
- 7개 프로바이더가 공통 모듈을 import하여 사용한다
- 프로바이더 파일에 로컬 amount 파싱 함수 정의가 0개이다
- 전체 lint, typecheck, test 통과
</success_criteria>

<output>
After completion, create `.planning/phases/375-utility-consolidation/375-01-SUMMARY.md`
</output>
