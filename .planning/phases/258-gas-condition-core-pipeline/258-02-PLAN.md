---
phase: 258-gas-condition-core-pipeline
plan: 02
type: execute
wave: 2
depends_on: [258-01]
files_modified:
  - packages/daemon/src/pipeline/gas-condition-tracker.ts
  - packages/daemon/src/services/async-polling-service.ts
  - packages/daemon/src/lifecycle/daemon.ts
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/daemon/src/__tests__/gas-condition-tracker.test.ts
  - packages/daemon/src/__tests__/gas-condition-pipeline.test.ts
autonomous: true

must_haves:
  truths:
    - "GasConditionTracker implements IAsyncStatusTracker with name='gas-condition', timeoutTransition='CANCELLED'"
    - "EVM: queries eth_gasPrice + eth_maxPriorityFeePerGas via JSON-RPC"
    - "Solana: queries getRecentPrioritizationFees via JSON-RPC"
    - "Batch evaluation: one RPC call per chain, evaluate all waiting txs"
    - "AsyncPollingService.processResult handles gas-condition COMPLETED → resumePipeline callback"
    - "Daemon registers GasConditionTracker + executeFromStage4 method"
    - "gas_condition.* settings (5 keys) in SETTING_DEFINITIONS + gas_condition category"
    - "max_pending_count enforcement in pipeline gas condition check"
    - "Daemon restart restores GAS_WAITING transactions via existing DB query"
---

<objective>
GasConditionTracker 구현, AsyncPollingService 가스 조건 완료 처리, 데몬 등록 + executeFromStage4, Settings 5개 키 추가.
</objective>

<tasks>
1. GasConditionTracker (IAsyncStatusTracker) - EVM/Solana gas price RPC queries
2. AsyncPollingService: add resumePipeline callback, special-case gas-condition COMPLETED
3. gas_condition.* settings (5 keys) in SETTING_DEFINITIONS
4. Daemon: register tracker + executeFromStage4 method + max_pending_count enforcement
5. Unit + integration tests
</tasks>
