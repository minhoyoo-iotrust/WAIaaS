---
phase: 258-gas-condition-core-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/schemas/transaction.schema.ts
  - packages/core/src/enums/notification.ts
  - packages/core/src/i18n/en.ts
  - packages/core/src/i18n/ko.ts
  - packages/core/src/__tests__/enums.test.ts
  - packages/daemon/src/pipeline/stages.ts
  - packages/daemon/src/pipeline/pipeline.ts
autonomous: true

must_haves:
  truths:
    - "GasConditionSchema validates maxGasPrice/maxPriorityFee (numeric string) + timeout (60-86400) with at-least-one refine"
    - "All 5 discriminatedUnion request schemas accept optional gasCondition field"
    - "TX_GAS_WAITING and TX_GAS_CONDITION_MET added to NOTIFICATION_EVENT_TYPES"
    - "Pipeline inserts gas condition check between Stage 3 and Stage 4"
    - "gasCondition present + gas_condition.enabled → GAS_WAITING state, PIPELINE_HALTED"
    - "gasCondition absent → existing behavior unchanged (backward compat)"
    - "Policy violation → immediate reject regardless of gasCondition"
    - "i18n en/ko messages for both new notification events"
---

<objective>
GasCondition Zod 스키마 정의, 파이프라인 Stage 3→4 사이 가스 조건 분기, 알림 이벤트 2개 추가, i18n 메시지 추가.
</objective>

<tasks>
1. GasConditionSchema + gasCondition field on all 5 request schemas
2. TX_GAS_WAITING, TX_GAS_CONDITION_MET notification events
3. i18n en/ko messages
4. Pipeline Stage 3.5: gas condition check + GAS_WAITING transition
5. Unit tests for schema validation
</tasks>
