---
phase: 437-large-file-split
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/pipeline/database-policy-engine.ts
  - packages/daemon/src/pipeline/evaluators/spending-limit.ts
  - packages/daemon/src/pipeline/evaluators/contract-whitelist.ts
  - packages/daemon/src/pipeline/evaluators/allowed-tokens.ts
  - packages/daemon/src/pipeline/evaluators/approved-spenders.ts
  - packages/daemon/src/pipeline/evaluators/lending-asset-whitelist.ts
  - packages/daemon/src/pipeline/evaluators/lending-ltv-limit.ts
autonomous: true
requirements: [DPE-01, DPE-02, DPE-03, DPE-04, DPE-05, DPE-06, DPE-07, DPE-08]

must_haves:
  truths:
    - "database-policy-engine.ts contains only the DatabasePolicyEngine class with evaluate(), evaluateBatch(), evaluateAndReserve() orchestration and shared parsing logic"
    - "6 evaluator files each contain their policy evaluation functions as standalone exported functions"
    - "All 7+ policy type tests pass without changes"
  artifacts:
    - path: "packages/daemon/src/pipeline/evaluators/spending-limit.ts"
      provides: "SpendingLimit 4-tier evaluation + token tier + native tier + USD tier"
      exports: ["evaluateSpendingLimit", "evaluateTokenTier", "evaluateNativeTier", "evaluateUsdTier"]
    - path: "packages/daemon/src/pipeline/evaluators/contract-whitelist.ts"
      provides: "CONTRACT_WHITELIST + METHOD_WHITELIST evaluation"
      exports: ["evaluateContractWhitelist", "evaluateMethodWhitelist"]
    - path: "packages/daemon/src/pipeline/evaluators/allowed-tokens.ts"
      provides: "ALLOWED_TOKENS + WHITELIST + ALLOWED_NETWORKS evaluation"
      exports: ["evaluateAllowedTokens", "evaluateWhitelist", "evaluateAllowedNetworks"]
    - path: "packages/daemon/src/pipeline/evaluators/approved-spenders.ts"
      provides: "APPROVED_SPENDERS + APPROVE_AMOUNT_LIMIT + APPROVE_TIER_OVERRIDE evaluation"
      exports: ["evaluateApprovedSpenders", "evaluateApproveAmountLimit", "evaluateApproveTierOverride"]
    - path: "packages/daemon/src/pipeline/evaluators/lending-asset-whitelist.ts"
      provides: "LENDING_ASSET_WHITELIST evaluation"
      exports: ["evaluateLendingAssetWhitelist"]
    - path: "packages/daemon/src/pipeline/evaluators/lending-ltv-limit.ts"
      provides: "LENDING_LTV_LIMIT evaluation"
      exports: ["evaluateLendingLtvLimit"]
    - path: "packages/daemon/src/pipeline/database-policy-engine.ts"
      provides: "Orchestrator class: evaluate, evaluateBatch, evaluateAndReserve, parseRules, resolveOverrides"
      exports: ["DatabasePolicyEngine"]
  key_links:
    - from: "packages/daemon/src/pipeline/database-policy-engine.ts"
      to: "evaluators/*.ts"
      via: "import { evaluateSpendingLimit, ... } from './evaluators/*.js'"
      pattern: "import.*from.*evaluators/"
    - from: "packages/daemon/src/__tests__/database-policy-engine.test.ts"
      to: "database-policy-engine.ts"
      via: "import { DatabasePolicyEngine }"
      pattern: "DatabasePolicyEngine"
---

<objective>
database-policy-engine.ts (2,318줄) 분할: 6개 정책 평가 로직을 evaluators/ 하위 모듈로 분리하고 database-policy-engine.ts는 조합/조율 로직만 유지한다.

Purpose: 정책 평가기를 개별 모듈로 분리하여 각 정책 타입의 독립적 이해와 수정이 가능하게 한다.
Output: 6 evaluator files + slimmed database-policy-engine.ts
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/daemon/src/pipeline/database-policy-engine.ts
@packages/daemon/src/__tests__/database-policy-engine.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 6 policy evaluator modules from database-policy-engine.ts</name>
  <files>
    packages/daemon/src/pipeline/evaluators/spending-limit.ts
    packages/daemon/src/pipeline/evaluators/contract-whitelist.ts
    packages/daemon/src/pipeline/evaluators/allowed-tokens.ts
    packages/daemon/src/pipeline/evaluators/approved-spenders.ts
    packages/daemon/src/pipeline/evaluators/lending-asset-whitelist.ts
    packages/daemon/src/pipeline/evaluators/lending-ltv-limit.ts
  </files>
  <action>
    Create `packages/daemon/src/pipeline/evaluators/` directory.

    **Extraction pattern**: Each evaluator method in DatabasePolicyEngine is a `private` method that takes `resolved` (policy array) and `transaction` (or subsets) as parameters and returns `PolicyEvaluation | null`. Extract each as a standalone exported function that accepts the same parameters without `this`.

    The evaluator methods reference shared types that must be defined in a common location. Create a shared types import block. The `PolicyRow` type (lines ~80-160 in database-policy-engine.ts) and `TransactionInput` interface (lines ~100-140) will need to be exported from database-policy-engine.ts (or a shared types file) so evaluators can import them.

    **Recommended approach**: Define `PolicyRow` and `TransactionInput` types + `parseRules` utility in database-policy-engine.ts and export them. Evaluator functions import these types.

    **1. spending-limit.ts** (DPE-01):
    Extract these methods (lines ~1505-1690):
    - `evaluateSpendingLimit(resolved, amount, batchUsdAmount?, tokenContext?)` -> returns `PolicyEvaluation | null`
    - `evaluateTokenTier(tokenContext, rules)` -> returns `PolicyTier`
    - `evaluateNativeTier(amountBig, rules)` -> returns `PolicyTier`
    - `evaluateUsdTier(usdAmount, rules)` -> returns `PolicyTier`
    These need: `SpendingLimitRulesSchema`, `SpendingLimitRules`, `PolicyEvaluation`, `PolicyTier`, `parseCaip19`, `safeJsonParse`, `parseRules` utility.
    Note: `evaluateSpendingLimit` references `this.parseRules` and `this.settingsService` -- pass these as parameters or via a context object.

    **2. contract-whitelist.ts** (DPE-02):
    Extract (lines ~1201-1321):
    - `evaluateContractWhitelist(resolved, transaction)` -> returns `PolicyEvaluation | null`
    - `evaluateMethodWhitelist(resolved, transaction)` -> returns `PolicyEvaluation | null`
    Need: `ContractWhitelistRulesSchema`, `MethodWhitelistRulesSchema`, `parseRules`.

    **3. allowed-tokens.ts** (DPE-03):
    Extract (lines ~1017-1180):
    - `evaluateAllowedTokens(resolved, transaction)` -> returns `PolicyEvaluation | null`
    - `evaluateWhitelist(resolved, toAddress)` -> returns `PolicyEvaluation | null`
    - `evaluateAllowedNetworks(resolved, network)` -> returns `PolicyEvaluation | null`
    Need: `AllowedTokensRulesSchema`, `WhitelistRulesSchema`, `AllowedNetworksRulesSchema`, `parseCaip19`, `parseRules`.

    **4. approved-spenders.ts** (DPE-04):
    Extract (lines ~1340-1490):
    - `evaluateApprovedSpenders(resolved, transaction)` -> returns `PolicyEvaluation | null`
    - `evaluateApproveAmountLimit(resolved, transaction)` -> returns `PolicyEvaluation | null`
    - `evaluateApproveTierOverride(resolved, transaction)` -> returns `PolicyEvaluation | null`
    Need: `ApprovedSpendersRulesSchema`, `ApproveAmountLimitRulesSchema`, `ApproveTierOverrideRulesSchema`, `UNLIMITED_THRESHOLD` constant, `parseRules`.

    **5. lending-asset-whitelist.ts** (DPE-05):
    Extract (lines ~1707-1758):
    - `evaluateLendingAssetWhitelist(resolved, transaction)` -> returns `PolicyEvaluation | null`
    Need: `LendingAssetWhitelistRulesSchema`, `parseRules`.

    **6. lending-ltv-limit.ts** (DPE-06):
    Extract (lines ~1760-1870):
    - `evaluateLendingLtvLimit(resolved, transaction, walletId)` -> returns `PolicyEvaluation | null`
    Need: `LendingLtvLimitRulesSchema`, `parseRules`, DB access for defi_positions.

    **Also extract into evaluators/ (group with related evaluators):**
    - `evaluatePerpAllowedMarkets`, `evaluatePerpMaxLeverage`, `evaluatePerpMaxPositionUsd` -> can go in contract-whitelist.ts or a separate perp evaluator. Since the requirements don't list these separately, bundle `evaluatePerpAllowedMarkets` with contract-whitelist.ts (both are market/venue whitelists) and leverage/position limits with lending-ltv-limit.ts (both are numeric limit evaluators). OR create a 7th file `perp-limits.ts`. Best: add them to existing files by affinity:
      - perp market whitelist -> contract-whitelist.ts (whitelist pattern)
      - perp leverage/position -> lending-ltv-limit.ts (numeric limit pattern)
    - `evaluateReputationThreshold` -> keep in database-policy-engine.ts (async, uses ReputationCacheService)
    - `evaluateVenueWhitelist` -> contract-whitelist.ts (whitelist pattern)
    - `evaluateActionCategoryLimit` -> spending-limit.ts (amount-based)
    - `evaluateInstructionPolicies` -> keep in database-policy-engine.ts (orchestration logic for batch)

    **parseRules pattern**: Since `parseRules` uses `this.settingsService`, create a `ParseRulesContext` type:
    ```typescript
    export type ParseRulesContext = {
      parseRules: <S extends z.ZodTypeAny>(rules: string, schema: S, policyType: string) => z.infer<S>;
    };
    ```
    Each evaluator function takes `ctx: ParseRulesContext` as first parameter. database-policy-engine.ts passes `{ parseRules: this.parseRules.bind(this) }`.

    **CRITICAL**: Keep `UNLIMITED_THRESHOLD` in approved-spenders.ts (or export from a shared location if lending evaluators also need it).

    **CRITICAL**: Keep the exact evaluation logic unchanged -- this is a pure move refactoring. Do not modify any business logic.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -5</automated>
  </verify>
  <done>6 evaluator files created in evaluators/ directory. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Slim database-policy-engine.ts and verify all policy tests pass</name>
  <files>
    packages/daemon/src/pipeline/database-policy-engine.ts
  </files>
  <action>
    **Rewrite database-policy-engine.ts** to contain only:
    1. Imports of evaluator functions from `./evaluators/*.js`
    2. The `DatabasePolicyEngine` class with:
       - Constructor (unchanged)
       - `parseRules()` private method (unchanged -- shared utility)
       - `evaluate()` method -- calls evaluator functions instead of private methods
       - `evaluateBatch()` method -- calls evaluator functions
       - `evaluateInstructionPolicies()` -- orchestration for batch items, calls evaluator functions
       - `evaluateAndReserve()` method (unchanged -- TOCTOU prevention with raw SQL)
       - `evaluateReputationThreshold()` -- keep here (async, uses ReputationCacheService service dependency)
       - `prefetchReputationFloorTier()` -- keep here (uses ReputationCacheService)
       - Policy loading and resolution logic (loadPolicies, resolveOverrides -- already inline in evaluate())
    3. `PolicyRow`, `TransactionInput`, `ParseRulesContext` type exports

    **Calling pattern change**: Where evaluate() previously called `this.evaluateSpendingLimit(...)`, it now calls `evaluateSpendingLimit({ parseRules: this.parseRules.bind(this) }, ...)`.

    **Target**: database-policy-engine.ts should be ~600-800 lines (orchestration + evaluate + evaluateBatch + evaluateAndReserve + reputation).

    **Run all policy engine tests**:
    - `pnpm vitest run --project daemon -- database-policy-engine`
    - `pnpm vitest run --project daemon -- policy-engine`
    - These tests import DatabasePolicyEngine and test all policy types. Since the public API is unchanged, tests should pass without modification.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run --project daemon 2>&1 | grep -E "policy|FAIL|PASS|Tests" | tail -20</automated>
  </verify>
  <done>
    1. database-policy-engine.ts contains only orchestration logic (~600-800 lines)
    2. 6 evaluator files in evaluators/ directory
    3. All 7+ policy type tests pass (SPENDING_LIMIT, WHITELIST, ALLOWED_TOKENS, CONTRACT_WHITELIST, APPROVED_SPENDERS, LENDING_ASSET_WHITELIST, LENDING_LTV_LIMIT, PERP_*, VENUE_WHITELIST, ACTION_CATEGORY_LIMIT)
    4. No changes to test files
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/daemon` passes
2. `pnpm vitest run --project daemon -- policy-engine` -- all policy engine tests pass
3. `pnpm vitest run --project daemon -- database-policy-engine` -- all tests pass
4. `wc -l packages/daemon/src/pipeline/database-policy-engine.ts` shows ~600-800 lines
5. `ls packages/daemon/src/pipeline/evaluators/` shows 6 files
</verification>

<success_criteria>
1. database-policy-engine.ts에 evaluator 조합과 공통 로직만 포함 (~600-800줄)
2. 6개 evaluator 파일이 각각 해당 정책 평가 로직을 포함
3. 7개+ 정책 타입 기존 테스트가 전체 통과
4. DatabasePolicyEngine 공개 API가 변경되지 않음
</success_criteria>

<output>
After completion, create `.planning/phases/437-large-file-split/437-03-SUMMARY.md`
</output>
