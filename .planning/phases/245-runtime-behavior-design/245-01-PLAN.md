---
phase: 245-runtime-behavior-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/objectives/m28-00-defi-basic-protocol-design.md
autonomous: true
requirements: [ASNC-01, ASNC-02, ASNC-03, ASNC-04, ASNC-05]

must_haves:
  truths:
    - "AsyncStatusTracker 공통 인터페이스가 설계되어 브릿지/unstake/가스대기 3개 구현체가 동일 패턴을 따를 수 있다"
    - "폴링 스케줄러(setTimeout 체인, 간격/최대횟수) 설계가 완성되어 BackgroundWorkers에 통합 가능하다"
    - "트랜잭션 상태 머신(8->9-state, GAS_WAITING 추가) 전이 다이어그램이 완성된다"
    - "통합 DB 마이그레이션(bridge_status + bridge_metadata + GAS_WAITING)이 단일 설계로 확정된다"
    - "브릿지 타임아웃 정책(2시간+ 폴링, 자동 취소 방지, BRIDGE_MONITORING 전환)이 명확하다"
  artifacts:
    - path: "internal/objectives/m28-00-defi-basic-protocol-design.md"
      provides: "DEFI-04 비동기 상태 추적 확정 설계 (섹션 4)"
      contains: "AsyncStatusTracker"
  key_links:
    - from: "DEFI-04 AsyncStatusTracker"
      to: "BackgroundWorkers pattern"
      via: "setTimeout chain polling scheduler design"
      pattern: "setTimeout.*chain|poll.*scheduler"
    - from: "DEFI-04 state machine"
      to: "TRANSACTION_STATUSES enum"
      via: "GAS_WAITING state addition"
      pattern: "GAS_WAITING"
---

<objective>
DEFI-04 비동기 상태 추적 설계를 m28-00-defi-basic-protocol-design.md의 섹션 4에 확정 설계로 작성한다. AsyncStatusTracker 공통 인터페이스, 폴링 스케줄러, 트랜잭션 상태 머신 확장(8->9-state), 통합 DB 마이그레이션, 브릿지 타임아웃 정책을 포함한다.

Purpose: 브릿지/unstake/가스대기의 비동기 완료 추적 패턴이 확정되어 m28-03~m28-05 구현 시 런타임 동작 설계 불확실성이 제거된다.
Output: m28-00 설계 문서 섹션 4 확정 (ASNC-01~05)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/244-core-design-foundation/244-01-SUMMARY.md
@.planning/phases/244-core-design-foundation/244-02-SUMMARY.md
@.planning/research/m28-defi-SUMMARY.md
@.planning/research/m28-defi-protocol-PITFALLS.md
@.planning/research/m28-defi-ARCHITECTURE.md
@internal/objectives/m28-00-defi-basic-protocol-design.md
@internal/objectives/m28-03-lifi-crosschain-bridge.md
@internal/objectives/m28-04-liquid-staking.md
@internal/objectives/m28-05-gas-conditional-execution.md
@packages/core/src/enums/transaction.ts
@packages/daemon/src/lifecycle/workers.ts
@packages/daemon/src/pipeline/stages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AsyncStatusTracker 인터페이스 + 폴링 스케줄러 + 상태 머신 확장 설계</name>
  <files>internal/objectives/m28-00-defi-basic-protocol-design.md</files>
  <action>
m28-00-defi-basic-protocol-design.md의 섹션 4 "비동기 상태 추적 패턴"을 "설계 범위/산출물" 구조에서 확정 설계로 전환한다. 기존 4.1~4.2 내용을 아래 구조로 교체한다.

**상태 표시:** `> **상태:** 확정 설계 (2026-02-23)` + `> **요구사항:** ASNC-01, ASNC-02, ASNC-03, ASNC-04, ASNC-05`

**ASNC-01: IAsyncStatusTracker 공통 인터페이스 확정:**

TypeScript 인터페이스를 확정한다:
```typescript
export interface IAsyncStatusTracker {
  /** 비동기 작업의 현재 상태를 확인한다 */
  checkStatus(txId: string, metadata: Record<string, unknown>): Promise<AsyncTrackingResult>;
  /** 이 트래커의 이름 (bridge, unstake, gas-condition) */
  readonly name: string;
  /** 최대 폴링 시도 횟수 */
  readonly maxAttempts: number;
  /** 폴링 간격 (ms) */
  readonly pollIntervalMs: number;
  /** 타임아웃 시 상태 전이 (TIMEOUT vs BRIDGE_MONITORING vs CANCELLED) */
  readonly timeoutTransition: 'TIMEOUT' | 'BRIDGE_MONITORING' | 'CANCELLED';
}

export interface AsyncTrackingResult {
  state: 'PENDING' | 'COMPLETED' | 'FAILED' | 'TIMEOUT';
  /** COMPLETED 시 도착 체인 정보, FAILED 시 에러 정보 */
  details?: Record<string, unknown>;
  /** 다음 폴링까지 대기 간격 오버라이드 (ms, 선택) -- 백오프 구현용 */
  nextIntervalOverride?: number;
}
```

3개 구현체 설계를 명시한다:
- BridgeStatusTracker: LI.FI /status API 폴링. maxAttempts=240 (2시간@30초). timeoutTransition='BRIDGE_MONITORING'.
- UnstakeStatusTracker: Lido WithdrawalQueue + Jito epoch 폴링. maxAttempts=4032 (14일@5분). timeoutTransition='TIMEOUT'.
- GasConditionTracker: RPC gas price 폴링. maxAttempts=config (기본 120, 1시간@30초). timeoutTransition='CANCELLED'.

**ASNC-02: 폴링 스케줄러 설계 확정:**

setInterval 대신 setTimeout 체인을 사용하는 이유를 명시한다:
- setInterval: 핸들러 실행 시간이 간격을 초과하면 중첩 실행 위험
- setTimeout 체인: 핸들러 완료 후 다음 타이머 설정으로 중첩 방지

AsyncPollingService 설계:
```typescript
class AsyncPollingService {
  /** DB에서 추적 대상 트랜잭션을 조회하고 각 tracker로 상태를 확인한다 */
  async pollAll(): Promise<void>;
}
```

BackgroundWorkers 등록 패턴:
```typescript
workers.register('async-status', {
  interval: 30_000,  // 최소 간격 (실제 간격은 tracker별로 다름)
  handler: () => asyncPollingService.pollAll(),
});
```

pollAll() 내부 로직:
1. SELECT * FROM transactions WHERE bridge_status IN ('PENDING', 'BRIDGE_MONITORING') OR status = 'GAS_WAITING'
2. 각 트랜잭션의 bridge_metadata에서 tracker 유형 판별
3. 해당 tracker.checkStatus() 호출
4. 결과에 따라 DB 업데이트 + 알림 발송
5. 에러 시 해당 트랜잭션만 skip, 다른 트랜잭션 계속 처리

주의: BackgroundWorkers는 기존에 setInterval을 사용하지만, pollAll() 내부에서 per-tracker 타이밍을 관리한다. 각 tracker의 last_polled_at을 bridge_metadata에 저장하여 pollIntervalMs 미경과 시 skip한다.

**ASNC-04: 트랜잭션 상태 머신 확장 (8-state -> 9-state):**

현재 10개 상태(TRANSACTION_STATUSES 배열)에 GAS_WAITING을 추가하여 11개로 확장한다.

전이 다이어그램을 ASCII art로 작성한다:
```
PENDING
  |
  v
QUEUED (Stage 1 INSERT)
  |
  v
EXECUTING (Stage 5 시작)
  |
  +--[gasCondition 존재]--> GAS_WAITING (Stage 3.5)
  |                           |
  |                           +--[조건 충족]--> SIGNED
  |                           +--[타임아웃]--> CANCELLED
  |
  +--[gasCondition 없음]--> SIGNED (Stage 5c)
                              |
                              v
                           SUBMITTED (Stage 5d)
                              |
                              +---> CONFIRMED (Stage 6)
                              +---> FAILED (Stage 6)
                              |
                              +--[bridge/unstake]--> bridge_status: PENDING
                                                      |
                                                      +---> COMPLETED
                                                      +---> FAILED
                                                      +---> BRIDGE_MONITORING
                                                      +---> TIMEOUT
```

GAS_WAITING 전이 규칙:
- 진입: Stage 3 (Policy) 통과 후, gasCondition이 존재하고 현재 가스 가격이 조건 미충족
- 탈출 1: GasConditionWorker가 조건 충족 감지 -> Stage 4 (Wait)부터 파이프라인 재개
- 탈출 2: 타임아웃 -> CANCELLED + TX_CANCELLED 알림

bridge_status 전이 규칙:
- 진입: Stage 6 (Confirm) 완료 후, 브릿지/unstake 메타데이터 존재 시
- PENDING -> COMPLETED: 외부 API에서 완료 확인
- PENDING -> FAILED: 외부 API에서 실패 확인
- PENDING -> BRIDGE_MONITORING: 2시간 폴링 후 미완료 (브릿지 전용)
- BRIDGE_MONITORING -> COMPLETED/FAILED: 축소 빈도(5분) 폴링 계속
- BRIDGE_MONITORING -> TIMEOUT: 24시간 후 미완료

**중요:** bridge_status는 transactions.status와 독립적이다. transactions.status=CONFIRMED인 상태에서 bridge_status=PENDING일 수 있다 (출발 체인 트랜잭션은 확인되었으나 도착 체인 미도착).

ASNC-05에 해당하는 브릿지 타임아웃 정책도 포함한다:
- 기본 폴링: 30초 간격, 최대 240회 (2시간)
- 2시간 후: BRIDGE_MONITORING으로 전환, 5분 간격으로 축소, 최대 24시간
- 24시간 후: TIMEOUT으로 전환, 폴링 중단, 운영자 수동 확인 필요
- **자동 취소 절대 금지**: TIMEOUT은 CANCELLED가 아님. 자금이 limbo에 있을 수 있으므로 SPENDING_LIMIT 예약 유지
- SPENDING_LIMIT 예약 해제 조건: bridge_status = COMPLETED 또는 REFUNDED만
  </action>
  <verify>
m28-00-defi-basic-protocol-design.md의 섹션 4에서:
- "확정 설계" 표시 존재
- IAsyncStatusTracker TypeScript 인터페이스 존재
- 3개 구현체(Bridge, Unstake, GasCondition) 설계 존재
- setTimeout 체인 폴링 스케줄러 설계 존재
- 전이 다이어그램(GAS_WAITING + bridge_status) 존재
- 브릿지 타임아웃 정책 (2시간 -> BRIDGE_MONITORING -> 24시간 -> TIMEOUT) 존재
- ASNC-01~05 모두 커버 확인
  </verify>
  <done>
ASNC-01~05가 m28-00 설계 문서에 확정 설계로 작성되어, AsyncStatusTracker 인터페이스, 폴링 스케줄러, 상태 머신 확장, 통합 DB 마이그레이션 설계가 완성된다
  </done>
</task>

<task type="auto">
  <name>Task 2: 통합 DB 마이그레이션 설계 확정</name>
  <files>internal/objectives/m28-00-defi-basic-protocol-design.md</files>
  <action>
Task 1에서 작성한 섹션 4 내에 ASNC-03 통합 DB 마이그레이션 설계를 확정한다. 기존 ARCHITECTURE.md의 DB Schema Changes 섹션을 참조하되, 더 구체적인 설계로 확정한다.

**ASNC-03: 통합 DB 마이그레이션 확정:**

마이그레이션 버전: v23 (현재 DB v22, v27.2 CAIP-19에서 마지막 마이그레이션)
실행 시점: m28-03 (LI.FI 브릿지, 첫 사용자)

SQL 마이그레이션:
```sql
-- Migration v23: DeFi async tracking + GAS_WAITING state
-- Applied at: m28-03 (first consumer of bridge_status)
-- Also used by: m28-04 (unstake tracking), m28-05 (GAS_WAITING)

-- 1. bridge_status: 비동기 추적 상태 (NULL = 해당 없음)
ALTER TABLE transactions ADD COLUMN bridge_status TEXT
  CHECK (bridge_status IS NULL OR bridge_status IN (
    'PENDING', 'COMPLETED', 'FAILED', 'BRIDGE_MONITORING', 'TIMEOUT', 'REFUNDED'
  ));

-- 2. bridge_metadata: 추적용 JSON 메타데이터
--    bridge: { tracker: 'bridge', tool, fromChainId, toChainId, txHash, estimatedDuration, pollCount, lastPolledAt }
--    unstake: { tracker: 'unstake', protocol: 'lido'|'jito', requestId, pollCount, lastPolledAt }
--    gas: { tracker: 'gas-condition', providerName, originalParams, gasCondition, pollCount, lastPolledAt }
ALTER TABLE transactions ADD COLUMN bridge_metadata TEXT;

-- 3. GAS_WAITING 상태 추가
--    기존 TRANSACTION_STATUSES 배열에 'GAS_WAITING' 추가
--    Drizzle schema의 CHECK constraint 업데이트 필요
--    packages/core/src/enums/transaction.ts의 TRANSACTION_STATUSES에 'GAS_WAITING' 추가

-- 4. 폴링 쿼리 최적화 인덱스
CREATE INDEX idx_transactions_bridge_status
  ON transactions(bridge_status)
  WHERE bridge_status IS NOT NULL;

CREATE INDEX idx_transactions_gas_waiting
  ON transactions(status)
  WHERE status = 'GAS_WAITING';
```

Drizzle 스키마 변경 사항:
- transactions 테이블에 bridge_status TEXT (nullable) 추가
- transactions 테이블에 bridge_metadata TEXT (nullable) 추가
- status CHECK constraint에 'GAS_WAITING' 추가

schema_version 테이블 업데이트:
- version: 23
- description: 'DeFi async tracking + GAS_WAITING state'

bridge_metadata JSON 구조 표:

| tracker 유형 | 필수 필드 | 선택 필드 |
|-------------|----------|----------|
| bridge | tracker, tool, fromChainId, toChainId, txHash | estimatedDuration, receivingTxHash |
| unstake | tracker, protocol, requestId | estimatedCompletionTime |
| gas-condition | tracker, providerName, originalParams, gasCondition | - |
| (공통) | pollCount, lastPolledAt, createdAt | errorMessage |

**마이그레이션 실행 순서:**
1. m28-03에서 마이그레이션 v23 실행 (bridge_status + bridge_metadata + GAS_WAITING)
2. m28-04에서 추가 마이그레이션 없음 (unstake는 bridge_metadata의 tracker='unstake'로 구분)
3. m28-05에서 추가 마이그레이션 없음 (GAS_WAITING은 이미 v23에서 추가)

이것으로 DeFi 관련 DB 변경을 단일 마이그레이션으로 통합하여 마이그레이션 피로도를 최소화한다.
  </action>
  <verify>
m28-00 문서에서:
- 마이그레이션 v23 SQL이 존재
- bridge_status CHECK constraint 값 목록이 존재 (PENDING, COMPLETED, FAILED, BRIDGE_MONITORING, TIMEOUT, REFUNDED)
- bridge_metadata JSON 구조 테이블이 존재
- 인덱스 2개 (bridge_status, gas_waiting) 존재
- "단일 마이그레이션" 원칙이 명시됨
  </verify>
  <done>
ASNC-03 통합 DB 마이그레이션이 확정되어 m28-03에서 단일 마이그레이션으로 bridge_status + bridge_metadata + GAS_WAITING을 추가할 수 있다
  </done>
</task>

</tasks>

<verification>
1. m28-00-defi-basic-protocol-design.md 섹션 4가 "확정 설계"로 표시
2. ASNC-01: IAsyncStatusTracker 인터페이스 + 3개 구현체 설계 존재
3. ASNC-02: setTimeout 체인 폴링 스케줄러 + BackgroundWorkers 통합 설계 존재
4. ASNC-03: 마이그레이션 v23 SQL + bridge_metadata JSON 구조 존재
5. ASNC-04: 전이 다이어그램 (GAS_WAITING + bridge_status) 존재
6. ASNC-05: 브릿지 타임아웃 정책 (2h -> BRIDGE_MONITORING -> 24h -> TIMEOUT) 존재
</verification>

<success_criteria>
- ASNC-01~05 모두 m28-00 설계 문서에 확정 설계로 작성됨
- AsyncStatusTracker 인터페이스와 3개 구현체 설계가 타입 수준에서 명확함
- 폴링 스케줄러가 BackgroundWorkers 기존 패턴과 호환되도록 설계됨
- 트랜잭션 상태 머신 전이 다이어그램이 기존 10-state에 GAS_WAITING을 추가한 11-state로 완성됨
- 통합 DB 마이그레이션이 단일 SQL로 확정됨
- 브릿지 타임아웃 정책이 Research P4 대응으로 2시간+ 폴링, 자동 취소 방지를 명시함
</success_criteria>

<output>
After completion, create `.planning/phases/245-runtime-behavior-design/245-01-SUMMARY.md`
</output>
