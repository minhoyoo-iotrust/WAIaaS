---
phase: 470-ssot-extension-db-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/caip/network-map.ts
  - packages/core/src/caip/asset-helpers.ts
  - packages/core/src/__tests__/caip.test.ts
  - packages/core/src/__tests__/asset-resolve.test.ts
autonomous: true
requirements: [INFRA-05, INFRA-06]
must_haves:
  truths:
    - "CAIP-2 식별자 xrpl:0이 xrpl-mainnet으로, xrpl:1이 xrpl-testnet으로, xrpl:2가 xrpl-devnet으로 변환된다"
    - "networkToCaip2('xrpl-mainnet')이 'xrpl:0'을 반환한다"
    - "nativeAssetId('xrpl-mainnet')이 'xrpl:0/slip44:144'를 반환한다"
    - "tokenAssetId('xrpl-mainnet', 'USD.rHb9CJA...')가 'xrpl:0/token:USD.rHb9CJA...'를 반환한다"
  artifacts:
    - path: "packages/core/src/caip/network-map.ts"
      provides: "CAIP2_TO_NETWORK에 xrpl:0/1/2 항목"
      contains: "xrpl:0"
    - path: "packages/core/src/caip/asset-helpers.ts"
      provides: "NATIVE_SLIP44에 XRP coin type 144, tokenAssetId xrpl namespace 지원"
      contains: "slip44.*144"
  key_links:
    - from: "packages/core/src/caip/asset-helpers.ts"
      to: "packages/core/src/caip/network-map.ts"
      via: "networkToCaip2 import"
      pattern: "networkToCaip2"
    - from: "packages/core/src/caip/network-map.ts"
      to: "packages/core/src/enums/chain.ts"
      via: "ChainType/NetworkType import"
      pattern: "import.*ChainType.*NetworkType"
---

<objective>
CAIP-2/CAIP-19 레지스트리에 XRPL 체인 및 자산 식별자 등록

Purpose: XRPL 네트워크와 자산(네이티브 XRP, Trust Line 토큰)이 표준 CAIP 식별자로 표현되어 REST API 응답에 chainId/assetId가 포함되도록 한다.
Output: CAIP-2 xrpl:0/1/2 매핑, CAIP-19 slip44:144 네이티브 자산, token:{currency}.{issuer} Trust Line 자산 식별자
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v33.6-ROADMAP.md

@packages/core/src/caip/network-map.ts
@packages/core/src/caip/asset-helpers.ts
@packages/core/src/caip/caip2.ts
@packages/core/src/caip/caip19.ts
@packages/core/src/caip/asset-resolve.ts
@packages/core/src/__tests__/caip.test.ts
@packages/core/src/__tests__/asset-resolve.test.ts

<interfaces>
<!-- CAIP-2 spec for XRPL: namespace='xrpl', reference=chain_id_number -->
<!-- XRPL chain IDs: 0=mainnet, 1=testnet, 2=devnet -->
<!-- See: github.com/ChainAgnostic/CAIPs -->

<!-- CAIP-19 for XRPL native: xrpl:{chainId}/slip44:144 (XRP SLIP-44 coin type = 144) -->
<!-- CAIP-19 for Trust Line: xrpl:{chainId}/token:{currency}.{issuer} -->
<!-- currency = 3-char ISO code OR 40-char hex; issuer = r-address -->

From packages/core/src/caip/caip2.ts:
```typescript
export function parseCaip2(chainId: string): Caip2Params;
export function formatCaip2(namespace: string, reference: string): string;
```

From packages/core/src/caip/caip19.ts:
```typescript
export function parseCaip19(assetType: string): Caip19Params;
export function formatCaip19(chainId: string, assetNamespace: string, assetReference: string): string;
```

From packages/core/src/caip/network-map.ts:
```typescript
export const CAIP2_TO_NETWORK: Record<string, { chain: ChainType; network: NetworkType }>;
export const NETWORK_TO_CAIP2: Record<NetworkType, string>;
export function networkToCaip2(network: NetworkType): string;
export function caip2ToNetwork(caip2: string): { chain: ChainType; network: NetworkType };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CAIP-2 네트워크 맵에 XRPL 체인 ID 등록</name>
  <files>packages/core/src/caip/network-map.ts, packages/core/src/__tests__/caip.test.ts</files>
  <behavior>
    - CAIP2_TO_NETWORK['xrpl:0'] = { chain: 'ripple', network: 'xrpl-mainnet' }
    - CAIP2_TO_NETWORK['xrpl:1'] = { chain: 'ripple', network: 'xrpl-testnet' }
    - CAIP2_TO_NETWORK['xrpl:2'] = { chain: 'ripple', network: 'xrpl-devnet' }
    - networkToCaip2('xrpl-mainnet') = 'xrpl:0'
    - networkToCaip2('xrpl-testnet') = 'xrpl:1'
    - networkToCaip2('xrpl-devnet') = 'xrpl:2'
    - caip2ToNetwork('xrpl:0').chain = 'ripple'
    - NETWORK_TO_CAIP2 역매핑이 자동 생성된다
  </behavior>
  <action>
1. packages/core/src/caip/network-map.ts 수정 (per INFRA-05):
   - CAIP2_TO_NETWORK에 XRPL 3개 항목 추가:
     ```typescript
     // XRPL (Ripple)
     'xrpl:0': { chain: 'ripple', network: 'xrpl-mainnet' },
     'xrpl:1': { chain: 'ripple', network: 'xrpl-testnet' },
     'xrpl:2': { chain: 'ripple', network: 'xrpl-devnet' },
     ```
   - XRPL CAIP-2 reference는 XRPL chain_id: 0=mainnet, 1=testnet, 2=devnet
   - NETWORK_TO_CAIP2는 Object.fromEntries로 자동 역매핑 생성 (변경 불필요)

2. packages/core/src/__tests__/caip.test.ts에 XRPL 테스트 추가:
   - xrpl:0 <-> xrpl-mainnet 양방향 변환 검증
   - xrpl:1 <-> xrpl-testnet 양방향 변환 검증
   - xrpl:2 <-> xrpl-devnet 양방향 변환 검증

3. packages/core/src/__tests__/asset-resolve.test.ts에 XRPL 항목 추가:
   - parseAssetId('xrpl:0/slip44:144') -> { chainId: 'xrpl:0', network: 'xrpl-mainnet', isNative: true }
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core test -- --run --grep "caip|CAIP|asset-resolve|network-map"</automated>
  </verify>
  <done>CAIP-2 xrpl:0/1/2가 양방향 매핑 동작, asset-resolve에서 xrpl 네이티브 파싱 동작</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CAIP-19 자산 헬퍼에 XRPL 네이티브/토큰 자산 ID 생성 추가</name>
  <files>packages/core/src/caip/asset-helpers.ts, packages/core/src/__tests__/caip.test.ts</files>
  <behavior>
    - nativeAssetId('xrpl-mainnet') = 'xrpl:0/slip44:144'
    - nativeAssetId('xrpl-testnet') = 'xrpl:1/slip44:144'
    - nativeAssetId('xrpl-devnet') = 'xrpl:2/slip44:144'
    - tokenAssetId('xrpl-mainnet', 'USD.rHb9CJAWyB4rj91VRWn96DkukG4bwdtyTh') = 'xrpl:0/token:USD.rHb9CJAWyB4rj91VRWn96DkukG4bwdtyTh'
    - isNativeAsset('xrpl:0/slip44:144') = true
    - isNativeAsset('xrpl:0/token:USD.rHb9CJAWyB4rj91VRWn96DkukG4bwdtyTh') = false
  </behavior>
  <action>
1. packages/core/src/caip/asset-helpers.ts 수정 (per INFRA-06):
   - NATIVE_SLIP44에 XRPL 3개 항목 추가:
     ```typescript
     // XRPL (XRP) -- SLIP-44 coin type 144
     'xrpl:0': 144,
     'xrpl:1': 144,
     'xrpl:2': 144,
     ```
   - tokenAssetId 함수에 xrpl namespace 분기 추가:
     ```typescript
     if (namespace === 'xrpl') {
       // Trust Line token: {currency}.{issuer} format (original case preserved)
       return formatCaip19(caip2, 'token', address);
     }
     ```
   - Trust Line 토큰 주소 형식: `{currency}.{issuer}` (예: `USD.rHb9CJA...`)
   - 3자리 ISO 코드와 40자리 hex 코드 모두 그대로 전달 (CAIP-19 assetReference로 충분)
   - NFT 관련: nftAssetId에 xrpl namespace 분기는 Phase 473에서 추가 (이 플랜 범위 밖)

2. packages/core/src/__tests__/caip.test.ts에 XRPL 자산 테스트 추가:
   - nativeAssetId 3개 네트워크 검증
   - tokenAssetId Trust Line 형식 검증 (3자리 코드, 40자리 hex 코드)
   - isNativeAsset / isNftAsset 검증
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core test -- --run --grep "caip|CAIP|asset"</automated>
  </verify>
  <done>nativeAssetId가 xrpl:N/slip44:144 형식 반환, tokenAssetId가 xrpl:N/token:{currency}.{issuer} 형식 반환, 모든 CAIP 테스트 통과</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/core test -- --run` 전체 테스트 통과
2. CAIP-2 양방향 매핑 (xrpl:0 <-> xrpl-mainnet) 동작 확인
3. CAIP-19 네이티브 자산 ID (xrpl:0/slip44:144) 생성 확인
4. CAIP-19 Trust Line 토큰 ID (xrpl:0/token:{currency}.{issuer}) 생성 확인
</verification>

<success_criteria>
- CAIP-2 xrpl:0/1/2가 양방향으로 xrpl-mainnet/testnet/devnet와 매핑
- nativeAssetId('xrpl-mainnet') === 'xrpl:0/slip44:144'
- tokenAssetId('xrpl-mainnet', 'USD.rHb9CJA...') === 'xrpl:0/token:USD.rHb9CJA...'
- core 패키지 전체 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/470-ssot-extension-db-migration/470-02-SUMMARY.md`
</output>
