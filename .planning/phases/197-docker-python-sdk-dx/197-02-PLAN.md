---
phase: 197-docker-python-sdk-dx
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - python-sdk/waiaas/__init__.py
  - python-sdk/README.md
  - python-sdk/waiaas/client.py
  - python-sdk/.gitignore
autonomous: true
requirements:
  - PY-01
  - PY-02
  - PY-03

must_haves:
  truths:
    - "Python SDK __version__ equals pyproject.toml version (1.7.0)"
    - "Python SDK README shows localhost:3100, not localhost:3000"
    - ".venv/ directory is not tracked by git"
  artifacts:
    - path: "python-sdk/waiaas/__init__.py"
      provides: "__version__ matching pyproject.toml"
      contains: '__version__ = "1.7.0"'
    - path: "python-sdk/README.md"
      provides: "Correct port 3100 in Quick Start"
      contains: "localhost:3100"
    - path: "python-sdk/.gitignore"
      provides: ".venv/ exclusion rule"
      contains: ".venv/"
  key_links:
    - from: "python-sdk/waiaas/__init__.py"
      to: "python-sdk/pyproject.toml"
      via: "version string must match"
      pattern: '__version__ = "1\\.7\\.0"'
    - from: "python-sdk/README.md"
      to: "python-sdk/waiaas/client.py"
      via: "Quick Start code matches client docstring"
      pattern: "localhost:3100"
---

<objective>
Python SDK 정합성 수정: 버전 동기화, 기본 포트 3100으로 수정, .venv/ gitignore 추가.

Purpose: Python SDK 사용자가 README를 보고 올바른 포트(3100)로 접속하고, 패키지 버전이 일관되며, .venv/가 실수로 커밋되지 않도록.
Output: 수정된 __init__.py (버전 동기화), README.md (포트 수정), client.py (docstring 포트 수정), .gitignore (.venv/ 추가)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@python-sdk/pyproject.toml
@python-sdk/waiaas/__init__.py
@python-sdk/README.md
@python-sdk/waiaas/client.py
@python-sdk/.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python SDK 버전 동기화 + 포트 수정</name>
  <files>python-sdk/waiaas/__init__.py, python-sdk/README.md, python-sdk/waiaas/client.py</files>
  <action>
1. **python-sdk/waiaas/__init__.py**: `__version__ = "0.1.0"` → `__version__ = "1.7.0"` (pyproject.toml의 version과 일치시킴).

2. **python-sdk/README.md**: Quick Start 코드 블록에서 `localhost:3000` → `localhost:3100`으로 수정. 데몬 기본 포트가 3100이므로 README가 이를 반영해야 함. 수정 대상 라인:
   ```python
   async with WAIaaSClient("http://localhost:3100", "wai_sess_xxx") as client:
   ```

3. **python-sdk/waiaas/client.py**: 클래스 docstring의 Usage 예시에서 `localhost:3000` → `localhost:3100`으로 수정. line 43:
   ```python
   async with WAIaaSClient("http://localhost:3100", "wai_sess_xxx") as client:
   ```

주의: pyproject.toml의 version은 `1.7.0`이다. __init__.py의 __version__을 이 값과 정확히 일치시킨다.
  </action>
  <verify>
1. `python3 -c "import tomllib; f=open('python-sdk/pyproject.toml','rb'); d=tomllib.load(f); print(d['project']['version'])"` -- `1.7.0` 출력
2. `grep '__version__' python-sdk/waiaas/__init__.py` -- `__version__ = "1.7.0"` 확인
3. `grep '3000' python-sdk/README.md python-sdk/waiaas/client.py` -- 매칭 0건 (3000 포트 잔존 없음)
4. `grep '3100' python-sdk/README.md python-sdk/waiaas/client.py` -- 매칭 있음
  </verify>
  <done>__version__이 "1.7.0"으로 pyproject.toml과 일치하고, README와 client.py docstring의 기본 포트가 3100이다.</done>
</task>

<task type="auto">
  <name>Task 2: .venv/ gitignore 추가</name>
  <files>python-sdk/.gitignore</files>
  <action>
python-sdk/.gitignore에 `.venv/` 엔트리를 추가. 현재 gitignore에는 `__pycache__/`, `*.py[cod]`, `*.egg-info/`, `dist/`, `build/`, `.pytest_cache/`, `*.egg`만 있고 `.venv/`가 빠져 있다.

기존 내용 뒤에 빈 줄 + `.venv/` 추가:
```
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
*.egg

.venv/
```

추가로, `.venv/`가 이미 git에 추적되고 있는지 확인하고, 추적 중이라면 `git rm -r --cached python-sdk/.venv/`로 제거해야 한다. (현재 `git ls-files python-sdk/.venv/`가 빈 결과이므로 추적되고 있지 않지만, 실행 시 재확인 필요.)
  </action>
  <verify>
1. `grep '.venv/' python-sdk/.gitignore` -- `.venv/` 엔트리 존재 확인
2. `git ls-files python-sdk/.venv/ | wc -l` -- 0 (git에서 추적되지 않음)
  </verify>
  <done>.venv/가 python-sdk/.gitignore에 포함되어 있고, git에서 추적되지 않는다.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import tomllib; f=open('python-sdk/pyproject.toml','rb'); d=tomllib.load(f); v=d['project']['version']; print(v)"` -- pyproject.toml 버전 확인
2. `grep -o '"[0-9.]*"' python-sdk/waiaas/__init__.py` -- __version__ 값이 위와 동일
3. `grep -c '3000' python-sdk/README.md python-sdk/waiaas/client.py` -- 0:0 (잔존 3000 없음)
4. `grep '3100' python-sdk/README.md` -- Quick Start에 3100 포트 존재
5. `grep '.venv/' python-sdk/.gitignore` -- .venv/ 엔트리 존재
6. `git ls-files python-sdk/.venv/ | wc -l` -- 0 (미추적)
</verification>

<success_criteria>
- python-sdk/waiaas/__init__.py의 __version__이 pyproject.toml의 version("1.7.0")과 동일하다
- python-sdk/README.md와 client.py의 기본 포트가 모두 3100이다 (3000 잔존 없음)
- python-sdk/.gitignore에 .venv/가 포함되어 있고 git에서 추적되지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/197-docker-python-sdk-dx/197-02-SUMMARY.md`
</output>
