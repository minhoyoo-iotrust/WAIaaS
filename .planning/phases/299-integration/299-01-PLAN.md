---
phase: 299-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/index.ts
  - packages/daemon/src/infrastructure/settings/hot-reload.ts
autonomous: true
requirements: [INTG-01, INTG-02, INTG-05, INTG-06, INTG-07]

must_haves:
  truths:
    - "DriftPerpProvider가 registerBuiltInProviders에 등록되어 actions.drift_enabled=true 시 자동 활성화된다"
    - "actions.drift_enabled 토글 변경 시 hot-reload로 데몬 재시작 없이 활성화/비활성화된다"
    - "MCP 5개 도구, SDK executeAction, Positions API PERP 카테고리가 등록만으로 자동 작동한다"
  artifacts:
    - path: "packages/actions/src/index.ts"
      provides: "DriftPerpProvider import, re-exports, registerBuiltInProviders entry"
      contains: "key: 'drift_perp'"
    - path: "packages/daemon/src/infrastructure/settings/hot-reload.ts"
      provides: "drift_perp and pendle_yield in BUILTIN_NAMES"
      contains: "drift_perp"
  key_links:
    - from: "packages/actions/src/index.ts"
      to: "packages/actions/src/providers/drift/index.ts"
      via: "import DriftPerpProvider"
      pattern: "import.*DriftPerpProvider.*from.*drift"
    - from: "packages/daemon/src/infrastructure/settings/hot-reload.ts"
      to: "packages/actions/src/index.ts"
      via: "registerBuiltInProviders key matching"
      pattern: "'drift_perp'"
---

<objective>
DriftPerpProvider를 registerBuiltInProviders에 등록하고 hot-reload BUILTIN_NAMES에 추가하여 MCP/SDK/Positions가 자동으로 작동하게 한다.

Purpose: Provider 등록이 완료되면 mcpExpose=true 자동 노출(INTG-01), generic executeAction(INTG-05), IPositionProvider duck-typing(INTG-06)이 모두 코드 변경 없이 자동 작동한다. INTG-02의 5개 설정키는 setting-keys.ts에 이미 정의되어 있고, hot-reload만 추가하면 런타임 조정이 가능하다.
Output: packages/actions/src/index.ts 수정, packages/daemon/src/infrastructure/settings/hot-reload.ts 수정
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/299-integration/299-RESEARCH.md

@packages/actions/src/index.ts
@packages/actions/src/providers/drift/index.ts
@packages/actions/src/providers/drift/config.ts
@packages/daemon/src/infrastructure/settings/hot-reload.ts
@packages/daemon/src/infrastructure/settings/setting-keys.ts

<interfaces>
<!-- DriftPerpProvider and DriftConfig from Phase 298 -->

From packages/actions/src/providers/drift/config.ts:
```typescript
export interface DriftConfig {
  enabled: boolean;
  subAccount: number;
}
export const DRIFT_DEFAULTS: DriftConfig = { enabled: false, subAccount: 0 };
export const DRIFT_PROGRAM_ID = 'dRiftyHA39MWEi3m9aunc5MzRF1JYuBsbn6VPcn33UH';
```

From packages/actions/src/providers/drift/index.ts:
```typescript
export class DriftPerpProvider implements IPerpProvider, IPositionProvider {
  readonly metadata: ActionProviderMetadata; // name: 'drift_perp', mcpExpose: true
  readonly actions: readonly ActionDefinition[]; // 5 actions
  readonly config: DriftConfig;
  constructor(config?: Partial<DriftConfig>, sdkWrapper?: IDriftSdkWrapper);
}
```

From packages/actions/src/providers/drift/drift-sdk-wrapper.ts:
```typescript
export interface IDriftSdkWrapper { ... }
export interface DriftInstruction { programId: string; instructionData: string; accounts: Array<{pubkey: string; isSigner: boolean; isWritable: boolean}>; }
export interface DriftPosition { ... }
export interface DriftMarginInfo { ... }
export class MockDriftSdkWrapper implements IDriftSdkWrapper { ... }
```

From packages/actions/src/index.ts (registerBuiltInProviders pattern):
```typescript
export interface SettingsReader { get(key: string): string; }
export function registerBuiltInProviders(
  registry: ProviderRegistry,
  settingsReader: SettingsReader,
  options?: { rpcCaller?: IRpcCaller },
): { loaded: string[]; skipped: string[] };
```

From packages/daemon/src/infrastructure/settings/hot-reload.ts (BUILTIN_NAMES):
```typescript
// Line 474 -- current list is MISSING pendle_yield and drift_perp
const BUILTIN_NAMES = [
  'jupiter_swap', 'zerox_swap', 'lifi',
  'lido_staking', 'jito_staking',
  'aave_v3', 'kamino',
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DriftPerpProvider to registerBuiltInProviders + re-exports</name>
  <files>packages/actions/src/index.ts</files>
  <action>
1. Add import at the top (after PendleYieldProvider import, around line 22):
```typescript
import { DriftPerpProvider } from './providers/drift/index.js';
import type { DriftConfig } from './providers/drift/config.js';
```

2. Add re-exports after Pendle re-exports (after line 65):
```typescript
export { DriftPerpProvider } from './providers/drift/index.js';
export { DRIFT_DEFAULTS, DRIFT_PROGRAM_ID } from './providers/drift/config.js';
export type { DriftConfig } from './providers/drift/config.js';
export { MockDriftSdkWrapper } from './providers/drift/drift-sdk-wrapper.js';
export type { IDriftSdkWrapper, DriftInstruction, DriftPosition, DriftMarginInfo } from './providers/drift/drift-sdk-wrapper.js';
```

3. Add entry to the `providers` array inside `registerBuiltInProviders()`, after the pendle_yield entry (after line 236):
```typescript
{
  key: 'drift_perp',
  enabledKey: 'actions.drift_enabled',
  factory: () => {
    const config: DriftConfig = {
      enabled: true,
      subAccount: 0,
    };
    return new DriftPerpProvider(config);
  },
},
```

NOTE: DriftConfig only needs `enabled` and `subAccount`. The 5 Admin Settings keys (drift_max_leverage, drift_max_position_usd, etc.) are NOT constructor params -- they are consumed by MarginMonitor and PerpPolicyEvaluator, not the provider factory. Do NOT add settingsReader.get() calls for these keys.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/actions 2>&1 | tail -5</automated>
  </verify>
  <done>DriftPerpProvider가 registerBuiltInProviders providers 배열에 key='drift_perp', enabledKey='actions.drift_enabled'로 등록되고, DriftPerpProvider/DriftConfig/MockDriftSdkWrapper/IDriftSdkWrapper 타입이 @waiaas/actions에서 re-export된다.</done>
</task>

<task type="auto">
  <name>Task 2: Add drift_perp + pendle_yield to BUILTIN_NAMES for hot-reload</name>
  <files>packages/daemon/src/infrastructure/settings/hot-reload.ts</files>
  <action>
In `reloadActionProviders()` method (around line 474), update the `BUILTIN_NAMES` array to include the two missing entries:

Change from:
```typescript
const BUILTIN_NAMES = [
  'jupiter_swap', 'zerox_swap', 'lifi',
  'lido_staking', 'jito_staking',
  'aave_v3', 'kamino',
];
```

To:
```typescript
const BUILTIN_NAMES = [
  'jupiter_swap', 'zerox_swap', 'lifi',
  'lido_staking', 'jito_staking',
  'aave_v3', 'kamino',
  'pendle_yield', 'drift_perp',
];
```

This fixes the pre-existing bug where `pendle_yield` was missing (hot-reload toggle didn't work for Pendle without restart) and adds `drift_perp` for Drift.

Both names MUST match the `key` values used in `registerBuiltInProviders()` exactly.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -5</automated>
  </verify>
  <done>BUILTIN_NAMES에 'pendle_yield'와 'drift_perp'가 추가되어 actions.drift_enabled/actions.pendle_yield_enabled 설정 변경 시 hot-reload가 provider를 정확히 unregister/re-register한다.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck` -- 전체 타입 체크 통과
2. `pnpm turbo run test --filter=@waiaas/actions` -- 기존 액션 테스트 통과
3. `grep -n 'drift_perp' packages/actions/src/index.ts` -- registerBuiltInProviders 엔트리 + re-export 확인
4. `grep -n 'drift_perp' packages/daemon/src/infrastructure/settings/hot-reload.ts` -- BUILTIN_NAMES 포함 확인
5. `grep -n 'pendle_yield' packages/daemon/src/infrastructure/settings/hot-reload.ts` -- pendle_yield 버그픽스 확인
</verification>

<success_criteria>
- registerBuiltInProviders에 drift_perp 엔트리가 있고, actions.drift_enabled=true 시 DriftPerpProvider를 생성/등록한다
- @waiaas/actions에서 DriftPerpProvider, DriftConfig, DRIFT_DEFAULTS, DRIFT_PROGRAM_ID, MockDriftSdkWrapper, IDriftSdkWrapper가 export된다
- BUILTIN_NAMES에 pendle_yield + drift_perp가 모두 포함되어 hot-reload가 정상 작동한다
- 기존 테스트가 모두 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/299-integration/299-01-SUMMARY.md`
</output>
