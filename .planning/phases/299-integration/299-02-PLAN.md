---
phase: 299-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/actions.tsx
  - packages/admin/src/utils/settings-helpers.ts
  - skills/actions.skill.md
  - packages/skills/skills/actions.skill.md
autonomous: true
requirements: [INTG-03, INTG-04]

must_haves:
  truths:
    - "Admin UI Actions 페이지에 Drift Perp Trading 카드가 표시되고 활성화/비활성화 토글이 작동한다"
    - "Drift 카드가 활성화되면 4개 고급 설정(max_leverage, max_position_usd, margin_warning_threshold_pct, position_sync_interval_sec)이 인라인 편집 가능하다"
    - "actions.skill.md에 Drift Perp Trading 섹션이 5개 액션, 설정 테이블, 예제와 함께 문서화된다"
  artifacts:
    - path: "packages/admin/src/pages/actions.tsx"
      provides: "Drift Perp card in BUILTIN_PROVIDERS + advanced settings block"
      contains: "drift_perp"
    - path: "packages/admin/src/utils/settings-helpers.ts"
      provides: "Human-readable labels for 5 drift setting keys"
      contains: "drift_max_leverage"
    - path: "skills/actions.skill.md"
      provides: "Section 11 Drift Perp Trading documentation"
      contains: "Drift Perp Trading"
    - path: "packages/skills/skills/actions.skill.md"
      provides: "Mirror of skills/actions.skill.md"
      contains: "Drift Perp Trading"
  key_links:
    - from: "packages/admin/src/pages/actions.tsx"
      to: "packages/admin/src/utils/settings-helpers.ts"
      via: "keyToLabel() for drift setting labels"
      pattern: "keyToLabel"
    - from: "packages/admin/src/pages/actions.tsx"
      to: "Admin Settings API"
      via: "handleAdvancedSave -> apiPut(API.ADMIN_SETTINGS)"
      pattern: "handleAdvancedSave"
---

<objective>
Admin UI에 Drift Perp Trading 카드와 고급 설정을 추가하고, actions.skill.md에 Drift 섹션을 문서화한다.

Purpose: Admin이 Drift를 GUI에서 활성화/비활성화하고 정책 파라미터를 런타임 조정할 수 있어야 한다(INTG-03). AI 에이전트가 Drift Perp 기능을 검색할 수 있도록 skill 파일에 문서화한다(INTG-04).
Output: Admin UI Actions 페이지 Drift 카드, settings-helpers.ts 라벨, actions.skill.md 섹션 11
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/299-integration/299-RESEARCH.md

@packages/admin/src/pages/actions.tsx
@packages/admin/src/utils/settings-helpers.ts
@skills/actions.skill.md

<interfaces>
<!-- Admin UI patterns from actions.tsx -->

From packages/admin/src/pages/actions.tsx:
```typescript
interface BuiltinProvider {
  key: string;
  name: string;
  description: string;
  chain: 'solana' | 'evm' | 'multi';
  requiresApiKey: boolean;
  docsUrl?: string;
}

const BUILTIN_PROVIDERS: BuiltinProvider[] = [
  // ... 8 existing entries (jupiter_swap through pendle_yield)
];

// Advanced settings pattern (Aave V3 example at line 348):
// bp.key === 'aave_v3' && enabled && ( <div> ... advancedDirty ... handleAdvancedSave ... </div> )
```

From packages/admin/src/utils/settings-helpers.ts:
```typescript
export function keyToLabel(key: string): string {
  const map: Record<string, string> = {
    // ... existing labels
    // Need to add: drift_enabled, drift_max_leverage, drift_max_position_usd,
    //   drift_margin_warning_threshold_pct, drift_position_sync_interval_sec
  };
  return map[key] ?? key.replace(/_/g, ' ').replace(/\b\w/g, (c) => c.toUpperCase());
}
```

<!-- DriftPerpProvider action metadata -->
From packages/actions/src/providers/drift/index.ts:
```typescript
// 5 actions:
// drift_open_position   - high risk, APPROVAL tier, solana
// drift_close_position  - medium risk, DELAY tier, solana
// drift_modify_position - high risk, APPROVAL tier, solana
// drift_add_margin      - low risk, INSTANT tier, solana
// drift_withdraw_margin - medium risk, DELAY tier, solana
```

<!-- Drift input schemas -->
From packages/actions/src/providers/drift/schemas.ts:
```typescript
// OpenPositionInputSchema: market, direction(LONG/SHORT), size, leverage?(1-100), orderType(MARKET/LIMIT), limitPrice?
// ClosePositionInputSchema: market, size?(omit for full close)
// ModifyPositionInputSchema: market, newSize?, newLimitPrice? (at least one required)
// AddMarginInputSchema: amount, asset (CAIP-19)
// WithdrawMarginInputSchema: amount, asset (CAIP-19)
```

<!-- Drift Admin Settings (already defined in setting-keys.ts) -->
5 settings keys:
- actions.drift_enabled (default: 'false')
- actions.drift_max_leverage (default: '5')
- actions.drift_max_position_usd (default: '10000')
- actions.drift_margin_warning_threshold_pct (default: '0.15')
- actions.drift_position_sync_interval_sec (default: '60')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Drift Perp card + advanced settings to Admin UI Actions page</name>
  <files>packages/admin/src/pages/actions.tsx, packages/admin/src/utils/settings-helpers.ts</files>
  <action>
**Step 1: settings-helpers.ts -- Add Drift labels to keyToLabel map**

Add the following entries to the `map` object in `keyToLabel()` (after the kamino keys block, around line 140, before `// gas_condition keys`):

```typescript
// drift perp keys
drift_enabled: 'Drift Perp Enabled',
drift_max_leverage: 'Max Leverage',
drift_max_position_usd: 'Max Position Size (USD)',
drift_margin_warning_threshold_pct: 'Margin Warning Threshold (%)',
drift_position_sync_interval_sec: 'Position Sync Interval (seconds)',
```

**Step 2: actions.tsx -- Add Drift card to BUILTIN_PROVIDERS array**

Add after the `pendle_yield` entry (end of the array, before the closing `]`):

```typescript
{ key: 'drift_perp', name: 'Drift Perp', description: 'Solana perpetual futures trading (open, close, modify positions with leverage)', chain: 'solana', requiresApiKey: false, docsUrl: 'https://docs.drift.trade' },
```

**Step 3: actions.tsx -- Add Drift advanced settings block**

Add a new conditional block after the Kamino advanced settings block (after line 415, before the `{/* Actions list` comment). Follow the exact same pattern as Aave V3 (line 348) and Kamino (line 383):

```tsx
{/* Drift Perp Advanced Settings -- only when Drift is enabled */}
{bp.key === 'drift_perp' && enabled && (
  <div style={{ borderTop: '1px solid var(--border)', paddingTop: 'var(--space-3)', marginBottom: 'var(--space-3)' }}>
    <div style={{ fontWeight: 600, fontSize: 'var(--font-size-sm)', marginBottom: 'var(--space-2)', color: 'var(--text-secondary)' }}>
      Advanced Settings
    </div>
    {(['drift_max_leverage', 'drift_max_position_usd', 'drift_margin_warning_threshold_pct', 'drift_position_sync_interval_sec'] as const).map((shortKey) => {
      const cat = settings.value['actions'] as Record<string, string> | undefined;
      const currentValue = advancedDirty.value[shortKey] ?? cat?.[shortKey] ?? '';
      return (
        <div
          key={shortKey}
          style={{ marginBottom: 'var(--space-2)' }}
          onBlur={() => {
            const val = advancedDirty.value[shortKey];
            if (val !== undefined) {
              void handleAdvancedSave(shortKey, val);
            }
          }}
        >
          <FormField
            label={keyToLabel(shortKey)}
            name={`actions.${shortKey}`}
            type="text"
            value={currentValue}
            onChange={(v) => {
              advancedDirty.value = { ...advancedDirty.value, [shortKey]: String(v) };
            }}
          />
        </div>
      );
    })}
  </div>
)}
```

The advanced settings block shows 4 keys (NOT drift_enabled, which is the main toggle). Each field saves on blur via `handleAdvancedSave` (already defined, reusable for all providers).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/admin 2>&1 | tail -5</automated>
  </verify>
  <done>Admin UI Actions 페이지에 Drift Perp 카드가 표시되고 (name='Drift Perp', chain='solana', requiresApiKey=false), 활성화 시 4개 고급 설정이 인라인 편집 가능하며, keyToLabel이 모든 drift 키에 대해 human-readable 라벨을 반환한다.</done>
</task>

<task type="auto">
  <name>Task 2: Add Drift Perp Trading section to actions.skill.md</name>
  <files>skills/actions.skill.md, packages/skills/skills/actions.skill.md</files>
  <action>
**Step 1: Update frontmatter tags**

In the `tags` array at the top of `skills/actions.skill.md`, add drift-related tags. Change:
```yaml
tags: [wallet, blockchain, defi, actions, waiass, jupiter, 0x, swap, lifi, bridge, cross-chain, lido, jito, staking, liquid-staking, pendle, yield, pt, yt]
```
To:
```yaml
tags: [wallet, blockchain, defi, actions, waiass, jupiter, 0x, swap, lifi, bridge, cross-chain, lido, jito, staking, liquid-staking, pendle, yield, pt, yt, drift, perp, perpetual, leverage, futures]
```

**Step 2: Renumber section 11 "Policy Integration" to section 12**

The current "## 11. Policy Integration" (around line 1403) becomes "## 12. Policy Integration".

**Step 3: Add new section 11 "Drift Perp Trading" before old section 11**

Insert after the Pendle section ends (after the "## 10. Pendle Yield Trading" section's last subsection "Position Monitoring", around line 1401) and before the renumbered Policy Integration section. The full section content:

```markdown
## 11. Drift Perp Trading -- Built-in Provider (Solana)

The Drift Perp Trading provider uses the [Drift Protocol V2](https://drift.trade/) to open, close, and modify leveraged perpetual futures positions on Solana. It supports LONG/SHORT positions with market and limit orders, margin management, and automated position/margin monitoring.

> AI agents must NEVER request the master password. Use only your session token.

### Configuration

Enable Drift Perp Trading via **Admin UI > Settings > Actions > Drift Perp**, or environment variables. No API key is required.

| Setting | Env Variable | Default | Description |
| ------- | ------------ | ------- | ----------- |
| Enabled | `WAIAAS_ACTIONS_DRIFT_ENABLED` | `false` | Enable Drift Perp Trading provider |
| Max Leverage | `WAIAAS_ACTIONS_DRIFT_MAX_LEVERAGE` | `5` | Maximum allowed leverage multiplier (policy enforcement) |
| Max Position Size (USD) | `WAIAAS_ACTIONS_DRIFT_MAX_POSITION_USD` | `10000` | Maximum single position size in USD (policy enforcement) |
| Margin Warning Threshold | `WAIAAS_ACTIONS_DRIFT_MARGIN_WARNING_THRESHOLD_PCT` | `0.15` | Margin ratio threshold for warning alerts (lower = more dangerous) |
| Position Sync Interval | `WAIAAS_ACTIONS_DRIFT_POSITION_SYNC_INTERVAL_SEC` | `60` | Position sync polling interval in seconds |

### Available Actions

| Action | Description | Chain | Risk | Tier |
| ------ | ----------- | ----- | ---- | ---- |
| `drift_open_position` | Open a leveraged perpetual position (LONG/SHORT) with market or limit order | solana | high | APPROVAL |
| `drift_close_position` | Close a perpetual position (full or partial close) | solana | medium | DELAY |
| `drift_modify_position` | Modify position size or pending order limit price | solana | high | APPROVAL |
| `drift_add_margin` | Deposit collateral to increase available margin | solana | low | INSTANT |
| `drift_withdraw_margin` | Withdraw excess collateral from margin account | solana | medium | DELAY |

### Supported Chains

| Network | WAIaaS Network Name |
| ------- | ------------------- |
| Solana Mainnet | `solana-mainnet` |

### Open Position Parameters (drift_open_position)

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `market` | string | Yes | Market symbol (e.g., `"SOL-PERP"`) |
| `direction` | string | Yes | Position direction: `"LONG"` or `"SHORT"` |
| `size` | string | Yes | Base asset amount as string (e.g., `"100"`) |
| `leverage` | number | No | Desired leverage multiplier (1-100, derived from margin if omitted) |
| `orderType` | string | Yes | Order type: `"MARKET"` or `"LIMIT"` |
| `limitPrice` | string | Conditional | Required when orderType is `"LIMIT"` |

### Close Position Parameters (drift_close_position)

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `market` | string | Yes | Market symbol (e.g., `"SOL-PERP"`) |
| `size` | string | No | Partial close amount; omit for full close |

### Modify Position Parameters (drift_modify_position)

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `market` | string | Yes | Market symbol (e.g., `"SOL-PERP"`) |
| `newSize` | string | No | New position size (at least one of newSize or newLimitPrice required) |
| `newLimitPrice` | string | No | New limit price for pending order |

### Add Margin Parameters (drift_add_margin)

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `amount` | string | Yes | Collateral amount as string (e.g., `"100"`) |
| `asset` | string | Yes | CAIP-19 asset identifier for collateral token |

### Withdraw Margin Parameters (drift_withdraw_margin)

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `amount` | string | Yes | Withdrawal amount as string |
| `asset` | string | Yes | CAIP-19 asset identifier for collateral token |

### Safety Features

- **Policy enforcement**: PerpPolicyEvaluator enforces max_leverage and max_position_usd limits. Exceeding warning zone triggers DELAY tier; exceeding hard limit triggers DENY.
- **Allowed markets**: PERP_ALLOWED_MARKETS policy restricts which markets can be traded (default-deny).
- **Margin monitoring**: MarginMonitor polls margin ratios and sends MARGIN_WARNING notifications at configurable thresholds (0.30 = warning, 0.15 = danger, 0.10 = critical/liquidation imminent).
- **Zod runtime validation**: All action inputs are validated against Zod schemas with refinement checks (e.g., limitPrice required for LIMIT orders).
- **Provider-trust bypass**: When Drift is enabled in Admin Settings, the CONTRACT_WHITELIST policy check is skipped for Drift-resolved transactions.
- **Position tracking**: Perp positions (category=PERP) are tracked in the defi_positions table with margin, leverage, unrealized PnL, and liquidation price metadata.

### Example: Open Long Position

**REST API:**
```bash
curl -s -X POST http://localhost:3100/v1/actions/drift_perp/drift_open_position \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer wai_sess_eyJ...' \
  -d '{
    "params": {
      "market": "SOL-PERP",
      "direction": "LONG",
      "size": "100",
      "leverage": 5,
      "orderType": "MARKET"
    },
    "network": "solana-mainnet"
  }'
```

**MCP Tool:**
```json
{
  "tool": "action_drift_perp_drift_open_position",
  "params": {
    "market": "SOL-PERP",
    "direction": "LONG",
    "size": "100",
    "leverage": 5,
    "orderType": "MARKET"
  },
  "network": "solana-mainnet"
}
```

**TypeScript SDK:**
```typescript
import { WAIaaSClient } from '@waiaas/sdk';

const client = new WAIaaSClient({ baseUrl: 'http://localhost:3100', token: 'wai_sess_...' });

const tx = await client.executeAction('drift_perp', 'drift_open_position', {
  params: {
    market: 'SOL-PERP',
    direction: 'LONG',
    size: '100',
    leverage: 5,
    orderType: 'MARKET',
  },
  network: 'solana-mainnet',
});
console.log('Transaction ID:', tx.id);
```

**Python SDK:**
```python
from waiaas import WAIaaSClient

async with WAIaaSClient(base_url="http://localhost:3100", token="wai_sess_...") as client:
    tx = await client.execute_action("drift_perp", "drift_open_position", {
        "market": "SOL-PERP",
        "direction": "LONG",
        "size": "100",
        "leverage": 5,
        "orderType": "MARKET",
    }, network="solana-mainnet")
    print("Transaction ID:", tx.id)
```

### Example: Add Margin

**REST API:**
```bash
curl -s -X POST http://localhost:3100/v1/actions/drift_perp/drift_add_margin \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer wai_sess_eyJ...' \
  -d '{
    "params": {
      "amount": "50",
      "asset": "solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp/slip44:501"
    },
    "network": "solana-mainnet"
  }'
```

### Position Monitoring

Drift perp positions are automatically tracked via PositionTracker and stored in the defi_positions table (category=PERP). MarginMonitor checks margin ratios periodically and sends alerts.

- **Positions**: `GET /v1/wallets/:id/positions` or MCP tool `waiaas_get_defi_positions`
- **Margin alerts**: Automatic at thresholds 0.30 (warning), 0.15 (danger), 0.10 (critical)
- **Liquidation alerts**: LIQUIDATION_IMMINENT notification when approaching liquidation price
```

**Step 4: Copy the updated file to the mirror location**

After updating `skills/actions.skill.md`, copy the entire file content to `packages/skills/skills/actions.skill.md` to keep them in sync.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c 'Drift Perp' skills/actions.skill.md && grep -c 'drift_open_position' skills/actions.skill.md && diff skills/actions.skill.md packages/skills/skills/actions.skill.md | head -3</automated>
  </verify>
  <done>actions.skill.md에 "## 11. Drift Perp Trading" 섹션이 5개 액션 파라미터 테이블, 설정 테이블, REST/MCP/SDK 예제, 안전 기능 설명과 함께 추가되고, frontmatter tags에 drift/perp/perpetual/leverage/futures가 포함되며, packages/skills/skills/actions.skill.md 미러가 동기화된다.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/admin` -- Admin UI 타입 체크 통과
2. `grep -n 'drift_perp' packages/admin/src/pages/actions.tsx` -- BUILTIN_PROVIDERS 엔트리 + advanced settings 블록 확인
3. `grep -n 'drift_max_leverage' packages/admin/src/utils/settings-helpers.ts` -- keyToLabel 라벨 확인
4. `grep -c 'drift_open_position' skills/actions.skill.md` -- 최소 3회 (테이블 + REST + MCP + SDK 예제)
5. `diff skills/actions.skill.md packages/skills/skills/actions.skill.md` -- 미러 동기화 확인 (차이 없음)
</verification>

<success_criteria>
- Admin UI Actions 페이지에 Drift Perp 카드가 9번째로 표시되고 enable/disable 토글이 작동한다
- Drift 카드 활성화 시 4개 고급 설정(max_leverage, max_position_usd, margin_warning_threshold_pct, position_sync_interval_sec)이 편집 가능하다
- keyToLabel이 5개 drift 키(drift_enabled 포함)에 대해 human-readable 라벨을 반환한다
- actions.skill.md section 11에 Drift Perp Trading이 5개 액션, 설정 테이블, REST/MCP/SDK 예제와 함께 문서화된다
- skills 미러 파일이 동기화된다
</success_criteria>

<output>
After completion, create `.planning/phases/299-integration/299-02-SUMMARY.md`
</output>
