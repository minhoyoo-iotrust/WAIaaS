---
phase: 253-인터페이스-통합
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/actions.skill.md
  - packages/mcp/src/__tests__/action-provider-lifi.test.ts
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]

must_haves:
  truths:
    - "MCP tool list includes action_lifi_cross_swap and action_lifi_bridge when lifi provider is enabled with mcpExpose=true"
    - "TS SDK executeAction('lifi', 'cross_swap', {...}) and executeAction('lifi', 'bridge', {...}) produce correct POST requests"
    - "Python SDK execute_action('lifi', 'cross_swap', {...}) and execute_action('lifi', 'bridge', {...}) produce correct POST requests"
    - "actions.skill.md contains LI.FI section with configuration, parameters, safety features, and examples for all 4 interfaces (REST/MCP/TS SDK/Python SDK)"
  artifacts:
    - path: "skills/actions.skill.md"
      provides: "LI.FI cross-chain bridge documentation (Section 5)"
      contains: "LI.FI"
    - path: "packages/mcp/src/__tests__/action-provider-lifi.test.ts"
      provides: "MCP LiFi tool registration verification tests"
      min_lines: 40
  key_links:
    - from: "packages/actions/src/providers/lifi/index.ts"
      to: "packages/mcp/src/tools/action-provider.ts"
      via: "mcpExpose: true in LiFiActionProvider.metadata -> registerActionProviderTools() -> action_lifi_cross_swap + action_lifi_bridge"
      pattern: "action_lifi_(cross_swap|bridge)"
    - from: "skills/actions.skill.md"
      to: "packages/actions/src/providers/lifi/index.ts"
      via: "documentation of fromChain/toChain/fromToken/toToken/fromAmount/slippage/toAddress params"
      pattern: "lifi.*cross_swap|lifi.*bridge"
---

<objective>
Verify MCP auto-exposure and SDK compatibility for LiFi cross-chain bridge/swap, then document the provider in actions.skill.md.

Purpose: AI agents need to discover and use LI.FI cross-chain bridge/swap through MCP tools, SDK methods, and skill file documentation. The underlying infrastructure (registerActionProviderTools, executeAction/execute_action) already works generically for any provider with mcpExpose=true -- this plan verifies it works for the specific LiFi provider and creates comprehensive documentation.

Output: 1 test file verifying MCP tool registration for LiFi, updated actions.skill.md with LI.FI Section 5.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 252 context (LiFi provider implementation)
@packages/actions/src/providers/lifi/index.ts
@packages/actions/src/providers/lifi/config.ts
@packages/actions/src/providers/lifi/lifi-api-client.ts

# MCP auto-registration mechanism
@packages/mcp/src/tools/action-provider.ts
@packages/mcp/src/__tests__/action-provider.test.ts

# Current skill file (add LI.FI section)
@skills/actions.skill.md

# Prior integration phase summary (v28.2 pattern reference)
@.planning/phases/250-integration/250-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MCP LiFi tool registration verification test</name>
  <files>packages/mcp/src/__tests__/action-provider-lifi.test.ts</files>
  <action>
Create a focused test file `packages/mcp/src/__tests__/action-provider-lifi.test.ts` that verifies the existing `registerActionProviderTools()` mechanism works correctly for the LiFi provider's 2 actions. This is NOT testing new code -- it's verifying the auto-registration pipeline works end-to-end for LiFi-shaped provider data.

Use the same mock patterns from `packages/mcp/src/__tests__/action-provider.test.ts` (createMockApiClient, createMockServer, makeProvidersResponse).

Tests to write (5 tests):

1. **LiFi provider with mcpExpose=true registers 2 MCP tools**: Create provider data matching LiFi metadata (name='lifi', mcpExpose=true, chains=['ethereum','solana'], 2 actions: cross_swap + bridge). Assert `registerActionProviderTools` returns Map with exactly 2 entries: `action_lifi_cross_swap` and `action_lifi_bridge`.

2. **action_lifi_cross_swap handler calls POST /v1/actions/lifi/cross_swap**: Invoke the captured handler with params `{ fromChain: 'solana', toChain: 'base', fromToken: 'So11...', toToken: '0xA0b8...', fromAmount: '1000000000' }`. Assert `apiClient.post` was called with `/v1/actions/lifi/cross_swap` and correct body.

3. **action_lifi_bridge handler calls POST /v1/actions/lifi/bridge**: Invoke with params `{ fromChain: 'ethereum', toChain: 'arbitrum', fromToken: '0xA0b8...eB48', toToken: '0xA0b8...eB48', fromAmount: '100000000' }`. Assert `/v1/actions/lifi/bridge`.

4. **LiFi tools pass network and wallet_id correctly**: Invoke handler with `{ params: {...}, network: 'base-mainnet', wallet_id: 'w-123' }`. Assert POST body includes `{ params: {...}, network: 'base-mainnet', walletId: 'w-123' }`.

5. **LiFi tool descriptions include chain and risk info**: Assert tool descriptions contain 'chain:', 'risk: high', and '[lifi]'.

Import `registerActionProviderTools` from `'../tools/action-provider.js'`. Reuse the mock factory patterns exactly as in the existing test file.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/mcp test -- --run packages/mcp/src/__tests__/action-provider-lifi.test.ts 2>&1 | tail -20</automated>
    <manual>5 tests pass, all covering LiFi-specific MCP tool registration</manual>
  </verify>
  <done>5 tests pass verifying action_lifi_cross_swap and action_lifi_bridge are registered by the existing registerActionProviderTools mechanism with correct REST routing, parameter forwarding, and tool metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Update actions.skill.md with LI.FI cross-chain bridge documentation</name>
  <files>skills/actions.skill.md</files>
  <action>
Update `skills/actions.skill.md` to add LI.FI as Section 5 (shifting existing sections 5-9 to 6-10). Follow the exact pattern established by Section 3 (Jupiter Swap) and Section 4 (0x Swap).

**Changes to make:**

1. **Update frontmatter**: Add `lifi, bridge, cross-chain` to tags array. Bump version to `2.8.3`.

2. **Update GET /v1/actions/providers response example** (Section 1): Add a third provider entry for lifi:
```json
{
  "name": "lifi",
  "description": "LI.FI cross-chain bridge and swap aggregator (100+ bridges, 40+ chains)",
  "version": "1.0.0",
  "chains": ["ethereum", "solana"],
  "mcpExpose": true,
  "requiresApiKey": false,
  "hasApiKey": false,
  "actions": [
    {
      "name": "cross_swap",
      "description": "Cross-chain bridge and swap via LI.FI aggregator",
      "chain": "ethereum",
      "riskLevel": "high",
      "defaultTier": "DELAY"
    },
    {
      "name": "bridge",
      "description": "Simple cross-chain bridge via LI.FI",
      "chain": "ethereum",
      "riskLevel": "high",
      "defaultTier": "DELAY"
    }
  ]
}
```

3. **Add Section 5: LI.FI Cross-Chain Bridge -- Built-in Provider (Multi-Chain)** with these subsections:

  a. **Configuration**: Table with env variables matching `config.toml [actions]` lifi keys:
     - `WAIAAS_ACTIONS_LIFI_ENABLED` (default false)
     - `WAIAAS_ACTIONS_LIFI_API_KEY` (empty, optional -- relaxes rate limits)
     - `WAIAAS_ACTIONS_LIFI_API_BASE_URL` (default `https://li.quest/v1`)
     - `WAIAAS_ACTIONS_LIFI_DEFAULT_SLIPPAGE_PCT` (default 0.03 = 3%)
     - `WAIAAS_ACTIONS_LIFI_MAX_SLIPPAGE_PCT` (default 0.05 = 5%)
     Note: No API key required (unlike 0x). LI.FI works without key but with rate limits.

  b. **Actions**: Two actions table -- `cross_swap` (bridge + swap different tokens across chains) and `bridge` (same token across chains). Both use the same parameters.

  c. **Cross-Swap / Bridge Parameters**: Table with all 7 params from LiFiCrossSwapInputSchema:
     - `fromChain` (string, required) -- source chain name (e.g., solana, ethereum, base)
     - `toChain` (string, required) -- destination chain name
     - `fromToken` (string, required) -- source token address or symbol
     - `toToken` (string, required) -- destination token address or symbol
     - `fromAmount` (string, required) -- amount in smallest units
     - `slippage` (number, optional) -- decimal 0-1, default 0.03 (3%), clamped to max 0.05 (5%)
     - `toAddress` (string, optional) -- destination address, defaults to wallet address

  d. **Supported Chains**: Table listing all LIFI_CHAIN_MAP entries:
     - Solana (LI.FI ID 1151111081099710)
     - Ethereum (1), Polygon (137), Arbitrum (42161), Optimism (10), Base (8453)

  e. **Safety Features** (bullet list):
     - Slippage protection: Default 3%, max 5% clamp (decimal, not bps)
     - Same-chain rejection: LI.FI API handles routing validation
     - Unsupported chain error: Descriptive error with list of supported chains
     - Zod runtime validation: All LI.FI API responses validated against Zod schemas
     - Provider-trust bypass: When LI.FI enabled, CONTRACT_WHITELIST skipped
     - 2-phase bridge monitoring: Active polling 30s x 240 (2h) then reduced 5min x 264 (22h)
     - SPENDING_LIMIT reservation: Released on COMPLETED/FAILED/REFUNDED, held on TIMEOUT

  f. **Bridge Status Tracking**: Brief explanation of the async 2-phase polling lifecycle (PENDING -> bridge active polling -> BRIDGE_MONITORING -> reduced polling -> terminal state). 5 notification events: BRIDGE_COMPLETED, BRIDGE_FAILED, BRIDGE_MONITORING_STARTED, BRIDGE_TIMEOUT, BRIDGE_REFUNDED.

  g. **Example: Bridge USDC from Ethereum to Arbitrum** with 4 code blocks:
     - REST API: `POST /v1/actions/lifi/bridge` with params
     - MCP Tool: `{ "tool": "action_lifi_bridge", "params": {...} }`
     - TypeScript SDK: `client.executeAction('lifi', 'bridge', { params: {...} })`
     - Python SDK: `client.execute_action('lifi', 'bridge', { ... })`

  h. **Example: Cross-Chain Swap SOL to Base USDC** with 4 code blocks:
     - REST API: `POST /v1/actions/lifi/cross_swap`
     - MCP Tool: `{ "tool": "action_lifi_cross_swap", "params": {...} }`
     - TypeScript SDK: `client.executeAction('lifi', 'cross_swap', { params: {...} })`
     - Python SDK: `client.execute_action('lifi', 'cross_swap', { ... })`

4. **Update Section 6 (was 5) Policy Integration**: Add LI.FI entry:
   - **LI.FI Bridge**: Routes resolved dynamically by LI.FI API (no fixed contract address). Provider-trust bypass applies when enabled.
   - Add note about SPENDING_LIMIT reservation lifecycle for bridge transactions.

5. **Update Section 8 (was 7) Error Reference**: Add LI.FI-specific errors:
   - `UNSUPPORTED_CHAIN` -- Chain not supported by LI.FI integration
   - `ACTION_API_ERROR` -- LI.FI API returned an error

6. **Update Section 9 (was 8) MCP Auto-Registration**: Add 2 new tools to the current MCP tools list:
   - `action_lifi_cross_swap` -- LI.FI Cross-Chain Swap (multi-chain)
   - `action_lifi_bridge` -- LI.FI Cross-Chain Bridge (multi-chain)

**Important patterns to follow:**
- Use the exact same formatting style as Jupiter Swap (Section 3) and 0x Swap (Section 4) sections
- Include the security notice at the top: `> AI agents must NEVER request the master password. Use only your session token.`
- Use `fromChain`/`toChain` (not `chainId`) since LI.FI is multi-chain
- Slippage is in DECIMAL (0.03 = 3%), NOT basis points (unlike Jupiter/0x which use bps)
- Note that API key is optional (not required like 0x)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c "LI.FI" skills/actions.skill.md && grep -c "action_lifi" skills/actions.skill.md && grep -c "cross_swap" skills/actions.skill.md && grep -c "bridge" skills/actions.skill.md</automated>
    <manual>actions.skill.md contains LI.FI Section 5 with configuration, parameters, supported chains, safety features, bridge status tracking, 2 examples with 4 code blocks each, and updated MCP tool list including action_lifi_cross_swap and action_lifi_bridge</manual>
  </verify>
  <done>actions.skill.md updated with comprehensive LI.FI documentation: Section 5 with config table, 2 actions, 7 parameters, 6 supported chains, safety features, bridge status tracking, 2 complete examples (REST/MCP/TS/Python each), updated policy section, error reference, and MCP tool list showing all 4 action tools.</done>
</task>

</tasks>

<verification>
1. Run LiFi MCP test: `pnpm --filter @waiaas/mcp test -- --run packages/mcp/src/__tests__/action-provider-lifi.test.ts` -- 5 tests pass
2. Run existing MCP tests: `pnpm --filter @waiaas/mcp test -- --run` -- all existing tests still pass (no regression)
3. Verify actions.skill.md contains: "LI.FI" (10+ occurrences), "action_lifi_cross_swap" (3+ occurrences), "action_lifi_bridge" (3+ occurrences), "fromChain" (5+ occurrences)
4. Verify actions.skill.md section count: 10 sections (was 9 -- Section 5 added)
5. Typecheck: `pnpm --filter @waiaas/mcp run typecheck` passes
</verification>

<success_criteria>
- 5 new MCP LiFi tool registration tests pass, proving action_lifi_cross_swap and action_lifi_bridge are auto-registered
- Existing 7 MCP action-provider tests still pass (no regression)
- actions.skill.md has LI.FI section with: config table, 2 actions, 7 params, 6 chains, safety features, bridge tracking, 2 examples (8 code blocks total)
- MCP Auto-Registration section lists all 4 action tools (jupiter_swap_swap, zerox_swap_swap, lifi_cross_swap, lifi_bridge)
- Typecheck passes for @waiaas/mcp package
</success_criteria>

<output>
After completion, create `.planning/phases/253-인터페이스-통합/253-01-SUMMARY.md`
</output>
