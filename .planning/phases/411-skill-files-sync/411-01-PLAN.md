---
phase: 411-skill-files-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/quickstart.skill.md
  - skills/wallet.skill.md
  - skills/transactions.skill.md
  - skills/policies.skill.md
autonomous: true
requirements: [DOC-05, DOC-06, DOC-07]

must_haves:
  truths:
    - "skills/*.skill.md 파일에 CAIP-2 네트워크 지정 형식과 예시가 포함된다"
    - "토큰 전송 예시에 assetId 단독 사용 패턴이 문서화된다"
    - "네트워크 지정 예시에 CAIP-2 형식이 기존 plain string과 병기된다"
  artifacts:
    - path: "skills/quickstart.skill.md"
      provides: "CAIP-2 network + assetId-only 패턴 quickstart 반영"
      contains: "eip155:1"
    - path: "skills/wallet.skill.md"
      provides: "CAIP-2 network 사용법 반영"
      contains: "eip155:1"
    - path: "skills/transactions.skill.md"
      provides: "assetId-only TOKEN_TRANSFER 예시 + CAIP-2 network 병기"
      contains: "assetId-only"
    - path: "skills/policies.skill.md"
      provides: "CAIP-2 network 형식 병기 in ALLOWED_NETWORKS"
      contains: "eip155:1"
  key_links:
    - from: "skills/quickstart.skill.md"
      to: "skills/transactions.skill.md"
      via: "Asset Identification section cross-reference"
      pattern: "transactions\\.skill\\.md"
---

<objective>
스킬 파일 4종(quickstart, wallet, transactions, policies)에 CAIP-2/19 사용법을 추가하여 AI 에이전트가 CAIP 표준 식별자를 인지하고 활용할 수 있게 한다.

Purpose: v31.16에서 구현된 CAIP-2 네트워크 입력, CAIP-19 assetId-only 토큰 특정, 응답 CAIP 필드 등의 기능이 스킬 파일에 반영되어야 AI 에이전트가 이 기능을 사용할 수 있다.
Output: 4개 스킬 파일에 CAIP-2/19 사용법, 예시, assetId-only 패턴이 추가된다.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/quickstart.skill.md
@skills/wallet.skill.md
@skills/transactions.skill.md
@skills/policies.skill.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: quickstart + transactions 스킬 파일 CAIP-2/19 업데이트</name>
  <files>skills/quickstart.skill.md, skills/transactions.skill.md</files>
  <action>
**quickstart.skill.md 수정사항:**

1. "Step 4: Check Balance" 섹션에서 `?network=<network>` 설명에 CAIP-2 형식 병기:
   - 변경 전: `Optional: Append ?network=<network> to query a specific network.`
   - 변경 후: `Optional: Append ?network=<network> to query a specific network. Accepts both plain string (e.g., ethereum-mainnet) and CAIP-2 format (e.g., eip155:1).`
   - Step 5, Step 6의 동일 패턴도 같이 수정

2. "Step 6: Send First Transfer" 섹션의 `network` 파라미터 설명에 CAIP-2 병기:
   - 변경 전: `network: target network for this transaction. Required for EVM wallets; auto-resolved for Solana.`
   - 변경 후: `network: target network (e.g., "ethereum-mainnet" or CAIP-2 "eip155:1"). Required for EVM wallets; auto-resolved for Solana.`

3. "Asset Identification (CAIP-19)" 섹션을 "CAIP Standard Identifiers (CAIP-2 / CAIP-19)"로 이름 변경하고, 기존 내용 유지하면서 다음 추가:
   - 상단에 CAIP-2 네트워크 식별 설명 추가:
     ```
     ### CAIP-2: Network Identification

     All `network` parameters accept CAIP-2 chain identifiers alongside plain strings:

     | Plain String | CAIP-2 | Chain |
     |---|---|---|
     | `ethereum-mainnet` | `eip155:1` | Ethereum |
     | `polygon-mainnet` | `eip155:137` | Polygon |
     | `arbitrum-mainnet` | `eip155:42161` | Arbitrum |
     | `base-mainnet` | `eip155:8453` | Base |
     | `solana-mainnet` | `solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp` | Solana |

     Example: `?network=eip155:1` is equivalent to `?network=ethereum-mainnet`.
     ```
   - assetId-only 패턴 예시 추가 (기존 assetId + address/decimals/symbol 예시 아래에):
     ```
     ### assetId-Only Token Transfer (Recommended)

     When using a registered token, you can send with just `assetId` -- the daemon auto-resolves address, decimals, and symbol from the token registry:

     ```bash
     curl -s -X POST http://localhost:3100/v1/transactions/send \
       -H 'Content-Type: application/json' \
       -H 'Authorization: Bearer wai_sess_eyJ...' \
       -d '{
         "type": "TOKEN_TRANSFER",
         "to": "0x742d35Cc6634C0532925a3b844Bc9e7595f2bD16",
         "humanAmount": "100",
         "token": {
           "assetId": "eip155:1/erc20:0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48"
         }
       }'
     ```

     The `network` is also auto-inferred from the CAIP-2 prefix in `assetId` (here `eip155:1` -> `ethereum-mainnet`).
     ```

4. "MCP Tool" 참조에 `resolve_asset` 도구 언급 추가:
   - `If using MCP, call the resolve_asset tool to look up token metadata from a CAIP-19 assetId before sending.`

**transactions.skill.md 수정사항:**

1. Section 2 (TOKEN_TRANSFER) 파라미터 `network` 설명에 CAIP-2 병기 (현재 line ~175 근처):
   - 모든 `network` 파라미터에 `(e.g., "ethereum-mainnet" or CAIP-2 "eip155:1")` 추가

2. Section 13 "CAIP-19 Asset Identification"을 "CAIP Standard Identifiers"로 개선:
   - 섹션 제목을 `## 13. CAIP Standard Identifiers (CAIP-2 / CAIP-19)`로 변경
   - 기존 CAIP-19 내용 유지
   - 상단에 CAIP-2 네트워크 입력 설명 추가 (quickstart와 동일한 매핑 테이블)
   - "assetId-Only Pattern" 하위 섹션 추가:
     ```
     ### assetId-Only Pattern (Recommended for Registered Tokens)

     When a token is registered in the daemon's token registry, you can omit `address`, `decimals`, and `symbol` entirely:

     ```bash
     curl -s -X POST http://localhost:3100/v1/transactions/send \
       -H 'Content-Type: application/json' \
       -H 'Authorization: Bearer wai_sess_eyJ...' \
       -d '{
         "type": "TOKEN_TRANSFER",
         "to": "0x742d35Cc6634C0532925a3b844Bc9e7595f2bD16",
         "humanAmount": "100",
         "token": {
           "assetId": "eip155:1/erc20:0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48"
         },
         "network": "eip155:1"
       }'
     ```

     - `token.address`, `token.decimals`, `token.symbol` are auto-resolved from the token registry
     - `network` can be omitted -- it is auto-inferred from the CAIP-2 prefix in `assetId`
     - For unregistered tokens: provide `assetId` + `decimals` + `symbol` (address is extracted from assetId)
     ```
   - "Response CAIP Fields" 하위 섹션 추가:
     ```
     ### Response CAIP Fields

     All responses now include CAIP identifiers:
     - `chainId`: CAIP-2 chain identifier (e.g., `"eip155:1"`)
     - `assetId`: CAIP-19 asset identifier (e.g., `"eip155:1/erc20:0xa0b..."`)

     These fields are additive -- existing `network`, `chain`, `address` fields are preserved.
     ```

3. Section 2, 4 (TOKEN_TRANSFER, APPROVE) 파라미터에서 `assetId` 설명을 "optional -> recommended" 으로 업데이트:
   - 변경 전: `assetId (optional): CAIP-19 asset identifier`
   - 변경 후: `assetId (recommended): CAIP-19 asset identifier. When provided alone (without address/decimals/symbol), the daemon auto-resolves from the token registry.`
  </action>
  <verify>
    <automated>grep -c "eip155:1" skills/quickstart.skill.md && grep -c "assetId-Only" skills/transactions.skill.md && grep -c "CAIP-2" skills/quickstart.skill.md && grep -c "CAIP Standard Identifiers" skills/transactions.skill.md</automated>
  </verify>
  <done>
    - quickstart.skill.md의 network 파라미터 설명에 CAIP-2 형식이 병기된다
    - quickstart.skill.md에 assetId-only TOKEN_TRANSFER 예시가 포함된다
    - transactions.skill.md Section 13이 CAIP-2 + CAIP-19 통합 섹션으로 확장된다
    - transactions.skill.md에 assetId-only 패턴이 문서화된다
    - transactions.skill.md에 응답 CAIP 필드 설명이 추가된다
  </done>
</task>

<task type="auto">
  <name>Task 2: wallet + policies 스킬 파일 CAIP-2 업데이트</name>
  <files>skills/wallet.skill.md, skills/policies.skill.md</files>
  <action>
**wallet.skill.md 수정사항:**

1. 모든 `network` 쿼리 파라미터 설명에 CAIP-2 형식 병기. 검색 대상 패턴: `?network=` 또는 `network` 파라미터 테이블 행. 각각에 다음 추가:
   - `Accepts plain string (e.g., "ethereum-mainnet") or CAIP-2 (e.g., "eip155:1").`

2. "2. Balance & Assets" 섹션의 balance/assets 응답 예시에 `chainId` 필드 추가:
   - 기존 응답에 `"chainId": "eip155:1"` (또는 해당 체인의 CAIP-2) 추가
   - assets 배열 항목에 `"assetId": "eip155:1/erc20:0x..."` 추가

3. "Token Registry" 섹션(Section 6)의 토큰 객체에 `assetId` 필드 설명 추가:
   - `assetId (auto-generated): CAIP-19 identifier, included in all token responses.`

4. 파일 최하단 또는 적절한 위치에 CAIP-2 네트워크 매핑 참조 테이블 추가:
   ```
   ## CAIP-2 Network Identifiers

   All `network` parameters accept CAIP-2 format alongside plain strings:

   | Plain String | CAIP-2 | Chain |
   |---|---|---|
   | `ethereum-mainnet` | `eip155:1` | Ethereum |
   | `polygon-mainnet` | `eip155:137` | Polygon |
   | `arbitrum-mainnet` | `eip155:42161` | Arbitrum |
   | `base-mainnet` | `eip155:8453` | Base |
   | `optimism-mainnet` | `eip155:10` | Optimism |
   | `solana-mainnet` | `solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp` | Solana |
   | `ethereum-sepolia` | `eip155:11155111` | Sepolia |
   | `solana-devnet` | `solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1` | Solana Devnet |
   ```

**policies.skill.md 수정사항:**

1. ALLOWED_NETWORKS 정책 (Section 2-k)의 `network` 필드 설명에 CAIP-2 병기:
   - 변경 전: `network: Network identifier (e.g., "ethereum-sepolia").`
   - 변경 후: `network: Network identifier (e.g., "ethereum-sepolia" or CAIP-2 "eip155:11155111").`

2. ALLOWED_NETWORKS 예시에 CAIP-2 형식 병기:
   - 기존 예시 유지하고, 아래에 CAIP-2 형식 예시 추가:
     ```bash
     # Using CAIP-2 format:
     curl -s -X POST http://localhost:3100/v1/policies \
       -H 'Content-Type: application/json' \
       -H 'X-Master-Password: <password>' \
       -d '{"walletId":"<uuid>","type":"ALLOWED_NETWORKS","rules":{"networks":[{"network":"eip155:11155111"},{"network":"eip155:137"}]}}'
     ```

3. "Restrict wallet to specific networks" 워크플로우(Section 4)에 CAIP-2 형식 주석 추가:
   - `Networks can be specified as plain strings or CAIP-2 identifiers.`

4. SPENDING_LIMIT의 token_limits 키에서 이미 CAIP-19 형식을 사용하고 있으므로, 해당 부분에 간단한 참조 추가:
   - `Token-specific limit keys use CAIP-19 format. See transactions.skill.md Section 13 for the complete CAIP reference.`

5. 정책의 `network` 스코프 파라미터(Section 1, POST /v1/policies)에 CAIP-2 병기:
   - 변경 전: `network: Network scope. When set, policy applies only to transactions on this network.`
   - 변경 후: `network: Network scope (e.g., "ethereum-mainnet" or CAIP-2 "eip155:1"). When set, policy applies only to transactions on this network.`
  </action>
  <verify>
    <automated>grep -c "eip155:" skills/wallet.skill.md && grep -c "CAIP-2" skills/wallet.skill.md && grep -c "CAIP-2" skills/policies.skill.md && grep -c "eip155:11155111" skills/policies.skill.md</automated>
  </verify>
  <done>
    - wallet.skill.md의 모든 network 파라미터에 CAIP-2 형식이 병기된다
    - wallet.skill.md에 CAIP-2 네트워크 매핑 참조 테이블이 포함된다
    - wallet.skill.md 응답 예시에 chainId/assetId 필드가 반영된다
    - policies.skill.md의 ALLOWED_NETWORKS와 network 스코프에 CAIP-2 형식이 병기된다
    - policies.skill.md에 CAIP-2 형식 정책 예시가 추가된다
  </done>
</task>

</tasks>

<verification>
1. `grep -l "CAIP-2" skills/quickstart.skill.md skills/wallet.skill.md skills/transactions.skill.md skills/policies.skill.md` -- 4개 파일 모두 CAIP-2 언급
2. `grep -l "assetId-Only" skills/quickstart.skill.md skills/transactions.skill.md` -- assetId-only 패턴 문서화
3. `grep -c "eip155:" skills/quickstart.skill.md` -- CAIP-2 예시 존재 (4개 이상)
4. `grep "resolve_asset" skills/quickstart.skill.md` -- MCP resolve_asset 도구 참조
</verification>

<success_criteria>
1. skills/*.skill.md 4개 파일에 CAIP-2 네트워크 지정 형식과 예시가 포함된다
2. 토큰 전송 예시에 assetId 단독 사용 패턴이 문서화된다 (quickstart + transactions)
3. 네트워크 지정 예시에 CAIP-2 형식이 기존 plain string과 병기된다 (4개 파일 모두)
4. 응답 예시에 chainId/assetId 필드가 반영된다
</success_criteria>

<output>
After completion, create `.planning/phases/411-skill-files-sync/411-01-SUMMARY.md`
</output>
