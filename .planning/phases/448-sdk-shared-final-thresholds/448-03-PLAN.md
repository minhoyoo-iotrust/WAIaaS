---
phase: 448-sdk-shared-final-thresholds
plan: 03
type: execute
wave: 2
depends_on: [448-01, 448-02]
files_modified:
  - packages/daemon/vitest.config.ts
  - packages/admin/vitest.config.ts
  - packages/cli/vitest.config.ts
  - packages/wallet-sdk/vitest.config.ts
  - packages/push-relay/vitest.config.ts
  - packages/adapters/solana/vitest.config.ts
  - packages/adapters/evm/vitest.config.ts
  - scripts/coverage-gate.sh
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, GATE-05]

must_haves:
  truths:
    - "전 패키지 vitest.config.ts 임계값이 max(현재, 통일기준)으로 인상된다"
    - "coverage-gate.sh 임계값이 vitest.config.ts와 동기화된다"
    - "pnpm turbo run test:unit 전체 0 failures로 통과한다"
    - "기존 임계값 하향이 없음이 확인된다"
  artifacts:
    - path: "scripts/coverage-gate.sh"
      provides: "전 패키지 lines 임계값 동기화"
    - path: "packages/daemon/vitest.config.ts"
      provides: "daemon 임계값 최종 인상"
    - path: "packages/admin/vitest.config.ts"
      provides: "admin 임계값 최종 인상"
    - path: "packages/cli/vitest.config.ts"
      provides: "cli 임계값 최종 인상"
  key_links:
    - from: "scripts/coverage-gate.sh"
      to: "packages/*/vitest.config.ts"
      via: "THRESHOLDS array parallel to PACKAGES array"
      pattern: "THRESHOLDS="
---

<objective>
전 패키지 vitest.config.ts 임계값을 통일 기준으로 최종 인상하고, coverage-gate.sh를 동기화하여 CI Gate로 커버리지 하락을 방지한다.

Purpose: Phase 444-447에서 daemon/admin/cli 임계값이 통일 기준(90/85/95)에 미달한 채로 남아있다. 실제 커버리지 수치를 확인하고 달성 가능한 최대치로 인상한 뒤, coverage-gate.sh의 lines 임계값을 vitest.config.ts와 동기화한다.
Output: 전 패키지 임계값 인상 + coverage-gate.sh 동기화 + 전체 테스트 통과 확인
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/445-daemon-infra-admin-notification-tests/445-03-SUMMARY.md
@.planning/phases/447-admin-cli-coverage/447-03-SUMMARY.md
@scripts/coverage-gate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전 패키지 vitest.config.ts 임계값 최종 인상</name>
  <files>packages/daemon/vitest.config.ts, packages/admin/vitest.config.ts, packages/cli/vitest.config.ts, packages/wallet-sdk/vitest.config.ts, packages/push-relay/vitest.config.ts, packages/adapters/solana/vitest.config.ts, packages/adapters/evm/vitest.config.ts</files>
  <action>
각 패키지별로 `pnpm --filter {pkg} exec vitest run --coverage` 실행하여 현재 실제 수치를 확인한 뒤, max(현재 임계값, 통일 기준, 실제 수치 - 2%p margin) 공식으로 인상.

통일 기준: Lines 90% / Branches 85% / Functions 95%

**daemon** (현재 L:89/B:81/F:95/S:89):
- B:81 -> 실제 달성 수치 확인 후 인상. Phase 445 SUMMARY에서 ~82%였으므로, 82 이상 목표.
- L:89 -> 90 인상 가능 여부 확인 (Phase 445에서 ~90% 달성)
- 통일 기준 미달 항목만 인상, 이미 달성한 항목은 유지 또는 인상

**admin** (현재 L:90/B:81/F:75/S:90):
- F:75 -> 실제 달성 수치 확인 후 가능한 최대치. Phase 447에서 75%로 인상했으나 95% 미달.
- B:81 -> 실제 수치 확인 후 인상
- 달성 불가 항목은 현 수준 유지 (이미 Phase 447에서 올릴 수 있는 만큼 올린 상태)

**cli** (현재 L:88/B:80/F:98/S:88):
- L:88 -> 90 가능 여부 확인. Phase 447에서 88%로 설정.
- B:80 -> 85 가능 여부 확인.
- 달성 불가 시 현 수준 유지

**wallet-sdk** (현재 L:87/B:85/F:98/S:87):
- L:87 -> 90 가능 여부 확인
- B:85 이미 달성, F:98 이미 달성

**push-relay** (현재 L:82/B:90/F:94/S:82):
- L:82 -> 90 가능 여부 확인
- F:94 -> 95 가능 여부 확인
- B:90 이미 달성

**solana** (현재 L:89/B:80/F:89/S:89):
- L:89 -> 90 가능 여부 확인
- B:80 -> 85 가능 여부 확인
- F:89 -> 95 가능 여부 확인

**evm** (현재 L:92/B:85/F:93/S:92):
- F:93 -> 95 가능 여부 확인
- L:92, B:85 이미 달성

각 패키지의 실제 달성 수치를 기반으로 안전하게 인상. 달성 불가한 항목은 현 수준 유지하되, 절대 하향 금지.

패키지별 `pnpm --filter {pkg} exec vitest run --coverage` 통과 확인 후 다음 패키지로 이동.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test:unit 2>&1 | tail -20</automated>
  </verify>
  <done>전 패키지 vitest.config.ts 임계값이 인상되고 `pnpm turbo run test:unit` 전체 통과</done>
</task>

<task type="auto">
  <name>Task 2: coverage-gate.sh 임계값 동기화 + 최종 검증</name>
  <files>scripts/coverage-gate.sh</files>
  <action>
1. scripts/coverage-gate.sh의 THRESHOLDS 배열을 각 패키지 vitest.config.ts의 lines 임계값과 동기화:
   - core: vitest lines 값
   - daemon: vitest lines 값
   - solana: vitest lines 값
   - sdk: vitest lines 값
   - cli: vitest lines 값
   - mcp: vitest lines 값
   - admin: vitest lines 값
   - evm: vitest lines 값
   - wallet-sdk: vitest lines 값
   - push-relay: vitest lines 값
   - actions: vitest lines 값

2. PACKAGES 배열에 shared가 없으면 추가:
   - "packages/shared" + 해당 lines threshold

3. 동기화 확인: 각 패키지의 vitest.config.ts lines threshold와 coverage-gate.sh THRESHOLDS가 일치하는지 검증

4. GATE-05 확인: git diff로 기존 임계값 대비 하향된 항목이 없는지 최종 확인
   `git diff HEAD -- packages/*/vitest.config.ts packages/adapters/*/vitest.config.ts scripts/coverage-gate.sh | grep -E "^[-+].*thresholds|^[-+].*lines:|^[-+].*branches:|^[-+].*functions:|^[-+].*statements:"`

5. 전체 최종 실행: `pnpm turbo run test:unit` 전체 0 failures 확인
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test:unit 2>&1 | grep -E "Tasks:|failed" | tail -5</automated>
  </verify>
  <done>coverage-gate.sh가 vitest.config.ts와 동기화되고, 전 패키지 `pnpm turbo run test:unit` 0 failures, 임계값 하향 없음 확인</done>
</task>

</tasks>

<verification>
cd /Users/minho.yoo/dev/wallet/WAIaaS
pnpm turbo run test:unit
- 전 패키지 0 failures
- coverage-gate.sh THRESHOLDS == vitest.config.ts lines thresholds
- git diff에서 임계값 하향 없음
</verification>

<success_criteria>
1. 전 패키지 vitest.config.ts 임계값이 max(현재, 통일기준)으로 인상된다
2. coverage-gate.sh와 vitest.config.ts 임계값이 동기화된다
3. `pnpm turbo run test:unit` 전체 0 failures로 통과한다
4. 기존 임계값 절대 하향 금지 규칙이 준수된다
</success_criteria>

<output>
After completion, create `.planning/phases/448-sdk-shared-final-thresholds/448-03-SUMMARY.md`
</output>
