---
phase: 404-typed-mcp-schemas-response-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/api/routes/openapi-schemas.ts
  - packages/daemon/src/api/routes/transactions.ts
  - packages/daemon/src/api/routes/wallet.ts
  - packages/core/src/utils/format-amount.ts
  - packages/daemon/src/__tests__/response-enrichment.test.ts
autonomous: true
requirements: [RESP-01, RESP-02, RESP-03, RESP-04, RESP-05, TEST-04]

must_haves:
  truths:
    - "GET /v1/transactions/:id 응답에 amountFormatted, decimals, symbol 필드가 포함된다"
    - "GET /v1/wallet/balance 응답에 balanceFormatted 필드가 포함된다"
    - "토큰 메타데이터를 알 수 없을 때 amountFormatted/decimals/symbol이 null이다"
    - "amountFormatted 값은 DB에 저장되지 않고 런타임에 계산된다"
  artifacts:
    - path: "packages/daemon/src/api/routes/openapi-schemas.ts"
      provides: "TxDetailResponseSchema에 amountFormatted/decimals/symbol 추가, WalletBalanceResponseSchema에 balanceFormatted 추가"
      contains: "amountFormatted"
    - path: "packages/daemon/src/api/routes/transactions.ts"
      provides: "트랜잭션 상세 응답에서 런타임 amount 포매팅"
      contains: "formatAmount"
    - path: "packages/daemon/src/api/routes/wallet.ts"
      provides: "잔액 응답에서 balanceFormatted 계산"
      contains: "balanceFormatted"
  key_links:
    - from: "packages/daemon/src/api/routes/transactions.ts"
      to: "packages/core/src/utils/format-amount.ts"
      via: "formatAmount(bigint, decimals) 호출"
      pattern: "formatAmount"
    - from: "packages/daemon/src/api/routes/wallet.ts"
      to: "BalanceInfo.balance/decimals"
      via: "adapter.getBalance() 결과로 balanceFormatted 계산"
      pattern: "formatAmount.*balance"
---

<objective>
트랜잭션 상세 응답에 amountFormatted/decimals/symbol을, 잔액 응답에 balanceFormatted를 추가하여 AI 에이전트가 사람이 읽을 수 있는 금액 정보를 받을 수 있게 한다.

Purpose: AI 에이전트가 raw smallest-unit 값을 직접 파싱하지 않고도 금액의 의미를 이해할 수 있다.
Output: 응답 스키마 보강 + 런타임 포매팅 로직 + 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From packages/core/src/utils/format-amount.ts:
```typescript
export function formatAmount(amount: bigint, decimals: number): string;
export function parseAmount(amount: string, decimals: number): bigint;
```

From packages/daemon/src/api/routes/openapi-schemas.ts:
```typescript
export const TxDetailResponseSchema = z.object({
  id: z.string().uuid(),
  walletId: z.string().uuid(),
  type: z.string(),
  status: z.string(),
  tier: z.string().nullable(),
  chain: z.string(),
  network: z.string().nullable(),
  toAddress: z.string().nullable(),
  amount: z.string().nullable(),
  txHash: z.string().nullable(),
  error: z.string().nullable(),
  createdAt: z.number().int().nullable(),
  displayAmount: z.string().nullable().optional(),
  displayCurrency: z.string().nullable().optional(),
  atomic: z.boolean().optional(),
}).openapi('TxDetailResponse');

export const WalletBalanceResponseSchema = z.object({
  walletId: z.string().uuid(),
  chain: z.string(),
  network: z.string(),
  address: z.string(),
  balance: z.string(),
  decimals: z.number().int(),
  symbol: z.string(),
  displayBalance: z.string().nullable().optional(),
  displayCurrency: z.string().nullable().optional(),
}).openapi('WalletBalanceResponse');
```

From packages/core/src/interfaces/chain-adapter.types.ts:
```typescript
export interface BalanceInfo {
  address: string;
  balance: bigint;
  decimals: number;
  symbol: string;
}
```

From packages/daemon/src/infrastructure/token-registry/token-registry-service.ts:
```typescript
export class TokenRegistryService {
  async getTokensForNetwork(network: string): Promise<RegistryToken[]>;
}
export interface RegistryToken extends TokenEntry {
  source: 'builtin' | 'custom';
}
// TokenEntry has: address, symbol, name, decimals
```

Transaction DB schema stores: chain, network, type, amount (string), toAddress.
For TRANSFER: amount is native token amount. For TOKEN_TRANSFER: amount is token amount, metadata may have tokenAddress.
Chain config provides native token decimals/symbol.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TxDetailResponseSchema 보강 + 트랜잭션 상세 응답 amountFormatted</name>
  <files>packages/daemon/src/api/routes/openapi-schemas.ts, packages/daemon/src/api/routes/transactions.ts, packages/daemon/src/__tests__/response-enrichment.test.ts</files>
  <behavior>
    - Test 1: GET /v1/transactions/:id 응답에 amountFormatted, decimals, symbol 필드가 존재한다
    - Test 2: TRANSFER 타입 트랜잭션은 chain의 native token decimals/symbol로 amountFormatted가 계산된다 (예: amount="1000000000000000000", chain="evm" -> amountFormatted="1", decimals=18, symbol="ETH")
    - Test 3: amount가 null인 트랜잭션은 amountFormatted/decimals/symbol이 모두 null이다
    - Test 4: decimals 6/8/9/18에 대해 올바른 포맷 확인
  </behavior>
  <action>
    1. `packages/daemon/src/api/routes/openapi-schemas.ts`의 TxDetailResponseSchema에 세 필드를 추가한다:
       ```typescript
       amountFormatted: z.string().nullable().optional(),
       decimals: z.number().int().nullable().optional(),
       symbol: z.string().nullable().optional(),
       ```

    2. `packages/daemon/src/api/routes/transactions.ts`에 amount 포매팅 헬퍼를 추가한다:
       - `resolveAmountMetadata(chain, network, type, amount, metadata)` 함수:
         - amount가 null/undefined이면 `{ amountFormatted: null, decimals: null, symbol: null }` 반환
         - TRANSFER 타입: chain config에서 native token decimals/symbol 조회. chain별 매핑:
           - evm: decimals=18, symbol 결정은 network 기반 (ethereum-mainnet -> "ETH", polygon-mainnet -> "MATIC" 등, fallback "ETH")
           - solana: decimals=9, symbol="SOL"
         - TOKEN_TRANSFER 타입: metadata에서 tokenAddress 추출 -> TokenRegistryService에서 조회. 미등록 시 null 반환.
         - 기타 타입(CONTRACT_CALL 등): decimals/symbol null (amount 의미가 타입별로 다름)
         - formatAmount(BigInt(amount), decimals)로 amountFormatted 계산
         - try-catch: 변환 실패 시 모든 필드 null

    3. GET /v1/transactions/:id 핸들러에서 resolveAmountMetadata를 호출하여 응답에 포함한다.
       TransactionRouteDeps에 tokenRegistryService를 옵션으로 추가한다 (TOKEN_TRANSFER 조회용).

    4. 네이티브 토큰 symbol 매핑을 위한 간단한 헬퍼 `getNativeTokenInfo(chain, network)`:
       ```typescript
       function getNativeTokenInfo(chain: string, network?: string | null): { decimals: number; symbol: string } | null {
         if (chain === 'solana') return { decimals: 9, symbol: 'SOL' };
         if (chain === 'evm') {
           // network별 symbol 매핑 (ethereum=ETH, polygon=MATIC/POL, etc.)
           const symbolMap: Record<string, string> = {
             'ethereum-mainnet': 'ETH', 'ethereum-sepolia': 'ETH', 'ethereum-holesky': 'ETH',
             'polygon-mainnet': 'POL', 'polygon-amoy': 'POL',
             'arbitrum-mainnet': 'ETH', 'arbitrum-sepolia': 'ETH',
             'optimism-mainnet': 'ETH', 'optimism-sepolia': 'ETH',
             'base-mainnet': 'ETH', 'base-sepolia': 'ETH',
             'avalanche-mainnet': 'AVAX', 'avalanche-fuji': 'AVAX',
             'bsc-mainnet': 'BNB', 'bsc-testnet': 'BNB',
           };
           return { decimals: 18, symbol: symbolMap[network ?? ''] ?? 'ETH' };
         }
         return null;
       }
       ```
       이 헬퍼를 transactions.ts 내부에 정의한다.

    5. 테스트 파일 신규: `packages/daemon/src/__tests__/response-enrichment.test.ts`
       - 다양한 chain/decimals 조합에 대한 포매팅 테스트
       - amount null 케이스
       - TOKEN_TRANSFER + 미등록 토큰 -> null fallback
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon exec vitest run src/__tests__/response-enrichment.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>GET /v1/transactions/:id 응답에 amountFormatted/decimals/symbol이 포함되며, chain/network 기반으로 올바르게 계산되고, amount null 시 null 반환</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 잔액 조회 API balanceFormatted + GET /v1/transactions 리스트 응답 보강</name>
  <files>packages/daemon/src/api/routes/openapi-schemas.ts, packages/daemon/src/api/routes/wallet.ts, packages/daemon/src/__tests__/response-enrichment.test.ts</files>
  <behavior>
    - Test 1: GET /v1/wallet/balance 응답에 balanceFormatted 필드가 존재한다
    - Test 2: balanceFormatted는 balance/decimals로부터 런타임 계산된 human-readable 문자열이다
    - Test 3: network=all 응답의 각 balance 항목에도 balanceFormatted가 포함된다
  </behavior>
  <action>
    1. `packages/daemon/src/api/routes/openapi-schemas.ts`의 WalletBalanceResponseSchema에 추가:
       ```typescript
       balanceFormatted: z.string().nullable().optional(),
       ```

    2. `packages/daemon/src/api/routes/wallet.ts`의 walletBalanceRoute 핸들러:
       - 기존 `balanceInfo.balance.toString()` 아래에 `balanceFormatted` 계산 추가:
         ```typescript
         import { formatAmount } from '@waiaas/core';
         const balanceFormatted = formatAmount(balanceInfo.balance, balanceInfo.decimals);
         ```
       - 응답 JSON에 `balanceFormatted` 필드 추가

    3. network=all 응답의 각 balance 항목에도 balanceFormatted 추가:
       - `results.map()` 내부에서 fulfilled 결과에 `balanceFormatted: formatAmount(BigInt(r.value.balance), r.value.decimals)` 추가

    4. GET /v1/wallet/assets 응답의 각 asset에도 balanceFormatted 추가:
       - WalletAssetsResponseSchema의 assets 배열 아이템에 `balanceFormatted: z.string().nullable().optional()` 추가
       - assets 매핑에서 `balanceFormatted: formatAmount(BigInt(asset.balance), asset.decimals)` 추가

    5. 기존 response-enrichment.test.ts에 balanceFormatted 테스트 케이스 추가:
       - SOL balance 9 decimals 포매팅
       - ETH balance 18 decimals 포매팅
       - 0 balance -> "0"
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon exec vitest run src/__tests__/response-enrichment.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>GET /v1/wallet/balance 응답에 balanceFormatted가 포함되며, DB 저장 없이 런타임 계산된다. assets 응답에도 balanceFormatted가 포함된다</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/daemon exec vitest run src/__tests__/response-enrichment.test.ts` 통과
2. TxDetailResponseSchema에 amountFormatted/decimals/symbol 필드 존재
3. WalletBalanceResponseSchema에 balanceFormatted 필드 존재
4. 런타임 계산 확인: DB 스키마 변경 없음
</verification>

<success_criteria>
- 트랜잭션 상세 응답에 amountFormatted, decimals, symbol이 포함된다
- 잔액 조회 응답에 balanceFormatted가 포함된다
- 토큰 메타데이터 미확인 시 해당 필드들이 null이다
- amountFormatted 값은 런타임 계산이며 DB 저장 없음
- 다양한 decimals(6, 8, 9, 18)에 대해 올바른 포맷
</success_criteria>

<output>
After completion, create `.planning/phases/404-typed-mcp-schemas-response-enrichment/404-02-SUMMARY.md`
</output>
