---
phase: 438-pipeline-split-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/pipeline/pipeline-helpers.ts
  - packages/daemon/src/pipeline/stage1-validate.ts
  - packages/daemon/src/pipeline/stage2-auth.ts
  - packages/daemon/src/pipeline/stage3-policy.ts
  - packages/daemon/src/pipeline/stage4-wait.ts
  - packages/daemon/src/pipeline/stage5-execute.ts
  - packages/daemon/src/pipeline/stage6-confirm.ts
  - packages/daemon/src/pipeline/stages.ts
  - packages/daemon/src/pipeline/index.ts
autonomous: true
requirements:
  - STG-01
  - STG-02
  - STG-03
  - STG-04
  - STG-05
  - STG-06
  - STG-07
  - STG-08

must_haves:
  truths:
    - "stages.ts is a barrel re-export module, not a 2,330-line monolith"
    - "Each stage function lives in its own file named stage{N}-{purpose}.ts"
    - "Shared helpers/types live in pipeline-helpers.ts"
    - "All existing pipeline tests pass without modification"
    - "All consumers import from stages.js unchanged (barrel re-exports)"
  artifacts:
    - path: "packages/daemon/src/pipeline/pipeline-helpers.ts"
      provides: "PipelineContext interface, helper functions (resolveActionTier, getRequestAmount, getRequestTo, resolveNotificationTo, getRequestMemo, buildTransactionParam, TransactionParam, truncateAddress, formatNotificationAmount, extractPolicyType, hintedTokens set, clearHintedTokens, hasHintedToken)"
      exports: ["PipelineContext", "resolveActionTier", "getRequestAmount", "getRequestTo", "resolveNotificationTo", "getRequestMemo", "buildTransactionParam", "TransactionParam", "clearHintedTokens", "hasHintedToken"]
    - path: "packages/daemon/src/pipeline/stage1-validate.ts"
      provides: "stage1Validate function"
      exports: ["stage1Validate"]
    - path: "packages/daemon/src/pipeline/stage2-auth.ts"
      provides: "stage2Auth function"
      exports: ["stage2Auth"]
    - path: "packages/daemon/src/pipeline/stage3-policy.ts"
      provides: "stage3Policy and stageGasCondition functions"
      exports: ["stage3Policy", "stageGasCondition"]
    - path: "packages/daemon/src/pipeline/stage4-wait.ts"
      provides: "stage4Wait function"
      exports: ["stage4Wait"]
    - path: "packages/daemon/src/pipeline/stage5-execute.ts"
      provides: "stage5Execute, buildByType, buildUserOpCalls, ERC721_USEROP_ABI, ERC1155_USEROP_ABI"
      exports: ["stage5Execute", "buildByType", "buildUserOpCalls", "ERC721_USEROP_ABI", "ERC1155_USEROP_ABI"]
    - path: "packages/daemon/src/pipeline/stage6-confirm.ts"
      provides: "stage6Confirm function"
      exports: ["stage6Confirm"]
    - path: "packages/daemon/src/pipeline/stages.ts"
      provides: "Barrel re-export of all stage modules for backward compatibility"
  key_links:
    - from: "packages/daemon/src/pipeline/stages.ts"
      to: "stage1-validate.ts through stage6-confirm.ts + pipeline-helpers.ts"
      via: "export * from re-exports"
      pattern: "export \\* from"
    - from: "packages/daemon/src/pipeline/pipeline.ts"
      to: "packages/daemon/src/pipeline/stages.ts"
      via: "import (unchanged)"
      pattern: "from './stages.js'"
---

<objective>
Split the 2,330-line stages.ts into 7 focused modules (pipeline-helpers.ts + 6 stage files), keeping stages.ts as a barrel re-export for backward compatibility.

Purpose: This follows the same pattern as Phase 437 (migrate.ts, daemon.ts, policy-engine.ts splits). Each stage becomes a single-responsibility module while all existing imports continue to work unchanged.
Output: 7 new files + simplified stages.ts barrel, all existing tests pass.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/daemon/src/pipeline/stages.ts
@packages/daemon/src/pipeline/pipeline.ts
@packages/daemon/src/pipeline/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract pipeline-helpers.ts and 6 stage files from stages.ts</name>
  <files>
    packages/daemon/src/pipeline/pipeline-helpers.ts
    packages/daemon/src/pipeline/stage1-validate.ts
    packages/daemon/src/pipeline/stage2-auth.ts
    packages/daemon/src/pipeline/stage3-policy.ts
    packages/daemon/src/pipeline/stage4-wait.ts
    packages/daemon/src/pipeline/stage5-execute.ts
    packages/daemon/src/pipeline/stage6-confirm.ts
  </files>
  <action>
    Extract code from stages.ts into 7 new files following these exact boundaries:

    **pipeline-helpers.ts** (lines 1-439 of stages.ts):
    - All import statements needed by helper functions
    - `hintedTokens` set + `clearHintedTokens()` + `hasHintedToken()` (lines 65-70)
    - `PipelineContext` interface (lines 76-155)
    - `resolveActionTier()` (lines 171-189)
    - `getRequestAmount()` (lines 191-195)
    - `getRequestTo()` (lines 197-208)
    - `truncateAddress()` (lines 210-225) -- private helper, NOT exported
    - `resolveNotificationTo()` (lines 227-245)
    - `getRequestMemo()` (lines 247-259)
    - `formatNotificationAmount()` (lines 261-324) -- private helper, NOT exported
    - `extractPolicyType()` (lines 326-359) -- private helper, NOT exported
    - `TransactionParam` interface (lines 343-359)
    - `buildTransactionParam()` (lines 361-439)
    - Export `formatNotificationAmount` and `extractPolicyType` so stage files can use them. Same for `truncateAddress`.

    **stage1-validate.ts** (lines 441-506):
    - `stage1Validate()` function
    - Import PipelineContext, formatNotificationAmount, resolveNotificationTo from `./pipeline-helpers.js`
    - Import generateId, transactions schema, etc. from their original paths

    **stage2-auth.ts** (lines 508-516):
    - `stage2Auth()` function
    - Import PipelineContext from `./pipeline-helpers.js`

    **stage3-policy.ts** (lines 518-936):
    - `stage3Policy()` function (lines 522-776)
    - `stageGasCondition()` function (lines 795-936) -- Stage 3.5 gas condition belongs here
    - Import PipelineContext, buildTransactionParam, getRequestAmount, resolveNotificationTo, etc. from `./pipeline-helpers.js`

    **stage4-wait.ts** (lines 938-1037):
    - `stage4Wait()` function
    - Import PipelineContext from `./pipeline-helpers.js`

    **stage5-execute.ts** (lines 1039-2221):
    - `buildByType()` function (lines 1039-1205)
    - `ERC721_USEROP_ABI` + `ERC1155_USEROP_ABI` constants (lines 1220-1247)
    - `buildUserOpCalls()` function (lines 1249-1834)
    - `stage5Execute()` function (lines 1855-2221)
    - Import PipelineContext, getRequestAmount, getRequestTo, getRequestMemo, resolveNotificationTo, formatNotificationAmount from `./pipeline-helpers.js`

    **stage6-confirm.ts** (lines 2223-2330):
    - `stage6Confirm()` function
    - Import PipelineContext, resolveNotificationTo, formatNotificationAmount from `./pipeline-helpers.js`

    Each file must:
    1. Include only the imports it actually needs (not copy all imports from stages.ts)
    2. Use `import type` for type-only imports
    3. Follow existing code style (JSDoc comments preserved)
    4. Keep the inline `import()` type for `reputationCache` in PipelineContext as-is (it's a single instance)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -5</automated>
  </verify>
  <done>7 new files exist with correct exports, typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Convert stages.ts to barrel re-export + update index.ts</name>
  <files>
    packages/daemon/src/pipeline/stages.ts
    packages/daemon/src/pipeline/index.ts
  </files>
  <action>
    **Replace stages.ts** with a barrel re-export file that maintains backward compatibility:
    ```typescript
    /**
     * Pipeline stages -- barrel re-export.
     * Individual stage modules: stage1-validate.ts through stage6-confirm.ts
     * Shared types/helpers: pipeline-helpers.ts
     */
    export * from './pipeline-helpers.js';
    export * from './stage1-validate.js';
    export * from './stage2-auth.js';
    export * from './stage3-policy.js';
    export * from './stage4-wait.js';
    export * from './stage5-execute.js';
    export * from './stage6-confirm.js';
    ```

    This ensures ALL existing import paths (`from '../pipeline/stages.js'`) continue to work without changes.

    **Update index.ts** -- no changes needed since it already imports from `./stages.js` which will re-export everything. Verify the existing index.ts exports still resolve correctly.

    Verify that NO consumer files need updating -- the barrel pattern preserves all import paths.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon && pnpm vitest run --project daemon 2>&1 | tail -20</automated>
  </verify>
  <done>stages.ts is a barrel re-export (~10 lines), all 25+ pipeline test files pass, typecheck passes, no consumer import changes needed</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/daemon` passes
2. `pnpm vitest run --project daemon` -- all pipeline tests pass
3. `wc -l packages/daemon/src/pipeline/stages.ts` shows ~10 lines (barrel only)
4. `ls packages/daemon/src/pipeline/stage*.ts` shows 6 stage files
5. `ls packages/daemon/src/pipeline/pipeline-helpers.ts` exists
</verification>

<success_criteria>
- stages.ts is a barrel re-export module (under 15 lines)
- 6 stage files (stage1-validate.ts through stage6-confirm.ts) exist
- pipeline-helpers.ts contains PipelineContext and shared helpers
- All existing pipeline tests pass without modification
- Typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/438-pipeline-split-cleanup/438-01-SUMMARY.md`
</output>
