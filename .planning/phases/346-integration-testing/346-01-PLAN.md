---
phase: 346-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/index.ts
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/daemon/src/api/routes/connect-info.ts
  - packages/daemon/src/infrastructure/config/loader.ts
autonomous: true
requirements: [INTG-01, INTG-02, INTG-05, INTG-06]

must_haves:
  truths:
    - "DcentSwapActionProvider가 registerBuiltInProviders()에 등록되어 데몬 시작 시 자동 로드된다"
    - "Admin Settings에서 dcent_swap 설정을 조회/변경할 수 있다"
    - "connect-info 응답에 dcent_swap capability가 포함된다"
    - "MCP 도구 4개가 action_dcent_swap_* 이름으로 자동 등록된다"
  artifacts:
    - path: "packages/actions/src/index.ts"
      provides: "dcent_swap entry in registerBuiltInProviders"
      contains: "dcent_swap"
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "dcent_swap Admin Settings keys"
      contains: "actions.dcent_swap_enabled"
    - path: "packages/daemon/src/api/routes/connect-info.ts"
      provides: "dcent_swap capability in connect-info"
      contains: "dcent_swap"
  key_links:
    - from: "packages/actions/src/index.ts"
      to: "packages/actions/src/providers/dcent-swap/index.ts"
      via: "import DcentSwapActionProvider"
      pattern: "DcentSwapActionProvider"
    - from: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      to: "packages/actions/src/index.ts"
      via: "settingsReader.get('actions.dcent_swap_*')"
      pattern: "actions\\.dcent_swap"
---

<objective>
DcentSwapActionProvider를 데몬 라이프사이클에 등록하고, Admin Settings 키를 추가하고, connect-info에 capability를 노출한다.

Purpose: Phase 343-345에서 구현한 DCent Swap 기능을 데몬 인프라에 연결하여 MCP 도구 자동 등록과 Admin 관리가 가능해진다.
Output: registerBuiltInProviders에 dcent_swap 등록, 7개 Admin Settings 키, connect-info dcent_swap capability
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/design/77-dcent-swap-aggregator.md (Section 10: MCP/SDK/정책/Admin 통합 설계)

@packages/actions/src/index.ts (registerBuiltInProviders 함수)
@packages/actions/src/providers/dcent-swap/index.ts (DcentSwapActionProvider)
@packages/actions/src/providers/dcent-swap/config.ts (DcentSwapConfig, DCENT_SWAP_DEFAULTS)
@packages/daemon/src/infrastructure/settings/setting-keys.ts (SETTING_DEFINITIONS)
@packages/daemon/src/api/routes/connect-info.ts (capabilities 빌드 로직)
@packages/daemon/src/lifecycle/daemon.ts (Step 4f: ActionProviderRegistry)

<interfaces>
From packages/actions/src/providers/dcent-swap/config.ts:
```typescript
export interface DcentSwapConfig {
  apiBaseUrl: string;
  requestTimeoutMs: number;
  defaultSlippageBps: number;
  maxSlippageBps: number;
  currencyCacheTtlMs: number;
  exchangePollIntervalMs: number;
  exchangePollMaxMs: number;
}

export const DCENT_SWAP_DEFAULTS: DcentSwapConfig = {
  apiBaseUrl: 'https://swapbuy-beta.dcentwallet.com',
  requestTimeoutMs: 15_000,
  defaultSlippageBps: 100,
  maxSlippageBps: 500,
  currencyCacheTtlMs: 86_400_000,
  exchangePollIntervalMs: 30_000,
  exchangePollMaxMs: 3_600_000,
};
```

From packages/actions/src/index.ts (registerBuiltInProviders pattern):
```typescript
interface ProviderRegistry { register(provider: IActionProvider): void; }
interface SettingsReader { get(key: string): string; hasApiKey?(name: string): boolean; }

// Each provider entry follows this pattern:
{ key: 'provider_name', enabledKey: 'actions.provider_name_enabled', factory: () => IActionProvider | null }
```

From packages/daemon/src/api/routes/connect-info.ts (capability pattern):
```typescript
// capabilities is a string[] built by checking settings/config:
if (deps.settingsService.get('signing_sdk.enabled') === 'true') capabilities.push('sign');
if (deps.config.x402?.enabled === true) capabilities.push('x402');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Settings 키 등록 + registerBuiltInProviders에 dcent_swap 추가</name>
  <files>packages/daemon/src/infrastructure/settings/setting-keys.ts, packages/actions/src/index.ts</files>
  <action>
1. `packages/daemon/src/infrastructure/settings/setting-keys.ts`의 SETTING_DEFINITIONS 배열에 dcent_swap Admin Settings 키 7개를 추가한다 (기존 `actions.*` 패턴을 따름). 설계 문서 77 섹션 10.4 참조:
   - `actions.dcent_swap_enabled` (defaultValue: 'true', category: 'actions', isCredential: false)
   - `actions.dcent_swap_api_url` (defaultValue: 'https://swapbuy-beta.dcentwallet.com', category: 'actions', isCredential: false)
   - `actions.dcent_swap_default_slippage_bps` (defaultValue: '100', category: 'actions', isCredential: false)
   - `actions.dcent_swap_max_slippage_bps` (defaultValue: '500', category: 'actions', isCredential: false)
   - `actions.dcent_swap_exchange_poll_interval_ms` (defaultValue: '30000', category: 'actions', isCredential: false)
   - `actions.dcent_swap_exchange_poll_max_ms` (defaultValue: '3600000', category: 'actions', isCredential: false)
   - `actions.dcent_swap_currency_cache_ttl_ms` (defaultValue: '86400000', category: 'actions', isCredential: false)

2. `packages/actions/src/index.ts`에 DcentSwapActionProvider를 import하고 registerBuiltInProviders의 providers 배열에 추가:
   - import 추가: `import { DcentSwapActionProvider } from './providers/dcent-swap/index.js';`
   - providers 배열에 entry 추가:
     ```
     {
       key: 'dcent_swap',
       enabledKey: 'actions.dcent_swap_enabled',
       factory: () => {
         const config: import('./providers/dcent-swap/config.js').DcentSwapConfig = {
           apiBaseUrl: settingsReader.get('actions.dcent_swap_api_url'),
           requestTimeoutMs: 15_000,
           defaultSlippageBps: Number(settingsReader.get('actions.dcent_swap_default_slippage_bps')),
           maxSlippageBps: Number(settingsReader.get('actions.dcent_swap_max_slippage_bps')),
           currencyCacheTtlMs: Number(settingsReader.get('actions.dcent_swap_currency_cache_ttl_ms')),
           exchangePollIntervalMs: Number(settingsReader.get('actions.dcent_swap_exchange_poll_interval_ms')),
           exchangePollMaxMs: Number(settingsReader.get('actions.dcent_swap_exchange_poll_max_ms')),
         };
         return new DcentSwapActionProvider(config);
       },
     },
     ```
   - 배열 위치: erc8004_agent 뒤 (기존 알파벳/등록 순서 유지)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon --filter=@waiaas/actions 2>&1 | tail -5</automated>
  </verify>
  <done>setting-keys.ts에 dcent_swap 키 7개, index.ts에 dcent_swap registerBuiltInProviders entry가 추가되고, typecheck 통과</done>
</task>

<task type="auto">
  <name>Task 2: connect-info에 dcent_swap capability 추가</name>
  <files>packages/daemon/src/api/routes/connect-info.ts</files>
  <action>
`packages/daemon/src/api/routes/connect-info.ts`의 capabilities 빌드 로직에 dcent_swap을 추가한다.

기존 패턴(signing_sdk, erc8128 등)을 따라서, settingsService에서 `actions.dcent_swap_enabled` 값이 'true'이면 capabilities에 'dcent_swap'을 push한다:

```typescript
// dcent_swap: check if enabled via settings
if (deps.settingsService) {
  try {
    if (deps.settingsService.get('actions.dcent_swap_enabled') === 'true') {
      capabilities.push('dcent_swap');
    }
  } catch {
    // Setting not found -- dcent_swap not available
  }
}
```

이 블록을 `erc8128` capability 블록 뒤에 배치한다.

또한 `buildConnectInfoPrompt`에 DCent Swap 관련 힌트를 추가한다:
- capabilities에 'dcent_swap'이 포함되면 prompt에 DCent Swap 사용 안내 라인을 추가:
  `'DCent Swap: Use action_dcent_swap_* tools for multi-provider DEX swaps and cross-chain exchanges.'`
- 이 라인은 기존 `'For Smart Account wallets...'` 라인 뒤에 추가한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -5</automated>
  </verify>
  <done>connect-info 응답에 dcent_swap capability가 포함되고, prompt에 DCent Swap 사용 힌트가 추가됨</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck` — 전체 typecheck 통과
2. `grep -n 'dcent_swap' packages/daemon/src/infrastructure/settings/setting-keys.ts` — 7개 키 확인
3. `grep -n 'dcent_swap' packages/actions/src/index.ts` — registerBuiltInProviders entry 확인
4. `grep -n 'dcent_swap' packages/daemon/src/api/routes/connect-info.ts` — capability 추가 확인
</verification>

<success_criteria>
- DcentSwapActionProvider가 registerBuiltInProviders에 등록됨
- Admin Settings에 dcent_swap 키 7개가 정의됨
- connect-info capabilities에 dcent_swap이 포함됨
- MCP 도구 4개가 action_dcent_swap_* 이름으로 자동 노출됨 (action-provider.ts의 기존 auto-conversion 로직)
- 전체 typecheck 통과
</success_criteria>

<output>
After completion, create `.planning/phases/346-integration-testing/346-01-SUMMARY.md`
</output>
