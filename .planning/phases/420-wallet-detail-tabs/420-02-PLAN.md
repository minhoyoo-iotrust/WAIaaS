---
phase: 420-wallet-detail-tabs
plan: 02
type: execute
wave: 2
depends_on: ["420-01"]
files_modified:
  - packages/admin/src/pages/wallets.tsx
  - packages/admin/src/__tests__/wallets.test.tsx
  - packages/admin/src/__tests__/wallets-coverage.test.tsx
autonomous: true
requirements:
  - DETL-06
  - DETL-07
  - DETL-08

must_haves:
  truths:
    - "Activity 탭에서 Transactions와 External Actions를 하나의 뷰에서 필터로 구분할 수 있다"
    - "Assets 탭에서 Staking Positions 섹션과 NFT Gallery 섹션이 상하로 통합 표시된다"
    - "Setup 탭에서 Credentials 섹션과 MCP Setup 섹션이 상하로 통합 표시된다"
  artifacts:
    - path: "packages/admin/src/pages/wallets.tsx"
      provides: "ActivityTab, AssetsTab, SetupTab 구현"
      contains: "function ActivityTab"
  key_links:
    - from: "ActivityTab"
      to: "TransactionsTab + ExternalActionsTab"
      via: "inline rendering with filter signal"
      pattern: "activityFilter"
    - from: "AssetsTab"
      to: "StakingTab + NftTab"
      via: "sequential section rendering"
      pattern: "Staking Positions.*NFTs"
    - from: "SetupTab"
      to: "CredentialsTab + McpTab"
      via: "sequential section rendering"
      pattern: "Credentials.*MCP Setup"
---

<objective>
Plan 01에서 만든 ActivityTab/AssetsTab/SetupTab 스텁을 실제 콘텐츠로 교체한다. 기존 독립 탭 컴포넌트(TransactionsTab, ExternalActionsTab, StakingTab, NftTab, CredentialsTab, McpTab)의 콘텐츠를 새 통합 탭 안에 섹션별로 배치한다.

Purpose: 8개 탭의 관련 기능을 4개 탭으로 논리적 그룹화하여 사용자가 관련 정보를 한 화면에서 확인할 수 있게 한다.
Output: ActivityTab/AssetsTab/SetupTab이 각각 기존 기능을 통합하여 완전히 동작한다.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/420-wallet-detail-tabs/420-01-SUMMARY.md

@packages/admin/src/pages/wallets.tsx
</context>

<interfaces>
<!-- Existing tab functions to be consolidated (from wallets.tsx) -->

TransactionsTab (line ~1035-1111): FilterBar + table with TX_COLUMNS + pagination
ExternalActionsTab (line ~1952-2051): Status/venue filters + table + detail modal
StakingTab (line ~1390-1471): Staking positions table with protocol/asset/balance/APY
NftTab (line ~1477-1774): Network selector + grid/list view + NFT detail modal
CredentialsTab (line ~1775-1951): Credential CRUD (add/delete/rotate) + modals
McpTab (line ~2054-2106): MCP provisioning + config display

Key signals already defined in WalletDetail scope:
- txFilters, filteredTxs, txsLoading, txPage, txTotal (for transactions)
- actionsStatusFilter, actionsVenueFilter, externalActions, actionsLoading, selectedAction (for external actions)
- stakingPositions, stakingLoading (for staking)
- nfts, nftsLoading, nftNetwork, nftCursor, nftViewMode, selectedNft, nftMetadata (for NFTs)
- walletCredentials, credentialsLoading, showCredAddModal, etc. (for credentials)
- mcpResult, mcpLoading (for MCP)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ActivityTab/AssetsTab/SetupTab 실제 구현</name>
  <files>packages/admin/src/pages/wallets.tsx</files>
  <action>
Plan 01에서 생성한 스텁 함수들을 실제 구현으로 교체한다.

**1. ActivityTab 구현:**
기존 TransactionsTab + ExternalActionsTab 콘텐츠를 하나의 탭에 통합. 상단에 "Transactions" / "External Actions" 필터 토글을 추가한다.

- WalletDetail 함수 스코프에 `const activityView = useSignal<'transactions' | 'external-actions'>('transactions');` 시그널 추가.

```tsx
function ActivityTab() {
  return (
    <div>
      {/* Filter toggle */}
      <div style={{ display: 'flex', gap: 'var(--space-2)', marginBottom: 'var(--space-3)' }}>
        <Button
          variant={activityView.value === 'transactions' ? 'primary' : 'secondary'}
          size="sm"
          onClick={() => { activityView.value = 'transactions'; }}
        >
          Transactions
        </Button>
        <Button
          variant={activityView.value === 'external-actions' ? 'primary' : 'secondary'}
          size="sm"
          onClick={() => { activityView.value = 'external-actions'; }}
        >
          External Actions
        </Button>
      </div>

      {activityView.value === 'transactions' && <TransactionsContent />}
      {activityView.value === 'external-actions' && <ExternalActionsContent />}
    </div>
  );
}
```

기존 TransactionsTab 함수를 `TransactionsContent`로 리네이밍. 기존 ExternalActionsTab 함수를 `ExternalActionsContent`로 리네이밍. 둘 다 내부 JSX는 그대로 유지.

**2. AssetsTab 구현:**
기존 StakingTab + NftTab 콘텐츠를 상하 섹션으로 배치한다.

```tsx
function AssetsTab() {
  return (
    <div>
      {/* Staking Positions section */}
      <StakingContent />

      {/* NFT Gallery section */}
      <div style={{ marginTop: 'var(--space-6)', paddingTop: 'var(--space-4)', borderTop: '1px solid var(--color-border)' }}>
        <NftContent />
      </div>
    </div>
  );
}
```

기존 StakingTab 함수를 `StakingContent`로 리네이밍. 기존 NftTab 함수를 `NftContent`로 리네이밍. 내부 JSX 그대로 유지.

**3. SetupTab 구현:**
기존 CredentialsTab + McpTab 콘텐츠를 상하 섹션으로 배치한다.

```tsx
function SetupTab() {
  return (
    <div>
      {/* Credentials section */}
      <CredentialsContent />

      {/* MCP Setup section */}
      <div style={{ marginTop: 'var(--space-6)', paddingTop: 'var(--space-4)', borderTop: '1px solid var(--color-border)' }}>
        <McpContent />
      </div>
    </div>
  );
}
```

기존 CredentialsTab 함수를 `CredentialsContent`로 리네이밍. 기존 McpTab 함수를 `McpContent`로 리네이밍. 내부 JSX 그대로 유지.

**4. 정리:**
- 기존 독립 탭 함수 이름(TransactionsTab, ExternalActionsTab, StakingTab, NftTab, CredentialsTab, McpTab)이 다른 곳에서 참조되지 않는지 확인. 있다면 새 이름으로 변경.
- 탭 렌더링 영역에서 기존 6개 탭 조건문 (`activeDetailTab.value === 'transactions'` 등)이 Plan 01에서 이미 제거되었는지 확인. 남아있다면 제거.

중요: 기존 탭 함수의 JSX/로직을 수정하지 않는다. 단순히 리네이밍 + 새 wrapper 함수에서 호출하는 구조. 이렇게 하면 기존 동작이 100% 보존된다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin exec -- npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ActivityTab이 Transactions/External Actions 필터로 구분하여 표시하고, AssetsTab이 Staking+NFT를 섹션별로 표시하고, SetupTab이 Credentials+MCP를 섹션별로 표시한다</done>
</task>

<task type="auto">
  <name>Task 2: 테스트 업데이트 + 전체 검증</name>
  <files>packages/admin/src/__tests__/wallets.test.tsx, packages/admin/src/__tests__/wallets-coverage.test.tsx</files>
  <action>
1. wallets.test.tsx에서 Plan 01에서 이미 업데이트한 탭 이름 외에, 추가로 필요한 테스트 업데이트:
   - Activity 탭 클릭 시 "Transactions" / "External Actions" 필터 버튼이 보이는지 확인하는 테스트 추가 (가능하면)
   - Setup 탭 클릭 시 내부에 MCP Setup/Credentials 관련 컨텐츠가 보이는지 확인

2. wallets-coverage.test.tsx에서 DETAIL_TABS이나 탭 이름 참조가 있다면 4탭 구조에 맞게 업데이트.

3. wallets-nft.test.tsx에서 NftTab → NftContent로 함수 이름이 변경되었지만, 이 파일은 NFT 기능 자체를 테스트하므로 탭 이름 참조만 있다면 업데이트.

4. 전체 admin 테스트 실행 및 타입 체크:
   - `pnpm --filter @waiaas/admin run test`
   - `pnpm --filter @waiaas/admin exec -- npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin run test 2>&1 | tail -30</automated>
  </verify>
  <done>모든 admin 테스트가 통과하고, 4탭 구조(Overview/Activity/Assets/Setup)가 올바르게 동작하며, 각 통합 탭이 기존 기능을 정확히 포함한다</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/admin exec -- npx tsc --noEmit` 타입 체크 통과
2. `pnpm --filter @waiaas/admin run test` 전체 테스트 통과
3. Activity 탭: Transactions 필터와 External Actions 필터 전환 동작
4. Assets 탭: Staking Positions + NFT Gallery 섹션 모두 존재
5. Setup 탭: Credentials + MCP Setup 섹션 모두 존재
</verification>

<success_criteria>
- ActivityTab이 Transactions/External Actions를 필터 버튼으로 구분하여 표시
- AssetsTab이 Staking Positions 섹션과 NFT Gallery 섹션을 상하로 통합
- SetupTab이 Credentials 섹션과 MCP Setup 섹션을 상하로 통합
- 기존 6개 독립 탭 함수(TransactionsTab 등)가 Content 이름으로 리네이밍되어 통합 탭 내에서 호출됨
- 모든 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/420-wallet-detail-tabs/420-02-SUMMARY.md`
</output>
