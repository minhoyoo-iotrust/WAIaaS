---
phase: 363-onchain-e2e-scenarios
plan: 02
type: execute
wave: 2
depends_on: [363-01]
files_modified:
  - packages/e2e-tests/src/scenarios/onchain-incoming.ts
  - packages/e2e-tests/src/__tests__/onchain-incoming.e2e.test.ts
  - packages/e2e-tests/src/scenarios/onchain-staking.ts
  - packages/e2e-tests/src/__tests__/onchain-staking.e2e.test.ts
  - packages/e2e-tests/src/scenarios/onchain-hyperliquid.ts
  - packages/e2e-tests/src/__tests__/onchain-hyperliquid.e2e.test.ts
autonomous: true
requirements: [ONCH-06, ONCH-07, ONCH-08]

must_haves:
  truths:
    - "자기 자신에게 전송 후 IncomingTxMonitor가 수신 트랜잭션을 감지한다"
    - "Lido Holesky stake가 실행되고 txId가 반환된다"
    - "Hyperliquid testnet Spot/Perp 주문이 skip 또는 성공한다"
    - "testnet 미지원이면 skip으로 처리된다"
  artifacts:
    - path: "packages/e2e-tests/src/scenarios/onchain-incoming.ts"
      provides: "수신 TX 감지 시나리오 등록"
    - path: "packages/e2e-tests/src/__tests__/onchain-incoming.e2e.test.ts"
      provides: "수신 TX 감지 E2E 테스트"
    - path: "packages/e2e-tests/src/scenarios/onchain-staking.ts"
      provides: "Lido staking 시나리오 등록"
    - path: "packages/e2e-tests/src/__tests__/onchain-staking.e2e.test.ts"
      provides: "Lido stake/unstake E2E 테스트"
    - path: "packages/e2e-tests/src/scenarios/onchain-hyperliquid.ts"
      provides: "Hyperliquid 시나리오 등록"
    - path: "packages/e2e-tests/src/__tests__/onchain-hyperliquid.e2e.test.ts"
      provides: "Hyperliquid Spot/Perp E2E 테스트"
  key_links:
    - from: "onchain-incoming.e2e.test.ts"
      to: "/v1/wallet/incoming"
      via: "E2EHttpClient GET"
      pattern: "http\\.get.*wallet/incoming"
    - from: "onchain-staking.e2e.test.ts"
      to: "/v1/actions/lido_staking/stake"
      via: "E2EHttpClient POST"
      pattern: "http\\.post.*actions/lido_staking"
    - from: "onchain-hyperliquid.e2e.test.ts"
      to: "/v1/actions/hyperliquid_spot/order"
      via: "E2EHttpClient POST"
      pattern: "http\\.post.*actions/hyperliquid"
---

<objective>
수신 트랜잭션 감지, Lido 스테이킹, Hyperliquid Spot/Perp 온체인 E2E 시나리오를 작성한다.

Purpose: 파이프라인 전송 외에 수신 감지, DeFi 스테이킹, 오프체인 DEX 패턴까지 E2E로 커버한다.
Output: 3개 시나리오 등록 파일 + 3개 E2E 테스트 파일
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/363-onchain-e2e-scenarios/363-01-SUMMARY.md

@packages/e2e-tests/src/types.ts
@packages/e2e-tests/src/helpers/index.ts
@packages/e2e-tests/src/helpers/http-client.ts
@packages/e2e-tests/src/helpers/session.ts
@packages/e2e-tests/src/helpers/onchain-skip.ts
@packages/e2e-tests/src/__tests__/onchain-transfer.e2e.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

Transaction send: POST /v1/transactions/send -> 201 { id, status }
Transaction status: GET /v1/transactions/:id -> { id, status, txId, network }

Incoming TX: GET /v1/wallet/incoming?limit=10 -> { items: [{ id, txHash, from, to, amount, network, detectedAt }], cursor }
Incoming summary: GET /v1/wallet/incoming/summary -> { buckets: [...] }

Action provider pattern: POST /v1/actions/:provider/:action
- Lido: POST /v1/actions/lido_staking/stake { amount: string (wei), network: 'holesky' }
- Lido: POST /v1/actions/lido_staking/unstake { amount: string (wei), network: 'holesky' }
- Response: 201 { id: string, status: string } (same as transaction send)

Hyperliquid (ApiDirectResult pattern — no pipeline, direct API call):
- POST /v1/actions/hyperliquid_spot/order { asset: string, side: 'buy'|'sell', size: string, price?: string, orderType: 'market'|'limit' }
- POST /v1/actions/hyperliquid_perp/order { asset: string, side: 'buy'|'sell', size: string, leverage?: number, orderType: 'market'|'limit' }
- POST /v1/actions/hyperliquid_sub_account/create { name: string }
- Response: 201 { result: { ... } } (ApiDirectResult, not pipeline txId)

Skip utility (from 363-01):
```typescript
import { shouldSkipNetwork } from '../helpers/onchain-skip.js';
// it.skipIf(shouldSkipNetwork('holesky'))('test name', ...)
```

Onchain test pattern (from 363-01):
- No DaemonManager — connects to already-running daemon via WAIAAS_E2E_DAEMON_URL
- Creates session for existing funded wallet
- Self-transfer to preserve balance
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 수신 트랜잭션 감지 E2E</name>
  <files>packages/e2e-tests/src/scenarios/onchain-incoming.ts, packages/e2e-tests/src/__tests__/onchain-incoming.e2e.test.ts</files>
  <action>
1. `scenarios/onchain-incoming.ts` — 시나리오 등록:
   - `incoming-tx-detection`: track 'onchain', category 'incoming', networks ['sepolia'], description: 자기 자신에게 ETH 전송 후 IncomingTxMonitor가 수신 트랜잭션을 감지하는지 확인.

2. `__tests__/onchain-incoming.e2e.test.ts`:
   - skipIf(shouldSkipNetwork('sepolia')) 패턴.
   - 환경변수에서 daemon URL, master password 읽기.
   - beforeAll: EVM 지갑 찾기, 세션 생성.
   - 테스트 순서:
     a) 자기 자신에게 소량(1 wei) ETH 전송 (POST /v1/transactions/send).
     b) 트랜잭션 확인 대기 (GET /v1/transactions/:id 폴링, 최대 60초).
     c) 약간의 대기(5-10초) 후 GET /v1/wallet/incoming?limit=10으로 수신 목록 조회.
     d) 방금 전송한 txHash가 수신 목록에 존재하는지 확인. 단, IncomingTxMonitor가 구독 중이 아닐 수 있으므로 (known issue #164) 감지되지 않으면 skip 처리 (assert 실패 대신).
   - afterAll: 세션 삭제.

   참고: IncomingTxMonitor는 환경 기본 네트워크만 구독하는 제한이 있음 (#164). Sepolia가 구독 대상이 아니면 수신 감지가 안 될 수 있으므로, incoming 목록이 비어있으면 skip으로 처리한다.
  </action>
  <verify>
    <automated>cd packages/e2e-tests && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>수신 TX 감지 시나리오가 등록되고, 감지 실패 시 skip으로 graceful하게 처리된다</done>
</task>

<task type="auto">
  <name>Task 2: Lido Staking + Hyperliquid Spot/Perp E2E</name>
  <files>packages/e2e-tests/src/scenarios/onchain-staking.ts, packages/e2e-tests/src/__tests__/onchain-staking.e2e.test.ts, packages/e2e-tests/src/scenarios/onchain-hyperliquid.ts, packages/e2e-tests/src/__tests__/onchain-hyperliquid.e2e.test.ts</files>
  <action>
1. `scenarios/onchain-staking.ts` — 시나리오 등록:
   - `lido-stake`: track 'onchain', category 'staking', networks ['holesky'], protocols ['staking']
   - `lido-unstake`: track 'onchain', category 'staking', networks ['holesky'], protocols ['staking']

2. `__tests__/onchain-staking.e2e.test.ts`:
   - skipIf(shouldSkipNetwork('holesky')) 패턴.
   - beforeAll: EVM 지갑 찾기, 세션 생성.
   - `lido-stake` 테스트:
     a) POST /v1/actions/lido_staking/stake `{ amount: '1000000000000000', network: 'holesky' }` (0.001 ETH).
     b) 201 응답 확인, body.id 존재.
     c) GET /v1/transactions/:id 폴링 (최대 90초). status CONFIRMED/COMPLETED 확인.
     d) Lido staking 실패 시 (잔액 부족, 컨트랙트 에러) graceful skip.
   - `lido-unstake` 테스트:
     a) stake이 성공한 경우에만 실행. stake 실패면 skip.
     b) POST /v1/actions/lido_staking/unstake `{ amount: '1000000000000000', network: 'holesky' }`.
     c) 201 응답 확인. unstake는 비동기 처리(withdrawal queue)이므로 제출만 확인.
   - afterAll: 세션 삭제.

3. `scenarios/onchain-hyperliquid.ts` — 시나리오 등록:
   - `hyperliquid-spot-order`: track 'onchain', category 'hyperliquid', networks ['sepolia'], protocols ['swap']
   - `hyperliquid-perp-order`: track 'onchain', category 'hyperliquid', networks ['sepolia'], protocols ['perp']

4. `__tests__/onchain-hyperliquid.e2e.test.ts`:
   - Hyperliquid testnet은 별도의 환경이므로, HyperEVM(999/998)이 설정되어 있어야 한다. 연결 불가 시 전체 describe skip.
   - skipIf(shouldSkipNetwork('sepolia')) 패턴 (Hyperliquid는 Arbitrum 기반이지만, EVM 지갑 필요).
   - 환경변수 WAIAAS_E2E_HYPERLIQUID_ENABLED가 'true'가 아니면 전체 skip. Hyperliquid testnet은 별도 계정/잔액 설정이 필요하므로 기본 skip 처리.
   - `hyperliquid-spot-order` 테스트:
     a) POST /v1/actions/hyperliquid_spot/order `{ asset: 'PURR/USDC', side: 'buy', size: '1', orderType: 'limit', price: '0.001' }`.
     b) 201 응답이면 성공, 4xx면 skip (Hyperliquid 설정 미완).
   - `hyperliquid-perp-order` 테스트:
     a) POST /v1/actions/hyperliquid_perp/order `{ asset: 'ETH', side: 'buy', size: '0.001', leverage: 1, orderType: 'limit', price: '1' }`.
     b) 201 응답이면 성공, 4xx면 skip.
   - afterAll: 주문 취소 시도 (cleanup), 세션 삭제.

   주의: Hyperliquid는 ApiDirectResult 패턴이므로 pipeline txId가 아닌 직접 결과를 반환한다. 응답 body.result를 확인한다.
  </action>
  <verify>
    <automated>cd packages/e2e-tests && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Lido 스테이킹 2개 + Hyperliquid 2개 시나리오가 등록되고, testnet 미설정 시 skip으로 처리된다</done>
</task>

</tasks>

<verification>
1. `cd packages/e2e-tests && npx tsc --noEmit` — 전체 타입 체크 통과
2. 시나리오 등록 확인: onchain-incoming(1건) + onchain-staking(2건) + onchain-hyperliquid(2건) = 5건
3. 모든 테스트가 skipIf 패턴을 사용하여 네트워크/환경 미충족 시 skip 처리
</verification>

<success_criteria>
- 5개 시나리오가 ScenarioRegistry에 등록된다
- 수신 TX 감지 테스트가 IncomingTxMonitor 미구독 시 skip한다
- Lido stake/unstake가 Holesky에서 실행되거나 skip된다
- Hyperliquid 테스트가 WAIAAS_E2E_HYPERLIQUID_ENABLED 미설정 시 skip된다
- 타입 체크가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/363-onchain-e2e-scenarios/363-02-SUMMARY.md`
</output>
