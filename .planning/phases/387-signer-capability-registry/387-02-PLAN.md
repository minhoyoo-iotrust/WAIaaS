---
phase: 387-signer-capability-registry
plan: 02
type: tdd
wave: 2
depends_on: ["387-01"]
files_modified:
  - packages/daemon/src/signing/registry.ts
  - packages/daemon/src/signing/bootstrap.ts
  - packages/daemon/src/signing/index.ts
  - packages/daemon/src/__tests__/signer-registry.test.ts
  - packages/daemon/src/__tests__/signer-backward-compat.test.ts
autonomous: true
requirements: [SIGN-07, SIGN-08]

must_haves:
  truths:
    - "SignerCapabilityRegistry에 signingScheme 문자열을 전달하면 해당 ISignerCapability 구현체가 반환된다"
    - "resolve()가 SignedDataAction/SignedHttpAction의 signingScheme에 매핑되는 capability를 반환한다"
    - "등록되지 않은 scheme에 대해 resolve()가 CAPABILITY_NOT_FOUND 에러를 던진다"
    - "bootstrapSignerCapabilities()가 7종 전부 등록한다"
    - "기존 sign-message/sign-only/ERC-8128 파이프라인 경로가 변경 없이 동작한다"
  artifacts:
    - path: "packages/daemon/src/signing/registry.ts"
      provides: "SignerCapabilityRegistry (ISignerCapabilityRegistry 구현)"
      exports: ["SignerCapabilityRegistry"]
    - path: "packages/daemon/src/signing/bootstrap.ts"
      provides: "bootstrapSignerCapabilities 팩토리 함수"
      exports: ["bootstrapSignerCapabilities"]
    - path: "packages/daemon/src/__tests__/signer-registry.test.ts"
      provides: "Registry 등록/조회/resolve 테스트"
    - path: "packages/daemon/src/__tests__/signer-backward-compat.test.ts"
      provides: "기존 서명 파이프라인 무변경 확인 테스트"
  key_links:
    - from: "packages/daemon/src/signing/registry.ts"
      to: "packages/daemon/src/signing/types.ts"
      via: "ISignerCapability interface import"
      pattern: "import.*ISignerCapability"
    - from: "packages/daemon/src/signing/bootstrap.ts"
      to: "packages/daemon/src/signing/capabilities/index.ts"
      via: "7 capability class imports"
      pattern: "new.*SignerCapability|new.*SignCapability"
---

<objective>
SignerCapabilityRegistry를 구현하여 signingScheme 문자열로 적절한 서명기를 자동 선택할 수 있게 하고, 기존 서명 파이프라인과의 하위 호환성을 검증한다.

Purpose: 7종 서명 Capability를 중앙 레지스트리에 등록하고, External Action 파이프라인이 action의 signingScheme에서 올바른 서명기를 자동으로 resolve할 수 있게 한다.
Output: SignerCapabilityRegistry, bootstrapSignerCapabilities, 레지스트리 테스트 + 하위 호환 테스트.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/387-signer-capability-registry/387-01-SUMMARY.md

<interfaces>
<!-- From Plan 387-01 (created by previous plan) -->
From packages/daemon/src/signing/types.ts:
```typescript
export interface ISignerCapability {
  readonly scheme: SigningScheme;
  canSign(params: SigningParams): boolean;
  sign(params: SigningParams): Promise<SigningResult>;
}

export interface SigningResult {
  signature: string | Uint8Array;
  metadata?: Record<string, unknown>;
}

export type SigningParams =
  | Eip712SigningParams | PersonalSigningParams | HmacSigningParams
  | RsaPssSigningParams | EcdsaSecp256k1SigningParams
  | Ed25519SigningParams | Erc8128SigningParams;
```

From packages/daemon/src/signing/signing-error.ts:
```typescript
export class SigningError extends Error {
  constructor(message: string, public readonly scheme: SigningScheme, public readonly code: SigningErrorCode, public readonly cause?: Error);
}
export type SigningErrorCode = 'INVALID_KEY' | 'INVALID_PARAMS' | 'CREDENTIAL_MISSING' | 'SIGNING_FAILED' | 'KEY_NOT_SUPPORTED' | 'CAPABILITY_NOT_FOUND';
```

<!-- ISignerCapabilityRegistry interface from doc-81 D2.5 -->
```typescript
export interface ISignerCapabilityRegistry {
  register(capability: ISignerCapability): void;
  get(scheme: SigningScheme): ISignerCapability | undefined;
  resolve(action: SignedDataAction | SignedHttpAction): ISignerCapability;
  listSchemes(): readonly SigningScheme[];
}
```

From packages/core/src/schemas/resolved-action.schema.ts:
```typescript
export type SignedDataAction = { kind: 'signedData'; signingScheme: SigningScheme; ... };
export type SignedHttpAction = { kind: 'signedHttp'; signingScheme: 'erc8128' | 'hmac-sha256'; ... };
```

<!-- Existing pipelines that must remain unchanged -->
- packages/daemon/src/pipeline/sign-message.ts (executeSignMessage)
- packages/daemon/src/pipeline/sign-only.ts (executeSignOnly)
- packages/daemon/src/api/routes/erc8128.ts (erc8128Routes)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SignerCapabilityRegistry + bootstrap + 하위 호환 검증</name>
  <files>
    packages/daemon/src/signing/registry.ts,
    packages/daemon/src/signing/bootstrap.ts,
    packages/daemon/src/signing/index.ts,
    packages/daemon/src/__tests__/signer-registry.test.ts,
    packages/daemon/src/__tests__/signer-backward-compat.test.ts
  </files>
  <behavior>
    ## SignerCapabilityRegistry

    - register(capability): Map에 scheme -> capability 저장, 중복 scheme 시 override (마지막 등록 우선)
    - get('eip712'): Eip712SignerCapability 인스턴스 반환
    - get('nonexistent' as any): undefined 반환
    - resolve(signedDataAction with scheme 'hmac-sha256'): HmacSignerCapability 반환
    - resolve(signedHttpAction with scheme 'erc8128'): Erc8128SignerCapability 반환
    - resolve(action with unregistered scheme): throw WAIaaSError('CAPABILITY_NOT_FOUND')
    - listSchemes(): 등록된 7종 scheme 배열 반환

    ## bootstrapSignerCapabilities

    - bootstrapSignerCapabilities(registry): 7종 전부 등록
    - 등록 후 registry.listSchemes().length === 7
    - 등록 후 각 scheme으로 get() 가능

    ## 하위 호환 검증

    - sign-message.ts의 executeSignMessage가 ISignerCapability를 import하지 않음 (static analysis)
    - sign-only.ts의 executeSignOnly가 ISignerCapability를 import하지 않음
    - erc8128.ts routes가 ISignerCapability를 import하지 않음
    - 기존 sign-message 테스트 통과 (회귀 없음)
    - 기존 sign-only 테스트 통과 (회귀 없음)
    - 기존 ERC-8128 route 테스트 통과 (회귀 없음)
  </behavior>
  <action>
    **RED phase**: Create two test files:

    1. `packages/daemon/src/__tests__/signer-registry.test.ts`:
       - Test register/get/resolve/listSchemes on SignerCapabilityRegistry
       - Test resolve() with SignedDataAction and SignedHttpAction fixtures
       - Test resolve() with unregistered scheme throws WAIaaSError('CAPABILITY_NOT_FOUND')
       - Test bootstrapSignerCapabilities() registers all 7
       - Test each scheme is retrievable after bootstrap

    2. `packages/daemon/src/__tests__/signer-backward-compat.test.ts`:
       - Static analysis tests: read source files of sign-message.ts, sign-only.ts, erc8128.ts and assert they do NOT contain 'ISignerCapability' or 'SignerCapabilityRegistry' import strings
       - This confirms the two code paths (old pipelines vs new signer registry) are independent per doc-81 design principle

    **GREEN phase**: Implement:

    1. `packages/daemon/src/signing/registry.ts`:
       - Export `ISignerCapabilityRegistry` interface (per doc-81 D2.5)
       - Export `SignerCapabilityRegistry` class implementing the interface
       - Internal: `Map<SigningScheme, ISignerCapability>`
       - `register()`: set in map
       - `get()`: map.get() or undefined
       - `resolve(action)`: extract `action.signingScheme`, call get(), throw WAIaaSError('CAPABILITY_NOT_FOUND') if not found. Do NOT call canSign() here (per doc-81 D2.5 design decision -- credential not yet injected at resolve time).
       - `listSchemes()`: Array.from(map.keys()) as readonly

    2. `packages/daemon/src/signing/bootstrap.ts`:
       - Export `bootstrapSignerCapabilities(registry: ISignerCapabilityRegistry): void`
       - Import all 7 capability classes from ./capabilities/
       - Instantiate and register each

    3. Update `packages/daemon/src/signing/index.ts`:
       - Re-export from registry.ts and bootstrap.ts (add to existing exports from 387-01)

    **Do NOT** modify any existing pipeline code (sign-message.ts, sign-only.ts, erc8128.ts, stages.ts).
    **Do NOT** wire the registry into the daemon server.ts -- that happens in Phase 390 (pipeline routing).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon && pnpm vitest run packages/daemon/src/__tests__/signer-registry.test.ts packages/daemon/src/__tests__/signer-backward-compat.test.ts</automated>
  </verify>
  <done>
    - SignerCapabilityRegistry가 7종 scheme에 대해 get()/resolve() 정상 반환
    - resolve()가 미등록 scheme에 대해 CAPABILITY_NOT_FOUND 에러를 던짐
    - bootstrapSignerCapabilities()가 7종 전부 등록
    - 기존 sign-message/sign-only/ERC-8128 코드에 ISignerCapability import가 없음 (독립성 확인)
    - 기존 테스트 회귀 없음
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo run typecheck` 전체 통과
- `pnpm vitest run packages/daemon/src/__tests__/signer-registry.test.ts packages/daemon/src/__tests__/signer-backward-compat.test.ts` 통과
- `pnpm vitest run packages/daemon/src/__tests__/signer-capabilities.test.ts` 통과 (Plan 01 회귀)
- `pnpm vitest run packages/daemon/src/__tests__/sign-message-api.test.ts packages/daemon/src/__tests__/sign-only-api.test.ts packages/daemon/src/__tests__/erc8128-routes.test.ts` 통과
</verification>

<success_criteria>
- SignerCapabilityRegistry에 signingScheme 문자열을 전달하면 해당 ISignerCapability 구현체가 반환된다
- 7종 scheme이 모두 등록되어 listSchemes()로 확인 가능하다
- 기존 sign-message/sign-only/ERC-8128 파이프라인 경로가 변경 없이 동작한다 (SIGN-08)
- 새 코드와 기존 코드가 import 단에서 완전히 독립적이다
</success_criteria>

<output>
After completion, create `.planning/phases/387-signer-capability-registry/387-02-SUMMARY.md`
</output>
