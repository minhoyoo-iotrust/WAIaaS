---
phase: 257-staking-pipeline-integration-fix
task: 0
total_tasks: 2
status: not_started
last_updated: 2026-02-24T13:03:13.180Z
---

<current_state>
Milestone v28.4 Liquid Staking: Phases 254-256 all executed and verified. Milestone audit completed — 2 integration gaps found. Phase 257 created in ROADMAP.md as gap closure phase. Phase needs planning and execution.

사용자가 오딧 후 오픈 이슈들도 처리해달라고 요청함. 이슈 목록은 MEMORY.md의 v28.2 이슈 섹션 참고:
- #157 IncomingTxMonitor column fix
- #159 거래 취소 UX
- #160 connect-info 글로벌 정책
- #161 프롬프트 읽기 엔드포인트
- #162 스킬 보안 안내
- #163 알림 txId 보간 회귀
- #164 IncomingTxMonitor 환경 기본 네트워크만 구독
</current_state>

<completed_work>
- Phase 254: Lido EVM Staking Provider — 2 plans, 18 tests, env key gap fixed
- Phase 255: Jito Solana Staking Provider — 2 plans, 21 tests, Ed25519 curve fix
- Phase 256: Staking API + Async Tracking + Interface Integration — 3 plans, 67 tests
  - 256-01: LidoWithdrawalTracker + JitoEpochTracker + notification events (48 tests)
  - 256-02: GET /v1/wallet/staking REST API + Zod schema (11 tests)
  - 256-03: MCP 4 tools + Admin StakingTab + actions.skill.md (8 tests)
- Post-execution fixes: settings count 18→24, SETTING_CATEGORIES policy 추가, SAPI-01 path update
- Milestone audit: v28.4-MILESTONE-AUDIT.md created
- Phase 257 added to ROADMAP.md with directory created
</completed_work>

<remaining_work>
1. Phase 257: Plan and execute gap closure (2 root cause fixes):
   - GAP-1: actions.ts — after staking unstake pipeline completes, write bridge_status=PENDING + bridge_metadata={tracker, notificationEvent} to DB so AsyncPollingService picks up the transaction
   - GAP-2: actions.ts or stages.ts — persist actionProvider + actionName into transactions.metadata column so GET /v1/wallet/staking LIKE query matches
   - Integration tests for both fixes
   - Re-audit to verify gaps closed

2. Open issues (from MEMORY.md, user requested):
   - #157, #159, #160, #161, #162, #163, #164
</remaining_work>

<decisions_made>
- Both audit gaps trace to actions pipeline — single phase 257 sufficient
- Phase 256 plans passed checker after 2 revision iterations (notification enum + async-polling-service test coverage)
- Settings test count regression fixed inline (not via GSD plan) since it was trivial
- LIDO-06 environment gap was already fixed in commit 99e1a95f before this session
</decisions_made>

<blockers>
None — phase 257 is straightforward. Both fixes are in the same file area (actions.ts / stages.ts).
</blockers>

<context>
The 2 gaps are:
1. bridge_status: When lido_staking/unstake or jito_staking/unstake completes Stage 6, need to write bridge_status=PENDING + bridge_metadata with tracker name ('lido-withdrawal' or 'jito-epoch') and notificationEvent ('STAKING_UNSTAKE_TIMEOUT'). This enrolls the transaction in AsyncPollingService polling.
2. metadata: When any action-provider transaction is created in Stage 1 (or post-pipeline in actions.ts), need to persist {provider, action} into transactions.metadata so the staking position query can find them.

Both fixes are in packages/daemon/src/api/routes/actions.ts (post-pipeline handler) and/or packages/daemon/src/pipeline/stages.ts (Stage 1 insert).

After phase 257, re-audit should pass → complete milestone → then process open issues.
</context>

<next_action>
1. /gsd:plan-phase 257 (or /gsd:quick 257 since it's a focused 2-fix phase)
2. Execute phase 257
3. Re-audit milestone: /gsd:audit-milestone
4. Complete milestone: /gsd:complete-milestone v28.4
5. Then process open issues (#157, #159, #160, #161, #162, #163, #164)
</next_action>
