---
phase: 467-db-migration-backend-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/database/migrations/v61.ts
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/infrastructure/database/schema-ddl.ts
  - packages/daemon/src/infrastructure/database/schema.ts
  - packages/daemon/src/__tests__/migration-v61.test.ts
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03, TST-02]

must_haves:
  truths:
    - "DB v61 마이그레이션 실행 후 같은 wallet_type에 signing_enabled=1인 앱이 2개 이상 존재할 수 없다"
    - "기존 데이터에서 같은 wallet_type에 signing_enabled=1이 여러 개면 created_at 가장 빠른 것만 유지된다"
    - "signing_enabled 컬럼에 0, 1 이외의 값을 넣을 수 없다"
  artifacts:
    - path: "packages/daemon/src/infrastructure/database/migrations/v61.ts"
      provides: "v61 migration with partial unique index and data dedup"
      exports: ["migrations"]
    - path: "packages/daemon/src/__tests__/migration-v61.test.ts"
      provides: "Migration v61 test"
      min_lines: 50
  key_links:
    - from: "packages/daemon/src/infrastructure/database/migrate.ts"
      to: "packages/daemon/src/infrastructure/database/migrations/v61.ts"
      via: "import and spread into migrations array"
      pattern: "v61"
---

<objective>
DB v61 마이그레이션: wallet_apps 테이블에 partial unique index + CHECK constraint 추가, 기존 중복 데이터 정리.

Purpose: wallet_type당 signing_enabled=1인 앱이 DB 레벨에서 최대 1개로 보장되어야 서비스 레이어가 안전하게 동작한다.
Output: v61 마이그레이션 파일, DDL 업데이트, 마이그레이션 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@packages/daemon/src/infrastructure/database/schema-ddl.ts
@packages/daemon/src/infrastructure/database/schema.ts
@packages/daemon/src/infrastructure/database/migrate.ts
@packages/daemon/src/infrastructure/database/migrations/v51-v59.ts
@packages/daemon/src/__tests__/migration-v60.test.ts

<interfaces>
<!-- Key types the executor needs -->

From packages/daemon/src/infrastructure/database/migrate.ts:
```typescript
export interface Migration {
  version: number;
  description: string;
  up(db: Database): void;
}
export { LATEST_SCHEMA_VERSION } from './schema-ddl.js';
```

From packages/daemon/src/infrastructure/database/schema-ddl.ts:
```typescript
export const LATEST_SCHEMA_VERSION = 60;  // Must become 61
// getCreateTableStatements() returns DDL strings
// getCreateIndexStatements() returns index DDL strings
```

Current wallet_apps DDL (line 317-331 of schema-ddl.ts):
```sql
CREATE TABLE IF NOT EXISTS wallet_apps (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  display_name TEXT NOT NULL,
  wallet_type TEXT NOT NULL DEFAULT '',
  signing_enabled INTEGER NOT NULL DEFAULT 1,
  alerts_enabled INTEGER NOT NULL DEFAULT 1,
  sign_topic TEXT,
  notify_topic TEXT,
  subscription_token TEXT,
  push_relay_url TEXT,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
)
```

Existing indexes (line 615-616 of schema-ddl.ts):
```sql
CREATE INDEX IF NOT EXISTS idx_wallet_apps_wallet_type ON wallet_apps(wallet_type)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create v61 migration file + update schema-ddl</name>
  <files>
    packages/daemon/src/infrastructure/database/migrations/v61.ts
    packages/daemon/src/infrastructure/database/schema-ddl.ts
    packages/daemon/src/infrastructure/database/schema.ts
    packages/daemon/src/infrastructure/database/migrate.ts
  </files>
  <behavior>
    - Test: v61 migration deduplicates signing_enabled=1 duplicates within same wallet_type, keeping earliest created_at
    - Test: After v61, inserting a second signing_enabled=1 row with same wallet_type fails with UNIQUE constraint error
    - Test: After v61, signing_enabled values outside 0/1 are rejected by CHECK constraint
    - Test: Migration is idempotent (running twice does not error)
  </behavior>
  <action>
    1. Create `packages/daemon/src/infrastructure/database/migrations/v61.ts`:
       - Export `migrations` array with a single Migration object (version: 61).
       - Migration `up()` steps:
         a. **Dedup step (MIG-02):** Run SQL to find wallet_type groups with multiple signing_enabled=1 rows. For each group, keep the one with MIN(created_at) and set the rest to signing_enabled=0. Use: `UPDATE wallet_apps SET signing_enabled = 0, updated_at = ? WHERE id IN (SELECT id FROM wallet_apps WHERE wallet_type = wa.wallet_type AND signing_enabled = 1 AND id != (SELECT id FROM wallet_apps wa2 WHERE wa2.wallet_type = wa.wallet_type AND wa2.signing_enabled = 1 ORDER BY wa2.created_at ASC LIMIT 1))` — but simpler: use a subquery to get all IDs that should be deactivated.
         b. **Partial unique index (MIG-01):** `CREATE UNIQUE INDEX IF NOT EXISTS idx_wallet_apps_signing_primary ON wallet_apps(wallet_type) WHERE signing_enabled = 1`
         c. **CHECK constraint (MIG-03):** SQLite cannot ADD CHECK after table creation. Instead, validate existing data: `SELECT COUNT(*) FROM wallet_apps WHERE signing_enabled NOT IN (0, 1)`. If any found, update them. The CHECK is enforced via Drizzle schema's integer mode boolean. Add a comment noting that the partial unique index implicitly enforces correctness. Note: Drizzle integer mode:'boolean' maps to 0/1 at the ORM level, and the partial unique index already constrains the signing_enabled column. For defense-in-depth, recreate the table with a CHECK constraint using the SQLite table-rebuild pattern (CREATE new table with CHECK, INSERT INTO...SELECT, DROP old, ALTER TABLE RENAME). HOWEVER, this is risky with existing data/indexes. Instead, use a simpler approach: add a TRIGGER that rejects invalid values: `CREATE TRIGGER IF NOT EXISTS trg_wallet_apps_signing_check BEFORE INSERT ON wallet_apps WHEN NEW.signing_enabled NOT IN (0, 1) BEGIN SELECT RAISE(ABORT, 'signing_enabled must be 0 or 1'); END` and same for UPDATE.
       - Description: "Signing primary partial unique index + CHECK triggers"

    2. Update `packages/daemon/src/infrastructure/database/schema-ddl.ts`:
       - Change `LATEST_SCHEMA_VERSION` from 60 to 61.
       - Add the partial unique index to `getCreateIndexStatements()`: `'CREATE UNIQUE INDEX IF NOT EXISTS idx_wallet_apps_signing_primary ON wallet_apps(wallet_type) WHERE signing_enabled = 1'`
       - Add the two CHECK triggers to a new section in `getCreateIndexStatements()` (triggers belong after indexes).
       - Update table 19 comment to note "v61: signing primary partial unique index".

    3. Update `packages/daemon/src/infrastructure/database/migrate.ts`:
       - Import migrations from `./migrations/v61.js` and spread into the combined migrations array.

    4. Update `packages/daemon/src/infrastructure/database/schema.ts`:
       - Update header comment: "30 tables" count stays same (no new table), bump version reference in doc comment to v61.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon exec vitest run src/__tests__/migration-v61.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - v61 migration file exists and exports migrations array
    - LATEST_SCHEMA_VERSION is 61
    - Partial unique index DDL in schema-ddl.ts
    - migrate.ts imports v61 migrations
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration v61 test</name>
  <files>packages/daemon/src/__tests__/migration-v61.test.ts</files>
  <behavior>
    - Test: Pre-v61 data with 2 apps of same wallet_type both signing_enabled=1 -> after migration, only earliest remains enabled (TST-02 partial)
    - Test: After migration, INSERT with duplicate wallet_type + signing_enabled=1 fails (TST-02)
    - Test: After migration, INSERT with signing_enabled=2 fails (CHECK trigger)
    - Test: Migration idempotent (run twice, no error)
    - Test: Different wallet_types can each have one signing_enabled=1
  </behavior>
  <action>
    Follow the pattern from `migration-v60.test.ts`:
    1. Create test file `packages/daemon/src/__tests__/migration-v61.test.ts`.
    2. Setup: Create a temporary SQLite DB with pre-v61 wallet_apps schema (full DDL without the new index/triggers).
    3. Seed test data: Insert apps with duplicate signing_enabled=1 for same wallet_type.
    4. Import and run the v61 migration.
    5. Test cases:
       - "deduplicates signing primary within same wallet_type": Verify only the earliest created_at row keeps signing_enabled=1.
       - "partial unique index rejects second signing primary": Try INSERT with same wallet_type + signing_enabled=1, expect UNIQUE constraint error.
       - "CHECK trigger rejects invalid signing_enabled value": Try INSERT with signing_enabled=2, expect ABORT error.
       - "allows different wallet_types to each have signing primary": Insert two apps with different wallet_types, both signing_enabled=1 -- should succeed.
       - "idempotent: running migration twice does not error": Run migration.up() twice.
    6. Cleanup: Close DB in afterEach.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon exec vitest run src/__tests__/migration-v61.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All 5 test cases pass
    - Partial unique index behavior verified (TST-02)
    - CHECK trigger behavior verified (MIG-03)
    - Dedup logic verified (MIG-02)
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon exec vitest run src/__tests__/migration-v61.test.ts --reporter=verbose
```
</verification>

<success_criteria>
- LATEST_SCHEMA_VERSION = 61
- Partial unique index `idx_wallet_apps_signing_primary` in DDL
- Migration deduplicates existing data
- CHECK triggers prevent invalid signing_enabled values
- All migration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/467-db-migration-backend-service/467-01-SUMMARY.md`
</output>
