---
phase: 376-type-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/api/routes/actions.ts
  - packages/daemon/src/api/routes/admin-actions.ts
  - packages/core/src/caip/caip19.ts
  - packages/core/src/caip/index.ts
  - packages/core/src/schemas/policy.schema.ts
  - packages/daemon/src/api/routes/nft-approvals.ts
  - packages/core/src/interfaces/IChainAdapter.ts
autonomous: true
requirements: [TYPE-02, TYPE-03, TYPE-04]

must_haves:
  truths:
    - "resolveChainId 함수가 단일 위치에만 정의되고, actions.ts와 admin-actions.ts 모두 해당 구현을 import한다"
    - "CAIP-19 regex가 caip19.ts에서만 정의되고, policy.schema.ts는 import하여 사용한다"
    - "nft-approvals.ts에서 (adapter as any) 캐스팅 없이 인터페이스 기반 타입 가드로 getNftApprovalStatus를 호출한다"
  artifacts:
    - path: "packages/daemon/src/api/routes/actions.ts"
      provides: "resolveChainId import"
      contains: "import.*resolveChainId"
    - path: "packages/daemon/src/api/routes/admin-actions.ts"
      provides: "resolveChainId import"
      contains: "import.*resolveChainId"
    - path: "packages/core/src/caip/caip19.ts"
      provides: "CAIP19_REGEX 상수 export"
      contains: "export const CAIP19_REGEX"
    - path: "packages/core/src/schemas/policy.schema.ts"
      provides: "CAIP19_REGEX import"
      contains: "import.*CAIP19_REGEX"
    - path: "packages/daemon/src/api/routes/nft-approvals.ts"
      provides: "INftApprovalQuery 타입 가드"
      contains: "hasNftApprovalQuery"
  key_links:
    - from: "packages/daemon/src/api/routes/actions.ts"
      to: "packages/daemon/src/api/helpers/resolve-chain-id.ts"
      via: "import"
      pattern: "import.*resolveChainId"
    - from: "packages/core/src/schemas/policy.schema.ts"
      to: "packages/core/src/caip/caip19.ts"
      via: "import CAIP19_REGEX"
      pattern: "import.*CAIP19_REGEX"
---

<objective>
resolveChainId 중복 통합, CAIP-19 regex 중복 제거, NFT 타입 가드 교체 — 3개의 독립적 타입 안전성 개선을 수행한다.

Purpose: 동일 함수/정규식의 중복 정의를 제거하고, 런타임 `in` 체크 + `as any` 캐스팅을 인터페이스 기반 타입 가드로 교체하여 유지보수성과 타입 안전성을 높인다.
Output: 중복 제거된 resolveChainId, CAIP19_REGEX, 타입 안전한 getNftApprovalStatus 호출
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- resolveChainId 현재 중복 (actions.ts:661, admin-actions.ts:127) — 동일 구현 -->
```typescript
function resolveChainId(network: string): number {
  const CHAIN_IDS: Record<string, number> = {
    'ethereum-mainnet': 1,
    'ethereum-sepolia': 11155111,
    'ethereum-goerli': 5,
    'polygon-mainnet': 137,
    'polygon-mumbai': 80001,
    'arbitrum-mainnet': 42161,
    'arbitrum-sepolia': 421614,
    'optimism-mainnet': 10,
    'optimism-sepolia': 11155420,
    'base-mainnet': 8453,
    'base-sepolia': 84532,
  };
  return CHAIN_IDS[network] ?? 1;
}
```

<!-- CAIP-19 regex 현재 중복 -->
```typescript
// caip19.ts (line 8-11): Zod schema 내부
export const Caip19AssetTypeSchema = z.string().regex(
  /^[-a-z0-9]{3,8}:[-_a-zA-Z0-9]{1,32}\/[-a-z0-9]{3,8}:[-.%a-zA-Z0-9]{1,128}$/,
  'Invalid CAIP-19 asset type format',
);

// policy.schema.ts (line 84): 인라인 중복
const CAIP19_REGEX = /^[-a-z0-9]{3,8}:[-_a-zA-Z0-9]{1,32}\/[-a-z0-9]{3,8}:[-.%a-zA-Z0-9]{1,128}$/;
```

<!-- IChainAdapter에 getNftApprovalStatus 없음 — nft-approvals.ts에서 as any 사용 -->
```typescript
// nft-approvals.ts (line 111-112)
if ('getNftApprovalStatus' in adapter && typeof (adapter as any).getNftApprovalStatus === 'function') {
  const result = await (adapter as any).getNftApprovalStatus({...});
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: resolveChainId 공통 헬퍼 추출 및 중복 제거</name>
  <files>packages/daemon/src/api/helpers/resolve-chain-id.ts, packages/daemon/src/api/routes/actions.ts, packages/daemon/src/api/routes/admin-actions.ts</files>
  <action>
1. `packages/daemon/src/api/helpers/resolve-chain-id.ts` 파일을 새로 생성하고 `resolveChainId` 함수를 이동:
```typescript
/**
 * Resolve numeric EVM chain ID from network identifier.
 * Falls back to 1 (mainnet) for unknown networks.
 */
const CHAIN_IDS: Record<string, number> = {
  'ethereum-mainnet': 1,
  'ethereum-sepolia': 11155111,
  'ethereum-goerli': 5,
  'polygon-mainnet': 137,
  'polygon-mumbai': 80001,
  'arbitrum-mainnet': 42161,
  'arbitrum-sepolia': 421614,
  'optimism-mainnet': 10,
  'optimism-sepolia': 11155420,
  'base-mainnet': 8453,
  'base-sepolia': 84532,
};

export function resolveChainId(network: string): number {
  return CHAIN_IDS[network] ?? 1;
}
```

2. `packages/daemon/src/api/routes/actions.ts`에서:
   - line 661-676의 로컬 `resolveChainId` 함수 삭제
   - 상단에 `import { resolveChainId } from '../helpers/resolve-chain-id.js';` 추가
   - line 416의 `resolveChainId(resolvedNetwork)` 호출은 그대로 유지

3. `packages/daemon/src/api/routes/admin-actions.ts`에서:
   - line 127-142의 로컬 `resolveChainId` 함수 삭제
   - 상단에 `import { resolveChainId } from '../helpers/resolve-chain-id.js';` 추가
   - line 284의 `resolveChainId(resolvedNetwork)` 호출은 그대로 유지

주의: `packages/daemon/src/api/helpers/` 디렉토리가 이미 존재하는지 확인 (`resolve-wallet-id.ts`가 있으므로 존재함).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c "function resolveChainId" packages/daemon/src/api/routes/actions.ts packages/daemon/src/api/routes/admin-actions.ts | grep -v ":0$" | wc -l | xargs -I{} test {} -eq 0 && grep -c "resolveChainId" packages/daemon/src/api/helpers/resolve-chain-id.ts | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>resolveChainId가 resolve-chain-id.ts 한 곳에만 정의되고, actions.ts와 admin-actions.ts 모두 import하여 사용</done>
</task>

<task type="auto">
  <name>Task 2: CAIP-19 regex 중복 제거 및 NFT 타입 가드 교체</name>
  <files>packages/core/src/caip/caip19.ts, packages/core/src/caip/index.ts, packages/core/src/schemas/policy.schema.ts, packages/daemon/src/api/routes/nft-approvals.ts, packages/core/src/interfaces/IChainAdapter.ts</files>
  <action>
**CAIP-19 regex 중복 제거 (TYPE-03):**

1. `packages/core/src/caip/caip19.ts`에서 regex를 독립 상수로 추출하고 export:
   ```typescript
   /** CAIP-19 asset type regex pattern. Single source of truth. */
   export const CAIP19_REGEX = /^[-a-z0-9]{3,8}:[-_a-zA-Z0-9]{1,32}\/[-a-z0-9]{3,8}:[-.%a-zA-Z0-9]{1,128}$/;

   export const Caip19AssetTypeSchema = z.string().regex(
     CAIP19_REGEX,
     'Invalid CAIP-19 asset type format (expected chainId/namespace:reference)',
   );
   ```

2. `packages/core/src/caip/index.ts`에서 `CAIP19_REGEX` re-export 추가:
   ```typescript
   export { CAIP19_REGEX, Caip19AssetTypeSchema, ... } from './caip19.js';
   ```

3. `packages/core/src/schemas/policy.schema.ts`에서:
   - 기존 import 라인에 `CAIP19_REGEX` 추가: `import { Caip19Schema, CAIP19_REGEX } from '../caip/index.js';`
   - line 83-84의 중복 주석 + 인라인 `CAIP19_REGEX` 정의 삭제 (`// Duplicated from caip19.ts...` 주석 및 `const CAIP19_REGEX = ...`)
   - line 91의 `CAIP19_REGEX.test(key)` 호출은 그대로 유지 (import된 상수 사용)

**NFT 타입 가드 교체 (TYPE-04):**

4. `packages/core/src/interfaces/IChainAdapter.ts`에 선택적 인터페이스 추가 (IChainAdapter 인터페이스 아래에):
   ```typescript
   /**
    * Optional interface for adapters that support NFT approval status queries.
    * Use hasNftApprovalQuery() type guard to check before calling.
    */
   export interface INftApprovalQuery {
     getNftApprovalStatus(params: {
       owner: string;
       contractAddress: string;
       tokenId?: string;
       operator?: string;
     }): Promise<{
       tokenId: string;
       contractAddress: string;
       standard: string;
       approvals: Array<{ operator: string; approved: boolean; type: string }>;
     }>;
   }

   /** Type guard: check if adapter supports NFT approval queries. */
   export function hasNftApprovalQuery(adapter: IChainAdapter): adapter is IChainAdapter & INftApprovalQuery {
     return 'getNftApprovalStatus' in adapter && typeof (adapter as any).getNftApprovalStatus === 'function';
   }
   ```
   NOTE: `hasNftApprovalQuery` 내부의 `as any`는 타입 가드 구현 내부에 한정되므로 허용. 호출부에서는 타입 안전하게 사용.

5. `packages/core/src/interfaces/IChainAdapter.ts`의 export를 core/index.ts에서 확인 — `INftApprovalQuery`와 `hasNftApprovalQuery`가 core 패키지 외부에서 필요한 경우 core의 index.ts에도 export 추가. nft-approvals.ts가 daemon 패키지이므로 직접 import 가능하면 core/index.ts 수정 불필요.

6. `packages/daemon/src/api/routes/nft-approvals.ts`에서:
   - 상단에 `import { hasNftApprovalQuery } from '@waiaas/core';` 추가 (또는 core가 export하지 않으면 직접 경로 import)
   - line 111-112를 교체:
     ```typescript
     if (hasNftApprovalQuery(adapter)) {
       const result = await adapter.getNftApprovalStatus({
         owner: wallet.publicKey,
         contractAddress,
         tokenId,
         operator,
       });
       return c.json(result, 200);
     }
     ```
   - `(adapter as any)` 캐스팅이 완전히 제거됨

7. @waiaas/core의 index.ts에서 `hasNftApprovalQuery`와 `INftApprovalQuery` export 추가:
   - `packages/core/src/index.ts`에서 IChainAdapter 관련 export 라인을 찾아 `INftApprovalQuery`, `hasNftApprovalQuery` 추가

주의:
- core 패키지 변경 시 `pnpm turbo run build --filter=@waiaas/core` 먼저 실행하여 dist 업데이트
- circular dependency 위험 없음 (caip -> policy.schema 방향, IChainAdapter -> nft-approvals 방향 모두 단방향)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run build --filter=@waiaas/core && pnpm turbo run typecheck --filter=@waiaas/daemon --filter=@waiaas/core && pnpm turbo run test --filter=@waiaas/daemon --filter=@waiaas/core 2>&1 | tail -10</automated>
  </verify>
  <done>
1. resolveChainId가 actions.ts, admin-actions.ts에서 로컬 정의 0개, 공통 헬퍼에서 import
2. CAIP19_REGEX가 caip19.ts에서만 정의, policy.schema.ts에서 import
3. nft-approvals.ts에서 (adapter as any) 0개, hasNftApprovalQuery 타입 가드 사용
4. typecheck + test 전체 통과
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "function resolveChainId" packages/daemon/src/api/routes/` 결과 0건
2. `grep -n "CAIP19_REGEX" packages/core/src/schemas/policy.schema.ts` — import 라인만 존재, 로컬 정의 없음
3. `grep -c "as any" packages/daemon/src/api/routes/nft-approvals.ts` 결과 0
4. `pnpm turbo run lint && pnpm turbo run typecheck && pnpm turbo run test` 전체 통과
</verification>

<success_criteria>
resolveChainId 단일 정의, CAIP-19 regex 단일 정의, nft-approvals.ts에서 as any 제거 + 인터페이스 기반 타입 가드 사용, 전체 lint/typecheck/test 통과
</success_criteria>

<output>
After completion, create `.planning/phases/376-type-safety/376-02-SUMMARY.md`
</output>
