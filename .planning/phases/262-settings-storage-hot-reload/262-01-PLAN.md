---
phase: 262-settings-storage-hot-reload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/rpc/rpc-pool.ts
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/daemon/src/infrastructure/settings/hot-reload.ts
  - packages/daemon/src/__tests__/rpc-pool-settings-integration.test.ts
autonomous: true
requirements: [CONF-02, CONF-03]

must_haves:
  truths:
    - "Admin Settings API를 통해 네트워크별 RPC URL JSON 배열을 저장/조회할 수 있다"
    - "rpc_pool.* 키 변경 시 RpcPool이 데몬 재시작 없이 새 URL 목록으로 갱신된다"
    - "사용자 URL이 config.toml URL보다 높은 우선순위, config.toml이 빌트인 기본값보다 높은 우선순위를 가진다"
    - "URL 추가/삭제/순서 변경 결과가 즉시 RpcPool.getUrl()에 반영된다"
  artifacts:
    - path: "packages/core/src/rpc/rpc-pool.ts"
      provides: "replaceNetwork() method for atomic URL list replacement"
      contains: "replaceNetwork"
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "rpc_pool.* setting definitions for 13 networks"
      contains: "rpc_pool."
    - path: "packages/daemon/src/infrastructure/settings/hot-reload.ts"
      provides: "reloadRpcPool() handler triggered on rpc_pool.* key changes"
      contains: "reloadRpcPool"
    - path: "packages/daemon/src/__tests__/rpc-pool-settings-integration.test.ts"
      provides: "Integration tests for settings-driven RPC pool management"
  key_links:
    - from: "packages/daemon/src/infrastructure/settings/hot-reload.ts"
      to: "packages/core/src/rpc/rpc-pool.ts"
      via: "reloadRpcPool calls pool.replaceNetwork()"
      pattern: "replaceNetwork"
    - from: "packages/daemon/src/api/routes/admin.ts"
      to: "packages/daemon/src/infrastructure/settings/hot-reload.ts"
      via: "PUT /admin/settings triggers handleChangedKeys -> reloadRpcPool"
      pattern: "handleChangedKeys"
---

<objective>
SettingsService에서 네트워크별 RPC URL 목록을 JSON 배열로 저장하고, 변경 시 RpcPool에 hot-reload하는 전체 파이프라인을 구현한다.

Purpose: Admin Settings API를 통해 RPC URL을 CRUD하면 데몬 재시작 없이 RpcPool에 즉시 반영되어, 관리자가 런타임에 RPC 엔드포인트를 유연하게 관리할 수 있다.
Output: RpcPool.replaceNetwork() + rpc_pool.* SettingDefinitions + HotReloadOrchestrator rpc_pool.* 핸들러 + 통합 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/261-adapter-integration/261-01-SUMMARY.md
@.planning/phases/261-adapter-integration/261-02-SUMMARY.md

@packages/core/src/rpc/rpc-pool.ts
@packages/core/src/rpc/built-in-defaults.ts
@packages/daemon/src/infrastructure/settings/setting-keys.ts
@packages/daemon/src/infrastructure/settings/settings-service.ts
@packages/daemon/src/infrastructure/settings/hot-reload.ts
@packages/daemon/src/infrastructure/adapter-pool.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RpcPool.replaceNetwork() + rpc_pool.* SettingDefinitions + hot-reload 핸들러</name>
  <files>
    packages/core/src/rpc/rpc-pool.ts
    packages/daemon/src/infrastructure/settings/setting-keys.ts
    packages/daemon/src/infrastructure/settings/hot-reload.ts
  </files>
  <action>
**1. RpcPool.replaceNetwork() 메서드 추가** (`packages/core/src/rpc/rpc-pool.ts`)

RpcPool 클래스에 `replaceNetwork(network: string, urls: string[]): void` 메서드를 추가한다:
- 해당 네트워크의 기존 엔드포인트 배열을 완전히 제거하고, 새 URL 배열로 교체한다
- 새 URL들은 모두 `failureCount: 0, cooldownUntil: 0`으로 초기화한다
- 빈 배열이 전달되면 해당 네트워크의 엔드포인트가 완전히 제거된다 (`endpoints.delete(network)`)
- 이 메서드는 register()와 달리 기존 URL을 유지하지 않고 완전 교체(atomic replace)한다
- 위치: `reset()` 메서드 바로 아래에 추가

또한 `replaceNetwork`를 export에 포함시키기 위해 별도 작업 불필요 (클래스 메서드이므로 자동 노출)

**2. rpc_pool.* SettingDefinitions 추가** (`packages/daemon/src/infrastructure/settings/setting-keys.ts`)

SETTING_CATEGORIES에 `'rpc_pool'` 카테고리를 추가한다.

SETTING_DEFINITIONS 배열의 rpc 카테고리 블록 바로 아래에 rpc_pool 카테고리 블록을 추가한다:
- BUILT_IN_RPC_DEFAULTS의 13개 네트워크 키에 대응하는 13개 정의를 추가
- 네트워크 키 목록: mainnet, devnet, testnet, ethereum-mainnet, ethereum-sepolia, arbitrum-mainnet, arbitrum-sepolia, optimism-mainnet, optimism-sepolia, base-mainnet, base-sepolia, polygon-mainnet, polygon-amoy
- 각 정의 형식:
  ```
  { key: 'rpc_pool.mainnet', category: 'rpc_pool', configPath: 'rpc_pool.mainnet', defaultValue: '[]', isCredential: false }
  ```
- configPath는 `rpc_pool.<network>`으로 설정 (config.toml에는 rpc_pool 섹션이 없으므로 항상 defaultValue '[]'로 fallback됨 -- 이것이 의도된 동작: URL 목록은 Admin Settings API로만 관리)
- defaultValue는 '[]' (빈 JSON 배열 문자열)

**3. HotReloadOrchestrator에 rpc_pool.* 핸들러 추가** (`packages/daemon/src/infrastructure/settings/hot-reload.ts`)

3a. 상수 추가:
```typescript
const RPC_POOL_KEYS_PREFIX = 'rpc_pool.';
```

3b. `handleChangedKeys()`에 감지 로직 추가:
```typescript
const hasRpcPoolChanges = changedKeys.some((k) => k.startsWith(RPC_POOL_KEYS_PREFIX));
```
기존 hasRpcChanges 아래에 추가.

`if (hasRpcPoolChanges)` 블록 추가 (기존 `if (hasRpcChanges)` 블록 바로 아래):
```typescript
if (hasRpcPoolChanges) {
  reloads.push(
    this.reloadRpcPool(changedKeys.filter((k) => k.startsWith(RPC_POOL_KEYS_PREFIX))).catch((err) => {
      console.warn('Hot-reload RPC pool failed:', err);
    }),
  );
}
```

3c. `reloadRpcPool()` private 메서드 추가 (reloadRpc 메서드 아래에):
```typescript
/**
 * Reload RPC Pool URLs when rpc_pool.* settings change.
 * For each changed network:
 * 1. Parse the JSON array of user-managed URLs from SettingsService
 * 2. Build merged URL list: [user URLs] + [config.toml URL] + [built-in defaults]
 * 3. Replace the network's URL list in RpcPool atomically
 * 4. Evict cached adapters so next resolve() uses the new pool
 */
private async reloadRpcPool(changedPoolKeys: string[]): Promise<void> {
  const pool = this.deps.adapterPool;
  if (!pool) return;

  const rpcPool = pool.pool;
  if (!rpcPool) return;

  const ss = this.deps.settingsService;

  // Dynamically import BUILT_IN_RPC_DEFAULTS
  const { BUILT_IN_RPC_DEFAULTS } = await import('@waiaas/core');

  for (const key of changedPoolKeys) {
    // key format: 'rpc_pool.mainnet' or 'rpc_pool.ethereum-sepolia'
    const network = key.replace('rpc_pool.', '');

    // 1. Parse user-managed URL list from settings
    const rawJson = ss.get(key);
    let userUrls: string[] = [];
    try {
      const parsed = JSON.parse(rawJson);
      if (Array.isArray(parsed)) {
        userUrls = parsed.filter((u: unknown) => typeof u === 'string' && u.length > 0);
      }
    } catch {
      // Invalid JSON -- treat as empty
      userUrls = [];
    }

    // 2. Get config.toml URL for this network (if any)
    // Reverse map: network name -> rpc.* setting key
    const configUrls: string[] = [];
    const configKey = this.networkToConfigKey(network);
    if (configKey) {
      const configUrl = ss.get(`rpc.${configKey}`);
      if (configUrl) {
        configUrls.push(configUrl);
      }
    }

    // 3. Get built-in defaults for this network
    const builtInUrls = (BUILT_IN_RPC_DEFAULTS as Record<string, readonly string[]>)[network] ?? [];

    // 4. Merge: user URLs (highest priority) -> config.toml -> built-in defaults
    // Deduplicate while preserving order
    const seen = new Set<string>();
    const mergedUrls: string[] = [];
    for (const url of [...userUrls, ...configUrls, ...builtInUrls]) {
      if (!seen.has(url)) {
        seen.add(url);
        mergedUrls.push(url);
      }
    }

    // 5. Atomic replace in RpcPool
    rpcPool.replaceNetwork(network, mergedUrls);
    console.log(`Hot-reload: RpcPool ${network} updated (${mergedUrls.length} URLs: ${userUrls.length} user + ${configUrls.length} config + dedup built-in)`);

    // 6. Evict cached adapters for affected network
    // Determine chain type from network name
    const solanaNetworks = new Set(['mainnet', 'devnet', 'testnet']);
    if (solanaNetworks.has(network)) {
      await pool.evict('solana' as any, network as any);
    } else {
      await pool.evict('ethereum' as any, network as any);
    }
  }
}
```

3d. `networkToConfigKey()` private helper 메서드 추가 (reloadRpcPool 아래):
```typescript
/**
 * Reverse map: network name -> config.toml rpc field key.
 * mainnet -> solana_mainnet, devnet -> solana_devnet, testnet -> solana_testnet
 * ethereum-sepolia -> evm_ethereum_sepolia, base-mainnet -> evm_base_mainnet
 */
private networkToConfigKey(network: string): string | null {
  const solanaNetworks = new Set(['mainnet', 'devnet', 'testnet']);
  if (solanaNetworks.has(network)) {
    return `solana_${network}`;
  }
  // EVM: ethereum-sepolia -> evm_ethereum_sepolia
  return `evm_${network.replace(/-/g, '_')}`;
}
```

**Verify** after implementation:
- `pnpm turbo run typecheck --filter=@waiaas/core --filter=@waiaas/daemon` passes
- `pnpm turbo run lint --filter=@waiaas/core --filter=@waiaas/daemon` passes with 0 errors
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/core --filter=@waiaas/daemon && pnpm turbo run lint --filter=@waiaas/core --filter=@waiaas/daemon</automated>
  </verify>
  <done>
    - RpcPool.replaceNetwork() 메서드가 존재하고, 네트워크의 URL 목록을 atomic 교체한다
    - rpc_pool 카테고리와 13개 rpc_pool.* SettingDefinitions이 setting-keys.ts에 존재한다
    - HotReloadOrchestrator가 rpc_pool.* 키 변경을 감지하고 reloadRpcPool()을 호출한다
    - reloadRpcPool()이 user URLs > config.toml > built-in 우선순위로 병합 후 RpcPool.replaceNetwork()으로 반영한다
    - typecheck + lint 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 통합 테스트 -- Settings RPC Pool hot-reload 파이프라인</name>
  <files>
    packages/daemon/src/__tests__/rpc-pool-settings-integration.test.ts
  </files>
  <action>
`packages/daemon/src/__tests__/rpc-pool-settings-integration.test.ts` 파일을 새로 생성한다.

기존 `settings-hot-reload.test.ts` 및 `adapter-pool-rpc-pool.test.ts` 패턴을 따른다:
- in-memory SQLite + pushSchema
- SettingsService + HotReloadOrchestrator + real RpcPool (mock adapter eviction)

테스트 구조:

```typescript
describe('RPC Pool Settings Integration', () => {

  describe('rpc_pool.* SettingDefinitions', () => {
    it('13개 네트워크의 rpc_pool.* 키가 SETTING_DEFINITIONS에 등록되어 있다', () => {
      // SETTING_DEFINITIONS에서 rpc_pool.* 키 필터링, 13개 확인
      // 카테고리가 모두 'rpc_pool'인지 확인
      // defaultValue가 모두 '[]'인지 확인
    });

    it('SettingsService.get()으로 rpc_pool.* 키를 조회하면 기본값 [] 반환', () => {
      // SettingsService 생성, get('rpc_pool.mainnet') === '[]'
    });

    it('SettingsService.set()으로 JSON 배열을 저장하고 get()으로 복원', () => {
      // set('rpc_pool.mainnet', '["https://custom.rpc.com","https://another.rpc.com"]')
      // get('rpc_pool.mainnet') === '["https://custom.rpc.com","https://another.rpc.com"]'
    });
  });

  describe('RpcPool.replaceNetwork()', () => {
    it('기존 URL을 완전히 교체한다', () => {
      // pool.register('mainnet', ['https://a.com', 'https://b.com'])
      // pool.replaceNetwork('mainnet', ['https://x.com', 'https://y.com'])
      // pool.getUrl('mainnet') === 'https://x.com'
      // pool.getStatus('mainnet') has exactly 2 entries
    });

    it('빈 배열로 교체하면 네트워크가 제거된다', () => {
      // pool.register('mainnet', ['https://a.com'])
      // pool.replaceNetwork('mainnet', [])
      // pool.hasNetwork('mainnet') === false
    });

    it('cooldown 상태의 기존 URL이 교체 후 초기화된다', () => {
      // pool에 URL 등록 후 reportFailure로 cooldown 적용
      // replaceNetwork 호출
      // 새 URL이 모두 available 상태
    });
  });

  describe('HotReloadOrchestrator rpc_pool.* dispatch', () => {
    it('rpc_pool.* 키 변경 시 reloadRpcPool이 호출된다', () => {
      // handleChangedKeys(['rpc_pool.mainnet']) 호출
      // RpcPool이 업데이트되었는지 확인 (getStatus로 검증)
    });

    it('user URL이 최고 우선순위로 등록된다', () => {
      // settings에 rpc_pool.devnet = '["https://custom.dev.com"]' 설정
      // handleChangedKeys(['rpc_pool.devnet']) 호출
      // pool.getUrl('devnet') === 'https://custom.dev.com' (user URL이 첫 번째)
    });

    it('user URL + config.toml + built-in이 올바른 순서로 병합된다', () => {
      // config에 rpc.solana_mainnet = 'https://config-rpc.com' 설정
      // settings에 rpc_pool.mainnet = '["https://user-rpc.com"]' 설정
      // handleChangedKeys 호출
      // pool.getStatus('mainnet')의 URL 순서 검증:
      //   [user-rpc, config-rpc, built-in-1, built-in-2, ...]
    });

    it('중복 URL이 제거된다', () => {
      // user URL에 built-in과 동일한 URL 포함
      // 병합 후 중복 없이 한 번만 등록됨을 확인
    });

    it('잘못된 JSON이면 빈 user URL로 처리된다', () => {
      // settings에 rpc_pool.mainnet = 'not-valid-json' 설정
      // handleChangedKeys 호출
      // pool에 config.toml + built-in만 등록됨
    });

    it('Solana 네트워크 변경 시 solana 어댑터가 evict된다', () => {
      // mock adapterPool.evict 설정
      // handleChangedKeys(['rpc_pool.mainnet']) 호출
      // evict('solana', 'mainnet') 호출 확인
    });

    it('EVM 네트워크 변경 시 ethereum 어댑터가 evict된다', () => {
      // mock adapterPool.evict 설정
      // handleChangedKeys(['rpc_pool.ethereum-sepolia']) 호출
      // evict('ethereum', 'ethereum-sepolia') 호출 확인
    });

    it('여러 네트워크 키가 동시에 변경되면 모두 처리된다', () => {
      // handleChangedKeys(['rpc_pool.mainnet', 'rpc_pool.ethereum-sepolia']) 호출
      // 두 네트워크 모두 업데이트됨 확인
    });
  });

});
```

테스트에서 AdapterPool의 evict 메서드는 spy/mock으로 처리한다 (실제 adapter 생성 불필요).
RpcPool은 실제 인스턴스를 사용하여 URL 등록/교체가 올바르게 동작하는지 검증한다.

**CRITICAL**: 테스트 작성 시 기존 adapter-pool-rpc-pool.test.ts의 패턴을 따른다:
- `new RpcPool({ nowFn: () => now })` for deterministic time
- `createDatabase(':memory:')` + `pushSchema()` for in-memory DB
- `DaemonConfigSchema.parse({})` for default config

총 테스트 케이스 수: 약 11개 (3 + 3 + 5 = 11, 추가 엣지 케이스 포함 가능)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/rpc-pool-settings-integration.test.ts</automated>
  </verify>
  <done>
    - rpc-pool-settings-integration.test.ts가 존재한다
    - 모든 테스트(~11개)가 통과한다
    - rpc_pool.* SettingDefinitions 등록, RpcPool.replaceNetwork(), hot-reload 파이프라인이 검증된다
    - user > config.toml > built-in 우선순위 순서가 테스트로 검증된다
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/core --filter=@waiaas/daemon` -- 전체 타입 체크 통과
2. `pnpm turbo run lint --filter=@waiaas/core --filter=@waiaas/daemon` -- 린트 0 에러
3. `pnpm vitest run packages/daemon/src/__tests__/rpc-pool-settings-integration.test.ts` -- 전체 통과
4. `pnpm vitest run packages/daemon/src/__tests__/settings-hot-reload.test.ts` -- 기존 테스트 영향 없음
5. `pnpm vitest run packages/daemon/src/__tests__/adapter-pool-rpc-pool.test.ts` -- 기존 테스트 영향 없음
6. `pnpm vitest run packages/core/src/__tests__/rpc-pool.test.ts` -- 기존 RpcPool 테스트 영향 없음 (존재 시)
</verification>

<success_criteria>
- Admin Settings API에서 `rpc_pool.mainnet` 같은 키로 JSON 배열 URL 목록을 저장/조회 가능
- `rpc_pool.*` 키 변경 시 HotReloadOrchestrator가 감지하고 RpcPool.replaceNetwork()으로 즉시 반영
- URL 우선순위: user URLs (Admin Settings) > config.toml > built-in defaults
- 중복 URL 자동 제거, 잘못된 JSON graceful 처리
- 기존 rpc.* 단일 URL 설정 및 hot-reload 동작이 영향받지 않음
- 11+ 통합 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/262-settings-storage-hot-reload/262-01-SUMMARY.md`
</output>
