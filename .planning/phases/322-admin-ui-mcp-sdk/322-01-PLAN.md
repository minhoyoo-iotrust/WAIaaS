---
phase: 322-admin-ui-mcp-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/tools/erc8004-get-agent-info.ts
  - packages/mcp/src/tools/erc8004-get-reputation.ts
  - packages/mcp/src/tools/erc8004-get-validation-status.ts
  - packages/mcp/src/server.ts
  - packages/sdk/src/client.ts
  - packages/sdk/src/types.ts
  - packages/mcp/src/__tests__/erc8004-tools.test.ts
  - packages/sdk/src/__tests__/erc8004-methods.test.ts
autonomous: true
requirements: [API-02, API-03, API-04]

must_haves:
  truths:
    - "MCP에서 8개 write 도구(action_erc8004_agent_*)가 자동 등록되어 사용 가능하다"
    - "MCP에서 3개 read-only 도구(erc8004_get_agent_info, erc8004_get_reputation, erc8004_get_validation_status)가 사용 가능하다"
    - "SDK에서 11개 ERC-8004 메서드가 호출 가능하고 올바른 타입을 반환한다"
  artifacts:
    - path: "packages/mcp/src/tools/erc8004-get-agent-info.ts"
      provides: "erc8004_get_agent_info MCP read-only tool"
      min_lines: 15
    - path: "packages/mcp/src/tools/erc8004-get-reputation.ts"
      provides: "erc8004_get_reputation MCP read-only tool"
      min_lines: 15
    - path: "packages/mcp/src/tools/erc8004-get-validation-status.ts"
      provides: "erc8004_get_validation_status MCP read-only tool"
      min_lines: 15
    - path: "packages/sdk/src/client.ts"
      provides: "11 ERC-8004 SDK methods"
      contains: "getAgentInfo"
    - path: "packages/sdk/src/types.ts"
      provides: "ERC-8004 response types"
      contains: "Erc8004AgentInfoResponse"
  key_links:
    - from: "packages/mcp/src/tools/erc8004-get-agent-info.ts"
      to: "/v1/erc8004/agent/:agentId"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*erc8004/agent"
    - from: "packages/sdk/src/client.ts"
      to: "/v1/erc8004/*"
      via: "this.http.get"
      pattern: "this\\.http\\.get.*erc8004"
    - from: "packages/mcp/src/server.ts"
      to: "packages/mcp/src/tools/erc8004-*.ts"
      via: "register function imports"
      pattern: "registerErc8004"
---

<objective>
MCP 11개 도구(8 write auto-expose + 3 read-only 수동 등록)와 SDK 11개 메서드(8 write via executeAction + 3 read via direct GET) 구현.

Purpose: AI 에이전트가 MCP와 SDK를 통해 ERC-8004 기능에 프로그래밍 방식으로 접근할 수 있는 인터페이스 완성
Output: MCP 3개 read-only 도구 파일, SDK 11개 메서드 + 타입, 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/321-eip712-approval-wallet-linking/321-02-SUMMARY.md

<interfaces>
<!-- MCP tool registration pattern (from packages/mcp/src/tools/connect-info.ts): -->
```typescript
import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { type ApiClient, toToolResult } from '../api-client.js';
import { type WalletContext, withWalletPrefix } from '../server.js';

export function registerConnectInfo(server: McpServer, apiClient: ApiClient): void {
  server.tool('connect_info', 'description', {}, async () => {
    const result = await apiClient.get('/v1/connect-info');
    return toToolResult(result);
  });
}
```

<!-- MCP server registration (from packages/mcp/src/server.ts): -->
```typescript
// Import at top:
import { registerConnectInfo } from './tools/connect-info.js';
// In createMcpServer():
registerConnectInfo(server, apiClient);
// Action providers auto-registered via registerActionProviderTools() in index.ts
```

<!-- SDK method pattern (from packages/sdk/src/client.ts): -->
```typescript
async executeAction(provider: string, action: string, params?: ExecuteActionParams): Promise<ExecuteActionResponse> {
  const body: Record<string, unknown> = {};
  if (params?.params) body.params = params.params;
  if (params?.network) body.network = params.network;
  if (params?.walletId) body.walletId = params.walletId;
  if (params?.gasCondition) body.gasCondition = params.gasCondition;
  return withRetry(
    () => this.http.post<ExecuteActionResponse>(`/v1/actions/${provider}/${action}`, body, this.authHeaders()),
    this.retryOptions,
  );
}
```

<!-- REST API endpoints (from packages/daemon/src/api/routes/erc8004.ts): -->
```
GET /v1/erc8004/agent/:agentId          -> { agentId, wallet, uri, metadata, registryAddress, chainId }
GET /v1/erc8004/agent/:agentId/reputation?tag1=&tag2= -> { agentId, count, score, decimals, tag1, tag2 }
GET /v1/erc8004/registration-file/:walletId -> (free-form JSON)
GET /v1/erc8004/validation/:requestHash  -> { requestHash, validator, agentId, response, responseHash, tag, lastUpdate }
```

<!-- 8 write actions auto-exposed via mcpExpose: true in erc8004_agent provider: -->
<!-- register_agent, set_agent_wallet, unset_agent_wallet, set_agent_uri, set_metadata, give_feedback, revoke_feedback, request_validation -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP 3개 read-only 도구 + SDK 11개 메서드 + 타입</name>
  <files>
    packages/mcp/src/tools/erc8004-get-agent-info.ts
    packages/mcp/src/tools/erc8004-get-reputation.ts
    packages/mcp/src/tools/erc8004-get-validation-status.ts
    packages/mcp/src/server.ts
    packages/sdk/src/client.ts
    packages/sdk/src/types.ts
  </files>
  <action>
    **MCP 3개 read-only 도구 생성** (기존 connect-info.ts 패턴 따름):

    1. `erc8004-get-agent-info.ts`:
       - Tool name: `erc8004_get_agent_info`
       - Description: `withWalletPrefix('Get ERC-8004 agent identity info (on-chain ID, wallet, URI, metadata).', walletContext?.walletName)`
       - Input: `{ agent_id: z.string().describe('On-chain agent ID (uint256)') }`
       - Calls: `apiClient.get(\`/v1/erc8004/agent/\${args.agent_id}\`)`

    2. `erc8004-get-reputation.ts`:
       - Tool name: `erc8004_get_reputation`
       - Description: `withWalletPrefix('Get ERC-8004 agent reputation summary (score, count, decimals).', walletContext?.walletName)`
       - Input: `{ agent_id: z.string(), tag1: z.string().optional(), tag2: z.string().optional() }`
       - Build query params from tag1/tag2, calls: `apiClient.get(\`/v1/erc8004/agent/\${args.agent_id}/reputation\${qs}\`)`

    3. `erc8004-get-validation-status.ts`:
       - Tool name: `erc8004_get_validation_status`
       - Description: `withWalletPrefix('Get ERC-8004 validation request status.', walletContext?.walletName)`
       - Input: `{ request_hash: z.string().describe('Validation request hash (bytes32)') }`
       - Calls: `apiClient.get(\`/v1/erc8004/validation/\${args.request_hash}\`)`

    **server.ts 수정**: import 3개 register 함수 추가, createMcpServer() 내에서 호출. 기존 24개 도구 뒤에 추가. 주석의 도구 개수 25 -> 28로 수정.

    **SDK types.ts에 ERC-8004 응답 타입 추가** (독립 타입, @waiaas/core 의존 없음):

    ```typescript
    // ERC-8004 Types
    export interface Erc8004AgentInfoResponse {
      agentId: string;
      wallet: string;
      uri: string;
      metadata: Record<string, unknown>;
      registryAddress: string;
      chainId: number;
    }

    export interface Erc8004ReputationResponse {
      agentId: string;
      count: number;
      score: string;
      decimals: number;
      tag1: string;
      tag2: string;
    }

    export interface Erc8004RegistrationFileResponse {
      [key: string]: unknown;
    }

    export interface Erc8004ValidationResponse {
      requestHash: string;
      validator: string;
      agentId: string;
      response: number;
      responseHash: string;
      tag: string;
      lastUpdate: number;
    }

    // ERC-8004 write action convenience params
    export interface Erc8004RegisterAgentParams {
      name: string;
      description?: string;
      services?: Array<{ name: string; endpoint: string; version?: string }>;
      metadata?: Record<string, string>;
      network?: string;
      walletId?: string;
      gasCondition?: GasCondition;
    }

    // ... (similar for each of the 8 write actions)
    ```

    **SDK client.ts에 11개 메서드 추가** (executeAction 이후, createWallet 이전):

    8개 write 메서드 (executeAction 래퍼):
    - `registerAgent(params)` -> `this.executeAction('erc8004_agent', 'register_agent', { params: { name, description, services, metadata }, network, walletId, gasCondition })`
    - `setAgentWallet(params)` -> `this.executeAction('erc8004_agent', 'set_agent_wallet', ...)`
    - `unsetAgentWallet(params)` -> `this.executeAction('erc8004_agent', 'unset_agent_wallet', ...)`
    - `setAgentUri(params)` -> `this.executeAction('erc8004_agent', 'set_agent_uri', ...)`
    - `setAgentMetadata(params)` -> `this.executeAction('erc8004_agent', 'set_metadata', ...)`
    - `giveFeedback(params)` -> `this.executeAction('erc8004_agent', 'give_feedback', ...)`
    - `revokeFeedback(params)` -> `this.executeAction('erc8004_agent', 'revoke_feedback', ...)`
    - `requestValidation(params)` -> `this.executeAction('erc8004_agent', 'request_validation', ...)`

    3개 read 메서드 (direct GET):
    - `getAgentInfo(agentId: string)` -> `this.http.get<Erc8004AgentInfoResponse>(\`/v1/erc8004/agent/\${agentId}\`, this.authHeaders())`
    - `getAgentReputation(agentId: string, opts?: { tag1?: string; tag2?: string })` -> `this.http.get<Erc8004ReputationResponse>(\`/v1/erc8004/agent/\${agentId}/reputation\${qs}\`, this.authHeaders())`
    - `getValidationStatus(requestHash: string)` -> `this.http.get<Erc8004ValidationResponse>(\`/v1/erc8004/validation/\${requestHash}\`, this.authHeaders())`

    Read 메서드에도 withRetry 래핑 적용. JSDoc 주석에 client.ts 상단의 메서드 카운트 21 -> 32로 수정.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run build --filter=@waiaas/mcp --filter=@waiaas/sdk && pnpm turbo run typecheck --filter=@waiaas/mcp --filter=@waiaas/sdk</automated>
  </verify>
  <done>MCP 3개 read-only 도구가 server.ts에 등록되고, SDK에 11개 ERC-8004 메서드(8 write + 3 read)와 타입이 추가되어 빌드 + 타입체크 통과</done>
</task>

<task type="auto">
  <name>Task 2: MCP + SDK 테스트</name>
  <files>
    packages/mcp/src/__tests__/erc8004-tools.test.ts
    packages/sdk/src/__tests__/erc8004-methods.test.ts
  </files>
  <action>
    **MCP 도구 테스트** (`erc8004-tools.test.ts`):
    기존 MCP 테스트 패턴 따름 (apiClient mock + server.tool 등록 확인).

    1. `erc8004_get_agent_info` 도구 등록 확인
    2. `erc8004_get_reputation` 도구 등록 확인 (tag1/tag2 query param 전달)
    3. `erc8004_get_validation_status` 도구 등록 확인
    4. 8개 write 도구가 `registerActionProviderTools`를 통해 자동 등록되는지 확인 (providers API mock으로 mcpExpose: true + erc8004_agent 반환 -> registered map에 8 entries)
    5. API 오류 시 isError=true 반환 확인

    **SDK 메서드 테스트** (`erc8004-methods.test.ts`):
    기존 SDK 테스트 패턴 따름 (HttpClient mock).

    1. `registerAgent` -> POST /v1/actions/erc8004_agent/register_agent 호출 확인
    2. `setAgentWallet` -> POST /v1/actions/erc8004_agent/set_agent_wallet 호출 확인
    3. `giveFeedback` -> POST /v1/actions/erc8004_agent/give_feedback 호출 확인
    4. `getAgentInfo` -> GET /v1/erc8004/agent/:agentId 호출 확인 + 응답 타입 확인
    5. `getAgentReputation` -> GET /v1/erc8004/agent/:agentId/reputation 호출 확인 + tag params
    6. `getValidationStatus` -> GET /v1/erc8004/validation/:hash 호출 확인
    7. Read 메서드에 withRetry 래핑 확인
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/mcp/src/__tests__/erc8004-tools.test.ts packages/sdk/src/__tests__/erc8004-methods.test.ts --reporter=verbose</automated>
  </verify>
  <done>MCP 5개 테스트와 SDK 7개 테스트가 모두 통과하여 11개 도구/메서드의 올바른 REST API 호출과 응답 처리를 검증</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run build --filter=@waiaas/mcp --filter=@waiaas/sdk` 성공
2. `pnpm turbo run typecheck --filter=@waiaas/mcp --filter=@waiaas/sdk` 성공
3. MCP 3개 read-only 도구 테스트 통과
4. SDK 11개 메서드 테스트 통과
5. 기존 MCP/SDK 테스트 회귀 없음
</verification>

<success_criteria>
- MCP에서 erc8004_get_agent_info, erc8004_get_reputation, erc8004_get_validation_status 3개 도구가 등록됨
- 8개 write 도구는 registerActionProviderTools를 통해 자동 등록됨 (mcpExpose: true)
- SDK에서 registerAgent, setAgentWallet, unsetAgentWallet, setAgentUri, setAgentMetadata, giveFeedback, revokeFeedback, requestValidation, getAgentInfo, getAgentReputation, getValidationStatus 11개 메서드가 사용 가능
- 12개 이상 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/322-admin-ui-mcp-sdk/322-01-SUMMARY.md`
</output>
