---
phase: 456-doc39-rewrite
plan: 02
type: execute
wave: 2
depends_on:
  - 456-01
files_modified:
  - internal/design/39-tauri-desktop-architecture.md
autonomous: true
requirements:
  - DOC-04
  - DOC-05
  - DOC-06

must_haves:
  truths:
    - "프로젝트 구조에서 apps/desktop/src/pages/ 8화면이 제거되고 packages/admin/src/ 확장으로 대체됨"
    - "UI 화면별 플로우가 기존 Admin Web UI 19페이지 + Desktop 전용 확장(Wizard, WalletConnect, Sidecar Status)으로 기술됨"
    - "구현 노트가 React 18 관련 내용에서 Admin Web UI 재사용 + isDesktop() 패턴으로 변경됨"
  artifacts:
    - path: "internal/design/39-tauri-desktop-architecture.md"
      provides: "Section 6, 7, 13 rewritten for Admin Web UI reuse architecture"
      contains: "packages/admin/src/"
  key_links:
    - from: "Section 6 project structure"
      to: "Section 7 UI flows"
      via: "File paths match page references"
      pattern: "packages/admin/src/pages"
    - from: "Section 13 implementation notes"
      to: "Section 2.2 layer separation"
      via: "isDesktop() pattern referenced consistently"
      pattern: "isDesktop"
---

<objective>
설계 문서 39의 하위 3개 섹션(6 프로젝트 구조, 7 UI 화면별 플로우, 13 구현 노트)을 React 18 SPA에서 Admin Web UI 재사용 구조로 재작성한다.

Purpose: 별도 8화면 프론트엔드 구조를 폐기하고, 기존 Admin Web UI 19페이지를 재사용하면서 Desktop 전용 확장만 추가하는 구조로 문서를 갱신한다.
Output: 설계 문서 39의 섹션 6, 7, 13이 Admin Web UI 재사용 + Desktop 확장 아키텍처를 정확히 기술하는 상태.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@internal/objectives/m33-00-desktop-architecture-redesign.md
@internal/objectives/m33-02-desktop-app.md
@internal/design/39-tauri-desktop-architecture.md (target file)
@.planning/phases/456-doc39-rewrite/456-01-SUMMARY.md (prior plan results)

<interfaces>
<!-- Key references for the rewrite -->

Current Admin Web UI 19 pages (packages/admin/src/pages/):
actions.tsx, audit-logs.tsx, credentials.tsx, dashboard.tsx, erc8004.tsx,
human-wallet-apps.tsx, hyperliquid.tsx, notifications.tsx, policies.tsx,
polymarket.tsx, rpc-proxy.tsx, security.tsx, sessions.tsx, system.tsx,
telegram-users.tsx, tokens.tsx, transactions.tsx, walletconnect.tsx, wallets.tsx

Admin Web UI sidebar structure (5 sections):
- Wallets: wallets, credentials, tokens, actions, transactions
- Protocols: erc8004, hyperliquid, polymarket, rpc-proxy
- Security: policies, sessions, security, audit-logs
- Channels: notifications, telegram-users, human-wallet-apps, walletconnect
- System: dashboard, system

Desktop-only extensions (from m33-02):
- packages/admin/src/pages/setup-wizard.tsx (new, Desktop only)
- packages/admin/src/components/desktop-status.tsx (new, Desktop only)
- packages/admin/src/utils/platform.ts (new, isDesktop())
- packages/admin/src/utils/tauri-bridge.ts (new, IPC wrapper)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Section 6 Project Structure</name>
  <files>internal/design/39-tauri-desktop-architecture.md</files>
  <action>
섹션 6 프로젝트 구조(line 821-952)를 전면 재작성한다.

**섹션 6 변경 (per DOC-04):**

1. **섹션 6.1** 제목 변경: `### 6.1 packages/desktop 디렉토리 트리` -> `### 6.1 apps/desktop + packages/admin 확장 구조`

2. **디렉토리 트리 전면 교체:**
   - `packages/desktop/src/` 전체(pages/ 8화면, components/, hooks/, lib/, styles/) 제거
   - `apps/desktop/` 경로로 변경 (Tauri 앱은 apps/ 하위)
   - `apps/desktop/src-tauri/` 부분은 유지 (Rust Backend는 변경 없음)
   - `apps/desktop/src/` 이하 React 프론트엔드 전체 제거 (별도 빌드 없음)
   - 대신 `packages/admin/src/` 확장 부분을 명시:

```
apps/desktop/
  src-tauri/
    src/
      main.rs              # Tauri entry point + WebView URL 설정 (http://localhost:{port}/admin)
      sidecar.rs           # SidecarManager (변경 없음)
      tray.rs              # 시스템 트레이 (변경 없음)
      commands.rs          # IPC commands (변경 없음)
    binaries/              # Sidecar 바이너리 (변경 없음)
    icons/                 # 트레이/앱 아이콘 (변경 없음)
    tauri.conf.json        # window.url = http://localhost:{port}/admin
    Cargo.toml
    capabilities/
      default.json

packages/admin/src/        # 기존 Admin Web UI (Desktop에서 그대로 재사용)
  pages/                   # 기존 19페이지 (변경 없음 — Desktop WebView에서 동일하게 렌더링)
    dashboard.tsx
    wallets.tsx
    sessions.tsx
    policies.tsx
    ... (19페이지 전체)
    setup-wizard.tsx       # [신규] Desktop 전용 — Setup Wizard 5단계
  components/
    ... (기존 컴포넌트)
    desktop-status.tsx     # [신규] Desktop 전용 — Sidecar 상태 카드
  utils/
    ... (기존 유틸)
    platform.ts            # [신규] isDesktop() 환경 감지
    tauri-bridge.ts        # [신규] window.__TAURI__.invoke() 래퍼
  desktop/                 # [신규] Desktop 전용 모듈 (dynamic import 대상)
    wizard/                # Setup Wizard 단계별 컴포넌트
    sidecar-panel/         # Sidecar 상태 패널
```

3. **섹션 6.2 package.json 변경:**
   - `@waiaas/desktop` package.json을 Tauri Rust 전용으로 간소화 (프론트엔드 의존성 제거)
   - React, react-dom, react-router-dom, tailwindcss, @vitejs/plugin-react 전부 제거
   - 대신: apps/desktop/package.json은 Tauri CLI만 포함

```json
{
  "name": "@waiaas/desktop",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
```

   - packages/admin/package.json에 Desktop 전용 optional dependencies 추가 설명:
     - `@tauri-apps/api` (Desktop 전용, dynamic import)
     - `@reown/appkit` (Desktop 전용, dynamic import)
     - 이들은 브라우저 빌드에서 tree-shake out됨

4. **섹션 6.3 Cargo.toml** 유지 (Rust 의존성은 변경 없음)

5. "src/index.html", "vite.config.ts", "tailwind.config.ts" 등 별도 프론트엔드 빌드 설정 파일 목록 전부 제거. WebView는 Admin Web UI를 URL로 로드하므로 별도 빌드 불필요함을 명시.

핵심 메시지: "Desktop 앱의 프론트엔드는 별도로 존재하지 않는다. `apps/desktop/`은 Tauri Rust 셸만 포함하고, UI는 Sidecar Manager가 시작한 데몬의 Admin Web UI(`http://localhost:{port}/admin`)를 WebView에서 로드한다."
  </action>
  <verify>
    <automated>grep -c "apps/desktop/src/pages/" internal/design/39-tauri-desktop-architecture.md | grep -q "^0$" && grep -c "packages/admin/src/" internal/design/39-tauri-desktop-architecture.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 섹션 6에서 packages/desktop/src/ 또는 apps/desktop/src/ 프론트엔드 파일 트리가 제거됨
    - apps/desktop/은 src-tauri/ (Rust 셸)만 포함
    - packages/admin/src/ 확장(setup-wizard, desktop-status, platform.ts, tauri-bridge.ts)이 명시됨
    - package.json에서 React 관련 의존성이 모두 제거됨
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Section 7 UI Flows + Section 13 Implementation Notes</name>
  <files>internal/design/39-tauri-desktop-architecture.md</files>
  <action>
섹션 7 UI 화면별 플로우(line 954-1413)와 섹션 13 구현 노트(line 2140-2248)를 재작성한다.

**섹션 7 변경 (per DOC-05):**

1. **섹션 7.1 화면 구성 개요** 전면 재작성:
   - 기존 8화면 Mermaid 다이어그램 제거
   - 새 구조: "Admin Web UI 기존 19페이지 + Desktop 전용 확장 3개"
   - 새 테이블:

| 구분 | 페이지 | Desktop | 브라우저 | 비고 |
|------|--------|:-------:|:--------:|------|
| 기존 19페이지 | dashboard, wallets, sessions, policies, tokens, transactions, actions, audit-logs, credentials, erc8004, hyperliquid, polymarket, rpc-proxy, security, notifications, telegram-users, human-wallet-apps, walletconnect, system | O | O | 코드 변경 없이 WebView에서 동일하게 동작 |
| Desktop 전용 | Setup Wizard | O | X | 최초 실행 시 자동 진입, isDesktop() 가드 |
| Desktop 전용 | Sidecar Status | O | X | 대시보드에 상태 카드 추가, isDesktop() 가드 |
| Desktop 전용 | WalletConnect QR (확장) | O | 제한적 | Desktop에서 @reown/appkit QR 페어링 활성화 |

   - 설계 핵심 변경 설명:
     "기존 설계(v0.5)는 8개 화면(Dashboard, Approvals, Sessions, Agents, Settings, Setup, OwnerConnect, KillSwitch)을 React 18로 별도 구현했다. 변경된 설계에서는 Admin Web UI의 기존 19페이지를 그대로 재사용하고, Desktop 전용 기능만 3개 확장으로 추가한다."

2. **섹션 7.2-7.9 (화면 1-8 상세)** 재작성:
   - 기존 8화면 상세(7.2-7.9)를 대폭 축소
   - 기존 19페이지에 대해서는 "Admin Web UI와 동일하므로 상세 생략. packages/admin/src/pages/ 참조." 한 문장으로 대체
   - 대신 Desktop 전용 확장 3개에 대한 상세를 기술:

   **7.2 Desktop 전용 확장: Setup Wizard**
   - 기존 7.7(Setup Wizard) 내용 중 v0.7 보완 부분은 유지하되, React 참조를 Preact로 변경
   - Mermaid 시퀀스 다이어그램에서 `Setup Wizard (React)` -> `Setup Wizard (Admin Web UI)` 변경
   - `react-router navigate` -> `Preact Router / 기존 Admin Web UI 라우팅` 변경
   - 5단계 흐름은 m33-02의 정의와 일치하도록 유지: (1)마스터 패스워드 설정 (2)체인 선택 (3)첫 월렛 생성 (4)Owner 연결(WalletConnect, 스킵 가능) (5)완료 요약

   **7.3 Desktop 전용 확장: Sidecar Status Panel**
   - 대시보드에 추가되는 Sidecar 상태 카드 설명
   - IPC `invoke('get_sidecar_status')` -> DaemonStatus 표시
   - 데몬 상태(Running/Stopped/Error), PID, uptime, restart count 표시
   - isDesktop() 가드로 브라우저에서는 렌더링하지 않음

   **7.4 Desktop 전용 확장: WalletConnect QR 통합**
   - 기존 7.8(Owner Connect) 내용을 축소하여 Desktop 확장으로 이동
   - @reown/appkit QR 페어링 흐름은 유지하되, "별도 페이지"가 아닌 "wallets 상세 페이지의 조건부 컴포넌트"로 변경
   - WalletConnect QR 시퀀스 다이어그램 유지 (기존 7.8의 Mermaid)

3. **기존 7.2-7.6 (화면 1-5: Dashboard, Approvals, Sessions, Agents, Settings) 및 7.9 (Kill Switch) 상세 제거:**
   - 이들은 Admin Web UI에 이미 구현되어 있으므로, 설계 문서에서 상세 레이아웃/컴포넌트 명세를 유지할 필요 없음
   - 다만, v0.5 변경 메모와 v0.7 보완 중 Desktop과 직접 관련된 내용(IPC 연동, Sidecar Manager 호출 등)은 섹션 7.2(Setup Wizard)로 이동하여 보존
   - "기존 Admin Web UI와 동일한 19페이지의 상세 레이아웃은 설계 문서 67(Admin Web UI 설계)을 참조한다." 참조 문구 추가

**섹션 13 변경 (per DOC-06):**

1. **섹션 13.1 IPC + HTTP 이중 채널 에러 처리** 재작성:
   - "React 에러 통합 패턴" 소제목 -> "Admin Web UI 에러 통합 패턴"
   - `useDaemonHealth()` hook 언급 -> Desktop 전용 에러 통합 패턴으로 변경
   - "Toast(일시적 에러) 또는 Banner(지속적 에러)" 패턴은 유지 (Admin Web UI에서도 유사 패턴 사용 가능)
   - 핵심 변경: "React state" -> "@preact/signals state"
   - ECONNREFUSED 처리 전략에서 `invoke()` IPC 호출 패턴은 유지 (Desktop 전용이므로 isDesktop() 가드 내에서 동작)

2. **섹션 13.2 Setup Wizard와 CLI init** 재작성:
   - "Setup Wizard(React)" -> "Setup Wizard(Admin Web UI Desktop 확장)"
   - "react-router navigate" -> "Admin Web UI 라우팅"
   - v0.7 보완 Mermaid 시퀀스에서 `Setup Wizard (React)` -> `Setup Wizard (Admin Web UI)` 변경
   - 나머지 내용(CLI 위임, idempotent, InitJsonResponse 스키마)은 변경 없이 유지

3. **섹션 13에 새 구현 노트 추가:**
   - **13.3 Admin Web UI 재사용 시 고려사항:**
     - "Admin Web UI는 브라우저에서 `http://localhost:{port}/admin`으로 접근하도록 설계되었다. Desktop WebView에서도 동일 URL을 로드하므로, Admin Web UI의 기존 동작(masterAuth, apiCall, 라우팅, CSP)이 그대로 적용된다."
     - "Desktop 전용 코드는 `isDesktop()` 가드 내에서만 실행되어야 한다. `window.__TAURI_INTERNALS__` 전역 객체의 존재 여부로 환경을 판별한다."
     - "Desktop 전용 모듈(@tauri-apps/api, @reown/appkit)은 반드시 dynamic import로 로드하여 브라우저 번들에 포함되지 않도록 한다."
     - "Admin Web UI의 기존 CSP(default-src 'none')는 Desktop에서 추가 조정이 필요할 수 있다. Tauri WebView의 CSP 정책은 tauri.conf.json에서 별도 설정한다."
  </action>
  <verify>
    <automated>grep -c "19페이지" internal/design/39-tauri-desktop-architecture.md | grep -q "[1-9]" && grep -c "isDesktop" internal/design/39-tauri-desktop-architecture.md | grep -q "[1-9]" && grep -c "Setup Wizard" internal/design/39-tauri-desktop-architecture.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 섹션 7이 기존 19페이지 + Desktop 전용 확장 3개 구조로 재작성됨
    - 기존 8화면 별도 상세(Dashboard~KillSwitch)가 제거되고 Admin Web UI 참조로 대체됨
    - Desktop 전용 확장(Setup Wizard, Sidecar Status, WalletConnect)이 상세 기술됨
    - 섹션 13에서 React 18 언급이 Admin Web UI + isDesktop()로 변경됨
    - 섹션 13.3에 Admin Web UI 재사용 시 고려사항이 추가됨
  </done>
</task>

</tasks>

<verification>
1. 섹션 6에서 "apps/desktop/src/pages/" 또는 "packages/desktop/src/pages/" 8화면이 없어야 한다
2. 섹션 7에서 기존 19페이지가 명시되고 Desktop 전용 확장 3개가 구분되어야 한다
3. 섹션 13에서 "React 에러 통합 패턴" 대신 "Admin Web UI" 패턴이 기술되어야 한다
4. 전체 문서에서 "React 18" 문자열이 아키텍처 설명에서 제거되어야 한다 (역사적 참조 제외)
</verification>

<success_criteria>
- 섹션 6, 7, 13이 Admin Web UI 재사용 + Desktop 전용 확장 아키텍처를 정확히 기술
- DOC-04, DOC-05, DOC-06 요구사항이 충족됨
- 기존 8화면 별도 구현 명세가 완전히 제거됨
- 문서 전체의 React 18 참조가 역사적 맥락(1.3절 v0.1->v0.2 변경 요약 등) 이외에서 제거됨
</success_criteria>

<output>
After completion, create `.planning/phases/456-doc39-rewrite/456-02-SUMMARY.md`
</output>
