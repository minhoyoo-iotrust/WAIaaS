---
phase: 456-doc39-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/design/39-tauri-desktop-architecture.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "Section 2.1 아키텍처 다이어그램이 WebView에서 Admin Web UI(http://localhost:{port}/admin)를 로드하는 구조를 보여준다"
    - "Section 2.2 계층 역할 분리에서 React 18 언급이 완전히 제거되고 Preact 10.x Admin Web UI 재사용이 기술되어 있다"
    - "Section 3.3 HTTP localhost 통신이 apiCall() 상대 경로 재사용 패턴으로 갱신되어 있다"
  artifacts:
    - path: "internal/design/39-tauri-desktop-architecture.md"
      provides: "Section 2.1, 2.2, 3.3 rewritten for Admin Web UI reuse architecture"
      contains: "Admin Web UI"
  key_links:
    - from: "Section 2.1 diagram"
      to: "Section 2.2 layer table"
      via: "WebView layer references same tech stack"
      pattern: "Preact 10.x"
    - from: "Section 3.3 HTTP localhost"
      to: "packages/admin/src/api/client.ts"
      via: "apiCall() relative path pattern"
      pattern: "apiCall"
---

<objective>
설계 문서 39의 아키텍처 상위 3개 섹션(2.1 다이어그램, 2.2 계층 분리, 3.3 HTTP 통신)을 React 18 SPA에서 Admin Web UI 재사용 구조로 재작성한다.

Purpose: React 18 SPA 별도 구현 설계를 폐기하고, 기존 Admin Web UI(Preact 10.x)를 WebView에서 로드하는 아키텍처로 문서를 정확하게 갱신한다.
Output: 설계 문서 39의 섹션 2.1, 2.2, 3.3이 Admin Web UI 재사용 아키텍처를 정확히 기술하는 상태.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@internal/objectives/m33-00-desktop-architecture-redesign.md
@internal/objectives/m33-02-desktop-app.md
@internal/design/39-tauri-desktop-architecture.md (target file)
@packages/admin/src/api/client.ts (apiCall pattern reference)

<interfaces>
<!-- Key references for the rewrite -->

From packages/admin/src/api/client.ts:
```typescript
// apiCall() uses relative paths — fetch(path, {...})
// No absolute URL, works from any origin (browser or WebView)
export async function apiCall<T>(path: string, options?: RequestInit): Promise<T>
```

Current Admin Web UI tech stack:
- Preact 10.x + @preact/signals
- Vite 6.x bundler
- No TailwindCSS — custom CSS
- CSP: default-src 'none'
- masterAuth only (X-Master-Password header)
- 19 pages in packages/admin/src/pages/

Admin Web UI 19 pages (v32.10):
actions.tsx, audit-logs.tsx, credentials.tsx, dashboard.tsx, erc8004.tsx,
human-wallet-apps.tsx, hyperliquid.tsx, notifications.tsx, policies.tsx,
polymarket.tsx, rpc-proxy.tsx, security.tsx, sessions.tsx, system.tsx,
telegram-users.tsx, tokens.tsx, transactions.tsx, walletconnect.tsx, wallets.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Section 2.1 Architecture Diagram + Section 2.2 Layer Separation</name>
  <files>internal/design/39-tauri-desktop-architecture.md</files>
  <action>
섹션 2.1 아키텍처 다이어그램(line 47-87)과 섹션 2.2 계층 역할 분리(line 89-97)를 재작성한다.

**섹션 2.1 변경 (per DOC-01):**
- Mermaid 다이어그램에서 WebView 서브그래프 내부를 변경:
  - `React["React 18 SPA"]` 제거
  - `SDK["@waiaas/sdk<br/>HTTP localhost 호출"]` 제거
  - `TW["TailwindCSS 4"]` 제거
  - 대신: `AdminUI["Admin Web UI<br/>(Preact 10.x)<br/>http://localhost:{port}/admin"]` 추가
  - `DesktopExt["Desktop 전용 확장<br/>isDesktop() 가드<br/>dynamic import"]` 추가 (점선 또는 별도 노드)
  - `AppKit["@reown/appkit<br/>WalletConnect QR"]` 유지 (Desktop 전용이므로 DesktopExt 내부 또는 연결)
- WebView -> Hono 연결 라벨을 `HTTP localhost<br/>apiCall() 상대 경로` 로 변경
- 서브그래프 라벨: `WebView Frontend (src/)` -> `WebView (Admin Web UI 로드)`

**섹션 2.2 변경 (per DOC-02):**
- 계층 역할 분리 테이블에서 WebView Frontend 행을 변경:
  - 기술: `React 18 + TailwindCSS 4` -> `기존 Admin Web UI (Preact 10.x + @preact/signals)`
  - 역할: `UI 렌더링, 사용자 상호작용, API 호출` -> `기존 Admin Web UI 19페이지 렌더링 + Desktop 전용 확장(isDesktop() 조건부)`
  - 상태 유지: `React state (in-memory)` -> `@preact/signals (in-memory)`
- **핵심 설계 결정** 문단 아래에 Admin Web UI 재사용 근거를 추가:
  - "WebView는 별도 프론트엔드를 빌드하지 않고, Sidecar Manager가 시작한 데몬의 Admin Web UI(`http://localhost:{port}/admin`)를 로드한다. 이를 통해 기존 19페이지를 코드 중복 없이 Desktop에서 제공하며, Desktop 전용 기능(Setup Wizard, WalletConnect, Sidecar 상태)은 `isDesktop()` 환경 감지 + dynamic import로 조건부 활성화한다."

변경하지 않는 부분: Rust Backend 서브그래프, Sidecar 서브그래프, 기존 연결선(SM->Sidecar, NB->OS 등)은 그대로 유지.
  </action>
  <verify>
    <automated>grep -c "React 18" internal/design/39-tauri-desktop-architecture.md | grep -q "^0$" && grep -c "Preact 10" internal/design/39-tauri-desktop-architecture.md | grep -q "[1-9]" && grep -c "Admin Web UI" internal/design/39-tauri-desktop-architecture.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 섹션 2.1 Mermaid 다이어그램에 "Admin Web UI", "Preact 10.x", "localhost:{port}/admin"이 포함됨
    - 섹션 2.1에서 "React 18 SPA", "TailwindCSS 4" 노드가 제거됨
    - 섹션 2.2 테이블에서 WebView Frontend 행이 Preact 10.x + @preact/signals로 변경됨
    - React 18 언급이 섹션 2.1-2.2에서 완전히 제거됨
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Section 3.3 HTTP localhost Communication</name>
  <files>internal/design/39-tauri-desktop-architecture.md</files>
  <action>
섹션 3.3 HTTP localhost(line 148-164)를 재작성한다.

**섹션 3.3 변경 (per DOC-03):**
- 제목: `### 3.3 HTTP localhost -- API 호출 전용` 유지
- 본문 첫 문장 변경: "API 호출은 WebView에서 **HTTP localhost:3100**을 직접 호출한다." -> "API 호출은 WebView에 로드된 **Admin Web UI가 기존 `apiCall()` 함수로 상대 경로 호출**한다. Desktop WebView에서도 브라우저와 동일한 코드 경로를 사용한다."
- `@waiaas/sdk` 사용 방식 설명을 변경:
  - 기존: "@waiaas/sdk WAIaaSOwnerClient" 사용
  - 변경: "Admin Web UI의 `packages/admin/src/api/client.ts`의 `apiCall()` 함수를 그대로 사용. `apiCall()`은 상대 경로(`/v1/admin/...`)로 `fetch()`를 호출하므로, WebView가 `http://localhost:{port}/admin`을 로드하면 동일 origin으로 API 요청이 자동 라우팅된다."
- API 경로 테이블에서:
  - 호출 방식 열을 `apiCall()` 또는 `fetch 직접 호출`로 변경 (@waiaas/sdk 언급 제거 — Admin UI는 SDK를 사용하지 않고 apiCall()을 직접 사용)
  - masterAuth 인증 열: Admin Web UI의 기존 `X-Master-Password` 헤더 자동 첨부 패턴 언급
- HTTP 사용 이유 문단 갱신:
  - 기존: "@waiaas/sdk를 WebView에서 그대로 재사용" -> "Admin Web UI의 apiCall()을 그대로 재사용 — Desktop 전용 HTTP 클라이언트 코드 불필요"
  - 추가: "Admin Web UI가 이미 masterAuth(X-Master-Password 헤더)를 apiCall()에서 자동 처리하므로, Desktop에서 별도 인증 로직이 필요 없다."

또한, 섹션 3.1의 Mermaid 다이어그램(line 107-131)에서 WebView 서브그래프 라벨을 `WebView (React)` -> `WebView (Admin Web UI)` 로 변경하고, fetch URL 라벨을 `fetch('http://127.0.0.1:3100/v1/...')<br/>@waiaas/sdk 사용` -> `apiCall('/v1/...')<br/>상대 경로 (기존 Admin Web UI 패턴)` 로 변경한다.

변경하지 않는 부분: 섹션 3.2 IPC 내용, 섹션 3.4 CORS 설정은 그대로 유지.
  </action>
  <verify>
    <automated>grep -c "apiCall" internal/design/39-tauri-desktop-architecture.md | grep -q "[1-9]" && grep "3.3" internal/design/39-tauri-desktop-architecture.md | grep -qi "localhost" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 섹션 3.3이 apiCall() 상대 경로 패턴으로 갱신됨
    - Admin Web UI의 기존 packages/admin/src/api/client.ts 재사용이 명시됨
    - @waiaas/sdk 직접 사용 언급이 apiCall() 재사용으로 대체됨
    - 섹션 3.1 다이어그램의 WebView 라벨이 Admin Web UI로 변경됨
  </done>
</task>

</tasks>

<verification>
1. 섹션 2.1 다이어그램에서 "React 18", "TailwindCSS 4" 문자열이 없어야 한다
2. 섹션 2.2 테이블에서 "Preact 10.x" 또는 "Admin Web UI"가 있어야 한다
3. 섹션 3.3에서 "apiCall" 문자열이 있어야 한다
4. 전체 문서에서 섹션 4, 5, 8-12는 변경되지 않아야 한다 (이 플랜에서는 건드리지 않음)
</verification>

<success_criteria>
- 섹션 2.1, 2.2, 3.3이 React 18 SPA 대신 Admin Web UI 재사용 아키텍처를 기술
- DOC-01, DOC-02, DOC-03 요구사항이 충족됨
- 변경하지 않는 섹션(4, 5, 8-12)이 무결함
</success_criteria>

<output>
After completion, create `.planning/phases/456-doc39-rewrite/456-01-SUMMARY.md`
</output>
