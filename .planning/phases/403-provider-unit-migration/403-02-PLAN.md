---
phase: 403-provider-unit-migration
plan: 02
type: execute
wave: 2
depends_on: ["403-01"]
files_modified:
  - packages/actions/src/providers/kamino/schemas.ts
  - packages/actions/src/providers/kamino/index.ts
  - packages/actions/src/providers/kamino/kamino-migration.test.ts
  - packages/actions/src/providers/lido-staking/index.ts
  - packages/actions/src/providers/lido-staking/lido-migration.test.ts
  - packages/actions/src/providers/jito-staking/index.ts
  - packages/actions/src/providers/jito-staking/jito-stake-pool.ts
  - packages/actions/src/providers/jito-staking/jito-migration.test.ts
autonomous: true
requirements: [UNIT-01, UNIT-02, UNIT-03, UNIT-05, TEST-01, TEST-02, TEST-07]

must_haves:
  truths:
    - "Kamino supply/borrow가 smallest-unit(lamports 기반) 입력을 직접 사용하고 parseTokenAmount 호출이 제거되어 있다"
    - "Kamino repay/withdraw에서 max 키워드가 단위 변환과 독립적으로 정상 동작한다"
    - "Lido stake/unstake가 smallest-unit(wei) 입력을 직접 사용한다"
    - "Jito stake/unstake가 smallest-unit(lamports) 입력을 직접 사용하고 parseSolAmount 호출이 제거되어 있다"
    - "4개 provider 모두 레거시 소수점 입력이 migrateAmount()를 통해 자동 변환된다"
  artifacts:
    - path: "packages/actions/src/providers/kamino/index.ts"
      provides: "smallest-unit 기반 Kamino resolve 메서드"
    - path: "packages/actions/src/providers/kamino/schemas.ts"
      provides: "smallest-unit .describe() 업데이트된 Kamino 스키마"
    - path: "packages/actions/src/providers/lido-staking/index.ts"
      provides: "smallest-unit 기반 Lido resolve 메서드 + 스키마"
    - path: "packages/actions/src/providers/jito-staking/index.ts"
      provides: "smallest-unit 기반 Jito resolve 메서드 + 스키마"
    - path: "packages/actions/src/providers/jito-staking/jito-stake-pool.ts"
      provides: "parseSolAmount 호출 제거된 stake pool"
  key_links:
    - from: "packages/actions/src/providers/kamino/index.ts"
      to: "packages/actions/src/common/migrate-amount.ts"
      via: "import migrateAmount"
      pattern: "migrateAmount\\(input\\.amount"
    - from: "packages/actions/src/providers/lido-staking/index.ts"
      to: "packages/actions/src/common/migrate-amount.ts"
      via: "import migrateAmount"
      pattern: "migrateAmount\\(input\\.amount"
    - from: "packages/actions/src/providers/jito-staking/index.ts"
      to: "packages/actions/src/common/migrate-amount.ts"
      via: "import migrateAmount"
      pattern: "migrateAmount\\(input\\.amount"
---

<objective>
Kamino, Lido, Jito 3개 provider를 smallest-unit 입력으로 전환하고 하위 호환성 및 max 키워드 테스트를 완성한다.

Purpose: 4개 레거시 provider 마이그레이션을 완료하여 모든 14개 non-CLOB provider가 일관된 단위 규칙을 따르도록 한다
Output: 3개 provider의 parseTokenAmount/parseSolAmount 제거, migrateAmount 적용, 종합 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/403-provider-unit-migration/403-01-SUMMARY.md

@packages/actions/src/common/migrate-amount.ts
@packages/actions/src/providers/kamino/index.ts
@packages/actions/src/providers/kamino/schemas.ts
@packages/actions/src/providers/lido-staking/index.ts
@packages/actions/src/providers/jito-staking/index.ts
@packages/actions/src/providers/jito-staking/jito-stake-pool.ts

<interfaces>
From packages/actions/src/common/migrate-amount.ts (created in 403-01):
```typescript
export function migrateAmount(value: string, decimals: number): bigint;
// - value에 소수점 포함 시: parseTokenAmount()로 변환 + console.warn deprecation 경고
// - value에 소수점 미포함 시: BigInt(value) 직접 변환 (smallest-unit)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kamino provider smallest-unit 마이그레이션</name>
  <files>packages/actions/src/providers/kamino/schemas.ts, packages/actions/src/providers/kamino/index.ts, packages/actions/src/providers/kamino/kamino-migration.test.ts</files>
  <behavior>
    - Test 1: kamino_supply({ asset: "...", amount: "1000000" }) => 1000000n lamports 직접 사용 (USDC 6 decimals 상당)
    - Test 2: kamino_supply({ asset: "...", amount: "100.5" }) => migrateAmount로 변환 + deprecation 경고
    - Test 3: kamino_repay({ asset: "...", amount: "max" }) => 'max' 키워드가 sdkWrapper에 전달됨
    - Test 4: kamino_withdraw({ asset: "...", amount: "max" }) => 'max' 키워드가 sdkWrapper에 전달됨
    - Test 5: kamino_borrow({ asset: "...", amount: "500000" }) => 500000n 직접 사용
    - Test 6: kamino_repay({ asset: "...", amount: "1000000" }) => smallest-unit 직접 사용
  </behavior>
  <action>
    1. **schemas.ts 업데이트**: 4개 스키마의 amount .describe()를 smallest-unit 형식으로 변경:
       - `'Amount in smallest units (e.g., lamports for SOL tokens). Example: "1000000" = 1.0 USDC. Legacy decimal input (e.g., "100.5") is auto-converted with deprecation warning.'`
       - repay/withdraw의 `.or(z.literal('max'))` 유지

    2. **index.ts 변경**:
       - `import { parseTokenAmount }` 제거
       - `import { migrateAmount } from '../../common/migrate-amount.js'` 추가
       - resolveSupply: `parseTokenAmount(input.amount, 6)` -> `migrateAmount(input.amount, 6)`
       - resolveBorrow: `parseTokenAmount(input.amount, 6)` -> `migrateAmount(input.amount, 6)`
       - resolveRepay: `input.amount === 'max' ? 'max' : parseTokenAmount(input.amount, 6)` -> `input.amount === 'max' ? 'max' : migrateAmount(input.amount, 6)`
       - resolveWithdraw: 동일 패턴

    3. **kamino-migration.test.ts 생성**:
       - MockKaminoSdkWrapper 사용 (기존 패턴)
       - 각 action에 대해 smallest-unit 정수 문자열 + 레거시 소수점 + max 키워드 테스트
       - sdkWrapper.buildSupplyInstruction 등에 전달되는 amount 값 검증
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/providers/kamino/kamino-migration.test.ts</automated>
  </verify>
  <done>Kamino 4개 action이 smallest-unit 입력을 직접 사용하고, 레거시 입력은 자동 변환되며, max 키워드가 정상 동작한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lido + Jito provider smallest-unit 마이그레이션</name>
  <files>packages/actions/src/providers/lido-staking/index.ts, packages/actions/src/providers/lido-staking/lido-migration.test.ts, packages/actions/src/providers/jito-staking/index.ts, packages/actions/src/providers/jito-staking/jito-stake-pool.ts, packages/actions/src/providers/jito-staking/jito-migration.test.ts</files>
  <behavior>
    - Lido Test 1: stake({ amount: "1000000000000000000" }) => 1 ETH wei 직접 사용, calldata의 value가 "1000000000000000000"
    - Lido Test 2: stake({ amount: "1.0" }) => migrateAmount로 변환 + deprecation 경고
    - Lido Test 3: unstake({ amount: "500000000000000000" }) => 0.5 stETH wei 직접 사용
    - Lido Test 4: unstake({ amount: "0.5" }) => 레거시 소수점 자동 변환
    - Jito Test 1: stake({ amount: "1500000000" }) => 1.5 SOL lamports 직접 사용
    - Jito Test 2: stake({ amount: "1.5" }) => migrateAmount로 변환 + deprecation 경고
    - Jito Test 3: unstake({ amount: "1000000000" }) => 1 SOL lamports 직접 사용
    - Jito Test 4: stake minimum check: migrateAmount 결과가 JITO_MIN_DEPOSIT_LAMPORTS 미만이면 에러
  </behavior>
  <action>
    1. **Lido index.ts 변경**:
       - `import { parseTokenAmount }` 제거
       - `import { migrateAmount } from '../../common/migrate-amount.js'` 추가
       - LidoStakeInputSchema의 amount .describe()를 smallest-unit 형식으로 변경:
         `'Amount in smallest units (wei). Example: "1000000000000000000" = 1.0 ETH. Legacy decimal input (e.g., "1.0") is auto-converted with deprecation warning.'`
       - LidoUnstakeInputSchema도 동일 패턴 (stETH wei)
       - resolveStake: `parseTokenAmount(input.amount, 18)` -> `migrateAmount(input.amount, 18)`
       - resolveUnstake: `parseTokenAmount(input.amount, 18)` -> `migrateAmount(input.amount, 18)`

    2. **Jito index.ts 변경**:
       - JitoStakeInputSchema의 amount .describe()를 smallest-unit 형식으로 변경:
         `'Amount in smallest units (lamports). Example: "1000000000" = 1.0 SOL. Legacy decimal input (e.g., "1.0") is auto-converted with deprecation warning.'`
       - JitoUnstakeInputSchema도 동일 (JitoSOL lamports)
       - resolveStake: `parseSolAmount(input.amount)` -> `migrateAmount(input.amount, 9)` (import migrateAmount 추가)
       - resolveUnstake: 동일 패턴
       - `import { parseSolAmount } from './jito-stake-pool.js'` 제거 (다른 곳에서 사용하지 않으면)

    3. **jito-stake-pool.ts 변경**:
       - `parseSolAmount` 함수는 유지하되 export only (외부 의존 없으면 제거 가능)
       - `import { parseTokenAmount }` 는 parseSolAmount가 남아있으면 유지, 제거되면 함께 제거
       - 주의: parseSolAmount는 jito-stake-pool.ts에 정의되고 index.ts에서만 사용됨 -> index.ts에서 미사용 시 parseSolAmount + parseTokenAmount import 모두 제거 가능

    4. **lido-migration.test.ts 생성**:
       - LidoStakingActionProvider 인스턴스 생성 (기본 config)
       - stake/unstake에 smallest-unit 문자열 입력 -> 반환된 ContractCallRequest의 value/calldata 검증
       - 레거시 소수점 입력 시 console.warn 호출 확인

    5. **jito-migration.test.ts 생성**:
       - JitoStakingActionProvider 인스턴스 생성 (기본 config)
       - stake/unstake에 smallest-unit 문자열 입력 테스트
       - 레거시 소수점 입력 시 자동 변환 + 경고 확인
       - JITO_MIN_DEPOSIT_LAMPORTS 검증 (smallest-unit로 전달한 값이 올바르게 비교되는지)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/providers/lido-staking/lido-migration.test.ts src/providers/jito-staking/jito-migration.test.ts</automated>
  </verify>
  <done>Lido와 Jito가 smallest-unit 입력을 직접 사용하고, 레거시 소수점 입력은 자동 변환되며, Jito minimum deposit이 정상 동작한다</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/actions exec vitest run src/providers/kamino/kamino-migration.test.ts` - Kamino 마이그레이션 테스트 통과
2. `pnpm --filter @waiaas/actions exec vitest run src/providers/lido-staking/lido-migration.test.ts` - Lido 마이그레이션 테스트 통과
3. `pnpm --filter @waiaas/actions exec vitest run src/providers/jito-staking/jito-migration.test.ts` - Jito 마이그레이션 테스트 통과
4. `pnpm turbo run typecheck --filter=@waiaas/actions` - 타입 체크 통과
5. parseTokenAmount import가 kamino/index.ts, lido-staking/index.ts에서 제거되었는지 확인
6. parseSolAmount 호출이 jito-staking/index.ts에서 제거되었는지 확인
</verification>

<success_criteria>
- Kamino 4개 action이 smallest-unit 입력을 직접 사용하고 max 키워드가 정상 동작한다
- Lido 2개 action이 smallest-unit(wei) 입력을 직접 사용한다
- Jito 2개 action이 smallest-unit(lamports) 입력을 직접 사용한다
- 3개 provider 모두 레거시 소수점 입력이 자동 변환되고 deprecation 경고가 출력된다
- 모든 테스트와 타입 체크가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/403-provider-unit-migration/403-02-SUMMARY.md`
</output>
