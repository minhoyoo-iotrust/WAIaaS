---
phase: 451-sdk-deprecated-admin-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/wallet-sdk/src/channels/ntfy.ts
  - packages/wallet-sdk/src/channels/index.ts
  - packages/wallet-sdk/src/index.ts
  - packages/daemon/src/services/signing-sdk/wallet-app-service.ts
  - packages/daemon/src/api/routes/wallet-apps.ts
  - packages/daemon/src/api/routes/openapi-schemas.ts
  - packages/admin/src/api/types.generated.ts
autonomous: true
requirements: [SDK-01, SDK-02, ADMIN-01, ADMIN-02]

must_haves:
  truths:
    - "sendViaNtfy, subscribeToRequests, subscribeToNotifications, parseNotification have @deprecated JSDoc"
    - "sendViaRelay, registerDevice, unregisterDevice, getSubscriptionToken remain without deprecated markers"
    - "WalletApp API response includes push_relay_url field"
    - "Register and Update wallet app API accepts push_relay_url"
    - "types.generated.ts reflects push_relay_url and sdk_push"
  artifacts:
    - path: "packages/wallet-sdk/src/channels/ntfy.ts"
      provides: "Deprecated ntfy channel functions"
      contains: "@deprecated"
    - path: "packages/daemon/src/api/routes/openapi-schemas.ts"
      provides: "WalletApp schema with push_relay_url"
      contains: "push_relay_url"
    - path: "packages/admin/src/api/types.generated.ts"
      provides: "Generated types with push_relay_url and sdk_push"
      contains: "push_relay_url"
  key_links:
    - from: "packages/daemon/src/api/routes/wallet-apps.ts"
      to: "packages/daemon/src/services/signing-sdk/wallet-app-service.ts"
      via: "toApiResponse mapper includes pushRelayUrl"
      pattern: "push_relay_url.*pushRelayUrl"
    - from: "packages/daemon/src/api/routes/openapi-schemas.ts"
      to: "packages/admin/src/api/types.generated.ts"
      via: "pnpm generate:api-types"
      pattern: "push_relay_url"
---

<objective>
Wallet SDK ntfy 함수 deprecated 처리 + WalletApp API에 push_relay_url 필드 추가

Purpose: SDK 사용자에게 ntfy 함수 사용 중단을 알리고, Admin UI가 Push Relay URL을 표시할 수 있도록 API를 확장한다.
Output: Deprecated ntfy 함수, push_relay_url 포함 API 응답, 갱신된 Admin UI 타입
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- WalletApp service interface (to be extended with pushRelayUrl) -->
From packages/daemon/src/services/signing-sdk/wallet-app-service.ts:
```typescript
export interface WalletApp {
  id: string;
  name: string;
  displayName: string;
  walletType: string;
  signingEnabled: boolean;
  alertsEnabled: boolean;
  signTopic: string | null;
  notifyTopic: string | null;
  subscriptionToken: string | null;
  createdAt: number;
  updatedAt: number;
}
```

From packages/daemon/src/api/routes/openapi-schemas.ts:
```typescript
export const WalletAppSchema = z.object({
  // ... existing fields
  sign_topic: z.string().nullable(),
  notify_topic: z.string().nullable(),
  subscription_token: z.string().nullable(),
  // push_relay_url needs to be added
});
```

From packages/daemon/src/api/routes/wallet-apps.ts:
```typescript
function toApiResponse(app: WalletApp | WalletAppWithUsedBy) {
  return {
    // ... maps WalletApp fields to API response
    // push_relay_url needs to be added
  };
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SDK ntfy 함수 deprecated 표시</name>
  <files>packages/wallet-sdk/src/channels/ntfy.ts, packages/wallet-sdk/src/channels/index.ts, packages/wallet-sdk/src/index.ts</files>
  <action>
1. In `packages/wallet-sdk/src/channels/ntfy.ts`:
   - Add `@deprecated Use Push Relay functions (sendViaRelay) instead. Will be removed in next major version.` JSDoc to `sendViaNtfy()` (before existing JSDoc, merge into same block)
   - Add `@deprecated Use Push Relay device registration instead. Will be removed in next major version.` JSDoc to `subscribeToRequests()`
   - Add `@deprecated Use Push Relay device registration instead. Will be removed in next major version.` JSDoc to `subscribeToNotifications()`
   - Add `@deprecated Use Push Relay notification parsing instead. Will be removed in next major version.` JSDoc to `parseNotification()`
   - Update the module-level JSDoc comment at the top to note these are deprecated in favor of Push Relay

2. In `packages/wallet-sdk/src/index.ts`:
   - Update the Public API doc comment: mark sendViaNtfy, subscribeToRequests, subscribeToNotifications, parseNotification as "(deprecated)" in the comment

3. Do NOT modify `packages/wallet-sdk/src/channels/relay.ts` -- those functions should remain unchanged (SDK-02 verification)

4. Do NOT modify `packages/wallet-sdk/src/channels/index.ts` -- re-exports should remain (we're deprecating, not removing)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c "@deprecated" packages/wallet-sdk/src/channels/ntfy.ts | grep -q "4" && grep -cL "@deprecated" packages/wallet-sdk/src/channels/relay.ts | grep -q "relay.ts" && echo "OK"</automated>
  </verify>
  <done>All 4 ntfy functions have @deprecated JSDoc. relay.ts functions remain without deprecated markers.</done>
</task>

<task type="auto">
  <name>Task 2: WalletApp API push_relay_url 필드 추가 + 타입 재생성</name>
  <files>packages/daemon/src/services/signing-sdk/wallet-app-service.ts, packages/daemon/src/api/routes/wallet-apps.ts, packages/daemon/src/api/routes/openapi-schemas.ts, packages/admin/src/api/types.generated.ts</files>
  <action>
1. **WalletApp interface** in `wallet-app-service.ts`:
   - Add `pushRelayUrl: string | null;` to the `WalletApp` interface
   - Update `mapRow()` to include: `pushRelayUrl: (row.push_relay_url as string) || null,`
   - Update all SQL SELECT queries in `getByName`, `getById`, `list`, `getAlertEnabledApps` to include `push_relay_url` in the column list
   - Update `register()` to accept `pushRelayUrl` in opts, insert it into the INSERT statement, and return it in the result object
   - Update `update()` to accept `pushRelayUrl` in fields and add SET clause for it
   - Update `register()` return object to include `pushRelayUrl: opts?.pushRelayUrl ?? null`

2. **API response mapper** in `wallet-apps.ts`:
   - Add `push_relay_url: app.pushRelayUrl,` to `toApiResponse()`
   - Update the `createAppRoute` handler to pass `pushRelayUrl: body.push_relay_url` to `register()`
   - Update the `updateAppRoute` handler to pass `pushRelayUrl: body.push_relay_url` to `update()`
   - Update the test notification handler (line ~247-251): replace ntfy topic publishing with Push Relay HTTP POST. Use the app's `pushRelayUrl` -- if it exists, POST to `${pushRelayUrl}/v1/push` with `{ subscriptionToken: app.subscriptionToken, category: 'notification', payload: { ... } }`. If no pushRelayUrl, return `{ success: false, error: 'No Push Relay URL configured for this app' }`.

3. **OpenAPI schemas** in `openapi-schemas.ts`:
   - Add `push_relay_url: z.string().nullable().openapi({ description: 'Push Relay server URL for sign requests and notifications' })` to `WalletAppSchema`
   - Add `push_relay_url: z.string().url().max(512).optional().openapi({ description: 'Push Relay server URL' })` to `WalletAppCreateRequestSchema`
   - Add `push_relay_url: z.string().url().max(512).optional().openapi({ description: 'Push Relay server URL' })` to `WalletAppUpdateRequestSchema`
   - Update `WalletAppTestNotificationResponseSchema` description: change "ntfy topic" references to "Push Relay"
   - Update `sign_topic` and `notify_topic` descriptions from "ntfy topic" to "(legacy, unused)"
   - Update `subscription_token` description from "ntfy topic isolation" to "Push Relay subscription token"
   - In the approval_method enum in owner-related schemas, change `sdk_ntfy` to `sdk_push` if present (check OwnerRegisterSchema or similar)

4. **Regenerate Admin types**: Run `pnpm generate:api-types` to regenerate `packages/admin/src/api/types.generated.ts`. This will pick up all schema changes including push_relay_url and sdk_push.

5. **Verify** the generated types include `push_relay_url` and `sdk_push` (not `sdk_ntfy`) in the approval_method enum.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon --filter=@waiaas/admin 2>&1 | tail -5</automated>
  </verify>
  <done>WalletApp API returns push_relay_url, create/update accept it, types.generated.ts has push_relay_url and sdk_push.</done>
</task>

</tasks>

<verification>
1. `grep -c "@deprecated" packages/wallet-sdk/src/channels/ntfy.ts` returns 4
2. `grep "push_relay_url" packages/daemon/src/api/routes/openapi-schemas.ts` shows 3 occurrences (schema, create, update)
3. `grep "push_relay_url" packages/admin/src/api/types.generated.ts` shows the field in WalletApp type
4. `grep "sdk_push" packages/admin/src/api/types.generated.ts` shows sdk_push in approval_method enum
5. `pnpm turbo run typecheck --filter=@waiaas/daemon --filter=@waiaas/admin` passes
</verification>

<success_criteria>
- All 4 ntfy SDK functions have @deprecated JSDoc tags
- Push Relay functions (sendViaRelay, registerDevice, etc.) are NOT deprecated
- WalletApp API response includes push_relay_url field
- Admin types.generated.ts reflects push_relay_url and sdk_push
- TypeScript compilation passes for daemon and admin packages
</success_criteria>

<output>
After completion, create `.planning/phases/451-sdk-deprecated-admin-ui/451-01-SUMMARY.md`
</output>
