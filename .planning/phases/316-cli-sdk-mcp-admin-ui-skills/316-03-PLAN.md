---
phase: 316-cli-sdk-mcp-admin-ui-skills
plan: 03
type: execute
wave: 2
depends_on: [316-01, 316-02]
files_modified:
  - skills/wallet.skill.md
  - skills/quickstart.skill.md
  - skills/admin.skill.md
autonomous: true
requirements: [INT-07, INT-08]

must_haves:
  truths:
    - "wallet.skill.md includes smart account creation guide with curl examples"
    - "wallet.skill.md documents accountType parameter and smart account response fields"
    - "quickstart.skill.md includes Smart Account quickstart section for EVM wallets"
    - "admin.skill.md documents Smart Account (ERC-4337) settings keys in Settings Management section"
    - "All skill files include the security notice about master password"
  artifacts:
    - path: "skills/wallet.skill.md"
      provides: "Smart account creation guide for AI agents"
      contains: "accountType.*smart"
    - path: "skills/quickstart.skill.md"
      provides: "Smart Account quickstart section"
      contains: "Smart Account"
    - path: "skills/admin.skill.md"
      provides: "Smart Account settings documentation in Settings Management"
      contains: "smart_account"
  key_links:
    - from: "skills/wallet.skill.md"
      to: "POST /v1/wallets"
      via: "curl example with accountType"
      pattern: "accountType.*smart"
    - from: "skills/quickstart.skill.md"
      to: "skills/wallet.skill.md"
      via: "cross-reference for full smart account details"
      pattern: "wallet.skill.md"
    - from: "skills/admin.skill.md"
      to: "PUT /v1/admin/settings"
      via: "smart_account settings keys documentation"
      pattern: "smart_account"
---

<objective>
Update wallet.skill.md, quickstart.skill.md, and admin.skill.md with smart account creation guides, examples, settings documentation, and configuration instructions.

Purpose: AI agents need skill file guidance to understand smart account capabilities, creation syntax, and how to identify smart account wallets in responses. Admin skill file must document the new Smart Account settings per CLAUDE.md interface sync rule.
Output: Updated skill files with smart account documentation including admin settings.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/wallet.skill.md
@skills/quickstart.skill.md
@skills/admin.skill.md

<interfaces>
<!-- POST /v1/wallets request with smart account -->
```json
{
  "name": "my-smart-wallet",
  "chain": "ethereum",
  "environment": "testnet",
  "accountType": "smart"
}
```

<!-- Response includes smart account fields -->
```json
{
  "id": "01958f3a-1234-7000-8000-abcdef123456",
  "name": "my-smart-wallet",
  "chain": "ethereum",
  "network": "ethereum-sepolia",
  "environment": "testnet",
  "publicKey": "0x1234...5678",
  "status": "ACTIVE",
  "accountType": "smart",
  "signerKey": "0xABCD...EFGH",
  "deployed": false,
  "createdAt": 1707000000,
  "session": {
    "id": "...",
    "token": "wai_sess_...",
    "expiresAt": 1709592000
  }
}
```

<!-- Key concepts for skill files -->
- accountType: "eoa" (default) or "smart"
- signerKey: EOA signer address (present only for smart accounts)
- deployed: false until first transaction (lazy deployment)
- EVM-only (solana not supported for smart accounts)
- Requires smart_account.enabled=true in Admin Settings
- Requires bundler_url configured
- BATCH transactions are atomic for smart accounts
- Paymaster can sponsor gas fees when paymaster_url configured
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update wallet.skill.md with smart account creation guide</name>
  <files>skills/wallet.skill.md</files>
  <action>
1. In `skills/wallet.skill.md`, in the "1. Wallet CRUD" section, specifically in the "POST /v1/wallets -- Create Wallet (masterAuth)" subsection:
   - Add `accountType` parameter to the Parameters list:
     ```
     - `accountType` (optional): `"eoa"` (default) or `"smart"` -- ERC-4337 Smart Account (EVM chains only)
     ```
   - Add a new subsection after the existing create wallet examples called "### Create Smart Account Wallet (EVM, ERC-4337)":
     ```markdown
     ### Create Smart Account Wallet (EVM, ERC-4337)

     Create an ERC-4337 smart account wallet with Paymaster gas sponsorship and atomic batch support. Requires `smart_account.enabled=true` in Admin Settings and a configured Bundler URL.

     ```bash
     curl -s -X POST http://localhost:3100/v1/wallets \
       -H 'Content-Type: application/json' \
       -H 'X-Master-Password: your-master-password' \
       -d '{"name": "smart-agent", "chain": "ethereum", "environment": "testnet", "accountType": "smart"}'
     ```

     Response (201):
     ```json
     {
       "id": "01958f3a-1234-7000-8000-abcdef123456",
       "name": "smart-agent",
       "chain": "ethereum",
       "network": "ethereum-sepolia",
       "environment": "testnet",
       "publicKey": "0x1234567890abcdef1234567890abcdef12345678",
       "status": "ACTIVE",
       "accountType": "smart",
       "signerKey": "0xabcdef1234567890abcdef1234567890abcdef12",
       "deployed": false,
       "createdAt": 1707000000,
       "session": {
         "id": "01958f3b-5678-7000-8000-abcdef654321",
         "token": "wai_sess_eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
         "expiresAt": 1709592000
       }
     }
     ```

     **Smart Account fields:**
     - `accountType`: `"smart"` -- ERC-4337 smart account
     - `signerKey`: EOA signer (owner) address that controls the smart account
     - `deployed`: `false` until the first transaction (lazy deployment via CREATE2)

     **Key differences from EOA:**
     - Gas can be sponsored by a Paymaster (agent needs no ETH for gas)
     - BATCH transactions execute atomically as a single UserOperation (all-or-nothing)
     - Address is predicted via CREATE2 before on-chain deployment
     - First transaction automatically deploys the smart account contract

     **Requirements:**
     - EVM chains only (ethereum). Solana does not support ERC-4337.
     - `smart_account.enabled` must be `true` in Admin Settings
     - `smart_account.bundler_url` must be configured (e.g., Pimlico, Stackup, Alchemy)
     - Optional: `smart_account.paymaster_url` for gas sponsorship
     ```

2. In the wallet list / wallet detail response documentation (if present), note that responses include `accountType`, `signerKey`, `deployed` fields for all wallets.

3. Add a note in the Wallet CRUD Parameters section that `accountType` is EVM-only:
   ```
   Note: `accountType: "smart"` is only valid for EVM chains (ethereum). Attempting to create a smart account on Solana returns an error.
   ```
  </action>
  <verify>
    <automated>grep -c "accountType" /Users/minho.yoo/dev/wallet/WAIaaS/skills/wallet.skill.md</automated>
  </verify>
  <done>
    - wallet.skill.md includes smart account creation curl example
    - wallet.skill.md documents accountType, signerKey, deployed fields
    - wallet.skill.md explains smart account vs EOA differences
    - wallet.skill.md includes requirements (enabled setting, bundler_url)
    - Security notice present
  </done>
</task>

<task type="auto">
  <name>Task 2: Update quickstart.skill.md with Smart Account quickstart section</name>
  <files>skills/quickstart.skill.md</files>
  <action>
1. In `skills/quickstart.skill.md`, after the "Step 2: Create a Wallet" section (which shows Solana and EVM examples), add a new subsection:

   ```markdown
   **EVM Smart Account (ERC-4337):**

   Create a smart account wallet for gas-free transactions with Paymaster sponsorship. Requires Admin to enable smart account support first (see admin.skill.md).

   ```bash
   curl -s -X POST http://localhost:3100/v1/wallets \
     -H 'Content-Type: application/json' \
     -H 'X-Master-Password: your-master-password' \
     -d '{"name": "my-smart-wallet", "chain": "ethereum", "environment": "mainnet", "accountType": "smart"}'
   ```

   Smart account wallets have `"deployed": false` initially. The smart account contract is deployed automatically with the first transaction (lazy deployment). Once deployed, BATCH transactions execute atomically (all-or-nothing) as a single UserOperation.

   See **wallet.skill.md** section "Create Smart Account Wallet" for full details on smart account fields, requirements, and differences from EOA wallets.
   ```

2. In the existing "Parameters:" section under Step 2, add `accountType` to the parameter list:
   ```
   - `accountType` (optional): `"eoa"` (default) or `"smart"` (ERC-4337, EVM only)
   ```

3. In the "CLI Quickset (Alternative)" section, add a note:
   ```markdown
   For smart account wallets, use the CLI directly:
   ```bash
   waiaas wallet create --chain ethereum --account-type smart --name my-smart-wallet
   ```
   ```

4. Ensure the security notice is present: `> AI agents must NEVER request the master password. Use only your session token.`
  </action>
  <verify>
    <automated>grep -c "Smart Account\|accountType.*smart\|smart.*account" /Users/minho.yoo/dev/wallet/WAIaaS/skills/quickstart.skill.md</automated>
  </verify>
  <done>
    - quickstart.skill.md includes Smart Account creation example in Step 2
    - quickstart.skill.md documents accountType parameter
    - quickstart.skill.md includes CLI alternative for smart account creation
    - quickstart.skill.md cross-references wallet.skill.md for full details
    - Security notice present
  </done>
</task>

<task type="auto">
  <name>Task 3: Update admin.skill.md with Smart Account settings documentation</name>
  <files>skills/admin.skill.md</files>
  <action>
1. In `skills/admin.skill.md`, in the "4. Settings Management" section, in the settings category table (the table listing setting prefixes like `display`, `daemon`, `oracle`, `gas_condition`):
   - Add a new row for `smart_account`:
     ```
     | `smart_account` | enabled, bundler_url, paymaster_url, paymaster_api_key, entry_point | ERC-4337 smart account infrastructure. |
     ```

2. After the existing settings category descriptions, add a "Smart Account (ERC-4337) Settings" subsection:
   ```markdown
   ### Smart Account (ERC-4337) Settings

   Configure ERC-4337 smart account infrastructure for gas-sponsored and atomic batch transactions.

   | Key | Default | Description |
   |-----|---------|-------------|
   | `smart_account.enabled` | `false` | Enable smart account wallet creation (opt-in) |
   | `smart_account.bundler_url` | -- | Bundler RPC endpoint URL (required for smart account transactions) |
   | `smart_account.paymaster_url` | -- | Paymaster RPC endpoint URL (optional, enables gas sponsorship) |
   | `smart_account.paymaster_api_key` | -- | Paymaster API key (stored encrypted, masked in responses) |
   | `smart_account.entry_point` | `0x0000000071727De22E5E9d8BAf0edAc6f37da032` | EntryPoint v0.7 contract address |

   Enable via Admin Settings API:
   ```bash
   curl -s -X PUT http://localhost:3100/v1/admin/settings \
     -H 'Content-Type: application/json' \
     -H 'X-Master-Password: <password>' \
     -d '{"settings": [
       {"key": "smart_account.enabled", "value": "true"},
       {"key": "smart_account.bundler_url", "value": "https://api.pimlico.io/v2/sepolia/rpc?apikey=YOUR_KEY"}
     ]}'
   ```

   Or configure via Admin UI > System > Smart Account (ERC-4337) section.
   ```

3. Ensure security notice is present at the top of the file.
  </action>
  <verify>
    <automated>grep -c "smart_account" /Users/minho.yoo/dev/wallet/WAIaaS/skills/admin.skill.md</automated>
  </verify>
  <done>
    - admin.skill.md documents smart_account settings prefix in category table
    - admin.skill.md includes Smart Account settings key table with descriptions
    - admin.skill.md includes curl example for enabling smart account via settings API
    - Security notice present
  </done>
</task>

</tasks>

<verification>
1. `grep -c "accountType" skills/wallet.skill.md` returns >= 5 (multiple references)
2. `grep -c "Smart Account" skills/quickstart.skill.md` returns >= 2
3. `grep -c "smart_account" skills/admin.skill.md` returns >= 5
4. `grep "NEVER request the master password" skills/wallet.skill.md skills/quickstart.skill.md skills/admin.skill.md` returns matches in all three files
5. No broken markdown formatting (verify with a quick visual scan)
</verification>

<success_criteria>
- wallet.skill.md has complete smart account creation guide with curl example, parameters, response, and requirements
- quickstart.skill.md has Smart Account quickstart section with creation example and CLI alternative
- admin.skill.md documents Smart Account settings keys and includes configuration example
- All three skill files include security notice about master password
- All files cross-reference each other appropriately
- Documentation is accurate and consistent with REST API behavior from Phase 314
</success_criteria>

<output>
After completion, create `.planning/phases/316-cli-sdk-mcp-admin-ui-skills/316-03-SUMMARY.md`
</output>
