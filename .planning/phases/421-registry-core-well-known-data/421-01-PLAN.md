---
phase: 421-registry-core-well-known-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/interfaces/action-provider.types.ts
  - packages/core/src/constants/well-known-contracts.ts
  - packages/core/src/constants/index.ts
  - packages/core/src/index.ts
  - packages/core/src/__tests__/well-known-contracts.test.ts
  - packages/actions/src/providers/jupiter-swap/index.ts
  - packages/actions/src/providers/lifi/index.ts
  - packages/actions/src/providers/zerox-swap/index.ts
  - packages/actions/src/providers/dcent-swap/index.ts
  - packages/actions/src/providers/across/index.ts
  - packages/actions/src/providers/jito-staking/index.ts
  - packages/actions/src/providers/pendle/index.ts
  - packages/actions/src/providers/kamino/index.ts
  - packages/actions/src/providers/drift/index.ts
  - packages/actions/src/providers/erc8004/index.ts
  - packages/actions/src/providers/aave-v3/index.ts
  - packages/actions/src/providers/lido-staking/index.ts
  - packages/actions/src/providers/hyperliquid/spot-provider.ts
  - packages/actions/src/providers/hyperliquid/sub-account-provider.ts
  - packages/actions/src/providers/hyperliquid/perp-provider.ts
  - packages/actions/src/providers/polymarket/order-provider.ts
  - packages/actions/src/providers/polymarket/ctf-provider.ts
autonomous: true
requirements:
  - WKD-01
  - WKD-02
  - WKD-03
  - WKD-04
  - WKD-05
  - APR-01
  - APR-02
  - APR-03

must_haves:
  truths:
    - "Well-known contracts data has 300+ entries across 6 networks"
    - "Each entry has address, name, protocol, network fields"
    - "ActionProviderMetadataSchema accepts optional displayName"
    - "All 17 providers have explicit displayName set"
    - "displayName auto-conversion works for snake_case names"
  artifacts:
    - path: "packages/core/src/constants/well-known-contracts.ts"
      provides: "Static well-known contract data"
      contains: "WELL_KNOWN_CONTRACTS"
    - path: "packages/core/src/interfaces/action-provider.types.ts"
      provides: "displayName field in ActionProviderMetadataSchema"
      contains: "displayName"
  key_links:
    - from: "packages/core/src/constants/well-known-contracts.ts"
      to: "packages/core/src/index.ts"
      via: "re-export from core barrel"
      pattern: "well-known-contracts"
    - from: "packages/core/src/interfaces/action-provider.types.ts"
      to: "packages/actions/src/providers/*/index.ts"
      via: "displayName in metadata"
      pattern: "displayName"
---

<objective>
Well-known contract data와 Action Provider displayName 메타데이터를 정의한다.

Purpose: ContractNameRegistry (Plan 02)의 데이터 소스 2개(well-known 정적 데이터, Action Provider displayName)를 준비한다.
Output: 300+ well-known 엔트리가 담긴 정적 const 파일, displayName이 추가된 ActionProviderMetadataSchema, 17개 프로바이더에 displayName 설정 완료.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/core/src/interfaces/action-provider.types.ts:
```typescript
export const ActionProviderMetadataSchema = z.object({
  name: z.string().regex(/^[a-z][a-z0-9_]*$/).min(3).max(50),
  description: z.string().min(10).max(500),
  version: z.string().regex(/^\d+\.\d+\.\d+$/),
  chains: z.array(ChainTypeEnum).min(1),
  mcpExpose: z.boolean().default(false),
  requiresApiKey: z.boolean().default(false),
  requiredApis: z.array(z.string()).optional().default([]),
  category: z.string().min(1).max(50).optional(),
  enabledKey: z.string().regex(/^[a-z][a-z0-9_]*$/).optional(),
  requiresSigningKey: z.boolean().default(false),
});
export type ActionProviderMetadata = z.infer<typeof ActionProviderMetadataSchema>;
```

From packages/core/src/constants/index.ts:
```typescript
export * from './aa-provider-chains.js';
```

Provider metadata names (17 providers):
- jupiter_swap, lifi, zerox_swap, dcent_swap, across_bridge
- jito_staking, pendle_yield, kamino, drift_perp
- erc8004_agent, aave_v3, lido_staking
- hyperliquid_spot, hyperliquid_sub, hyperliquid_perp
- polymarket_order, polymarket_ctf
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Well-Known Contract Data + ActionProviderMetadata displayName</name>
  <files>
    packages/core/src/constants/well-known-contracts.ts
    packages/core/src/constants/index.ts
    packages/core/src/index.ts
    packages/core/src/interfaces/action-provider.types.ts
    packages/core/src/__tests__/well-known-contracts.test.ts
  </files>
  <behavior>
    - Test: WELL_KNOWN_CONTRACTS array length >= 300
    - Test: Every entry has { address: string, name: string, protocol: string, network: string }
    - Test: Ethereum mainnet entries include Uniswap V3 Router, Aave V3 Pool, Lido stETH, WETH, USDC, USDT, DAI, etc.
    - Test: Base entries include Uniswap, Aerodrome, etc.
    - Test: Arbitrum entries include GMX, Camelot, etc.
    - Test: Optimism entries include Velodrome, etc.
    - Test: Polygon entries include QuickSwap, Aave, etc.
    - Test: Solana entries include Jupiter, Raydium, Marinade, Token Program, System Program, etc.
    - Test: No duplicate (address, network) pairs
    - Test: All EVM addresses are lowercase hex (0x-prefixed, 42 chars)
    - Test: All Solana addresses are base58 strings
    - Test: ActionProviderMetadataSchema accepts displayName as optional string
    - Test: ActionProviderMetadataSchema works without displayName (backward compat)
    - Test: snakeCaseToDisplayName('jupiter_swap') === 'Jupiter Swap'
    - Test: snakeCaseToDisplayName('lifi') === 'Lifi'
    - Test: snakeCaseToDisplayName('aave_v3') === 'Aave V3'
    - Test: snakeCaseToDisplayName('zerox_swap') === 'Zerox Swap'
  </behavior>
  <action>
    1. Add optional `displayName` field to `ActionProviderMetadataSchema` in action-provider.types.ts:
       ```typescript
       displayName: z.string().min(1).max(100).optional(),
       ```
       Add a helper function `snakeCaseToDisplayName(name: string): string` that converts snake_case metadata.name to Title Case (split on _, capitalize each word, join with space). Export it.

    2. Create `packages/core/src/constants/well-known-contracts.ts`:
       - Define `WellKnownContractEntry` interface: `{ address: string; name: string; protocol: string; network: string }`
       - Create `WELL_KNOWN_CONTRACTS: readonly WellKnownContractEntry[]` as a `const` array
       - Organize by network sections with comments
       - Include at minimum these categories per EVM chain:
         * DEX routers (Uniswap V2/V3, SushiSwap, etc.)
         * DEX factories
         * Lending protocols (Aave V3 Pool, Comptroller, etc.)
         * Staking (Lido stETH, wstETH)
         * Bridge contracts (Across SpokePool, etc.)
         * Stablecoins (USDC, USDT, DAI contracts)
         * Wrapped native (WETH, WMATIC, etc.)
         * Governance (ENS Registry, etc.)
         * NFT marketplaces (OpenSea Seaport, Blur, etc.)
       - For Solana mainnet-beta include:
         * System programs (System Program, Token Program, Token-2022 Program, Associated Token Account Program)
         * DEX (Jupiter V6, Raydium AMM/CLMM, Orca Whirlpool)
         * Staking (Marinade, Jito Staking Pool, Lido on Solana)
         * Lending (Kamino, Solend)
         * Perp (Drift Protocol)
         * Infrastructure (Metaplex, SPL Memo, SPL Name Service)
       - All EVM addresses MUST be lowercase (no checksummed mixed-case)
       - Target: 300+ entries total across 6 networks (Ethereum ~100, Base ~40, Arbitrum ~40, Optimism ~30, Polygon ~40, Solana ~50+)
       - Export the array and the type

    3. Update `packages/core/src/constants/index.ts` to re-export from `well-known-contracts.js`
    4. Update `packages/core/src/index.ts` to ensure constants barrel is exported (verify existing export)
    5. Write tests in `packages/core/src/__tests__/well-known-contracts.test.ts`
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core run test -- --run well-known-contracts</automated>
  </verify>
  <done>
    - WELL_KNOWN_CONTRACTS has 300+ entries across 6 networks
    - Each entry has address, name, protocol, network
    - No duplicate (address, network) pairs
    - EVM addresses all lowercase, Solana addresses base58
    - ActionProviderMetadataSchema.displayName optional field works
    - snakeCaseToDisplayName utility exported and tested
  </done>
</task>

<task type="auto">
  <name>Task 2: Set displayName on All 17 Action Providers</name>
  <files>
    packages/actions/src/providers/jupiter-swap/index.ts
    packages/actions/src/providers/lifi/index.ts
    packages/actions/src/providers/zerox-swap/index.ts
    packages/actions/src/providers/dcent-swap/index.ts
    packages/actions/src/providers/across/index.ts
    packages/actions/src/providers/jito-staking/index.ts
    packages/actions/src/providers/pendle/index.ts
    packages/actions/src/providers/kamino/index.ts
    packages/actions/src/providers/drift/index.ts
    packages/actions/src/providers/erc8004/index.ts
    packages/actions/src/providers/aave-v3/index.ts
    packages/actions/src/providers/lido-staking/index.ts
    packages/actions/src/providers/hyperliquid/spot-provider.ts
    packages/actions/src/providers/hyperliquid/sub-account-provider.ts
    packages/actions/src/providers/hyperliquid/perp-provider.ts
    packages/actions/src/providers/polymarket/order-provider.ts
    packages/actions/src/providers/polymarket/ctf-provider.ts
  </files>
  <action>
    Add `displayName` to each provider's metadata object. Use human-readable protocol names:

    | Provider name | displayName |
    |---|---|
    | jupiter_swap | "Jupiter Swap" |
    | lifi | "LI.FI" |
    | zerox_swap | "0x Swap" |
    | dcent_swap | "D'CENT Swap" |
    | across_bridge | "Across Bridge" |
    | jito_staking | "Jito Staking" |
    | pendle_yield | "Pendle Yield" |
    | kamino | "Kamino" |
    | drift_perp | "Drift Perp" |
    | erc8004_agent | "ERC-8004 Agent" |
    | aave_v3 | "Aave V3" |
    | lido_staking | "Lido Staking" |
    | hyperliquid_spot | "Hyperliquid Spot" |
    | hyperliquid_sub | "Hyperliquid Sub-Account" |
    | hyperliquid_perp | "Hyperliquid Perp" |
    | polymarket_order | "Polymarket Order" |
    | polymarket_ctf | "Polymarket CTF" |

    For each provider file, add `displayName: 'Xxx',` to the metadata object, right after the `name` field.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions run test -- --run</automated>
  </verify>
  <done>
    - All 17 provider metadata objects include displayName
    - Existing tests still pass (ActionProviderMetadataSchema validation)
    - displayName values are human-readable brand names
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @waiaas/core run test -- --run` passes (well-known data + metadata schema tests)
- `pnpm --filter @waiaas/actions run test -- --run` passes (all provider tests)
- WELL_KNOWN_CONTRACTS exported from @waiaas/core
- snakeCaseToDisplayName exported from @waiaas/core
</verification>

<success_criteria>
1. well-known-contracts.ts contains 300+ entries across 6 networks (5 EVM + Solana)
2. Each entry has address, name, protocol, network fields
3. ActionProviderMetadataSchema accepts optional displayName
4. All 17 Action Providers have explicit displayName in metadata
5. snakeCaseToDisplayName fallback utility available for providers without displayName
</success_criteria>

<output>
After completion, create `.planning/phases/421-registry-core-well-known-data/421-01-SUMMARY.md`
</output>
