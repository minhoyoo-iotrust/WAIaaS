# Plan 217-01: EvmIncomingSubscriber 감지 전략 + ETH/ERC-20 파싱 + 폴링 명세

## Goal
EvmIncomingSubscriber가 네이티브 ETH + ERC-20 토큰 수신을 빠짐없이 감지하는 전략을 설계 문서로 정의한다.

## Requirements
- MON-03: eth_subscribe(logs) Transfer + watchBlocks(네이티브 ETH) 이중 전략 정의

## Tasks
1. ERC-20 Transfer 이벤트 감지 전략 (eth_subscribe logs / getLogs 폴링)
2. 네이티브 ETH 수신 감지 전략 (watchBlocks + to-주소 필터)
3. getLogs 폴링 커서 관리 (fromBlock/toBlock)
4. token_registry 화이트리스트 필터로 오탐 방지
5. EvmIncomingSubscriber 전체 구조

## Output
설계 문서 76 섹션 4: EVM 수신 감지 전략
