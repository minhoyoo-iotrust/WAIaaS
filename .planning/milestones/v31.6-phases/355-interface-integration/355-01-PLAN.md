---
phase: 355-interface-integration
plan: 01
type: execute
wave: 1
depends_on: [354-01]
files_modified:
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/daemon/src/infrastructure/settings/hot-reload.ts
  - packages/daemon/src/api/routes/connect-info.ts
  - packages/sdk/src/types.ts
  - packages/sdk/src/client.ts
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-06]

must_haves:
  truths:
    - "MCP 도구(across-bridge-quote/execute/status/routes)로 AI 에이전트가 브릿지 기능을 사용할 수 있다"
    - "SDK 메서드(acrossBridgeQuote/Execute/Status/Routes)로 프로그래밍 방식 접근이 가능하다"
    - "Admin Settings에서 across_bridge_* 설정을 런타임으로 변경할 수 있다"
    - "connect-info에 across_bridge capability가 노출된다"
  artifacts:
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "Across bridge 6 setting keys"
      contains: "actions.across_bridge_enabled"
    - path: "packages/daemon/src/api/routes/connect-info.ts"
      provides: "across_bridge capability + prompt line"
      contains: "across_bridge"
    - path: "packages/sdk/src/client.ts"
      provides: "4 Across bridge convenience methods"
      contains: "acrossBridgeQuote"
    - path: "packages/sdk/src/types.ts"
      provides: "Across bridge SDK param types"
      contains: "AcrossBridgeQuoteParams"
  key_links:
    - from: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      to: "packages/daemon/src/lifecycle/daemon.ts"
      via: "settingsService.get('actions.across_bridge_enabled')"
      pattern: "across_bridge_enabled"
    - from: "packages/daemon/src/infrastructure/settings/hot-reload.ts"
      to: "packages/actions/src/index.ts"
      via: "registerBuiltInProviders reload"
      pattern: "across_bridge"
    - from: "packages/sdk/src/client.ts"
      to: "/v1/actions/across_bridge/{action}"
      via: "this.executeAction('across_bridge', ...)"
      pattern: "executeAction.*across_bridge"
---

<objective>
Across Protocol MCP 도구 + SDK 메서드 + Admin Settings 키 등록 + connect-info capability 노출

Purpose: AI 에이전트(MCP)와 개발자(SDK)가 Across 브릿지 기능에 접근할 수 있도록 인터페이스를 제공한다.
Output: setting-keys.ts에 6개 키, hot-reload에 across_bridge 등록, connect-info에 capability, SDK에 4개 메서드
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/353-api-client-bridge-provider/353-02-SUMMARY.md
@.planning/phases/354-status-tracking-daemon-integration/354-01-SUMMARY.md
@internal/design/79-across-protocol-bridge.md

<interfaces>
<!-- MCP: mcpExpose=true in AcrossBridgeActionProvider metadata auto-exposes 4 tools (quote/execute/status/routes). No MCP code needed. -->
<!-- SDK: uses executeAction('across_bridge', action, params) pattern (same as DCent, Hyperliquid) -->

From packages/actions/src/index.ts:
```typescript
export { AcrossBridgeActionProvider } from './providers/across/index.js';
export { ACROSS_DEFAULTS, ACROSS_CHAIN_MAP, getAcrossChainId } from './providers/across/config.js';
export type { AcrossConfig } from './providers/across/config.js';
// registerBuiltInProviders already includes across_bridge entry (key: 'across_bridge', enabledKey: 'actions.across_bridge_enabled')
```

From packages/sdk/src/client.ts (DCent convenience method pattern):
```typescript
async getDcentQuotes(params: DcentQuoteParams): Promise<ExecuteActionResponse> {
  const { network, walletId, ...rest } = params;
  return this.executeAction('dcent_swap', 'get_quotes', { params: rest, network, walletId });
}
```

From packages/daemon/src/api/routes/connect-info.ts (capability pattern):
```typescript
// dcent_swap: check if enabled via settings
if (deps.settingsService.get('actions.dcent_swap_enabled') === 'true') {
  capabilities.push('dcent_swap');
}
// hyperliquid: check if enabled via settings
if (deps.settingsService.get('actions.hyperliquid_enabled') === 'true') {
  capabilities.push('hyperliquid');
}
```

From packages/daemon/src/infrastructure/settings/setting-keys.ts (LI.FI pattern):
```typescript
{ key: 'actions.lifi_enabled', category: 'actions', configPath: 'actions.lifi_enabled', defaultValue: 'true', isCredential: false },
{ key: 'actions.lifi_api_key', category: 'actions', configPath: 'actions.lifi_api_key', defaultValue: '', isCredential: true },
{ key: 'actions.lifi_api_base_url', category: 'actions', configPath: 'actions.lifi_api_base_url', defaultValue: 'https://li.quest/v1', isCredential: false },
```

From packages/daemon/src/infrastructure/settings/hot-reload.ts:
```typescript
const BUILTIN_NAMES = [
  'jupiter_swap', 'zerox_swap', 'lifi',
  'lido_staking', 'jito_staking',
  'aave_v3', 'kamino',
  'pendle_yield', 'drift_perp',
  'dcent_swap',
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Settings 키 등록 + hot-reload + connect-info capability</name>
  <files>packages/daemon/src/infrastructure/settings/setting-keys.ts, packages/daemon/src/infrastructure/settings/hot-reload.ts, packages/daemon/src/api/routes/connect-info.ts</files>
  <action>
1. **setting-keys.ts**: Hyperliquid 설정 키 블록 뒤에 Across Bridge 설정 키 6개를 추가한다. 기존 LI.FI 패턴을 따른다.

```typescript
// --- Across Bridge ---
{ key: 'actions.across_bridge_enabled', category: 'actions', configPath: 'actions.across_bridge_enabled', defaultValue: 'false', isCredential: false },
{ key: 'actions.across_bridge_api_base_url', category: 'actions', configPath: 'actions.across_bridge_api_base_url', defaultValue: 'https://app.across.to/api', isCredential: false },
{ key: 'actions.across_bridge_integrator_id', category: 'actions', configPath: 'actions.across_bridge_integrator_id', defaultValue: '', isCredential: false },
{ key: 'actions.across_bridge_fill_deadline_buffer_sec', category: 'actions', configPath: 'actions.across_bridge_fill_deadline_buffer_sec', defaultValue: '21600', isCredential: false },
{ key: 'actions.across_bridge_default_slippage_pct', category: 'actions', configPath: 'actions.across_bridge_default_slippage_pct', defaultValue: '0.01', isCredential: false },
{ key: 'actions.across_bridge_max_slippage_pct', category: 'actions', configPath: 'actions.across_bridge_max_slippage_pct', defaultValue: '0.03', isCredential: false },
```

참고: `requestTimeoutMs`는 daemon.ts의 `actions/index.ts`에서 이미 `ACROSS_DEFAULTS.requestTimeoutMs` 폴백으로 처리하므로 별도 설정 키 불필요. 단, 이미 actions/index.ts에서 `across_bridge_request_timeout_ms`를 참조하고 있다면 해당 키도 추가:
```typescript
{ key: 'actions.across_bridge_request_timeout_ms', category: 'actions', configPath: 'actions.across_bridge_request_timeout_ms', defaultValue: '10000', isCredential: false },
```
먼저 `packages/actions/src/index.ts`의 registerBuiltInProviders에서 `across_bridge_request_timeout_ms`를 참조하는지 확인하고, 참조하면 7개, 아니면 6개 키를 등록한다.

2. **hot-reload.ts**: `BUILTIN_NAMES` 배열에 `'across_bridge'`를 추가한다. 기존 `'dcent_swap'` 뒤에:
```typescript
'dcent_swap',
'across_bridge',
```
이 때 Hyperliquid는 BUILTIN_NAMES에 포함되지 않아도 되는 이유(별도 daemon lifecycle 관리)를 참고하여, across_bridge는 registerBuiltInProviders에 이미 등록되어 있으므로 BUILTIN_NAMES에 포함해야 hot-reload 시 재등록된다.

3. **connect-info.ts**: hyperliquid capability 체크 블록(약 376-384행) 뒤에 across_bridge capability를 추가한다:
```typescript
// across_bridge: check if enabled via settings
if (deps.settingsService) {
  try {
    if (deps.settingsService.get('actions.across_bridge_enabled') === 'true') {
      capabilities.push('across_bridge');
    }
  } catch {
    // Setting not found -- across_bridge not available
  }
}
```

4. **connect-info.ts prompt**: `buildConnectInfoPrompt` 함수에서 hyperliquid 프롬프트 라인(약 162-165행) 뒤에 across_bridge 프롬프트를 추가:
```typescript
if (capabilities.includes('across_bridge')) {
  lines.push('Across Bridge: Use action_across_bridge_* tools for intent-based cross-chain EVM bridge with fast relayer fills (2-10 seconds). Supports Ethereum, Arbitrum, Optimism, Base, Polygon, Linea.');
}
```
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -5</automated>
  </verify>
  <done>setting-keys.ts에 across_bridge 6-7개 키, hot-reload BUILTIN_NAMES에 'across_bridge', connect-info에 across_bridge capability + prompt 라인 추가 완료. typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 2: SDK 4개 Across bridge 메서드 추가</name>
  <files>packages/sdk/src/types.ts, packages/sdk/src/client.ts</files>
  <action>
1. **types.ts**: 파일 끝(또는 DCent/Hyperliquid params 근처)에 Across bridge 파라미터 타입 4개 추가:

```typescript
// --- Across Bridge params ---

export interface AcrossBridgeQuoteParams {
  fromChain: string;
  toChain: string;
  inputToken: string;
  outputToken: string;
  amount: string;
  recipient?: string;
  walletId?: string;
  network?: string;
}

export interface AcrossBridgeExecuteParams {
  fromChain: string;
  toChain: string;
  inputToken: string;
  outputToken: string;
  amount: string;
  recipient?: string;
  slippage?: number;
  walletId?: string;
  network?: string;
  gasCondition?: Record<string, unknown>;
}

export interface AcrossBridgeStatusParams {
  depositTxHash: string;
  originChainId?: number;
  walletId?: string;
  network?: string;
}

export interface AcrossBridgeRoutesParams {
  fromChain?: string;
  toChain?: string;
  inputToken?: string;
  outputToken?: string;
  walletId?: string;
  network?: string;
}
```

2. **client.ts**: types.ts에서 새 타입 4개를 import에 추가한다. Hyperliquid 메서드 블록(약 1155행) 뒤에 Across bridge 메서드 4개를 추가:

```typescript
// --- Across Bridge convenience methods ---

/** Get Across bridge quote (fees, limits, estimated fill time). */
async acrossBridgeQuote(params: AcrossBridgeQuoteParams): Promise<ExecuteActionResponse> {
  const { walletId, network, ...rest } = params;
  return this.executeAction('across_bridge', 'quote', { params: rest, walletId, network });
}

/** Execute Across bridge (approve+depositV3 BATCH for ERC-20 or msg.value for native ETH). */
async acrossBridgeExecute(params: AcrossBridgeExecuteParams): Promise<ExecuteActionResponse> {
  const { walletId, network, gasCondition, ...rest } = params;
  return this.executeAction('across_bridge', 'execute', { params: rest, walletId, network, gasCondition });
}

/** Check Across bridge deposit status (filled/pending/expired/refunded). */
async acrossBridgeStatus(params: AcrossBridgeStatusParams): Promise<ExecuteActionResponse> {
  const { walletId, network, ...rest } = params;
  return this.executeAction('across_bridge', 'status', { params: rest, walletId, network });
}

/** List available Across bridge routes (supported chain/token combinations). */
async acrossBridgeRoutes(params?: AcrossBridgeRoutesParams): Promise<ExecuteActionResponse> {
  const { walletId, network, ...rest } = params ?? {};
  return this.executeAction('across_bridge', 'routes', { params: rest, walletId, network });
}
```

3. **client.ts JSDoc**: 파일 선두의 JSDoc 주석(약 1-22행)을 업데이트하여 메서드 수와 across bridge 메서드를 추가:
- 메서드 수 카운트를 +4 증가
- `acrossBridgeQuote(), acrossBridgeExecute(), acrossBridgeStatus(), acrossBridgeRoutes() (Across bridge)` 라인 추가
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/sdk 2>&1 | tail -5</automated>
  </verify>
  <done>SDK types.ts에 4개 파라미터 타입, client.ts에 4개 convenience 메서드 추가 완료. typecheck 통과.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/daemon` -- PASS
2. `pnpm turbo run typecheck --filter=@waiaas/sdk` -- PASS
3. `grep -c 'across_bridge' packages/daemon/src/infrastructure/settings/setting-keys.ts` >= 6
4. `grep 'across_bridge' packages/daemon/src/api/routes/connect-info.ts` returns capability line
5. `grep 'acrossBridgeQuote' packages/sdk/src/client.ts` returns method definition
6. `grep 'across_bridge' packages/daemon/src/infrastructure/settings/hot-reload.ts` returns BUILTIN_NAMES entry
</verification>

<success_criteria>
- setting-keys.ts에 actions.across_bridge_* 6-7개 설정 키가 등록되어 Admin Settings API로 런타임 설정 가능
- hot-reload.ts BUILTIN_NAMES에 'across_bridge'가 포함되어 설정 변경 시 프로바이더 재등록
- connect-info에 across_bridge capability가 노출되어 에이전트 자기 발견 가능
- SDK에 acrossBridgeQuote/Execute/Status/Routes 4개 메서드가 추가되어 프로그래밍 접근 가능
- MCP 도구는 mcpExpose=true 설정으로 자동 노출 (별도 작업 불필요)
- 모든 패키지 typecheck 통과
</success_criteria>

<output>
After completion, create `.planning/phases/355-interface-integration/355-01-SUMMARY.md`
</output>
