---
phase: 355-interface-integration
plan: 02
type: execute
wave: 1
depends_on: [354-01]
files_modified:
  - packages/admin/src/pages/actions.tsx
  - packages/admin/src/utils/settings-helpers.ts
  - skills/actions.skill.md
autonomous: true
requirements: [INT-04, INT-05]

must_haves:
  truths:
    - "Admin UI DeFi/Actions 페이지에서 Across Bridge 프로바이더가 Bridge 카테고리에 표시된다"
    - "Admin Settings에서 across_bridge 설정 라벨이 사용자 친화적으로 표시된다"
    - "actions.skill.md에 Across Bridge 도구 설명이 포함되어 있다"
  artifacts:
    - path: "packages/admin/src/pages/actions.tsx"
      provides: "Across Bridge BUILTIN_PROVIDERS entry"
      contains: "across_bridge"
    - path: "packages/admin/src/utils/settings-helpers.ts"
      provides: "Across bridge setting labels"
      contains: "across_bridge"
    - path: "skills/actions.skill.md"
      provides: "Across Bridge tool documentation"
      contains: "Across"
  key_links:
    - from: "packages/admin/src/pages/actions.tsx"
      to: "/v1/actions/providers API"
      via: "BUILTIN_PROVIDERS array"
      pattern: "across_bridge"
    - from: "skills/actions.skill.md"
      to: "packages/actions/src/providers/across/index.ts"
      via: "Action tool documentation"
      pattern: "across-bridge"
---

<objective>
Admin UI Across Bridge 프로바이더 표시 + Settings 라벨 + Skill File 업데이트

Purpose: 관리자가 Admin UI에서 Across 브릿지 프로바이더를 확인하고 설정할 수 있으며, AI 에이전트 skill 문서가 Across 기능을 반영한다.
Output: actions.tsx에 BUILTIN_PROVIDERS entry, settings-helpers.ts에 라벨, actions.skill.md에 Across 문서
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/353-api-client-bridge-provider/353-02-SUMMARY.md
@internal/design/79-across-protocol-bridge.md

<interfaces>
From packages/admin/src/pages/actions.tsx (BUILTIN_PROVIDERS pattern):
```typescript
type ProviderCategory = 'Swap' | 'Bridge' | 'Staking' | 'Lending' | 'Yield' | 'Perp';

const BUILTIN_PROVIDERS: BuiltinProvider[] = [
  // ...
  { key: 'lifi', name: 'LI.FI', description: 'Multi-chain DEX/bridge aggregator', chain: 'multi', category: 'Bridge', requiresApiKey: false, docsUrl: 'https://docs.li.fi' },
  // ...
];

const CATEGORY_ORDER: ProviderCategory[] = ['Swap', 'Bridge', 'Staking', 'Lending', 'Yield', 'Perp'];
```

From packages/admin/src/utils/settings-helpers.ts (label map pattern):
```typescript
const map: Record<string, string> = {
  // ...
  aave_v3_enabled: 'Aave V3 Enabled',
  aave_v3_health_factor_warning_threshold: 'HF Warning Threshold',
  // ...
};
```

From skills/actions.skill.md (existing providers documented):
```
Action Provider framework for executing DeFi protocol actions (swaps, staking, liquidity, cross-chain bridges) through the WAIaaS transaction pipeline.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin UI BUILTIN_PROVIDERS + Settings 라벨 추가</name>
  <files>packages/admin/src/pages/actions.tsx, packages/admin/src/utils/settings-helpers.ts</files>
  <action>
1. **actions.tsx**: `BUILTIN_PROVIDERS` 배열에서 `lifi` 항목 뒤에 Across Bridge 항목을 추가한다:

```typescript
{ key: 'across_bridge', name: 'Across Bridge', description: 'Intent-based cross-chain EVM bridge with fast relayer fills (2-10 sec)', chain: 'evm', category: 'Bridge', requiresApiKey: false, docsUrl: 'https://docs.across.to' },
```

참고: chain은 'evm' (EVM 체인 간 브릿지만 지원). category는 'Bridge' (LI.FI와 동일 카테고리).

2. **settings-helpers.ts**: `getSettingLabel` 함수의 `map` 객체에 Across bridge 라벨을 추가한다. kamino 또는 drift 라벨 뒤에:

```typescript
// across_bridge keys
across_bridge_enabled: 'Across Bridge Enabled',
across_bridge_api_base_url: 'API Base URL',
across_bridge_integrator_id: 'Integrator ID',
across_bridge_fill_deadline_buffer_sec: 'Fill Deadline Buffer (seconds)',
across_bridge_default_slippage_pct: 'Default Slippage (%)',
across_bridge_max_slippage_pct: 'Max Slippage (%)',
across_bridge_request_timeout_ms: 'Request Timeout (ms)',
```

355-01에서 request_timeout_ms 키를 추가했으면 7개 라벨, 아니면 6개. 355-01에서 추가한 키 수에 맞춘다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/admin 2>&1 | tail -5</automated>
  </verify>
  <done>Admin UI actions.tsx에 Across Bridge BUILTIN_PROVIDERS entry, settings-helpers.ts에 라벨 매핑 추가. typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 2: actions.skill.md에 Across Bridge 도구 문서 추가</name>
  <files>skills/actions.skill.md</files>
  <action>
1. **actions.skill.md**: 기존 LI.FI 브릿지 도구 섹션(또는 파일 끝 DeFi 프로바이더 목록) 근처에 Across Bridge 섹션을 추가한다. 기존 DCent Swap 또는 Hyperliquid 패턴을 따른다.

추가할 내용 (tags에 `across` 추가, 본문에 Across Bridge 섹션):

```markdown
## N. Across Bridge (Cross-chain EVM Bridge)

Intent-based cross-chain bridge with fast relayer fills (2-10 seconds). Supports Ethereum, Arbitrum, Optimism, Base, Polygon, Linea.

### Prerequisites (Admin)
- Enable via Admin Settings: `actions.across_bridge_enabled = true`
- Add SpokePool addresses to CONTRACT_WHITELIST for each chain

### Available Actions

#### across_bridge / quote -- Get Bridge Quote
```bash
curl -s -X POST http://localhost:3100/v1/actions/across_bridge/quote \
  -H 'Authorization: Bearer wai_sess_eyJ...' \
  -H 'Content-Type: application/json' \
  -d '{"params":{"fromChain":"ethereum","toChain":"arbitrum","inputToken":"0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48","outputToken":"0xaf88d065e77c8cC2239327C5EDb3A432268e5831","amount":"1000000000"}}'
```

Returns: inputAmount, outputAmount, totalFee, feeBreakdown (lpFee, relayerCapitalFee, relayerGasFee), estimatedFillTimeSec, limits.

#### across_bridge / execute -- Execute Cross-chain Bridge
```bash
curl -s -X POST http://localhost:3100/v1/actions/across_bridge/execute \
  -H 'Authorization: Bearer wai_sess_eyJ...' \
  -H 'Content-Type: application/json' \
  -d '{"params":{"fromChain":"ethereum","toChain":"arbitrum","inputToken":"0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48","outputToken":"0xaf88d065e77c8cC2239327C5EDb3A432268e5831","amount":"1000000000"}}'
```

Executes approve+depositV3 BATCH (ERC-20) or single depositV3 with msg.value (native ETH). Bridge status tracked automatically via 2-phase polling.

#### across_bridge / status -- Check Bridge Status
```bash
curl -s -X POST http://localhost:3100/v1/actions/across_bridge/status \
  -H 'Authorization: Bearer wai_sess_eyJ...' \
  -H 'Content-Type: application/json' \
  -d '{"params":{"depositTxHash":"0xabc..."}}'
```

Returns: status (filled/pending/expired/refunded), fillTxHash, depositId.

#### across_bridge / routes -- List Available Routes
```bash
curl -s -X POST http://localhost:3100/v1/actions/across_bridge/routes \
  -H 'Authorization: Bearer wai_sess_eyJ...' \
  -H 'Content-Type: application/json' \
  -d '{"params":{}}'
```

Returns supported chain/token route combinations.

### Admin Settings
| Key | Default | Description |
|-----|---------|-------------|
| `actions.across_bridge_enabled` | `false` | Enable Across Bridge |
| `actions.across_bridge_api_base_url` | `https://app.across.to/api` | API base URL |
| `actions.across_bridge_integrator_id` | (empty) | Integrator ID |
| `actions.across_bridge_fill_deadline_buffer_sec` | `21600` | Fill deadline buffer (6h) |
| `actions.across_bridge_default_slippage_pct` | `0.01` | Default slippage (1%) |
| `actions.across_bridge_max_slippage_pct` | `0.03` | Max slippage (3%) |
```

2. 파일 상단 tags에 `across` 추가 (기존 tags 배열에).

3. version을 현재 릴리스와 맞춤 (기존 값 유지 또는 v31.6 반영).

**CLAUDE.md 준수**: skill 파일에 보안 경고 포함 확인: `> AI agents must NEVER request the master password. Use only your session token.`
  </action>
  <verify>
    <automated>grep -c "across_bridge" /Users/minho.yoo/dev/wallet/WAIaaS/skills/actions.skill.md</automated>
  </verify>
  <done>actions.skill.md에 Across Bridge 4개 action 문서, Admin Settings 테이블, Prerequisites 추가. 보안 경고 포함.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/admin` -- PASS
2. `grep 'across_bridge' packages/admin/src/pages/actions.tsx` returns BUILTIN_PROVIDERS entry
3. `grep 'across_bridge' packages/admin/src/utils/settings-helpers.ts` returns label entries
4. `grep -c 'across_bridge' skills/actions.skill.md` >= 5
5. `grep 'AI agents must NEVER' skills/actions.skill.md` returns security notice
</verification>

<success_criteria>
- Admin UI Actions 페이지에 Across Bridge가 Bridge 카테고리로 표시된다
- Settings 페이지에서 across_bridge_* 설정이 사용자 친화적 라벨로 표시된다
- actions.skill.md에 Across Bridge 4개 action의 curl 예제와 Admin Settings 테이블이 문서화되어 있다
- 보안 경고 문구가 skill 파일에 포함되어 있다
</success_criteria>

<output>
After completion, create `.planning/phases/355-interface-integration/355-02-SUMMARY.md`
</output>
