---
phase: 353-api-client-bridge-provider
plan: 02
type: execute
wave: 2
depends_on: ["353-01"]
files_modified:
  - packages/actions/src/providers/across/index.ts
  - packages/actions/src/index.ts
autonomous: true
requirements:
  - BRG-01
  - BRG-02
  - BRG-03
  - BRG-04
  - BRG-05
  - BRG-06
  - BRG-07
must_haves:
  truths:
    - "사용자가 quote action으로 브릿지 견적(수수료, 수령액, 예상 fill 시간, 한도)을 조회할 수 있다"
    - "사용자가 execute action으로 ERC-20 토큰을 approve+depositV3 BATCH로 크로스체인 전송할 수 있다"
    - "사용자가 execute action으로 네이티브 ETH를 msg.value로 크로스체인 전송할 수 있다"
    - "지원하지 않는 route 요청 시 UNSUPPORTED_ROUTE ChainError가 반환된다"
    - "유동성 부족(isAmountTooLow) 시 AMOUNT_TOO_LOW ChainError가 반환된다"
    - "Stage 5 실행 직전에 fresh quote를 재조회하여 stale quote를 방지한다 (late-bind)"
    - "outputAmount이 inputAmount - totalRelayFee.total로 정확히 계산된다"
  artifacts:
    - path: "packages/actions/src/providers/across/index.ts"
      provides: "AcrossBridgeActionProvider with 5 actions (quote, execute, status, routes, limits)"
      exports: ["AcrossBridgeActionProvider"]
      min_lines: 150
    - path: "packages/actions/src/index.ts"
      provides: "Across provider registration in ACTION_PROVIDER_REGISTRY"
      contains: "across_bridge"
  key_links:
    - from: "packages/actions/src/providers/across/index.ts"
      to: "packages/actions/src/providers/across/across-api-client.ts"
      via: "AcrossApiClient instantiation for API calls"
      pattern: "new AcrossApiClient"
    - from: "packages/actions/src/providers/across/index.ts"
      to: "packages/actions/src/providers/across/config.ts"
      via: "getAcrossChainId, getSpokePoolAddress, isNativeTokenBridge"
      pattern: "getAcrossChainId|getSpokePoolAddress|isNativeTokenBridge"
    - from: "packages/actions/src/providers/across/index.ts"
      to: "@waiaas/core"
      via: "IActionProvider interface + ContractCallRequest type + viem encodeFunctionData"
      pattern: "implements IActionProvider"
    - from: "packages/actions/src/index.ts"
      to: "packages/actions/src/providers/across/index.ts"
      via: "ACTION_PROVIDER_REGISTRY factory entry"
      pattern: "key: 'across_bridge'"
---

<objective>
AcrossBridgeActionProvider 5 actions 구현 + ACTION_PROVIDER_REGISTRY 등록

Purpose: AI 에이전트가 Across Protocol을 통해 ERC-20 토큰과 네이티브 ETH를 크로스체인 브릿지할 수 있게 한다. quote/execute/status/routes/limits 5개 action을 제공한다.
Output: packages/actions/src/providers/across/index.ts (Provider), packages/actions/src/index.ts (Registry 등록)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/design/79-across-protocol-bridge.md
@.planning/phases/353-api-client-bridge-provider/353-01-SUMMARY.md

Reference files:
@packages/actions/src/providers/lifi/index.ts
@packages/actions/src/index.ts
@packages/core/src/interfaces/action-provider.types.ts
</context>

<interfaces>
<!-- Key types from 353-01 that this plan uses -->

From packages/actions/src/providers/across/across-api-client.ts (created in 353-01):
```typescript
export class AcrossApiClient extends ActionApiClient {
  constructor(config: AcrossConfig);
  async getSuggestedFees(params): Promise<AcrossSuggestedFeesResponse>;
  async getLimits(params): Promise<AcrossLimitsResponse>;
  async getAvailableRoutes(params?): Promise<AcrossAvailableRoutesResponse>;
  async getDepositStatus(params): Promise<AcrossDepositStatusResponse>;
  async getSwapApproval(params): Promise<AcrossSwapApprovalResponse>;
}
```

From packages/actions/src/providers/across/config.ts (created in 353-01):
```typescript
export interface AcrossConfig {
  enabled: boolean;
  apiBaseUrl: string;
  integratorId: string;
  fillDeadlineBufferSec: number;
  defaultSlippagePct: number;
  maxSlippagePct: number;
  requestTimeoutMs: number;
}
export const ACROSS_DEFAULTS: AcrossConfig;
export const ACROSS_CHAIN_MAP: ReadonlyMap<string, number>;
export function getAcrossChainId(chain: string): number;
export const SPOKE_POOL_ADDRESSES: ReadonlyMap<number, string>;
export const WETH_ADDRESSES: ReadonlyMap<number, string>;
export function getSpokePoolAddress(chainId: number): string;
export function getWethAddress(chainId: number): string;
export function isNativeTokenBridge(inputToken: string, chainId: number): boolean;
```

From packages/core/src/interfaces/action-provider.types.ts:
```typescript
export interface IActionProvider {
  readonly metadata: ActionProviderMetadata;
  readonly actions: readonly ActionDefinition[];
  resolve(actionName: string, params: Record<string, unknown>, context: ActionContext): Promise<ContractCallRequest | ContractCallRequest[] | Record<string, unknown>>;
}
```

From packages/core/src/schemas/transaction.schema.ts:
```typescript
export type ContractCallRequest = {
  type: 'CONTRACT_CALL';
  to: string;
  calldata?: string;
  value?: string;
  chainId?: number;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: AcrossBridgeActionProvider 5 actions 구현</name>
  <files>packages/actions/src/providers/across/index.ts</files>
  <action>
`packages/actions/src/providers/across/index.ts` 생성 -- 설계 문서 79 섹션 8 전체를 구현. LiFiActionProvider 패턴 참조.

**1. Import + Input Schemas (Zod)**:
- z, ChainError, IActionProvider/ActionProviderMetadata/ActionDefinition/ActionContext/ContractCallRequest from @waiaas/core
- AcrossApiClient, AcrossConfig/ACROSS_DEFAULTS/getAcrossChainId/getSpokePoolAddress/isNativeTokenBridge/getWethAddress from 353-01 파일들
- viem의 `encodeFunctionData` (기존 의존성)
- 5개 InputSchema 정의 -- 설계 문서 79 섹션 8.3 그대로:
  - AcrossQuoteInputSchema: fromChain, toChain, inputToken, outputToken, amount, recipient?
  - AcrossExecuteInputSchema: fromChain, toChain, inputToken, outputToken, amount, recipient?, slippage?
  - AcrossStatusInputSchema: depositTxHash, originChainId?
  - AcrossRoutesInputSchema: fromChain?, toChain?, inputToken?, outputToken?
  - AcrossLimitsInputSchema: fromChain, toChain, inputToken, outputToken

**2. SPOKE_POOL_DEPOSIT_V3_ABI** -- 설계 문서 79 섹션 4.3의 ABI fragment 그대로. 12 inputs (address,address,address,address,uint256,uint256,uint256,address,uint32,uint32,uint32,bytes), stateMutability 'payable'.

**3. ERC20_APPROVE_ABI** -- 표준 ERC-20 approve(address,uint256) ABI fragment.

**4. class AcrossBridgeActionProvider implements IActionProvider**:

metadata -- 설계 문서 79 섹션 8.1:
- name: 'across_bridge', description: 'Across Protocol intent-based cross-chain bridge...', version: '1.0.0'
- chains: ['ethereum'], mcpExpose: true, requiresApiKey: false, requiredApis: ['across'], requiresSigningKey: false

actions -- 설계 문서 79 섹션 8.2의 5개 action 정의 (quote/execute/status/routes/limits). riskLevel과 defaultTier 그대로.

constructor(config?: Partial<AcrossConfig>) -- merge with ACROSS_DEFAULTS.

**5. resolve() 메서드** -- 설계 문서 79 섹션 8.4의 switch 분기:
- quote -> resolveQuote
- execute -> resolveExecute
- status -> resolveStatus
- routes -> resolveRoutes
- limits -> resolveLimits

**6. resolveQuote** (설계 문서 79 섹션 9.2 AcrossBridgeQuoteResult 형식으로 반환):
- Parse AcrossQuoteInputSchema
- getAcrossChainId for fromChain/toChain
- apiClient.getSuggestedFees() 호출
- outputAmount = BigInt(amount) - BigInt(fees.totalRelayFee.total)
- fillDeadline 계산 (calculateFillDeadline 헬퍼)
- Record<string,unknown> 반환: inputAmount, outputAmount, totalFee, feeBreakdown(lpFee/relayerCapitalFee/relayerGasFee), estimatedFillTimeSec, fillDeadline, limits(minDeposit/maxDeposit/maxDepositInstant)

**7. resolveExecute** (설계 문서 79 섹션 8.5 그대로):
- Parse AcrossExecuteInputSchema
- getAcrossChainId for fromChain/toChain
- apiClient.getSuggestedFees() (fresh quote, no caching -- DS-04 late-bind)
- validateBridgeParams(inputAmount, outputAmount, fees) -- 설계 문서 79 섹션 5.3의 4개 검증
- calculateFillDeadline + resolveExclusivity (설계 문서 79 섹션 7)
- fillDeadline > nowSec 검증 (Pitfall 3)
- isNativeTokenBridge 판정
- encodeFunctionData로 depositV3 calldata 인코딩 (12 args)
- Native: 단일 CONTRACT_CALL with value=amount, to=spokePool
- ERC-20: approve CONTRACT_CALL + depositV3 CONTRACT_CALL 배열 반환 (BATCH)
  - approve(spokePoolAddress, inputAmount) -- MaxUint256 금지 (Pitfall 10)
  - depositV3 with value 없음

**8. resolveStatus**:
- Parse AcrossStatusInputSchema
- apiClient.getDepositStatus() 호출
- 결과를 Record<string,unknown>으로 반환 (status, fillTxHash, depositId, destinationChainId)

**9. resolveRoutes**:
- Parse AcrossRoutesInputSchema
- Optional fromChain/toChain을 getAcrossChainId로 변환 (있을 때만)
- apiClient.getAvailableRoutes() 호출
- 배열 결과 반환

**10. resolveLimits**:
- Parse AcrossLimitsInputSchema
- getAcrossChainId 변환
- apiClient.getLimits() 호출
- Record<string,unknown> 반환

**Helper functions** (private 또는 module-level):
- `validateBridgeParams(inputAmount, outputAmount, fees)` -- 설계 문서 79 섹션 5.3
- `calculateFillDeadline(fees, config)` -- 설계 문서 79 섹션 7.1
- `resolveExclusivity(fees)` -- 설계 문서 79 섹션 7.2
- `clampSlippage(input, config)` -- LiFi 패턴 참조

**주의사항**:
- ContractCallRequest의 calldata 필드명은 `calldata` (not `data`)
- value는 decimal string (BigInt(x).toString())
- resolve()의 반환 타입: quote/status/routes/limits는 Record<string,unknown>, execute는 ContractCallRequest[]
- 시간 단위: Math.floor(Date.now() / 1000) for uint32 seconds
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit packages/actions/src/providers/across/index.ts 2>&1 | head -20</automated>
  </verify>
  <done>AcrossBridgeActionProvider가 IActionProvider를 구현하고, 5개 action의 resolve 분기가 동작하며, execute action이 ERC-20 approve+deposit BATCH와 native ETH msg.value를 올바르게 생성한다</done>
</task>

<task type="auto">
  <name>Task 2: ACTION_PROVIDER_REGISTRY에 across_bridge 등록</name>
  <files>packages/actions/src/index.ts</files>
  <action>
packages/actions/src/index.ts를 수정하여 AcrossBridgeActionProvider를 등록한다. 기존 LiFi 등록 패턴을 따른다.

1. 파일 상단 import 추가:
```typescript
import { AcrossBridgeActionProvider } from './providers/across/index.js';
```

2. Export 섹션에 추가 (LiFi exports 근처):
```typescript
export { AcrossBridgeActionProvider } from './providers/across/index.js';
export { ACROSS_DEFAULTS, ACROSS_CHAIN_MAP, getAcrossChainId } from './providers/across/config.js';
export type { AcrossConfig } from './providers/across/config.js';
export { AcrossApiClient } from './providers/across/across-api-client.js';
```

3. ACTION_PROVIDER_REGISTRY 배열에 entry 추가 (LiFi entry 바로 뒤):
```typescript
{
  key: 'across_bridge',
  enabledKey: 'actions.across_bridge_enabled',
  factory: () => {
    const config: import('./providers/across/config.js').AcrossConfig = {
      enabled: true,
      apiBaseUrl: settingsReader.get('actions.across_bridge_api_base_url'),
      integratorId: settingsReader.get('actions.across_bridge_integrator_id'),
      fillDeadlineBufferSec: Number(settingsReader.get('actions.across_bridge_fill_deadline_buffer_sec')),
      defaultSlippagePct: Number(settingsReader.get('actions.across_bridge_default_slippage_pct')),
      maxSlippagePct: Number(settingsReader.get('actions.across_bridge_max_slippage_pct')),
      requestTimeoutMs: Number(settingsReader.get('actions.across_bridge_request_timeout_ms')),
    };
    return new AcrossBridgeActionProvider(config);
  },
},
```

주의: settingsReader.get() 키는 Admin Settings에서 Phase 355에서 등록될 예정이므로, 지금은 ACROSS_DEFAULTS fallback이 동작하도록 config spreading 방식 사용. settingsReader가 빈 문자열 반환 시 기본값으로 fallback하도록 처리 (예: `settingsReader.get('...') || ACROSS_DEFAULTS.apiBaseUrl`).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit packages/actions/src/index.ts 2>&1 | head -20</automated>
  </verify>
  <done>packages/actions/src/index.ts에 AcrossBridgeActionProvider가 import/export되고, ACTION_PROVIDER_REGISTRY에 across_bridge entry가 등록되어, Admin Settings에서 actions.across_bridge_enabled=true 시 Provider가 활성화된다</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 에서 packages/actions/src/ 내 across 관련 파일 모두 에러 없이 컴파일
- packages/actions/src/providers/across/index.ts에서 AcrossBridgeActionProvider가 IActionProvider를 구현
- packages/actions/src/index.ts에서 across_bridge가 ACTION_PROVIDER_REGISTRY에 등록됨
- resolveExecute가 ERC-20일 때 2개 ContractCallRequest (approve + depositV3), native일 때 1개 (depositV3 with value) 반환
- outputAmount = inputAmount - totalRelayFee.total 공식이 정확히 적용됨
</verification>

<success_criteria>
- AcrossBridgeActionProvider가 5개 action (quote, execute, status, routes, limits)을 제공한다
- execute action이 ERC-20 approve+depositV3 BATCH와 네이티브 ETH msg.value 브릿지를 모두 지원한다
- outputAmount 계산이 설계 문서 DS-07 공식을 정확히 따른다
- fillDeadline/exclusivity 로직이 설계 문서 섹션 7을 따른다
- ACTION_PROVIDER_REGISTRY에 across_bridge로 등록되어 Admin Settings로 활성화 가능하다
- 지원하지 않는 route, liquidity 부족 등 에러 케이스에서 명확한 ChainError를 반환한다
</success_criteria>

<output>
After completion, create `.planning/phases/353-api-client-bridge-provider/353-02-SUMMARY.md`
</output>
