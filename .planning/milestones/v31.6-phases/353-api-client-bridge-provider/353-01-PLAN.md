---
phase: 353-api-client-bridge-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/across/schemas.ts
  - packages/actions/src/providers/across/config.ts
  - packages/actions/src/providers/across/across-api-client.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
must_haves:
  truths:
    - "AcrossApiClient.getSuggestedFees()가 /suggested-fees 엔드포인트를 호출하여 견적(수수료, 한도, 예상 fill 시간)을 반환한다"
    - "AcrossApiClient.getAvailableRoutes()가 /available-routes를 호출하여 지원 라우트 목록을 반환한다"
    - "AcrossApiClient.getLimits()가 /limits를 호출하여 min/max 금액을 반환한다"
    - "AcrossApiClient.getDepositStatus()가 /deposit/status를 호출하여 deposit 상태를 반환한다"
    - "AcrossApiClient.getSwapApproval()가 /swap/approval을 호출하여 approval+bridge TX calldata를 반환한다"
    - "getSuggestedFees는 캐싱 없이 매번 fresh quote를 요청한다"
  artifacts:
    - path: "packages/actions/src/providers/across/schemas.ts"
      provides: "Zod schemas for all 5 Across API endpoints"
      exports: ["AcrossSuggestedFeesResponseSchema", "AcrossLimitsResponseSchema", "AcrossAvailableRoutesResponseSchema", "AcrossDepositStatusResponseSchema", "AcrossSwapApprovalResponseSchema"]
    - path: "packages/actions/src/providers/across/config.ts"
      provides: "AcrossConfig type, ACROSS_DEFAULTS, ACROSS_CHAIN_MAP, SPOKE_POOL_ADDRESSES, WETH_ADDRESSES, getAcrossChainId, getSpokePoolAddress, isNativeTokenBridge"
      exports: ["AcrossConfig", "ACROSS_DEFAULTS", "ACROSS_CHAIN_MAP", "getAcrossChainId", "SPOKE_POOL_ADDRESSES", "WETH_ADDRESSES", "getSpokePoolAddress", "isNativeTokenBridge"]
    - path: "packages/actions/src/providers/across/across-api-client.ts"
      provides: "HTTP client wrapping 5 Across REST API endpoints"
      exports: ["AcrossApiClient"]
  key_links:
    - from: "packages/actions/src/providers/across/across-api-client.ts"
      to: "packages/actions/src/common/action-api-client.ts"
      via: "extends ActionApiClient"
      pattern: "class AcrossApiClient extends ActionApiClient"
    - from: "packages/actions/src/providers/across/across-api-client.ts"
      to: "packages/actions/src/providers/across/schemas.ts"
      via: "Zod schema validation on API responses"
      pattern: "this\\.get\\(.*Schema"
---

<objective>
AcrossApiClient 5개 엔드포인트 래핑 + Zod 스키마 + AcrossConfig 구현

Purpose: Across Protocol REST API를 타입 안전하게 호출할 수 있는 기반 인프라를 구축한다. 후속 Plan 02의 AcrossBridgeActionProvider가 이 클라이언트를 사용한다.
Output: packages/actions/src/providers/across/ 디렉토리에 schemas.ts, config.ts, across-api-client.ts 3개 파일
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/design/79-across-protocol-bridge.md

Reference files (LI.FI precedent pattern):
@packages/actions/src/providers/lifi/schemas.ts
@packages/actions/src/providers/lifi/config.ts
@packages/actions/src/providers/lifi/lifi-api-client.ts
@packages/actions/src/common/action-api-client.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/actions/src/common/action-api-client.ts:
```typescript
export class ActionApiClient {
  constructor(baseUrl: string, timeoutMs?: number, headers?: Record<string, string>);
  async get<T>(path: string, schema: z.ZodType<T>, params?: Record<string, string>): Promise<T>;
  async post<T>(path: string, body: unknown, schema: z.ZodType<T>): Promise<T>;
}
```

From packages/core/src/errors/chain-error.ts:
```typescript
import { ChainError } from '@waiaas/core';
// ChainError(code: string, chain: string, details?: object)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Zod 스키마 + AcrossConfig + 체인/주소 매핑</name>
  <files>packages/actions/src/providers/across/schemas.ts, packages/actions/src/providers/across/config.ts</files>
  <action>
1. `packages/actions/src/providers/across/schemas.ts` 생성 -- 설계 문서 79 섹션 3의 Zod 스키마를 그대로 구현:
   - `AcrossFeeComponentSchema` (pct: string, total: string, .passthrough())
   - `AcrossSuggestedFeesResponseSchema` (totalRelayFee, relayerCapitalFee, relayerGasFee, lpFee, timestamp, isAmountTooLow, exclusiveRelayer, exclusivityDeadline, expectedFillTimeSec optional, limits 객체, .passthrough())
   - `AcrossLimitsResponseSchema` (minDeposit, maxDeposit, maxDepositInstant, maxDepositShortDelay, .passthrough())
   - `AcrossAvailableRoutesResponseSchema` (z.array of route objects with originChainId, destinationChainId, originToken, destinationToken, symbols optional, isNative optional)
   - `AcrossDepositStatusResponseSchema` (status enum ['filled','pending','expired','refunded'], fillTx optional, fillTxHash optional, depositTxHash optional, depositId optional, destinationChainId optional, updatedAt optional, .passthrough())
   - `AcrossSwapApprovalResponseSchema` (approvalTxns array, swapTx object, inputAmount, expectedOutputAmount, minOutputAmount, expectedFillTimeSec, .passthrough())
   - Export all schemas + inferred types

2. `packages/actions/src/providers/across/config.ts` 생성 -- 설계 문서 79 섹션 8.6-8.8의 내용 그대로:
   - `AcrossConfig` interface: enabled, apiBaseUrl, integratorId, fillDeadlineBufferSec, defaultSlippagePct, maxSlippagePct, requestTimeoutMs
   - `ACROSS_DEFAULTS` const: enabled=false, apiBaseUrl='https://app.across.to/api', integratorId='', fillDeadlineBufferSec=21600, defaultSlippagePct=0.01, maxSlippagePct=0.03, requestTimeoutMs=10000
   - `ACROSS_CHAIN_MAP` ReadonlyMap: ethereum(1), arbitrum(42161), optimism(10), base(8453), polygon(137), linea(59144) + `-mainnet` 변형
   - `getAcrossChainId(chain)` -- LI.FI의 getLiFiChainId와 동일 패턴. 미지원 체인 시 ChainError('UNSUPPORTED_CHAIN') throw
   - `SPOKE_POOL_ADDRESSES` ReadonlyMap<number, string> -- 설계 문서 섹션 4.5 테이블 참조
   - `WETH_ADDRESSES` ReadonlyMap<number, string> -- 설계 문서 섹션 4.6 테이블 참조
   - `getSpokePoolAddress(chainId)` -- ChainError throw on missing
   - `getWethAddress(chainId)` -- ChainError throw on missing
   - `isNativeTokenBridge(inputToken, chainId)` -- WETH 주소와 비교 (case-insensitive)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit packages/actions/src/providers/across/schemas.ts packages/actions/src/providers/across/config.ts 2>&1 | head -20</automated>
  </verify>
  <done>schemas.ts에 5개 Zod 스키마와 타입이 export되고, config.ts에 AcrossConfig/ACROSS_DEFAULTS/체인맵/주소맵이 export되며, TypeScript 컴파일 에러가 없다</done>
</task>

<task type="auto">
  <name>Task 2: AcrossApiClient 5개 엔드포인트 구현</name>
  <files>packages/actions/src/providers/across/across-api-client.ts</files>
  <action>
`packages/actions/src/providers/across/across-api-client.ts` 생성 -- LiFiApiClient 패턴을 따르되 5개 엔드포인트 래핑:

1. `class AcrossApiClient extends ActionApiClient` 생성
   - constructor(config: AcrossConfig) -- super(config.apiBaseUrl, config.requestTimeoutMs)
   - integratorId가 있으면 URL에 자동 append (Across API recommendation)

2. 5개 메서드 구현:
   - `getSuggestedFees(params: { inputToken, outputToken, originChainId, destinationChainId, amount, recipient? })` -> AcrossSuggestedFeesResponse
     - GET /suggested-fees with query params (모든 값 String 변환)
     - 캐싱 없음 (DS-03: suggested-fees 캐싱 금지)
   - `getLimits(params: { inputToken, outputToken, originChainId, destinationChainId })` -> AcrossLimitsResponse
     - GET /limits
   - `getAvailableRoutes(params?: { originChainId?, destinationChainId?, originToken?, destinationToken? })` -> AcrossAvailableRoutesResponse
     - GET /available-routes with optional filters
   - `getDepositStatus(params: { depositTxnRef, originChainId? })` -> AcrossDepositStatusResponse
     - GET /deposit/status
   - `getSwapApproval(params: { tokenAddr, originChainId, destinationChainId, amount, depositor, recipient?, inputToken?, outputToken? })` -> AcrossSwapApprovalResponse
     - GET /swap/approval

3. 모든 메서드는 ActionApiClient.get()을 사용하여 Zod 스키마 검증을 수행한다.
4. 파라미터 변환: number -> String(number), optional params는 존재할 때만 query에 추가.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit packages/actions/src/providers/across/across-api-client.ts 2>&1 | head -20</automated>
  </verify>
  <done>AcrossApiClient가 ActionApiClient를 extend하고 5개 메서드가 각각 올바른 Zod 스키마로 검증하며, TypeScript 컴파일 에러가 없다</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 에서 packages/actions/src/providers/across/ 내 모든 파일이 에러 없이 컴파일
- schemas.ts에서 5개 스키마가 export됨
- config.ts에서 AcrossConfig, ACROSS_DEFAULTS, ACROSS_CHAIN_MAP, SPOKE_POOL_ADDRESSES, WETH_ADDRESSES가 export됨
- across-api-client.ts에서 AcrossApiClient가 export되고 5개 public 메서드가 존재함
</verification>

<success_criteria>
- AcrossApiClient가 Across REST API 5개 엔드포인트를 Zod 검증과 함께 래핑한다
- 체인 ID 매핑, SpokePool 주소, WETH 주소가 설계 문서 79와 일치한다
- LI.FI 선례 패턴(ActionApiClient 상속, Zod passthrough, config 분리)을 정확히 따른다
</success_criteria>

<output>
After completion, create `.planning/phases/353-api-client-bridge-provider/353-01-SUMMARY.md`
</output>
