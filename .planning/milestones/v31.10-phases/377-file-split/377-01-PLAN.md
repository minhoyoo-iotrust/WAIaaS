---
phase: 377-file-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/api/routes/admin-auth.ts
  - packages/daemon/src/api/routes/admin-settings.ts
  - packages/daemon/src/api/routes/admin-notifications.ts
  - packages/daemon/src/api/routes/admin-wallets.ts
  - packages/daemon/src/api/routes/admin-monitoring.ts
autonomous: true
requirements: [SPLIT-01]

must_haves:
  truths:
    - "admin.ts의 38개 핸들러가 5개 도메인 모듈에 분산되어 있다"
    - "각 도메인 모듈이 독립적으로 import 가능한 함수를 export한다"
    - "모든 기존 Admin API 엔드포인트가 동일하게 동작한다"
  artifacts:
    - path: "packages/daemon/src/api/routes/admin-auth.ts"
      provides: "status, killSwitch, recover, shutdown, masterPasswordChange, rotateSecret 핸들러"
      exports: ["registerAdminAuthRoutes"]
    - path: "packages/daemon/src/api/routes/admin-settings.ts"
      provides: "settings, testRpc, oracleStatus, apiKeys, forexRates 핸들러"
      exports: ["registerAdminSettingsRoutes"]
    - path: "packages/daemon/src/api/routes/admin-notifications.ts"
      provides: "notifications status/test/log 핸들러"
      exports: ["registerAdminNotificationRoutes"]
    - path: "packages/daemon/src/api/routes/admin-wallets.ts"
      provides: "walletTransactions, walletBalance, walletStaking, telegramUsers, defiPositions 핸들러"
      exports: ["registerAdminWalletRoutes"]
    - path: "packages/daemon/src/api/routes/admin-monitoring.ts"
      provides: "transactions, incoming, agentPrompt, sessionReissue, txCancel, txReject, rpcStatus, backup, stats, autostop 핸들러"
      exports: ["registerAdminMonitoringRoutes"]
  key_links:
    - from: "admin-*.ts"
      to: "admin.ts"
      via: "import and call register function with router + deps"
      pattern: "registerAdmin.*Routes\\(router, deps\\)"
    - from: "admin-*.ts"
      to: "openapi-schemas.ts"
      via: "import schema types"
      pattern: "from.*openapi-schemas"
---

<objective>
admin.ts (3,107줄)의 38개 핸들러를 5개 도메인별 모듈 파일로 추출한다.

Purpose: 단일 파일 3,107줄을 관리 가능한 크기(각 200-700줄)로 분할하여 유지보수성을 높인다.
Output: admin-auth.ts, admin-settings.ts, admin-notifications.ts, admin-wallets.ts, admin-monitoring.ts 5개 파일
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/daemon/src/api/routes/admin.ts
@packages/daemon/src/api/routes/openapi-schemas.ts

<interfaces>
<!-- admin.ts 내부 구조 요약 -->

Lines 1-95: imports (34줄 import, schema imports from openapi-schemas.ts)
Lines 96-137: type exports (KillSwitchState, AdminRouteDeps interfaces)
Lines 139-878: route definitions (const XxxRoute = createRoute({...}))
Lines 880-956: helper functions (formatTxAmount, NATIVE_DECIMALS, NATIVE_SYMBOLS)
Lines 958-3107: export function adminRoutes(deps) — 38 router.openapi() handlers

Domain grouping of handlers inside adminRoutes():
1. Auth (lines 965-1391): statusRoute, activateKillSwitchRoute, getKillSwitchRoute, escalateKillSwitchRoute, recoverRoute, shutdownRoute, masterPasswordChangeRoute, rotateSecretRoute — 8 handlers
2. Notifications (lines 1395-1563): notificationsStatusRoute, notificationsTestRoute, notificationsLogRoute — 3 handlers
3. Settings (lines 1567-1868): settingsGetRoute, settingsPutRoute, testRpcRoute, oracleStatusRoute, apiKeysListRoute, apiKeyPutRoute, apiKeyDeleteRoute, forexRatesRoute — 8 handlers
4. Wallets (lines 1873-2199): adminWalletTransactionsRoute, adminWalletBalanceRoute, adminWalletStakingRoute, telegramUsersListRoute, telegramUserUpdateRoute, telegramUserDeleteRoute — 6 handlers
5. Monitoring (lines 2204-3107): adminTransactionsRoute, adminIncomingRoute, agentPromptRoute, sessionReissueRoute, adminTxCancelRoute, adminTxRejectRoute, rpcStatusRoute, adminDefiPositionsRoute, createBackupRoute, listBackupsRoute, adminStatsRoute, autostopRulesRoute, autostopRuleUpdateRoute — 13 handlers

Key pattern: Each module file will export a function `registerAdminXxxRoutes(router: OpenAPIHono, deps: AdminRouteDeps)` that registers its routes on the provided router. Each module must also include its own `createRoute()` definitions (not just handlers).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin-auth.ts, admin-notifications.ts, admin-settings.ts 추출</name>
  <files>
    packages/daemon/src/api/routes/admin-auth.ts
    packages/daemon/src/api/routes/admin-notifications.ts
    packages/daemon/src/api/routes/admin-settings.ts
  </files>
  <action>
admin.ts에서 3개 도메인 모듈을 추출한다. 각 파일은 다음 구조를 따른다:

1. **admin-auth.ts** (~500줄 예상):
   - admin.ts에서 statusRoute, activateKillSwitchRoute, getKillSwitchRoute, escalateKillSwitchRoute, recoverRoute, shutdownRoute, masterPasswordChangeRoute, rotateSecretRoute의 `createRoute()` 정의를 이동
   - admin.ts의 `adminRoutes()` 내부에서 해당 8개 `router.openapi(xxxRoute, async (c) => {...})` 핸들러 코드를 이동
   - `export function registerAdminAuthRoutes(router: OpenAPIHono, deps: AdminRouteDeps): void` 함수로 감싸기
   - AdminRouteDeps 타입은 admin.ts에서 import (admin.ts가 계속 export)
   - 필요한 openapi-schemas.ts import 가져오기: AdminStatusResponseSchema, KillSwitchResponseSchema, KillSwitchActivateResponseSchema, KillSwitchEscalateResponseSchema, MasterPasswordChangeRequestSchema, MasterPasswordChangeResponseSchema, RecoverResponseSchema, KillSwitchRecoverRequestSchema, ShutdownResponseSchema, RotateSecretResponseSchema, ErrorResponseSchema, buildErrorResponses, openApiValidationHook
   - 필요한 외부 import: @hono/zod-openapi (createRoute, z), drizzle-orm (sql, eq, etc), @waiaas/core (WAIaaSError), schema (wallets, sessions, etc), generateId, 기타 deps에서 사용하는 타입

2. **admin-notifications.ts** (~200줄 예상):
   - notificationsStatusRoute, notificationsTestRoute, notificationsLogRoute의 createRoute() 정의 이동
   - 해당 3개 핸들러 코드 이동
   - `export function registerAdminNotificationRoutes(router: OpenAPIHono, deps: AdminRouteDeps): void`
   - 필요한 schema import: NotificationStatusResponseSchema, NotificationTestRequestSchema, NotificationTestResponseSchema, NotificationLogResponseSchema

3. **admin-settings.ts** (~400줄 예상):
   - settingsGetRoute, settingsPutRoute, testRpcRoute, oracleStatusRoute, apiKeysListRoute, apiKeyPutRoute, apiKeyDeleteRoute, forexRatesRoute의 createRoute() 정의 이동
   - 해당 8개 핸들러 코드 이동
   - `export function registerAdminSettingsRoutes(router: OpenAPIHono, deps: AdminRouteDeps): void`
   - 필요한 schema import: SettingsResponseSchema, SettingsUpdateRequestSchema, SettingsUpdateResponseSchema, TestRpcRequestSchema, TestRpcResponseSchema, OracleStatusResponseSchema
   - 필요한 import: getSettingDefinition, ActionTierOverrideSchema (settings/index), resolveRpcUrl (adapter-pool), CurrencyCodeSchema, formatRatePreview (@waiaas/core)

각 파일의 import는 admin.ts의 기존 import에서 해당 핸들러가 사용하는 것만 선별한다. AdminRouteDeps는 `import type { AdminRouteDeps } from './admin.js'`로 가져온다.

**중요:** 아직 admin.ts에서 코드를 제거하지 않는다 — 이 Task는 새 파일 생성만 한다. Task 2에서 나머지 2개 파일을 생성한다. Plan 377-02에서 admin.ts를 thin aggregator로 재구성한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit --project packages/daemon/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>admin-auth.ts, admin-notifications.ts, admin-settings.ts 3개 파일이 존재하고 각각 registerAdminAuthRoutes, registerAdminNotificationRoutes, registerAdminSettingsRoutes 함수를 export하며 TypeScript 컴파일이 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: admin-wallets.ts, admin-monitoring.ts 추출</name>
  <files>
    packages/daemon/src/api/routes/admin-wallets.ts
    packages/daemon/src/api/routes/admin-monitoring.ts
  </files>
  <action>
admin.ts에서 나머지 2개 도메인 모듈을 추출한다:

1. **admin-wallets.ts** (~400줄 예상):
   - adminWalletTransactionsRoute, adminWalletBalanceRoute, adminWalletStakingRoute, telegramUsersListRoute, telegramUserUpdateRoute, telegramUserDeleteRoute의 createRoute() 정의 이동
   - 해당 6개 핸들러 코드 이동
   - adminDefiPositionsRoute (defi positions per wallet)도 여기에 포함 — 지갑별 조회이므로 wallets 도메인
   - `export function registerAdminWalletRoutes(router: OpenAPIHono, deps: AdminRouteDeps): void`
   - 필요한 schema import: StakingPositionsResponseSchema
   - formatTxAmount 헬퍼 함수, NATIVE_DECIMALS, NATIVE_SYMBOLS 상수도 이 파일로 이동 (wallets 핸들러가 주 사용자)
   - 필요한 import: @waiaas/core (formatAmount, getNetworksForEnvironment 등), drizzle-orm, schema (wallets, transactions, tokenRegistry, walletApps 등)

2. **admin-monitoring.ts** (~700줄 예상):
   - adminTransactionsRoute, adminIncomingRoute, agentPromptRoute, sessionReissueRoute, adminTxCancelRoute, adminTxRejectRoute, rpcStatusRoute, createBackupRoute, listBackupsRoute, adminStatsRoute, autostopRulesRoute, autostopRuleUpdateRoute의 createRoute() 정의 이동
   - 해당 12개 핸들러 코드 이동 (일부 createRoute는 adminRoutes 함수 내부에 인라인으로 정의되어 있음 — agentPromptRoute, sessionReissueRoute, adminTxCancelRoute, adminTxRejectRoute, createBackupRoute, listBackupsRoute, adminStatsRoute, autostopRulesRoute, autostopRuleUpdateRoute는 함수 내부 정의이므로 register 함수 내부에 createRoute도 함께 넣거나 함수 밖으로 추출)
   - `export function registerAdminMonitoringRoutes(router: OpenAPIHono, deps: AdminRouteDeps): void`
   - 필요한 schema import: AgentPromptRequestSchema, AgentPromptResponseSchema, SessionReissueResponseSchema, RpcStatusResponseSchema, BackupInfoResponseSchema, BackupListResponseSchema
   - 필요한 import: buildConnectInfoPrompt/DefaultDenyStatus (connect-info.js), JwtPayload (jwt-secret-manager), DelayQueue, ApprovalWorkflow, EncryptedBackupService, AdminStatsService, AutoStopService 등

**중요:** 아직 admin.ts에서 코드를 제거하지 않는다. Plan 377-02에서 admin.ts를 thin aggregator로 재구성한다.

**formatTxAmount 배치 결정:** formatTxAmount은 adminWalletTransactionsRoute(wallet별 tx), adminTransactionsRoute(cross-wallet tx), adminIncomingRoute(incoming tx)에서 사용된다. admin-wallets.ts에 정의하고 export하여 admin-monitoring.ts에서 import하는 구조로 한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit --project packages/daemon/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>admin-wallets.ts, admin-monitoring.ts 2개 파일이 존재하고 각각 registerAdminWalletRoutes, registerAdminMonitoringRoutes 함수를 export하며 TypeScript 컴파일이 통과한다</done>
</task>

</tasks>

<verification>
- 5개 새 파일이 모두 존재: admin-auth.ts, admin-settings.ts, admin-notifications.ts, admin-wallets.ts, admin-monitoring.ts
- 각 파일이 registerAdminXxxRoutes 함수를 export
- TypeScript 컴파일 오류 없음
- admin.ts는 아직 변경하지 않음 (377-02에서 처리)
</verification>

<success_criteria>
1. 5개 도메인 모듈 파일이 생성되었다
2. 38개 핸들러가 도메인별로 올바르게 분류되었다
3. 각 파일의 import가 필요한 것만 포함한다
4. TypeScript 컴파일이 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/377-file-split/377-01-SUMMARY.md`
</output>
