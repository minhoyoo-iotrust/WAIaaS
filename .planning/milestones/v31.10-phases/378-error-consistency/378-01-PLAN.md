---
phase: 378-error-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/api/routes/nft-approvals.ts
  - packages/daemon/src/api/routes/sessions.ts
  - packages/daemon/src/api/routes/admin-monitoring.ts
  - packages/daemon/src/api/routes/erc8004.ts
autonomous: true
requirements: [ERR-01, ERR-02, ERR-03]

must_haves:
  truths:
    - "nft-approvals.ts의 모든 에러 응답이 WAIaaSError throw로 처리된다"
    - "sessions.ts의 204 응답이 as any 없이 표준 Hono 패턴으로 반환된다"
    - "API 라우트 파일에서 응답 관련 as any 타입 escape가 0개이다"
  artifacts:
    - path: "packages/daemon/src/api/routes/nft-approvals.ts"
      provides: "WAIaaSError 기반 에러 응답"
      contains: "throw new WAIaaSError"
    - path: "packages/daemon/src/api/routes/sessions.ts"
      provides: "표준 204 응답"
      contains: "c.body(null, 204)"
    - path: "packages/daemon/src/api/routes/admin-monitoring.ts"
      provides: "WAIaaSError 기반 503 응답"
      contains: "throw new WAIaaSError"
    - path: "packages/daemon/src/api/routes/erc8004.ts"
      provides: "타입 안전한 JSON 응답"
  key_links:
    - from: "packages/daemon/src/api/routes/nft-approvals.ts"
      to: "@waiaas/core WAIaaSError"
      via: "throw new WAIaaSError('NETWORK_REQUIRED')"
      pattern: "throw new WAIaaSError"
    - from: "packages/daemon/src/api/routes/admin-monitoring.ts"
      to: "@waiaas/core error-codes"
      via: "STATS_NOT_CONFIGURED error code"
      pattern: "throw new WAIaaSError\\('STATS_NOT_CONFIGURED'"
---

<objective>
비표준 에러/응답 패턴을 WAIaaSError 및 표준 Hono 응답 패턴으로 통일한다.

Purpose: API 응답 일관성 확보. 모든 에러가 WAIaaSError 통해 처리되어 클라이언트가 일관된 `{code, message, retryable}` 형식을 받는다.
Output: 4개 라우트 파일에서 비표준 패턴 제거, 1개 에러 코드 추가
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- WAIaaSError 생성자 및 에러 코드 등록 패턴 -->

From packages/core/src/errors/base-error.ts:
```typescript
export class WAIaaSError extends Error {
  constructor(code: ErrorCode, options?: { message?: string; details?: Record<string, unknown>; hint?: string; cause?: Error });
}
```

From packages/core/src/errors/error-codes.ts:
```typescript
export type ErrorDomain = 'AUTH' | 'SESSION' | 'TX' | 'POLICY' | 'OWNER' | 'SYSTEM' | 'WALLET' | 'WITHDRAW' | 'ACTION' | 'ADMIN' | 'X402' | 'SIGNING' | 'CHAIN' | 'ERC8128' | 'NFT' | 'USEROP';
export const ERROR_CODES = { ... } as const satisfies Record<string, ErrorCodeEntry>;
export type ErrorCode = keyof typeof ERROR_CODES;
```

기존 관련 에러 코드:
- `NETWORK_REQUIRED`: domain TX, httpStatus 400 -- nft-approvals.ts의 network 누락 에러에 사용 가능
- `INVALID_TRANSACTION`: domain TX, httpStatus 400 -- 유효하지 않은 토큰 식별자에 근사하나, NFT 도메인의 기존 코드가 더 적합
- `NFT_NOT_FOUND`: domain NFT, httpStatus 404
- `UNSUPPORTED_NFT_STANDARD`: domain NFT, httpStatus 400

표준 204 응답 패턴 (webhooks.ts에서 사용):
```typescript
return c.body(null, 204);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 에러 코드 추가 및 nft-approvals.ts / admin-monitoring.ts WAIaaSError 교체</name>
  <files>
    packages/core/src/errors/error-codes.ts
    packages/daemon/src/api/routes/nft-approvals.ts
    packages/daemon/src/api/routes/admin-monitoring.ts
  </files>
  <action>
1. `packages/core/src/errors/error-codes.ts`에 2개 에러 코드 추가:
   - `INVALID_TOKEN_IDENTIFIER`: domain 'NFT', httpStatus 400, retryable false, message 'Invalid NFT token identifier format'. nft-approvals.ts의 `c.json({ error: "Invalid token identifier: ..." }, 400)` 대체용.
   - `STATS_NOT_CONFIGURED`: domain 'ADMIN', httpStatus 503, retryable false, message 'Stats service not configured'. admin-monitoring.ts의 비표준 503 응답 대체용.
   상단 주석의 에러 코드 카운트도 갱신한다 (108 -> 110).

2. `packages/daemon/src/api/routes/nft-approvals.ts` 수정:
   - 81행 `return c.json({ error: 'network query parameter is required' }, 400)` -> `throw new WAIaaSError('NETWORK_REQUIRED', { message: 'network query parameter is required' })`
   - 86행 `return c.json({ error: \`Invalid token identifier: ${tokenIdentifier}\` }, 400)` -> `throw new WAIaaSError('INVALID_TOKEN_IDENTIFIER', { message: \`Invalid token identifier: ${tokenIdentifier}\` })`
   - WAIaaSError는 이미 import되어 있음 (17행).

3. `packages/daemon/src/api/routes/admin-monitoring.ts` 수정:
   - 797행 `return c.json({ code: 'NOT_CONFIGURED', message: 'Stats service not configured', retryable: false }, 503 as any)` -> `throw new WAIaaSError('STATS_NOT_CONFIGURED')`
   - WAIaaSError import 추가 필요: `import { WAIaaSError } from '@waiaas/core'` (기존 import에 추가).
   - `503 as any` 타입 escape 제거됨.

주의: nft-approvals.ts의 에러 응답 구조가 `{ error: string }` 에서 WAIaaSError의 `{ code, message, retryable }` 로 바뀌지만, 이는 비표준 -> 표준 통일이므로 의도된 행위 변경이다. 기존 OpenAPI 라우트 정의에 `buildErrorResponses`가 이미 있거나, 글로벌 에러 핸들러가 WAIaaSError를 catch하므로 별도 라우트 정의 수정은 불필요.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/core --filter=@waiaas/daemon 2>&1 | tail -5</automated>
  </verify>
  <done>
    - nft-approvals.ts에 c.json({ error: ... }, 400) 패턴이 0개
    - admin-monitoring.ts에 503 as any 패턴이 0개
    - error-codes.ts에 INVALID_TOKEN_IDENTIFIER, STATS_NOT_CONFIGURED 코드 존재
    - typecheck 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: sessions.ts 표준 204 응답 + erc8004.ts 응답 as any 제거</name>
  <files>
    packages/daemon/src/api/routes/sessions.ts
    packages/daemon/src/api/routes/erc8004.ts
  </files>
  <action>
1. `packages/daemon/src/api/routes/sessions.ts` 수정:
   - 775행 `return new Response(null, { status: 204 }) as any` -> `return c.body(null, 204)`
   - 이것은 Hono의 표준 204 응답 패턴이다 (webhooks.ts에서 동일하게 사용 중).
   - OpenAPI 라우트 정의(removeWalletRoute)에 `204: { description: 'Wallet removed' }` 가 이미 content 없이 정의되어 있으므로 타입 호환.

2. `packages/daemon/src/api/routes/erc8004.ts` 수정:
   - 345행 `return c.json(file as any, 200)` 에서 `as any` 제거.
   - `file` 변수의 타입을 확인하고 OpenAPI 라우트 정의의 200 응답 스키마와 맞추어 타입 캐스트 없이 반환 가능하도록 한다.
   - 라우트 정의에서 200 응답 스키마가 `z.any()`이면 `as any` 없이도 타입이 호환될 수 있다. 스키마를 확인 후, z.any()이면 단순히 `as any` 제거. 구체적 스키마이면 file 변수 타입을 스키마에 맞게 assertion한다.

3. 최종 검증: API 라우트 디렉터리에서 응답 관련 `as any` 패턴이 남아있지 않은지 확인.
   - `userop.ts`의 `publicClient ... as any` (188, 463행)와 `actions.ts`/`admin-actions.ts`의 `(contractCall as any).eip712`는 응답이 아닌 내부 타입 캐스트이므로 이 Phase 범위 밖 (ERR-03는 "응답 패턴"에 한정).
   - `nfts.ts`의 `network as any` (265, 335행)도 validateNetworkEnvironment 호출의 인자 캐스트이므로 응답 패턴이 아님.
   - `wc.ts`의 `(db as any).session?.client`도 DB 접근 캐스트이므로 범위 밖.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run lint && pnpm turbo run typecheck && pnpm turbo run test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - sessions.ts에 `new Response(null, { status: 204 }) as any` 패턴이 0개
    - sessions.ts에 `c.body(null, 204)` 사용
    - erc8004.ts에 응답 관련 `as any` 0개
    - lint + typecheck + 전체 테스트 통과
  </done>
</task>

</tasks>

<verification>
1. `grep -rn 'c\.json.*error.*400' packages/daemon/src/api/routes/nft-approvals.ts` -- 0 결과
2. `grep -rn 'as any' packages/daemon/src/api/routes/sessions.ts` -- 0 결과 (응답 관련)
3. `grep -rn '503 as any' packages/daemon/src/api/routes/admin-monitoring.ts` -- 0 결과
4. `grep -rn 'as any.*200\|as any.*204\|as any.*503' packages/daemon/src/api/routes/` -- 응답 관련 as any 0개
5. `pnpm turbo run lint && pnpm turbo run typecheck && pnpm turbo run test` 전체 통과
</verification>

<success_criteria>
1. nft-approvals.ts에서 c.json({ error: "..." }, 400) 패턴이 0개이고 모두 WAIaaSError throw로 교체
2. sessions.ts에서 new Response(null, { status: 204 }) as any가 c.body(null, 204)로 교체
3. admin-monitoring.ts에서 503 as any가 WAIaaSError throw로 교체
4. erc8004.ts에서 응답 관련 as any 제거
5. pnpm turbo run lint && pnpm turbo run typecheck && pnpm turbo run test 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/378-error-consistency/378-01-SUMMARY.md`
</output>
