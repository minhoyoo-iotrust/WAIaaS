---
phase: 375-utility-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/common/contract-encoding.ts
  - packages/actions/src/providers/aave-v3/aave-contracts.ts
  - packages/actions/src/providers/lido-staking/lido-contract.ts
  - packages/actions/src/providers/zerox-swap/index.ts
  - packages/actions/src/providers/dcent-swap/dex-swap.ts
autonomous: true
requirements: [UTIL-02, UTIL-03, UTIL-04]

must_haves:
  truths:
    - "padHex, addressToHex, uint256ToHex, encodeApproveCalldata가 공통 모듈에 단일 정의로 존재한다"
    - "프로바이더 파일에 로컬 padHex/addressToHex/uint256ToHex/encodeApproveCalldata 정의가 0개이다"
    - "기존 테스트가 100% 통과하며 행위 변경이 없다"
  artifacts:
    - path: "packages/actions/src/common/contract-encoding.ts"
      provides: "EVM ABI 인코딩 공통 유틸리티"
      exports: ["padHex", "addressToHex", "uint256ToHex", "encodeApproveCalldata"]
    - path: "packages/actions/src/common/contract-encoding.test.ts"
      provides: "contract-encoding 유닛 테스트"
      contains: "encodeApproveCalldata"
  key_links:
    - from: "packages/actions/src/providers/aave-v3/aave-contracts.ts"
      to: "packages/actions/src/common/contract-encoding.ts"
      via: "import { padHex, addressToHex, uint256ToHex, encodeApproveCalldata }"
      pattern: "from.*common/contract-encoding"
    - from: "packages/actions/src/providers/lido-staking/lido-contract.ts"
      to: "packages/actions/src/common/contract-encoding.ts"
      via: "import { addressToHex, uint256ToHex, encodeApproveCalldata }"
      pattern: "from.*common/contract-encoding"
    - from: "packages/actions/src/providers/zerox-swap/index.ts"
      to: "packages/actions/src/common/contract-encoding.ts"
      via: "import { encodeApproveCalldata }"
      pattern: "from.*common/contract-encoding"
    - from: "packages/actions/src/providers/dcent-swap/dex-swap.ts"
      to: "packages/actions/src/common/contract-encoding.ts"
      via: "import { encodeApproveCalldata }"
      pattern: "from.*common/contract-encoding"
---

<objective>
contract-encoding 공통 모듈을 추출하고 4개 프로바이더의 중복 구현을 교체한다.

Purpose: padHex/addressToHex/uint256ToHex/encodeApproveCalldata가 lido-contract.ts, aave-contracts.ts, zerox-swap, dcent-swap에 중복 정의되어 있다. 통합하면 ~400줄 중복을 제거한다.
Output: `packages/actions/src/common/contract-encoding.ts` 공통 모듈 + 4개 프로바이더 교체
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 중복 구현 위치와 시그니처 차이 분석 -->

**Variant A (lido-contract.ts, aave-contracts.ts) -- bigint amount:**
```typescript
function padHex(value: string, length: number = 64): string {
  return value.padStart(length, '0');
}
function addressToHex(address: string): string {
  return padHex(address.slice(2).toLowerCase());
}
function uint256ToHex(value: bigint): string {
  if (value < 0n) throw new Error('uint256 cannot be negative');
  return padHex(value.toString(16));
}
export function encodeApproveCalldata(spender: string, amount: bigint): string {
  const selector = '0x095ea7b3';
  const paddedSpender = addressToHex(spender);
  const paddedAmount = uint256ToHex(amount);
  return `${selector}${paddedSpender}${paddedAmount}`;
}
```

**Variant B (zerox-swap, dcent-swap) -- string amount:**
```typescript
function encodeApproveCalldata(spender: string, amount: string): string {
  const selector = '0x095ea7b3';
  const paddedSpender = spender.slice(2).toLowerCase().padStart(64, '0');
  const paddedAmount = BigInt(amount).toString(16).padStart(64, '0');
  return `${selector}${paddedSpender}${paddedAmount}`;
}
```

**차이점:** Variant B는 amount를 string으로 받아 내부에서 BigInt 변환. 인라인으로 pad 수행 (padHex 함수 없음). 공통 모듈에서는 `amount: bigint` 시그니처를 표준으로 하되, zerox-swap/dcent-swap 호출부에서 BigInt(amount)를 전달하도록 수정.

**aave-contracts.ts에서 padHex/addressToHex/uint256ToHex를 사용하는 다른 함수들:**
- encodeSupplyCalldata, encodeBorrowCalldata, encodeRepayCalldata, encodeWithdrawCalldata
- encodeGetUserAccountDataCalldata, encodeGetReserveDataCalldata

**lido-contract.ts에서 padHex/addressToHex/uint256ToHex를 사용하는 다른 함수들:**
- encodeSubmitCalldata, encodeRequestWithdrawalsCalldata
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: contract-encoding 공통 모듈 생성 및 테스트</name>
  <files>packages/actions/src/common/contract-encoding.ts, packages/actions/src/common/contract-encoding.test.ts</files>
  <behavior>
    - padHex("ff", 64) returns "00...00ff" (64 chars)
    - padHex("abc", 8) returns "00000abc"
    - addressToHex("0x1234567890abcdef1234567890abcdef12345678") returns "0000...1234567890abcdef1234567890abcdef12345678" (64 chars, lowercase)
    - addressToHex("0xABCD...") converts to lowercase
    - uint256ToHex(0n) returns "0000...0000" (64 chars)
    - uint256ToHex(255n) returns "0000...00ff" (64 chars)
    - uint256ToHex(-1n) throws Error('uint256 cannot be negative')
    - encodeApproveCalldata("0xSPENDER", 1000n) returns "0x095ea7b3" + padded spender + padded amount
    - encodeApproveCalldata 결과가 기존 lido-contract.ts 구현과 동일한 출력
  </behavior>
  <action>
    1. `packages/actions/src/common/contract-encoding.ts` 생성:
       - `export function padHex(value: string, length: number = 64): string`
       - `export function addressToHex(address: string): string`
       - `export function uint256ToHex(value: bigint): string` (음수 시 throw)
       - `export function encodeApproveCalldata(spender: string, amount: bigint): string`
         - selector `0x095ea7b3` 하드코딩
         - addressToHex + uint256ToHex 사용
       - 순수 함수, 외부 의존성 없음 (ChainError 불필요)

    2. `packages/actions/src/common/contract-encoding.test.ts` 생성:
       - behavior에 명시된 케이스 모두 테스트
       - 기존 aave-contracts.ts/lido-contract.ts와 동일한 입력/출력 검증
       - MAX_UINT256 = (1n << 256n) - 1n 케이스 포함

    테스트 먼저 작성 후 구현 (RED -> GREEN).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/common/contract-encoding.test.ts</automated>
  </verify>
  <done>contract-encoding.ts가 존재하고, padHex/addressToHex/uint256ToHex/encodeApproveCalldata를 export하며, 모든 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: 4개 프로바이더의 로컬 구현을 공통 모듈 import으로 교체</name>
  <files>
    packages/actions/src/providers/aave-v3/aave-contracts.ts,
    packages/actions/src/providers/lido-staking/lido-contract.ts,
    packages/actions/src/providers/zerox-swap/index.ts,
    packages/actions/src/providers/dcent-swap/dex-swap.ts
  </files>
  <action>
    각 프로바이더에서 로컬 인코딩 함수를 제거하고 공통 모듈에서 import한다.

    **1. aave-v3/aave-contracts.ts:**
    - 라인 21-32의 `padHex`, `addressToHex`, `uint256ToHex` 3개 로컬 함수 삭제 (~12줄)
    - 라인 158-160의 `encodeApproveCalldata` 함수 삭제 (~10줄, JSDoc 포함)
    - `import { padHex, addressToHex, uint256ToHex, encodeApproveCalldata } from '../../common/contract-encoding.js';` 추가
    - encodeSupplyCalldata, encodeBorrowCalldata, encodeRepayCalldata, encodeWithdrawCalldata, encodeGetUserAccountDataCalldata, encodeGetReserveDataCalldata는 그대로 유지 (이들은 aave-specific이며 공통화 대상이 아님). 이 함수들이 내부에서 사용하는 addressToHex/uint256ToHex가 import로 대체됨
    - `export { encodeApproveCalldata }` re-export 추가 (aave-v3/index.ts가 aave-contracts에서 encodeApproveCalldata를 import하므로)

    **2. lido-staking/lido-contract.ts:**
    - 라인 16-27의 `padHex`, `addressToHex`, `uint256ToHex` 3개 로컬 함수 삭제 (~12줄)
    - 라인 93-98의 `encodeApproveCalldata` 함수 삭제 (~10줄, JSDoc 포함)
    - `import { addressToHex, uint256ToHex, encodeApproveCalldata } from '../../common/contract-encoding.js';` 추가
    - encodeSubmitCalldata, encodeRequestWithdrawalsCalldata는 유지 (lido-specific)
    - `export { encodeApproveCalldata }` re-export 추가 (lido-staking/index.ts가 lido-contract에서 import하므로)

    **3. zerox-swap/index.ts:**
    - 라인 42-47의 로컬 `encodeApproveCalldata` 함수 삭제 (~7줄)
    - `import { encodeApproveCalldata } from '../../common/contract-encoding.js';` 추가
    - 호출부 변경 (라인 175): `encodeApproveCalldata(expectedAllowanceHolder, input.sellAmount)` -> `encodeApproveCalldata(expectedAllowanceHolder, BigInt(input.sellAmount))`
      (기존에는 string amount를 받았으나, 공통 모듈은 bigint. sellAmount는 이미 smallest unit string이므로 BigInt 변환만 추가)

    **4. dcent-swap/dex-swap.ts:**
    - 라인 59-64의 로컬 `encodeApproveCalldata` 함수 삭제 (~7줄)
    - `import { encodeApproveCalldata } from '../../common/contract-encoding.js';` 추가
    - 호출부 변경 (라인 265): `encodeApproveCalldata(spender, params.amount)` -> `encodeApproveCalldata(spender, BigInt(params.amount))`

    **주의사항:**
    - import 경로는 `.js` 확장자 포함 (ESM 프로젝트 규칙)
    - aave-contracts.ts와 lido-contract.ts에서 `export { encodeApproveCalldata }` re-export가 필요한 이유: 각각의 index.ts에서 `import { encodeApproveCalldata } from './aave-contracts.js'` / `'./lido-contract.js'` 패턴으로 import하고 있기 때문
    - 행위 변경 없음: 동일한 입력에 동일한 출력
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run lint && pnpm turbo run typecheck && pnpm turbo run test</automated>
  </verify>
  <done>4개 프로바이더 파일에 로컬 padHex/addressToHex/uint256ToHex/encodeApproveCalldata 함수 정의가 0개이고, 모두 common/contract-encoding.ts에서 import하며, 전체 테스트 통과</done>
</task>

</tasks>

<verification>
1. `grep -rn "function padHex\|function addressToHex\|function uint256ToHex" packages/actions/src/providers/` 결과가 0줄
2. `grep -rn "function encodeApproveCalldata" packages/actions/src/providers/` 결과가 0줄
3. `grep -rn "from.*common/contract-encoding" packages/actions/src/providers/ | wc -l` 결과가 4 이상
4. `pnpm turbo run lint && pnpm turbo run typecheck && pnpm turbo run test` 전체 통과
</verification>

<success_criteria>
- contract-encoding.ts가 존재하고 padHex, addressToHex, uint256ToHex, encodeApproveCalldata를 export한다
- 4개 프로바이더가 공통 모듈을 import하여 사용한다 (+ re-export)
- 프로바이더 파일에 로컬 인코딩 함수 정의가 0개이다
- 전체 lint, typecheck, test 통과
</success_criteria>

<output>
After completion, create `.planning/phases/375-utility-consolidation/375-02-SUMMARY.md`
</output>
