---
phase: 398-type-system-infra-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/infrastructure/database/schema.ts
  - packages/daemon/src/lifecycle/daemon.ts
  - packages/adapters/evm/src/evm-chain-map.ts
  - packages/adapters/evm/src/__tests__/evm-chain-map.test.ts
  - packages/daemon/src/__tests__/migration-v58.test.ts
autonomous: true
requirements:
  - DEPL-03
  - ASYNC-05
  - RPC-07

must_haves:
  truths:
    - "DB v58 마이그레이션이 tx_history type CHECK에 CONTRACT_DEPLOY를 포함한다"
    - "toAddress가 nullable로 저장 가능하다 (이미 nullable이므로 CHECK 제약만 확인)"
    - "Node.js keepAliveTimeout이 600초 이상으로 설정된다"
    - "chainId 숫자로 EvmNetworkType slug를 역조회할 수 있다"
    - "존재하지 않는 chainId로 역조회 시 undefined를 반환한다"
  artifacts:
    - path: "packages/daemon/src/infrastructure/database/migrate.ts"
      provides: "v58 migration: recreate transactions table CHECK with 9-type TRANSACTION_TYPES"
      contains: "version: 58"
    - path: "packages/adapters/evm/src/evm-chain-map.ts"
      provides: "EVM_CHAIN_ID_TO_NETWORK reverse lookup map"
      exports: ["EVM_CHAIN_ID_TO_NETWORK", "getNetworkByChainId"]
    - path: "packages/daemon/src/lifecycle/daemon.ts"
      provides: "keepAliveTimeout 600s configuration on HTTP server"
      contains: "keepAliveTimeout"
    - path: "packages/daemon/src/__tests__/migration-v58.test.ts"
      provides: "Migration v58 test"
      contains: "version 58"
  key_links:
    - from: "packages/adapters/evm/src/evm-chain-map.ts"
      to: "packages/core/src/enums/chain.ts"
      via: "EvmNetworkType import"
      pattern: "EvmNetworkType"
    - from: "packages/daemon/src/lifecycle/daemon.ts"
      to: "@hono/node-server serve()"
      via: "server.keepAliveTimeout assignment"
      pattern: "keepAliveTimeout"
---

<objective>
DB v58 마이그레이션으로 transactions type CHECK를 9-type으로 업데이트하고, RPC 프록시 인프라 전제 조건(keepAliveTimeout, chainId 역방향 조회)을 준비한다.

Purpose: Phase 399-400에서 RPC 프록시 라우트가 chainId → NetworkType 변환과 Long-poll을 사용하므로, 인프라 기반을 먼저 구축한다. DB CHECK 제약도 CONTRACT_DEPLOY를 허용하도록 업데이트해야 한다.
Output: v58 migration, EVM_CHAIN_ID_TO_NETWORK map, keepAliveTimeout 600s
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/daemon/src/infrastructure/database/migrate.ts
@packages/daemon/src/infrastructure/database/schema.ts
@packages/daemon/src/lifecycle/daemon.ts
@packages/adapters/evm/src/evm-chain-map.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/daemon/src/infrastructure/database/migrate.ts:
```typescript
export const LATEST_SCHEMA_VERSION = 57;

export interface Migration {
  version: number;
  description: string;
  up: (sqlite: Database) => void;
  managesOwnTransaction?: boolean;
}

const MIGRATIONS: Migration[] = [];
// Append with MIGRATIONS.push({ version: N, description, up })
```

From packages/adapters/evm/src/evm-chain-map.ts:
```typescript
export interface EvmChainEntry {
  viemChain: Chain;
  chainId: number;
  nativeSymbol: string;
  nativeName: string;
}

export const EVM_CHAIN_MAP: Record<EvmNetworkType, EvmChainEntry> = {
  'ethereum-mainnet': { viemChain: mainnet, chainId: 1, ... },
  'ethereum-sepolia': { viemChain: sepolia, chainId: 11155111, ... },
  // ... 12 entries total
};
```

From packages/daemon/src/lifecycle/daemon.ts (Step 5):
```typescript
const server = serve({ fetch: app.fetch, hostname, port });
this.httpServer = server;
// server is a Node.js http.Server (from @hono/node-server)
```

From packages/daemon/src/infrastructure/database/migrate.ts (transactions DDL):
```sql
to_address TEXT,  -- already nullable (no NOT NULL constraint)
type TEXT NOT NULL CHECK (type IN (${inList(TRANSACTION_TYPES)})),
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB v58 마이그레이션 + EVM_CHAIN_ID_TO_NETWORK 역방향 맵</name>
  <files>
    packages/daemon/src/infrastructure/database/migrate.ts,
    packages/daemon/src/infrastructure/database/schema.ts,
    packages/daemon/src/__tests__/migration-v58.test.ts,
    packages/adapters/evm/src/evm-chain-map.ts,
    packages/adapters/evm/src/__tests__/evm-chain-map.test.ts,
    packages/adapters/evm/src/index.ts
  </files>
  <behavior>
    - Test 1: v58 마이그레이션 후 transactions 테이블에 type='CONTRACT_DEPLOY' INSERT 성공
    - Test 2: v58 마이그레이션 후 toAddress=NULL인 row INSERT 성공
    - Test 3: v57 → v58 마이그레이션 체인이 정상 실행
    - Test 4: getNetworkByChainId(1) === 'ethereum-mainnet'
    - Test 5: getNetworkByChainId(8453) === 'base-mainnet'
    - Test 6: getNetworkByChainId(99999) === undefined
    - Test 7: EVM_CHAIN_ID_TO_NETWORK 맵이 EVM_CHAIN_MAP과 1:1 대응 (12 entries)
  </behavior>
  <action>
    1. **packages/daemon/src/infrastructure/database/migrate.ts**:
       - `LATEST_SCHEMA_VERSION`을 57에서 58로 변경.
       - v58 마이그레이션 추가. SQLite는 CHECK 제약을 ALTER로 변경 불가하므로, 12-step table recreation 패턴 사용 (`managesOwnTransaction: true`):
         ```typescript
         MIGRATIONS.push({
           version: 58,
           description: 'Add CONTRACT_DEPLOY to transactions type CHECK constraint',
           managesOwnTransaction: true,
           up: (sqlite) => {
             // 12-step: 1) BEGIN, 2) CREATE new table, 3) INSERT SELECT, 4) DROP old,
             // 5) RENAME new, 6) Re-create indexes, 7) COMMIT
             // CHECK constraint: type IN (${inList(TRANSACTION_TYPES)}) -- now includes CONTRACT_DEPLOY
             // toAddress is already nullable, no change needed
           },
         });
         ```
         주의: 기존 v6b, v7, v8 마이그레이션의 12-step 패턴을 참고. `PRAGMA foreign_keys = OFF` 후 테이블 재생성 → 데이터 복사 → 이름 변경 → 인덱스 재생성 → `PRAGMA foreign_keys = ON`.
       - DDL `getCreateTableStatements()`의 transactions CREATE TABLE은 이미 `TRANSACTION_TYPES` SSoT 배열을 참조하므로 자동 반영됨 (Plan 01에서 enum 추가 후).
       - pushSchema의 `LATEST_SCHEMA_VERSION = 58` 업데이트로 fresh DB에서 v58까지 기록.

    2. **packages/daemon/src/infrastructure/database/schema.ts**: 변경 없음 (Drizzle schema의 `check_tx_type`은 이미 `TRANSACTION_TYPES` SSoT 참조).

    3. **packages/adapters/evm/src/evm-chain-map.ts**:
       - `EVM_CHAIN_ID_TO_NETWORK` 역방향 맵 추가:
         ```typescript
         /** Reverse lookup: EVM chainId (number) → WAIaaS EvmNetworkType slug. */
         export const EVM_CHAIN_ID_TO_NETWORK: ReadonlyMap<number, EvmNetworkType> = new Map(
           (Object.entries(EVM_CHAIN_MAP) as [EvmNetworkType, EvmChainEntry][])
             .map(([network, entry]) => [entry.chainId, network])
         );

         /**
          * Resolve EvmNetworkType from EVM chainId number.
          * @returns EvmNetworkType slug or undefined if chainId is not registered.
          */
         export function getNetworkByChainId(chainId: number): EvmNetworkType | undefined {
           return EVM_CHAIN_ID_TO_NETWORK.get(chainId);
         }
         ```
       - `packages/adapters/evm/src/index.ts`에 `EVM_CHAIN_ID_TO_NETWORK`, `getNetworkByChainId` export 추가.

    4. **packages/adapters/evm/src/__tests__/evm-chain-map.test.ts**: 역방향 맵 테스트 추가:
       - 모든 12 chainId가 올바른 네트워크로 매핑되는지 확인.
       - 미등록 chainId가 undefined 반환하는지 확인.
       - EVM_CHAIN_MAP과 EVM_CHAIN_ID_TO_NETWORK 크기 일치 확인.

    5. **packages/daemon/src/__tests__/migration-v58.test.ts**: 마이그레이션 테스트:
       - v57 상태 DB 생성 후 v58 적용.
       - CONTRACT_DEPLOY type INSERT 성공 확인.
       - toAddress=NULL INSERT 성공 확인.
       - 기존 데이터 보존 확인.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm -F @waiaas/daemon test -- --run -t "migration.*v58|evm-chain-map" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - LATEST_SCHEMA_VERSION = 58
    - v58 마이그레이션이 transactions type CHECK에 CONTRACT_DEPLOY 포함
    - getNetworkByChainId() 함수가 12개 chainId를 올바르게 역조회
    - 미등록 chainId에 대해 undefined 반환
    - 마이그레이션 테스트 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: keepAliveTimeout 600s 설정</name>
  <files>
    packages/daemon/src/lifecycle/daemon.ts
  </files>
  <action>
    1. **packages/daemon/src/lifecycle/daemon.ts** Step 5에서 `serve()` 반환값(`http.Server`) 설정:
       `@hono/node-server`의 `serve()`는 Node.js `http.Server`를 반환한다. 기존 코드 (line ~1528-1533):
       ```typescript
       const server = serve({ fetch: app.fetch, hostname, port });
       this.httpServer = server;
       ```
       이 블록 바로 아래(promise 대기 전)에 추가:
       ```typescript
       // v31.14: Long-poll RPC proxy support — keep connections alive for 10 minutes
       // Default Node.js keepAliveTimeout is 5s, which is too short for DELAY/APPROVAL tier
       // long-poll responses that can take up to 600s.
       server.keepAliveTimeout = 600_000; // 600 seconds in milliseconds
       server.headersTimeout = 605_000;   // Must be > keepAliveTimeout (Node.js docs)
       ```
       `headersTimeout`은 `keepAliveTimeout`보다 약간 커야 Node.js 내부 레이스 컨디션을 방지한다 (Node.js 공식 문서 권장).

    2. 상수화 고려: `600_000`을 `RPC_PROXY_KEEP_ALIVE_MS` 같은 상수로 추출할 수도 있으나, 한 곳에서만 사용하므로 인라인으로 충분. 향후 Admin Settings에서 조절 가능하도록 Phase 401에서 처리.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -n "keepAliveTimeout" packages/daemon/src/lifecycle/daemon.ts</automated>
  </verify>
  <done>
    - daemon.ts에 server.keepAliveTimeout = 600_000 설정
    - server.headersTimeout = 605_000 설정
    - Long-poll 연결이 600초 동안 유지 가능
  </done>
</task>

</tasks>

<verification>
1. `pnpm -F @waiaas/daemon test -- --run -t "migration"` — 마이그레이션 테스트 통과
2. `pnpm -F @waiaas/evm-adapter test -- --run` — evm-chain-map 테스트 통과
3. `grep -c "keepAliveTimeout" packages/daemon/src/lifecycle/daemon.ts` — 1+ hit
4. `grep -c "getNetworkByChainId" packages/adapters/evm/src/evm-chain-map.ts` — 1+ hit
5. `grep -c "version: 58" packages/daemon/src/infrastructure/database/migrate.ts` — 1 hit
</verification>

<success_criteria>
- DB v58 마이그레이션이 transactions type CHECK에 CONTRACT_DEPLOY 포함
- toAddress nullable INSERT 동작 확인 (이미 nullable이지만 테스트로 검증)
- EVM_CHAIN_ID_TO_NETWORK가 12개 chainId를 역조회 가능
- getNetworkByChainId(99999) === undefined
- keepAliveTimeout이 600s로 설정됨
- headersTimeout이 605s로 설정됨
</success_criteria>

<output>
After completion, create `.planning/phases/398-type-system-infra-foundation/398-02-SUMMARY.md`
</output>
