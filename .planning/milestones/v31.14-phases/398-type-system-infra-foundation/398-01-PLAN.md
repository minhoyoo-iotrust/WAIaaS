---
phase: 398-type-system-infra-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/enums/transaction.ts
  - packages/core/src/schemas/transaction.schema.ts
  - packages/daemon/src/pipeline/stages.ts
  - packages/daemon/src/pipeline/sign-only.ts
  - packages/daemon/src/pipeline/resolve-effective-amount-usd.ts
  - packages/daemon/src/pipeline/database-policy-engine.ts
  - scripts/verify-enum-ssot.ts
  - packages/core/src/__tests__/enums.test.ts
  - packages/daemon/src/__tests__/enum-db-consistency.test.ts
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-04
  - DEPL-05
  - DEPL-06

must_haves:
  truths:
    - "TRANSACTION_TYPES 배열이 9개 요소를 포함하고 CONTRACT_DEPLOY가 마지막에 위치한다"
    - "TransactionRequestSchema discriminatedUnion이 7-type으로 확장되어 CONTRACT_DEPLOY를 포함한다"
    - "to=null인 요청이 Stage 1에서 CONTRACT_DEPLOY 타입으로 분류되어 DB에 기록된다"
    - "CONTRACT_DEPLOY 타입이 정책 엔진에서 기본 APPROVAL 티어로 평가된다"
    - "pnpm verify:enums가 expectedCount=9로 통과한다"
  artifacts:
    - path: "packages/core/src/enums/transaction.ts"
      provides: "9-type TRANSACTION_TYPES array with CONTRACT_DEPLOY"
      contains: "CONTRACT_DEPLOY"
    - path: "packages/core/src/schemas/transaction.schema.ts"
      provides: "ContractDeployRequestSchema + 7-type discriminatedUnion"
      exports: ["ContractDeployRequestSchema", "TransactionRequestSchema"]
    - path: "packages/daemon/src/pipeline/database-policy-engine.ts"
      provides: "CONTRACT_DEPLOY default APPROVAL tier policy"
      contains: "CONTRACT_DEPLOY"
  key_links:
    - from: "packages/core/src/enums/transaction.ts"
      to: "packages/core/src/schemas/transaction.schema.ts"
      via: "TransactionTypeEnum import"
      pattern: "TransactionTypeEnum"
    - from: "packages/daemon/src/pipeline/stages.ts"
      to: "packages/core/src/schemas/transaction.schema.ts"
      via: "ContractDeployRequest type import"
      pattern: "ContractDeployRequest"
    - from: "packages/daemon/src/pipeline/database-policy-engine.ts"
      to: "packages/core/src/enums/transaction.ts"
      via: "CONTRACT_DEPLOY string literal comparison"
      pattern: "CONTRACT_DEPLOY"
---

<objective>
CONTRACT_DEPLOY를 9번째 트랜잭션 타입으로 Zod SSoT 전체 체인에 전파하고, 파이프라인 switch/case와 정책 엔진에 통합한다.

Purpose: EVM RPC 프록시에서 `eth_sendTransaction(to=null)`을 CONTRACT_DEPLOY로 분류하려면 먼저 타입 시스템이 준비되어야 한다. 이 plan이 Phase 399의 RpcTransactionAdapter가 의존하는 타입 기반을 제공한다.
Output: 9-type enum, 7-type discriminatedUnion schema, pipeline switch/case, APPROVAL 기본 정책
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/core/src/enums/transaction.ts
@packages/core/src/schemas/transaction.schema.ts
@packages/daemon/src/pipeline/stages.ts
@packages/daemon/src/pipeline/database-policy-engine.ts
@scripts/verify-enum-ssot.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/core/src/enums/transaction.ts:
```typescript
export const TRANSACTION_TYPES = [
  'TRANSFER', 'TOKEN_TRANSFER', 'CONTRACT_CALL', 'APPROVE',
  'BATCH', 'NFT_TRANSFER', 'SIGN', 'X402_PAYMENT',
] as const;
export type TransactionType = (typeof TRANSACTION_TYPES)[number];
export const TransactionTypeEnum = z.enum(TRANSACTION_TYPES);
```

From packages/core/src/schemas/transaction.schema.ts:
```typescript
export const TransactionRequestSchema = z.discriminatedUnion('type', [
  TransferRequestSchema, TokenTransferRequestSchema, ContractCallRequestSchema,
  ApproveRequestSchema, BatchRequestSchema, NftTransferRequestSchema,
]);
// Note: SIGN and X402_PAYMENT are NOT in discriminatedUnion (they use separate endpoints)
```

From packages/daemon/src/pipeline/stages.ts (Stage 1):
```typescript
const txType = ('type' in req && req.type) ? req.type : 'TRANSFER';
const toAddress = 'to' in req ? (req as { to?: string }).to : undefined;
// toAddress is already nullable in DB INSERT
```

From packages/daemon/src/pipeline/database-policy-engine.ts:
```typescript
// NFT_TRANSFER defaults to APPROVAL tier (line 374-396)
if (transaction.type === 'NFT_TRANSFER') { ... return { allowed: true, tier: 'APPROVAL' }; }
// Default: INSTANT passthrough (line 449-453)
const defaultTier = 'INSTANT' as PolicyTier;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CONTRACT_DEPLOY enum + Zod schema + discriminatedUnion 확장</name>
  <files>
    packages/core/src/enums/transaction.ts,
    packages/core/src/schemas/transaction.schema.ts,
    packages/core/src/__tests__/enums.test.ts,
    packages/core/src/__tests__/schemas.test.ts,
    scripts/verify-enum-ssot.ts
  </files>
  <behavior>
    - Test 1: TRANSACTION_TYPES.length === 9 and includes 'CONTRACT_DEPLOY'
    - Test 2: TransactionTypeEnum.parse('CONTRACT_DEPLOY') succeeds
    - Test 3: ContractDeployRequestSchema.parse({ type: 'CONTRACT_DEPLOY', bytecode: '0x6080...', value: '0' }) succeeds
    - Test 4: ContractDeployRequestSchema.parse({ type: 'CONTRACT_DEPLOY' }) fails (bytecode required)
    - Test 5: TransactionRequestSchema.parse({ type: 'CONTRACT_DEPLOY', bytecode: '0x6080...' }) succeeds (7-type union)
    - Test 6: TransactionSchema.parse with toAddress: null succeeds (toAddress becomes nullable)
  </behavior>
  <action>
    1. **packages/core/src/enums/transaction.ts**: TRANSACTION_TYPES 배열에 'CONTRACT_DEPLOY'를 9번째로 추가 (SIGN, X402_PAYMENT 뒤).

    2. **packages/core/src/schemas/transaction.schema.ts**:
       - `ContractDeployRequestSchema` 추가:
         ```typescript
         export const ContractDeployRequestSchema = z.object({
           type: z.literal('CONTRACT_DEPLOY'),
           bytecode: z.string().min(1), // hex-encoded contract creation bytecode (0x-prefixed)
           constructorArgs: z.string().optional(), // ABI-encoded constructor args (hex)
           value: z.string().regex(numericStringPattern).optional(), // payable constructor ETH value
           network: NetworkTypeEnumWithLegacy.optional(),
           ...gasConditionField,
         });
         export type ContractDeployRequest = z.infer<typeof ContractDeployRequestSchema>;
         ```
       - `TransactionRequestSchema` discriminatedUnion에 `ContractDeployRequestSchema` 추가 (7-type).
       - `TransactionSchema`의 `toAddress` 필드를 `z.string().nullable()`로 변경 (현재 `z.string()`).
       - `ContractDeployRequestSchema` export를 packages/core/src/schemas/index.ts와 packages/core/src/index.ts에 추가.

    3. **scripts/verify-enum-ssot.ts**: TransactionType expectedCount를 8에서 9로 업데이트.

    4. **packages/core/src/__tests__/enums.test.ts**: CONTRACT_DEPLOY 관련 assertion 추가.

    5. 기존 테스트가 깨지지 않도록 import/export 확인.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm -F @waiaas/core test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TRANSACTION_TYPES 배열이 9개 요소 포함
    - ContractDeployRequestSchema가 export되고 parse 동작
    - TransactionRequestSchema가 7-type discriminatedUnion
    - TransactionSchema.toAddress가 nullable
    - verify:enums 스크립트 expectedCount=9
  </done>
</task>

<task type="auto">
  <name>Task 2: Pipeline switch/case + 정책 엔진 CONTRACT_DEPLOY 통합</name>
  <files>
    packages/daemon/src/pipeline/stages.ts,
    packages/daemon/src/pipeline/sign-only.ts,
    packages/daemon/src/pipeline/resolve-effective-amount-usd.ts,
    packages/daemon/src/pipeline/database-policy-engine.ts,
    packages/daemon/src/__tests__/enum-db-consistency.test.ts
  </files>
  <action>
    1. **packages/daemon/src/pipeline/stages.ts**:
       - `import { ..., ContractDeployRequest } from '@waiaas/core'` 추가.
       - `buildTransactionParam()` 함수 (line ~318)에 `case 'CONTRACT_DEPLOY'` 추가:
         ```typescript
         case 'CONTRACT_DEPLOY': {
           const r = req as ContractDeployRequest;
           return {
             type: 'CONTRACT_DEPLOY',
             to: '', // no recipient for contract deployment
             amount: r.value ?? '0',
             bytecode: r.bytecode,
             constructorArgs: r.constructorArgs,
           };
         }
         ```
       - `buildUnsignedTx()` 함수 (line ~988)에 `case 'CONTRACT_DEPLOY'` 추가:
         ```typescript
         case 'CONTRACT_DEPLOY': {
           const req = request as ContractDeployRequest;
           // Contract deployment: to=undefined, data=bytecode(+constructorArgs)
           const deployData = req.constructorArgs
             ? req.bytecode + req.constructorArgs.replace(/^0x/, '')
             : req.bytecode;
           return adapter.buildContractCall({
             from: walletPublicKey,
             to: '', // adapter will handle to=undefined for deploy
             calldata: deployData,
             value: req.value ?? '0',
             network: ctx.resolvedNetwork,
           });
         }
         ```
         주의: EvmAdapter.buildContractCall은 `to=''`일 때 viem `to: undefined`로 변환 필요. 이 변환은 Phase 399에서 RpcTransactionAdapter와 함께 처리. 현재 Plan에서는 switch/case 스켈레톤만 추가.
       - `buildSmartAccountCalls()` 함수 (line ~1183)에 `case 'CONTRACT_DEPLOY'` 추가:
         ```typescript
         case 'CONTRACT_DEPLOY': {
           const req = request as ContractDeployRequest;
           const deployData = req.constructorArgs
             ? req.bytecode + req.constructorArgs.replace(/^0x/, '')
             : req.bytecode;
           return [{
             data: deployData as Hex,
             value: BigInt(req.value ?? '0'),
           }];
         }
         ```
       - Stage 5 확인 후(line ~850+) 성공 시 `deployedAddress`를 metadata에 저장하는 로직 추가:
         CONTRACT_DEPLOY 완료 시 receipt에서 `contractAddress`를 추출하여 `tx_history.metadata`에 `{ ..., deployedAddress }` 저장 (DEPL-05).
         ```typescript
         // After successful submission, if CONTRACT_DEPLOY, store deployedAddress
         if (txType === 'CONTRACT_DEPLOY' && submitResult.txHash) {
           // deployedAddress will be populated by confirmation stage from receipt
           // Store placeholder in metadata for now; confirmation updates it
         }
         ```
       - Stage 5 감사 로그에 bytecodeHash(keccak256) 기록 (DEPL-06):
         CONTRACT_DEPLOY일 때 `keccak256(bytecode)` 계산하여 audit log metadata에 포함.

    2. **packages/daemon/src/pipeline/sign-only.ts**: sign-only에는 CONTRACT_DEPLOY가 일반적으로 사용되지 않지만, switch에 exhaustive case가 있다면 추가 (없으면 skip).

    3. **packages/daemon/src/pipeline/resolve-effective-amount-usd.ts**: CONTRACT_DEPLOY의 amount는 `value` 필드 (payable constructor). switch에 case가 있다면 `case 'CONTRACT_DEPLOY'`를 CONTRACT_CALL과 동일하게 처리.

    4. **packages/daemon/src/pipeline/database-policy-engine.ts**:
       - NFT_TRANSFER default APPROVAL 블록 (line ~374) 아래에 CONTRACT_DEPLOY default APPROVAL 추가:
         ```typescript
         // v31.14: CONTRACT_DEPLOY default tier APPROVAL (DEPL-04)
         // Contract deployments default to APPROVAL tier (owner approval required) unless overridden.
         if (transaction.type === 'CONTRACT_DEPLOY') {
           let deployTierOverride: string | null = null;
           try {
             deployTierOverride = ctx.settingsService?.get('rpc_proxy.deploy_default_tier') ?? null;
           } catch { /* settings unavailable, use default */ }
           return {
             allowed: true,
             tier: (deployTierOverride as PolicyTier) ?? 'APPROVAL',
           };
         }
         ```

    5. **packages/daemon/src/__tests__/enum-db-consistency.test.ts**: TRANSACTION_TYPES count assertion을 9로 업데이트.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm -F @waiaas/daemon test -- --run --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Pipeline stages.ts에 CONTRACT_DEPLOY case 3개소 (buildTransactionParam, buildUnsignedTx, buildSmartAccountCalls) 추가
    - database-policy-engine.ts에 CONTRACT_DEPLOY 기본 APPROVAL 티어 정책 추가
    - deployedAddress metadata 저장 로직 스켈레톤 추가 (DEPL-05)
    - bytecodeHash audit log 기록 추가 (DEPL-06)
    - 기존 테스트 전체 통과
  </done>
</task>

</tasks>

<verification>
1. `pnpm verify:enums` — 16 enums, expectedCount TransactionType=9
2. `pnpm -F @waiaas/core test -- --run` — core 패키지 테스트 통과
3. `pnpm -F @waiaas/daemon test -- --run` — daemon 패키지 테스트 통과
4. `grep -c CONTRACT_DEPLOY packages/core/src/enums/transaction.ts` — 1 hit
5. `grep -c CONTRACT_DEPLOY packages/daemon/src/pipeline/stages.ts` — 3+ hits
6. `grep -c CONTRACT_DEPLOY packages/daemon/src/pipeline/database-policy-engine.ts` — 2+ hits
</verification>

<success_criteria>
- TRANSACTION_TYPES에 CONTRACT_DEPLOY 추가 (9개)
- ContractDeployRequestSchema가 bytecode 필수 필드로 정의됨
- TransactionRequestSchema가 7-type discriminatedUnion
- Pipeline switch/case에 CONTRACT_DEPLOY case 존재
- 정책 엔진에서 CONTRACT_DEPLOY가 기본 APPROVAL 티어
- pnpm verify:enums 통과
- 기존 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/398-type-system-infra-foundation/398-01-SUMMARY.md`
</output>
