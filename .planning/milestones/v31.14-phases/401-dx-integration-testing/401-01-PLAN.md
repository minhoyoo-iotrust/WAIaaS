---
phase: 401-dx-integration-testing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/admin/src/pages/rpc-proxy.tsx
  - packages/admin/src/components/layout.tsx
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06]

must_haves:
  truths:
    - "Admin can toggle rpc_proxy.enabled to activate/deactivate RPC proxy at runtime"
    - "Admin can configure delay_timeout, approval_timeout, max_gas_limit, allowed_methods via Settings API"
    - "Admin UI shows RPC Proxy page with enable toggle, settings form, and recent request log from audit logs"
  artifacts:
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "6 rpc_proxy.* setting definitions in SETTING_DEFINITIONS + rpc_proxy category"
      contains: "rpc_proxy.enabled"
    - path: "packages/admin/src/pages/rpc-proxy.tsx"
      provides: "Admin UI RPC Proxy page with status, settings, and audit log"
      min_lines: 80
    - path: "packages/admin/src/components/layout.tsx"
      provides: "RPC Proxy nav item and page route"
      contains: "rpc-proxy"
  key_links:
    - from: "packages/admin/src/pages/rpc-proxy.tsx"
      to: "/v1/admin/settings"
      via: "fetch GET/PUT for rpc_proxy.* keys"
      pattern: "rpc_proxy"
    - from: "packages/daemon/src/api/routes/rpc-proxy.ts"
      to: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      via: "settingsService.get('rpc_proxy.enabled')"
      pattern: "rpc_proxy\\.enabled"
---

<objective>
Register 6 rpc_proxy.* settings in the SettingsService SSoT and create an Admin UI page for RPC Proxy management.

Purpose: Allow runtime control of the RPC proxy feature via Admin Settings API and provide a visual management interface.
Output: Setting definitions in setting-keys.ts, Admin UI RPC Proxy page with toggle/settings/audit log.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/400-route-assembly-async-approval/400-01-SUMMARY.md

<interfaces>
<!-- Existing rpc_proxy.* keys already used in daemon code (but NOT in setting-keys.ts yet): -->
From packages/daemon/src/api/routes/rpc-proxy.ts:
  settingsService?.get('rpc_proxy.enabled')       // returns 'true'/'false'
  settingsService?.get('rpc_proxy.max_bytecode_size')  // returns string number (bytes)

From packages/daemon/src/rpc-proxy/sync-pipeline.ts:
  settingsService?.get('rpc_proxy.delay_timeout_seconds')    // returns string number
  settingsService?.get('rpc_proxy.approval_timeout_seconds') // returns string number

From packages/daemon/src/pipeline/database-policy-engine.ts:
  settingsService?.get('rpc_proxy.deploy_default_tier')      // returns 'IMMEDIATE'|'DELAY'|'APPROVAL'|null

<!-- SettingDefinition interface from setting-keys.ts: -->
interface SettingDefinition {
  key: string;
  category: string;
  configPath: string;
  defaultValue: string;
  isCredential: boolean;
}

<!-- SETTING_CATEGORIES includes categories like 'rpc', 'security', 'actions', etc. -->
<!-- Add 'rpc_proxy' as new category. -->

<!-- Admin UI pattern: Preact 10.x + @preact/signals + Vite, pages in packages/admin/src/pages/*.tsx -->
<!-- Layout nav registration: packages/admin/src/components/layout.tsx (NAV_ITEMS, PAGE_TITLES, PAGE_SUBTITLES, PageRouter) -->
<!-- Admin API: GET /v1/admin/settings?category=rpc_proxy, PUT /v1/admin/settings { key, value } -->
<!-- Audit logs: GET /v1/admin/audit-logs?source=rpc-proxy -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 6 rpc_proxy.* settings in setting-keys.ts</name>
  <files>packages/daemon/src/infrastructure/settings/setting-keys.ts</files>
  <action>
Add 'rpc_proxy' to SETTING_CATEGORIES array (after 'erc8128').

Add 6 setting definitions to SETTING_DEFINITIONS array in a new "--- rpc_proxy category ---" section (after erc8128 block):

1. `rpc_proxy.enabled` — category: 'rpc_proxy', configPath: 'rpc_proxy.enabled', defaultValue: 'false', isCredential: false
2. `rpc_proxy.allowed_methods` — category: 'rpc_proxy', configPath: 'rpc_proxy.allowed_methods', defaultValue: '[]', isCredential: false
   (JSON array of method names. Empty = all supported methods allowed. Example: '["eth_sendTransaction","eth_call"]')
3. `rpc_proxy.delay_timeout_seconds` — category: 'rpc_proxy', configPath: 'rpc_proxy.delay_timeout_seconds', defaultValue: '300', isCredential: false
4. `rpc_proxy.approval_timeout_seconds` — category: 'rpc_proxy', configPath: 'rpc_proxy.approval_timeout_seconds', defaultValue: '600', isCredential: false
5. `rpc_proxy.max_gas_limit` — category: 'rpc_proxy', configPath: 'rpc_proxy.max_gas_limit', defaultValue: '30000000', isCredential: false
6. `rpc_proxy.max_bytecode_size` — category: 'rpc_proxy', configPath: 'rpc_proxy.max_bytecode_size', defaultValue: '49152', isCredential: false
   (49152 = 48 * 1024 bytes, matching existing default)

Also add deploy_default_tier as a 7th key since it's already used in database-policy-engine.ts:
7. `rpc_proxy.deploy_default_tier` — category: 'rpc_proxy', configPath: 'rpc_proxy.deploy_default_tier', defaultValue: 'APPROVAL', isCredential: false

This formalizes all rpc_proxy.* keys already used in code into the SSoT.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsx -e "const { SETTING_DEFINITIONS, SETTING_CATEGORIES } = require('./packages/daemon/src/infrastructure/settings/setting-keys.ts'); const rpcKeys = SETTING_DEFINITIONS.filter(d => d.category === 'rpc_proxy'); console.log('rpc_proxy keys:', rpcKeys.length); console.log('category exists:', SETTING_CATEGORIES.includes('rpc_proxy')); if (rpcKeys.length < 6 || !SETTING_CATEGORIES.includes('rpc_proxy')) process.exit(1);"</automated>
  </verify>
  <done>7 rpc_proxy.* setting definitions registered in SETTING_DEFINITIONS, 'rpc_proxy' in SETTING_CATEGORIES. Settings are now discoverable via Admin Settings API (GET /v1/admin/settings?category=rpc_proxy).</done>
</task>

<task type="auto">
  <name>Task 2: Admin UI RPC Proxy page + layout registration</name>
  <files>packages/admin/src/pages/rpc-proxy.tsx, packages/admin/src/components/layout.tsx</files>
  <action>
Create `packages/admin/src/pages/rpc-proxy.tsx` following the Preact + @preact/signals pattern used by other pages (e.g., security.tsx, system.tsx). The page should have:

1. **Status section**: Show whether RPC proxy is enabled/disabled with a toggle button (PUT /v1/admin/settings with key=rpc_proxy.enabled).

2. **Settings form**: Display configurable settings:
   - Delay timeout (seconds) — input number
   - Approval timeout (seconds) — input number
   - Max gas limit — input number
   - Max bytecode size (bytes) — input number
   - Deploy default tier — select (IMMEDIATE/DELAY/APPROVAL)
   - Allowed methods — textarea (JSON array, empty = all)
   Use existing admin page patterns: fetch settings from GET /v1/admin/settings?category=rpc_proxy, save via PUT /v1/admin/settings.

3. **Usage info section**: Display the RPC proxy URL pattern: `{baseUrl}/v1/rpc-evm/{walletId}/{chainId}`. Brief usage instructions for Forge/Hardhat/viem.

4. **Recent activity section**: Fetch recent audit logs filtered by source=rpc-proxy (GET /v1/admin/audit-logs?source=rpc-proxy&limit=20). Show table with timestamp, method, walletId, status.

Follow existing Admin UI styling conventions (CSS variables, card layout, form elements from styles/).

Update `packages/admin/src/components/layout.tsx`:
- Import RpcProxyPage from '../pages/rpc-proxy'
- Add to NAV_ITEMS: `{ path: '/rpc-proxy', label: 'RPC Proxy' }` (after 'Credentials' entry)
- Add to PAGE_TITLES: `'/rpc-proxy': 'RPC Proxy'`
- Add to PAGE_SUBTITLES: `'/rpc-proxy': 'EVM JSON-RPC proxy settings and monitoring'`
- Add to PageRouter: `if (path === '/rpc-proxy') return <RpcProxyPage />;`
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run build --filter=@waiaas/admin 2>&1 | tail -5</automated>
  </verify>
  <done>Admin UI has RPC Proxy page accessible at #/rpc-proxy with enable toggle, settings form, URL pattern info, and recent audit log table. Navigation sidebar shows "RPC Proxy" link.</done>
</task>

</tasks>

<verification>
- `pnpm turbo run build --filter=@waiaas/admin` succeeds
- `pnpm turbo run typecheck --filter=@waiaas/daemon` passes (setting-keys.ts valid)
- 7 rpc_proxy.* settings accessible via Admin Settings API
</verification>

<success_criteria>
1. SETTING_DEFINITIONS contains 7 rpc_proxy.* entries with correct defaults
2. Admin UI builds without errors and shows RPC Proxy page in sidebar
3. RPC Proxy page has enable toggle, settings form, usage info, and audit log section
</success_criteria>

<output>
After completion, create `.planning/phases/401-dx-integration-testing/401-01-SUMMARY.md`
</output>
