---
phase: 368-advanced-admin-scenarios
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-uat/admin/nft-tab.md
  - agent-uat/admin/defi-positions.md
  - agent-uat/admin/notifications.md
  - agent-uat/admin/audit-logs.md
  - agent-uat/admin/backup-restore.md
  - agent-uat/admin/token-registry.md
  - agent-uat/admin/stats-monitoring.md
  - agent-uat/_index.md
autonomous: true
requirements: ["ADMIN-07", "ADMIN-08", "ADMIN-09", "ADMIN-10", "ADMIN-11", "ADMIN-12", "ADMIN-13"]

must_haves:
  truths:
    - "NFT 탭 시나리오가 온체인 NFT 보유 현황과 Admin 표시를 비교한다"
    - "DeFi 포지션 탭 시나리오가 온체인 포지션과 Admin 표시를 비교한다"
    - "알림 설정+수신 시나리오가 실제 알림 전송을 확인한다"
    - "감사 로그 시나리오가 실행 작업의 정확한 기록을 확인한다"
    - "백업/복원 시나리오가 데이터 무결성을 검증한다"
    - "토큰 레지스트리 시나리오가 커스텀 토큰 등록 후 잔액 반영을 확인한다"
    - "통계/모니터링 시나리오가 Stats API 정확성을 확인한다"
  artifacts:
    - path: "agent-uat/admin/nft-tab.md"
      provides: "NFT tab verification scenario"
      contains: "## Scenario Steps"
    - path: "agent-uat/admin/defi-positions.md"
      provides: "DeFi positions tab scenario"
      contains: "## Scenario Steps"
    - path: "agent-uat/admin/notifications.md"
      provides: "Notification settings and delivery scenario"
      contains: "## Scenario Steps"
    - path: "agent-uat/admin/audit-logs.md"
      provides: "Audit log accuracy scenario"
      contains: "## Scenario Steps"
    - path: "agent-uat/admin/backup-restore.md"
      provides: "Backup/restore integrity scenario"
      contains: "## Scenario Steps"
    - path: "agent-uat/admin/token-registry.md"
      provides: "Token registry scenario"
      contains: "## Scenario Steps"
    - path: "agent-uat/admin/stats-monitoring.md"
      provides: "Stats/monitoring API accuracy scenario"
      contains: "## Scenario Steps"
    - path: "agent-uat/_index.md"
      provides: "Updated scenario index with admin + advanced entries"
      contains: "### Admin"
  key_links:
    - from: "agent-uat/_index.md"
      to: "agent-uat/admin/*.md"
      via: "index registration"
      pattern: "admin-\\d+"
    - from: "agent-uat/_index.md"
      to: "agent-uat/advanced/*.md"
      via: "index registration"
      pattern: "advanced-\\d+"
---

<objective>
Admin UI 검증 Agent UAT 시나리오 후반부 7개를 작성하고, _index.md를 업데이트하여 admin + advanced 카테고리 전체를 등록한다.

Purpose: Admin UI의 나머지 기능(NFT, DeFi, 알림, 감사로그, 백업, 토큰, 통계)을 에이전트가 검증할 수 있는 시나리오를 완비하고, 인덱스를 최신화한다.
Output: `agent-uat/admin/`에 7개 시나리오 + `_index.md` 업데이트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agent-uat/README.md
@agent-uat/_template.md
@agent-uat/_index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin UI 시나리오 4개 작성 (NFT, DeFi, 알림, 감사로그)</name>
  <files>agent-uat/admin/nft-tab.md, agent-uat/admin/defi-positions.md, agent-uat/admin/notifications.md, agent-uat/admin/audit-logs.md</files>
  <action>
표준 시나리오 포맷으로 4개 Admin UI 후반부 시나리오를 작성한다.

**nft-tab.md** (id: admin-07):
- frontmatter: category=admin, network=[ethereum-mainnet, solana-mainnet], requires_funds=false, estimated_cost_usd=0, risk_level=none, tags=[admin, nft, tab, onchain, indexer]
- Prerequisites: 데몬 실행, masterAuth 인증, NFT 보유 지갑 (없으면 SKIP 가능)
- Scenario Steps:
  1. NFT 인덱서 상태 확인 (Alchemy/Helius 설정 여부)
  2. 지갑별 NFT 조회 API (GET /v1/wallets/<ID>/nfts?network=<NETWORK>)
  3. Admin UI NFT 탭 데이터와 API 조회 결과 비교
  4. NFT 메타데이터 확인 (이름, 컬렉션, 이미지 URL, CAIP-19 식별자)
  5. 페이지네이션 동작 확인 (보유 NFT 다수인 경우)
- NOTE: NFT 미보유 시 빈 목록이 정상적으로 표시되는지 확인한다.
- Verification: API 조회 결과와 Admin 표시 일치, 메타데이터 정확, CAIP-19 형식 준수
- Estimated Cost: $0
- Troubleshooting: 인덱서 미설정, 캐시 만료(24h TTL), 네트워크별 인덱서 불일치

**defi-positions.md** (id: admin-08):
- frontmatter: category=admin, network=[ethereum-mainnet, solana-mainnet], requires_funds=false, estimated_cost_usd=0, risk_level=none, tags=[admin, defi, positions, lending, staking]
- Prerequisites: 데몬 실행, masterAuth 인증, DeFi 포지션 보유 (Aave/Kamino/Lido 등, 없으면 SKIP 가능)
- Scenario Steps:
  1. 지갑별 DeFi 포지션 조회 (관련 API 엔드포인트)
  2. Admin UI DeFi 탭 표시 데이터 확인
  3. 프로토콜별 포지션 상세 확인 (supply amount, borrow amount, health factor 등)
  4. 온체인 실제 포지션과 Admin 표시 비교 (해당 프로토콜 API로 직접 확인)
  5. 포지션 미보유 시 빈 상태 표시 확인
- NOTE: DeFi 포지션은 실시간 변동(이자 누적)이 있으므로 +-1% 오차를 허용한다.
- Verification: 프로토콜별 포지션 일치(오차 1% 이내), 빈 상태 정상 표시
- Estimated Cost: $0
- Troubleshooting: 포지션 미보유, 프로토콜 API 오류, 이자 누적에 의한 미세 차이

**notifications.md** (id: admin-09):
- frontmatter: category=admin, network=[all], requires_funds=true, estimated_cost_usd=0.01, risk_level=low, tags=[admin, notification, ntfy, alert, channel]
- Prerequisites: 데몬 실행, masterAuth 인증, ntfy 또는 다른 알림 채널 설정 가능, Testnet 지갑(Sepolia/Devnet) 보유
- Scenario Steps:
  1. 현재 알림 설정 조회 (GET /v1/admin/settings, notification 관련 설정)
  2. ntfy 채널 설정 (PUT /v1/admin/settings, ntfy topic 설정) -- 이미 설정 시 SKIP
  3. 지갑별 알림 토픽 확인 (wallet_apps.sign_topic/notify_topic)
  4. 알림 발생 트리거 -- Testnet 자기 전송 실행 (POST /v1/transactions, Sepolia 0.001 ETH)
  5. 알림 수신 확인 (ntfy 채널에서 트랜잭션 알림 메시지 확인)
  6. 알림 이벤트 유형 확인 (transaction_sent, incoming_tx 등)
- Verification: 알림 채널 설정 성공, 트랜잭션 후 알림 수신, 이벤트 유형 정확
- Estimated Cost: Sepolia gas ~$0.01
- Troubleshooting: ntfy 서버 미접근, 토픽 미설정, 글로벌 NtfyChannel 미지원(per-wallet 방식)

**audit-logs.md** (id: admin-10):
- frontmatter: category=admin, network=[all], requires_funds=false, estimated_cost_usd=0, risk_level=none, tags=[admin, audit, logs, compliance]
- Prerequisites: 데몬 실행, masterAuth 인증, 1건 이상 작업 수행 이력
- Scenario Steps:
  1. 감사 로그 전체 조회 (GET /v1/admin/audit-logs, masterAuth)
  2. 최근 수행한 작업 식별 (트랜잭션, 설정 변경, 정책 변경 등)
  3. 각 작업에 대한 감사 로그 존재 확인
  4. 로그 상세 확인: timestamp, actor, action, resource, details
  5. 이벤트 필터링 확인 (GET /v1/admin/audit-logs?event=<TYPE>)
  6. 시간 범위 필터링 확인 (GET /v1/admin/audit-logs?from=<DATE>&to=<DATE>)
- Verification: 수행 작업에 대한 로그 존재, 필드 정확, 필터링 동작
- Estimated Cost: $0
- Troubleshooting: 감사 로그 비활성, 오래된 로그 삭제, 이벤트 타입 불일치
  </action>
  <verify>
    <automated>for f in agent-uat/admin/nft-tab.md agent-uat/admin/defi-positions.md agent-uat/admin/notifications.md agent-uat/admin/audit-logs.md; do echo "=== $f ==="; head -5 "$f"; echo "---"; grep -c "^## " "$f"; done</automated>
  </verify>
  <done>4개 Admin UI 후반부 시나리오 파일이 agent-uat/admin/에 존재하고, 각각 6개 필수 섹션을 포함한다</done>
</task>

<task type="auto">
  <name>Task 2: Admin UI 시나리오 3개 작성 (백업, 토큰, 통계) + _index.md 업데이트</name>
  <files>agent-uat/admin/backup-restore.md, agent-uat/admin/token-registry.md, agent-uat/admin/stats-monitoring.md, agent-uat/_index.md</files>
  <action>
나머지 3개 Admin UI 시나리오를 작성하고 _index.md를 업데이트한다.

**backup-restore.md** (id: admin-11):
- frontmatter: category=admin, network=[all], requires_funds=false, estimated_cost_usd=0, risk_level=low, tags=[admin, backup, restore, integrity, encryption]
- Prerequisites: 데몬 실행, masterAuth 인증, CLI 접근 가능 (waiaas backup 커맨드)
- Scenario Steps:
  1. 현재 데이터 상태 기록 (지갑 수, 정책 수, 설정 값 스냅샷)
  2. 암호화 백업 생성 (POST /v1/admin/backup 또는 CLI `waiaas backup create`)
  3. 백업 파일 존재 확인 (파일 크기, 암호화 확인)
  4. 데이터 변경 시뮬레이션 (테스트 정책 추가 등 -- 비파괴적)
  5. 백업에서 복원 (POST /v1/admin/restore 또는 CLI `waiaas backup restore`)
  6. 복원 후 데이터 상태 비교 (지갑 수, 정책 수, 설정 값이 Step 1과 일치)
- Cleanup: Step 4에서 생성한 테스트 데이터는 복원으로 자동 삭제된다
- Verification: 백업 생성 성공, 복원 후 데이터 일치, 암호화 무결성
- Estimated Cost: $0
- Troubleshooting: 백업 파일 경로 오류, 암호화 키 불일치, 복원 중 데몬 재시작 필요

**token-registry.md** (id: admin-12):
- frontmatter: category=admin, network=[ethereum-mainnet], requires_funds=false, estimated_cost_usd=0, risk_level=none, tags=[admin, token, registry, custom, erc20]
- Prerequisites: 데몬 실행, masterAuth 인증, 등록할 커스텀 토큰의 contract address 준비
- Scenario Steps:
  1. 현재 토큰 레지스트리 조회 (GET /v1/admin/tokens 또는 /v1/tokens, masterAuth)
  2. 기본 등록 토큰 확인 (USDC, USDT, WETH 등)
  3. 커스텀 토큰 등록 (POST /v1/admin/tokens, contract address + network)
  4. 등록된 토큰 조회 확인 (GET /v1/admin/tokens, 새 토큰 포함 확인)
  5. 등록된 토큰으로 잔액 조회 (GET /v1/wallets/<ID>/tokens) -- 보유 시 잔액 표시, 미보유 시 0
  6. 테스트 토큰 삭제 (DELETE /v1/admin/tokens/<ID>)
- Cleanup: Step 6에서 테스트 토큰을 삭제한다
- Verification: 토큰 등록/조회/삭제 성공, 잔액 조회에 반영
- Estimated Cost: $0
- Troubleshooting: 잘못된 contract address, 지원하지 않는 네트워크, default-deny 정책과 충돌

**stats-monitoring.md** (id: admin-13):
- frontmatter: category=admin, network=[all], requires_funds=false, estimated_cost_usd=0, risk_level=none, tags=[admin, stats, monitoring, api, health]
- Prerequisites: 데몬 실행, masterAuth 인증
- Scenario Steps:
  1. Stats API 전체 조회 (GET /v1/admin/stats, masterAuth)
  2. 핵심 메트릭 확인: 총 지갑 수, 총 트랜잭션 수, 활성 세션 수, 업타임
  3. 실제 데이터와 비교 (GET /v1/wallets -> count, GET /v1/transactions -> count)
  4. Stats 지갑 수 == 실제 지갑 수 확인
  5. Stats 트랜잭션 수 == 실제 트랜잭션 수 확인
  6. 모니터링 상태 확인: IncomingTxMonitor, BalanceMonitor, RPC Pool 건강 상태
  7. AutoStop Plugin 상태 확인 (활성화 여부)
- Verification: Stats 메트릭이 실제 데이터와 일치, 모니터 상태 정확
- Estimated Cost: $0
- Troubleshooting: 통계 집계 지연, 캐시된 값, 모니터 비활성

**_index.md 업데이트**:
기존 _index.md를 읽고 다음을 추가한다:

1. Summary 테이블의 admin 카운트를 13으로, advanced 카운트를 6으로 업데이트
2. Advanced 카테고리 테이블 추가:
   | ID | Title | Network | Funds | Cost | Risk |
   advanced-01 ~ advanced-06 항목

3. Admin 카테고리 테이블 추가:
   | ID | Title | Network | Funds | Cost | Risk |
   admin-01 ~ admin-13 항목

4. Network Index에 new 시나리오 추가:
   - all: + admin-01, admin-02, admin-03, admin-04, admin-05, admin-09, admin-10, admin-11, admin-13
   - ethereum-mainnet: + advanced-04, advanced-05, advanced-06, admin-06, admin-07, admin-08, admin-12
   - solana-mainnet: + advanced-04, advanced-05, admin-06, admin-07, admin-08
   - ethereum-sepolia: + advanced-01, admin-09
   - base-mainnet: + advanced-03

5. Quick Filters 업데이트:
   - 무료: + admin-01, admin-02, admin-03, admin-05, admin-06, admin-07, admin-08, admin-10, admin-11, admin-12, admin-13, advanced-02, advanced-06
   - Low risk: + advanced-01, admin-04, admin-09, admin-11
   - Medium risk: + advanced-03, advanced-04, advanced-05
  </action>
  <verify>
    <automated>echo "=== backup ===" && head -5 agent-uat/admin/backup-restore.md && echo "=== token ===" && head -5 agent-uat/admin/token-registry.md && echo "=== stats ===" && head -5 agent-uat/admin/stats-monitoring.md && echo "=== index ===" && grep -c "admin-" agent-uat/_index.md && grep -c "advanced-" agent-uat/_index.md</automated>
  </verify>
  <done>3개 Admin UI 시나리오 + _index.md 업데이트 완료. _index.md에 admin-01~13, advanced-01~06이 모두 등록되어 있다.</done>
</task>

</tasks>

<verification>
- `ls agent-uat/admin/*.md | wc -l` == 13 (plan 02의 6개 + 이 plan의 7개)
- `ls agent-uat/advanced/*.md | wc -l` == 6
- `grep -c "admin-" agent-uat/_index.md` >= 13
- `grep -c "advanced-" agent-uat/_index.md` >= 6
- 각 파일에 6개 필수 섹션이 있다
</verification>

<success_criteria>
Admin UI 후반부 7개 시나리오가 agent-uat/admin/에 존재하고, _index.md가 admin(13개) + advanced(6개) 카테고리를 모두 포함하며, 전체 19개 시나리오가 표준 포맷을 따른다.
</success_criteria>

<output>
After completion, create `.planning/phases/368-advanced-admin-scenarios/368-03-SUMMARY.md`
</output>
