---
phase: 369-ci-scenario-enforcement
plan: 02
type: execute
wave: 2
depends_on: [369-01]
files_modified:
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-05]

must_haves:
  truths:
    - "pnpm verify:agent-uat 명령이 4개 검증 스크립트를 모두 실행한다"
    - "CI workflow에서 PR 시 verify:agent-uat 스텝이 실행되어 실패 시 PR 차단"
    - "push to main에서도 Stage 1에서 verify:agent-uat가 실행된다"
  artifacts:
    - path: "package.json"
      provides: "verify:agent-uat npm script"
      contains: "verify:agent-uat"
    - path: ".github/workflows/ci.yml"
      provides: "Agent UAT Coverage Check CI step"
      contains: "verify:agent-uat"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "pnpm verify:agent-uat"
      pattern: "pnpm verify:agent-uat"
    - from: "package.json"
      to: "scripts/verify-agent-uat-*.ts"
      via: "tsx script execution"
      pattern: "tsx scripts/verify-agent-uat"
---

<objective>
4개 검증 스크립트를 CI workflow에 통합하여 PR 시 자동 실행되도록 한다.

Purpose: 시나리오 누락/무효/미등록/Admin 라우트 불일치를 PR 단계에서 차단한다
Output: package.json에 통합 스크립트 등록 + ci.yml에 검증 스텝 추가
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/369-ci-scenario-enforcement/369-01-SUMMARY.md

<interfaces>
<!-- From 369-01: 4 verification scripts -->
scripts/verify-agent-uat-provider-map.ts  -- Provider -> scenario mapping check
scripts/verify-agent-uat-format.ts        -- Scenario file format validation
scripts/verify-agent-uat-index.ts         -- Index registration check (orphan detection)
scripts/verify-admin-route-consistency.ts -- Admin UI route/menu consistency

<!-- Existing pattern in package.json -->
"verify:e2e-coverage": "tsx scripts/verify-e2e-coverage.ts"

<!-- Existing CI pattern in ci.yml Stage 1 -->
- name: E2E Coverage Check
  run: pnpm verify:e2e-coverage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: package.json 스크립트 등록 + CI workflow 통합</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <action>
**1. package.json에 스크립트 등록:**

`scripts` 섹션에 아래 5개 스크립트를 추가한다 (기존 `verify:e2e-coverage` 근처에 배치):

```json
"verify:agent-uat": "tsx scripts/verify-agent-uat-provider-map.ts && tsx scripts/verify-agent-uat-format.ts && tsx scripts/verify-agent-uat-index.ts && tsx scripts/verify-admin-route-consistency.ts",
"verify:agent-uat:providers": "tsx scripts/verify-agent-uat-provider-map.ts",
"verify:agent-uat:format": "tsx scripts/verify-agent-uat-format.ts",
"verify:agent-uat:index": "tsx scripts/verify-agent-uat-index.ts",
"verify:admin-routes": "tsx scripts/verify-admin-route-consistency.ts"
```

`verify:agent-uat`는 4개를 `&&`로 체인하여 하나라도 실패하면 전체 실패.
개별 스크립트도 등록하여 디버깅 시 단독 실행 가능하게 한다.

**2. ci.yml Stage 1에 검증 스텝 추가:**

`E2E Coverage Check` 스텝 바로 아래에 추가:

```yaml
      - name: Agent UAT Coverage Check
        run: pnpm verify:agent-uat
```

Stage 1은 push + PR 모두 실행되므로 (if 조건 없음) 두 경우 모두 커버된다.
Stage 2 (PR only)에는 추가하지 않는다 -- Stage 1에서 이미 실행되고, 검증 스크립트는 빌드 불필요 (마크다운 파싱만).

**3. 통합 검증:**

`pnpm verify:agent-uat` 실행하여 4개 스크립트 모두 pass 확인.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm verify:agent-uat</automated>
  </verify>
  <done>pnpm verify:agent-uat가 4개 검증을 순서대로 실행하여 모두 pass하고, ci.yml Stage 1에 Agent UAT Coverage Check 스텝이 E2E Coverage Check 다음에 존재한다</done>
</task>

</tasks>

<verification>
1. `pnpm verify:agent-uat` 실행 시 4개 검증 모두 pass
2. `grep "Agent UAT" .github/workflows/ci.yml` 결과 존재 확인
3. `grep "verify:agent-uat" package.json` 결과 존재 확인
</verification>

<success_criteria>
- `pnpm verify:agent-uat` 명령이 현재 코드베이스에서 exit 0
- ci.yml Stage 1에 `Agent UAT Coverage Check` 스텝이 추가됨
- 개별 스크립트도 `pnpm verify:agent-uat:providers` 등으로 독립 실행 가능
</success_criteria>

<output>
After completion, create `.planning/phases/369-ci-scenario-enforcement/369-02-SUMMARY.md`
</output>
