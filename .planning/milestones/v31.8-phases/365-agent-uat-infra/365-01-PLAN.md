---
phase: 365-agent-uat-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-uat/README.md
  - agent-uat/_template.md
  - agent-uat/_index.md
  - agent-uat/testnet/wallet-crud.md
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-07]

must_haves:
  truths:
    - "시나리오 마크다운 파일이 표준 섹션(메타데이터, 사전 조건, 시나리오 단계, 검증 항목, 예상 비용, 실패 시 조치)을 갖추고 있다"
    - "_index.md에서 전체 시나리오를 카테고리별/네트워크별로 조회할 수 있다"
    - "지갑 CRUD 검증 시나리오가 생성->테스트->삭제를 원자적으로 묶어 기존 지갑을 오염시키지 않는다"
  artifacts:
    - path: "agent-uat/_template.md"
      provides: "시나리오 마크다운 표준 포맷 템플릿"
      contains: "## Metadata"
    - path: "agent-uat/_index.md"
      provides: "전체 시나리오 인덱스 (카테고리별/네트워크별)"
      contains: "## Categories"
    - path: "agent-uat/testnet/wallet-crud.md"
      provides: "지갑 CRUD 검증 시나리오 (원자적 생성->테스트->삭제)"
      contains: "## Cleanup"
    - path: "agent-uat/README.md"
      provides: "Agent UAT 시스템 개요 및 포맷 규격"
      contains: "## Format Specification"
  key_links:
    - from: "agent-uat/_template.md"
      to: "agent-uat/testnet/wallet-crud.md"
      via: "wallet-crud가 template 포맷을 준수"
      pattern: "## Metadata"
    - from: "agent-uat/testnet/wallet-crud.md"
      to: "agent-uat/_index.md"
      via: "인덱스에 시나리오 등록"
      pattern: "wallet-crud"
---

<objective>
Agent UAT 마크다운 시나리오 포맷을 정의하고, 인덱스 구조를 생성하며, 첫 번째 검증 시나리오(지갑 CRUD)를 작성한다.

Purpose: 이후 모든 시나리오가 따를 표준 포맷과 디렉토리 구조를 확립한다. 에이전트가 파싱 가능한 마크다운 섹션 규격을 정의한다.
Output: agent-uat/ 디렉토리 (README, 템플릿, 인덱스, 지갑 CRUD 시나리오)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<!-- 기존 skill 파일 포맷 참고 -->
@skills/quickstart.skill.md
@skills/transactions.skill.md

<!-- 기존 e2e-tests 시나리오 구조 참고 -->
@packages/e2e-tests/src/scenarios/core-auth-wallet-session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 시나리오 포맷 규격 + README + 템플릿 작성</name>
  <files>agent-uat/README.md, agent-uat/_template.md</files>
  <action>
1. `agent-uat/README.md` 작성 -- Agent UAT 시스템 개요:
   - 목적: AI 에이전트가 마크다운 시나리오를 읽고 인터랙티브하게 실행
   - 디렉토리 구조 설명: `testnet/`, `mainnet/`, `defi/`, `admin/`, `advanced/`
   - 포맷 규격 (Format Specification) 섹션:
     - 필수 섹션 6개: Metadata, Prerequisites, Scenario Steps, Verification, Estimated Cost, Troubleshooting
     - 각 섹션 역할과 파싱 규칙 설명
   - 네트워크 태그: `ethereum-mainnet`, `solana-mainnet`, `ethereum-sepolia`, `solana-devnet`, `polygon-mainnet`, `arbitrum-mainnet`, `base-mainnet`, `hyperliquid-mainnet`, `hyperliquid-testnet`
   - 에이전트 실행 원칙: 기존 지갑 재사용 우선, dry-run 먼저, 자기 전송 패턴, CRUD는 원자적
   - 한국어로 작성

2. `agent-uat/_template.md` 작성 -- 표준 시나리오 템플릿:

```markdown
---
id: "{category}-{nn}"
title: "{시나리오 제목}"
category: "testnet|mainnet|defi|admin|advanced"
network: ["{network-id}"]
requires_funds: true|false
estimated_cost_usd: "{0.00}"
risk_level: "none|low|medium|high"
tags: ["{tag1}", "{tag2}"]
---

# {시나리오 제목}

## Metadata
- **ID**: {category}-{nn}
- **Category**: {category}
- **Network**: {network-id}
- **Requires Funds**: Yes/No
- **Estimated Cost**: ~${estimated_cost_usd}
- **Risk Level**: {risk_level} -- {위험 설명}

## Prerequisites
- [ ] WAIaaS 데몬 실행 중 (`http://localhost:3100`)
- [ ] 세션 토큰 보유 (sessionAuth)
- [ ] {네트워크별 추가 조건}

## Scenario Steps

### Step 1: {단계 제목}
**Action**: {수행할 API 호출 또는 동작}
```bash
curl -s http://localhost:3100/v1/{endpoint} \
  -H 'Authorization: Bearer <session-token>' \
  {추가 옵션}
```
**Expected**: {기대 결과 설명}
**Check**: {확인할 응답 필드 또는 상태}

### Step 2: {단계 제목}
...

## Verification
- [ ] {검증 항목 1 -- 구체적인 확인 사항}
- [ ] {검증 항목 2}
- [ ] {검증 항목 3}

## Estimated Cost
| Item | Network | Estimated Gas | USD |
|------|---------|---------------|-----|
| {작업} | {network} | {gas} | ~${cost} |
| **Total** | | | **~${total}** |

## Troubleshooting
| Symptom | Cause | Resolution |
|---------|-------|------------|
| {증상 1} | {원인} | {해결 방법} |
| {증상 2} | {원인} | {해결 방법} |

## Cleanup
{필요한 경우 정리 작업 -- 특히 CRUD 시나리오에서 생성한 리소스 삭제}
```

모든 섹션이 `##` 레벨 헤딩으로 구분되어 에이전트가 정규식(`^## `)으로 파싱 가능해야 한다.
frontmatter YAML은 에이전트가 `---` 구분자로 파싱하여 카테고리/네트워크 필터링에 사용한다.
  </action>
  <verify>
    <automated>test -f agent-uat/README.md && test -f agent-uat/_template.md && grep -q "## Metadata" agent-uat/_template.md && grep -q "## Prerequisites" agent-uat/_template.md && grep -q "## Scenario Steps" agent-uat/_template.md && grep -q "## Verification" agent-uat/_template.md && grep -q "## Estimated Cost" agent-uat/_template.md && grep -q "## Troubleshooting" agent-uat/_template.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md에 포맷 규격이 정의되어 있고, _template.md에 6개 필수 섹션(Metadata, Prerequisites, Scenario Steps, Verification, Estimated Cost, Troubleshooting)이 모두 존재한다</done>
</task>

<task type="auto">
  <name>Task 2: 인덱스 파일 + 지갑 CRUD 시나리오 작성</name>
  <files>agent-uat/_index.md, agent-uat/testnet/wallet-crud.md</files>
  <action>
1. `agent-uat/_index.md` 작성 -- 전체 시나리오 인덱스:

```markdown
---
title: "Agent UAT Scenario Index"
updated: "2026-03-09"
---

# Agent UAT Scenario Index

## Summary
| Category | Count | Description |
|----------|-------|-------------|
| testnet | 1 | Testnet 기능 검증 |
| mainnet | 0 | Mainnet 전송 검증 |
| defi | 0 | DeFi 프로토콜 검증 |
| admin | 0 | Admin UI 검증 |
| advanced | 0 | 고급 기능 검증 |

## Categories

### Testnet
| ID | Title | Network | Funds | Cost | Risk |
|----|-------|---------|-------|------|------|
| testnet-01 | 지갑 CRUD 검증 | all | No | $0 | none |

### Mainnet
_No scenarios yet._

### DeFi
_No scenarios yet._

### Admin
_No scenarios yet._

### Advanced
_No scenarios yet._

## Network Index
| Network | Scenarios |
|---------|-----------|
| all | testnet-01 |

## Quick Filters
- **무료 (no funds)**: testnet-01
- **Low risk**: (none yet)
- **Medium risk**: (none yet)
- **High risk**: (none yet)
```

카테고리별 테이블과 네트워크별 인덱스를 모두 포함하여 에이전트가 `run testnet`, `run --network ethereum-sepolia` 등으로 필터링 가능하게 한다.

2. `agent-uat/testnet/wallet-crud.md` 작성 -- 지갑 CRUD 검증 시나리오:
   - 템플릿 포맷을 정확히 따름
   - frontmatter: id=testnet-01, category=testnet, network=[all], requires_funds=false, estimated_cost_usd=0, risk_level=none
   - Prerequisites: 데몬 실행, 마스터 패스워드 (지갑 생성에 masterAuth 필요)
   - Step 1: EVM 지갑 생성 (`POST /v1/wallets` with masterAuth, environment=ethereum, label=uat-test-{timestamp})
   - Step 2: 생성된 지갑 상세 조회 (`GET /v1/wallets/{id}`)
   - Step 3: 지갑 라벨 변경 (`PATCH /v1/wallets/{id}` with masterAuth)
   - Step 4: 지갑 목록에서 생성된 지갑 확인 (`GET /v1/wallets`)
   - Step 5: Solana 지갑 생성 (environment=solana, label=uat-test-sol-{timestamp})
   - Step 6: Solana 지갑 조회
   - Step 7: 생성한 테스트 지갑 삭제 (`DELETE /v1/wallets/{id}` with masterAuth) -- EVM 먼저, 그 다음 Solana
   - Step 8: 삭제 확인 (지갑 목록에서 제외 확인)
   - Verification: 생성/조회/수정/삭제 각 단계 성공 확인, 원래 지갑 목록 불변 확인
   - Cleanup: Step 7-8이 cleanup 역할 (원자적 생성->테스트->삭제)
   - Troubleshooting: 401 에러(masterAuth 누락), 409 에러(중복 라벨), 삭제 실패(세션 연결됨) 대응

testnet/ 디렉토리는 자동 생성한다.
  </action>
  <verify>
    <automated>test -f agent-uat/_index.md && test -f agent-uat/testnet/wallet-crud.md && grep -q "testnet-01" agent-uat/_index.md && grep -q "## Cleanup" agent-uat/testnet/wallet-crud.md && grep -q "DELETE" agent-uat/testnet/wallet-crud.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>_index.md에 카테고리별/네트워크별 인덱스가 존재하고, wallet-crud.md가 생성->조회->수정->삭제 흐름을 원자적으로 포함하며 Cleanup 섹션에서 테스트 지갑을 정리한다</done>
</task>

</tasks>

<verification>
1. `agent-uat/` 디렉토리에 README.md, _template.md, _index.md, testnet/wallet-crud.md 4개 파일이 존재
2. _template.md에 6개 필수 섹션이 `##` 헤딩으로 존재하여 에이전트 파싱 가능
3. _index.md에 카테고리별 테이블과 네트워크별 인덱스가 있어 필터링 가능
4. wallet-crud.md가 템플릿 포맷을 준수하고 원자적 CRUD 패턴을 포함
5. wallet-crud.md가 _index.md에 등록되어 있음
</verification>

<success_criteria>
- 시나리오 마크다운 포맷이 표준 6개 섹션으로 정의되어 에이전트가 `^## ` 정규식으로 파싱 가능하다
- _index.md에서 카테고리별/네트워크별로 시나리오를 조회할 수 있다
- wallet-crud.md가 생성->테스트->삭제를 원자적으로 묶어 기존 지갑을 오염시키지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/365-agent-uat-infra/365-01-SUMMARY.md`
</output>
