---
phase: 435-n-plus-one-query
plan: 02
type: execute
wave: 2
depends_on: [435-01]
files_modified:
  - packages/daemon/src/api/routes/admin-wallets.ts
  - packages/daemon/src/api/routes/admin-monitoring.ts
  - packages/daemon/src/api/routes/admin-auth.ts
autonomous: true
requirements: [NQ-04, NQ-05, NQ-07]

must_haves:
  truths:
    - "formatTxAmount accepts optional pre-fetched tokenMap instead of querying DB per call"
    - "All 4 formatTxAmount call sites pre-batch unique token addresses before mapping"
    - "All existing tests pass without modification"
  artifacts:
    - path: "packages/daemon/src/api/routes/admin-wallets.ts"
      provides: "Batch-aware formatTxAmount and pre-batch in wallet transactions route"
      contains: "tokenMap"
    - path: "packages/daemon/src/api/routes/admin-monitoring.ts"
      provides: "Pre-batch tokenMap in admin transactions and incoming routes"
      contains: "tokenMap"
    - path: "packages/daemon/src/api/routes/admin-auth.ts"
      provides: "Pre-batch tokenMap in admin status recent transactions"
      contains: "tokenMap"
  key_links:
    - from: "admin-wallets.ts:formatTxAmount"
      to: "tokenRegistry"
      via: "Optional tokenMap parameter bypasses DB query"
      pattern: "tokenMap.*Map"
    - from: "admin-wallets.ts:walletTransactions"
      to: "formatTxAmount"
      via: "Pre-batch tokenMap passed to each call"
      pattern: "buildTokenMap|batchTokenLookup"
    - from: "admin-monitoring.ts:adminTransactions"
      to: "formatTxAmount"
      via: "Pre-batch tokenMap passed to each call"
      pattern: "buildTokenMap|batchTokenLookup"
---

<objective>
formatTxAmount의 행당 token_registry 개별 조회를 배치 조회로 전환한다.

Purpose: 트랜잭션 목록 조회 시 행마다 발생하는 token_registry 개별 쿼리를 사전 배치 조회 + Map 전달 방식으로 변경하여 DB 라운드트립을 줄인다.
Output: formatTxAmount가 선택적 tokenMap을 받고, 4개 호출부가 사전 배치 조회 후 tokenMap을 전달하는 코드.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/daemon/src/api/routes/admin-wallets.ts
@packages/daemon/src/api/routes/admin-monitoring.ts
@packages/daemon/src/api/routes/admin-auth.ts
</context>

<interfaces>
From packages/daemon/src/api/routes/admin-wallets.ts:
```typescript
export function formatTxAmount(
  amount: string | null,
  chain: string,
  network: string | null,
  tokenAddress: string | null,
  db: BetterSQLite3Database<typeof schema>,
): string | null;
```

From drizzle-orm:
```typescript
import { inArray } from 'drizzle-orm';
```

tokenRegistry schema has: address, network, symbol, decimals columns.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add tokenMap parameter to formatTxAmount and batch helper (NQ-04)</name>
  <files>packages/daemon/src/api/routes/admin-wallets.ts</files>
  <action>
1. Add `inArray` to the drizzle-orm import.

2. Define a `TokenInfo` type and a `buildTokenMap` batch helper function, both exported:
   ```typescript
   export type TokenInfo = { symbol: string; decimals: number };

   /**
    * Batch-fetch token info for multiple addresses in a single IN() query.
    * Returns a Map keyed by `${address}:${network ?? '*'}`.
    */
   export function buildTokenMap(
     tokenAddresses: Array<{ address: string; network: string | null }>,
     db: BetterSQLite3Database<typeof schema>,
   ): Map<string, TokenInfo> {
     if (tokenAddresses.length === 0) return new Map();
     const uniqueAddrs = [...new Set(tokenAddresses.map(t => t.address))];
     const rows = db
       .select({ address: tokenRegistry.address, network: tokenRegistry.network, symbol: tokenRegistry.symbol, decimals: tokenRegistry.decimals })
       .from(tokenRegistry)
       .where(inArray(tokenRegistry.address, uniqueAddrs))
       .all();
     const map = new Map<string, TokenInfo>();
     for (const row of rows) {
       map.set(`${row.address}:${row.network ?? '*'}`, { symbol: row.symbol, decimals: row.decimals });
       // Also store without network as fallback
       if (!map.has(`${row.address}:*`)) {
         map.set(`${row.address}:*`, { symbol: row.symbol, decimals: row.decimals });
       }
     }
     return map;
   }
   ```

3. Modify `formatTxAmount` signature to accept an optional `tokenMap` parameter:
   ```typescript
   export function formatTxAmount(
     amount: string | null,
     chain: string,
     network: string | null,
     tokenAddress: string | null,
     db: BetterSQLite3Database<typeof schema>,
     tokenMap?: Map<string, TokenInfo>,  // NEW: optional pre-fetched map
   ): string | null {
   ```

4. Inside `formatTxAmount`, when `tokenAddress` is set, check `tokenMap` first before falling back to individual DB query:
   ```typescript
   if (tokenAddress) {
     // Try pre-fetched map first
     const cached = tokenMap?.get(`${tokenAddress}:${network ?? '*'}`)
       ?? tokenMap?.get(`${tokenAddress}:*`);
     if (cached) {
       return `${formatAmount(BigInt(amount), cached.decimals)} ${cached.symbol}`;
     }
     // Fallback: individual query (backward compat for callers without tokenMap)
     const token = db.select(...).from(tokenRegistry)...;
     ...
   }
   ```
   This ensures backward compatibility — callers without tokenMap still work.

5. **Update the wallet transactions call site (line ~347)** in the same file:
   - Before `rows.map()`, collect unique token addresses:
     ```typescript
     const tokenAddrs = rows
       .map(tx => ({ address: tx.tokenMint ?? tx.contractAddress ?? '', network: tx.network ?? null }))
       .filter(t => t.address !== '');
     const tokenMap = buildTokenMap(tokenAddrs, deps.db);
     ```
   - Pass `tokenMap` as the 6th argument to `formatTxAmount`.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -10</automated>
  </verify>
  <done>
  - formatTxAmount accepts optional tokenMap parameter (backward compatible)
  - buildTokenMap helper exported for batch token lookups
  - admin-wallets wallet transactions route pre-batches token lookups
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update remaining 3 call sites to use batch tokenMap (NQ-05, NQ-07)</name>
  <files>packages/daemon/src/api/routes/admin-monitoring.ts, packages/daemon/src/api/routes/admin-auth.ts</files>
  <action>
1. **admin-monitoring.ts — GET /admin/transactions (line ~251):**
   - Import `buildTokenMap` and `TokenInfo` from `./admin-wallets.js`.
   - Before `rows.map()`, collect unique token addresses and build tokenMap:
     ```typescript
     const tokenAddrs = rows
       .map(row => ({ address: row.tokenMint ?? row.contractAddress ?? '', network: row.network ?? null }))
       .filter(t => t.address !== '');
     const tokenMap = buildTokenMap(tokenAddrs, deps.db);
     ```
   - Pass `tokenMap` as the 6th argument to `formatTxAmount` in the map callback.

2. **admin-monitoring.ts — GET /admin/incoming (line ~330):**
   - Before `rows.map()`, collect unique token addresses and build tokenMap:
     ```typescript
     const tokenAddrs = rows
       .map(row => ({ address: row.tokenAddress ?? '', network: row.network ?? null }))
       .filter(t => t.address !== '');
     const tokenMap = buildTokenMap(tokenAddrs, deps.db);
     ```
   - Pass `tokenMap` as the 6th argument to `formatTxAmount` in the map callback.

3. **admin-auth.ts — GET /admin/status recent transactions (line ~240):**
   - Import `buildTokenMap` from `./admin-wallets.js` (formatTxAmount is already imported).
   - Before `recentTxRows.map()`, collect unique token addresses and build tokenMap:
     ```typescript
     const tokenAddrs = recentTxRows
       .map(tx => ({ address: tx.tokenMint ?? tx.contractAddress ?? '', network: tx.network ?? null }))
       .filter(t => t.address !== '');
     const tokenMap = buildTokenMap(tokenAddrs, deps.db);
     ```
   - Pass `tokenMap` as the 6th argument to `formatTxAmount` in the map callback.

4. **NQ-07: Run full test suite** to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon && pnpm vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
  - admin-monitoring.ts의 2개 formatTxAmount 호출부가 사전 배치 tokenMap을 전달한다
  - admin-auth.ts의 1개 formatTxAmount 호출부가 사전 배치 tokenMap을 전달한다
  - 전체 테스트 스위트가 변경 없이 통과한다
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/minho.yoo/dev/wallet/WAIaaS
pnpm turbo run typecheck --filter=@waiaas/daemon
pnpm vitest run --reporter=verbose
pnpm turbo run lint --filter=@waiaas/daemon
```
</verification>

<success_criteria>
1. formatTxAmount가 선택적 tokenMap을 받아 DB 쿼리 없이 토큰 정보를 조회할 수 있다
2. 4개 호출부 모두 사전 배치 조회 후 tokenMap을 전달한다
3. 기존 전체 테스트 스위트가 변경 없이 통과한다
4. TypeScript 컴파일과 lint가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/435-n-plus-one-query/435-02-SUMMARY.md`
</output>
