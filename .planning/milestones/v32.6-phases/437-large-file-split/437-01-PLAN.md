---
phase: 437-large-file-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/infrastructure/database/schema-ddl.ts
  - packages/daemon/src/infrastructure/database/migrations/v2-v10.ts
  - packages/daemon/src/infrastructure/database/migrations/v11-v20.ts
  - packages/daemon/src/infrastructure/database/migrations/v21-v30.ts
  - packages/daemon/src/infrastructure/database/migrations/v31-v40.ts
  - packages/daemon/src/infrastructure/database/migrations/v41-v50.ts
  - packages/daemon/src/infrastructure/database/migrations/v51-v59.ts
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03, MIG-04, MIG-05, MIG-06, MIG-07, MIG-08, MIG-09]

must_haves:
  truths:
    - "migrate.ts contains only the Migration type, MIGRATIONS array, runMigrations(), pushSchema(), and imports from submodules"
    - "schema-ddl.ts contains getCreateTableStatements() and getCreateIndexStatements()"
    - "6 migration files each contain their version range's MIGRATIONS.push() calls"
    - "v1 to v59 sequential migration chain test passes without changes"
  artifacts:
    - path: "packages/daemon/src/infrastructure/database/schema-ddl.ts"
      provides: "DDL table + index creation functions"
      exports: ["getCreateTableStatements", "getCreateIndexStatements"]
    - path: "packages/daemon/src/infrastructure/database/migrations/v2-v10.ts"
      provides: "Migrations v2 through v10"
      contains: "MIGRATIONS.push"
    - path: "packages/daemon/src/infrastructure/database/migrations/v11-v20.ts"
      provides: "Migrations v11 through v20"
      contains: "MIGRATIONS.push"
    - path: "packages/daemon/src/infrastructure/database/migrations/v21-v30.ts"
      provides: "Migrations v21 through v30"
      contains: "MIGRATIONS.push"
    - path: "packages/daemon/src/infrastructure/database/migrations/v31-v40.ts"
      provides: "Migrations v31 through v40"
      contains: "MIGRATIONS.push"
    - path: "packages/daemon/src/infrastructure/database/migrations/v41-v50.ts"
      provides: "Migrations v41 through v50"
      contains: "MIGRATIONS.push"
    - path: "packages/daemon/src/infrastructure/database/migrations/v51-v59.ts"
      provides: "Migrations v51 through v59"
      contains: "MIGRATIONS.push"
    - path: "packages/daemon/src/infrastructure/database/migrate.ts"
      provides: "Migration runner + pushSchema orchestration"
      exports: ["Migration", "MIGRATIONS", "LATEST_SCHEMA_VERSION", "runMigrations", "pushSchema"]
  key_links:
    - from: "packages/daemon/src/infrastructure/database/migrate.ts"
      to: "schema-ddl.ts, migrations/*.ts"
      via: "import { getCreateTableStatements, getCreateIndexStatements } and import './migrations/*.js'"
      pattern: "import.*from.*schema-ddl|import.*from.*migrations/"
    - from: "packages/daemon/src/infrastructure/database/index.ts"
      to: "migrate.ts"
      via: "re-export pushSchema, runMigrations, MIGRATIONS, LATEST_SCHEMA_VERSION"
      pattern: "export.*from.*migrate"
---

<objective>
migrate.ts (3,529줄) 분할: 초기 DDL을 schema-ddl.ts로, 58개 마이그레이션(v2-v59)을 6개 파일로 분리하고 migrate.ts는 러너 로직만 유지한다.

Purpose: 3,500줄 단일 파일을 책임 단위 8개 모듈로 분리하여 유지보수성을 높인다.
Output: schema-ddl.ts + 6 migration files + slimmed migrate.ts
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/daemon/src/infrastructure/database/migrate.ts
@packages/daemon/src/infrastructure/database/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract DDL and migration modules from migrate.ts</name>
  <files>
    packages/daemon/src/infrastructure/database/schema-ddl.ts
    packages/daemon/src/infrastructure/database/migrations/v2-v10.ts
    packages/daemon/src/infrastructure/database/migrations/v11-v20.ts
    packages/daemon/src/infrastructure/database/migrations/v21-v30.ts
    packages/daemon/src/infrastructure/database/migrations/v31-v40.ts
    packages/daemon/src/infrastructure/database/migrations/v41-v50.ts
    packages/daemon/src/infrastructure/database/migrations/v51-v59.ts
  </files>
  <action>
    **schema-ddl.ts**: Extract from migrate.ts:
    - The `inList()` utility function (line ~50)
    - The `LEGACY_SOLANA_NETWORKS`, `NETWORK_TYPES_WITH_LEGACY`, `LEGACY_NETWORK_NORMALIZE` constants (lines ~59-71)
    - `LATEST_SCHEMA_VERSION` constant (line ~82)
    - `getCreateTableStatements()` function (lines ~84-554)
    - `getCreateIndexStatements()` function (lines ~559-699)
    - Export all of them. Import the needed @waiaas/core types at the top.
    - Also export `inList`, `NETWORK_TYPES_WITH_LEGACY`, `LEGACY_NETWORK_NORMALIZE` since migration files need them.

    **Migration type**: Keep the `Migration` interface exported from migrate.ts (it's used by the runner and by submodules).

    **migrations/ directory**: Create `packages/daemon/src/infrastructure/database/migrations/` directory.

    Each migration file follows the same pattern:
    - Import `type { Database } from 'better-sqlite3'`
    - Import `type { Migration }` from `../migrate.js`
    - Import needed @waiaas/core types (TRANSACTION_TYPES, POLICY_TYPES, etc.)
    - Import shared constants from `../schema-ddl.js` (inList, NETWORK_TYPES_WITH_LEGACY, LEGACY_NETWORK_NORMALIZE)
    - Export a `const migrations: Migration[] = [...]` array containing all MIGRATIONS.push() entries for that range.
    - Each file must preserve the exact `up()` function body from the original.

    **File splits by version** (based on line analysis of migrate.ts):

    1. **v2-v10.ts**: Versions 2-10 (lines ~727-1413). Includes v2 (agents.network CHECK), v3 (agents→wallets rename), v4 (token_registry), v5 (settings), v6a/v6b (environment model, versions 6-7), v8 (policies network), v9 (SIGNED+SIGN), v10 (notification_logs message).

    2. **v11-v20.ts**: Versions 11-20 (lines ~1414-1877). Includes v11 (api_keys), v12 (X402_PAYMENT), v13 (amount_usd), v14 (kill_switch migrate), v15 (telegram_users), v16 (WC infra), v17-v20 (session model + session_wallets + related).

    3. **v21-v30.ts**: Versions 21-30 (lines ~1878-2505). Includes v21-v24 (asset_id, incoming_transactions, CAIP-19), v25 (defi_positions), v26 (lending policies), v27 (default wallet removal), v28 (api_keys→settings), v29 (Solana network rename), v30 (extended fields).

    4. **v31-v40.ts**: Versions 31-40 (lines ~2506-2748). Includes v31-v32 (session model changes), v33 (compound index), v34 (wallet_type), v35 (subscription_token), v36 (audit log index), v37 (webhooks), v38 (smart account), v39 (ERC-8004), v40 (typed_data_json).

    5. **v41-v50.ts**: Versions 41-50 (lines ~2749-3017). Includes v41 (smart account provider), v42 (action provider defaults), v43 (NFT columns), v44 (nft_metadata_cache), v45 (userop_builds), v46-v50 (Hyperliquid tables, perp policies, etc.).

    6. **v51-v59.ts**: Versions 51-59 (lines ~3017-3415). Includes v51 (hyperliquid_orders indexes), v52 (sub_accounts), v53-v54 (polymarket), v55 (wallet_credentials), v56 (action tracking), v57 (composite index), v58 (CONTRACT_DEPLOY), v59 (defi_positions environment).

    **CRITICAL**: Each migration file must import exactly the same @waiaas/core values that the corresponding migrations use. Check each `up()` function body for references to TRANSACTION_TYPES, POLICY_TYPES, NETWORK_TYPES, tokenAssetId, NETWORK_TO_CAIP2, etc. and import them.

    **CRITICAL**: Ensure the `migrations` array in each file lists entries in ascending version order.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -5</automated>
  </verify>
  <done>7 new files exist, each exports its content. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Slim migrate.ts to runner-only and wire imports</name>
  <files>
    packages/daemon/src/infrastructure/database/migrate.ts
    packages/daemon/src/infrastructure/database/index.ts
  </files>
  <action>
    **Rewrite migrate.ts** to contain only:
    1. Imports from `better-sqlite3` (type Database)
    2. Import `getCreateTableStatements, getCreateIndexStatements, LATEST_SCHEMA_VERSION` from `./schema-ddl.js`
    3. Import `{ migrations as v2to10 }` from `./migrations/v2-v10.js` (and similarly for all 6 files)
    4. The `Migration` interface export (unchanged)
    5. The `MIGRATIONS` array — built by spreading all 6 imported arrays: `export const MIGRATIONS: Migration[] = [...v2to10, ...v11to20, ...v21to30, ...v31to40, ...v41to50, ...v51to59];`
    6. The `runMigrations()` function (unchanged, lines ~3331-3414)
    7. The `pushSchema()` function (unchanged, lines ~3427-3529) — which calls getCreateTableStatements(), getCreateIndexStatements(), MIGRATIONS, runMigrations()

    **index.ts**: No changes needed — it already re-exports `{ pushSchema, runMigrations, MIGRATIONS, LATEST_SCHEMA_VERSION }` from `./migrate.js` and `type { Migration }`. Verify it still works.

    **Target**: migrate.ts should be ~250 lines (Migration interface + MIGRATIONS build + runMigrations + pushSchema).

    **Verify**: All existing test imports (`from '../infrastructure/database/migrate.js'`) should still work since the public API is unchanged.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run --project daemon --reporter=verbose 2>&1 | grep -E "migration|FAIL|PASS|Tests" | tail -30</automated>
  </verify>
  <done>migrate.ts contains only runner logic (~250 lines). LATEST_SCHEMA_VERSION re-exported from schema-ddl. All migration chain tests pass (v1→v59). All individual migration tests pass.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/daemon` passes
2. `pnpm vitest run --project daemon -- migration` — all migration tests pass
3. `wc -l packages/daemon/src/infrastructure/database/migrate.ts` shows ~250 lines
4. `ls packages/daemon/src/infrastructure/database/migrations/` shows 6 files
5. `wc -l packages/daemon/src/infrastructure/database/schema-ddl.ts` shows ~650 lines
</verification>

<success_criteria>
1. migrate.ts는 Migration 타입, MIGRATIONS 배열 조립, runMigrations(), pushSchema()만 포함 (~250줄)
2. schema-ddl.ts에 DDL 생성 함수와 공유 상수가 분리됨
3. 6개 migration 파일이 각각 해당 버전 범위의 마이그레이션을 포함
4. v1→v59 전체 마이그레이션 체인 테스트 통과
5. 기존 import 경로(migrate.js, database/index.js)가 변경 없이 동작
</success_criteria>

<output>
After completion, create `.planning/phases/437-large-file-split/437-01-SUMMARY.md`
</output>
