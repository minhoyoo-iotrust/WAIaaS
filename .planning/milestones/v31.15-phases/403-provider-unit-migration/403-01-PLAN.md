---
phase: 403-provider-unit-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/common/migrate-amount.ts
  - packages/actions/src/common/migrate-amount.test.ts
  - packages/actions/src/providers/aave-v3/schemas.ts
  - packages/actions/src/providers/aave-v3/index.ts
autonomous: true
requirements: [UNIT-01, UNIT-02, UNIT-03, UNIT-05, TEST-01, TEST-02, TEST-07]

must_haves:
  truths:
    - "migrateAmount() 헬퍼가 소수점 포함 입력을 smallest-unit으로 변환하고 deprecation 경고를 출력한다"
    - "migrateAmount()가 순수 정수 문자열(smallest-unit)을 변환 없이 통과시킨다"
    - "Aave V3 supply/borrow가 smallest-unit(wei) 입력을 parseTokenAmount 없이 직접 BigInt로 사용한다"
    - "Aave V3 repay/withdraw에서 max 키워드가 단위 변환과 독립적으로 동작한다"
    - "Aave V3 레거시 소수점 입력이 migrateAmount()를 통해 자동 변환된다"
  artifacts:
    - path: "packages/actions/src/common/migrate-amount.ts"
      provides: "migrateAmount(value, decimals) 공유 헬퍼"
      exports: ["migrateAmount"]
    - path: "packages/actions/src/common/migrate-amount.test.ts"
      provides: "migrateAmount 단위 테스트"
    - path: "packages/actions/src/providers/aave-v3/index.ts"
      provides: "smallest-unit 기반 Aave V3 resolve 메서드"
    - path: "packages/actions/src/providers/aave-v3/schemas.ts"
      provides: "smallest-unit .describe() 업데이트된 스키마"
  key_links:
    - from: "packages/actions/src/providers/aave-v3/index.ts"
      to: "packages/actions/src/common/migrate-amount.ts"
      via: "import migrateAmount"
      pattern: "migrateAmount\\(input\\.amount"
    - from: "packages/actions/src/providers/aave-v3/index.ts"
      to: "packages/actions/src/common/amount-parser.ts"
      via: "import removed"
      pattern: "NOT parseTokenAmount"
---

<objective>
migrateAmount() 공유 헬퍼를 생성하고 Aave V3 provider를 smallest-unit 입력으로 전환한다.

Purpose: 4개 레거시 provider 마이그레이션의 기반 헬퍼를 만들고 Aave V3를 첫 번째 전환 대상으로 구현
Output: migrateAmount() 헬퍼 + Aave V3 provider에서 parseTokenAmount 제거 + 하위 호환성 유지
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/402-schema-hardening/402-01-SUMMARY.md

@packages/actions/src/common/amount-parser.ts
@packages/actions/src/providers/aave-v3/index.ts
@packages/actions/src/providers/aave-v3/schemas.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: migrateAmount() 공유 헬퍼 생성</name>
  <files>packages/actions/src/common/migrate-amount.ts, packages/actions/src/common/migrate-amount.test.ts</files>
  <behavior>
    - Test 1: migrateAmount("1000000000000000000", 18) => 1000000000000000000n (순수 정수 문자열, 변환 없음)
    - Test 2: migrateAmount("100.5", 18) => parseTokenAmount("100.5", 18)과 동일한 결과 (소수점 감지 -> 변환 + 경고)
    - Test 3: migrateAmount("1.5", 9) => 1500000000n (SOL 9 decimals 소수점 변환)
    - Test 4: migrateAmount("0", 18) => 0n (제로값 통과, 검증은 provider 레벨에서)
    - Test 5: console.warn이 소수점 입력 시에만 호출되고, 정수 입력 시에는 호출되지 않음
    - Test 6: migrateAmount("1000000", 6) => 1000000n (정수 문자열 그대로 BigInt)
    - Test 7: migrateAmount(".5", 18) => 소수점 감지 -> 변환 (fractional-only)
  </behavior>
  <action>
    1. `packages/actions/src/common/migrate-amount.ts` 생성:
       - `export function migrateAmount(value: string, decimals: number): bigint`
       - 소수점 감지: `value.includes('.')`
       - 소수점 있으면: `parseTokenAmount(value, decimals)` 호출 + `console.warn('[WAIaaS DEPRECATION] Amount "${value}" contains decimal point. This will be rejected in a future version. Please provide amounts in smallest units (e.g., wei/lamports).')`
       - 소수점 없으면: `BigInt(value)` 직접 변환 (smallest-unit 입력)
       - parseTokenAmount는 기존 `./amount-parser.js`에서 import
    2. 테스트 파일에서 console.warn을 vi.spyOn으로 감시하여 deprecation 경고 출력 여부 검증
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/common/migrate-amount.test.ts</automated>
  </verify>
  <done>migrateAmount()가 소수점 입력은 변환+경고, 정수 입력은 직접 BigInt 변환하며 모든 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aave V3 provider smallest-unit 마이그레이션</name>
  <files>packages/actions/src/providers/aave-v3/schemas.ts, packages/actions/src/providers/aave-v3/index.ts, packages/actions/src/providers/aave-v3/aave-v3.test.ts</files>
  <behavior>
    - Test 1: aave_supply({ asset: "0x...", amount: "1000000000000000000" }) => parseTokenAmount 없이 1000000000000000000n wei가 calldata에 인코딩됨
    - Test 2: aave_supply({ asset: "0x...", amount: "1.0" }) => migrateAmount가 wei로 변환 + deprecation 경고
    - Test 3: aave_repay({ asset: "0x...", amount: "max" }) => MAX_UINT256 사용, migrateAmount 호출 안 함
    - Test 4: aave_withdraw({ asset: "0x...", amount: "max" }) => MAX_UINT256 사용
    - Test 5: aave_borrow({ asset: "0x...", amount: "500000000000000000" }) => 0.5 ETH 상당 wei 직접 사용
    - Test 6: aave_repay({ asset: "0x...", amount: "1000000000000000000" }) => smallest-unit 직접 사용 (max 아닌 경우)
  </behavior>
  <action>
    1. **schemas.ts 업데이트**: 4개 스키마의 amount .describe()를 smallest-unit 형식으로 변경:
       - `'Amount in smallest units (wei). Example: "1000000000000000000" = 1.0 token. Legacy decimal input (e.g., "1.5") is auto-converted with deprecation warning.'`
       - repay/withdraw의 `.or(z.literal('max'))` 유지

    2. **index.ts 변경**:
       - `import { parseTokenAmount }` 제거
       - `import { migrateAmount } from '../../common/migrate-amount.js'` 추가
       - resolveSupply: `parseTokenAmount(input.amount, 18)` -> `migrateAmount(input.amount, 18)`
       - resolveBorrow: `parseTokenAmount(input.amount, 18)` -> `migrateAmount(input.amount, 18)`
       - resolveRepay: `input.amount === 'max' ? MAX_UINT256 : parseTokenAmount(input.amount, 18)` -> `input.amount === 'max' ? MAX_UINT256 : migrateAmount(input.amount, 18)`
       - resolveWithdraw: 동일 패턴

    3. **aave-v3.test.ts 생성**: AaveV3LendingProvider의 resolve 메서드 테스트
       - MockRpcCaller 없이 기본 생성자 사용 (HF 시뮬레이션 스킵)
       - 각 action에 대해 smallest-unit 정수 문자열 입력 테스트
       - 레거시 소수점 입력 시 정상 동작 + console.warn 호출 확인
       - max 키워드 테스트 (repay, withdraw)
       - calldata에 올바른 bigint가 인코딩되었는지 검증 (encodeSupplyCalldata 등의 결과 확인)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/providers/aave-v3/aave-v3.test.ts src/common/migrate-amount.test.ts</automated>
  </verify>
  <done>Aave V3가 smallest-unit 입력을 직접 사용하고, 레거시 소수점 입력은 migrateAmount()로 자동 변환하며, max 키워드가 정상 동작한다</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/actions exec vitest run src/common/migrate-amount.test.ts` - migrateAmount 헬퍼 테스트 통과
2. `pnpm --filter @waiaas/actions exec vitest run src/providers/aave-v3/aave-v3.test.ts` - Aave V3 마이그레이션 테스트 통과
3. `pnpm turbo run typecheck --filter=@waiaas/actions` - 타입 체크 통과
4. parseTokenAmount import가 aave-v3/index.ts에서 제거되었는지 확인
</verification>

<success_criteria>
- migrateAmount() 헬퍼가 소수점/정수 입력을 올바르게 처리한다
- Aave V3 4개 action이 smallest-unit 입력을 직접 사용한다
- 레거시 소수점 입력이 자동 변환되고 deprecation 경고가 출력된다
- max 키워드가 repay/withdraw에서 정상 동작한다
- 모든 테스트와 타입 체크가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/403-provider-unit-migration/403-01-SUMMARY.md`
</output>
