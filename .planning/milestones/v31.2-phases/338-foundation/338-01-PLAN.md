---
phase: 338-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/schemas/wallet.schema.ts
  - packages/daemon/src/api/routes/wallets.ts
  - packages/daemon/src/api/routes/transactions.ts
  - packages/daemon/src/api/routes/connect-info.ts
  - packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04, PROV-05]

must_haves:
  truths:
    - "Smart Account 지갑을 aaProvider 없이 생성할 수 있다"
    - "프로바이더 미설정 지갑은 Lite 모드로 인식된다"
    - "Lite 모드에서 POST /transactions/send 호출 시 CHAIN_ERROR + userop API 안내를 받는다"
    - "PUT /wallets/:id/provider로 프로바이더 추가 시 Full 모드로 전환된다"
    - "프로바이더 설정된 지갑은 Full 모드로 기존 AA 기능이 그대로 동작한다"
  artifacts:
    - path: "packages/core/src/schemas/wallet.schema.ts"
      provides: "aaProvider optional for smart account creation"
      contains: "accountType === 'smart'"
    - path: "packages/daemon/src/api/routes/wallets.ts"
      provides: "Lite/Full mode detection, provider-less wallet creation"
      contains: "buildProviderStatus"
    - path: "packages/daemon/src/api/routes/transactions.ts"
      provides: "Lite mode send blocking with userop API guidance"
      contains: "userop"
    - path: "packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts"
      provides: "Lite/Full mode unit tests"
      min_lines: 50
  key_links:
    - from: "packages/core/src/schemas/wallet.schema.ts"
      to: "packages/daemon/src/api/routes/wallets.ts"
      via: "CreateWalletRequestSchema import"
      pattern: "CreateWalletRequestSchema"
    - from: "packages/daemon/src/api/routes/transactions.ts"
      to: "packages/daemon/src/api/routes/wallets.ts"
      via: "buildProviderStatus or direct aaProvider check"
      pattern: "aaProvider.*null"
---

<objective>
Provider Lite/Full 모드 분기를 구현하여 Smart Account 지갑이 프로바이더 없이도 생성/관리되게 한다.

Purpose: UserOp Build/Sign API의 전제 조건. 프로바이더 없는 Smart Account(Lite mode)가 존재해야 외부 플랫폼 대납 워크플로우가 가능하다.
Output: Lite/Full 모드가 동작하는 지갑 생성/관리 코드 + 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/schemas/wallet.schema.ts
@packages/daemon/src/api/routes/wallets.ts
@packages/daemon/src/api/routes/transactions.ts
@packages/daemon/src/api/routes/connect-info.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/core/src/schemas/wallet.schema.ts:
```typescript
export const CreateWalletRequestSchema = CreateWalletRequestBaseSchema.superRefine((data, ctx) => {
  if (data.accountType === 'smart') {
    if (!data.aaProvider) {
      ctx.addIssue({ ... }); // THIS MUST BE REMOVED (PROV-01)
    }
  }
});
```

From packages/daemon/src/api/routes/wallets.ts:
```typescript
// Line ~466: Provider must be configured (PROV-05) -- THIS CHECK MUST BE REMOVED
if (!aaProvider) {
  throw new WAIaaSError('ACTION_VALIDATION_FAILED', {
    message: 'Smart Account wallet requires provider configuration...',
  });
}

export function buildProviderStatus(wallet: { aaProvider: string | null; aaPaymasterUrl?: string | null }): { ... } | null;
```

From packages/core/src/errors/error-codes.ts:
```typescript
CHAIN_ERROR: { code: 'CHAIN_ERROR', domain: 'TX', httpStatus: 502, retryable: true, message: 'Blockchain RPC error' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CreateWalletRequestSchema aaProvider 필수 검증 제거 + 라우트 핸들러 Lite 모드 허용</name>
  <files>packages/core/src/schemas/wallet.schema.ts, packages/daemon/src/api/routes/wallets.ts, packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts</files>
  <behavior>
    - Test 1: CreateWalletRequestSchema에서 accountType='smart' + aaProvider 미지정 시 validation 통과
    - Test 2: accountType='smart' + aaProvider='pimlico' + aaProviderApiKey 미지정 시 여전히 validation 실패 (기존 동작 유지)
    - Test 3: accountType='smart' + aaProvider='custom' + aaBundlerUrl 미지정 시 여전히 validation 실패
    - Test 4: buildProviderStatus에서 aaProvider=null 반환 시 mode='lite' 정보 포함
    - Test 5: POST /wallets 에서 accountType='smart', aaProvider 없이 생성 성공, 응답에 provider=null
  </behavior>
  <action>
1. **packages/core/src/schemas/wallet.schema.ts**: CreateWalletRequestBaseSchema의 superRefine에서 `if (!data.aaProvider)` 에러 블록 제거. aaProvider가 지정된 경우의 하위 검증(pimlico/alchemy API key, custom bundlerUrl)은 그대로 유지.

2. **packages/daemon/src/api/routes/wallets.ts**:
   - POST /wallets 핸들러(~라인 466)에서 `if (!aaProvider) { throw new WAIaaSError('ACTION_VALIDATION_FAILED'...) }` 블록 제거
   - 프로바이더 없는 smart account 생성 시 deployed=false로 설정 (아직 배포 불가)
   - buildProviderStatus 함수는 변경 없음 (aaProvider=null이면 이미 null 반환)

3. **packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts**: 새 테스트 파일 생성. CreateWalletRequestSchema validation 테스트 5개 + buildProviderStatus Lite/Full 동작 테스트.

PUT /wallets/:id/provider 엔드포인트는 이미 존재하며 (PROV-05), 프로바이더 추가 시 자동으로 Full 모드로 전환됨 (buildProviderStatus가 non-null 반환). 추가 구현 불필요.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts packages/core/src/__tests__/wallet-schema.test.ts 2>/dev/null || pnpm vitest run packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts</automated>
  </verify>
  <done>Smart Account 지갑이 aaProvider 없이 생성 가능하고, buildProviderStatus가 null을 반환하여 Lite 모드로 인식된다. 기존 프로바이더 검증(pimlico API key, custom bundlerUrl)은 그대로 유지된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lite 모드 send 차단 + CHAIN_ERROR 응답에 userop API 안내</name>
  <files>packages/daemon/src/api/routes/transactions.ts, packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts</files>
  <behavior>
    - Test 6: Lite 모드 Smart Account에서 POST /transactions/send 호출 시 CHAIN_ERROR(502) 반환
    - Test 7: 에러 메시지에 "userop/build" 와 "userop/sign" 안내가 포함
    - Test 8: Full 모드(프로바이더 설정) Smart Account에서 POST /transactions/send는 기존대로 동작
    - Test 9: EOA 지갑에서 POST /transactions/send는 영향 없음
  </behavior>
  <action>
1. **packages/daemon/src/api/routes/transactions.ts**: POST /transactions/send 핸들러에서 파이프라인 실행 전 Smart Account Lite 모드 체크 추가.
   - walletId로 지갑 조회 후 `accountType === 'smart' && !aaProvider` 조건 확인
   - 조건 매칭 시 WAIaaSError('CHAIN_ERROR') throw, message에 "Smart Account in Lite mode cannot send transactions directly. Use POST /v1/wallets/:id/userop/build and POST /v1/wallets/:id/userop/sign to construct and sign UserOperations for external sponsorship." 포함
   - 이 체크는 Stage 1 (파이프라인 진입) 전에 수행하여 불필요한 처리 방지

2. **packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts**: Task 1의 테스트 파일에 Lite mode send 차단 테스트 추가. DB에 직접 Lite 모드 지갑 레코드를 INSERT하고 send 핸들러 동작을 검증. 기존 wallets.ts의 테스트 패턴을 따른다.

Note: 지갑 DB 조회는 이미 transactions.ts 핸들러에서 수행 중 (resolveWalletId 또는 세션에서 walletId 추출). 추가 쿼리 불필요 -- 기존 wallet 객체의 account_type, aa_provider 필드를 확인하면 된다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts</automated>
  </verify>
  <done>Lite 모드 Smart Account에서 POST /transactions/send 호출 시 CHAIN_ERROR + userop API 안내 메시지가 반환된다. Full 모드와 EOA 지갑은 영향 없다.</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run packages/daemon/src/__tests__/wallet-provider-lite-mode.test.ts` -- 모든 Lite/Full 모드 테스트 통과
2. `pnpm vitest run packages/core/` -- core 패키지 기존 테스트 깨지지 않음
3. `pnpm turbo run typecheck` -- 타입 에러 없음
</verification>

<success_criteria>
- Smart Account 지갑이 aaProvider 없이 생성 가능 (PROV-01)
- 프로바이더 없는 지갑 = Lite 모드, buildProviderStatus = null (PROV-02)
- 프로바이더 설정된 지갑 = Full 모드, 기존 기능 유지 (PROV-03)
- Lite 모드에서 send 시 CHAIN_ERROR + userop API 안내 (PROV-04)
- PUT /wallets/:id/provider로 Full 전환 (기존 동작 유지) (PROV-05)
</success_criteria>

<output>
After completion, create `.planning/phases/338-foundation/338-01-SUMMARY.md`
</output>
