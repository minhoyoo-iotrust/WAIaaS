---
phase: 329-mcp-sdk-admin-ui-skill-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/tools/erc8128-sign-request.ts
  - packages/mcp/src/tools/erc8128-verify-signature.ts
  - packages/mcp/src/server.ts
  - packages/mcp/src/resources/skills.ts
  - packages/daemon/src/api/routes/connect-info.ts
  - packages/mcp/src/__tests__/erc8128-tools.test.ts
  - packages/daemon/src/__tests__/connect-info.test.ts
autonomous: true
requirements: [API-03, API-04, INT-01]

must_haves:
  truths:
    - "User can sign HTTP requests via MCP erc8128_sign_request tool"
    - "User can verify signatures via MCP erc8128_verify_signature tool"
    - "User can discover ERC-8128 support via connect-info capabilities"
  artifacts:
    - path: "packages/mcp/src/tools/erc8128-sign-request.ts"
      provides: "MCP tool wrapping POST /v1/erc8128/sign"
      exports: ["registerErc8128SignRequest"]
    - path: "packages/mcp/src/tools/erc8128-verify-signature.ts"
      provides: "MCP tool wrapping POST /v1/erc8128/verify"
      exports: ["registerErc8128VerifySignature"]
  key_links:
    - from: "packages/mcp/src/tools/erc8128-sign-request.ts"
      to: "/v1/erc8128/sign"
      via: "apiClient.post"
      pattern: "apiClient\\.post.*erc8128/sign"
    - from: "packages/mcp/src/tools/erc8128-verify-signature.ts"
      to: "/v1/erc8128/verify"
      via: "apiClient.post"
      pattern: "apiClient\\.post.*erc8128/verify"
    - from: "packages/daemon/src/api/routes/connect-info.ts"
      to: "settingsService"
      via: "erc8128.enabled check"
      pattern: "erc8128"
---

<objective>
MCP ERC-8128 tools + connect-info capability discovery

Purpose: AI agents can sign/verify HTTP messages via MCP tools and discover ERC-8128 availability via connect-info
Output: 2 MCP tool files, server registration, connect-info erc8128 capability, skill resource update, tests
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/328-rest-api-policy-settings/328-02-SUMMARY.md

<interfaces>
<!-- MCP tool registration pattern (from x402-fetch.ts): -->
```typescript
import { z } from 'zod';
import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { type ApiClient, toToolResult } from '../api-client.js';
import { type WalletContext, withWalletPrefix } from '../server.js';

export function registerX402Fetch(server: McpServer, apiClient: ApiClient, walletContext?: WalletContext): void {
  server.tool('x402_fetch', withWalletPrefix('...description...', walletContext?.walletName), { /* zod schema */ }, async (args) => {
    const result = await apiClient.post('/v1/x402/fetch', requestBody);
    return toToolResult(result);
  });
}
```

<!-- REST API endpoints from Phase 328 (packages/daemon/src/api/routes/erc8128.ts): -->
POST /v1/erc8128/sign - body: { method, url, headers?, body?, walletId?, network?, preset?, ttlSeconds?, includeNonce?, algorithm? }
POST /v1/erc8128/verify - body: { method, url, headers, signatureInput, signature, contentDigest? }

<!-- connect-info capabilities pattern (packages/daemon/src/api/routes/connect-info.ts line 283-325): -->
```typescript
const capabilities: string[] = ['transfer', 'token_transfer', 'balance', 'assets'];
// ... various checks ...
if (deps.config.x402?.enabled === true) { capabilities.push('x402'); }
// ERC-8128 should check settingsService for erc8128.enabled
```

<!-- MCP server registration (packages/mcp/src/server.ts): -->
30 tools registered via registerXxx(server, apiClient, walletContext)
Tool count in JSDoc comment: "30 tools + 4 resource groups"

<!-- Skill resources (packages/mcp/src/resources/skills.ts): -->
SKILL_NAMES = ['quickstart', 'wallet', 'transactions', 'policies', 'admin', 'actions', 'x402']
Need to add 'erc8128' to this array

<!-- Skill file serving (daemon): -->
GET /v1/skills/:name serves skill files from skills/ directory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP erc8128_sign_request + erc8128_verify_signature tools + server registration + skill resource</name>
  <files>
    packages/mcp/src/tools/erc8128-sign-request.ts
    packages/mcp/src/tools/erc8128-verify-signature.ts
    packages/mcp/src/server.ts
    packages/mcp/src/resources/skills.ts
  </files>
  <action>
    1. Create `packages/mcp/src/tools/erc8128-sign-request.ts`:
       - Follow x402-fetch.ts pattern exactly
       - Export `registerErc8128SignRequest(server, apiClient, walletContext)`
       - Tool name: `erc8128_sign_request`
       - Description (with withWalletPrefix): "Sign an HTTP request using ERC-8128 (RFC 9421 + EIP-191). Returns Signature-Input, Signature, and Content-Digest headers for the given HTTP method, URL, headers, and body."
       - Zod input schema:
         - `method`: z.enum(['GET', 'POST', 'PUT', 'DELETE', 'PATCH']).describe('HTTP method')
         - `url`: z.string().url().describe('Target URL to sign for')
         - `headers`: z.record(z.string()).optional().describe('HTTP headers to include in signature')
         - `body`: z.string().optional().describe('Request body (used for Content-Digest)')
         - `wallet_id`: z.string().optional().describe('Target wallet ID. Required for multi-wallet sessions; auto-resolved when session has a single wallet.')
         - `network`: z.string().optional().describe('Network ID (e.g., evm-ethereum-mainnet)')
         - `preset`: z.enum(['minimal', 'standard', 'strict']).optional().describe('Covered Components preset (default: standard)')
         - `ttl_seconds`: z.number().optional().describe('Signature TTL in seconds (default: 300)')
       - Handler: build requestBody mapping camelCase (walletId, ttlSeconds), POST to `/v1/erc8128/sign`, return toToolResult

    2. Create `packages/mcp/src/tools/erc8128-verify-signature.ts`:
       - Export `registerErc8128VerifySignature(server, apiClient, walletContext)`
       - Tool name: `erc8128_verify_signature`
       - Description: "Verify an ERC-8128 HTTP message signature. Checks RFC 9421 signature validity, recovers the signer's Ethereum address, and validates expiry."
       - Zod input schema:
         - `method`: z.enum(['GET', 'POST', 'PUT', 'DELETE', 'PATCH']).describe('HTTP method')
         - `url`: z.string().url().describe('Request URL')
         - `headers`: z.record(z.string()).describe('HTTP headers from the signed request')
         - `signature_input`: z.string().describe('Signature-Input header value')
         - `signature`: z.string().describe('Signature header value')
         - `content_digest`: z.string().optional().describe('Content-Digest header value')
       - Handler: build requestBody with snake_case -> camelCase mapping (signatureInput, contentDigest), POST to `/v1/erc8128/verify`, return toToolResult

    3. Update `packages/mcp/src/server.ts`:
       - Add imports: `import { registerErc8128SignRequest } from './tools/erc8128-sign-request.js'` and `import { registerErc8128VerifySignature } from './tools/erc8128-verify-signature.js'`
       - Register both tools after registerSignMessage: `registerErc8128SignRequest(server, apiClient, walletContext)` and `registerErc8128VerifySignature(server, apiClient, walletContext)`
       - Update JSDoc comment: "32 tools + 4 resource groups"
       - Update comment "Register 32 tools"

    4. Update `packages/mcp/src/resources/skills.ts`:
       - Add 'erc8128' to SKILL_NAMES array
       - Update JSDoc comment: "8 skill files" (was 7)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/mcp exec tsc --noEmit</automated>
  </verify>
  <done>Two MCP tools registered and type-check pass, server has 32 tools, skill resources includes erc8128</done>
</task>

<task type="auto">
  <name>Task 2: connect-info erc8128 capability + MCP tool tests</name>
  <files>
    packages/daemon/src/api/routes/connect-info.ts
    packages/daemon/src/__tests__/connect-info.test.ts
    packages/mcp/src/__tests__/erc8128-tools.test.ts
  </files>
  <action>
    1. Update `packages/daemon/src/api/routes/connect-info.ts`:
       - After the `smart_account` capability check (line ~325), add erc8128 capability:
         ```typescript
         // erc8128: check if enabled via settings
         if (deps.settingsService) {
           try {
             if (deps.settingsService.get('erc8128.enabled') === 'true') {
               capabilities.push('erc8128');
             }
           } catch {
             // Setting not found -- erc8128 not available
           }
         }
         ```
       - This follows the signing_sdk pattern (check settingsService, not config)

    2. Create `packages/mcp/src/__tests__/erc8128-tools.test.ts`:
       - Follow erc8004-tools.test.ts pattern
       - Import registerErc8128SignRequest and registerErc8128VerifySignature
       - Test `erc8128_sign_request tool`:
         - "calls POST /v1/erc8128/sign with method and url"
         - "passes optional parameters (headers, body, wallet_id, preset, ttl_seconds)"
         - "returns error result on API failure"
       - Test `erc8128_verify_signature tool`:
         - "calls POST /v1/erc8128/verify with all required params"
         - "passes optional content_digest"
       - Use the same mock apiClient/McpServer pattern as erc8004-tools.test.ts

    3. Add test in `packages/daemon/src/__tests__/connect-info.test.ts`:
       - Add test: "capabilities includes erc8128 when setting enabled"
         - Mock settingsService.get('erc8128.enabled') to return 'true'
         - Verify capabilities array contains 'erc8128'
       - Add test: "capabilities excludes erc8128 when setting disabled or not set"
         - Mock settingsService.get('erc8128.enabled') to return 'false'
         - Verify capabilities array does NOT contain 'erc8128'
       - Follow the existing x402/erc8004 capability test patterns in that file
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/mcp exec vitest run src/__tests__/erc8128-tools.test.ts --reporter=verbose && pnpm --filter @waiaas/daemon exec vitest run src/__tests__/connect-info.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>MCP tool tests pass (5+ tests), connect-info includes erc8128 capability when enabled</done>
</task>

</tasks>

<verification>
- `pnpm --filter @waiaas/mcp exec tsc --noEmit` passes
- `pnpm --filter @waiaas/mcp exec vitest run` passes
- `pnpm --filter @waiaas/daemon exec vitest run src/__tests__/connect-info.test.ts` passes
- connect-info response includes erc8128 in capabilities when enabled
</verification>

<success_criteria>
- erc8128_sign_request MCP tool wraps POST /v1/erc8128/sign
- erc8128_verify_signature MCP tool wraps POST /v1/erc8128/verify
- connect-info capabilities dynamically includes 'erc8128' when erc8128.enabled setting is true
- MCP server registers 32 tools total
- SKILL_NAMES includes 'erc8128' (8 skills)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/329-mcp-sdk-admin-ui-skill-files/329-01-SUMMARY.md`
</output>
