---
phase: 326-admin-ui-mcp-connect-info
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/wallets.tsx
  - packages/admin/src/api/endpoints.ts
  - packages/admin/src/__tests__/wallets-provider.test.tsx
autonomous: true
requirements: [PROV-06, PROV-07, GUID-01, GUID-02, GUID-03]

must_haves:
  truths:
    - "Admin UI 지갑 생성 폼에서 accountType: smart 선택 시에만 프로바이더/API키 필드가 노출된다"
    - "프로바이더 선택 시 해당 프로바이더의 API 키 발급 대시보드 링크가 동적으로 표시된다"
    - "Admin UI 지갑 상세 페이지에서 Smart Account 프로바이더 설정을 변경할 수 있다"
  artifacts:
    - path: "packages/admin/src/pages/wallets.tsx"
      provides: "Provider fields in create form + detail page provider management"
      contains: "formProvider|providerEditing|AA_PROVIDER_DASHBOARD_URLS"
    - path: "packages/admin/src/api/endpoints.ts"
      provides: "WALLET_PROVIDER endpoint helper"
      contains: "WALLET_PROVIDER"
    - path: "packages/admin/src/__tests__/wallets-provider.test.tsx"
      provides: "Tests for provider UI"
      min_lines: 50
  key_links:
    - from: "packages/admin/src/pages/wallets.tsx"
      to: "PUT /v1/wallets/:id/provider"
      via: "apiPut(API.WALLET_PROVIDER(id), body)"
      pattern: "WALLET_PROVIDER"
---

<objective>
Admin UI 지갑 생성 폼에 프로바이더 선택 + API 키 필드를 추가하고, 지갑 상세 페이지에서 프로바이더를 변경할 수 있게 한다.

Purpose: 운영자가 Admin UI에서 Smart Account 프로바이더를 시각적으로 관리할 수 있어야 한다.
Output: wallets.tsx에 프로바이더 UI, endpoints.ts에 WALLET_PROVIDER 헬퍼, 테스트 파일
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/325-rest-api-agent-self-service/325-01-SUMMARY.md
@.planning/phases/325-rest-api-agent-self-service/325-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/core/src/constants/aa-provider-chains.ts:
```typescript
export const AA_PROVIDER_CHAIN_MAP: Record<'pimlico' | 'alchemy', Record<string, string>>;
export const AA_PROVIDER_DASHBOARD_URLS: Record<'pimlico' | 'alchemy', string> = {
  pimlico: 'https://dashboard.pimlico.io',
  alchemy: 'https://dashboard.alchemy.com',
};
```

From packages/core/src/enums/wallet.ts:
```typescript
export const AA_PROVIDER_NAMES = ['pimlico', 'alchemy', 'custom'] as const;
export type AaProviderName = (typeof AA_PROVIDER_NAMES)[number];
```

From PUT /v1/wallets/:id/provider (325-01):
```
Request body (preset): { provider: 'pimlico' | 'alchemy', apiKey: string }
Request body (custom): { provider: 'custom', bundlerUrl: string, paymasterUrl?: string }
Response: { provider: { name, supportedChains, paymasterEnabled } }
```

From GET /v1/wallets/:id response (325-02):
```
{ ...walletFields, provider: { name: AaProviderName, supportedChains: string[], paymasterEnabled: boolean } | null }
```

Existing Admin UI patterns:
- FormField component for form fields (label, name, type, value, onChange, options, placeholder)
- apiPut/apiPost/apiGet from '../api/client'
- API endpoints object from '../api/endpoints'
- useSignal for local state
- showToast('success'|'error', message) for feedback
- DetailRow component for detail page info display
- WALLET_PRESETS pattern for conditional form fields
- CoinGecko API key guide pattern (existing in settings page)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provider fields in wallet create form + API endpoint helper</name>
  <files>packages/admin/src/pages/wallets.tsx, packages/admin/src/api/endpoints.ts</files>
  <action>
1. In `packages/admin/src/api/endpoints.ts`, add:
   ```
   WALLET_PROVIDER: (id: string) => `/v1/wallets/${id}/provider`,
   ```

2. In `packages/admin/src/pages/wallets.tsx`:

   a. Add `provider` field to `Wallet` and `WalletDetail` interfaces:
      ```typescript
      provider?: { name: string; supportedChains: string[]; paymasterEnabled: boolean } | null;
      ```

   b. Add AA provider dashboard URLs constant (mirror from @waiaas/core, not import -- Admin UI is browser-side):
      ```typescript
      const AA_PROVIDER_OPTIONS = [
        { value: 'pimlico', label: 'Pimlico' },
        { value: 'alchemy', label: 'Alchemy' },
        { value: 'custom', label: 'Custom' },
      ] as const;

      const AA_PROVIDER_DASHBOARD_URLS: Record<string, string> = {
        pimlico: 'https://dashboard.pimlico.io',
        alchemy: 'https://dashboard.alchemy.com',
      };
      ```

   c. In WalletListContent, add form signals after existing formAccountType:
      ```typescript
      const formProvider = useSignal('pimlico');
      const formApiKey = useSignal('');
      const formBundlerUrl = useSignal('');
      const formPaymasterUrl = useSignal('');
      ```

   d. In the create form JSX, after the accountType select, add conditional provider fields that show ONLY when `formAccountType.value === 'smart'`:
      - Provider select (pimlico/alchemy/custom)
      - For pimlico/alchemy: API Key text input + dynamic dashboard link below it:
        `Get your API key at: <a href={AA_PROVIDER_DASHBOARD_URLS[formProvider.value]} target="_blank">{label}</a>`
      - For custom: bundlerUrl + paymasterUrl text inputs (no dashboard link)

   e. In handleCreate, when accountType is 'smart', add provider fields to createBody:
      - For preset: `{ ...createBody, provider: formProvider.value, apiKey: formApiKey.value }`
      - For custom: `{ ...createBody, provider: 'custom', bundlerUrl: formBundlerUrl.value, paymasterUrl: formPaymasterUrl.value || undefined }`
      Note: The POST /v1/wallets endpoint already accepts provider fields via CreateWalletRequestSchema (from 324-01).

   f. Reset provider fields along with other form fields after successful create.

   g. In handleChainChange, when chain is 'solana', also reset provider fields.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/admin</automated>
  </verify>
  <done>Smart Account 지갑 생성 폼에서 accountType: smart 선택 시 프로바이더/API키 필드가 노출되고, 프로바이더 변경 시 대시보드 링크가 동적 전환된다</done>
</task>

<task type="auto">
  <name>Task 2: Provider display + edit in wallet detail page + tests</name>
  <files>packages/admin/src/pages/wallets.tsx, packages/admin/src/__tests__/wallets-provider.test.tsx</files>
  <action>
1. In WalletDetailView (wallets.tsx), in the OverviewTab where accountType === 'smart' fields are rendered (after Signer Key and Deployed rows):

   a. Add provider display rows:
      ```tsx
      {wallet.value.provider && (
        <>
          <DetailRow label="Provider">
            <Badge variant="info">{wallet.value.provider.name}</Badge>
          </DetailRow>
          <DetailRow label="Supported Chains" value={wallet.value.provider.supportedChains.join(', ')} />
          <DetailRow label="Paymaster">
            <Badge variant={wallet.value.provider.paymasterEnabled ? 'success' : 'neutral'}>
              {wallet.value.provider.paymasterEnabled ? 'Enabled' : 'Disabled'}
            </Badge>
          </DetailRow>
        </>
      )}
      ```

   b. Add provider edit capability. Add signals:
      ```typescript
      const providerEditing = useSignal(false);
      const editProvider = useSignal('pimlico');
      const editApiKey = useSignal('');
      const editBundlerUrl = useSignal('');
      const editPaymasterUrl = useSignal('');
      const providerEditLoading = useSignal(false);
      ```

   c. Below the provider display rows, add a "Change Provider" button (only for smart accounts).
      When clicked, show inline edit form with same structure as create form provider fields
      (provider select + conditional API key/URL fields + dashboard link).

   d. Add handleProviderUpdate function:
      ```typescript
      const handleProviderUpdate = async () => {
        providerEditLoading.value = true;
        try {
          const body = editProvider.value === 'custom'
            ? { provider: 'custom', bundlerUrl: editBundlerUrl.value, paymasterUrl: editPaymasterUrl.value || undefined }
            : { provider: editProvider.value, apiKey: editApiKey.value };
          await apiPut(API.WALLET_PROVIDER(id), body);
          showToast('success', 'Provider updated');
          providerEditing.value = false;
          // Re-fetch wallet detail
          await fetchWallet();
        } catch (err) {
          const e = err instanceof ApiError ? err : new ApiError(0, 'UNKNOWN', 'Unknown error');
          showToast('error', getErrorMessage(e.code));
        } finally {
          providerEditLoading.value = false;
        }
      };
      ```

   e. When entering edit mode, pre-fill editProvider from current wallet.provider.name.

2. Create `packages/admin/src/__tests__/wallets-provider.test.tsx` with tests:
   - Test: Create form shows provider fields when accountType is 'smart'
   - Test: Create form hides provider fields when accountType is 'eoa'
   - Test: Dashboard link switches between Pimlico and Alchemy when provider changes
   - Test: Custom provider shows URL fields instead of API key
   - Test: Detail page shows provider info for smart account wallets
   - Test: Provider edit calls PUT /v1/wallets/:id/provider

   Follow the same mock pattern as existing wallets.test.tsx (vi.mock for api/client, toast, auth/store, layout, copy-button, empty-state, error-messages).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/admin/src/__tests__/wallets-provider.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Admin UI 지갑 상세 페이지에서 Smart Account 프로바이더 상태가 표시되고, Change Provider 버튼으로 프로바이더를 변경할 수 있다. 6개 이상의 테스트가 통과한다.</done>
</task>

</tasks>

<verification>
- `pnpm turbo run typecheck --filter=@waiaas/admin` passes
- `pnpm vitest run packages/admin/src/__tests__/wallets-provider.test.tsx` passes with 6+ tests
- Smart account create form shows provider/apiKey fields conditionally
- Detail page shows provider info and allows editing
</verification>

<success_criteria>
- PROV-06: accountType: smart 선택 시에만 프로바이더 필드 노출
- PROV-07: 상세 페이지에서 프로바이더 변경 가능
- GUID-01: 프로바이더 선택 시 대시보드 링크 표시
- GUID-02: CoinGecko 안내와 동일한 패턴
- GUID-03: 프로바이더 변경 시 링크 동적 전환
</success_criteria>

<output>
After completion, create `.planning/phases/326-admin-ui-mcp-connect-info/326-01-SUMMARY.md`
</output>
