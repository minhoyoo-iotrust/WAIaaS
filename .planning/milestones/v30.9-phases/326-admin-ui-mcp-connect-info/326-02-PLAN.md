---
phase: 326-admin-ui-mcp-connect-info
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/api/routes/connect-info.ts
  - packages/daemon/src/__tests__/connect-info-provider.test.ts
  - packages/mcp/src/tools/get-provider-status.ts
  - packages/mcp/src/server.ts
  - packages/mcp/src/__tests__/provider-tool.test.ts
autonomous: true
requirements: [STAT-04, STAT-05]

must_haves:
  truths:
    - "connect-info 프롬프트에 Smart Account 프로바이더 상태(이름, 지원 체인, 페이마스터 활성)가 포함된다"
    - "MCP get_provider_status 도구로 프로바이더 상태를 조회할 수 있다"
  artifacts:
    - path: "packages/daemon/src/api/routes/connect-info.ts"
      provides: "Provider info in connect-info prompt"
      contains: "provider|Smart Account"
    - path: "packages/mcp/src/tools/get-provider-status.ts"
      provides: "MCP tool for provider status query"
      contains: "get_provider_status"
    - path: "packages/mcp/src/server.ts"
      provides: "Tool registration"
      contains: "registerGetProviderStatus"
    - path: "packages/daemon/src/__tests__/connect-info-provider.test.ts"
      provides: "Tests for connect-info provider prompt"
      min_lines: 30
    - path: "packages/mcp/src/__tests__/provider-tool.test.ts"
      provides: "Tests for MCP provider tool"
      min_lines: 20
  key_links:
    - from: "packages/daemon/src/api/routes/connect-info.ts"
      to: "wallets.aaProvider DB column"
      via: "SQL select in connect-info route"
      pattern: "aaProvider|aa_provider"
    - from: "packages/mcp/src/tools/get-provider-status.ts"
      to: "GET /v1/wallets/:id"
      via: "apiClient.get"
      pattern: "wallets.*provider"
---

<objective>
connect-info 프롬프트에 프로바이더 상태를 추가하고, MCP 도구로 프로바이더 상태를 조회할 수 있게 한다.

Purpose: 에이전트가 자신의 지갑이 가스 대납 가능한지, 어떤 체인에서 Smart Account를 사용할 수 있는지 인지해야 한다.
Output: connect-info.ts 수정, MCP get_provider_status 도구 신규, 테스트 파일 2개
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/325-rest-api-agent-self-service/325-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/daemon/src/api/routes/connect-info.ts:
```typescript
export interface BuildConnectInfoPromptParams {
  wallets: Array<{
    id: string;
    name: string;
    chain: string;
    environment: string;
    address: string;
    networks: string[];
    policies: Array<{ type: string }>;
    erc8004?: { agentId: string; registryAddress: string; status: string };
  }>;
  capabilities: string[];
  defaultDeny: DefaultDenyStatus;
  baseUrl: string;
  version: string;
}

export function buildConnectInfoPrompt(params: BuildConnectInfoPromptParams): string;
```

From packages/daemon/src/infrastructure/database/schema.ts (wallets table):
```typescript
// Provider columns added in migration v41 (324-01):
aaProvider: text('aa_provider'),        // 'pimlico' | 'alchemy' | 'custom' | null
aaBundlerUrl: text('aa_bundler_url'),   // URL for custom bundler
aaPaymasterUrl: text('aa_paymaster_url'), // URL for custom paymaster
aaApiKeyEnc: text('aa_api_key_enc'),    // Encrypted API key
```

From packages/daemon/src/api/routes/wallets.ts:
```typescript
export function buildProviderStatus(wallet: {
  aaProvider: string | null;
  aaPaymasterUrl?: string | null;
}): { name: AaProviderName; supportedChains: string[]; paymasterEnabled: boolean } | null;
```

From GET /v1/wallets/:id response:
```
{ ...walletFields, provider: { name, supportedChains, paymasterEnabled } | null }
```

MCP tool pattern (from packages/mcp/src/tools/get-wallet-info.ts):
```typescript
export function registerGetWalletInfo(server: McpServer, apiClient: ApiClient, walletContext?: WalletContext): void {
  server.tool('get_wallet_info', withWalletPrefix('description', walletContext?.walletName), { ... }, async (args) => { ... });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider status to connect-info prompt</name>
  <files>packages/daemon/src/api/routes/connect-info.ts, packages/daemon/src/__tests__/connect-info-provider.test.ts</files>
  <action>
1. In `packages/daemon/src/api/routes/connect-info.ts`:

   a. Extend `BuildConnectInfoPromptParams.wallets` array item type to include:
      ```typescript
      provider?: { name: string; supportedChains: string[]; paymasterEnabled: boolean } | null;
      accountType?: string;
      ```

   b. In `buildConnectInfoPrompt`, for each wallet, after the ERC-8004 section, add provider info:
      ```typescript
      if (w.accountType === 'smart' && w.provider) {
        lines.push(`   Smart Account: ${w.provider.name} provider`);
        lines.push(`   Gas Sponsorship: ${w.provider.paymasterEnabled ? 'ENABLED (paymaster active)' : 'DISABLED'}`);
        lines.push(`   Provider Chains: ${w.provider.supportedChains.join(', ')}`);
      } else if (w.accountType === 'smart') {
        lines.push(`   Smart Account: No provider configured (gas sponsorship unavailable)`);
      }
      ```

   c. In the route handler, extend the `linkedWallets` query to include `aaProvider`:
      Add `aaProvider: wallets.aaProvider` and `aaPaymasterUrl: wallets.aaPaymasterUrl` to the select.

   d. Import `buildProviderStatus` from `./wallets.js` and use it when building promptWallets:
      ```typescript
      provider: buildProviderStatus({ aaProvider: w.aaProvider, aaPaymasterUrl: w.aaPaymasterUrl }),
      accountType: (w.accountType as string) ?? 'eoa',
      ```

   e. Also add `accountType` and `provider` to the wallets array in the JSON response body
      (alongside existing fields like id, name, chain, etc.) so the structured data also includes it.

   f. Add 'smart_account' to capabilities when any linked wallet has accountType === 'smart' and a provider configured.

2. Create `packages/daemon/src/__tests__/connect-info-provider.test.ts`:
   - Test: buildConnectInfoPrompt includes provider info for smart account wallet
   - Test: buildConnectInfoPrompt shows "No provider configured" for smart account without provider
   - Test: buildConnectInfoPrompt omits provider section for EOA wallet
   - Test: Gas sponsorship shows ENABLED when paymasterEnabled is true

   Import buildConnectInfoPrompt directly and test the string output.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/connect-info-provider.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>connect-info 프롬프트에 Smart Account 프로바이더 상태가 포함되고, 4개 테스트가 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: MCP get_provider_status tool</name>
  <files>packages/mcp/src/tools/get-provider-status.ts, packages/mcp/src/server.ts, packages/mcp/src/__tests__/provider-tool.test.ts</files>
  <action>
1. Create `packages/mcp/src/tools/get-provider-status.ts`:
   ```typescript
   /**
    * get_provider_status tool: Get Smart Account provider status for a wallet.
    *
    * Reads provider info from GET /v1/wallets/:id response.
    * Shows provider name, supported chains, and paymaster (gas sponsorship) status.
    */
   import { z } from 'zod';
   import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
   import { type ApiClient, toToolResult } from '../api-client.js';
   import { type WalletContext, withWalletPrefix } from '../server.js';

   export function registerGetProviderStatus(server: McpServer, apiClient: ApiClient, walletContext?: WalletContext): void {
     server.tool(
       'get_provider_status',
       withWalletPrefix('Get Smart Account provider status: provider name, supported chains, gas sponsorship (paymaster) status.', walletContext?.walletName),
       {
         wallet_id: z.string().describe('Target wallet ID (UUID).'),
       },
       async (args) => {
         const result = await apiClient.get<Record<string, unknown>>(
           '/v1/wallets/' + args.wallet_id,
         );
         if (!result.ok) return toToolResult(result);

         const wallet = result.data;
         const provider = wallet['provider'] as { name: string; supportedChains: string[]; paymasterEnabled: boolean } | null;
         const accountType = (wallet['accountType'] as string) ?? 'eoa';

         if (accountType !== 'smart') {
           return {
             content: [{ type: 'text' as const, text: JSON.stringify({ accountType, provider: null, message: 'This is an EOA wallet. Smart Account provider is only applicable to smart account wallets.' }) }],
           };
         }

         if (!provider) {
           return {
             content: [{ type: 'text' as const, text: JSON.stringify({ accountType: 'smart', provider: null, message: 'No provider configured. Use PUT /v1/wallets/:id/provider to set up a provider (pimlico, alchemy, or custom).' }) }],
           };
         }

         return {
           content: [{ type: 'text' as const, text: JSON.stringify({
             accountType: 'smart',
             provider: {
               name: provider.name,
               supportedChains: provider.supportedChains,
               paymasterEnabled: provider.paymasterEnabled,
               gasSponsorshipStatus: provider.paymasterEnabled ? 'Gas fees are sponsored by the paymaster on supported chains.' : 'No paymaster configured. Transactions require the wallet to hold native gas tokens.',
             },
           }) }],
         };
       },
     );
   }
   ```

2. In `packages/mcp/src/server.ts`:
   a. Add import: `import { registerGetProviderStatus } from './tools/get-provider-status.js';`
   b. Register after existing tools (before resource registrations):
      `registerGetProviderStatus(server, apiClient, walletContext);`
   c. Update the tool count comment from 28 to 29.

3. Create `packages/mcp/src/__tests__/provider-tool.test.ts`:
   Follow the pattern from tools.test.ts (mock ApiClient, capture handler via mock server.tool).
   - Test: Returns provider info for smart account with provider
   - Test: Returns 'no provider' message for smart account without provider
   - Test: Returns 'EOA wallet' message for non-smart account
   - Test: Passes error through when API call fails
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/mcp/src/__tests__/provider-tool.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>MCP get_provider_status 도구가 등록되고, 4개 테스트가 통과하며, Smart Account 프로바이더 상태를 조회할 수 있다</done>
</task>

</tasks>

<verification>
- `pnpm turbo run typecheck --filter=@waiaas/daemon --filter=@waiaas/mcp` passes
- `pnpm vitest run packages/daemon/src/__tests__/connect-info-provider.test.ts` passes with 4+ tests
- `pnpm vitest run packages/mcp/src/__tests__/provider-tool.test.ts` passes with 4+ tests
- connect-info prompt includes provider section for smart account wallets
- MCP server registers 29 tools (was 28)
</verification>

<success_criteria>
- STAT-04: connect-info 프롬프트에 프로바이더 상태 포함 (이름, 지원 체인, 페이마스터)
- STAT-05: MCP get_provider_status 도구로 프로바이더 상태 조회 가능
</success_criteria>

<output>
After completion, create `.planning/phases/326-admin-ui-mcp-connect-info/326-02-SUMMARY.md`
</output>
