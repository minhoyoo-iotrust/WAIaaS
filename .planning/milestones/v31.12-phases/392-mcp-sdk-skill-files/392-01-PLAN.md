---
phase: 392-mcp-sdk-skill-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/tools/list-offchain-actions.ts
  - packages/mcp/src/tools/list-credentials.ts
  - packages/mcp/src/server.ts
  - packages/mcp/src/__tests__/external-action-tools.test.ts
  - packages/sdk/src/client.ts
  - packages/sdk/src/types.ts
  - packages/sdk/src/index.ts
  - packages/sdk/src/__tests__/client-external-actions.test.ts
autonomous: true
requirements: [INTEG-01, INTEG-02, INTEG-03, INTEG-04, INTEG-05, INTEG-06]

must_haves:
  truths:
    - "MCP action_* 도구로 off-chain action 실행 시 기존 REST API를 통해 올바른 파이프라인이 호출된다"
    - "MCP list_offchain_actions 도구로 off-chain action 이력을 venue/status 필터와 페이지네이션으로 조회할 수 있다"
    - "MCP list_credentials 도구로 현재 세션의 credential 메타데이터 목록을 확인할 수 있다"
    - "SDK executeAction()으로 off-chain action이 자동 실행된다"
    - "SDK listOffchainActions()/getActionResult()로 off-chain action 이력/상세를 조회할 수 있다"
    - "SDK AdminClient로 credential CRUD(create/delete/rotate)가 가능하다"
  artifacts:
    - path: "packages/mcp/src/tools/list-offchain-actions.ts"
      provides: "MCP tool for off-chain action list query"
    - path: "packages/mcp/src/tools/list-credentials.ts"
      provides: "MCP tool for credential metadata list"
    - path: "packages/sdk/src/client.ts"
      provides: "listOffchainActions, getActionResult, listCredentials, createCredential, deleteCredential, rotateCredential methods"
    - path: "packages/sdk/src/types.ts"
      provides: "OffchainActionItem, OffchainActionDetail, OffchainActionsListResponse, CredentialMetadata, CreateCredentialParams types"
  key_links:
    - from: "packages/mcp/src/tools/list-offchain-actions.ts"
      to: "GET /v1/wallets/:id/actions"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*wallets.*actions"
    - from: "packages/sdk/src/client.ts"
      to: "GET /v1/wallets/:id/actions"
      via: "http.get"
      pattern: "http\\.get.*wallets.*actions"
---

<objective>
MCP 2개 신규 도구 + SDK 6개 메서드를 추가하여 AI 에이전트가 off-chain action 이력 조회와 credential 확인을 수행할 수 있게 한다.

Purpose: External Action 프레임워크의 최종 통합 인터페이스 -- MCP/SDK를 통해 AI 에이전트가 off-chain action을 실행하고 조회한다.
Output: MCP 도구 2종 + SDK 메서드 6종 + 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/390-pipeline-routing-query-api/390-02-SUMMARY.md

<interfaces>
<!-- REST API endpoints consumed by MCP/SDK (from Phase 390) -->

GET /v1/wallets/:walletId/actions?venue=&status=&limit=&offset=
Response: { actions: ExternalActionItem[], total: number, limit: number, offset: number }

GET /v1/wallets/:walletId/actions/:actionId
Response: ExternalActionDetail (id, actionKind, venue, operation, status, bridgeStatus, createdAt, provider, actionName, metadata, bridgeMetadata, error, txHash)

POST /v1/actions/:provider/:action (existing -- auto-routes off-chain via kind field)

<!-- Credential REST API (from Phase 388) -->

GET /v1/wallets/:walletId/credentials (masterAuth) -> CredentialMetadata[]
POST /v1/wallets/:walletId/credentials (masterAuth) -> CredentialMetadata
DELETE /v1/wallets/:walletId/credentials/:ref (masterAuth) -> { deleted: true }
PUT /v1/wallets/:walletId/credentials/:ref/rotate (masterAuth) -> CredentialMetadata

GET /v1/admin/credentials (masterAuth) -> CredentialMetadata[]
POST /v1/admin/credentials (masterAuth) -> CredentialMetadata
DELETE /v1/admin/credentials/:ref (masterAuth) -> { deleted: true }
PUT /v1/admin/credentials/:ref/rotate (masterAuth) -> CredentialMetadata

<!-- SDK existing pattern -->
From packages/sdk/src/client.ts:
```typescript
async executeAction(provider: string, action: string, params?: ExecuteActionParams): Promise<ExecuteActionResponse>
// Already calls POST /v1/actions/:provider/:action -- off-chain actions auto-route via kind field (INTEG-06 satisfied)
```

<!-- MCP existing pattern -->
From packages/mcp/src/tools/action-provider.ts:
```typescript
// registerActionProviderTools already calls POST /v1/actions/:provider/:action
// Off-chain actions auto-route via kind field (INTEG-01 satisfied)
```

<!-- MCP tool registration pattern -->
From packages/mcp/src/server.ts:
```typescript
export function createMcpServer(apiClient: ApiClient, walletContext?: WalletContext): McpServer
// Import and call registerXxx(server, apiClient, walletContext)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP 도구 2종 (list_offchain_actions + list_credentials) + 테스트</name>
  <files>
    packages/mcp/src/tools/list-offchain-actions.ts
    packages/mcp/src/tools/list-credentials.ts
    packages/mcp/src/server.ts
    packages/mcp/src/__tests__/external-action-tools.test.ts
  </files>
  <action>
1. **list-offchain-actions.ts**: MCP 도구 등록 함수 `registerListOffchainActions(server, apiClient, walletContext)` 생성.
   - 도구 이름: `list_offchain_actions`
   - 설명: withWalletPrefix('List off-chain action history (signedData/signedHttp) with venue/status filter and pagination.', walletContext?.walletName)
   - 입력 파라미터: venue (z.string().optional()), status (z.string().optional()), limit (z.number().optional()), offset (z.number().optional()), wallet_id (z.string().optional())
   - GET /v1/wallets/{walletId}/actions?venue=&status=&limit=&offset= 호출 (wallet_id가 있으면 URL에 포함, 없으면 기존 세션 기본 지갑 사용을 위해 /v1/wallet/actions 대신 walletId를 query에 전달). 실제 API는 /v1/wallets/:walletId/actions이므로 wallet_id 필수 -- MCP에서 wallet_id 없으면 apiClient.get('/v1/wallets/actions') fallback 시도 or walletId를 QueryString으로 전달.
   - 패턴 참고: list-transactions.ts (URLSearchParams + apiClient.get + toToolResult)

2. **list-credentials.ts**: MCP 도구 등록 함수 `registerListCredentials(server, apiClient, walletContext)` 생성.
   - 도구 이름: `list_credentials`
   - 설명: withWalletPrefix('List credential metadata for a wallet (names, types, expiry -- never shows values).', walletContext?.walletName)
   - 입력 파라미터: wallet_id (z.string().optional())
   - GET /v1/wallets/{walletId}/credentials 호출 (sessionAuth -- credential 목록 조회는 list route가 현재 masterAuth이나, INTEG-03 요구사항이 "sessionAuth 기반 credential 목록 확인"이므로 기존 daemon credentials route가 masterAuth로 되어 있어도 MCP에서는 그대로 호출. masterAuth가 아닌 sessionAuth로 호출하면 403이 올 수 있으므로, 실제로는 apiClient가 sessionToken을 사용하므로 daemon의 credentials list route에 sessionAuth 읽기가 이미 구현됨(CRED-05: "sessionAuth 읽기, masterAuth 쓰기")). wallet_id query param으로 전달.

3. **server.ts**: createMcpServer 함수에서 두 도구 import + register 호출 추가. 도구 수 주석 업데이트 (37 -> 39).

4. **external-action-tools.test.ts**: 테스트 파일 생성.
   - list_offchain_actions 도구: (1) venue/status/limit/offset 파라미터로 올바른 GET URL 호출 확인, (2) wallet_id 전달 시 URL에 walletId 포함 확인, (3) 파라미터 없을 때 기본 호출 확인
   - list_credentials 도구: (1) wallet_id 전달 시 올바른 GET URL 호출 확인, (2) 응답에 credential value가 포함되지 않음 확인
   - 패턴 참고: polymarket-tools.test.ts 또는 nft-tools.test.ts (McpServer mock + apiClient mock)

INTEG-01 참고: MCP 기존 action 도구(action-provider.ts의 registerActionProviderTools)는 POST /v1/actions/:provider/:action을 호출하고, daemon이 kind-based routing으로 자동 분기하므로 MCP 코드 변경 불필요. 이 사실을 테스트 파일 주석으로 문서화.

INTEG-03 참고: credential list는 per-wallet credentials GET route가 CRED-05에서 "sessionAuth 읽기"로 정의됨. daemon의 credentials.ts route를 확인 -- 현재 masterAuth이면, MCP에서는 그대로 호출하되 403 에러를 toToolResult가 처리함. 만약 daemon route가 sessionAuth 읽기를 지원하면 정상 동작.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/mcp run test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - list_offchain_actions 도구가 venue/status/limit/offset/wallet_id 파라미터로 올바른 REST API를 호출한다
    - list_credentials 도구가 wallet_id로 credential 메타데이터 목록을 조회한다
    - server.ts에 두 도구가 등록되어 총 39개 도구가 된다
    - 5개 이상 테스트가 통과한다
  </done>
</task>

<task type="auto">
  <name>Task 2: SDK 메서드 6종 (listOffchainActions + getActionResult + credential CRUD) + 테스트</name>
  <files>
    packages/sdk/src/client.ts
    packages/sdk/src/types.ts
    packages/sdk/src/index.ts
    packages/sdk/src/__tests__/client-external-actions.test.ts
  </files>
  <action>
1. **types.ts**: 다음 타입 추가 (existing types 섹션 뒤에).
   ```typescript
   // External Action Types
   export interface OffchainActionItem {
     id: string;
     actionKind: string;
     venue: string | null;
     operation: string | null;
     status: string;
     bridgeStatus: string | null;
     createdAt: number;
     provider: string | null;
     actionName: string | null;
   }

   export interface OffchainActionDetail extends OffchainActionItem {
     metadata: Record<string, unknown> | null;
     bridgeMetadata: Record<string, unknown> | null;
     error: string | null;
     txHash: string | null;
   }

   export interface OffchainActionsListResponse {
     actions: OffchainActionItem[];
     total: number;
     limit: number;
     offset: number;
   }

   export interface ListOffchainActionsParams {
     walletId: string;
     venue?: string;
     status?: string;
     limit?: number;
     offset?: number;
   }

   // Credential Types
   export interface CredentialMetadata {
     id: string;
     name: string;
     type: string;
     walletId: string | null;
     expiresAt: number | null;
     createdAt: number;
     updatedAt: number;
   }

   export interface CreateCredentialParams {
     name: string;
     type: string;
     value: string;
     expiresAt?: number;
   }
   ```

2. **client.ts**: WAIaaSClient 클래스에 다음 메서드 추가 (Actions 섹션 뒤에 "--- External Actions ---" 섹션).
   - `listOffchainActions(params: ListOffchainActionsParams): Promise<OffchainActionsListResponse>` -- GET /v1/wallets/:walletId/actions with query params, sessionAuth
   - `getActionResult(walletId: string, actionId: string): Promise<OffchainActionDetail>` -- GET /v1/wallets/:walletId/actions/:actionId, sessionAuth
   - `listCredentials(walletId: string): Promise<CredentialMetadata[]>` -- GET /v1/wallets/:walletId/credentials, sessionAuth

   그리고 "--- Admin Credential CRUD ---" 섹션:
   - `createCredential(walletId: string, params: CreateCredentialParams): Promise<CredentialMetadata>` -- POST /v1/wallets/:walletId/credentials, masterAuth
   - `deleteCredential(walletId: string, ref: string): Promise<{ deleted: boolean }>` -- DELETE /v1/wallets/:walletId/credentials/:ref, masterAuth
   - `rotateCredential(walletId: string, ref: string, newValue: string): Promise<CredentialMetadata>` -- PUT /v1/wallets/:walletId/credentials/:ref/rotate, masterAuth. Body: { value: newValue }

   masterAuth 헤더는 기존 masterAuthHeaders() private method 사용 (client.ts에 이미 있으면 사용, 없으면 `{ 'X-Master-Password': this.masterPassword }` 패턴 사용).

   주의: INTEG-06(SDK 기존 executeAction() off-chain action 자동 지원)은 이미 완료됨 -- executeAction()은 POST /v1/actions/:provider/:action을 호출하고 daemon이 kind-based routing으로 자동 분기. 코드 변경 불필요.

3. **index.ts**: 새 타입들 export 추가 (OffchainActionItem, OffchainActionDetail, OffchainActionsListResponse, ListOffchainActionsParams, CredentialMetadata, CreateCredentialParams).

4. **client-external-actions.test.ts**: 테스트 파일 생성.
   - listOffchainActions: (1) 기본 호출 확인 (GET + query params), (2) venue/status 필터 포함 확인
   - getActionResult: (1) 올바른 URL 호출 확인
   - listCredentials: (1) sessionAuth 헤더로 GET 호출 확인
   - createCredential: (1) masterAuth 헤더로 POST 호출 확인
   - deleteCredential: (1) masterAuth 헤더로 DELETE 호출 확인
   - rotateCredential: (1) masterAuth 헤더로 PUT 호출 확인, body에 value 포함
   - 패턴 참고: client.test.ts의 기존 executeAction 테스트 (HttpClient mock + 응답 검증)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/sdk run test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - listOffchainActions/getActionResult로 off-chain action 이력/상세를 조회할 수 있다
    - listCredentials로 sessionAuth 기반 credential 메타데이터를 조회할 수 있다
    - createCredential/deleteCredential/rotateCredential로 masterAuth 기반 credential CRUD가 가능하다
    - 타입이 index.ts에서 export된다
    - 8개 이상 테스트가 통과한다
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @waiaas/mcp run test` -- MCP 전체 테스트 통과
- `pnpm --filter @waiaas/sdk run test` -- SDK 전체 테스트 통과
- `pnpm turbo run typecheck --filter=@waiaas/mcp --filter=@waiaas/sdk` -- 타입 체크 통과
</verification>

<success_criteria>
1. MCP list_offchain_actions 도구로 off-chain action 이력을 venue/status 필터와 페이지네이션으로 조회할 수 있다
2. MCP list_credentials 도구로 credential 메타데이터 목록을 확인할 수 있다
3. SDK listOffchainActions()/getActionResult()로 off-chain action 이력/상세를 조회할 수 있다
4. SDK createCredential()/deleteCredential()/rotateCredential()로 credential CRUD가 가능하다
5. MCP 기존 action 도구와 SDK 기존 executeAction()은 off-chain action을 변경 없이 자동 지원한다 (daemon 자동 라우팅)
</success_criteria>

<output>
After completion, create `.planning/phases/392-mcp-sdk-skill-files/392-01-SUMMARY.md`
</output>
