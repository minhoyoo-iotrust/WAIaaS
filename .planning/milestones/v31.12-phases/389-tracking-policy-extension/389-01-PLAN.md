---
phase: 389-tracking-policy-extension
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/common/async-status-tracker.ts
  - packages/actions/src/index.ts
  - packages/actions/src/__tests__/async-tracking-state.test.ts
  - packages/daemon/src/services/async-polling-service.ts
  - packages/daemon/src/__tests__/async-polling-service.test.ts
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/infrastructure/database/schema.ts
  - packages/daemon/src/__tests__/migration-v57.test.ts
  - packages/core/src/enums/notification.ts
  - packages/core/src/i18n/en.ts
  - packages/core/src/i18n/ko.ts
  - packages/core/src/interfaces/action-provider.types.ts
autonomous: true
requirements: [TRACK-01, TRACK-02, TRACK-03, TRACK-04, TRACK-05, TRACK-06]

must_haves:
  truths:
    - "AsyncTrackingResult.state가 9종 값(PENDING/COMPLETED/FAILED/TIMEOUT/PARTIALLY_FILLED/FILLED/CANCELED/SETTLED/EXPIRED)을 허용한다"
    - "AsyncPollingService가 PARTIALLY_FILLED 상태인 트랜잭션도 폴링 대상에 포함한다"
    - "ExternalActionTracker가 IAsyncStatusTracker를 구현하고 ActionProvider.checkStatus()에 위임한다"
    - "IActionProvider에 선택적 checkStatus()/execute() 메서드가 추가된다"
    - "상태 전이 시 EventBus로 action:status-changed/completed/failed 이벤트가 발행된다"
    - "6종 알림 이벤트(external_action_*)가 NotificationEventType에 등록된다"
  artifacts:
    - path: "packages/actions/src/common/async-status-tracker.ts"
      provides: "AsyncTrackingResult 9-state, BRIDGE_STATUS_VALUES 확장"
      contains: "PARTIALLY_FILLED"
    - path: "packages/daemon/src/services/async-polling-service.ts"
      provides: "PARTIALLY_FILLED 폴링 포함, ExternalActionTracker 위임"
      contains: "PARTIALLY_FILLED"
    - path: "packages/daemon/src/infrastructure/database/migrate.ts"
      provides: "v57 migration (composite index)"
      contains: "version = 57"
    - path: "packages/core/src/enums/notification.ts"
      provides: "6 new external_action notification events"
      contains: "EXTERNAL_ACTION_PARTIALLY_FILLED"
  key_links:
    - from: "packages/daemon/src/services/async-polling-service.ts"
      to: "packages/actions/src/common/async-status-tracker.ts"
      via: "AsyncTrackingResult state type"
      pattern: "PARTIALLY_FILLED|FILLED|SETTLED"
    - from: "packages/daemon/src/services/async-polling-service.ts"
      to: "packages/core/src/enums/notification.ts"
      via: "emitNotification callback"
      pattern: "EXTERNAL_ACTION"
---

<objective>
AsyncTrackingResult 9-state 확장 + ExternalActionTracker + DB v57 복합 인덱스 + 알림 이벤트 6종

Purpose: off-chain action(CLOB order, CEX trade 등)의 부분 체결/완전 체결/정산/취소/만료 상태를 추적하는 인프라를 확장한다.
Output: 9-state tracking type system, ExternalActionTracker, DB v57, 6 notification events
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/design/81-external-action-framework.md (D6 section)

@.planning/phases/386-type-system-errors-db/386-01-SUMMARY.md
@.planning/phases/386-type-system-errors-db/386-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/actions/src/common/async-status-tracker.ts:
```typescript
export const BRIDGE_STATUS_VALUES = [
  'PENDING', 'COMPLETED', 'FAILED', 'BRIDGE_MONITORING', 'TIMEOUT', 'REFUNDED',
] as const;
export type BridgeStatus = (typeof BRIDGE_STATUS_VALUES)[number];
export const BridgeStatusEnum = z.enum(BRIDGE_STATUS_VALUES);

export interface AsyncTrackingResult {
  state: 'PENDING' | 'COMPLETED' | 'FAILED' | 'TIMEOUT';
  details?: Record<string, unknown>;
  nextIntervalOverride?: number;
}

export interface IAsyncStatusTracker {
  checkStatus(txId: string, metadata: Record<string, unknown>): Promise<AsyncTrackingResult>;
  readonly name: string;
  readonly maxAttempts: number;
  readonly pollIntervalMs: number;
  readonly timeoutTransition: 'TIMEOUT' | 'BRIDGE_MONITORING' | 'CANCELLED';
}
```

From packages/core/src/interfaces/action-provider.types.ts:
```typescript
export interface IActionProvider {
  readonly metadata: ActionProviderMetadata;
  readonly actions: readonly ActionDefinition[];
  resolve(actionName: string, params: Record<string, unknown>, context: ActionContext): Promise<...>;
}
```

From packages/daemon/src/services/async-polling-service.ts:
```typescript
export interface AsyncPollingCallbacks {
  emitNotification?(eventType: string, walletId: string, data: Record<string, unknown>): void;
  releaseReservation?(txId: string): void;
  resumePipeline?(txId: string, walletId: string): void;
}
```

From packages/core/src/enums/notification.ts:
```typescript
export const NOTIFICATION_EVENT_TYPES = [..., 'EXCHANGE_TIMEOUT'] as const;
// 현재 60+ event types
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AsyncTrackingResult 9-state + BRIDGE_STATUS_VALUES 확장 + IActionProvider 선택적 메서드</name>
  <files>
    packages/actions/src/common/async-status-tracker.ts,
    packages/actions/src/__tests__/async-tracking-state.test.ts,
    packages/actions/src/index.ts,
    packages/core/src/interfaces/action-provider.types.ts,
    packages/core/src/__tests__/action-provider-compat.test.ts
  </files>
  <behavior>
    - AsyncTrackingResult.state가 9종 값(PENDING/COMPLETED/FAILED/TIMEOUT/PARTIALLY_FILLED/FILLED/CANCELED/SETTLED/EXPIRED) 허용
    - BRIDGE_STATUS_VALUES에 5종 추가 (PARTIALLY_FILLED/FILLED/CANCELED/SETTLED/EXPIRED)
    - BridgeStatus 타입이 11-value union
    - AsyncTrackingStateEnum Zod enum으로 9종 파싱 성공
    - isTerminalState() 유틸리티: COMPLETED/FILLED/SETTLED/CANCELED/EXPIRED/FAILED/TIMEOUT -> true, PENDING/PARTIALLY_FILLED -> false
    - isContinuePolling() 유틸리티: PENDING/PARTIALLY_FILLED -> true, 나머지 -> false
    - IActionProvider에 선택적 checkStatus?(actionId: string, metadata: Record<string, unknown>): Promise<AsyncTrackingResult> 추가
    - IActionProvider에 선택적 execute?(actionName: string, signedPayload: unknown, context: ActionContext): Promise<Record<string, unknown>> 추가
    - 기존 IAsyncStatusTracker 인터페이스 무변경 (기존 4종 state subset 유지)
    - 기존 5종 tracker(LiFi, Across, Lido, Jito, GasCondition) 컴파일 무변경
  </behavior>
  <action>
    1. RED: packages/actions/src/__tests__/async-tracking-state.test.ts 생성 - 9종 state 파싱, isTerminalState/isContinuePolling 유틸리티, BRIDGE_STATUS_VALUES 11종 확인, 기존 4종 state 호환 테스트 작성
    2. RED: packages/core/src/__tests__/action-provider-compat.test.ts에 checkStatus/execute 선택적 메서드 테스트 추가
    3. GREEN: packages/actions/src/common/async-status-tracker.ts 수정:
       - BRIDGE_STATUS_VALUES에 'PARTIALLY_FILLED', 'FILLED', 'CANCELED', 'SETTLED', 'EXPIRED' 추가
       - AsyncTrackingResult.state 타입을 9종으로 확장
       - AsyncTrackingStateEnum = z.enum([...9종]) 추가 export
       - isTerminalState(state: string): boolean 유틸리티 추가
       - isContinuePolling(state: string): boolean 유틸리티 추가
    4. GREEN: packages/core/src/interfaces/action-provider.types.ts에 IActionProvider 선택적 메서드 추가:
       - checkStatus?(actionId: string, metadata: Record<string, unknown>): Promise<AsyncTrackingResult>
       - execute?(actionName: string, signedPayload: unknown, context: ActionContext): Promise<Record<string, unknown>>
       - AsyncTrackingResult import 추가 (from @waiaas/actions)
    5. packages/actions/src/index.ts에서 AsyncTrackingStateEnum, isTerminalState, isContinuePolling 신규 export 추가
    6. pnpm turbo run typecheck으로 기존 5종 tracker 컴파일 확인

    주의: BRIDGE_STATUS_VALUES 변경 시 packages/daemon/src/infrastructure/database/schema.ts의 check_bridge_status SQL도 동기화 필요 (Task 2에서 처리).
    주의: IAsyncStatusTracker 인터페이스 자체는 변경하지 않음 (기존 tracker 코드 무변경 보장).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/actions/src/__tests__/async-tracking-state.test.ts packages/core/src/__tests__/action-provider-compat.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>AsyncTrackingResult 9-state 파싱 성공, BRIDGE_STATUS_VALUES 11종, isTerminalState/isContinuePolling 유틸리티 동작, IActionProvider 선택적 checkStatus/execute 추가, 기존 tracker 타입 호환</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DB v57 마이그레이션 + check_bridge_status 확장 + AsyncPollingService 쿼리 확장 + ExternalActionTracker + 알림 이벤트 6종</name>
  <files>
    packages/daemon/src/infrastructure/database/migrate.ts,
    packages/daemon/src/infrastructure/database/schema.ts,
    packages/daemon/src/__tests__/migration-v57.test.ts,
    packages/daemon/src/services/async-polling-service.ts,
    packages/daemon/src/__tests__/async-polling-service.test.ts,
    packages/core/src/enums/notification.ts,
    packages/core/src/i18n/en.ts,
    packages/core/src/i18n/ko.ts
  </files>
  <behavior>
    - v57 마이그레이션: idx_transactions_action_kind_bridge_status 복합 인덱스 생성 (CREATE INDEX IF NOT EXISTS)
    - LATEST_SCHEMA_VERSION = 57
    - check_bridge_status SQL이 신규 5종 값 허용 (PARTIALLY_FILLED/FILLED/CANCELED/SETTLED/EXPIRED)
    - Drizzle schema check constraint 동기화
    - AsyncPollingService.pollAll() 쿼리가 bridge_status IN ('PENDING', 'BRIDGE_MONITORING', 'PARTIALLY_FILLED')로 확장
    - AsyncPollingService.resolveTrackerName()이 metadata.trackerName을 metadata.tracker보다 우선
    - AsyncPollingService.processResult()가 PARTIALLY_FILLED/FILLED/SETTLED/CANCELED/EXPIRED 상태 처리
    - PARTIALLY_FILLED: bridge_status 업데이트 + 알림 + 폴링 계속
    - FILLED/SETTLED: bridge_status 업데이트 + reservation 해제 + 알림 + 폴링 종료
    - CANCELED/EXPIRED: bridge_status 업데이트 + reservation 해제 + 알림 + 폴링 종료
    - 6종 NotificationEventType 추가: EXTERNAL_ACTION_PARTIALLY_FILLED/FILLED/SETTLED/CANCELED/EXPIRED/FAILED
    - en.ts/ko.ts i18n 번역 추가
    - 기존 COMPLETED/FAILED/TIMEOUT/PENDING 처리 무변경
  </behavior>
  <action>
    1. RED: packages/daemon/src/__tests__/migration-v57.test.ts 생성 - v56->v57 마이그레이션 테스트 (인덱스 생성 확인, 스키마 버전 확인)
    2. RED: packages/daemon/src/__tests__/async-polling-service.test.ts에 테스트 추가:
       - PARTIALLY_FILLED 폴링 대상 포함 확인
       - PARTIALLY_FILLED processResult -> bridge_status 업데이트 + 알림 + 폴링 계속
       - FILLED processResult -> bridge_status 업데이트 + reservation 해제 + 알림
       - SETTLED processResult -> bridge_status 업데이트 + reservation 해제 + 알림
       - CANCELED processResult -> bridge_status 업데이트 + reservation 해제 + 알림
       - EXPIRED processResult -> bridge_status 업데이트 + reservation 해제 + 알림
       - resolveTrackerName() trackerName 우선 테스트
    3. GREEN: packages/daemon/src/infrastructure/database/migrate.ts 수정:
       - v57 마이그레이션 함수 추가: CREATE INDEX IF NOT EXISTS idx_transactions_action_kind_bridge_status ON transactions(action_kind, bridge_status) WHERE bridge_status IS NOT NULL
       - check_bridge_status ALTER: 신규 5종 값 추가 (SQLite CHECK 제약은 재생성 불가이므로, 기존 패턴 확인 후 적절히 처리 -- 실제로 Drizzle push에서 CHECK가 적용되므로 migration에서는 인덱스만 추가)
       - LATEST_SCHEMA_VERSION = 57
    4. GREEN: packages/daemon/src/infrastructure/database/schema.ts 수정:
       - check_bridge_status SQL에 'PARTIALLY_FILLED', 'FILLED', 'CANCELED', 'SETTLED', 'EXPIRED' 추가
    5. GREEN: packages/daemon/src/services/async-polling-service.ts 수정:
       - pollAll() WHERE 절에 'PARTIALLY_FILLED' 추가: inArray(transactions.bridgeStatus, ['PENDING', 'BRIDGE_MONITORING', 'PARTIALLY_FILLED'])
       - resolveTrackerName()에서 metadata.trackerName을 metadata.tracker보다 우선 참조
       - processResult() switch에 신규 state 처리 추가:
         * 'PARTIALLY_FILLED': bridge_status 업데이트 + bridge_metadata 업데이트 + 알림 (EXTERNAL_ACTION_PARTIALLY_FILLED) + break (폴링 계속)
         * 'FILLED': bridge_status='FILLED' + reservation 해제 + 알림 (EXTERNAL_ACTION_FILLED)
         * 'SETTLED': bridge_status='SETTLED' + reservation 해제 + 알림 (EXTERNAL_ACTION_SETTLED)
         * 'CANCELED': bridge_status='CANCELED' + reservation 해제 + 알림 (EXTERNAL_ACTION_CANCELED)
         * 'EXPIRED': bridge_status='EXPIRED' + reservation 해제 + 알림 (EXTERNAL_ACTION_EXPIRED)
    6. GREEN: packages/core/src/enums/notification.ts에 6종 이벤트 추가:
       'EXTERNAL_ACTION_PARTIALLY_FILLED', 'EXTERNAL_ACTION_FILLED', 'EXTERNAL_ACTION_SETTLED', 'EXTERNAL_ACTION_CANCELED', 'EXTERNAL_ACTION_EXPIRED', 'EXTERNAL_ACTION_FAILED'
    7. GREEN: packages/core/src/i18n/en.ts, ko.ts에 6종 이벤트 번역 추가
    8. 기존 테스트 assertion 업데이트 (notification event count, LATEST_SCHEMA_VERSION 56->57, bridge_status CHECK constraint)

    주의: 기존 COMPLETED/FAILED/TIMEOUT/PENDING 처리 코드는 절대 수정하지 않는다. switch case 추가만.
    주의: ExternalActionTracker 자체는 이 태스크에서 구현하지 않음 (AsyncPollingService가 state를 처리하는 인프라만 준비). ExternalActionTracker는 Phase 390에서 파이프라인과 함께 구현.
    주의: bridge_status CHECK constraint는 SQLite에서 ALTER로 변경 불가. Drizzle pushSchema가 새 DB 생성 시 적용. 기존 DB는 CHECK 미적용이나 실질적 영향 없음 (코드 레벨에서 Zod 검증).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/migration-v57.test.ts packages/daemon/src/__tests__/async-polling-service.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>v57 마이그레이션 성공 (복합 인덱스), AsyncPollingService가 PARTIALLY_FILLED 폴링 포함 + 5종 신규 state 처리, 6종 알림 이벤트 등록, LATEST_SCHEMA_VERSION=57, 기존 tracker 동작 무변경</done>
</task>

</tasks>

<verification>
1. pnpm vitest run packages/actions/src/__tests__/async-tracking-state.test.ts -- 9-state 파싱 + 유틸리티
2. pnpm vitest run packages/daemon/src/__tests__/migration-v57.test.ts -- DB v57 마이그레이션
3. pnpm vitest run packages/daemon/src/__tests__/async-polling-service.test.ts -- 폴링 확장 + state 처리
4. pnpm turbo run typecheck -- 전체 타입 체크 (기존 tracker 무변경 확인)
5. pnpm vitest run packages/core/src/__tests__/package-exports.test.ts -- core exports 확인
</verification>

<success_criteria>
- AsyncTrackingResult.state 9종 값 허용, 기존 4종 하위 호환
- BRIDGE_STATUS_VALUES 11종, BridgeStatusEnum 11-value
- isTerminalState/isContinuePolling 유틸리티 동작
- IActionProvider 선택적 checkStatus/execute 메서드 추가, 기존 13개 provider 무변경
- DB v57 복합 인덱스 생성, LATEST_SCHEMA_VERSION=57
- AsyncPollingService가 PARTIALLY_FILLED/FILLED/SETTLED/CANCELED/EXPIRED 처리
- 6종 알림 이벤트 등록 + i18n 번역
- 전체 typecheck 통과
</success_criteria>

<output>
After completion, create `.planning/phases/389-tracking-policy-extension/389-01-SUMMARY.md`
</output>
