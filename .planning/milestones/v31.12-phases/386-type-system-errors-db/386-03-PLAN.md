---
phase: 386-type-system-errors-db
plan: 03
type: execute
wave: 2
depends_on: [386-01]
files_modified:
  - packages/core/src/interfaces/action-provider.types.ts
  - packages/core/src/__tests__/action-provider-compat.test.ts
autonomous: true
requirements: [RTYPE-05, RTYPE-07]

must_haves:
  truths:
    - "IActionProvider.resolve() 반환 타입이 ContractCallRequest | ContractCallRequest[] | ApiDirectResult | SignedDataAction | SignedHttpAction | ResolvedAction[]를 허용한다"
    - "기존 13개 ActionProvider의 ContractCallRequest 반환이 타입 에러 없이 유효하다"
    - "ActionDefinition.riskLevel에 'critical' 값이 추가되어 4등급 체계가 된다"
  artifacts:
    - path: "packages/core/src/interfaces/action-provider.types.ts"
      provides: "확장된 IActionProvider.resolve() 반환 타입 + riskLevel 4등급"
      contains: "SignedDataAction"
    - path: "packages/core/src/__tests__/action-provider-compat.test.ts"
      provides: "기존 provider 하위 호환성 + 새 반환 타입 검증 테스트"
      min_lines: 50
  key_links:
    - from: "packages/core/src/interfaces/action-provider.types.ts"
      to: "packages/core/src/schemas/resolved-action.schema.ts"
      via: "import SignedDataAction, SignedHttpAction, ResolvedAction types"
      pattern: "import.*resolved-action"
---

<objective>
IActionProvider.resolve() 반환 타입을 확장하고 기존 13개 ActionProvider의 하위 호환성을 검증한다.

Purpose: 새 ActionProvider가 signedData/signedHttp를 반환할 수 있도록 타입을 확장하면서, 기존 provider의 무변경 동작을 보장한다.
Output: 확장된 IActionProvider 인터페이스, 하위 호환성 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/design/81-external-action-framework.md (sections D1.8~D1.9)
@.planning/phases/386-type-system-errors-db/386-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From packages/core/src/schemas/resolved-action.schema.ts (created in Plan 01):
```typescript
export const SignedDataActionSchema = z.object({ kind: z.literal('signedData'), ... });
export const SignedHttpActionSchema = z.object({ kind: z.literal('signedHttp'), ... });
export const ResolvedActionSchema = z.discriminatedUnion('kind', [...]);
export type SignedDataAction = z.infer<typeof SignedDataActionSchema>;
export type SignedHttpAction = z.infer<typeof SignedHttpActionSchema>;
export type ResolvedAction = z.infer<typeof ResolvedActionSchema>;
```

From packages/core/src/interfaces/action-provider.types.ts (current):
```typescript
export interface IActionProvider {
  readonly metadata: ActionProviderMetadata;
  readonly actions: readonly ActionDefinition[];
  resolve(
    actionName: string,
    params: Record<string, unknown>,
    context: ActionContext,
  ): Promise<ContractCallRequest | ContractCallRequest[] | ApiDirectResult>;
}

export const ActionDefinitionSchema = z.object({
  name: z.string()...,
  description: z.string()...,
  chain: ChainTypeEnum,
  inputSchema: z.any(),
  riskLevel: z.enum(['low', 'medium', 'high']),
  defaultTier: z.enum(['INSTANT', 'NOTIFY', 'DELAY', 'APPROVAL']),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IActionProvider.resolve() 반환 타입 확장 + riskLevel 4등급</name>
  <files>packages/core/src/interfaces/action-provider.types.ts, packages/core/src/__tests__/action-provider-compat.test.ts</files>
  <behavior>
    - Test: 기존 ContractCallRequest를 반환하는 mock provider가 IActionProvider 타입을 만족한다
    - Test: ContractCallRequest[]를 반환하는 mock provider가 IActionProvider 타입을 만족한다
    - Test: ApiDirectResult를 반환하는 mock provider가 IActionProvider 타입을 만족한다
    - Test: SignedDataAction을 반환하는 mock provider가 IActionProvider 타입을 만족한다
    - Test: SignedHttpAction을 반환하는 mock provider가 IActionProvider 타입을 만족한다
    - Test: ResolvedAction[]을 반환하는 mock provider가 IActionProvider 타입을 만족한다
    - Test: ActionDefinitionSchema.parse({ riskLevel: 'critical', ... }) 성공
    - Test: ActionDefinitionSchema.parse({ riskLevel: 'low', ... }) 성공 (기존 값 유효)
  </behavior>
  <action>
1. `packages/core/src/interfaces/action-provider.types.ts` 수정:

   a) import 추가: `import type { SignedDataAction, SignedHttpAction, ResolvedAction } from '../schemas/resolved-action.schema.js';`

   b) IActionProvider.resolve() 반환 타입 확장 (doc-81 D1.8):
   ```typescript
   resolve(
     actionName: string,
     params: Record<string, unknown>,
     context: ActionContext,
   ): Promise<
     | ContractCallRequest
     | ContractCallRequest[]
     | ApiDirectResult
     | SignedDataAction
     | SignedHttpAction
     | ResolvedAction[]
   >;
   ```

   c) ActionDefinitionSchema.riskLevel에 'critical' 추가 (doc-81 POLICY-06):
   ```typescript
   riskLevel: z.enum(['low', 'medium', 'high', 'critical']),
   ```

   d) defaultTier는 변경 없음 (기존 4값 유지)

2. 테스트 파일 `packages/core/src/__tests__/action-provider-compat.test.ts` 작성:
   - 각 반환 타입별 mock provider 생성하여 IActionProvider 타입 만족 확인 (TypeScript satisfies 패턴)
   - ActionDefinitionSchema 파싱 테스트: riskLevel 4등급 모두 유효
   - 기존 3종 반환 타입(ContractCallRequest, ContractCallRequest[], ApiDirectResult)이 여전히 유효한지 타입-level 검증
   - isApiDirectResult() 타입 가드가 SignedDataAction/SignedHttpAction을 false로 반환하는지 검증

3. 기존 ActionProvider 소스 코드는 수정하지 않음 (무변경 원칙). 반환 타입이 Union의 부분집합이므로 기존 코드가 자동으로 호환됨.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core test -- --run action-provider-compat && pnpm --filter @waiaas/core run typecheck</automated>
  </verify>
  <done>IActionProvider.resolve()가 6종 반환 타입을 허용하고, 기존 13개 provider가 타입 에러 없이 컴파일된다</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/core test -- --run` 전체 통과 (기존 + 신규)
2. `pnpm --filter @waiaas/core run typecheck` 통과
3. `pnpm turbo run typecheck` (전체 모노레포) 통과 -- 기존 ActionProvider 구현체 타입 에러 없음
</verification>

<success_criteria>
- IActionProvider.resolve()가 SignedDataAction/SignedHttpAction/ResolvedAction[]을 반환 가능하다
- 기존 13개 ActionProvider 소스 코드 무변경으로 타입체크 통과
- riskLevel에 'critical' 등급이 추가되었다
- isApiDirectResult()가 새 타입에 대해 올바르게 동작한다
</success_criteria>

<output>
After completion, create `.planning/phases/386-type-system-errors-db/386-03-SUMMARY.md`
</output>
