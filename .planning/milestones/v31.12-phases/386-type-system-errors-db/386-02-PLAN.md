---
phase: 386-type-system-errors-db
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/infrastructure/database/schema.ts
  - packages/daemon/src/__tests__/migration-v55-v56.test.ts
autonomous: true
requirements: [DBMIG-01, DBMIG-02, DBMIG-03]

must_haves:
  truths:
    - "v55 마이그레이션 실행 후 wallet_credentials 테이블이 7개 컬럼과 4개 인덱스로 생성된다"
    - "v56 마이그레이션 실행 후 transactions 테이블에 action_kind/venue/operation/external_id 4개 컬럼이 추가된다"
    - "기존 v54 데이터베이스에서 v55->v56 순차 마이그레이션이 데이터 손실 없이 완료된다"
    - "LATEST_SCHEMA_VERSION이 56으로 업데이트되어 신규 DB에서도 DDL이 일치한다"
  artifacts:
    - path: "packages/daemon/src/infrastructure/database/migrate.ts"
      provides: "v55+v56 migration functions, updated DDL, LATEST_SCHEMA_VERSION=56"
      contains: "LATEST_SCHEMA_VERSION = 56"
    - path: "packages/daemon/src/infrastructure/database/schema.ts"
      provides: "Drizzle schema with walletCredentials table and extended transactions columns"
      contains: "walletCredentials"
    - path: "packages/daemon/src/__tests__/migration-v55-v56.test.ts"
      provides: "Migration tests: schema snapshot + data transformation"
      min_lines: 100
  key_links:
    - from: "packages/daemon/src/infrastructure/database/migrate.ts"
      to: "packages/daemon/src/infrastructure/database/schema.ts"
      via: "DDL matches Drizzle schema"
      pattern: "wallet_credentials"
---

<objective>
DB v55(wallet_credentials 테이블) + v56(transactions 컬럼 확장) 마이그레이션과 Drizzle 스키마를 구현한다.

Purpose: CredentialVault(Phase 388)과 off-chain action DB 기록(Phase 390)이 이 스키마에 의존한다.
Output: migrate.ts에 v55+v56 마이그레이션, schema.ts에 Drizzle 스키마, 마이그레이션 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/design/81-external-action-framework.md (sections D3.4~D3.5, D4.5)

<interfaces>
<!-- Key patterns from existing migration code -->

From packages/daemon/src/infrastructure/database/migrate.ts:
```typescript
export const LATEST_SCHEMA_VERSION = 54;

// Migration registration pattern:
interface MigrationEntry {
  version: number;
  description: string;
  up: (sqlite: Database) => void;
  managesOwnTransaction?: boolean;
}

// migrations array example:
{ version: 53, description: '...', up: (sqlite) => { sqlite.exec(`...`); } },

export function runMigrations(sqlite: Database, ...): void;
export function pushSchema(sqlite: Database): void;
```

From packages/daemon/src/__tests__/migration-v53-v54.test.ts:
```typescript
// Test pattern: create DB at previous version, run migrations, verify
function createV52Db(): InstanceType<typeof Database> {
  const sqlite = new Database(':memory:');
  pushSchema(sqlite);
  // Downgrade by dropping newer artifacts
  sqlite.exec('DROP TABLE IF EXISTS ...');
  sqlite.exec('DELETE FROM schema_version WHERE version >= 53');
  return sqlite;
}
function tableExists(sqlite, name): boolean;
function getTableColumns(sqlite, table): string[];
function indexExists(sqlite, name): boolean;
```

From packages/daemon/src/infrastructure/database/schema.ts (Drizzle pattern):
```typescript
import { sqliteTable, text, integer, blob, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
// Tables use: text('col_name').notNull(), integer('col_name'), blob('col_name', { mode: 'buffer' })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB v55+v56 마이그레이션 + DDL 업데이트</name>
  <files>packages/daemon/src/infrastructure/database/migrate.ts</files>
  <behavior>
    - Test: v54 DB에서 runMigrations() 후 wallet_credentials 테이블이 존재한다
    - Test: wallet_credentials 테이블에 id, wallet_id, type, name, encrypted_value, iv, auth_tag, metadata, expires_at, created_at, updated_at 컬럼이 있다
    - Test: idx_wallet_credentials_wallet_name, idx_wallet_credentials_global_name, idx_wallet_credentials_wallet_id, idx_wallet_credentials_expires_at 인덱스가 존재한다
    - Test: v56 후 transactions 테이블에 action_kind, venue, operation, external_id 컬럼이 있다
    - Test: 기존 transaction 레코드의 action_kind 값이 'contractCall'이다
    - Test: idx_transactions_action_kind, idx_transactions_venue, idx_transactions_external_id 인덱스가 존재한다
    - Test: schema_version 테이블의 최대 버전이 56이다
    - Test: 신규 DB(pushSchema)도 wallet_credentials 테이블과 transactions 확장 컬럼을 가진다
  </behavior>
  <action>
1. `packages/daemon/src/infrastructure/database/migrate.ts` 수정:

   a) LATEST_SCHEMA_VERSION을 54 -> 56으로 변경

   b) getCreateTableStatements()에 wallet_credentials DDL 추가 (doc-81 D3.4 그대로):
   ```sql
   CREATE TABLE IF NOT EXISTS wallet_credentials (
     id TEXT NOT NULL PRIMARY KEY,
     wallet_id TEXT,
     type TEXT NOT NULL CHECK (type IN ('api-key','hmac-secret','rsa-private-key','session-token','custom')),
     name TEXT NOT NULL,
     encrypted_value BLOB NOT NULL,
     iv BLOB NOT NULL,
     auth_tag BLOB NOT NULL,
     metadata TEXT NOT NULL DEFAULT '{}',
     expires_at INTEGER,
     created_at INTEGER NOT NULL DEFAULT (unixepoch()),
     updated_at INTEGER NOT NULL DEFAULT (unixepoch()),
     FOREIGN KEY (wallet_id) REFERENCES wallets(id) ON DELETE CASCADE
   )
   ```

   c) getCreateIndexStatements()에 wallet_credentials 인덱스 4개 추가

   d) transactions DDL에 4개 신규 컬럼 추가 (action_kind TEXT NOT NULL DEFAULT 'contractCall', venue TEXT, operation TEXT, external_id TEXT)

   e) transactions 인덱스에 idx_transactions_action_kind, idx_transactions_venue, idx_transactions_external_id 추가

   f) migrations 배열에 v55 마이그레이션 추가:
   - wallet_credentials 테이블 CREATE TABLE
   - 4개 인덱스 CREATE INDEX

   g) migrations 배열에 v56 마이그레이션 추가:
   - ALTER TABLE transactions ADD COLUMN action_kind TEXT NOT NULL DEFAULT 'contractCall'
   - ALTER TABLE transactions ADD COLUMN venue TEXT
   - ALTER TABLE transactions ADD COLUMN operation TEXT
   - ALTER TABLE transactions ADD COLUMN external_id TEXT
   - 3개 인덱스 CREATE INDEX
   - 컬럼 존재 여부를 먼저 확인하는 패턴 사용 (pushSchema로 생성된 DB에서 중복 방지)

2. 테스트 파일 `packages/daemon/src/__tests__/migration-v55-v56.test.ts` 작성 (기존 migration-v53-v54.test.ts 패턴 따름):
   - createV54Db() 헬퍼: pushSchema 후 v55+ 아티팩트를 DROP하여 v54 상태로 되돌림
   - v55 테스트: wallet_credentials 테이블 생성, 컬럼 검증, 인덱스 검증
   - v56 테스트: transactions 컬럼 추가 검증, 기존 행 action_kind='contractCall' 검증, 인덱스 검증
   - 전체 마이그레이션 체인 테스트: v54 -> v56 성공 검증
   - 신규 DB 스냅샷 일치 테스트: pushSchema()로 생성한 DB와 마이그레이션 DB의 테이블/컬럼이 동일
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon test -- --run migration-v55-v56</automated>
  </verify>
  <done>v55 wallet_credentials 테이블과 v56 transactions 확장이 마이그레이션으로 정상 생성되고, 스냅샷 테스트가 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: Drizzle 스키마 확장 (walletCredentials + transactions 컬럼)</name>
  <files>packages/daemon/src/infrastructure/database/schema.ts</files>
  <action>
`packages/daemon/src/infrastructure/database/schema.ts`에 다음을 추가:

1. walletCredentials 테이블 Drizzle 정의 (doc-81 D3.5 그대로):
   ```typescript
   export const walletCredentials = sqliteTable('wallet_credentials', {
     id: text('id').primaryKey(),
     walletId: text('wallet_id').references(() => wallets.id, { onDelete: 'cascade' }),
     type: text('type').notNull(),
     name: text('name').notNull(),
     encryptedValue: blob('encrypted_value', { mode: 'buffer' }).notNull(),
     iv: blob('iv', { mode: 'buffer' }).notNull(),
     authTag: blob('auth_tag', { mode: 'buffer' }).notNull(),
     metadata: text('metadata').notNull().default('{}'),
     expiresAt: integer('expires_at'),
     createdAt: integer('created_at').notNull().default(sql`(unixepoch())`),
     updatedAt: integer('updated_at').notNull().default(sql`(unixepoch())`),
   });
   ```

2. transactions 테이블에 4개 컬럼 추가:
   ```typescript
   actionKind: text('action_kind').notNull().default('contractCall'),
   venue: text('venue'),
   operation: text('operation'),
   externalId: text('external_id'),
   ```

Drizzle 인덱스 정의는 테이블 3번째 인자에 추가. uniqueIndex/index 패턴은 기존 코드 참조.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon run typecheck</automated>
  </verify>
  <done>Drizzle 스키마에 walletCredentials 테이블과 transactions 확장 컬럼이 정의되어 ORM 쿼리에 사용 가능하다</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/daemon test -- --run migration-v55-v56` 통과
2. `pnpm --filter @waiaas/daemon run typecheck` 통과
3. 기존 migration 테스트 (`migration-v53-v54`, `schema-compatibility`) 깨지지 않음
4. LATEST_SCHEMA_VERSION=56이 설정됨
</verification>

<success_criteria>
- v55 마이그레이션으로 wallet_credentials 테이블이 생성된다
- v56 마이그레이션으로 transactions에 action_kind/venue/operation/external_id가 추가된다
- 기존 행의 action_kind가 'contractCall' 기본값을 가진다
- Drizzle 스키마가 DDL과 일치한다
- 마이그레이션 테스트가 스냅샷 검증과 데이터 변환 검증을 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/386-type-system-errors-db/386-02-SUMMARY.md`
</output>
