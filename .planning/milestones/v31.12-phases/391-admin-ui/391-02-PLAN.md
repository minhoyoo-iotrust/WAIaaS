---
phase: 391-admin-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/components/policy-forms/venue-whitelist-form.tsx
  - packages/admin/src/components/policy-forms/action-category-limit-form.tsx
  - packages/admin/src/components/policy-forms/index.tsx
  - packages/admin/src/pages/policies.tsx
  - packages/admin/src/__tests__/venue-whitelist-form.test.tsx
  - packages/admin/src/__tests__/action-category-limit-form.test.tsx
autonomous: true
requirements: [ADMIN-03, ADMIN-04]

must_haves:
  truths:
    - "Venue Whitelist 정책 생성/수정 시 전용 폼으로 venue를 추가/제거할 수 있다"
    - "ACTION_CATEGORY_LIMIT 정책 생성/수정 시 카테고리별 USD 한도를 per_action/daily/monthly 단위로 설정할 수 있다"
    - "venue_whitelist_enabled 토글이 Admin Settings에서 설정 가능하다"
    - "두 정책 타입이 기존 정책 목록/생성 UI에 통합되어 선택 가능하다"
  artifacts:
    - path: "packages/admin/src/components/policy-forms/venue-whitelist-form.tsx"
      provides: "Venue Whitelist policy form component"
      min_lines: 40
    - path: "packages/admin/src/components/policy-forms/action-category-limit-form.tsx"
      provides: "Action Category Limit policy form component"
      min_lines: 60
    - path: "packages/admin/src/__tests__/venue-whitelist-form.test.tsx"
      provides: "Venue Whitelist form tests"
      min_lines: 40
    - path: "packages/admin/src/__tests__/action-category-limit-form.test.tsx"
      provides: "Action Category Limit form tests"
      min_lines: 50
  key_links:
    - from: "packages/admin/src/components/policy-forms/index.tsx"
      to: "VenueWhitelistForm"
      via: "PolicyFormRouter switch case"
      pattern: "VENUE_WHITELIST.*VenueWhitelistForm"
    - from: "packages/admin/src/components/policy-forms/index.tsx"
      to: "ActionCategoryLimitForm"
      via: "PolicyFormRouter switch case"
      pattern: "ACTION_CATEGORY_LIMIT.*ActionCategoryLimitForm"
---

<objective>
VENUE_WHITELIST + ACTION_CATEGORY_LIMIT 정책 폼 UI 구현

Purpose: 운영자가 Admin UI에서 Venue Whitelist와 Action Category Limit 정책을 전용 폼으로 생성/수정할 수 있다
Output: 2개 정책 폼 컴포넌트 + PolicyFormRouter 연결 + policies.tsx 타입 목록 확장 + 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts from existing policy forms -->

From packages/admin/src/components/policy-forms/index.tsx:
```typescript
export interface PolicyFormProps {
  rules: Record<string, unknown>;
  onChange: (rules: Record<string, unknown>) => void;
  errors: Record<string, string>;
  network?: string;
}

export function PolicyFormRouter({ type, rules, onChange, errors, network }: { type: string } & PolicyFormProps) {
  switch (type) {
    case 'CONTRACT_WHITELIST': return <ContractWhitelistForm .../>;
    // ... add VENUE_WHITELIST and ACTION_CATEGORY_LIMIT cases
  }
}
```

VENUE_WHITELIST rules schema (from database-policy-engine.ts):
```typescript
interface VenueWhitelistRules {
  venues: Array<{ id: string; name?: string }>;
}
```

ACTION_CATEGORY_LIMIT rules schema (from database-policy-engine.ts):
```typescript
interface ActionCategoryLimitRules {
  category: string;  // e.g., 'trade', 'withdraw', 'stake'
  per_action_limit_usd?: number;
  daily_limit_usd?: number;
  monthly_limit_usd?: number;
  tier_on_exceed?: string;  // default 'DELAY'
}
```

From packages/admin/src/pages/policies.tsx:
```typescript
const POLICY_TYPES = [
  { label: 'Spending Limit', value: 'SPENDING_LIMIT' },
  // ... add { label: 'Venue Whitelist', value: 'VENUE_WHITELIST' },
  // ... add { label: 'Action Category Limit', value: 'ACTION_CATEGORY_LIMIT' },
];
const POLICY_DESCRIPTIONS: Record<string, string> = { ... };
const DEFAULT_RULES: Record<string, Record<string, unknown>> = { ... };
```

Existing pattern (contract-whitelist-form.tsx uses DynamicRowList):
```typescript
import { DynamicRowList } from '../dynamic-row-list';
import { FormField } from '../form';
import type { PolicyFormProps } from './index';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VenueWhitelistForm + ActionCategoryLimitForm 컴포넌트 + PolicyFormRouter 연결</name>
  <files>
    packages/admin/src/components/policy-forms/venue-whitelist-form.tsx,
    packages/admin/src/components/policy-forms/action-category-limit-form.tsx,
    packages/admin/src/components/policy-forms/index.tsx,
    packages/admin/src/pages/policies.tsx
  </files>
  <action>
1. venue-whitelist-form.tsx 생성 (CONTRACT_WHITELIST 패턴 참조):
   - VenueWhitelistForm({ rules, onChange, errors }: PolicyFormProps) export
   - DynamicRowList로 venues 배열 관리: 각 행은 { id: string, name: string }
   - FormField: venue id (필수, placeholder "e.g. polymarket, hyperliquid"), venue name (선택, placeholder "Display name")
   - rules.venues as Array<{ id: string; name?: string }> 파싱
   - onChange({ ...rules, venues: [...] }) 패턴

2. action-category-limit-form.tsx 생성:
   - ActionCategoryLimitForm({ rules, onChange, errors }: PolicyFormProps) export
   - FormField: category (select: trade/withdraw/stake/lend/bridge/swap/custom 또는 text input), per_action_limit_usd (number, optional), daily_limit_usd (number, optional), monthly_limit_usd (number, optional), tier_on_exceed (select: INSTANT/NOTIFY/DELAY/APPROVAL, default DELAY)
   - 각 limit 필드는 optional -- 비어있으면 해당 한도 미적용
   - 숫자 입력은 USD 단위, placeholder에 "e.g. 1000" 표시
   - onChange({ ...rules, category, per_action_limit_usd, daily_limit_usd, monthly_limit_usd, tier_on_exceed }) 패턴

3. index.tsx PolicyFormRouter에 2개 case 추가:
   - import { VenueWhitelistForm } from './venue-whitelist-form';
   - import { ActionCategoryLimitForm } from './action-category-limit-form';
   - case 'VENUE_WHITELIST': return <VenueWhitelistForm rules={rules} onChange={onChange} errors={errors} />;
   - case 'ACTION_CATEGORY_LIMIT': return <ActionCategoryLimitForm rules={rules} onChange={onChange} errors={errors} />;
   - 주석 업데이트: "Supports all 16 core types" (14 -> 16)

4. policies.tsx POLICY_TYPES 배열에 추가:
   - { label: 'Venue Whitelist', value: 'VENUE_WHITELIST' }
   - { label: 'Action Category Limit', value: 'ACTION_CATEGORY_LIMIT' }

5. policies.tsx POLICY_DESCRIPTIONS에 추가:
   - VENUE_WHITELIST: 'Allow external actions only from pre-approved venues (default-deny when enabled).'
   - ACTION_CATEGORY_LIMIT: 'Set per-action, daily, and monthly USD limits per action category with tier escalation.'

6. policies.tsx DEFAULT_RULES에 추가:
   - VENUE_WHITELIST: { venues: [{ id: '', name: '' }] }
   - ACTION_CATEGORY_LIMIT: { category: 'trade', per_action_limit_usd: 1000, daily_limit_usd: 10000, monthly_limit_usd: 100000, tier_on_exceed: 'DELAY' }
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin run build 2>&1 | tail -10</automated>
  </verify>
  <done>VENUE_WHITELIST와 ACTION_CATEGORY_LIMIT 정책 타입이 정책 생성 드롭다운에 표시되고, 각각 전용 폼으로 규칙을 입력할 수 있다. 빌드 성공.</done>
</task>

<task type="auto">
  <name>Task 2: 정책 폼 테스트 + venue_whitelist_enabled 설정 확인</name>
  <files>
    packages/admin/src/__tests__/venue-whitelist-form.test.tsx,
    packages/admin/src/__tests__/action-category-limit-form.test.tsx
  </files>
  <action>
1. venue-whitelist-form.test.tsx 생성 (기존 erc8128-domains-form.test.tsx 패턴 참조):
   - VenueWhitelistForm 임포트, PolicyFormProps mock
   - 초기 렌더링 테스트: venues 배열이 폼 필드로 표시
   - venue 추가 테스트: Add 버튼 클릭 → 새 행 생성
   - venue 삭제 테스트: Remove 버튼 클릭 → 행 제거
   - onChange 호출 검증: venue id 입력 시 올바른 rules 객체 전달
   - 빈 venues 배열 초기 상태 테스트
   - 최소 5개 테스트 케이스

2. action-category-limit-form.test.tsx 생성:
   - ActionCategoryLimitForm 임포트, PolicyFormProps mock
   - 초기 렌더링 테스트: category, limit 필드들 표시
   - category 변경 테스트: select 변경 → onChange 호출
   - limit 입력 테스트: per_action_limit_usd 입력 → onChange 호출
   - tier_on_exceed 변경 테스트: select 변경 → onChange 호출
   - optional 필드 비어있을 때 처리 테스트
   - 최소 5개 테스트 케이스

3. 기존 settings-completeness.test.ts 확인: venue_whitelist_enabled가 이미 Admin Settings에 등록되어 있는지 확인. 이미 setting-keys.ts에 등록되어 있으므로 (Phase 389에서 추가됨) UI에서 System > Admin Settings 페이지에서 자동으로 표시된다. 별도 UI 작업 불필요.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin test -- --run venue-whitelist-form.test action-category-limit-form.test 2>&1 | tail -20</automated>
  </verify>
  <done>VenueWhitelistForm 테스트 5개 이상, ActionCategoryLimitForm 테스트 5개 이상 통과. venue_whitelist_enabled 설정이 Admin Settings에서 접근 가능.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/admin test -- --run` 전체 admin 테스트 통과
2. `pnpm --filter @waiaas/admin run build` 빌드 성공
3. VENUE_WHITELIST가 정책 타입 목록에 표시
4. ACTION_CATEGORY_LIMIT가 정책 타입 목록에 표시
5. 각 정책 타입에 전용 폼이 렌더링
</verification>

<success_criteria>
- Venue Whitelist 정책 생성 시 venue 추가/제거 폼이 표시된다
- Action Category Limit 정책 생성 시 category, limit, tier 설정 폼이 표시된다
- 두 정책 타입이 기존 정책 관리 UI에 완전히 통합된다
- venue_whitelist_enabled 설정이 System > Admin Settings에서 토글 가능하다
- 전체 admin 테스트 통과, 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/391-admin-ui/391-02-SUMMARY.md`
</output>
