---
phase: 391-admin-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/credentials.tsx
  - packages/admin/src/pages/wallets.tsx
  - packages/admin/src/api/endpoints.ts
  - packages/admin/src/components/layout.tsx
  - packages/admin/src/__tests__/credentials.test.tsx
  - packages/admin/src/__tests__/wallets-external-actions.test.tsx
autonomous: true
requirements: [ADMIN-01, ADMIN-02]

must_haves:
  truths:
    - "Credentials 페이지에서 글로벌 credential 목록을 조회하고 등록/삭제/로테이션할 수 있다"
    - "Wallet Detail에서 per-wallet credential 탭으로 해당 지갑의 credential을 관리할 수 있다"
    - "credential 원문(value)은 UI 어디에서도 표시되지 않는다"
    - "Wallet Detail의 External Actions 탭에서 off-chain action 이력(venue, operation, status, createdAt)을 확인할 수 있다"
  artifacts:
    - path: "packages/admin/src/pages/credentials.tsx"
      provides: "Global credentials management page"
      min_lines: 150
    - path: "packages/admin/src/__tests__/credentials.test.tsx"
      provides: "Credentials page unit tests"
      min_lines: 80
    - path: "packages/admin/src/__tests__/wallets-external-actions.test.tsx"
      provides: "External Actions tab unit tests"
      min_lines: 60
  key_links:
    - from: "packages/admin/src/pages/credentials.tsx"
      to: "/admin/credentials"
      via: "apiGet/apiPost/apiDelete/apiPut"
      pattern: "API\\.ADMIN_CREDENTIALS"
    - from: "packages/admin/src/pages/wallets.tsx"
      to: "/v1/wallets/:id/actions"
      via: "apiGet fetch"
      pattern: "WALLET_ACTIONS"
---

<objective>
Credentials 관리 페이지 + Wallet Detail External Actions 탭 구현

Purpose: 운영자가 Admin UI에서 글로벌/per-wallet credential을 CRUD 관리하고, 지갑별 off-chain action 이력을 모니터링할 수 있다
Output: credentials.tsx 신규 페이지, wallets.tsx에 Credentials/External Actions 탭 추가, 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/390-pipeline-routing-query-api/390-02-SUMMARY.md

<interfaces>
<!-- Key types and API contracts the executor needs -->

From packages/core/src/schemas/credential.schema.ts:
```typescript
export const CredentialTypeEnum = z.enum(['api-key', 'hmac-secret', 'rsa-private-key', 'session-token', 'custom']);
export const CreateCredentialParamsSchema = z.object({
  type: CredentialTypeEnum,
  name: z.string().min(1).max(128),
  value: z.string().min(1),
  metadata: z.record(z.unknown()).optional(),
  expiresAt: z.number().int().positive().optional(),
});
export const CredentialMetadataSchema = z.object({
  id: z.string(),
  walletId: z.string().nullable(),
  type: CredentialTypeEnum,
  name: z.string(),
  metadata: z.record(z.unknown()),
  expiresAt: z.number().nullable(),
  createdAt: z.number(),
  updatedAt: z.number(),
});
```

API endpoints (daemon routes already exist):
- GET    /admin/credentials                     (masterAuth) - list global credentials
- POST   /admin/credentials                     (masterAuth) - create global credential
- DELETE /admin/credentials/:ref                 (masterAuth) - delete global credential
- PUT    /admin/credentials/:ref/rotate          (masterAuth) - rotate credential value
- GET    /wallets/:walletId/credentials          (masterAuth) - list per-wallet credentials
- POST   /wallets/:walletId/credentials          (masterAuth) - create per-wallet credential
- DELETE /wallets/:walletId/credentials/:ref      (masterAuth) - delete credential
- PUT    /wallets/:walletId/credentials/:ref/rotate (masterAuth) - rotate credential value
- GET    /wallets/:id/actions                    (sessionAuth) - list off-chain actions (venue, status filter + pagination)
- GET    /wallets/:id/actions/:actionId          (sessionAuth) - detail

From packages/daemon/src/api/routes/external-actions.ts:
```typescript
const ExternalActionItemSchema = z.object({
  id: z.string(),
  actionKind: z.string(),
  venue: z.string().nullable(),
  operation: z.string().nullable(),
  status: z.string(),
  createdAt: z.number(),
  actionProvider: z.string().nullable(),
  actionName: z.string().nullable(),
});
```

From packages/admin/src/api/endpoints.ts (existing pattern):
```typescript
export const API = {
  ADMIN_SETTINGS: '/v1/admin/settings',
  WALLETS: '/v1/wallets',
  WALLET: (id: string) => `/v1/wallets/${id}`,
  // ... etc
} as const;
```

From packages/admin/src/components/layout.tsx (nav + routing pattern):
```typescript
const NAV_ITEMS = [
  { path: '/dashboard', label: 'Dashboard' },
  // ... add new { path: '/credentials', label: 'Credentials' }
];
// PageRouter switch: if (path === '/credentials') return <CredentialsPage />;
```

Wallet Detail tabs pattern (wallets.tsx):
```typescript
const DETAIL_TABS = [
  { key: 'overview', label: 'Overview' },
  { key: 'transactions', label: 'Transactions' },
  // ... add { key: 'credentials', label: 'Credentials' }
  // ... add { key: 'external-actions', label: 'External Actions' }
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credentials 페이지 + endpoints + layout 연결</name>
  <files>
    packages/admin/src/pages/credentials.tsx,
    packages/admin/src/api/endpoints.ts,
    packages/admin/src/components/layout.tsx,
    packages/admin/src/__tests__/credentials.test.tsx
  </files>
  <action>
1. endpoints.ts에 API 상수 추가:
   - ADMIN_CREDENTIALS: '/v1/admin/credentials'
   - ADMIN_CREDENTIAL_DELETE: (ref: string) => `/v1/admin/credentials/${ref}`
   - ADMIN_CREDENTIAL_ROTATE: (ref: string) => `/v1/admin/credentials/${ref}/rotate`
   - WALLET_CREDENTIALS: (id: string) => `/v1/wallets/${id}/credentials`
   - WALLET_CREDENTIAL_DELETE: (id: string, ref: string) => `/v1/wallets/${id}/credentials/${ref}`
   - WALLET_CREDENTIAL_ROTATE: (id: string, ref: string) => `/v1/wallets/${id}/credentials/${ref}/rotate`

2. credentials.tsx 신규 페이지 생성 (기존 tokens.tsx 패턴 참조):
   - CredentialsPage 컴포넌트: useSignal로 상태 관리
   - 글로벌 credential 목록 테이블: name, type(Badge), expiresAt(formatDate), createdAt(formatDate), actions(Delete/Rotate 버튼)
   - Add Credential 모달: FormField로 type(select: api-key/hmac-secret/rsa-private-key/session-token/custom), name, value(password input), metadata(optional JSON), expiresAt(optional datetime)
   - Delete 확인 모달
   - Rotate 모달: new value 입력 필드
   - credential value(원문)는 어디에서도 표시하지 않음 -- API가 CredentialMetadata만 반환
   - apiGet(API.ADMIN_CREDENTIALS)로 목록 조회, apiPost로 생성, apiDelete로 삭제, apiPut으로 rotate
   - 에러 처리: showToast + getErrorMessage 패턴
   - EmptyState 컴포넌트 사용 (credential 없을 때)

3. layout.tsx 수정:
   - import CredentialsPage
   - NAV_ITEMS에 { path: '/credentials', label: 'Credentials' } 추가 (Policies와 Security 사이)
   - PAGE_TITLES에 '/credentials': 'Credentials' 추가
   - PAGE_SUBTITLES에 '/credentials': 'Manage encryption credentials for external service authentication' 추가
   - PageRouter에 if (path === '/credentials') return <CredentialsPage />; 추가

4. credentials.test.tsx 테스트:
   - 기존 테스트 패턴 (polymarket.test.tsx 등 참조: vi.mock('../api/client'), renderPage, waitFor)
   - 목록 렌더링 테스트 (mock apiGet → credential 목록 표시)
   - 빈 목록 EmptyState 테스트
   - Add 모달 열기/닫기 테스트
   - Delete 확인 모달 테스트
   - credential value가 응답에 포함되지 않는 것 확인 (CredentialMetadata에 value 필드 없음)
   - 최소 6개 테스트 케이스
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin test -- --run credentials.test 2>&1 | tail -20</automated>
  </verify>
  <done>Credentials 페이지가 /credentials 경로에 렌더링되고, 글로벌 credential CRUD가 동작하며, credential value가 UI에 노출되지 않는다. 테스트 6개 이상 통과.</done>
</task>

<task type="auto">
  <name>Task 2: Wallet Detail에 Credentials 탭 + External Actions 탭 추가</name>
  <files>
    packages/admin/src/pages/wallets.tsx,
    packages/admin/src/api/endpoints.ts,
    packages/admin/src/__tests__/wallets-external-actions.test.tsx
  </files>
  <action>
1. endpoints.ts에 추가 (Task 1에서 WALLET_CREDENTIALS는 이미 추가됨):
   - WALLET_ACTIONS: (id: string) => `/v1/wallets/${id}/actions`
   - WALLET_ACTION_DETAIL: (id: string, actionId: string) => `/v1/wallets/${id}/actions/${actionId}`

2. wallets.tsx의 DETAIL_TABS에 2개 탭 추가:
   - { key: 'credentials', label: 'Credentials' } (nfts 뒤, mcp 앞)
   - { key: 'external-actions', label: 'External Actions' } (credentials 뒤, mcp 앞)

3. CredentialsTab 컴포넌트 구현 (wallets.tsx 내부):
   - Per-wallet credential 목록 테이블 (name, type Badge, expiresAt, createdAt, actions)
   - Add/Delete/Rotate 모달 (credentials.tsx와 동일 패턴이지만 walletId 포함)
   - apiGet(API.WALLET_CREDENTIALS(walletId))로 조회
   - apiPost(API.WALLET_CREDENTIALS(walletId), body)로 생성
   - apiDelete(API.WALLET_CREDENTIAL_DELETE(walletId, ref))로 삭제
   - apiPut(API.WALLET_CREDENTIAL_ROTATE(walletId, ref), { value })로 rotate
   - EmptyState: "No credentials. Add credentials for external service authentication."

4. ExternalActionsTab 컴포넌트 구현 (wallets.tsx 내부):
   - off-chain action 목록 테이블: venue(Badge), operation, status(Badge with color - submitted=blue, signed=green, failed=red, pending=yellow), actionProvider, actionName, createdAt(formatDate)
   - apiGet(API.WALLET_ACTIONS(walletId), { params: { limit: 50 } })로 조회
   - status 필터: 'all', 'submitted', 'signed', 'pending', 'failed', 'completed' select
   - venue 필터: text input
   - 상세 보기: 행 클릭 시 모달로 ExternalActionDetail 표시 (metadata JSON 포함)
   - EmptyState: "No external actions recorded for this wallet."
   - 새로고침 버튼

5. TabNav 렌더링 부분에 추가:
   - {activeDetailTab.value === 'credentials' && <CredentialsTab />}
   - {activeDetailTab.value === 'external-actions' && <ExternalActionsTab />}

6. wallets-external-actions.test.tsx 테스트:
   - ExternalActionsTab 렌더링 테스트 (mock apiGet → action 목록 표시)
   - 빈 목록 EmptyState 테스트
   - status 뱃지 색상 테스트
   - 상세 모달 테스트
   - 최소 5개 테스트 케이스
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin test -- --run wallets-external-actions.test 2>&1 | tail -20</automated>
  </verify>
  <done>Wallet Detail에 Credentials 탭과 External Actions 탭이 표시되고, per-wallet credential CRUD와 off-chain action 이력 조회가 동작한다. 테스트 5개 이상 통과.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/admin test -- --run` 전체 admin 테스트 통과
2. `pnpm --filter @waiaas/admin run build` 빌드 성공
3. Credentials 페이지 네비게이션에 표시
4. Wallet Detail에 Credentials + External Actions 탭 존재
</verification>

<success_criteria>
- Credentials 페이지에서 글로벌 credential 등록/삭제/로테이션 가능
- Wallet Detail Credentials 탭에서 per-wallet credential 관리 가능
- credential value(원문)가 UI 어디에서도 표시되지 않음
- External Actions 탭에서 off-chain action 이력 확인 가능 (venue, operation, status, createdAt)
- 전체 admin 테스트 통과, 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/391-admin-ui/391-01-SUMMARY.md`
</output>
