---
phase: 363-onchain-e2e-scenarios
plan: 03
type: execute
wave: 2
depends_on: [363-01]
files_modified:
  - packages/e2e-tests/src/scenarios/onchain-nft.ts
  - packages/e2e-tests/src/__tests__/onchain-nft.e2e.test.ts
autonomous: true
requirements: [ONCH-09]

must_haves:
  truths:
    - "ERC-721 NFT 전송이 성공하거나 NFT 미보유 시 skip된다"
    - "ERC-1155 NFT 전송이 성공하거나 NFT 미보유 시 skip된다"
    - "네트워크 부족 시 fail이 아닌 skip으로 처리된다"
  artifacts:
    - path: "packages/e2e-tests/src/scenarios/onchain-nft.ts"
      provides: "NFT 전송 시나리오 등록 (ERC-721, ERC-1155)"
    - path: "packages/e2e-tests/src/__tests__/onchain-nft.e2e.test.ts"
      provides: "NFT 전송 E2E 테스트"
  key_links:
    - from: "onchain-nft.e2e.test.ts"
      to: "/v1/transactions/send"
      via: "E2EHttpClient POST with NFT_TRANSFER type"
      pattern: "type.*NFT_TRANSFER"
    - from: "onchain-nft.e2e.test.ts"
      to: "/v1/wallet/nfts"
      via: "E2EHttpClient GET for NFT ownership check"
      pattern: "http\\.get.*wallet/nfts"
---

<objective>
ERC-721 및 ERC-1155 NFT 전송 온체인 E2E 시나리오를 작성한다.

Purpose: NFT 파이프라인(NFT_TRANSFER 6th type)이 실제 testnet에서 동작하는지 검증한다.
Output: NFT 시나리오 등록 파일 + NFT E2E 테스트 파일
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/363-onchain-e2e-scenarios/363-01-SUMMARY.md

@packages/e2e-tests/src/types.ts
@packages/e2e-tests/src/helpers/index.ts
@packages/e2e-tests/src/helpers/http-client.ts
@packages/e2e-tests/src/helpers/onchain-skip.ts
@packages/e2e-tests/src/__tests__/onchain-transfer.e2e.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

NFT_TRANSFER request schema:
```typescript
{ type: 'NFT_TRANSFER', to: string, token: { address: string, tokenId: string, standard: 'ERC-721' | 'ERC-1155' | 'METAPLEX', assetId?: string }, amount?: string (default '1'), network?: string }
```

POST /v1/transactions/send with NFT_TRANSFER type -> 201 { id, status }
GET /v1/transactions/:id -> { id, status, txId, network }

NFT query: GET /v1/wallet/nfts -> { items: [{ contractAddress, tokenId, standard, name, ... }], cursor }
- Used to check if the wallet owns any NFTs before attempting transfer.

Skip utility (from 363-01):
```typescript
import { shouldSkipNetwork } from '../helpers/onchain-skip.js';
```

Onchain test pattern (from 363-01):
- Connects to already-running daemon via WAIAAS_E2E_DAEMON_URL
- Creates session for existing funded wallet
- Self-transfer to preserve balance/ownership
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NFT ERC-721/ERC-1155 전송 E2E</name>
  <files>packages/e2e-tests/src/scenarios/onchain-nft.ts, packages/e2e-tests/src/__tests__/onchain-nft.e2e.test.ts</files>
  <action>
1. `scenarios/onchain-nft.ts` — 시나리오 등록:
   - `nft-erc721-transfer`: track 'onchain', category 'nft', networks ['sepolia'], protocols ['nft']
   - `nft-erc1155-transfer`: track 'onchain', category 'nft', networks ['sepolia'], protocols ['nft']

2. `__tests__/onchain-nft.e2e.test.ts`:
   - skipIf(shouldSkipNetwork('sepolia')) 패턴.
   - 환경변수에서 daemon URL, master password 읽기.
   - beforeAll: EVM 지갑 찾기, 세션 생성. GET /v1/wallet/nfts로 보유 NFT 목록 조회.

   a) `nft-erc721-transfer` describe:
      - 보유 NFT 중 standard가 'ERC-721'인 것을 찾는다. 없으면 skip ("No ERC-721 NFT owned").
      - 자기 지갑 주소로 NFT 전송:
        POST /v1/transactions/send `{ type: 'NFT_TRANSFER', to: walletAddress, token: { address: nft.contractAddress, tokenId: nft.tokenId, standard: 'ERC-721' }, network: 'sepolia' }`
      - 201 응답 확인, GET /v1/transactions/:id 폴링 (최대 90초).
      - status CONFIRMED/COMPLETED 확인. txId 존재 확인.
      - 전송 실패 시 (가스 부족, 컨트랙트 에러) graceful skip.

   b) `nft-erc1155-transfer` describe:
      - 보유 NFT 중 standard가 'ERC-1155'인 것을 찾는다. 없으면 skip ("No ERC-1155 NFT owned").
      - 자기 지갑 주소로 NFT 전송:
        POST /v1/transactions/send `{ type: 'NFT_TRANSFER', to: walletAddress, token: { address: nft.contractAddress, tokenId: nft.tokenId, standard: 'ERC-1155' }, amount: '1', network: 'sepolia' }`
      - 201 응답 확인, 상태 폴링, txId 존재 확인.
      - 전송 실패 시 graceful skip.

   - afterAll: 세션 삭제.

   참고: Sepolia testnet에서 NFT를 미리 보유하고 있어야 테스트가 실행된다. NFT가 없으면 skip 처리하여 fail이 아닌 skip으로 기록된다. Solana NFT(Metaplex)는 Out of Scope (Devnet 사전 민팅 복잡).
  </action>
  <verify>
    <automated>cd packages/e2e-tests && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>ERC-721/ERC-1155 NFT 전송 시나리오 2개가 등록되고, NFT 미보유 시 skip으로 처리된다</done>
</task>

</tasks>

<verification>
1. `cd packages/e2e-tests && npx tsc --noEmit` — 전체 타입 체크 통과
2. 시나리오 등록 확인: onchain-nft 2건
3. NFT 미보유 + 네트워크 미충족 모두 skip으로 graceful 처리
</verification>

<success_criteria>
- 2개 NFT 전송 시나리오(ERC-721, ERC-1155)가 ScenarioRegistry에 등록된다
- NFT 미보유 시 테스트가 fail이 아닌 skip으로 처리된다
- 네트워크 부족 시 skip으로 처리된다
- 타입 체크가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/363-onchain-e2e-scenarios/363-03-SUMMARY.md`
</output>
