---
phase: 359-offchain-smoke-interface-ops
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/e2e-tests/src/scenarios/ops-notification-token-connectinfo.ts
  - packages/e2e-tests/src/__tests__/ops-notification-token-connectinfo.e2e.test.ts
autonomous: true
requirements: [IFACE-04, IFACE-05, IFACE-06]
must_haves:
  truths:
    - "Notification channel status is queryable and test notification can be sent"
    - "Token registry supports CRUD: add custom token, list includes it, delete removes it"
    - "connect-info returns session info, linked wallets, and capabilities including userop"
  artifacts:
    - path: "packages/e2e-tests/src/scenarios/ops-notification-token-connectinfo.ts"
      provides: "Scenario registration for 3 operational E2E scenarios"
    - path: "packages/e2e-tests/src/__tests__/ops-notification-token-connectinfo.e2e.test.ts"
      provides: "E2E test file with notification, token registry, connect-info tests"
      min_lines: 80
  key_links:
    - from: "packages/e2e-tests/src/__tests__/ops-notification-token-connectinfo.e2e.test.ts"
      to: "daemon REST API /v1/admin/notifications/*"
      via: "E2EHttpClient with masterAuth"
      pattern: "admin/notifications"
    - from: "packages/e2e-tests/src/__tests__/ops-notification-token-connectinfo.e2e.test.ts"
      to: "daemon REST API /v1/tokens"
      via: "E2EHttpClient POST/GET/DELETE"
      pattern: "/v1/tokens"
    - from: "packages/e2e-tests/src/__tests__/ops-notification-token-connectinfo.e2e.test.ts"
      to: "daemon REST API /v1/connect-info"
      via: "E2EHttpClient sessionAuth GET"
      pattern: "/v1/connect-info"
---

<objective>
Notification channel, token registry CRUD, and connect-info self-discovery E2E tests.

Purpose: Verifies operational features that AI agents depend on -- notification events, token management, and API capability discovery.
Output: 1 scenario registration file + 1 test file with 3 describe blocks.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/e2e-tests/src/scenarios/core-auth-wallet-session.ts
@packages/e2e-tests/src/__tests__/core-policy.e2e.test.ts
@packages/e2e-tests/src/helpers/index.ts
@packages/e2e-tests/src/helpers/session.ts
@packages/e2e-tests/src/types.ts

<interfaces>
<!-- Relevant API endpoints for this plan -->

Token Registry API:
- GET /v1/tokens?network={network} (masterAuth) -> { tokens: [...], total: N }
- POST /v1/tokens (masterAuth, body: { network, address, symbol, name, decimals }) -> { success, token }
- DELETE /v1/tokens (masterAuth, body: { network, address }) -> { success }

Notification API:
- GET /v1/admin/notifications/status (masterAuth) -> { channels: [...], enabled: bool }
- POST /v1/admin/notifications/test (masterAuth, body: { message?: string }) -> { success, channel }
- GET /v1/admin/notifications/log (masterAuth) -> { items: [...], total: N }

Connect-Info API:
- GET /v1/connect-info (sessionAuth) -> { session, wallets: [...], capabilities, daemon, prompt }
  - capabilities includes userop, nft, defi, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 3 ops scenarios + create E2E test file</name>
  <files>packages/e2e-tests/src/scenarios/ops-notification-token-connectinfo.ts, packages/e2e-tests/src/__tests__/ops-notification-token-connectinfo.e2e.test.ts</files>
  <action>
1. Create scenario registration file `packages/e2e-tests/src/scenarios/ops-notification-token-connectinfo.ts`:
   - Register 3 scenarios in global registry:
     - id: 'notification-channel', name: 'Notification Channel', track: 'offchain', category: 'interface', description: 'Notification status query + test notification send + log check'
     - id: 'token-registry-crud', name: 'Token Registry CRUD', track: 'offchain', category: 'interface', description: 'Add custom ERC-20 token -> list -> delete -> confirm removal'
     - id: 'connect-info-discovery', name: 'Connect-Info Discovery', track: 'offchain', category: 'interface', description: 'Session connect-info returns wallets, capabilities, userop status'

2. Create test file `packages/e2e-tests/src/__tests__/ops-notification-token-connectinfo.e2e.test.ts`:
   - Import DaemonManager, setupDaemonSession, E2EHttpClient from helpers
   - Import scenario registration (side-effect)
   - beforeAll: start daemon, create session via setupDaemonSession
   - afterAll: stop daemon

   **describe('notification-channel')** (IFACE-04):
   - it('queries notification status'): GET /v1/admin/notifications/status with masterAuth, expect 200, expect body to have a channels property (may be empty array if no channel configured, that's ok)
   - it('sends test notification (may fail gracefully if no channel configured)'): POST /v1/admin/notifications/test with masterAuth and body { message: 'e2e-test' }. Accept either 200 (success) or 400/422 (no channel configured) -- both are valid smoke outcomes. The key verification is that the endpoint responds properly.
   - it('queries notification log'): GET /v1/admin/notifications/log with masterAuth, expect 200, expect body to have items array (may be empty)

   **describe('token-registry-crud')** (IFACE-05):
   - it('adds a custom token to registry'): POST /v1/tokens with masterAuth, body: { network: 'ethereum-sepolia', address: '0x1234567890abcdef1234567890abcdef12345678', symbol: 'E2ETKN', name: 'E2E Test Token', decimals: 18 }. Expect 201 or 200.
   - it('lists tokens and finds the custom token'): GET /v1/tokens?network=ethereum-sepolia with masterAuth, expect 200, expect body.tokens (or body array) to contain a token with symbol 'E2ETKN'.
   - it('deletes the custom token and confirms removal'): DELETE /v1/tokens with masterAuth, body: { network: 'ethereum-sepolia', address: '0x1234567890abcdef1234567890abcdef12345678' }, expect 200. Then GET /v1/tokens?network=ethereum-sepolia, confirm E2ETKN is not in the list (it may still show built-in tokens, just not the custom one).

   **describe('connect-info-discovery')** (IFACE-06):
   - it('returns session and wallets from connect-info'): GET /v1/connect-info with sessionAuth (Bearer token), expect 200. Expect body to have session object with id. Expect body.wallets to be an array with at least 1 wallet.
   - it('includes capabilities in connect-info'): Same GET /v1/connect-info. Expect body.capabilities or body.daemon to exist. Check that the response includes some form of capability listing (the exact structure may vary, so check for the presence of key fields rather than exact values).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run build --filter=@waiaas/e2e-tests && node -e "require('./packages/e2e-tests/dist/scenarios/ops-notification-token-connectinfo.js')"</automated>
  </verify>
  <done>3 scenarios registered. Notification status/test/log endpoints smoke-tested. Token registry CRUD (add/list/delete) verified. connect-info returns session, wallets, and capabilities.</done>
</task>

</tasks>

<verification>
- `pnpm turbo run build --filter=@waiaas/e2e-tests` succeeds
- Scenario file exports 3 registered scenarios
- Test file follows established pattern
</verification>

<success_criteria>
- Notification endpoints respond without crash (status/test/log)
- Token registry add/list/delete cycle works for custom token
- connect-info returns session info with linked wallet and capabilities
</success_criteria>

<output>
After completion, create `.planning/phases/359-offchain-smoke-interface-ops/359-02-SUMMARY.md`
</output>
