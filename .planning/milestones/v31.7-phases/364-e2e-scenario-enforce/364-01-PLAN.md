---
phase: 364-e2e-scenario-enforce
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-e2e-coverage.ts
  - packages/e2e-tests/src/e2e-coverage-map.ts
autonomous: true
requirements:
  - ENFORCE-01
  - ENFORCE-02
  - ENFORCE-04

must_haves:
  truths:
    - "신규 Action Provider를 추가하고 E2E 매핑을 등록하지 않으면 스크립트가 exit 1로 실패한다"
    - "신규 REST API route 파일을 추가하고 E2E 매핑을 등록하지 않으면 스크립트가 exit 1로 실패한다"
    - "시나리오 파일에 registry.register 호출이 없으면 빈 파일로 감지되어 실패한다"
    - "모든 매핑이 등록되어 있으면 스크립트가 exit 0으로 성공한다"
  artifacts:
    - path: "scripts/verify-e2e-coverage.ts"
      provides: "E2E coverage verification script"
      min_lines: 100
    - path: "packages/e2e-tests/src/e2e-coverage-map.ts"
      provides: "Provider/API to scenario mapping registry"
      min_lines: 50
  key_links:
    - from: "scripts/verify-e2e-coverage.ts"
      to: "packages/e2e-tests/src/e2e-coverage-map.ts"
      via: "import coverage map"
      pattern: "e2e-coverage-map"
    - from: "scripts/verify-e2e-coverage.ts"
      to: "packages/actions/src/providers/*"
      via: "filesystem scan for provider dirs"
      pattern: "providers/"
    - from: "scripts/verify-e2e-coverage.ts"
      to: "packages/daemon/src/api/routes/*"
      via: "filesystem scan for route files"
      pattern: "api/routes/"
---

<objective>
Action Provider 목록과 REST API route 파일을 filesystem에서 스캔한 뒤, E2E 시나리오 매핑 레지스트리와 대조하여 미등록 항목을 탐지하는 검증 스크립트를 작성한다. 빈 시나리오 파일도 동시에 검증한다.

Purpose: 신규 기능 추가 시 E2E 시나리오 누락을 구조적으로 방지
Output: verify-e2e-coverage.ts 스크립트 + e2e-coverage-map.ts 매핑 파일
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/e2e-tests/src/types.ts
@packages/e2e-tests/src/scenarios/
@packages/actions/src/providers/
@packages/daemon/src/api/routes/index.ts
@scripts/verify-enum-ssot.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E 커버리지 매핑 레지스트리 생성</name>
  <files>packages/e2e-tests/src/e2e-coverage-map.ts</files>
  <action>
E2E 시나리오가 커버하는 Action Provider와 REST API route를 선언적으로 매핑하는 파일을 생성한다.

1. `providerCoverage` 맵 생성: Action Provider 디렉토리 이름(key) -> 커버하는 E2E 시나리오 파일명(value).
   현재 providers 목록 (packages/actions/src/providers/):
   - aave-v3, across, dcent-swap, drift, erc8004, hyperliquid, jito-staking, jupiter-swap, kamino, lido-staking, lifi, pendle, zerox-swap
   각 provider에 대응하는 시나리오 파일을 매핑한다. DeFi provider 중 mainnet-only인 것(jupiter-swap, zerox-swap, lifi, aave-v3, kamino, pendle, drift, dcent-swap, across)은 offchain settings 테스트(advanced-defi-settings-push-relay.ts)로 매핑한다. erc8004는 advanced-x402-erc8004-erc8128.ts로 매핑. hyperliquid는 onchain-hyperliquid.ts + advanced-defi-settings-push-relay.ts. lido-staking은 onchain-staking.ts. jito-staking은 onchain-staking.ts.

2. `routeCoverage` 맵 생성: REST API route 파일명(key, .ts 제외) -> 커버하는 E2E 시나리오 파일명(value).
   현재 route 파일 목록 (packages/daemon/src/api/routes/):
   - actions, admin-actions, admin, audit-logs, connect-info, defi-positions, erc8004, erc8128, health, hyperliquid, incoming, mcp, nft-approvals, nfts, nonce, openapi-schemas, policies, sessions, skills, staking, tokens, transactions, userop, utils, wallet-apps, wallet, wallets, wc, webhooks, x402
   유틸리티/내부용 route 파일(index, openapi-schemas, utils, display-currency-helper)을 제외 목록(ROUTE_EXCLUDES)에 넣는다.
   각 route 파일을 해당 E2E 시나리오에 매핑한다.

3. `scenarioMinTests` 상수: 시나리오 파일당 최소 registry.register() 호출 수 (기본값 1).

4. 모든 맵과 제외 목록을 named export로 내보낸다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsx --eval "import { providerCoverage, routeCoverage } from './packages/e2e-tests/src/e2e-coverage-map.js'; console.log('providers:', Object.keys(providerCoverage).length, 'routes:', Object.keys(routeCoverage).length)" 2>/dev/null || echo "TypeScript import check: verify file compiles"</automated>
  </verify>
  <done>providerCoverage에 13개 provider가 매핑되고, routeCoverage에 유틸리티 제외 모든 route 파일이 매핑되어 있다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verify-e2e-coverage.ts 검증 스크립트 작성</name>
  <files>scripts/verify-e2e-coverage.ts</files>
  <behavior>
    - Test 1: 현재 코드베이스 상태에서 모든 provider/route가 매핑되어 있으므로 exit 0
    - Test 2: providerCoverage에 없는 provider 디렉토리가 있으면 에러 리포트 출력 후 exit 1
    - Test 3: routeCoverage에 없는 route 파일이 있으면 에러 리포트 출력 후 exit 1
    - Test 4: 시나리오 파일에 registry.register 호출이 0개면 빈 파일로 감지하여 exit 1
    - Test 5: --fix-hint 모드에서 누락 항목에 대한 수정 힌트를 출력
  </behavior>
  <action>
scripts/verify-enum-ssot.ts 패턴을 따르는 검증 스크립트를 작성한다. tsx로 실행 가능하도록 #!/usr/bin/env tsx shebang 사용.

**Step 1: Provider coverage 검증**
- fs.readdirSync로 packages/actions/src/providers/ 하위 디렉토리 목록을 수집
- e2e-coverage-map.ts의 providerCoverage와 대조
- 매핑에 없는 provider가 있으면 에러로 기록

**Step 2: Route coverage 검증**
- fs.readdirSync로 packages/daemon/src/api/routes/ 하위 .ts 파일 목록을 수집
- ROUTE_EXCLUDES에 있는 파일은 제외 (index.ts, openapi-schemas.ts, utils.ts, display-currency-helper.ts)
- e2e-coverage-map.ts의 routeCoverage와 대조
- 매핑에 없는 route 파일이 있으면 에러로 기록

**Step 3: 빈 시나리오 파일 검증 (ENFORCE-04)**
- packages/e2e-tests/src/scenarios/ 하위 .ts 파일을 읽기
- 각 파일에서 `registry.register(` 문자열 출현 횟수를 카운트
- 0회인 파일을 빈 파일로 에러 기록

**Step 4: 결과 리포트**
- 성공 시: "E2E coverage: ALL PASSED (N providers, M routes, K scenarios)" 출력 후 exit 0
- 실패 시: 누락 항목을 카테고리별로 출력하고, "Add missing mappings to packages/e2e-tests/src/e2e-coverage-map.ts" 힌트와 함께 exit 1

**root package.json에 스크립트 추가:**
"verify:e2e-coverage": "tsx scripts/verify-e2e-coverage.ts"
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsx scripts/verify-e2e-coverage.ts</automated>
  </verify>
  <done>현재 코드베이스에서 스크립트가 exit 0으로 성공하고, 모든 provider/route/시나리오가 매핑 및 비어있지 않음이 확인된다. root package.json에 verify:e2e-coverage 스크립트가 등록된다.</done>
</task>

</tasks>

<verification>
1. `pnpm verify:e2e-coverage` 가 exit 0으로 성공
2. providerCoverage에 packages/actions/src/providers/ 하위 모든 디렉토리가 포함
3. routeCoverage에 유틸리티 제외 packages/daemon/src/api/routes/ 하위 모든 .ts 파일이 포함
4. 시나리오 파일 전부에 registry.register 호출이 1회 이상 존재
</verification>

<success_criteria>
- scripts/verify-e2e-coverage.ts가 현재 코드베이스에서 정상 통과 (exit 0)
- packages/e2e-tests/src/e2e-coverage-map.ts에 모든 매핑이 선언적으로 등록
- 누락 provider/route 발생 시 명확한 에러 메시지와 수정 힌트 출력
</success_criteria>

<output>
After completion, create `.planning/phases/364-e2e-scenario-enforce/364-01-SUMMARY.md`
</output>
