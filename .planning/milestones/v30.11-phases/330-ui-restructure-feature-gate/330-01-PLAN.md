---
phase: 330-ui-restructure-feature-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/__tests__/settings-service.test.ts
  - packages/daemon/src/__tests__/erc8004-routes.test.ts
  - packages/daemon/src/__tests__/kamino-lending-e2e.test.ts
  - packages/actions/src/__tests__/erc8004-provider.test.ts
  - packages/actions/src/__tests__/kamino-provider.test.ts
  - packages/admin/src/__tests__/erc8004.test.tsx
  - packages/admin/src/__tests__/actions-kamino.test.tsx
autonomous: true
requirements:
  - GATE-02
  - GATE-03
  - GATE-04

must_haves:
  truths:
    - "All 10 action providers default to enabled (true) on fresh install"
    - "Existing operator settings are preserved by INSERT OR IGNORE in DB v42 migration"
    - "All existing tests pass after default value changes"
  artifacts:
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "4 provider defaults changed from false to true"
      contains: "kamino_enabled.*defaultValue: 'true'"
    - path: "packages/daemon/src/infrastructure/database/migrate.ts"
      provides: "v42 migration with INSERT OR IGNORE for 10 enabled settings"
      contains: "version: 42"
  key_links:
    - from: "packages/daemon/src/infrastructure/database/migrate.ts"
      to: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      via: "INSERT OR IGNORE uses same setting keys"
      pattern: "INSERT OR IGNORE INTO settings"
---

<objective>
Change all 10 action provider defaults to true and create DB v42 migration.

Purpose: New installations should have all DeFi providers and ERC-8004 agent enabled by default, while existing operators who manually disabled providers keep their settings.
Output: Updated setting-keys.ts defaults, v42 migration, updated tests.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change 4 provider defaults to true + create DB v42 migration</name>
  <files>
    packages/daemon/src/infrastructure/settings/setting-keys.ts
    packages/daemon/src/infrastructure/database/migrate.ts
  </files>
  <action>
In `setting-keys.ts`, change `defaultValue` from `'false'` to `'true'` for these 4 keys (the other 6 are already true):
- `actions.kamino_enabled` (line ~214)
- `actions.pendle_yield_enabled` (line ~219)
- `actions.drift_enabled` (line ~228) -- note: key is `drift_enabled` not `drift_perp_enabled`
- `actions.erc8004_agent_enabled` (line ~266)

In `migrate.ts`:
1. Update `LATEST_SCHEMA_VERSION` from 41 to 42 (line ~82)
2. Add v42 migration after the v41 block. The migration uses `INSERT OR IGNORE INTO settings` for all 10 `_enabled` keys to seed them as `'true'` for fresh installs AND respect existing operator values (INSERT OR IGNORE skips if key already exists):

```typescript
MIGRATIONS.push({
  version: 42,
  description: 'Seed all 10 action provider _enabled defaults to true (INSERT OR IGNORE preserves existing)',
  up: (sqlite) => {
    const keys = [
      'actions.jupiter_swap_enabled',
      'actions.zerox_swap_enabled',
      'actions.lifi_enabled',
      'actions.lido_staking_enabled',
      'actions.jito_staking_enabled',
      'actions.aave_v3_enabled',
      'actions.kamino_enabled',
      'actions.pendle_yield_enabled',
      'actions.drift_enabled',
      'actions.erc8004_agent_enabled',
    ];
    const now = Math.floor(Date.now() / 1000);
    const stmt = sqlite.prepare(
      "INSERT OR IGNORE INTO settings (key, value, source, updated_at) VALUES (?, 'true', 'migration', ?)",
    );
    for (const key of keys) {
      stmt.run(key, now);
    }
  },
});
```

NOTE: Check the actual `settings` table column names first by looking at pushSchema or existing INSERT statements. The columns might be `(key, value, source, updated_at)` or similar -- match the existing pattern exactly.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c "defaultValue: 'false'" packages/daemon/src/infrastructure/settings/setting-keys.ts | grep -v "actions\." ; grep "LATEST_SCHEMA_VERSION = 42" packages/daemon/src/infrastructure/database/migrate.ts</automated>
  </verify>
  <done>All 10 action provider defaults are 'true' in setting-keys.ts, LATEST_SCHEMA_VERSION is 42, v42 migration exists with INSERT OR IGNORE for all 10 keys</done>
</task>

<task type="auto">
  <name>Task 2: Update tests for default value changes</name>
  <files>
    packages/daemon/src/__tests__/settings-service.test.ts
    packages/daemon/src/__tests__/erc8004-routes.test.ts
    packages/daemon/src/__tests__/kamino-lending-e2e.test.ts
    packages/actions/src/__tests__/erc8004-provider.test.ts
    packages/actions/src/__tests__/kamino-provider.test.ts
    packages/admin/src/__tests__/erc8004.test.tsx
    packages/admin/src/__tests__/actions-kamino.test.tsx
  </files>
  <action>
Search all test files for assertions that expect `kamino_enabled`, `pendle_yield_enabled`, `drift_enabled`, or `erc8004_agent_enabled` to be `'false'` by default, and update them to expect `'true'`.

Key files to update:
1. `packages/daemon/src/__tests__/settings-service.test.ts` -- any test asserting default values for these 4 keys
2. `packages/daemon/src/__tests__/erc8004-routes.test.ts` -- tests may mock `erc8004_agent_enabled` as `'false'` for "disabled" scenarios; keep those but fix any "default state" assertions
3. `packages/daemon/src/__tests__/kamino-lending-e2e.test.ts` -- may expect kamino disabled by default
4. `packages/actions/src/__tests__/erc8004-provider.test.ts` -- provider registration tests expecting disabled default
5. `packages/actions/src/__tests__/kamino-provider.test.ts` -- similar
6. `packages/admin/src/__tests__/erc8004.test.tsx` -- the `defaultSettings` mock has `erc8004_agent_enabled: { value: 'false', source: 'default' }`. Update to `'true'`. Also review the "disabled" test case -- keep it but make sure it explicitly sets the value to false (not relying on default)
7. `packages/admin/src/__tests__/actions-kamino.test.tsx` -- similar pattern

IMPORTANT: Do NOT change tests that intentionally test the "disabled" state. Only change tests that assert the DEFAULT value. Disabled-state tests should explicitly set the value to `'false'` rather than relying on the default.

Also write a new test for the v42 migration in one of the existing migration test files (or inline in settings-service.test.ts):
- Create a DB at v41, insert some settings manually (e.g., `actions.kamino_enabled = 'false'`), run v42 migration, verify the manually-set value is preserved while new keys get `'true'`.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test --filter=@waiaas/daemon -- --run --reporter=verbose 2>&1 | tail -20 && pnpm turbo run test --filter=@waiaas/actions -- --run --reporter=verbose 2>&1 | tail -20 && pnpm turbo run test --filter=@waiaas/admin -- --run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass with updated default values. v42 migration test confirms INSERT OR IGNORE preserves existing operator settings while seeding defaults for new installs.</done>
</task>

</tasks>

<verification>
1. `grep "defaultValue: 'false'" packages/daemon/src/infrastructure/settings/setting-keys.ts` should NOT match any action provider `_enabled` key
2. `grep "version: 42" packages/daemon/src/infrastructure/database/migrate.ts` confirms migration exists
3. `pnpm turbo run test --filter=@waiaas/daemon --filter=@waiaas/actions --filter=@waiaas/admin -- --run` all pass
</verification>

<success_criteria>
- 10 action provider _enabled settings all default to 'true' in setting-keys.ts
- DB v42 migration seeds all 10 keys with INSERT OR IGNORE
- All existing tests updated and passing
- New migration test verifies INSERT OR IGNORE preserves existing values
</success_criteria>

<output>
After completion, create `.planning/phases/330-ui-restructure-feature-gate/330-01-SUMMARY.md`
</output>
