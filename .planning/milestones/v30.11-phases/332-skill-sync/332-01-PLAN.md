---
phase: 332-skill-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/admin.skill.md
  - skills/erc8004.skill.md
  - skills/actions.skill.md
  - skills/policies.skill.md
autonomous: true
requirements: [SKIL-01, SKIL-02, SKIL-03, SKIL-04]

must_haves:
  truths:
    - "admin.skill.md에 DeFi/Agent Identity 메뉴 이름과 tier 오버라이드 설정 방법이 문서화되어 있다"
    - "erc8004.skill.md에 #/agent-identity 경로와 기본 활성화 상태가 반영되어 있다"
    - "actions.skill.md에 DeFi 메뉴 이름, tier 오버라이드 기능, 전 프로바이더 기본 활성화가 반영되어 있다"
    - "policies.skill.md에 tier 오버라이드와 정책 tier 에스컬레이션의 관계가 명시되어 있다"
  artifacts:
    - path: "skills/admin.skill.md"
      provides: "Admin UI 메뉴 이름 변경 + tier 오버라이드 설정 문서"
      contains: "DeFi"
    - path: "skills/erc8004.skill.md"
      provides: "Agent Identity 경로 + 기본 활성화 반영"
      contains: "agent-identity"
    - path: "skills/actions.skill.md"
      provides: "DeFi 메뉴 이름 + tier 오버라이드 + 기본 활성화"
      contains: "tier override"
    - path: "skills/policies.skill.md"
      provides: "Action tier 오버라이드와 정책 tier 에스컬레이션 관계"
      contains: "action tier"
  key_links:
    - from: "skills/actions.skill.md"
      to: "skills/policies.skill.md"
      via: "tier override -> policy escalation cross-reference"
      pattern: "max.*policyTier.*actionTier"
---

<objective>
v30.11에서 변경된 Admin UI 메뉴 이름(Actions->DeFi, ERC-8004->Agent Identity), 라우트, 기본 활성화 상태, tier 오버라이드 기능을 4개 스킬 파일에 반영한다.

Purpose: AI 에이전트와 운영자가 스킬 파일을 참조할 때 현재 코드베이스의 실제 동작과 일치하는 정보를 얻을 수 있도록 한다.
Output: 4개 스킬 파일 업데이트 (admin.skill.md, erc8004.skill.md, actions.skill.md, policies.skill.md)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

Changes from Phase 330-331 to reflect:
1. Menu rename: Actions -> DeFi, ERC-8004 -> Agent Identity
2. Route rename: #/actions -> #/defi, #/erc8004 -> #/agent-identity
3. All 10 action providers default-enabled (was: only some enabled by default)
4. Tier override via Settings: actions.{provider_key}_{action_name}_tier (e.g., actions.jupiter_swap_swap_tier)
5. Allowed values: INSTANT/NOTIFY/DELAY/APPROVAL, empty string = use provider default
6. Pipeline floor: max(policyTier, actionTier) -- action tier is escalation-only
7. Admin UI: Description column in Registered Actions, tier dropdown, "customized" badge, reset to default
8. ERC-8004 default changed from false to true (all providers default-enabled)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin.skill.md and erc8004.skill.md</name>
  <files>skills/admin.skill.md, skills/erc8004.skill.md</files>
  <action>
**admin.skill.md (SKIL-01):**
1. Section 15 heading: "ERC-8004 Settings" -> "ERC-8004 / Agent Identity Settings"
2. Section 15 description: change "Configure via Admin UI > Settings > Actions" to "Configure via Admin UI > Agent Identity (route: `#/agent-identity`), or via the Settings API"
3. Change `actions.erc8004_agent_enabled` default from `false` to `true` in the table
4. Add a new section 15.1 or expand section 15 with "Tier Override Settings" subsection explaining:
   - Setting key pattern: `actions.{provider_key}_{action_name}_tier`
   - Allowed values: INSTANT, NOTIFY, DELAY, APPROVAL (Zod enum)
   - Empty string or unset = provider default
   - Examples: `actions.jupiter_swap_swap_tier`, `actions.erc8004_agent_register_agent_tier`
   - Pipeline behavior: `max(policyTier, actionTier)` -- escalation only, never downgrade
   - Configure via Admin UI > DeFi (route: `#/defi`) or Agent Identity (route: `#/agent-identity`) tier dropdown
5. In section 16 (Related Skill Files), no changes needed

**erc8004.skill.md (SKIL-02):**
1. Section 3 (Admin configuration note): change "Admin UI > Settings > Actions" references to "Admin UI > Agent Identity (`#/agent-identity`)"
2. Section 5 (Admin Settings): change "Configure ERC-8004 via Admin UI > Settings > Actions" to "Configure ERC-8004 via Admin UI > Agent Identity (`#/agent-identity`)"
3. Change `actions.erc8004_agent_enabled` default from `false` to `true` in the settings table
4. Update the description from "Master feature gate. Must be true to use ERC-8004." to "Master feature gate. Enabled by default since v30.11."
5. Section 2 "Write Actions" prerequisite note: change "Prerequisite: Enable the provider via Admin Settings: `actions.erc8004_agent_enabled=true`." to "The provider is enabled by default since v30.11. To disable: set `actions.erc8004_agent_enabled=false` via Admin UI > Agent Identity."
6. In section 5 "Admin Settings", add a note about tier override: "Action tier can be overridden per-action via the tier dropdown in Admin UI > Agent Identity, or via Settings API key pattern `actions.erc8004_agent_{action}_tier`. See **admin.skill.md** Section 15."
7. Section 7 "Common Workflows" step 1: remove the "Enable ERC-8004" step since it is now enabled by default, or change to "Verify ERC-8004 is enabled (default: true since v30.11)"
  </action>
  <verify>
    <automated>grep -c "agent-identity" skills/erc8004.skill.md && grep -c "true" skills/erc8004.skill.md | head -1 && grep -c "tier override\|Tier Override\|tier_tier" skills/admin.skill.md</automated>
  </verify>
  <done>
- admin.skill.md에 DeFi/Agent Identity 메뉴 경로가 언급되고, tier 오버라이드 설정 방법(키 패턴, 허용 값, 파이프라인 동작)이 문서화됨
- erc8004.skill.md에 #/agent-identity 경로가 반영되고, 기본 활성화(true) 상태가 반영됨
  </done>
</task>

<task type="auto">
  <name>Task 2: Update actions.skill.md and policies.skill.md</name>
  <files>skills/actions.skill.md, skills/policies.skill.md</files>
  <action>
**actions.skill.md (SKIL-03):**
1. All occurrences of "Admin UI > Settings > Actions" -> "Admin UI > DeFi (`#/defi`)" for DeFi providers, and "Admin UI > Agent Identity (`#/agent-identity`)" for ERC-8004
2. Section 14 heading/content: "Configuration via Admin Settings" -- update Admin UI path from "Settings > Actions > [Provider Name]" to "DeFi (`#/defi`) > [Provider Name]" for DeFi providers
3. Section 12 (ERC-8004): change `actions.erc8004_agent_enabled` feature gate description: "(default: false)" -> "(default: true since v30.11)"
4. Per-provider "Enable X via" instructions: remove "Admin UI > Settings > Actions > X" and replace with "Admin UI > DeFi (`#/defi`)" for each DeFi provider section
5. Add a new section (or subsection of Section 14) titled "Action Tier Override (v30.11)" documenting:
   - Setting key pattern: `actions.{provider}_{action}_tier` (e.g., `actions.jupiter_swap_swap_tier`)
   - Values: INSTANT / NOTIFY / DELAY / APPROVAL -- empty or unset uses provider hardcoded default
   - All 10 providers now default-enabled (no manual enablement needed for fresh install)
   - Pipeline: `effectiveTier = max(policyTier, actionTier)` -- action tier is a floor, can only escalate
   - Admin UI: tier dropdown in DeFi/Agent Identity Registered Actions table, "customized" badge for overrides, "Reset to default" button
   - Cross-reference: "Policy tier escalation rules apply on top of action tier. See **policies.skill.md** Section 3."
6. Provider status section: add note about default-enabled state -- "Since v30.11, all 10 built-in providers are enabled by default on fresh install."

**policies.skill.md (SKIL-04):**
1. Section 3 (Policy Evaluation Flow), step 3 (Tier assignment): add a bullet or paragraph explaining action tier interaction:
   - "**Action tier override (v30.11):** When an action is executed via the Action Provider framework, the action's tier override (configured via Admin Settings key `actions.{provider}_{action}_tier`) is combined with the policy-assigned tier using escalation-only logic: `effectiveTier = max(policyTier, actionTier)`. The action tier can only raise the security level, never lower it. For example, if a SPENDING_LIMIT policy assigns NOTIFY but the action tier override is DELAY, the effective tier is DELAY. If the policy assigns APPROVAL, it remains APPROVAL regardless of action tier."
2. Add cross-reference at end of Section 3: "For action tier configuration, see **actions.skill.md** Section 14 and **admin.skill.md** Section 15."
  </action>
  <verify>
    <automated>grep -c "DeFi\|#/defi" skills/actions.skill.md && grep -c "tier override\|action tier\|actionTier" skills/actions.skill.md && grep -c "action tier\|actionTier\|Action tier" skills/policies.skill.md</automated>
  </verify>
  <done>
- actions.skill.md에 DeFi 메뉴 이름, tier 오버라이드 기능 설명, 전 프로바이더 기본 활성화가 반영됨
- policies.skill.md Section 3에 action tier override와 정책 tier 에스컬레이션의 관계(escalation-only, max logic)가 명시됨
  </done>
</task>

</tasks>

<verification>
1. `grep -c "agent-identity" skills/erc8004.skill.md` -- erc8004 스킬에 새 경로 반영 확인
2. `grep -c "#/defi" skills/actions.skill.md` -- actions 스킬에 새 DeFi 라우트 반영 확인
3. `grep "default.*true" skills/erc8004.skill.md` -- 기본 활성화 반영 확인
4. `grep "max.*policyTier\|escalation" skills/policies.skill.md` -- 정책-액션 tier 관계 명시 확인
5. `grep "tier override\|Tier Override" skills/admin.skill.md` -- tier 오버라이드 설정 방법 문서화 확인
</verification>

<success_criteria>
1. admin.skill.md에 DeFi/Agent Identity 메뉴 이름과 tier 오버라이드 설정 방법(키 패턴, 허용 값, 파이프라인 동작)이 문서화되어 있다
2. erc8004.skill.md에 `#/agent-identity` 경로와 기본 활성화 상태(true)가 반영되어 있다
3. actions.skill.md에 DeFi 메뉴 이름, tier 오버라이드 기능, 전 프로바이더 기본 활성화가 반영되어 있다
4. policies.skill.md에 tier 오버라이드와 정책 tier 에스컬레이션의 관계(escalation-only, max logic)가 명시되어 있다
5. 모든 "Admin UI > Settings > Actions" 참조가 "Admin UI > DeFi" 또는 "Admin UI > Agent Identity"로 업데이트됨
</success_criteria>

<output>
After completion, create `.planning/phases/332-skill-sync/332-01-SUMMARY.md`
</output>
