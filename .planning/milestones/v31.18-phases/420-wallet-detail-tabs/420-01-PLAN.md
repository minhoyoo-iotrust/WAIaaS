---
phase: 420-wallet-detail-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/wallets.tsx
  - packages/admin/src/__tests__/wallets.test.tsx
autonomous: true
requirements:
  - DETL-01
  - DETL-02
  - DETL-03
  - DETL-04
  - DETL-05
  - DETL-09

must_haves:
  truths:
    - "DETAIL_TABS 배열이 4개(overview/activity/assets/setup)만 포함한다"
    - "Overview 탭에 Wallet Info 아래 Owner Protection 카드가 항상 보인다"
    - "Owner 미등록(NONE) 시 경고 배너 + Register Owner CTA 버튼이 표시된다"
    - "Owner 등록(GRACE/LOCKED) 시 상태 요약(ownerState/approvalMethod/ownerAddress) + Manage 버튼이 표시된다"
    - "Register Owner 또는 Manage 클릭 시 기존 OwnerTab의 전체 등록/관리 UI가 인라인 토글로 표시된다"
  artifacts:
    - path: "packages/admin/src/pages/wallets.tsx"
      provides: "DETAIL_TABS 4-tab definition + OwnerProtectionCard inline in OverviewTab"
      contains: "overview.*activity.*assets.*setup"
  key_links:
    - from: "OverviewTab"
      to: "OwnerProtectionCard"
      via: "inline JSX within OverviewTab function"
      pattern: "Owner Protection"
---

<objective>
DETAIL_TABS 배열을 8탭에서 4탭(Overview/Activity/Assets/Setup)으로 재정의하고, Overview 탭에 Owner Protection 요약 카드를 통합한다.

Purpose: 지갑 상세 탭 수를 줄여 사용자 탐색 부담을 감소시키고, Owner 보호 상태를 가장 눈에 띄는 Overview 탭에 노출한다.
Output: wallets.tsx에서 DETAIL_TABS가 4개로 변경되고, OverviewTab 내 Owner Protection 카드가 동작한다.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/admin/src/pages/wallets.tsx
</context>

<interfaces>
<!-- Key structures in wallets.tsx that this plan modifies -->

Current DETAIL_TABS (line 204):
```typescript
const DETAIL_TABS = [
  { key: 'overview', label: 'Overview' },
  { key: 'transactions', label: 'Transactions' },
  { key: 'owner', label: 'Owner' },
  { key: 'staking', label: 'Staking' },
  { key: 'nfts', label: 'NFTs' },
  { key: 'credentials', label: 'Credentials' },
  { key: 'external-actions', label: 'External Actions' },
  { key: 'mcp', label: 'MCP' },
];
```

Tab rendering (line 2167-2175):
```typescript
<TabNav tabs={DETAIL_TABS} activeTab={activeDetailTab.value} onTabChange={(k) => { activeDetailTab.value = k; }} />
{activeDetailTab.value === 'overview' && <OverviewTab />}
{activeDetailTab.value === 'transactions' && <TransactionsTab />}
{activeDetailTab.value === 'owner' && <OwnerTab />}
{activeDetailTab.value === 'staking' && <StakingTab />}
{activeDetailTab.value === 'nfts' && <NftTab />}
{activeDetailTab.value === 'credentials' && <CredentialsTab />}
{activeDetailTab.value === 'external-actions' && <ExternalActionsTab />}
{activeDetailTab.value === 'mcp' && <McpTab />}
```

Owner-related wallet fields used:
- wallet.value.ownerState: 'NONE' | 'GRACE' | 'LOCKED'
- wallet.value.ownerAddress: string | null
- wallet.value.approvalMethod: string | null
- wallet.value.walletType: string | null

Existing OwnerTab function (line 1116-1385): Full owner registration/management UI including wallet type selection, address edit, approval method radio buttons, WalletConnect section.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DETAIL_TABS 4탭 재정의 + Overview Owner Protection 카드</name>
  <files>packages/admin/src/pages/wallets.tsx</files>
  <action>
1. DETAIL_TABS 배열을 4개로 변경:
```typescript
const DETAIL_TABS = [
  { key: 'overview', label: 'Overview' },
  { key: 'activity', label: 'Activity' },
  { key: 'assets', label: 'Assets' },
  { key: 'setup', label: 'Setup' },
];
```

2. Tab 렌더링 영역(line ~2167-2175)을 4개 탭에 맞게 변경:
```typescript
{activeDetailTab.value === 'overview' && <OverviewTab />}
{activeDetailTab.value === 'activity' && <ActivityTab />}
{activeDetailTab.value === 'assets' && <AssetsTab />}
{activeDetailTab.value === 'setup' && <SetupTab />}
```
(ActivityTab, AssetsTab, SetupTab 함수는 Plan 02에서 구현. 이 Plan에서는 각 함수를 임시 스텁으로 생성: `function ActivityTab() { return <div>Activity</div>; }` 형태. 이렇게 해야 빌드가 깨지지 않는다.)

3. OverviewTab 함수(line ~781) 끝에 Owner Protection 카드를 추가. Wallet Info detail-grid 다음(balances 테이블 직전 또는 직후)에 배치:

- `showOwnerManage`라는 useSignal(false) 시그널을 WalletDetail 함수 스코프에 추가 (owner 관리 UI 토글용).

- Owner Protection 카드 JSX:
```tsx
{/* Owner Protection */}
<div style={{
  border: '1px solid var(--color-border)',
  borderRadius: 'var(--radius-lg)',
  padding: 'var(--space-4)',
  marginTop: 'var(--space-4)',
}}>
  <h3 style={{ margin: '0 0 var(--space-3) 0', fontSize: '1rem' }}>Owner Protection</h3>

  {wallet.value.ownerState === 'NONE' ? (
    // 미등록: 경고 배너 + Register Owner CTA
    <div>
      <div style={{
        background: 'var(--color-warning-bg, #fff3cd)',
        border: '1px solid var(--color-warning-border, #ffc107)',
        borderRadius: 'var(--radius-md)',
        padding: 'var(--space-2) var(--space-3)',
        marginBottom: 'var(--space-3)',
        fontSize: '0.85rem',
        color: 'var(--color-warning-text, #856404)',
      }}>
        No owner registered. High-value transactions cannot use APPROVAL policy.
      </div>
      <Button size="sm" onClick={() => { showOwnerManage.value = !showOwnerManage.value; }}>
        {showOwnerManage.value ? 'Hide Registration' : 'Register Owner'}
      </Button>
    </div>
  ) : (
    // 등록됨: 상태 요약 + Manage 버튼
    <div>
      <div class="detail-grid" style={{ marginBottom: 'var(--space-3)' }}>
        <DetailRow label="State">
          <Badge variant={ownerStateBadge(wallet.value.ownerState)}>
            {wallet.value.ownerState}
          </Badge>
        </DetailRow>
        <DetailRow label="Approval Method">
          {wallet.value.approvalMethod
            ? APPROVAL_OPTIONS.find(o => o.value === wallet.value!.approvalMethod)?.label ?? wallet.value.approvalMethod
            : 'Auto (Global Fallback)'}
        </DetailRow>
        <DetailRow label="Address">
          {wallet.value.ownerAddress ? formatAddress(wallet.value.ownerAddress) : 'Not set'}
          {wallet.value.ownerAddress && <CopyButton value={wallet.value.ownerAddress} />}
        </DetailRow>
      </div>
      <Button size="sm" variant="secondary" onClick={() => { showOwnerManage.value = !showOwnerManage.value; }}>
        {showOwnerManage.value ? 'Hide Details' : 'Manage'}
      </Button>
    </div>
  )}

  {/* 인라인 확장: 기존 OwnerTab 전체 UI */}
  {showOwnerManage.value && (
    <div style={{ marginTop: 'var(--space-4)', paddingTop: 'var(--space-3)', borderTop: '1px solid var(--color-border)' }}>
      <OwnerTab />
    </div>
  )}
</div>
```

4. 기존 OwnerTab 함수는 그대로 유지 (Overview 카드에서 인라인으로 렌더링하므로). 다만 tab 렌더링에서 `{activeDetailTab.value === 'owner' && <OwnerTab />}` 줄은 제거한다.

5. 테스트 관련: wallets.test.tsx의 `Owner` 탭 참조를 업데이트해야 함 -- Task 2에서 처리.

중요: `ownerStateBadge` 함수와 `APPROVAL_OPTIONS` 배열, `formatAddress`, `CopyButton`, `DetailRow`, `Badge`는 이미 wallets.tsx 내에 존재하므로 추가 import 불요.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin exec -- npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>DETAIL_TABS가 4개(overview/activity/assets/setup)로 변경되고, OverviewTab에 Owner Protection 카드가 렌더링되며, Activity/Assets/Setup 스텁 함수가 존재하여 빌드가 성공한다</done>
</task>

<task type="auto">
  <name>Task 2: 테스트 업데이트</name>
  <files>packages/admin/src/__tests__/wallets.test.tsx</files>
  <action>
wallets.test.tsx에서 기존 8탭 참조를 4탭으로 업데이트:

1. `expect(screen.getByText('Owner')).toBeTruthy()` -- Owner는 독립 탭이 아니라 Overview 내 카드로 변경. 이 assertion을 제거하거나 `expect(screen.getByText('Activity')).toBeTruthy()`로 교체.

2. `expect(screen.getByText('MCP')).toBeTruthy()` -- MCP는 Setup 탭 내부로 이동. `expect(screen.getByText('Setup')).toBeTruthy()`로 교체.

3. `fireEvent.click(screen.getByText('Owner'))` -- Owner 탭 클릭 테스트를 Owner Protection 카드 테스트로 변경:
   - Overview 탭 (기본 활성)에서 "Owner Protection" 텍스트가 보이는지 확인
   - ownerState가 'NONE'인 mock의 경우 "Register Owner" 버튼이 보이는지 확인

4. `fireEvent.click(screen.getByText('MCP'))` + `expect(screen.getByText('MCP Setup')).toBeTruthy()` -- Setup 탭 클릭 후 MCP 관련 컨텐츠 확인으로 변경. 단, MCP 실제 통합은 Plan 02이므로 이 테스트는 Setup 탭 클릭이 동작하는지만 확인:
   ```typescript
   fireEvent.click(screen.getByText('Setup'));
   await waitFor(() => {
     expect(screen.getByText('Setup')).toBeTruthy();
   });
   ```

5. 'Transactions' 탭 참조가 있다면 'Activity'로 변경.

6. wallets-coverage.test.tsx나 wallets-nft.test.tsx에서도 DETAIL_TABS나 탭 이름 참조가 있으면 동일하게 업데이트.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin run test 2>&1 | tail -30</automated>
  </verify>
  <done>모든 wallets 관련 테스트가 4탭 구조에 맞게 업데이트되어 통과한다</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/admin exec -- npx tsc --noEmit` 타입 체크 통과
2. `pnpm --filter @waiaas/admin run test` 테스트 통과
3. DETAIL_TABS에 4개 탭만 존재 (grep으로 확인)
</verification>

<success_criteria>
- DETAIL_TABS = ['overview', 'activity', 'assets', 'setup'] 4개만 존재
- OverviewTab에 Owner Protection 카드가 인라인으로 렌더링됨
- Owner NONE 상태: 경고 배너 + Register Owner 버튼
- Owner GRACE/LOCKED 상태: 상태 요약 + Manage 버튼
- Register Owner/Manage 클릭 시 기존 OwnerTab UI가 인라인 토글
- ActivityTab/AssetsTab/SetupTab 스텁 존재 (빌드 통과)
- 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/420-wallet-detail-tabs/420-01-SUMMARY.md`
</output>
