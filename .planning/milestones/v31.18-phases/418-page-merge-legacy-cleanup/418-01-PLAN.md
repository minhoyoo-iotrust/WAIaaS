---
phase: 418-page-merge-legacy-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/wallets.tsx
  - packages/admin/src/pages/tokens.tsx
  - packages/admin/src/components/layout.tsx
autonomous: true
requirements: [MERG-01, MERG-02, MERG-05, LGCY-02, ROUT-01]

must_haves:
  truths:
    - "Wallets 페이지에서 Wallets/Tokens/RPC Endpoints/WalletConnect 4개 탭을 전환할 수 있다"
    - "Tokens 탭에서 기존 TokensPage의 모든 기능(네트워크 선택, 토큰 목록, 추가/삭제)이 동작한다"
    - "#/tokens 접근 시 #/wallets로 리다이렉트되고 Tokens 탭이 활성화된다"
  artifacts:
    - path: "packages/admin/src/pages/wallets.tsx"
      provides: "TokensTab 컴포넌트 + WALLETS_TABS에 tokens 항목 추가"
      contains: "tokens"
    - path: "packages/admin/src/pages/tokens.tsx"
      provides: "default export 제거, TokensContent export만 유지"
  key_links:
    - from: "packages/admin/src/pages/wallets.tsx"
      to: "packages/admin/src/pages/tokens.tsx"
      via: "import { TokensContent }"
      pattern: "TokensContent"
    - from: "packages/admin/src/components/layout.tsx"
      to: "packages/admin/src/pages/wallets.tsx"
      via: "/tokens route redirect"
      pattern: "tokens.*wallets"
---

<objective>
Tokens 독립 페이지를 Wallets 페이지의 "Tokens" 탭으로 병합하고, walletconnect.tsx의 default export를 제거한다.

Purpose: 관련 기능(Wallets/Tokens/RPC/WalletConnect)을 하나의 페이지 내 탭으로 통합하여 정보 구조 개선
Output: Wallets 페이지 4탭 구성, tokens.tsx default export 제거, /tokens 리다이렉트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/417-sidebar-renaming-routes/417-01-SUMMARY.md

<interfaces>
<!-- wallets.tsx: 기존 WALLETS_TABS 및 탭 구조 -->
From packages/admin/src/pages/wallets.tsx (line 3263-3293):
```typescript
const WALLETS_TABS = [
  { key: 'wallets', label: 'Wallets' },
  { key: 'rpc', label: 'RPC Endpoints' },
  { key: 'walletconnect', label: 'WalletConnect' },
];

function WalletListWithTabs() {
  const activeTab = useSignal('wallets');
  // ... pendingNavigation handling
  return (
    <div class="page">
      <Breadcrumb pageName="Wallets" tabName={...} onPageClick={() => { activeTab.value = 'wallets'; }} />
      <TabNav tabs={WALLETS_TABS} activeTab={activeTab.value} onTabChange={(k) => { activeTab.value = k; }} />
      {activeTab.value === 'wallets' && <WalletListContent />}
      {activeTab.value === 'rpc' && <RpcEndpointsTab />}
      {activeTab.value === 'walletconnect' && <WalletConnectTab />}
    </div>
  );
}
```

From packages/admin/src/pages/tokens.tsx (line 37):
```typescript
export default function TokensPage() { ... }
```

From packages/admin/src/pages/walletconnect.tsx (line 50+):
```typescript
export default function WalletConnectPage() { ... }
// wallets.tsx already has WalletConnectTab() that duplicates this functionality
```

From packages/admin/src/components/layout.tsx (line 146, 171-173):
```typescript
else if (path === '/tokens') page = <TokensPage />;
// ...
else if (path === '/walletconnect') {
  window.location.hash = '#/wallets';
  page = <WalletsPage />;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokens 탭 병합 + Wallets 4탭 구성</name>
  <files>packages/admin/src/pages/tokens.tsx, packages/admin/src/pages/wallets.tsx, packages/admin/src/pages/walletconnect.tsx</files>
  <action>
1. **tokens.tsx**: `export default function TokensPage()` 를 제거하고, 내부 컴포넌트를 `export function TokensContent()` 로 리네이밍하여 named export로 변경한다. 기존 내부 로직(네트워크 선택, 토큰 목록, 추가/삭제)은 그대로 유지. TokensPage 호출부가 있다면 TokensContent 래퍼로 교체.

2. **wallets.tsx**:
   - `import { TokensContent } from './tokens';` 추가
   - WALLETS_TABS 배열을 4개 탭으로 변경 (wallets 다음에 tokens 추가):
     ```typescript
     const WALLETS_TABS = [
       { key: 'wallets', label: 'Wallets' },
       { key: 'tokens', label: 'Tokens' },
       { key: 'rpc', label: 'RPC Endpoints' },
       { key: 'walletconnect', label: 'WalletConnect' },
     ];
     ```
   - WalletListWithTabs 내에 `{activeTab.value === 'tokens' && <TokensContent />}` 렌더링 추가

3. **walletconnect.tsx**: `export default function WalletConnectPage()` 를 제거. 이미 wallets.tsx에 WalletConnectTab이 있으므로 독립 페이지 export만 제거. 파일 자체는 유지하되 default export만 삭제 (내부에 WalletConnectPage 래핑만 제거). 다른 곳에서 import하지 않는다면 파일 전체 삭제 가능.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/admin</automated>
  </verify>
  <done>WALLETS_TABS가 4개 탭(wallets/tokens/rpc/walletconnect)이고, TokensContent가 Wallets 페이지 내 탭으로 렌더링되며, tokens.tsx에 default export가 없다</done>
</task>

<task type="auto">
  <name>Task 2: /tokens 리다이렉트 + layout.tsx 정리</name>
  <files>packages/admin/src/components/layout.tsx</files>
  <action>
1. **layout.tsx PageRouter**: `/tokens` 경로 처리를 리다이렉트로 변경:
   - 기존: `else if (path === '/tokens') page = <TokensPage />;`
   - 변경: `else if (path === '/tokens') { window.location.hash = '#/wallets'; page = <WalletsPage />; }` (기존 /walletconnect 리다이렉트 패턴과 동일)
   - 추가로 wallets.tsx의 WalletListWithTabs에서 URL이 /tokens로 왔을 때 activeTab을 'tokens'로 설정하는 로직 필요 — `useEffect`에서 `currentPath.value`가 '/tokens'일 때 activeTab을 'tokens'으로 세팅 후 hash 변경 (또는 hash에 tab query를 포함하지 않고, /wallets 리다이렉트 시 pendingNavigation으로 tab을 전달)

2. **layout.tsx imports**: `import TokensPage from '../pages/tokens';` 제거 (TokensPage default export가 없으므로). walletconnect.tsx default export도 제거했다면 해당 import도 제거 (현재 layout.tsx에 WalletConnectPage import는 없으므로 확인만).

3. **PAGE_TITLES/PAGE_SUBTITLES**: `/tokens` 항목 제거 (더 이상 독립 페이지가 아님). `/rpc-proxy` 항목도 유지 (Plan 02에서 처리).

4. **NAV_SECTIONS**: /tokens 항목이 있다면 제거 (현재 NAV_SECTIONS에 /tokens는 없으므로 확인만).

5. **/tokens 리다이렉트 시 Tokens 탭 활성화**: wallets.tsx의 WalletListWithTabs에서, 리다이렉트로 도착했을 때 tokens 탭이 활성화되도록 처리. 방법: layout.tsx에서 `/tokens` 리다이렉트 전에 `pendingNavigation.value = { page: '/wallets', tab: 'tokens', fieldName: '' };` 설정. 이미 pendingNavigation 패턴이 사용되고 있으므로 동일하게 적용.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/admin && pnpm turbo run test --filter=@waiaas/admin</automated>
  </verify>
  <done>#/tokens로 접근하면 #/wallets로 리다이렉트되고 Tokens 탭이 활성화되며, layout.tsx에서 TokensPage import가 제거되었고, 모든 테스트 통과</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/admin` — 타입 에러 없음
2. `pnpm turbo run test --filter=@waiaas/admin` — 모든 테스트 통과
3. WALLETS_TABS 배열에 4개 탭 존재 확인
4. tokens.tsx에 `export default` 없음 확인
</verification>

<success_criteria>
- Wallets 페이지에서 Wallets/Tokens/RPC Endpoints/WalletConnect 4개 탭 전환 가능
- Tokens 탭에서 기존 토큰 레지스트리 기능 모두 동작
- #/tokens 접근 시 #/wallets로 리다이렉트 + Tokens 탭 활성
- tokens.tsx에 default export 없음
- typecheck + test 통과
</success_criteria>

<output>
After completion, create `.planning/phases/418-page-merge-legacy-cleanup/418-01-SUMMARY.md`
</output>
