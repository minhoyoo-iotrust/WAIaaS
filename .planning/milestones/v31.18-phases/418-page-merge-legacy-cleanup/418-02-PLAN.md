---
phase: 418-page-merge-legacy-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["418-01"]
files_modified:
  - packages/admin/src/pages/system.tsx
  - packages/admin/src/pages/rpc-proxy.tsx
  - packages/admin/src/pages/telegram-users.tsx
  - packages/admin/src/components/layout.tsx
  - skills/admin.skill.md
autonomous: true
requirements: [MERG-03, MERG-04, MERG-06, LGCY-01, LGCY-03, LGCY-04, ROUT-02]

must_haves:
  truths:
    - "Settings 페이지에서 General/API Keys/RPC Proxy 3개 탭을 전환할 수 있다"
    - "RPC Proxy 탭에서 기존 RpcProxyPage의 모든 기능(설정, 감사로그)이 동작한다"
    - "#/rpc-proxy 접근 시 #/settings로 리다이렉트되고 RPC Proxy 탭이 활성화된다"
    - "telegram-users.tsx의 default export가 제거되어 있다"
    - "layout.tsx에서 삭제된 페이지의 import가 모두 제거되어 있다"
    - "skills/admin.skill.md가 새 메뉴 구조를 반영한다"
  artifacts:
    - path: "packages/admin/src/pages/system.tsx"
      provides: "TabNav 3탭(General/API Keys/RPC Proxy) 구조"
      contains: "SETTINGS_TABS"
    - path: "packages/admin/src/pages/rpc-proxy.tsx"
      provides: "default export 제거, RpcProxyContent named export"
    - path: "skills/admin.skill.md"
      provides: "새 메뉴 구조(섹션 그룹, 병합된 탭) 반영"
  key_links:
    - from: "packages/admin/src/pages/system.tsx"
      to: "packages/admin/src/pages/rpc-proxy.tsx"
      via: "import { RpcProxyContent }"
      pattern: "RpcProxyContent"
    - from: "packages/admin/src/components/layout.tsx"
      to: "packages/admin/src/pages/system.tsx"
      via: "/rpc-proxy route redirect"
      pattern: "rpc-proxy.*settings"
---

<objective>
RPC Proxy 독립 페이지를 Settings 페이지의 탭으로 병합하고, 레거시 파일 정리 및 스킬 파일을 업데이트한다.

Purpose: Settings 페이지를 3탭(General/API Keys/RPC Proxy)으로 구성하고, 불필요한 독립 페이지 export를 제거하여 정보 구조 완성
Output: Settings 3탭, rpc-proxy.tsx/telegram-users.tsx default export 제거, layout.tsx import 정리, 스킬 파일 업데이트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/417-sidebar-renaming-routes/417-01-SUMMARY.md
@.planning/phases/418-page-merge-legacy-cleanup/418-01-PLAN.md

<interfaces>
<!-- system.tsx: 현재 flat 구조 (TabNav 없음) -->
From packages/admin/src/pages/system.tsx:
```typescript
export default function SystemPage() {
  // Settings state, API Keys state, Shutdown state
  // Sections: NftIndexer, Oracle, DisplaySettings, GlobalRateLimit, LogLevel, GasCondition, SmartAccount, Erc8128
  // + API Keys section (inline, not separate tab)
  // + Danger Zone (Shutdown)
}
```
SystemPage는 현재 탭 없이 flat 구조. API Keys 섹션이 인라인으로 포함됨.
General = 기존 settings 섹션들 (NftIndexer~Erc8128 + Danger Zone)
API Keys = 기존 ApiKeysSection
RPC Proxy = rpc-proxy.tsx에서 가져올 콘텐츠

From packages/admin/src/pages/rpc-proxy.tsx:
```typescript
export default function RpcProxyPage() { ... }
// 319 lines, settings + audit logs for RPC proxy
```

From packages/admin/src/pages/telegram-users.tsx:
```typescript
export function TelegramUsersContent() { return <TelegramUsersInner />; }
export default function TelegramUsersPage() { return <TelegramUsersInner />; }
// TelegramUsersContent already exists as named export
// default export (TelegramUsersPage) needs removal
```

From packages/admin/src/components/layout.tsx imports:
```typescript
import TokensPage from '../pages/tokens';        // Plan 01에서 제거됨
import RpcProxyPage from '../pages/rpc-proxy';    // 이 plan에서 제거
// telegram-users는 직접 import 없음 (layout에서 redirect만)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings 3탭 구조 + RPC Proxy 병합</name>
  <files>packages/admin/src/pages/system.tsx, packages/admin/src/pages/rpc-proxy.tsx</files>
  <action>
1. **rpc-proxy.tsx**: `export default function RpcProxyPage()` 를 `export function RpcProxyContent()` 로 변경 (named export로 전환, default export 제거). 내부 로직은 그대로 유지.

2. **system.tsx**:
   - `import { RpcProxyContent } from './rpc-proxy';` 추가
   - `import { TabNav } from '../components/tab-nav';` 추가
   - `import { Breadcrumb } from '../components/breadcrumb';` 추가
   - `import { useSignal } from '@preact/signals';` 는 이미 있음
   - `import { pendingNavigation, highlightField } from '../components/settings-search';` 가 이미 있는지 확인하고 없으면 추가

   - SETTINGS_TABS 상수 정의:
     ```typescript
     const SETTINGS_TABS = [
       { key: 'general', label: 'General' },
       { key: 'api-keys', label: 'API Keys' },
       { key: 'rpc-proxy', label: 'RPC Proxy' },
     ];
     ```

   - SystemPage를 TabNav 기반으로 리팩토링:
     - 기존 인라인 섹션들을 GeneralTab 내부 함수로 그룹화 (NftIndexer~Erc8128 + Danger Zone + save bar)
     - ApiKeysSection을 ApiKeysTab으로 분리 (기존 ApiKeysSection 로직을 그대로 사용)
     - RPC Proxy 탭은 `<RpcProxyContent />`를 렌더
     - 최상위 return:
       ```tsx
       <div class="page">
         <Breadcrumb pageName="Settings" tabName={SETTINGS_TABS.find(t => t.key === activeTab.value)?.label ?? ''} onPageClick={() => { activeTab.value = 'general'; }} />
         <TabNav tabs={SETTINGS_TABS} activeTab={activeTab.value} onTabChange={(k) => { activeTab.value = k; }} />
         {activeTab.value === 'general' && <GeneralTab />}
         {activeTab.value === 'api-keys' && <ApiKeysTab />}
         {activeTab.value === 'rpc-proxy' && <RpcProxyContent />}
       </div>
       ```

   - 주의: GeneralTab에는 기존 settings/dirty/saving/loading 상태와 fetchSettings, handleSave, handleDiscard, handleFieldChange 등 모든 로직이 포함되어야 함. API Keys 상태(apiKeys, apiKeysLoading 등)는 ApiKeysTab에서 자체 관리.
   - 주의: GeneralTab과 ApiKeysTab은 SystemPage 내부 함수 컴포넌트로 유지 (기존 패턴 유지). state가 SystemPage 스코프에 있으므로 클로저로 접근 가능.
   - pendingNavigation 처리: useEffect에서 nav.tab이 있을 때 activeTab 변경 (wallets.tsx 패턴과 동일)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/admin</automated>
  </verify>
  <done>Settings 페이지가 General/API Keys/RPC Proxy 3탭으로 구성되고, rpc-proxy.tsx에 default export가 없으며, typecheck 통과</done>
</task>

<task type="auto">
  <name>Task 2: 레거시 정리 + layout import 제거 + 리다이렉트 + 스킬 파일</name>
  <files>packages/admin/src/pages/telegram-users.tsx, packages/admin/src/components/layout.tsx, skills/admin.skill.md</files>
  <action>
1. **telegram-users.tsx**: `export default function TelegramUsersPage()` 제거. `export function TelegramUsersContent()` named export만 유지. TelegramUsersContent는 이미 notifications.tsx에서 사용 중이므로 그대로.

2. **layout.tsx**:
   - `import RpcProxyPage from '../pages/rpc-proxy';` 제거
   - `import TelegramUsersPage from '../pages/telegram-users';` 가 있다면 제거 (현재 layout에서는 직접 import 없이 redirect만 하므로 확인)
   - PageRouter에서 `/rpc-proxy` 경로를 리다이렉트로 변경:
     ```typescript
     else if (path === '/rpc-proxy') {
       pendingNavigation.value = { page: '/settings', tab: 'rpc-proxy', fieldName: '' };
       window.location.hash = '#/settings';
       page = <SystemPage />;
     }
     ```
   - PAGE_TITLES에서 `/tokens` 항목 제거 (Plan 01에서 리다이렉트 처리됨)
   - PAGE_TITLES에서 `/rpc-proxy` 항목 제거
   - PAGE_SUBTITLES에서 `/tokens`, `/rpc-proxy` 항목 제거

3. **skills/admin.skill.md**: 새 메뉴 구조를 반영하여 업데이트:
   - 사이드바 섹션 구조 (Wallets/Trading/Security/Channels/System) 설명
   - Wallets 페이지: 4탭 (Wallets/Tokens/RPC Endpoints/WalletConnect)
   - Settings 페이지: 3탭 (General/API Keys/RPC Proxy)
   - 이전 독립 페이지(Token Registry, RPC Proxy)가 탭으로 통합된 내용 반영
   - 메뉴 리네이밍 반영 (DeFi->Providers, Security->Protection, System->Settings, Human Wallet Apps->Wallet Apps)
   - 기존 기능 설명은 유지하되 메뉴 경로와 이름만 업데이트
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/admin && pnpm turbo run test --filter=@waiaas/admin</automated>
  </verify>
  <done>#/rpc-proxy 접근 시 #/settings로 리다이렉트 + RPC Proxy 탭 활성, telegram-users.tsx default export 제거, layout.tsx에서 삭제된 페이지 import 제거, admin.skill.md가 새 구조 반영, typecheck+test 통과</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/admin` — 타입 에러 없음
2. `pnpm turbo run test --filter=@waiaas/admin` — 모든 테스트 통과
3. Settings 페이지에 SETTINGS_TABS 3개 탭 존재 확인
4. rpc-proxy.tsx, telegram-users.tsx에 `export default` 없음 확인
5. layout.tsx에 TokensPage, RpcProxyPage import 없음 확인
6. admin.skill.md에 새 메뉴 구조 반영 확인
</verification>

<success_criteria>
- Settings 페이지에서 General/API Keys/RPC Proxy 3개 탭 전환 가능
- RPC Proxy 탭에서 기존 설정 및 감사 로그 기능 동작
- #/rpc-proxy 접근 시 #/settings로 리다이렉트 + RPC Proxy 탭 활성
- telegram-users.tsx default export 제거됨
- layout.tsx에서 삭제된 페이지 import 모두 제거됨
- skills/admin.skill.md가 새 메뉴 구조 반영
- typecheck + test 통과
</success_criteria>

<output>
After completion, create `.planning/phases/418-page-merge-legacy-cleanup/418-02-SUMMARY.md`
</output>
