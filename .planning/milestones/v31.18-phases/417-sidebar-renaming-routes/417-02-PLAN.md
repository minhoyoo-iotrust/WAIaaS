---
phase: 417-sidebar-renaming-routes
plan: 02
type: execute
wave: 2
depends_on:
  - 417-01
files_modified:
  - packages/admin/src/pages/hyperliquid.tsx
  - packages/admin/src/pages/polymarket.tsx
  - packages/admin/src/pages/transactions.tsx
  - packages/admin/src/pages/policies.tsx
autonomous: true
requirements:
  - TNAV-01
  - TNAV-02
  - TNAV-03
  - TNAV-04

must_haves:
  truths:
    - "Hyperliquid 페이지가 TabNav 공통 컴포넌트를 사용한다"
    - "Polymarket 페이지가 TabNav 공통 컴포넌트를 사용한다"
    - "Transactions 페이지의 첫 번째 탭 라벨이 History이다"
    - "Policies 페이지의 첫 번째 탭 라벨이 Rules이다"
  artifacts:
    - path: "packages/admin/src/pages/hyperliquid.tsx"
      provides: "TabNav 기반 탭 렌더링"
      contains: "TabNav"
    - path: "packages/admin/src/pages/polymarket.tsx"
      provides: "TabNav 기반 탭 렌더링"
      contains: "TabNav"
    - path: "packages/admin/src/pages/transactions.tsx"
      provides: "History 탭 라벨"
      contains: "History"
    - path: "packages/admin/src/pages/policies.tsx"
      provides: "Rules 탭 라벨"
      contains: "Rules"
  key_links:
    - from: "packages/admin/src/pages/hyperliquid.tsx"
      to: "packages/admin/src/components/tab-nav.tsx"
      via: "import TabNav"
      pattern: "import.*TabNav.*tab-nav"
    - from: "packages/admin/src/pages/polymarket.tsx"
      to: "packages/admin/src/components/tab-nav.tsx"
      via: "import TabNav"
      pattern: "import.*TabNav.*tab-nav"
---

<objective>
Hyperliquid/Polymarket 페이지의 커스텀 탭 바를 TabNav 공통 컴포넌트로 교체하고, Transactions의 "All Transactions" 탭을 "History"로, Policies의 "Policies" 탭을 "Rules"로 라벨 변경한다.

Purpose: 탭 UI의 일관성 확보 (공통 TabNav 사용) 및 직관적 라벨로 변경.
Output: TabNav 통일 + 라벨 변경된 4개 페이지 파일
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/admin/src/components/tab-nav.tsx
@packages/admin/src/pages/hyperliquid.tsx
@packages/admin/src/pages/polymarket.tsx
@packages/admin/src/pages/transactions.tsx
@packages/admin/src/pages/policies.tsx
</context>

<interfaces>
<!-- TabNav 공통 컴포넌트 인터페이스 -->
From packages/admin/src/components/tab-nav.tsx:
```typescript
export interface TabItem {
  key: string;
  label: string;
}
export interface TabNavProps {
  tabs: TabItem[];
  activeTab: string;
  onTabChange: (key: string) => void;
}
export function TabNav({ tabs, activeTab, onTabChange }: TabNavProps): JSX.Element;
```

현재 Hyperliquid/Polymarket는 커스텀 인라인 tabStyle/tabButton 스타일로 탭을 직접 렌더링.
현재 Transactions/Policies는 이미 TabNav를 사용 중 -- 라벨만 변경.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hyperliquid + Polymarket TabNav 전환</name>
  <files>packages/admin/src/pages/hyperliquid.tsx, packages/admin/src/pages/polymarket.tsx</files>
  <action>
1. **hyperliquid.tsx 수정**:
   - `import { TabNav } from '../components/tab-nav';` 추가
   - 커스텀 `tabStyle`, `tabButton` 상수 삭제
   - 탭 정의를 TabItem[] 상수로 추출 (Settings 탭은 Phase 419에서 제거 예정이므로 이번에는 유지):
     ```typescript
     const HYPERLIQUID_TABS: TabItem[] = [
       { key: 'overview', label: 'Overview' },
       { key: 'orders', label: 'Orders' },
       { key: 'spot', label: 'Spot' },
       { key: 'subaccounts', label: 'Sub-accounts' },
       { key: 'settings', label: 'Settings' },
     ];
     ```
   - 기존 `<div style={tabStyle}>...<button style={tabButton(...)}>...</div>` 블록을 `<TabNav tabs={HYPERLIQUID_TABS} activeTab={activeTab.value} onTabChange={(k) => { activeTab.value = k as Tab; if (k === 'subaccounts') selectedSubAccount.value = null; }} />` 로 교체
   - subaccounts 탭 전환 시 selectedSubAccount.value = null 리셋 로직 유지 (onTabChange 핸들러에서 처리)

2. **polymarket.tsx 수정**:
   - `import { TabNav } from '../components/tab-nav';` 추가
   - 커스텀 `tabStyle`, `tabButton` 상수 삭제
   - 탭 정의:
     ```typescript
     const POLYMARKET_TABS: TabItem[] = [
       { key: 'overview', label: 'Overview' },
       { key: 'markets', label: 'Markets' },
       { key: 'orders', label: 'Orders' },
       { key: 'positions', label: 'Positions' },
       { key: 'settings', label: 'Settings' },
     ];
     ```
   - 기존 커스텀 탭 바를 `<TabNav tabs={POLYMARKET_TABS} activeTab={activeTab.value} onTabChange={(k) => { activeTab.value = k as Tab; }} />` 로 교체

두 파일 모두: TabNav가 hasDirty + showUnsavedDialog를 내장하므로 커스텀 탭에서 별도 처리하던 게 없다면(현재 없음) 그대로 교체.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin exec -- npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - Hyperliquid 페이지에서 TabNav import 사용, 커스텀 tabStyle/tabButton 삭제됨
  - Polymarket 페이지에서 TabNav import 사용, 커스텀 tabStyle/tabButton 삭제됨
  - 두 페이지 모두 기존과 동일한 탭(5개)이 TabNav로 렌더링됨
  - TypeScript 컴파일 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: Transactions "History" + Policies "Rules" 탭 라벨 변경</name>
  <files>packages/admin/src/pages/transactions.tsx, packages/admin/src/pages/policies.tsx</files>
  <action>
1. **transactions.tsx 수정**:
   - TABS 배열에서 `{ key: 'transactions', label: 'All Transactions' }` -> `{ key: 'transactions', label: 'History' }` 로 변경
   - 나머지 로직(TabNav 사용, activeTab, 렌더링) 변경 없음

2. **policies.tsx 수정**:
   - POLICIES_TABS 배열에서 `{ key: 'policies', label: 'Policies' }` -> `{ key: 'policies', label: 'Rules' }` 로 변경
   - 나머지 로직 변경 없음

**주의**: key 값은 변경하지 않는다 (key는 내부 식별자로 사용되며, URL 파라미터나 다른 코드에서 참조될 수 있음). label만 변경한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin exec -- npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - Transactions 탭 라벨이 "History"로 표시됨
  - Policies 탭 라벨이 "Rules"로 표시됨
  - key 값은 변경되지 않아 기존 동작에 영향 없음
  - TypeScript 컴파일 에러 없음
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/admin exec -- npx tsc --noEmit` -- TypeScript 컴파일 성공
2. `pnpm --filter @waiaas/admin test` -- 기존 테스트 통과
3. hyperliquid.tsx에 `import { TabNav }` 가 있고 커스텀 tabStyle이 없는지 확인
4. polymarket.tsx에 `import { TabNav }` 가 있고 커스텀 tabStyle이 없는지 확인
5. transactions.tsx TABS에서 'History' 라벨 확인
6. policies.tsx POLICIES_TABS에서 'Rules' 라벨 확인
</verification>

<success_criteria>
- Hyperliquid/Polymarket 페이지가 TabNav 공통 컴포넌트 사용
- Transactions 첫 번째 탭이 "History", Policies 첫 번째 탭이 "Rules"
- 커스텀 탭 스타일 코드(tabStyle, tabButton)가 제거됨
- 기존 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/417-sidebar-renaming-routes/417-02-SUMMARY.md`
</output>
