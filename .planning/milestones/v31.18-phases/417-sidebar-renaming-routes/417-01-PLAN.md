---
phase: 417-sidebar-renaming-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/components/layout.tsx
  - packages/admin/src/utils/settings-search-index.ts
  - packages/admin/src/components/settings-search.tsx
autonomous: true
requirements:
  - SIDE-01
  - SIDE-02
  - SIDE-03
  - SIDE-04
  - SIDE-05
  - SIDE-06
  - SIDE-07
  - SIDE-08
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
  - ROUT-05
  - ROUT-06
  - ROUT-07

must_haves:
  truths:
    - "사이드바에 Wallets/Trading/Security/Channels/System 5개 섹션 헤더가 보이고 각 섹션 아래에 관련 메뉴가 그룹화되어 있다"
    - "Dashboard는 섹션 밖 최상단에 위치한다"
    - "DeFi가 Providers로, Security가 Protection으로, System이 Settings로, Human Wallet Apps가 Wallet Apps로 표시된다"
    - "이전 경로(#/defi, #/security, #/system, #/tokens, #/rpc-proxy, #/actions)로 접근하면 새 경로로 리다이렉트된다"
    - "기존 리다이렉트(erc8004, incoming, walletconnect, telegram-users, settings)가 유지된다"
    - "Ctrl+K 검색에서 변경된 페이지명과 경로가 반영된다"
  artifacts:
    - path: "packages/admin/src/components/layout.tsx"
      provides: "섹션 그룹 사이드바 렌더링 + 리네이밍된 PAGE_TITLES/PAGE_SUBTITLES + 리다이렉트 라우터"
      contains: "NAV_SECTIONS"
    - path: "packages/admin/src/utils/settings-search-index.ts"
      provides: "변경된 경로/페이지명이 반영된 검색 인덱스"
      contains: "/providers"
    - path: "packages/admin/src/components/settings-search.tsx"
      provides: "변경된 PAGE_LABELS"
      contains: "/protection"
  key_links:
    - from: "packages/admin/src/components/layout.tsx"
      to: "NAV_SECTIONS"
      via: "sidebar render loop"
      pattern: "NAV_SECTIONS\\.map"
    - from: "packages/admin/src/components/settings-search.tsx"
      to: "packages/admin/src/utils/settings-search-index.ts"
      via: "import SETTINGS_SEARCH_INDEX"
      pattern: "PAGE_LABELS.*protection"
---

<objective>
NAV_ITEMS를 섹션 그룹 구조(NAV_SECTIONS)로 전환하고, 사이드바를 섹션 헤더+메뉴 형태로 렌더링하며, 메뉴 리네이밍(DeFi->Providers, Security->Protection, System->Settings, Human Wallet Apps->Wallet Apps)과 라우트 리다이렉트를 구현한다. Ctrl+K 검색의 PAGE_LABELS와 SETTINGS_SEARCH_INDEX도 변경된 이름/경로에 맞게 업데이트한다.

Purpose: Admin UI의 17개 플랫 메뉴를 5개 논리 섹션으로 그룹화하여 정보 탐색성을 개선한다.
Output: 섹션 그룹 사이드바 + 리네이밍 + 리다이렉트 + 검색 반영이 완료된 layout.tsx, settings-search-index.ts, settings-search.tsx
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/admin/src/components/layout.tsx
@packages/admin/src/utils/settings-search-index.ts
@packages/admin/src/components/settings-search.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/admin/src/components/tab-nav.tsx:
```typescript
export interface TabItem {
  key: string;
  label: string;
}
export interface TabNavProps {
  tabs: TabItem[];
  activeTab: string;
  onTabChange: (key: string) => void;
}
export function TabNav({ tabs, activeTab, onTabChange }: TabNavProps): JSX.Element;
```

From packages/admin/src/components/layout.tsx (current):
```typescript
// 현재 NAV_ITEMS: { path: string; label: string }[]
// 현재 PAGE_TITLES: Record<string, string>
// 현재 PAGE_SUBTITLES: Record<string, string>
export const currentPath: Signal<string>;
export function getPageSubtitle(path: string): string | undefined;
export function Layout(): JSX.Element;
```

From packages/admin/src/utils/settings-search-index.ts:
```typescript
export interface SearchIndexEntry {
  id: string; label: string; description: string;
  page: string; tab: string; fieldName: string; keywords: string[];
}
export const SETTINGS_SEARCH_INDEX: SearchIndexEntry[];
```

From packages/admin/src/components/settings-search.tsx:
```typescript
// PAGE_LABELS: Record<string, string> — used for breadcrumb display in Ctrl+K search
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: NAV_SECTIONS 구조 전환 + 사이드바 렌더링 + 리네이밍 + 라우트 리다이렉트</name>
  <files>packages/admin/src/components/layout.tsx</files>
  <action>
layout.tsx에서 다음을 수행한다:

1. **NAV_ITEMS -> NAV_SECTIONS 전환**: 기존 flat `NAV_ITEMS` 배열을 아래 구조로 교체한다:
```typescript
interface NavItem { path: string; label: string; }
interface NavSection { section: string; items: NavItem[]; }

// Dashboard는 섹션 밖 독립 항목
const DASHBOARD_NAV: NavItem = { path: '/dashboard', label: 'Dashboard' };

const NAV_SECTIONS: NavSection[] = [
  {
    section: 'Wallets',
    items: [
      { path: '/wallets', label: 'Wallets' },
      { path: '/transactions', label: 'Transactions' },
      { path: '/sessions', label: 'Sessions' },
    ],
  },
  {
    section: 'Trading',
    items: [
      { path: '/providers', label: 'Providers' },     // was /defi, "DeFi"
      { path: '/hyperliquid', label: 'Hyperliquid' },
      { path: '/polymarket', label: 'Polymarket' },
    ],
  },
  {
    section: 'Security',
    items: [
      { path: '/policies', label: 'Policies' },
      { path: '/protection', label: 'Protection' },   // was /security, "Security"
      { path: '/agent-identity', label: 'Agent Identity' },
      { path: '/credentials', label: 'Credentials' },
    ],
  },
  {
    section: 'Channels',
    items: [
      { path: '/notifications', label: 'Notifications' },
      { path: '/wallet-apps', label: 'Wallet Apps' },  // was "Human Wallet Apps"
    ],
  },
  {
    section: 'System',
    items: [
      { path: '/settings', label: 'Settings' },       // was /system, "System"
      { path: '/audit-logs', label: 'Audit Logs' },
    ],
  },
];
```

2. **PAGE_TITLES 업데이트**: 기존 키/값 교체:
   - `/defi` -> `/providers`: 'Providers' (키+값 변경)
   - `/security` -> `/protection`: 'Protection' (키+값 변경)
   - `/system` -> `/settings`: 'Settings' (키+값 변경)
   - `/wallet-apps`: 값을 'Wallet Apps'로 변경 (키는 유지)
   - `/tokens`, `/rpc-proxy` 키는 삭제 (Phase 418에서 병합될 예정이므로 아직 유지 -- 실제로는 해당 페이지가 아직 존재하므로 유지해야 함. 단, NAV_SECTIONS에서는 제거)

3. **PAGE_SUBTITLES 업데이트**: PAGE_TITLES 변경에 맞춰 동일하게:
   - `/defi` -> `/providers`: 'Manage DeFi action providers and API keys'
   - `/security` -> `/protection`: 'Emergency controls and automatic protection rules'
   - `/system` -> `/settings`: 'API keys, display preferences, and daemon configuration'
   - `/wallet-apps`: 'Manage wallet apps for signing and notifications'
   - **중요**: `/tokens`, `/rpc-proxy` 는 아직 해당 페이지가 존재하므로 PAGE_TITLES/PAGE_SUBTITLES에서 유지한다 (사이드바에서만 제거)

4. **사이드바 렌더링 변경**: Layout 컴포넌트의 `<nav class="sidebar-nav">` 부분을 수정하여:
   - 먼저 DASHBOARD_NAV를 독립 `<a>` 링크로 렌더
   - 그 아래 NAV_SECTIONS.map으로 각 section에 대해:
     - `<div class="sidebar-section-header">{section.section}</div>` 렌더
     - section.items.map으로 기존과 같은 `<a>` 링크 렌더
   - 기존 hasDirty + showUnsavedDialog 로직 그대로 유지

5. **PageRouter 리다이렉트 추가/변경**:
   - `/defi` 를 현재 그대로 ActionsPage 렌더 (경로를 `/providers`로 변경 -- 즉 `/providers`에서 ActionsPage 렌더)
   - `/defi` 접근 시 `#/providers`로 리다이렉트 (기존 `/actions` -> `/defi` 리다이렉트를 `/actions` -> `/providers`로 변경)
   - `/security` 접근 시 `#/protection`으로 리다이렉트, `/protection`에서 SecurityPage 렌더
   - `/system` 접근 시 `#/settings`로 리다이렉트, `/settings`에서 SystemPage 렌더 (현재 `/settings`는 dashboard로 리다이렉트되는데, 이를 SystemPage로 변경)
   - `/tokens` 접근 시 아직 TokensPage 렌더 유지 (Phase 418에서 병합)
   - `/rpc-proxy` 접근 시 아직 RpcProxyPage 렌더 유지 (Phase 418에서 병합)
   - 기존 리다이렉트 유지: `/incoming`->`/transactions`, `/erc8004`->`/agent-identity`, `/walletconnect`->`/wallets`, `/telegram-users`->`/notifications`
   - `/wallet-apps`에서 HumanWalletAppsPage 렌더 (경로 변경 없음, 라벨만 변경)

6. **getPageTitle 함수**: `/wallets/`로 시작하는 경로 처리 유지. `/providers`, `/protection`, `/settings` 등 새 경로로 매핑되도록 PAGE_TITLES에 추가되었으므로 자동으로 동작.

**주의**: `sidebar-section-header` CSS 클래스는 인라인 스타일로 구현한다 (기존 Admin UI 패턴 -- CSS 파일 수정 없이 인라인으로):
```typescript
<div style={{
  fontSize: 'var(--font-size-xs)',
  fontWeight: 'var(--font-weight-semibold)',
  color: 'var(--color-text-muted)',
  textTransform: 'uppercase',
  letterSpacing: '0.05em',
  padding: 'var(--space-3) var(--space-4) var(--space-1)',
  marginTop: 'var(--space-2)',
}}>
  {section.section}
</div>
```
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin exec -- npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - NAV_SECTIONS가 5개 섹션(Wallets/Trading/Security/Channels/System)으로 정의됨
  - Dashboard가 섹션 밖 독립 최상단에 위치
  - DeFi->Providers, Security->Protection, System->Settings, Human Wallet Apps->Wallet Apps 리네이밍 완료
  - /defi->/providers, /security->/protection, /system->/settings 리다이렉트 동작
  - 기존 리다이렉트(erc8004, incoming, walletconnect, telegram-users) 유지
  - TypeScript 컴파일 에러 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: Ctrl+K 검색 인덱스 + PAGE_LABELS 업데이트</name>
  <files>packages/admin/src/utils/settings-search-index.ts, packages/admin/src/components/settings-search.tsx</files>
  <action>
1. **settings-search-index.ts 업데이트**:
   - `page: '/security'` -> `page: '/protection'` (AutoStop Rules 항목들, 약 6개)
   - `page: '/system'` -> `page: '/settings'` (System 페이지 항목들)
   - `page: '/wallet-apps'` 항목은 경로 변경 없으므로 유지
   - 검색 키워드에 이전 이름도 추가하여 하위 호환: security 항목에 `'protection'` 키워드 추가, system 항목에 `'settings'` 키워드 추가

2. **settings-search.tsx의 PAGE_LABELS 업데이트**:
   - `/security` -> `/protection` 키 변경, 값을 'Protection'으로 변경
   - `/system` -> `/settings` 키 변경, 값을 'Settings'로 변경
   - `/wallet-apps` 추가: 'Wallet Apps' (현재 없으면)
   - `/providers` 추가: 'Providers' (현재 없을 수 있으므로)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin exec -- npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - SETTINGS_SEARCH_INDEX의 page 필드가 /protection, /settings 등 새 경로를 사용
  - PAGE_LABELS가 Protection, Settings, Wallet Apps, Providers로 업데이트됨
  - Ctrl+K 검색에서 "protection" 검색 시 기존 Security 항목이 표시됨
  - TypeScript 컴파일 에러 없음
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/admin exec -- npx tsc --noEmit` -- TypeScript 컴파일 성공
2. `pnpm --filter @waiaas/admin test` -- 기존 테스트 통과 (settings-search 테스트 등)
3. layout.tsx에서 NAV_SECTIONS가 5개 섹션으로 정의되고 Dashboard가 섹션 밖에 있는지 확인
4. PageRouter에서 /defi->/providers, /security->/protection, /system->/settings 리다이렉트 경로 확인
</verification>

<success_criteria>
- 사이드바가 Dashboard(독립) + 5개 섹션 헤더(Wallets/Trading/Security/Channels/System)로 그룹화됨
- DeFi/Security/System/Human Wallet Apps 메뉴가 Providers/Protection/Settings/Wallet Apps로 리네이밍됨
- 이전 경로 접근 시 새 경로로 리다이렉트되고 기존 리다이렉트도 유지됨
- Ctrl+K 검색에 변경된 경로/이름이 반영됨
- TypeScript 컴파일 및 기존 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/417-sidebar-renaming-routes/417-01-SUMMARY.md`
</output>
