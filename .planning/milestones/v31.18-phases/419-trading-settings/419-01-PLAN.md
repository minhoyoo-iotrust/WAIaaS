---
phase: 419-trading-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/hyperliquid.tsx
  - packages/admin/src/pages/polymarket.tsx
  - packages/admin/src/pages/actions.tsx
  - packages/admin/src/components/hyperliquid/SettingsPanel.tsx
  - packages/admin/src/components/polymarket/PolymarketSettings.tsx
  - packages/admin/src/__tests__/hyperliquid.test.tsx
  - packages/admin/src/__tests__/polymarket.test.tsx
autonomous: true
requirements: [TRAD-01, TRAD-02, TRAD-03, TRAD-04, TRAD-05]

must_haves:
  truths:
    - "Hyperliquid 페이지에 Settings 탭이 없고 4개 탭(Overview/Orders/Spot/Sub-accounts)만 보인다"
    - "Polymarket 페이지에 Settings 탭이 없고 4개 탭(Overview/Markets/Orders/Positions)만 보인다"
    - "Hyperliquid/Polymarket 페이지 상단에 'Configure in Trading > Providers' 링크가 있고 클릭 시 #/providers 로 이동한다"
    - "Providers 페이지(actions.tsx)에서 Hyperliquid/Polymarket 설정을 모두 관리할 수 있다"
  artifacts:
    - path: "packages/admin/src/pages/hyperliquid.tsx"
      provides: "Settings 탭 제거 + Providers 링크"
      contains: "Configure in"
    - path: "packages/admin/src/pages/polymarket.tsx"
      provides: "Settings 탭 제거 + Providers 링크"
      contains: "Configure in"
    - path: "packages/admin/src/pages/actions.tsx"
      provides: "Hyperliquid/Polymarket 고급 설정 항목"
      contains: "hyperliquid"
  key_links:
    - from: "packages/admin/src/pages/hyperliquid.tsx"
      to: "#/providers"
      via: "Configure link at top"
      pattern: "providers"
    - from: "packages/admin/src/pages/actions.tsx"
      to: "PROVIDER_ADVANCED_SETTINGS"
      via: "hyperliquid/polymarket keys added"
      pattern: "hyperliquid.*network|polymarket.*fee"
---

<objective>
Hyperliquid/Polymarket 페이지에서 Settings 탭을 제거하고, 해당 설정을 Providers 페이지(actions.tsx)의 PROVIDER_ADVANCED_SETTINGS에 이관한다. 두 페이지 상단에 Providers 페이지로의 링크를 추가한다.

Purpose: Trading 페이지의 중복 설정 UI를 제거하고 Providers 페이지로 일원화
Output: Settings 탭 없는 4탭 구조 + Providers 페이지 설정 이관
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/admin/src/pages/hyperliquid.tsx
@packages/admin/src/pages/polymarket.tsx
@packages/admin/src/pages/actions.tsx
@packages/admin/src/components/hyperliquid/SettingsPanel.tsx
@packages/admin/src/components/polymarket/PolymarketSettings.tsx

<interfaces>
<!-- Providers 페이지(actions.tsx)의 PROVIDER_ADVANCED_SETTINGS 맵에 설정 키를 추가하면 자동으로 UI에 렌더링됨 -->

From packages/admin/src/pages/actions.tsx:
```typescript
const PROVIDER_ADVANCED_SETTINGS: Record<string, readonly string[]> = {
  jupiter_swap: ['jupiter_swap_default_slippage_bps', ...],
  drift: ['drift_max_leverage', ...],
  // hyperliquid, polymarket 키 추가 필요
};
```

From packages/admin/src/components/hyperliquid/SettingsPanel.tsx:
```typescript
const HL_KEYS = [
  { key: 'actions.hyperliquid_enabled', ... },           // enabled는 이미 Providers에서 토글
  { key: 'actions.hyperliquid_network', ... },            // 이관 대상
  { key: 'actions.hyperliquid_api_url', ... },            // 이관 대상
  { key: 'actions.hyperliquid_rate_limit_weight_per_min', ... },  // 이관 대상
  { key: 'actions.hyperliquid_default_leverage', ... },   // 이관 대상
  { key: 'actions.hyperliquid_default_margin_mode', ... },// 이관 대상
  { key: 'actions.hyperliquid_builder_address', ... },    // 이관 대상
  { key: 'actions.hyperliquid_builder_fee', ... },        // 이관 대상
  { key: 'actions.hyperliquid_order_status_poll_interval_ms', ... }, // 이관 대상
];
```

From packages/admin/src/components/polymarket/PolymarketSettings.tsx:
```typescript
const PM_KEYS = [
  { key: 'actions.polymarket_enabled', ... },             // enabled는 이미 Providers에서 토글
  { key: 'actions.polymarket_default_fee_bps', ... },     // 이관 대상
  { key: 'actions.polymarket_order_expiry_seconds', ... },// 이관 대상
  { key: 'actions.polymarket_max_position_usdc', ... },   // 이관 대상
  { key: 'actions.polymarket_proxy_wallet', ... },        // 이관 대상
  { key: 'actions.polymarket_neg_risk_enabled', ... },    // 이관 대상
  { key: 'actions.polymarket_auto_approve_ctf', ... },    // 이관 대상
];
```

From packages/admin/src/components/tab-nav.tsx:
```typescript
export type TabItem = { key: string; label: string };
export function TabNav({ tabs, activeTab, onTabChange }: { tabs: TabItem[]; activeTab: string; onTabChange: (key: string) => void }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings 탭 제거 + Providers 링크 추가 + 설정 이관</name>
  <files>
    packages/admin/src/pages/hyperliquid.tsx
    packages/admin/src/pages/polymarket.tsx
    packages/admin/src/pages/actions.tsx
  </files>
  <action>
1. **hyperliquid.tsx 수정:**
   - HYPERLIQUID_TABS에서 `{ key: 'settings', label: 'Settings' }` 항목 제거
   - Tab type에서 `'settings'` 제거: `type Tab = 'overview' | 'orders' | 'spot' | 'subaccounts'`
   - `SettingsPanel` import 제거
   - `activeTab.value === 'settings'` 조건부 렌더링 블록 제거
   - 월렛 셀렉터 위(div 최상단)에 Providers 페이지 링크 추가:
     ```tsx
     <p style={{ fontSize: 'var(--font-size-sm)', color: 'var(--color-text-muted)', marginBottom: 'var(--space-3)' }}>
       <a href="#/providers" style={{ color: 'var(--color-primary)' }}>Configure in Trading &gt; Providers</a>
     </p>
     ```

2. **polymarket.tsx 수정:**
   - POLYMARKET_TABS에서 `{ key: 'settings', label: 'Settings' }` 항목 제거
   - Tab type에서 `'settings'` 제거: `type Tab = 'overview' | 'markets' | 'orders' | 'positions'`
   - `PolymarketSettings` import 제거
   - `activeTab.value === 'settings'` 조건부 렌더링 블록 제거
   - 월렛 셀렉터 위(div 최상단)에 동일한 Providers 페이지 링크 추가

3. **actions.tsx PROVIDER_ADVANCED_SETTINGS에 키 추가:**
   - `hyperliquid` 키 추가 (enabled 제외, 나머지 8개):
     ```typescript
     hyperliquid: [
       'hyperliquid_network',
       'hyperliquid_api_url',
       'hyperliquid_rate_limit_weight_per_min',
       'hyperliquid_default_leverage',
       'hyperliquid_default_margin_mode',
       'hyperliquid_builder_address',
       'hyperliquid_builder_fee',
       'hyperliquid_order_status_poll_interval_ms',
     ],
     ```
   - `polymarket` 키 추가 (enabled 제외, 나머지 6개):
     ```typescript
     polymarket: [
       'polymarket_default_fee_bps',
       'polymarket_order_expiry_seconds',
       'polymarket_max_position_usdc',
       'polymarket_proxy_wallet',
       'polymarket_neg_risk_enabled',
       'polymarket_auto_approve_ctf',
     ],
     ```
   - CATEGORY_ORDER에 'Prediction' 추가 (Polymarket이 prediction 카테고리): 이미 'Other'로 분류될 수 있으므로, 기존 카테고리에 없으면 추가. CATEGORY_ORDER 배열 끝 'Other' 앞에 'Prediction' 추가.

Note: SettingsPanel.tsx와 PolymarketSettings.tsx 파일은 삭제하지 않는다. import가 제거되었으므로 dead code이지만, 레거시 정리는 별도로 할 수 있다. 단, 페이지에서 사용하지 않으므로 번들에 포함되지 않는다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/admin</automated>
  </verify>
  <done>
    - hyperliquid.tsx에 4개 탭만 존재 (settings 없음), Providers 링크 있음
    - polymarket.tsx에 4개 탭만 존재 (settings 없음), Providers 링크 있음
    - actions.tsx PROVIDER_ADVANCED_SETTINGS에 hyperliquid 8키 + polymarket 6키 등록
    - TypeScript 타입체크 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 테스트 업데이트</name>
  <files>
    packages/admin/src/__tests__/hyperliquid.test.tsx
    packages/admin/src/__tests__/polymarket.test.tsx
  </files>
  <action>
1. **hyperliquid.test.tsx 수정:**
   - Settings 탭 관련 테스트 케이스 제거 또는 수정 (settings 탭 클릭/렌더링 테스트)
   - "Configure in Trading > Providers" 링크가 렌더링되는지 확인하는 테스트 추가:
     ```typescript
     it('shows configure link to Providers page', async () => {
       // ... setup with mock wallets
       await waitFor(() => {
         expect(screen.getByText(/Configure in/)).toBeDefined();
       });
       const link = screen.getByText(/Configure in/).closest('a');
       expect(link?.getAttribute('href')).toBe('#/providers');
     });
     ```
   - 탭 목록이 4개인지 확인하는 테스트 유지/수정

2. **polymarket.test.tsx 수정:**
   - Settings 탭 관련 테스트 케이스 제거 또는 수정
   - "Configure in Trading > Providers" 링크 렌더링 테스트 추가
   - 탭 목록이 4개인지 확인하는 테스트 유지/수정
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/admin/src/__tests__/hyperliquid.test.tsx packages/admin/src/__tests__/polymarket.test.tsx</automated>
  </verify>
  <done>
    - Settings 탭 관련 테스트가 제거됨
    - Providers 링크 렌더링 테스트 통과
    - 기존 테스트(로딩, 월렛 셀렉터, 탭 네비게이션)가 모두 통과
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/admin` -- 타입 에러 없음
2. `pnpm vitest run packages/admin/src/__tests__/hyperliquid.test.tsx packages/admin/src/__tests__/polymarket.test.tsx` -- 테스트 통과
3. hyperliquid.tsx에서 'settings' 문자열 grep 결과 없음
4. polymarket.tsx에서 'settings' 문자열 grep 결과 없음 (Settings 탭 관련)
5. actions.tsx PROVIDER_ADVANCED_SETTINGS에 'hyperliquid'와 'polymarket' 키 존재
</verification>

<success_criteria>
- Hyperliquid 페이지: Overview/Orders/Spot/Sub-accounts 4탭만 존재, Settings 탭 없음
- Polymarket 페이지: Overview/Markets/Orders/Positions 4탭만 존재, Settings 탭 없음
- 두 페이지 상단에 "Configure in Trading > Providers" 링크가 있고 #/providers로 연결
- Providers 페이지에서 Hyperliquid 8개 설정 + Polymarket 6개 설정이 Advanced Settings로 표시
- 모든 테스트 통과, 타입체크 통과
</success_criteria>

<output>
After completion, create `.planning/phases/419-trading-settings/419-01-SUMMARY.md`
</output>
