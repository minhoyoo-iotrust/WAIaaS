---
phase: 414-interface-migration
plan: 02
type: execute
wave: 1
depends_on: [413-02]
files_modified:
  - packages/admin/src/pages/actions.tsx
  - packages/admin/src/pages/sessions.tsx
  - packages/admin/src/pages/policies.tsx
  - packages/admin/src/pages/notifications.tsx
  - packages/admin/src/pages/walletconnect.tsx
  - packages/admin/src/pages/erc8004.tsx
  - packages/admin/src/pages/security.tsx
  - packages/admin/src/pages/system.tsx
  - packages/admin/src/pages/transactions.tsx
  - packages/admin/src/__tests__/actions.test.tsx
  - packages/admin/src/__tests__/actions-aave-v3.test.tsx
  - packages/admin/src/__tests__/actions-kamino.test.tsx
  - packages/admin/src/__tests__/sessions.test.tsx
  - packages/admin/src/__tests__/policies.test.tsx
  - packages/admin/src/__tests__/policies-coverage.test.tsx
  - packages/admin/src/__tests__/policy-forms.test.tsx
  - packages/admin/src/__tests__/notifications.test.tsx
  - packages/admin/src/__tests__/notifications-coverage.test.tsx
  - packages/admin/src/__tests__/notifications-filters.test.tsx
  - packages/admin/src/__tests__/walletconnect.test.tsx
  - packages/admin/src/__tests__/erc8004.test.tsx
  - packages/admin/src/__tests__/erc8004-reputation.test.tsx
  - packages/admin/src/__tests__/security.test.tsx
  - packages/admin/src/__tests__/system.test.tsx
  - packages/admin/src/__tests__/transactions.test.tsx
  - packages/admin/src/__tests__/transactions-cancel.test.tsx
autonomous: true
requirements: [MIG-01, MIG-02]
must_haves:
  truths:
    - "9개 중규모 페이지가 typed client + generated type을 사용한다"
    - "모든 apiGet/apiPost/apiPut/apiDelete 호출이 typed client로 교체되었다"
    - "manual interface 선언이 generated type alias로 전환되었다"
    - "테스트 mock이 typed-client를 mock하고 satisfies 검증을 포함한다"
  artifacts:
    - path: "packages/admin/src/pages/transactions.tsx"
      provides: "Migrated transactions page"
      contains: "import { api"
    - path: "packages/admin/src/pages/sessions.tsx"
      provides: "Migrated sessions page"
      contains: "import { api"
    - path: "packages/admin/src/pages/policies.tsx"
      provides: "Migrated policies page"
      contains: "import { api"
  key_links:
    - from: "packages/admin/src/pages/transactions.tsx"
      to: "packages/admin/src/api/typed-client.ts"
      via: "import { api } from '../api/typed-client'"
      pattern: "import.*api.*typed-client"
    - from: "packages/admin/src/pages/notifications.tsx"
      to: "packages/admin/src/api/types.generated.ts"
      via: "type aliases from components['schemas']"
      pattern: "components\\['schemas'\\]"
---

<objective>
9개 중규모 페이지(actions, sessions, policies, notifications, walletconnect, erc8004, security, system, transactions)를 typed client + generated type으로 마이그레이션한다.

Purpose: 인터페이스 수가 많은 중규모 페이지들을 전환하여 수동 타입 제거를 가속한다.
Output: 9개 페이지 + 16개 테스트 파일 마이그레이션 완료
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/413-typed-client-first-migration/413-02-SUMMARY.md

<interfaces>
<!-- Migration pattern from dashboard.tsx -->

From packages/admin/src/pages/dashboard.tsx (reference pattern):
```typescript
import { api, ApiError } from '../api/typed-client';
import type { paths, components } from '../api/types.generated';

// Generated type aliases
type AdminStatus = components['schemas']['AdminStatusResponse'];
type RecentTransaction = AdminStatus['recentTransactions'][number];

// API call pattern:
const { data } = await api.GET('/v1/admin/status');
// With params:
const { data } = await api.GET('/v1/admin/defi/positions', {
  params: { query: { walletId, category: 'staking' } }
});
// POST:
const { data } = await api.POST('/v1/admin/agent-prompt', {
  body: { prompt: text }
});
```

From packages/admin/src/api/typed-client.ts:
```typescript
export function createTypedClient(customFetch?): ...;
export const api: ReturnType<typeof createTypedClient>;
export { ApiError } from './client';
```

From packages/admin/src/utils/settings-helpers.ts:
```typescript
export type SettingsData = Record<string, Record<string, string | boolean>>;
export interface KillSwitchState { state: string; activatedAt: number | null; activatedBy: string | null; }
export interface ApiKeyEntry { providerName: string; hasKey: boolean; maskedKey: string | null; requiresApiKey: boolean; updatedAt: string | null; }
export interface RpcTestResult { success: boolean; latencyMs: number; blockNumber?: number; error?: string; }
export interface NotifTestResult { channel: string; success: boolean; error?: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: actions + sessions + policies + security + system 페이지 마이그레이션</name>
  <files>
    packages/admin/src/pages/actions.tsx,
    packages/admin/src/pages/sessions.tsx,
    packages/admin/src/pages/policies.tsx,
    packages/admin/src/pages/security.tsx,
    packages/admin/src/pages/system.tsx,
    packages/admin/src/__tests__/actions.test.tsx,
    packages/admin/src/__tests__/actions-aave-v3.test.tsx,
    packages/admin/src/__tests__/actions-kamino.test.tsx,
    packages/admin/src/__tests__/sessions.test.tsx,
    packages/admin/src/__tests__/policies.test.tsx,
    packages/admin/src/__tests__/policies-coverage.test.tsx,
    packages/admin/src/__tests__/policy-forms.test.tsx,
    packages/admin/src/__tests__/security.test.tsx,
    packages/admin/src/__tests__/system.test.tsx
  </files>
  <action>
각 페이지에 dashboard.tsx 마이그레이션 패턴을 적용한다:

**actions.tsx (3 interfaces, 10 api calls):**
- BuiltinProvider, ProviderAction, ProviderInfo interfaces → generated type aliases
- `apiGet/apiPut/apiDelete` → typed client
- API.ADMIN_SETTINGS, API.ADMIN_API_KEYS, API.ACTIONS_PROVIDERS 경로를 리터럴 문자열로

**sessions.tsx (4 interfaces, 8 api calls):**
- Wallet, SessionWallet, Session, CreatedSession → generated type aliases
- `apiGet/apiPost/apiPut/apiDelete` → typed client

**policies.tsx (2 interfaces, 8 api calls):**
- Wallet, Policy → generated type aliases
- `apiGet/apiPost/apiPut/apiDelete` → typed client

**security.tsx (0 local interfaces, 9 api calls):**
- KillSwitchState, SettingsData는 settings-helpers에서 import하므로 그대로 유지 (Plan 01에서 이미 generated로 전환됨)
- `apiGet/apiPost/apiPut` → typed client만 교체

**system.tsx (0 local interfaces, 7 api calls):**
- SettingsData, ApiKeyEntry는 settings-helpers에서 import하므로 유지
- `apiGet/apiPost/apiPut/apiDelete` → typed client만 교체

**테스트 마이그레이션 (9개 파일):**
- `vi.mock('../api/client')` → `vi.mock('../api/typed-client')`
- mock을 `{ api: { GET: vi.fn(), POST: vi.fn(), ... }, ApiError }` 구조로 변경
- mock return을 `{ data: ... }` 구조로
- mock 객체에 `satisfies` 추가

**주의:** 동적 경로 변환 패턴:
- `API.ADMIN_API_KEY(name)` = `/v1/admin/api-keys/${name}` → `api.PUT('/v1/admin/api-keys/{provider}', { params: { path: { provider: name } }, body: { apiKey } })`
- `API.POLICY(id)` = `/v1/policies/${id}` → `api.PUT('/v1/policies/{policyId}', { params: { path: { policyId: id } }, body: { ... } })`
- path parameter 이름은 types.generated.ts의 paths 정의에서 확인한다
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin test -- --run actions sessions policies security system 2>&1 | tail -20</automated>
  </verify>
  <done>5개 페이지에서 manual interface 0개 (security, system은 원래 0개), apiGet/apiPost 0개. 9개 테스트 통과.</done>
</task>

<task type="auto">
  <name>Task 2: notifications + walletconnect + erc8004 + transactions 페이지 마이그레이션</name>
  <files>
    packages/admin/src/pages/notifications.tsx,
    packages/admin/src/pages/walletconnect.tsx,
    packages/admin/src/pages/erc8004.tsx,
    packages/admin/src/pages/transactions.tsx,
    packages/admin/src/__tests__/notifications.test.tsx,
    packages/admin/src/__tests__/notifications-coverage.test.tsx,
    packages/admin/src/__tests__/notifications-filters.test.tsx,
    packages/admin/src/__tests__/walletconnect.test.tsx,
    packages/admin/src/__tests__/erc8004.test.tsx,
    packages/admin/src/__tests__/erc8004-reputation.test.tsx,
    packages/admin/src/__tests__/transactions.test.tsx,
    packages/admin/src/__tests__/transactions-cancel.test.tsx
  </files>
  <action>
**notifications.tsx (5 interfaces, 10 api calls):**
- ChannelStatus, NotificationStatus, TestResult, NotificationLogEntry, NotificationLogResponse → generated type aliases
- SettingsData는 settings-helpers에서 import (유지)
- `apiGet/apiPost/apiPut` → typed client
- 주의: NotifTestResult는 settings-helpers에서 이미 export됨 — notifications.tsx의 TestResult와 중복 여부 확인

**walletconnect.tsx (5 interfaces, 6 api calls):**
- WalletSummary, WcSession, WcPairingResult, WcPairingStatus, WcTableRow → generated type aliases
- WcTableRow는 UI 전용 타입이라 API 스키마에 없을 수 있음 → manual 유지 가능 (UI-only 타입은 허용)
- `apiGet/apiPost/apiDelete` → typed client

**erc8004.tsx (4 interfaces, 16 api calls):**
- Wallet, AgentEntry, ReputationData, ProviderAction → generated type aliases
- `apiGet/apiPost/apiPut` → typed client
- 동적 경로가 많음: ERC8004_REGISTRATION_FILE, ERC8004_REPUTATION 등 — path parameter 매핑 주의
- `apiPost(\`${API.ADMIN_ACTIONS}/erc8004_agent/register_agent\`, ...)` 같은 문자열 연결 패턴 → typed client path로 변환 가능한지 확인. 불가능하면 api 대신 fetch raw 사용하거나 기존 apiPost 유지하되 별도 주석

**transactions.tsx (6 interfaces, 9 api calls):**
- TransactionItem, TransactionsResponse, IncomingTxItem, IncomingTxResponse, WalletListItem, UnifiedTxRow → generated type aliases
- UnifiedTxRow는 UI 전용 union 타입 — manual 유지 가능 (UI-only 타입은 허용)
- `apiGet/apiPost/apiPut/apiPatch` → typed client

**테스트 마이그레이션 (7개 파일):**
동일한 패턴: vi.mock 변경, mock 구조 변경, satisfies 추가

**UI-only 타입 정책:** API 응답 스키마에 매핑되지 않고 순수 UI 조합용인 타입(WcTableRow, UnifiedTxRow 등)은 manual interface로 유지해도 MIG-01 범위 외이다. 해당 타입에 `// UI-only type (not from API schema)` 주석을 추가한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin test -- --run notifications walletconnect erc8004 transactions 2>&1 | tail -20</automated>
  </verify>
  <done>4개 페이지에서 API-backed manual interface 0개, apiGet/apiPost 0개. UI-only 타입만 남음. 7개 테스트 통과.</done>
</task>

</tasks>

<verification>
- `grep -rn 'apiGet\|apiPost\|apiPut\|apiDelete\|apiPatch' packages/admin/src/pages/{actions,sessions,policies,security,system,notifications,walletconnect,erc8004,transactions}.tsx` → 결과 0건
- `grep -rn "from '../api/client'" packages/admin/src/pages/{actions,sessions,policies,security,system,notifications,walletconnect,erc8004,transactions}.tsx` → 결과 0건
- API 스키마에 매핑되는 manual interface가 0개 (UI-only 제외)
- `pnpm --filter @waiaas/admin exec tsc --noEmit` 통과
</verification>

<success_criteria>
- 9개 중규모 페이지의 `from '../api/client'` import가 0개이다
- 9개 페이지의 apiGet/apiPost 등 수동 타입 단언이 0개이다
- API-backed manual interface가 모두 generated type alias로 교체되었다
- UI-only 조합 타입은 `// UI-only type` 주석과 함께 유지되었다
- 16개 테스트 파일이 typed-client mock을 사용한다
- typecheck 통과
</success_criteria>

<output>
After completion, create `.planning/phases/414-interface-migration/414-02-SUMMARY.md`
</output>
