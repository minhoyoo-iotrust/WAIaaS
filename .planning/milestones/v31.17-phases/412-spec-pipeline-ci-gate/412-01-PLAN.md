---
phase: 412-spec-pipeline-ci-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract-openapi.ts
  - scripts/generate-api-types.ts
  - packages/admin/src/api/types.generated.ts
  - packages/admin/package.json
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04]

must_haves:
  truths:
    - "createApp() stub에서 OpenAPI spec JSON이 추출되고, 모든 조건부 라우트가 포함된다"
    - "추출된 spec의 path 수가 실제 등록 라우트 수와 일치한다"
    - "openapi-typescript가 openapi.json에서 types.generated.ts를 생성한다"
    - "pnpm run generate:api-types 한 명령으로 추출+생성이 완료된다"
  artifacts:
    - path: "scripts/extract-openapi.ts"
      provides: "OpenAPI spec 빌드타임 추출 스크립트"
      contains: "createApp"
    - path: "scripts/generate-api-types.ts"
      provides: "openapi-typescript 타입 생성 오케스트레이션"
      contains: "openapi-typescript"
    - path: "packages/admin/src/api/types.generated.ts"
      provides: "생성된 TypeScript 타입 파일"
      contains: "paths"
  key_links:
    - from: "scripts/extract-openapi.ts"
      to: "packages/daemon/src/api/server.ts"
      via: "createApp() import + /doc 요청"
      pattern: "createApp.*request.*doc"
    - from: "scripts/generate-api-types.ts"
      to: "scripts/extract-openapi.ts"
      via: "추출 후 openapi-typescript 실행"
      pattern: "extract.*openapi-typescript"
---

<objective>
OpenAPI spec을 빌드타임에 JSON으로 추출하고, openapi-typescript로 TypeScript 타입을 생성하는 파이프라인을 구축한다.

Purpose: Admin UI가 백엔드 스키마와 자동 동기화되는 타입을 사용할 수 있는 기반을 만든다.
Output: extract-openapi.ts, generate-api-types.ts, types.generated.ts, openapi.json
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts the executor needs -->

From packages/daemon/src/api/server.ts:
```typescript
export interface CreateAppDeps {
  // All fields are optional (duck-typed) -- createApp({}) returns app with public routes only
  db?: BetterSQLite3Database<typeof schema>;
  sqlite?: SQLiteDatabase;
  keyStore?: LocalKeyStore;
  masterPasswordHash?: string;
  config?: DaemonConfig;
  adapterPool?: AdapterPool | null;
  policyEngine?: IPolicyEngine;
  jwtSecretManager?: JwtSecretManager;
  // ... 30+ more optional deps
}
export function createApp(deps: CreateAppDeps = {}): OpenAPIHono;
```

Existing validate-openapi.ts pattern (scripts/validate-openapi.ts):
```typescript
const app = createApp();
const res = await app.request('/doc', { headers: { Host: '127.0.0.1:3100' } });
const spec = await res.json();
```

NOTE: createApp({}) without deps registers ONLY public routes (/health, /doc, /v1/nonce, /v1/skills, /v1/utils).
All other routes are behind `if (deps.db)`, `if (deps.jwtSecretManager && deps.db)`, etc.
To get ALL routes in the spec, the script MUST pass typed stub deps that satisfy all conditional branches.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAPI spec 추출 스크립트 + stub deps</name>
  <files>scripts/extract-openapi.ts, packages/admin/openapi.json</files>
  <action>
1. `pnpm add -D openapi-typescript` (root devDependency로 설치).

2. `scripts/extract-openapi.ts` 생성:
   - `createApp(deps)` import하여 모든 조건부 라우트가 등록되도록 typed stub deps 전달.
   - STATE.md 블로커에 명시된 대로, 각 조건부 분기(`if (deps.db)`, `if (deps.jwtSecretManager && deps.db)`, etc.)를 모두 만족시키는 stub 객체 필요.
   - Stub 전략: `as unknown as Type` 캐스트로 최소 stub 생성. 실제 메서드 호출은 없으므로 (라우트 등록만 하므로) 빈 객체면 충분. 단, `deps.policyEngine instanceof DatabasePolicyEngine` 체크가 있는 x402Routes 분기를 위해 DatabasePolicyEngine 인스턴스 stub 필요 -- DatabasePolicyEngine constructor(db)이므로 stub db 전달.
   - `app.request('/doc', { headers: { Host: '127.0.0.1:3100' } })` 호출로 spec 추출.
   - 추출된 spec에서 paths 키 카운트를 출력하고, `packages/admin/openapi.json`에 JSON.stringify(spec, null, 2)로 저장.

3. Route count 검증 로직 추가:
   - 추출된 spec.paths의 키 수를 출력.
   - 최소 endpoint threshold 설정 (현재 validate-openapi.ts에서 확인되는 패턴 참고, 10개 이상이면 유효).
   - 이 threshold를 밑돌면 exit(1) -- stub deps가 불완전함을 의미.

4. 스크립트 끝에 성공 메시지와 path count, output file path 출력.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsx scripts/extract-openapi.ts && test -f packages/admin/openapi.json && node -e "const s=require('./packages/admin/openapi.json'); const n=Object.keys(s.paths||{}).length; console.log('paths:', n); if(n<10) process.exit(1)"</automated>
  </verify>
  <done>scripts/extract-openapi.ts가 createApp(stubDeps)로 모든 라우트를 포함한 openapi.json을 packages/admin/에 생성하고, path 수가 10개 이상이다</done>
</task>

<task type="auto">
  <name>Task 2: openapi-typescript 타입 생성 + generate:api-types 통합 명령</name>
  <files>scripts/generate-api-types.ts, packages/admin/src/api/types.generated.ts, package.json</files>
  <action>
1. `scripts/generate-api-types.ts` 생성:
   - Step 1: `extract-openapi.ts`를 child_process.execSync 또는 dynamic import로 실행하여 openapi.json 추출.
   - Step 2: openapi-typescript 라이브러리를 프로그래매틱으로 호출 (import astToString from 'openapi-typescript' API 사용).
     입력: `packages/admin/openapi.json`
     출력: `packages/admin/src/api/types.generated.ts`
   - 파일 상단에 `/* eslint-disable */` + `// This file is auto-generated by scripts/generate-api-types.ts -- DO NOT EDIT` 주석 추가.
   - 성공 시 생성된 타입 파일의 export 수와 크기를 출력.

2. root `package.json`의 scripts에 추가:
   ```
   "generate:api-types": "tsx scripts/generate-api-types.ts"
   ```

3. `packages/admin/openapi.json`을 `.gitignore`에 추가 (중간 산출물이므로 git에 포함하지 않음). 대신 `packages/admin/src/api/types.generated.ts`는 git에 커밋하여 CI freshness 비교 대상으로 사용.

4. openapi-typescript v7 API 사용: `import openapiTS, { astToString } from 'openapi-typescript'` -- URL이 아닌 로컬 파일 경로 사용. `node:fs`로 JSON 읽어서 전달.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm run generate:api-types && test -f packages/admin/src/api/types.generated.ts && head -5 packages/admin/src/api/types.generated.ts | grep -q "auto-generated"</automated>
  </verify>
  <done>pnpm run generate:api-types 실행 시 openapi.json 추출 + types.generated.ts 생성이 한 번에 완료되고, 생성 파일에 auto-generated 주석이 포함된다</done>
</task>

</tasks>

<verification>
1. `pnpm run generate:api-types` 성공 (exit 0)
2. `packages/admin/openapi.json` 존재하고 paths > 10
3. `packages/admin/src/api/types.generated.ts` 존재하고 `paths` 타입 포함
4. TypeScript 컴파일 에러 없음: `pnpm --filter @waiaas/admin typecheck`
</verification>

<success_criteria>
- generate:api-types 명령이 openapi.json과 types.generated.ts를 한 번에 생성
- 생성된 openapi.json의 endpoint 수가 실제 라우트 수와 일치 (10개 이상)
- types.generated.ts가 유효한 TypeScript이고 paths/components 타입을 export
</success_criteria>

<output>
After completion, create `.planning/phases/412-spec-pipeline-ci-gate/412-01-SUMMARY.md`
</output>
