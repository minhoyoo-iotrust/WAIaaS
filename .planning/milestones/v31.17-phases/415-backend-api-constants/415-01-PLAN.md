---
phase: 415-backend-api-constants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/interfaces/action-provider.types.ts
  - packages/daemon/src/api/routes/actions.ts
  - packages/daemon/src/api/routes/admin-settings.ts
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/daemon/src/__tests__/action-providers-api.test.ts
  - packages/daemon/src/__tests__/admin-settings-schema.test.ts
autonomous: true
requirements: [API-01, API-02, API-03, API-04]

must_haves:
  truths:
    - "GET /v1/actions/providers 응답에 각 프로바이더의 enabledKey, category, isEnabled 필드가 포함된다"
    - "GET /v1/admin/settings/schema가 모든 등록된 설정 키와 category/label/description 메타데이터를 반환한다"
    - "프로바이더 응답의 isEnabled 값이 SettingsService 실제 상태와 일치한다"
  artifacts:
    - path: "packages/daemon/src/api/routes/actions.ts"
      provides: "Enhanced provider listing with enabledKey, category, isEnabled"
    - path: "packages/daemon/src/api/routes/admin-settings.ts"
      provides: "GET /v1/admin/settings/schema endpoint"
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "SettingDefinition에 label, description 메타데이터 추가"
  key_links:
    - from: "actions.ts provider listing"
      to: "ActionProviderMetadata.category + SettingsService"
      via: "enabledKey/isEnabled derivation from settings"
    - from: "admin-settings.ts /schema"
      to: "SETTING_DEFINITIONS"
      via: "direct mapping to response"
---

<objective>
Backend API를 확장하여 Admin UI가 하드코딩 없이 프로바이더와 설정 메타데이터를 API에서 가져올 수 있게 한다.

Purpose: Admin UI의 BUILTIN_PROVIDERS 하드코딩 제거를 위한 백엔드 지원. 프로바이더 디스커버리 API와 설정 스키마 API를 제공한다.
Output: Enhanced GET /v1/actions/providers + new GET /v1/admin/settings/schema
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/daemon/src/api/routes/actions.ts
@packages/daemon/src/api/routes/admin-settings.ts
@packages/daemon/src/infrastructure/settings/setting-keys.ts
@packages/core/src/interfaces/action-provider.types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance GET /v1/actions/providers with enabledKey, category, isEnabled</name>
  <files>
    packages/core/src/interfaces/action-provider.types.ts
    packages/daemon/src/api/routes/actions.ts
    packages/daemon/src/__tests__/action-providers-api.test.ts
  </files>
  <behavior>
    - Test: GET /v1/actions/providers response includes enabledKey, category, isEnabled for each provider
    - Test: isEnabled reflects actual settings state (enabled provider -> true, disabled -> false)
    - Test: category field matches provider metadata category
    - Test: enabledKey defaults to provider name if not explicitly set
    - Test: Hyperliquid providers share enabledKey 'hyperliquid'
  </behavior>
  <action>
    1. In `packages/core/src/interfaces/action-provider.types.ts`, add optional fields to `ActionProviderMetadataSchema`:
       - `category: z.string().min(1).max(50).optional()` -- provider category (e.g. 'Swap', 'Bridge', 'Staking', 'Lending', 'Yield', 'Perp')
       - `enabledKey: z.string().regex(/^[a-z][a-z0-9_]*$/).optional()` -- settings key override (e.g. 'hyperliquid' for all 3 hyperliquid providers)

    2. In `packages/daemon/src/api/routes/actions.ts`:
       - Add `enabledKey`, `category`, `isEnabled` to `ProviderResponseSchema`:
         ```
         enabledKey: z.string(),
         category: z.string(),
         isEnabled: z.boolean(),
         ```
       - In the listProviders handler, compute:
         - `enabledKey`: `meta.enabledKey ?? meta.name`
         - `category`: `meta.category ?? 'Other'`
         - `isEnabled`: check `deps.settingsService.get(`actions.${enabledKey}_enabled`)` === 'true'
       - Include these fields in the response mapping

    3. Update existing action provider implementations (if any metadata files exist) to include category. This is optional -- the new fields have defaults, so existing providers work without changes.

    Note: The OpenAPI spec will auto-include these new fields since they're added to the Zod response schema.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test --filter=@waiaas/daemon -- --grep "actions/providers" --run 2>&1 | tail -20</automated>
  </verify>
  <done>GET /v1/actions/providers response includes enabledKey, category, isEnabled for every provider. OpenAPI spec updated automatically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GET /v1/admin/settings/schema endpoint</name>
  <files>
    packages/daemon/src/infrastructure/settings/setting-keys.ts
    packages/daemon/src/api/routes/admin-settings.ts
    packages/daemon/src/__tests__/admin-settings-schema.test.ts
  </files>
  <behavior>
    - Test: GET /v1/admin/settings/schema returns array of setting definitions
    - Test: Each entry has key, category, label, description, defaultValue, isCredential
    - Test: label is derived from key (e.g. 'notifications.enabled' -> 'Enabled')
    - Test: description provides human-readable explanation
    - Test: Credential settings have isCredential=true
    - Test: Response is grouped by category when ?grouped=true query param
  </behavior>
  <action>
    1. In `packages/daemon/src/infrastructure/settings/setting-keys.ts`:
       - Add optional `label` and `description` fields to `SettingDefinition` interface
       - Add label/description metadata to each setting definition entry. Use the key's last segment as label basis (e.g. 'telegram_bot_token' -> 'Telegram Bot Token'), add concise description.
       - Export a `SETTING_SCHEMA` array that maps SETTING_DEFINITIONS to a public schema format (key, category, label, description, defaultValue, isCredential)

    2. In `packages/daemon/src/api/routes/admin-settings.ts`:
       - Add new route: `GET /admin/settings/schema`
       - Create Zod response schema: `SettingsSchemaResponseSchema` with `settings` array
       - Handler returns SETTING_DEFINITIONS mapped to { key, category, label, description, defaultValue, isCredential }
       - Add optional `?grouped=true` query param that returns grouped by category: `{ categories: [{ name, label, settings: [...] }] }`

    3. Register the route in the admin settings router.

    Note: This endpoint uses masterAuth (already applied to admin routes).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test --filter=@waiaas/daemon -- --grep "settings/schema" --run 2>&1 | tail -20</automated>
  </verify>
  <done>GET /v1/admin/settings/schema returns all setting definitions with metadata. Supports flat and grouped response modes.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run test --filter=@waiaas/daemon` -- all daemon tests pass
2. `pnpm turbo run typecheck` -- no type errors
3. `pnpm run generate:api-types` -- types regenerated with new fields
</verification>

<success_criteria>
- GET /v1/actions/providers includes enabledKey, category, isEnabled in response
- GET /v1/admin/settings/schema returns all registered settings with metadata
- All existing tests continue to pass
- OpenAPI spec updated with new response fields
</success_criteria>

<output>
After completion, create `.planning/phases/415-backend-api-constants/415-01-SUMMARY.md`
</output>
