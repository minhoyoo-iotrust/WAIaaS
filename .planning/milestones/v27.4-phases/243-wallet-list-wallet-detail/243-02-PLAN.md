---
phase: 243-wallet-list-wallet-detail
plan: 02
type: execute
wave: 2
depends_on: [243-01]
files_modified:
  - packages/admin/src/pages/wallets.tsx
  - packages/admin/src/__tests__/wallets.test.tsx
autonomous: true
requirements: [WDET-01, WDET-02, WDET-03, WDET-04]

must_haves:
  truths:
    - "Wallet detail page has Overview/Transactions/Owner/MCP tab navigation and renders correct content per tab"
    - "Transactions tab supports server-side pagination with Previous/Next controls"
    - "Transactions tab shows txHash as ExplorerLink and has status/type filter dropdowns"
    - "Balance section shows USD value next to native balance using display currency"
    - "Balance section has a manual Refresh button that re-fetches balances"
  artifacts:
    - path: "packages/admin/src/pages/wallets.tsx"
      provides: "WalletDetailView with 4-tab structure, enhanced Transactions tab, USD balance, refresh button"
      contains: "TabNav"
    - path: "packages/admin/src/__tests__/wallets.test.tsx"
      provides: "Tests for 4-tab structure, transactions pagination, explorer link, USD, refresh"
  key_links:
    - from: "packages/admin/src/pages/wallets.tsx"
      to: "../components/tab-nav"
      via: "import TabNav"
      pattern: "TabNav.*tabs.*DETAIL_TABS"
    - from: "packages/admin/src/pages/wallets.tsx"
      to: "../components/explorer-link"
      via: "import ExplorerLink"
      pattern: "ExplorerLink.*network.*txHash"
    - from: "packages/admin/src/pages/wallets.tsx"
      to: "../components/filter-bar"
      via: "import FilterBar"
      pattern: "FilterBar.*txFilters"
    - from: "packages/admin/src/pages/wallets.tsx"
      to: "../utils/display-currency"
      via: "import fetchDisplayCurrency, formatWithDisplay"
      pattern: "formatWithDisplay"
---

<objective>
Restructure the wallet detail page into a 4-tab layout (Overview/Transactions/Owner/MCP) and enhance the Transactions tab with pagination, explorer links, and filters. Add USD display next to balances and a manual refresh button.

Purpose: Enable operators to efficiently navigate wallet information across organized tabs, browse transaction history with server-side pagination and filtering, and see USD-equivalent balance values with the ability to refresh on demand.
Output: Refactored WalletDetailView with TabNav-based 4-tab structure, enhanced Transactions tab, USD balance display, and refresh button.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/243-wallet-list-wallet-detail/243-01-SUMMARY.md
@packages/admin/src/pages/wallets.tsx
@packages/admin/src/pages/transactions.tsx
@packages/admin/src/components/tab-nav.tsx
@packages/admin/src/components/explorer-link.tsx
@packages/admin/src/components/filter-bar.tsx
@packages/admin/src/utils/display-currency.ts
@packages/admin/src/api/endpoints.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure wallet detail into 4-tab layout with enhanced transactions tab, USD balance, and refresh</name>
  <files>
    packages/admin/src/pages/wallets.tsx
  </files>
  <action>
Refactor `WalletDetailView` in `wallets.tsx` to organize content into 4 tabs using the existing `TabNav` component:

**1. Define detail tabs and add tab state:**
```typescript
const DETAIL_TABS = [
  { key: 'overview', label: 'Overview' },
  { key: 'transactions', label: 'Transactions' },
  { key: 'owner', label: 'Owner' },
  { key: 'mcp', label: 'MCP' },
];
```
Add `const activeDetailTab = useSignal('overview');` to WalletDetailView.

**2. Tab layout structure:**
After the detail-header (name + action buttons), render:
```tsx
<TabNav tabs={DETAIL_TABS} activeTab={activeDetailTab.value} onTabChange={(k) => { activeDetailTab.value = k; }} />
{activeDetailTab.value === 'overview' && <OverviewTab />}
{activeDetailTab.value === 'transactions' && <TransactionsTab />}
{activeDetailTab.value === 'owner' && <OwnerTab />}
{activeDetailTab.value === 'mcp' && <McpTab />}
```

**3. Overview tab content:**
Move the existing detail-grid (ID, Public Key, Chain, Environment, Default Network, Status, Suspended At, Suspension Reason, Created, Updated), the balance section, and the networks section into an inline `OverviewTab` component (can be a local function inside WalletDetailView that accesses the parent's signals via closure).

**4. Enhanced Transactions tab:**
Create a `TransactionsTab` local component inside WalletDetailView with:

a) **Server-side pagination with offset/limit:**
   - Add signals: `txPage` (number, default 0), `txTotal` (number), `txItems` (array).
   - Use `PAGE_SIZE = 20` constant.
   - Modify `fetchTransactions` to accept and pass `offset` and `limit` query params to `API.ADMIN_WALLET_TRANSACTIONS(id)`.
   - NOTE: The current endpoint at `GET /admin/wallets/:id/transactions` supports a `limit` query param but NOT `offset`. You need to check the endpoint — if it only supports `limit`, add `offset` support in the route handler AND the request schema. Alternatively, just use the existing endpoint with limit=20 and rely on client-side behavior.
   - Actually, look at the admin cross-wallet transactions endpoint (GET /admin/transactions) which already has offset/limit. The per-wallet endpoint (GET /admin/wallets/:id/transactions) currently only has limit. For consistency and simplicity, update the per-wallet endpoint to also accept `offset` query param. Add `offset` to the route query schema in `packages/daemon/src/api/routes/admin.ts` at the `adminWalletTransactionsRoute` definition, and use it in the handler (`.offset(offset)`).
   - Add Previous/Next pagination controls matching the pattern from `transactions.tsx`.

b) **ExplorerLink for txHash:**
   - Import `ExplorerLink` from `../components/explorer-link`.
   - In the transaction table, replace the plain txHash display with `<ExplorerLink network={tx.network ?? ''} txHash={tx.txHash} />`.

c) **Status and Type filter dropdowns:**
   - Import `FilterBar` from `../components/filter-bar`.
   - Add `txFilters` signal with keys: `status`, `type`.
   - Define filter fields for status (PENDING/APPROVED/SUBMITTED/CONFIRMED/FAILED) and type (TRANSFER/TOKEN_TRANSFER/CONTRACT_CALL/APPROVE/BATCH).
   - Apply client-side filtering to the transaction items (since the per-wallet endpoint doesn't support server-side type/status filters, filter client-side after fetch).
   - Place FilterBar above the transactions table with `syncUrl={false}`.

d) **Replace the basic Table with a custom table** matching the pattern from transactions.tsx (needed for consistency and row structure):
   - Columns: Time, Type, To, Amount, Network, Status, Tx Hash
   - Each row renders badges for type and status.

**5. Owner tab content:**
Move the existing owner-section content (owner wallet info, approval method radio buttons, WalletConnect subsection) into an `OwnerTab` local component.

**6. MCP tab content:**
Move the existing mcp-setup-section content into an `McpTab` local component.

**7. USD display next to balances (WDET-03):**
- Import `fetchDisplayCurrency` and `formatWithDisplay` from `../utils/display-currency`.
- Add signals: `displayCurrency` (string, default 'USD'), `displayRate` (number | null, default 1).
- On mount, call `fetchDisplayCurrency()` to set currency + rate.
- In the balance section (Overview tab), after each native balance display (e.g., "1.5 SOL"), if the balance API returns no USD, attempt to calculate:
  - The balance API does NOT return USD. For a simple approach, call the price oracle only if a known mapping exists: SOL and ETH have CoinGecko IDs. But this adds complexity.
  - SIMPLER approach: The admin cross-wallet transactions endpoint returns `amountUsd`. But for balances, we don't have that.
  - SIMPLEST approach that works: Just display the native balance. The USD requirement (WDET-03) can be met by showing "USD value" only when we have it. Since the balance API doesn't return USD, leave a placeholder note "USD value not available" or skip for now.
  - ACTUAL IMPLEMENTATION: Use a lightweight approach — call `GET /v1/admin/settings` to check if a price oracle is configured, then for each balance network, use a simple native token symbol-to-USD lookup. But the admin API doesn't expose price lookup.
  - BEST APPROACH: Add a new `amountUsd` field to the balance API response. In the admin route handler for `/admin/wallets/:id/balance`, after getting the native balance, resolve the USD price using `deps.priceOracle?.getPrice(symbol)` if available.
  - DECISION: Modify the balance API response to include `usd` field for native balance. In `packages/daemon/src/api/routes/admin.ts`, the `adminWalletBalanceRoute` handler already gets the balance. After computing `nativeBalance`, use the price oracle (if available in deps) to get the USD value:
    ```typescript
    let nativeUsd: number | null = null;
    if (deps.priceOracle) {
      try {
        const symbol = balanceInfo.symbol;
        const priceResult = await deps.priceOracle.getPrice(symbol, 'usd');
        if (priceResult) {
          nativeUsd = Number(nativeBalance) * priceResult.price;
        }
      } catch { /* non-critical */ }
    }
    ```
    Include `nativeUsd` in the response: `native: { balance: nativeBalance, symbol: balanceInfo.symbol, usd: nativeUsd }`.
  - In the Admin UI balance section, show the USD value using `formatWithDisplay(nb.native.usd, displayCurrency.value, displayRate.value)`.

**8. Balance refresh button (WDET-04):**
- Add a "Refresh" button next to the "Balances" heading in the Overview tab.
- On click, call `fetchBalance()` again and show a loading indicator.
- Button: `<Button variant="secondary" size="sm" onClick={fetchBalance} loading={balanceLoading.value}>Refresh</Button>`

**Files to modify:**
- `packages/admin/src/pages/wallets.tsx` — Main refactor
- `packages/daemon/src/api/routes/admin.ts` — Add offset to per-wallet transactions endpoint + USD to balance endpoint
  </action>
  <verify>
Run `pnpm turbo run typecheck` to confirm no type errors across all packages.
Verify the 4-tab layout renders correctly with tab switching.
Verify transactions tab shows pagination, explorer links, and filter dropdowns.
Verify balance section shows USD and has a Refresh button.
  </verify>
  <done>
Wallet detail page is organized into Overview/Transactions/Owner/MCP tabs. Transactions tab has pagination, ExplorerLink, and status/type filters. Balance section shows USD values and has a Refresh button. Per-wallet transactions endpoint supports offset parameter.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for wallet detail tabs, transactions pagination, USD balance, and refresh</name>
  <files>
    packages/admin/src/__tests__/wallets.test.tsx
  </files>
  <action>
Add new test cases to `wallets.test.tsx` in a new `describe('WalletDetailView - 4-tab structure', () => { ... })` block:

1. **Tab rendering**: Mock apiGet for wallet detail and render with a walletId path. Verify that Overview, Transactions, Owner, MCP tab buttons are visible. Verify that clicking "Transactions" tab shows the transactions content.

2. **Tab switching**: Click each tab and verify the corresponding content renders (Overview shows "Balances", Transactions shows the table, Owner shows "Owner Wallet", MCP shows "MCP Setup").

3. **Transactions pagination**: Mock apiGet for per-wallet transactions endpoint to return `{ items: [...], total: 25 }`. Verify Previous/Next buttons appear. Click Next and verify the API is called with offset=20.

4. **Transactions ExplorerLink**: Mock a transaction with txHash and network. Verify an `<a>` tag with the correct explorer URL is rendered.

5. **Transactions filters**: Verify that status and type filter dropdowns are rendered in the Transactions tab.

6. **Balance USD display**: Mock the balance API to return `{ balances: [{ network: 'devnet', isDefault: true, native: { balance: '1.5', symbol: 'SOL', usd: 750.0 }, tokens: [] }] }`. Verify that the USD value is displayed.

7. **Balance refresh button**: Verify the "Refresh" button exists in the balance section. Click it and verify `apiGet` is called for the balance endpoint again.

Follow the existing test patterns in the file. Set `currentPath.value = '/wallets/test-wallet-id'` to render the detail view.
  </action>
  <verify>
Run `pnpm vitest run packages/admin/src/__tests__/wallets.test.tsx` and confirm all tests pass including the new ones.
  </verify>
  <done>
Tests verify 4-tab rendering and switching, transactions pagination/ExplorerLink/filters, balance USD display, and balance refresh button functionality.
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck` passes with no errors across all packages
2. `pnpm vitest run packages/admin/src/__tests__/wallets.test.tsx` all tests pass
3. Wallet detail page renders 4 tabs (Overview/Transactions/Owner/MCP)
4. Transactions tab has pagination, ExplorerLink, status/type filters
5. Balance section shows USD and has Refresh button
</verification>

<success_criteria>
- Wallet detail has 4-tab structure with correct content per tab
- Transactions tab supports pagination, txHash ExplorerLink, and status/type filters
- Balance section shows USD value next to native balance
- Balance section has a manual Refresh button
- Per-wallet transactions endpoint supports offset parameter
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/243-wallet-list-wallet-detail/243-02-SUMMARY.md`
</output>
