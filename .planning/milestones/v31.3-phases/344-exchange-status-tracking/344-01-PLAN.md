---
phase: 344-exchange-status-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/dcent-swap/exchange.ts
  - packages/actions/src/providers/dcent-swap/exchange-status-tracker.ts
  - packages/actions/src/providers/dcent-swap/index.ts
  - packages/actions/src/providers/dcent-swap/config.ts
  - packages/actions/src/index.ts
  - packages/core/src/enums/notification.ts
  - packages/core/src/i18n/en.ts
  - packages/core/src/i18n/ko.ts
  - packages/actions/src/__tests__/dcent-exchange.test.ts
autonomous: true
requirements: [XCHG-01, XCHG-02, XCHG-03, XCHG-04]

must_haves:
  truths:
    - "Exchange 견적 요청 시 exchange 타입 프로바이더별 비교를 받을 수 있다"
    - "Exchange 실행 시 create_exchange_transaction → payInAddress TRANSFER 파이프라인 요청이 생성된다"
    - "ExchangeStatusTracker가 IAsyncStatusTracker를 구현하여 AsyncPollingService에 등록 가능하다"
    - "Exchange 완료/실패/환불/타임아웃 시 알림 이벤트가 매핑되어 있다"
  artifacts:
    - path: "packages/actions/src/providers/dcent-swap/exchange.ts"
      provides: "getExchangeQuotes + executeExchange 함수"
      exports: ["getExchangeQuotes", "executeExchange"]
    - path: "packages/actions/src/providers/dcent-swap/exchange-status-tracker.ts"
      provides: "ExchangeStatusTracker implementing IAsyncStatusTracker"
      exports: ["ExchangeStatusTracker"]
    - path: "packages/actions/src/providers/dcent-swap/index.ts"
      provides: "DcentSwapActionProvider with exchange + swap_status actions"
      exports: ["DcentSwapActionProvider"]
    - path: "packages/actions/src/__tests__/dcent-exchange.test.ts"
      provides: "Exchange 견적/실행/상태추적 단위 테스트"
      min_lines: 80
  key_links:
    - from: "packages/actions/src/providers/dcent-swap/exchange.ts"
      to: "packages/actions/src/providers/dcent-swap/dcent-api-client.ts"
      via: "DcentSwapApiClient.createExchangeTransaction + getQuotes"
      pattern: "client\\.(createExchangeTransaction|getQuotes)"
    - from: "packages/actions/src/providers/dcent-swap/exchange-status-tracker.ts"
      to: "packages/actions/src/providers/dcent-swap/dcent-api-client.ts"
      via: "DcentSwapApiClient.getTransactionsStatus"
      pattern: "client\\.getTransactionsStatus"
    - from: "packages/actions/src/providers/dcent-swap/exchange-status-tracker.ts"
      to: "packages/actions/src/common/async-status-tracker.ts"
      via: "implements IAsyncStatusTracker"
      pattern: "implements IAsyncStatusTracker"
---

<objective>
Exchange 견적 조회, 실행(payInAddress TRANSFER), 상태 폴링 추적, 알림 이벤트 매핑을 구현한다.

Purpose: 크로스체인 Exchange를 WAIaaS 파이프라인으로 실행하고 비동기 상태를 추적하여 완료/실패를 알린다.
Output: exchange.ts (견적+실행), exchange-status-tracker.ts (폴링), notification events, tests
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/design/77-dcent-swap-aggregator.md

@.planning/phases/343-currency-mapping-dex-swap/343-01-SUMMARY.md
@.planning/phases/343-currency-mapping-dex-swap/343-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/actions/src/providers/dcent-swap/dcent-api-client.ts:
```typescript
export interface ExchangeParams {
  fromId: string; toId: string; fromAmount: string;
  fromDecimals: number; toDecimals: number;
  fromWalletAddress: string; toWalletAddress: string; providerId: string;
}
export interface StatusParams { txId: string; providerId: string; }

class DcentSwapApiClient {
  async createExchangeTransaction(params: ExchangeParams): Promise<DcentExchangeResponse>;
  async getTransactionsStatus(params: StatusParams[]): Promise<DcentStatusResponse>;
  async getQuotes(params: GetQuotesParams): Promise<DcentQuotesResponse>;
}
```

From packages/actions/src/providers/dcent-swap/schemas.ts:
```typescript
export type DcentExchangeResponse = { status: string; transactionId?: string; transactionStatusUrl?: string; payInAddress?: string; fromAmount?: string; toAmount?: string; extraId?: string; }
export type DcentExchangeStatusType = 'waiting' | 'confirming' | 'exchanging' | 'sending' | 'finished' | 'failed' | 'refunded' | 'error';
export type DcentStatusResponseItem = { providerId: string; status: DcentExchangeStatusType; txId: string; contactEmail?: string[]; payInAddress?: string; payOutAddress?: string; fromAmount?: string; toAmount?: string; }
export type DcentQuoteProvider = { id: string; status: string; providerId: string; providerType: 'swap' | 'cross_swap' | 'exchange'; ... }
```

From packages/actions/src/providers/dcent-swap/dex-swap.ts:
```typescript
export interface DcentQuoteResult {
  dexProviders: DcentQuoteProvider[];
  exchangeProviders: DcentQuoteProvider[];
  bestDexProvider: DcentQuoteProvider | null;
  bestOrder: string[];
}
export interface GetQuotesParams { fromAsset: string; toAsset: string; amount: string; fromDecimals: number; toDecimals: number; }
export async function getDcentQuotes(client: DcentSwapApiClient, params: GetQuotesParams): Promise<DcentQuoteResult>;
```

From packages/actions/src/common/async-status-tracker.ts:
```typescript
export interface IAsyncStatusTracker {
  checkStatus(txId: string, metadata: Record<string, unknown>): Promise<AsyncTrackingResult>;
  readonly name: string;
  readonly maxAttempts: number;
  readonly pollIntervalMs: number;
  readonly timeoutTransition: 'TIMEOUT' | 'BRIDGE_MONITORING' | 'CANCELLED';
}
export interface AsyncTrackingResult {
  state: 'PENDING' | 'COMPLETED' | 'FAILED' | 'TIMEOUT';
  details?: Record<string, unknown>;
}
```

From packages/actions/src/providers/dcent-swap/config.ts:
```typescript
export interface DcentSwapConfig {
  apiBaseUrl: string; requestTimeoutMs: number;
  defaultSlippageBps: number; maxSlippageBps: number; currencyCacheTtlMs: number;
}
```

From packages/core/src/enums/notification.ts:
```typescript
export const NOTIFICATION_EVENT_TYPES = [ /* 51 events */ ] as const;
```

Existing bridge_status column: transactions.bridgeStatus (text, nullable), transactions.bridgeMetadata (text, nullable)
bridge_status CHECK: PENDING | COMPLETED | FAILED | BRIDGE_MONITORING | TIMEOUT | REFUNDED
AsyncPollingService: picks up bridge_status IN ('PENDING', 'BRIDGE_MONITORING'), calls tracker.checkStatus()
Tracker registration: daemon.ts imports from @waiaas/actions, registerTracker(new ExchangeStatusTracker(...))
Notification emit: callbacks.emitNotification?.(eventType, walletId, data)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exchange 견적 조회 + 실행 로직 + ExchangeStatusTracker + 테스트</name>
  <files>
    packages/actions/src/providers/dcent-swap/exchange.ts
    packages/actions/src/providers/dcent-swap/exchange-status-tracker.ts
    packages/actions/src/providers/dcent-swap/config.ts
    packages/actions/src/__tests__/dcent-exchange.test.ts
  </files>
  <behavior>
    - getExchangeQuotes: DCent API 호출 후 exchange 타입 프로바이더만 필터, expectedAmount 내림차순 정렬, bestExchangeProvider 반환
    - getExchangeQuotes: 모든 프로바이더 실패 시 ChainError('INVALID_INSTRUCTION') throw
    - executeExchange: get_quotes → exchange 프로바이더 선택 → create_exchange_transaction → { transferRequest, exchangeMetadata } 반환
    - executeExchange: transferRequest = { type: 'TRANSFER', to: payInAddress, amount: fromAmount, memo: extraId (if present) }
    - executeExchange: exchangeMetadata = { transactionId, providerId, transactionStatusUrl, toAmount, tracker: 'dcent-exchange' }
    - executeExchange: providerId 미지정 시 bestOrder에서 exchange 프로바이더 자동 선택
    - executeExchange: create_exchange_transaction 실패 (payInAddress 없음) 시 ChainError throw
    - ExchangeStatusTracker: name='dcent-exchange', pollIntervalMs=30000, maxAttempts=120, timeoutTransition='TIMEOUT'
    - ExchangeStatusTracker.checkStatus: 'finished' → COMPLETED, 'failed' → FAILED, 'refunded' → COMPLETED(refunded=true), 'waiting'|'confirming'|'exchanging'|'sending' → PENDING, 'error' → FAILED
    - ExchangeStatusTracker.checkStatus: metadata에서 dcentTransactionId + dcentProviderId 읽어 getTransactionsStatus 호출
  </behavior>
  <action>
    **exchange.ts** 생성:
    1. `getExchangeQuotes(client, params)` — getDcentQuotes 호출 후 exchangeProviders만 필터/정렬하여 `ExchangeQuoteResult { providers, bestProvider }` 반환. 빈 경우 NO_ROUTE 에러.
    2. `executeExchange(client, params, config)` — params에 fromAsset, toAsset, amount, decimals, fromWalletAddress, toWalletAddress, providerId?(optional). CAIP-19 → DCent ID 변환, get_quotes → exchange 필터, provider 선택, create_exchange_transaction 호출, 응답에서 payInAddress/transactionId/toAmount/extraId 추출. `{ transferRequest: { type: 'TRANSFER', to: payInAddress, amount: fromAmount, memo?: extraId }, exchangeMetadata: { dcentTransactionId, dcentProviderId, transactionStatusUrl, toAmount, tracker: 'dcent-exchange', notificationEvent: 'EXCHANGE_TIMEOUT', enrolledAt: Date.now() } }` 반환.
    3. ExchangeQuoteResult, ExecuteExchangeParams, ExchangeResult 타입 export.

    **exchange-status-tracker.ts** 생성:
    1. `ExchangeStatusTracker implements IAsyncStatusTracker` — LI.FI BridgeStatusTracker 패턴 따라.
    2. name='dcent-exchange', pollIntervalMs from config (default 30000), maxAttempts=120 (30s * 120 = 1h), timeoutTransition='TIMEOUT'.
    3. constructor(config: DcentSwapConfig) — DcentSwapApiClient 생성.
    4. checkStatus(txId, metadata) — metadata에서 dcentTransactionId, dcentProviderId 추출 → client.getTransactionsStatus 호출 → mapDcentExchangeStatus 함수로 AsyncTrackingResult 변환.
    5. mapDcentExchangeStatus: 'finished' → COMPLETED, 'failed' → FAILED, 'refunded' → COMPLETED(details: { refunded: true }), 'waiting'/'confirming'/'exchanging'/'sending' → PENDING(details: { dcentStatus }), 'error' → FAILED(details: { error }).

    **config.ts** 수정:
    - `exchangePollIntervalMs: number` (default 30000) 추가
    - `exchangePollMaxMs: number` (default 3600000) 추가
    - DCENT_SWAP_DEFAULTS에 값 추가

    **테스트 (dcent-exchange.test.ts)**:
    msw 기반. describe('getExchangeQuotes'), describe('executeExchange'), describe('ExchangeStatusTracker').
    - 정상 exchange 견적 조회 (exchange 프로바이더만 반환)
    - 빈 exchange 프로바이더 에러
    - 정상 execute (payInAddress TRANSFER 반환, exchangeMetadata 포함)
    - extraId 있는 경우 memo 포함
    - payInAddress 없는 응답 에러
    - ExchangeStatusTracker: finished → COMPLETED
    - ExchangeStatusTracker: failed → FAILED
    - ExchangeStatusTracker: refunded → COMPLETED(refunded=true)
    - ExchangeStatusTracker: waiting/confirming/exchanging/sending → PENDING
    - ExchangeStatusTracker: error → FAILED
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/__tests__/dcent-exchange.test.ts</automated>
  </verify>
  <done>getExchangeQuotes가 exchange 프로바이더별 비교를 반환하고, executeExchange가 payInAddress TRANSFER + 메타데이터를 반환하며, ExchangeStatusTracker가 DCent 상태를 AsyncTrackingResult로 매핑한다. 10+ 테스트 통과.</done>
</task>

<task type="auto">
  <name>Task 2: DcentSwapActionProvider 확장 + 알림 이벤트 + 패키지 export</name>
  <files>
    packages/actions/src/providers/dcent-swap/index.ts
    packages/actions/src/index.ts
    packages/core/src/enums/notification.ts
    packages/core/src/i18n/en.ts
    packages/core/src/i18n/ko.ts
  </files>
  <action>
    **index.ts (DcentSwapActionProvider)** 수정:
    1. ExchangeInputSchema 추가 (doc 77 section 9.3: fromAsset, toAsset, amount, fromDecimals, toDecimals, toAddress, providerId?).
    2. SwapStatusInputSchema 추가 (transactionId, providerId).
    3. actions 배열에 'exchange' (riskLevel: 'high', defaultTier: 'APPROVAL') + 'swap_status' (riskLevel: 'low', defaultTier: 'INSTANT') 추가.
    4. resolve('exchange') — DS-07과 동일 패턴: executeExchange 호출 후 ChainError throw with result data. Exchange는 TRANSFER 파이프라인이므로 ContractCallRequest로 반환 불가. MCP/SDK에서 executeExchange() 직접 호출.
    5. resolve('swap_status') — DS-07 패턴: ChainError throw with status data.
    6. public `executeExchange(params)` 메서드 추가 — exchange.ts의 executeExchange 래핑.
    7. public `querySwapStatus(params)` 메서드 추가 — client.getTransactionsStatus 호출 후 결과 반환.
    8. re-export: ExchangeStatusTracker, ExchangeQuoteResult, ExchangeResult, ExecuteExchangeParams.

    **packages/actions/src/index.ts** 수정:
    - `export { ExchangeStatusTracker } from './providers/dcent-swap/exchange-status-tracker.js';` 추가.
    - 기존 DcentSwapActionProvider export 확인 (이미 있으면 skip).

    **notification.ts** 수정:
    - NOTIFICATION_EVENT_TYPES에 4개 추가: 'EXCHANGE_COMPLETED', 'EXCHANGE_FAILED', 'EXCHANGE_REFUNDED', 'EXCHANGE_TIMEOUT'
    - 기존 BRIDGE_* 이벤트와 별도로 Exchange 전용 이벤트 사용 (doc 77 section 8.5 설계)

    **en.ts / ko.ts** (i18n 메시지 템플릿):
    - EXCHANGE_COMPLETED: "Cross-chain exchange completed" / "크로스체인 교환 완료"
    - EXCHANGE_FAILED: "Cross-chain exchange failed" / "크로스체인 교환 실패"
    - EXCHANGE_REFUNDED: "Cross-chain exchange refunded" / "크로스체인 교환 환불됨"
    - EXCHANGE_TIMEOUT: "Cross-chain exchange monitoring timeout" / "크로스체인 교환 모니터링 타임아웃"

    **주의사항**:
    - resolve('exchange')는 ContractCallRequest 반환 불가 → ChainError throw with result data (DS-07 패턴). MCP/SDK/REST에서 executeExchange() 직접 호출 후 TRANSFER tx를 파이프라인에 제출하고, 확인 후 bridge_status='PENDING' + bridge_metadata={tracker:'dcent-exchange', dcentTransactionId, dcentProviderId, notificationEvent:'EXCHANGE_TIMEOUT'} 설정하는 것은 Phase 346 통합에서 처리.
    - ExchangeStatusTracker의 notificationEvent 기본값은 'EXCHANGE_TIMEOUT' (metadata에 설정됨, AsyncPollingService가 timeout 시 사용).
    - COMPLETED 시 AsyncPollingService.processResult가 metadata.notificationEvent ?? 'BRIDGE_COMPLETED' 패턴 사용 → exchange metadata에 notificationEvent를 설정하지 않으면 BRIDGE_COMPLETED가 됨. exchange.ts에서 exchangeMetadata에 별도 completedEvent/failedEvent를 넣지 않음 — 대신 exchange-status-tracker의 checkStatus에서 details에 notificationEvent를 'EXCHANGE_COMPLETED'/'EXCHANGE_FAILED'/'EXCHANGE_REFUNDED' 로 설정하여 AsyncPollingService가 올바른 이벤트를 emit하도록 한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/__tests__/dcent-exchange.test.ts src/__tests__/dcent-dex-swap.test.ts && pnpm --filter @waiaas/core exec vitest run src/__tests__/enums.test.ts</automated>
  </verify>
  <done>DcentSwapActionProvider가 4개 액션(get_quotes, dex_swap, exchange, swap_status)을 노출하고, exchange/swap_status는 public 메서드로 직접 접근 가능하며, ExchangeStatusTracker가 @waiaas/actions에서 export되고, 4개 Exchange 알림 이벤트가 @waiaas/core에 등록되어 있다.</done>
</task>

</tasks>

<verification>
1. `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/__tests__/dcent-exchange.test.ts` — Exchange 전체 테스트 통과
2. `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions exec vitest run src/__tests__/dcent-dex-swap.test.ts` — 기존 DEX Swap 테스트 회귀 없음
3. `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/actions --filter=@waiaas/core` — 타입 체크 통과
4. `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core exec vitest run src/__tests__/enums.test.ts` — 알림 이벤트 enum 테스트 통과
</verification>

<success_criteria>
- getExchangeQuotes가 exchange 프로바이더만 필터하여 비교 결과를 반환한다 (XCHG-01)
- executeExchange가 create_exchange_transaction → { transferRequest: TRANSFER, exchangeMetadata } 를 반환한다 (XCHG-02)
- ExchangeStatusTracker가 IAsyncStatusTracker를 구현하여 DCent 상태를 COMPLETED/FAILED/PENDING으로 매핑한다 (XCHG-03)
- EXCHANGE_COMPLETED/FAILED/REFUNDED/TIMEOUT 알림 이벤트가 등록되고 i18n 메시지가 있다 (XCHG-04)
- 기존 DEX Swap 테스트가 회귀 없이 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/344-exchange-status-tracking/344-01-SUMMARY.md`
</output>
