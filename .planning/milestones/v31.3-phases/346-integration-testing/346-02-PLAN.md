---
phase: 346-integration-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sdk/src/client.ts
  - packages/sdk/src/types.ts
  - skills/actions.skill.md
  - skills/transactions.skill.md
autonomous: true
requirements: [INTG-03, INTG-04, INTG-07]

must_haves:
  truths:
    - "SDK에서 getDcentQuotes, dcentDexSwap, dcentExchange, getDcentSwapStatus 4개 편의 메서드가 동작한다"
    - "DEX Swap 실행이 CONTRACT_WHITELIST 정책을 통과한다 (CONTRACT_CALL 파이프라인)"
    - "Exchange 실행이 ALLOWED_TOKENS 정책을 통과한다 (TRANSFER 파이프라인)"
    - "actions.skill.md와 transactions.skill.md에 DCent Swap 기능이 문서화되어 있다"
  artifacts:
    - path: "packages/sdk/src/client.ts"
      provides: "DCent Swap convenience methods"
      contains: "getDcentQuotes"
    - path: "packages/sdk/src/types.ts"
      provides: "DCent Swap parameter types"
      contains: "DcentQuoteParams"
    - path: "skills/actions.skill.md"
      provides: "DCent Swap provider documentation"
      contains: "dcent_swap"
  key_links:
    - from: "packages/sdk/src/client.ts"
      to: "POST /v1/actions/dcent_swap/{action}"
      via: "this.executeAction('dcent_swap', ...)"
      pattern: "executeAction.*dcent_swap"
---

<objective>
SDK에 DCent Swap 편의 메서드 4개를 추가하고, 스킬 파일을 업데이트한다.

Purpose: AI 에이전트가 SDK를 통해 DCent Swap을 편리하게 사용하고, 정책 엔진이 스왑/Exchange 요청을 올바르게 평가한다.
Output: SDK 4메서드, actions.skill.md + transactions.skill.md 업데이트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@internal/design/77-dcent-swap-aggregator.md (Section 10: MCP/SDK/정책/Admin 통합 설계)

@packages/sdk/src/client.ts (WAIaaSClient class, executeAction pattern)
@packages/sdk/src/types.ts (existing param/response types)
@skills/actions.skill.md (actions skill file)
@skills/transactions.skill.md (transactions skill file)

<interfaces>
From packages/sdk/src/client.ts (executeAction pattern):
```typescript
async executeAction(
  provider: string,
  action: string,
  params?: ExecuteActionParams,
): Promise<ExecuteActionResponse>

// ERC-8004 convenience pattern:
async registerAgent(params: Erc8004RegisterAgentParams): Promise<ExecuteActionResponse> {
  const { network, walletId, gasCondition, ...rest } = params;
  return this.executeAction('erc8004_agent', 'register_agent', {
    params: rest, network, walletId, gasCondition,
  });
}
```

From packages/sdk/src/types.ts (ExecuteActionParams):
```typescript
export interface ExecuteActionParams {
  params?: Record<string, unknown>;
  network?: string;
  walletId?: string;
  gasCondition?: GasConditionInput;
}
export interface ExecuteActionResponse {
  txId: string;
  status: string;
}
```

From design doc 77 section 10.2 (SDK methods):
```typescript
getDcentQuotes(params) -> DcentQuoteResult
dcentDexSwap(params) -> TransactionResult
dcentExchange(params) -> TransactionResult
getDcentSwapStatus(params) -> DcentStatusResult
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SDK에 DCent Swap 편의 메서드 4개 추가</name>
  <files>packages/sdk/src/client.ts, packages/sdk/src/types.ts</files>
  <action>
1. `packages/sdk/src/types.ts`에 DCent Swap 파라미터 타입 4개를 추가한다:

```typescript
// --- DCent Swap types ---
export interface DcentQuoteParams {
  fromAsset: string;    // CAIP-19 identifier
  toAsset: string;      // CAIP-19 identifier
  amount: string;       // from token smallest unit
  fromDecimals: number;
  toDecimals: number;
  network?: string;
  walletId?: string;
}

export interface DcentDexSwapParams {
  fromAsset: string;
  toAsset: string;
  amount: string;
  fromDecimals: number;
  toDecimals: number;
  providerId?: string;
  slippageBps?: number;
  network?: string;
  walletId?: string;
  gasCondition?: GasConditionInput;
}

export interface DcentExchangeParams {
  fromAsset: string;
  toAsset: string;
  amount: string;
  fromDecimals: number;
  toDecimals: number;
  toAddress: string;
  providerId?: string;
  network?: string;
  walletId?: string;
  gasCondition?: GasConditionInput;
}

export interface DcentSwapStatusParams {
  transactionId: string;
  providerId: string;
  network?: string;
  walletId?: string;
}
```

2. `packages/sdk/src/client.ts`에 4개 편의 메서드를 추가한다 (기존 ERC-8004 패턴을 따름). `// --- Actions ---` 섹션의 `executeAction` 메서드 뒤, `// --- ERC-8004` 섹션 앞에 배치:

```typescript
// --- DCent Swap convenience methods ---

/** Get DCent swap quotes (informational — no transaction created). */
async getDcentQuotes(params: DcentQuoteParams): Promise<ExecuteActionResponse> {
  const { network, walletId, ...rest } = params;
  return this.executeAction('dcent_swap', 'get_quotes', {
    params: rest, network, walletId,
  });
}

/** Execute DCent DEX swap (same-chain, approve+txdata BATCH). */
async dcentDexSwap(params: DcentDexSwapParams): Promise<ExecuteActionResponse> {
  const { network, walletId, gasCondition, ...rest } = params;
  return this.executeAction('dcent_swap', 'dex_swap', {
    params: rest, network, walletId, gasCondition,
  });
}

/** Execute DCent cross-chain exchange (payInAddress TRANSFER). */
async dcentExchange(params: DcentExchangeParams): Promise<ExecuteActionResponse> {
  const { network, walletId, gasCondition, ...rest } = params;
  return this.executeAction('dcent_swap', 'exchange', {
    params: rest, network, walletId, gasCondition,
  });
}

/** Get DCent swap/exchange status (informational — no transaction created). */
async getDcentSwapStatus(params: DcentSwapStatusParams): Promise<ExecuteActionResponse> {
  const { network, walletId, ...rest } = params;
  return this.executeAction('dcent_swap', 'swap_status', {
    params: rest, network, walletId,
  });
}
```

Import 추가: types.ts에서 새 타입들을 export하고, client.ts에서 import한다. 기존 import 라인에 추가.

주의: `getDcentQuotes`와 `getDcentSwapStatus`는 정보성 액션이므로 파이프라인 실행 없이 결과를 반환한다. 현재 resolve()에서 ChainError를 throw하므로, 이 메서드들은 REST API에서 에러 응답을 받게 된다. 이는 Phase 346의 향후 개선 대상이나, 현재는 executeAction의 기존 응답 구조를 그대로 사용한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/sdk 2>&1 | tail -5</automated>
  </verify>
  <done>SDK client.ts에 4개 편의 메서드가 추가되고 types.ts에 파라미터 타입이 정의됨, typecheck 통과</done>
</task>

<task type="auto">
  <name>Task 2: 스킬 파일 업데이트 (actions.skill.md + transactions.skill.md)</name>
  <files>skills/actions.skill.md, skills/transactions.skill.md</files>
  <action>
1. `skills/actions.skill.md`에 DCent Swap 프로바이더 섹션을 추가한다.

기존 패턴 (Jupiter, 0x, LI.FI 등의 섹션 구조)을 따라서:
- "## N. DCent Swap Aggregator (Multi-Provider DEX + Cross-Chain Exchange)" 섹션 추가
- Provider name: `dcent_swap`
- 4개 액션 설명:
  - `get_quotes` — 스왑 견적 조회 (informational, INSTANT tier)
  - `dex_swap` — DEX 스왑 실행 (DELAY tier, approve+txdata BATCH)
  - `exchange` — 크로스체인 Exchange 실행 (APPROVAL tier, payInAddress TRANSFER)
  - `swap_status` — 스왑/Exchange 상태 조회 (informational, INSTANT tier)
- Admin Settings 키 목록 (7개)
- curl 예제 (get_quotes, dex_swap)
- 정책 연동 설명:
  - DEX Swap: CONTRACT_WHITELIST (DEX 라우터 주소) + ALLOWED_TOKENS + SPENDING_LIMIT 적용
  - Exchange: ALLOWED_TOKENS + SPENDING_LIMIT 적용 (CONTRACT_WHITELIST는 TRANSFER이므로 미적용)
- 지원 체인: ethereum, solana (multi-chain)
- 프로바이더: 1inch, SushiSwap, Uniswap Labs, Rubic, ButterSwap, LI.FI, Changelly, ChangeNOW, Exolix
- 2-hop 자동 라우팅: 직접 경로 없을 때 중간 토큰(ETH/USDC/USDT) 경유 fallback

2. `skills/transactions.skill.md`에 DCent Swap 관련 참조를 추가한다.
- "## 2. Type 1: TRANSFER" 섹션에 Exchange payInAddress TRANSFER 패턴 언급 추가 (한 줄: "DCent Exchange uses TRANSFER to send funds to the exchange service's payInAddress.")
- "## 5. Type 4: BATCH" 섹션에 DEX Swap approve+txdata BATCH 패턴 언급 추가 (한 줄: "DCent DEX Swap uses BATCH with approve + swap txdata for ERC-20 sell orders.")

3. version을 업데이트한다:
- actions.skill.md: version을 적절히 증가 (예: "2.9.0-rc")
- transactions.skill.md: version을 적절히 증가

4. tags에 'dcent-swap', 'dcent', 'exchange', 'cross-chain-exchange', 'aggregator' 추가 (actions.skill.md)

반드시 보안 공지를 유지한다: `> AI agents must NEVER request the master password. Use only your session token.`
  </action>
  <verify>
    <automated>grep -c 'dcent_swap' /Users/minho.yoo/dev/wallet/WAIaaS/skills/actions.skill.md && grep -c 'DCent' /Users/minho.yoo/dev/wallet/WAIaaS/skills/transactions.skill.md</automated>
  </verify>
  <done>actions.skill.md에 DCent Swap 프로바이더 전체 섹션 추가, transactions.skill.md에 DCent 관련 참조 2개 추가</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/sdk` — SDK typecheck 통과
2. `grep -n 'getDcentQuotes\|dcentDexSwap\|dcentExchange\|getDcentSwapStatus' packages/sdk/src/client.ts` — 4개 메서드 확인
3. `grep -c 'dcent_swap' skills/actions.skill.md` — DCent Swap 섹션이 actions.skill.md에 존재
4. `grep -c 'DCent' skills/transactions.skill.md` — transactions.skill.md에 DCent 참조
</verification>

<success_criteria>
- SDK에 4개 편의 메서드 추가됨 (getDcentQuotes, dcentDexSwap, dcentExchange, getDcentSwapStatus)
- SDK types.ts에 4개 파라미터 타입 정의됨
- actions.skill.md에 DCent Swap 프로바이더 완전한 섹션 추가 (4액션, Admin Settings, 정책, curl 예제)
- transactions.skill.md에 DCent TRANSFER/BATCH 참조 추가
- 정책 엔진은 기존 파이프라인 통과로 자동 적용됨 (INTG-04)
</success_criteria>

<output>
After completion, create `.planning/phases/346-integration-testing/346-02-SUMMARY.md`
</output>
