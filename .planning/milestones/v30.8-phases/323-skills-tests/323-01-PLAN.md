---
phase: 323-skills-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/erc8004.skill.md
  - skills/policies.skill.md
  - skills/admin.skill.md
  - skills/actions.skill.md
autonomous: true
requirements: [SKILL-01, SKILL-02]

must_haves:
  truths:
    - "erc8004.skill.md documents all 4 REST GET endpoints with request/response schemas"
    - "erc8004.skill.md documents all 8 write actions with input schemas and risk levels"
    - "erc8004.skill.md documents 3 MCP read-only tools and 11 SDK methods"
    - "policies.skill.md includes REPUTATION_THRESHOLD as 13th policy type with rules schema"
    - "admin.skill.md includes 9 ERC-8004 Admin Settings keys with descriptions"
    - "All skill files include the security notice about session tokens"
  artifacts:
    - path: "skills/erc8004.skill.md"
      provides: "Complete ERC-8004 skill documentation"
      min_lines: 200
    - path: "skills/policies.skill.md"
      provides: "REPUTATION_THRESHOLD policy type section"
      contains: "REPUTATION_THRESHOLD"
    - path: "skills/admin.skill.md"
      provides: "ERC-8004 settings section"
      contains: "erc8004_agent_enabled"
  key_links:
    - from: "skills/erc8004.skill.md"
      to: "skills/policies.skill.md"
      via: "cross-reference in Related Skill Files"
      pattern: "policies.skill.md"
    - from: "skills/erc8004.skill.md"
      to: "skills/actions.skill.md"
      via: "cross-reference for action provider framework"
      pattern: "actions.skill.md"
---

<objective>
Create erc8004.skill.md documenting all ERC-8004 REST endpoints, MCP tools, SDK methods, and admin settings. Update policies.skill.md with REPUTATION_THRESHOLD and admin.skill.md with ERC-8004 settings.

Purpose: AI agents using WAIaaS need skill file documentation to discover and use ERC-8004 trustless agent capabilities (identity registration, reputation management, validation).
Output: 3 updated skill files + 1 new skill file
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key source files for documentation:
@packages/daemon/src/api/routes/erc8004.ts
@packages/actions/src/providers/erc8004/index.ts
@packages/actions/src/providers/erc8004/schemas.ts
@packages/actions/src/providers/erc8004/config.ts
@packages/sdk/src/client.ts (erc8004 methods section)
@packages/sdk/src/types.ts (erc8004 types)
@packages/daemon/src/infrastructure/settings/setting-keys.ts (erc8004 settings)
@packages/mcp/src/tools/erc8004-get-agent-info.ts
@packages/mcp/src/tools/erc8004-get-reputation.ts
@packages/mcp/src/tools/erc8004-get-validation-status.ts

Existing skill files for pattern/format reference:
@skills/x402.skill.md
@skills/policies.skill.md
@skills/admin.skill.md
@skills/actions.skill.md
</context>

<interfaces>
<!-- REST API endpoints (from packages/daemon/src/api/routes/erc8004.ts) -->
GET /v1/erc8004/agent/{agentId} - Agent identity info (sessionAuth)
GET /v1/erc8004/agent/{agentId}/reputation?tag1=&tag2= - Reputation summary (sessionAuth)
GET /v1/erc8004/registration-file/{walletId} - Auto-generated registration file (sessionAuth)
GET /v1/erc8004/validation/{requestHash} - Validation status (sessionAuth)

<!-- 8 Write Actions (from packages/actions/src/providers/erc8004/index.ts) -->
register_agent: APPROVAL tier, Identity Registry
set_agent_wallet: APPROVAL tier, Identity Registry (EIP-712)
unset_agent_wallet: APPROVAL tier, Identity Registry
set_agent_uri: DELAY tier, Identity Registry
set_metadata: NOTIFY tier, Identity Registry
give_feedback: NOTIFY tier, Reputation Registry
revoke_feedback: INSTANT tier, Reputation Registry
request_validation: DELAY tier, Validation Registry (feature-gated)

<!-- SDK Methods (from packages/sdk/src/client.ts) -->
registerAgent(params) -> ExecuteActionResponse
setAgentWallet(params) -> ExecuteActionResponse
unsetAgentWallet(params) -> ExecuteActionResponse
setAgentUri(params) -> ExecuteActionResponse
setAgentMetadata(params) -> ExecuteActionResponse
giveFeedback(params) -> ExecuteActionResponse
revokeFeedback(params) -> ExecuteActionResponse
requestValidation(params) -> ExecuteActionResponse
getAgentInfo(agentId) -> Erc8004AgentInfoResponse
getAgentReputation(agentId, opts?) -> Erc8004ReputationResponse
getValidationStatus(requestHash) -> Erc8004ValidationResponse

<!-- MCP Tools (3 read-only, 8 write auto-exposed via mcpExpose) -->
Read-only: erc8004_get_agent_info, erc8004_get_reputation, erc8004_get_validation_status
Write (auto): action_erc8004_agent_register_agent, etc. (8 tools via mcpExpose: true)

<!-- 9 Admin Settings keys -->
actions.erc8004_agent_enabled (default: false)
actions.erc8004_identity_registry_address (default: 0x8004A169FB4a3325136EB29fA0ceB6D2e539a432)
actions.erc8004_reputation_registry_address (default: 0x8004BAa17C55a88189AE136b182e5fdA19dE9b63)
actions.erc8004_validation_registry_address (default: empty)
actions.erc8004_registration_file_base_url (default: empty)
actions.erc8004_auto_publish_registration (default: true)
actions.erc8004_reputation_cache_ttl_sec (default: 300)
actions.erc8004_min_reputation_score (default: 0)
actions.erc8004_reputation_rpc_timeout_ms (default: 3000)

<!-- REPUTATION_THRESHOLD policy rules schema -->
{
  "min_score": 50,
  "below_threshold_tier": "APPROVAL",
  "unrated_tier": "APPROVAL",
  "tag1": "",
  "tag2": "",
  "check_counterparty": true
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create skills/erc8004.skill.md</name>
  <files>skills/erc8004.skill.md</files>
  <action>
Create `skills/erc8004.skill.md` following the same structure as `skills/x402.skill.md` (frontmatter + sections):

**Frontmatter:**
- name: "WAIaaS ERC-8004"
- description: "ERC-8004 Trustless Agents: identity registration, reputation management, on-chain validation"
- category: "api"
- tags: [wallet, blockchain, erc-8004, identity, reputation, validation, trust, waiass]
- version: "3.0.0-rc"
- dispatch: kind: "tool", allowedCommands: ["curl"]

**Sections to include:**

1. **Header** with security notice: `> AI agents must NEVER request the master password. Use only your session token.`
   Brief intro explaining ERC-8004 = trustless agent identity/reputation/validation on Ethereum.

2. **Base URL / Authentication** - sessionAuth for all endpoints

3. **Permissions** - Agent (sessionAuth): read queries + write actions. Admin (masterAuth prerequisite): enable provider, configure registries

4. **REST API Endpoints** (4 GET endpoints) - Document each with:
   - Full curl example with request params
   - Response schema table
   - Error codes

   4a. `GET /v1/erc8004/agent/{agentId}` - Returns agentId, wallet, uri, metadata, registryAddress, chainId
   4b. `GET /v1/erc8004/agent/{agentId}/reputation?tag1=&tag2=` - Returns agentId, count, score, decimals, tag1, tag2
   4c. `GET /v1/erc8004/registration-file/{walletId}` - Returns ERC-8004 registration file JSON (type, name, active, services, supportedTrust, registrations)
   4d. `GET /v1/erc8004/validation/{requestHash}` - Returns requestHash, validator, agentId, response, responseHash, tag, lastUpdate

5. **Write Actions** (via Action Provider framework) - Document each of the 8 actions with:
   - Action name, risk level, default tier
   - Input parameters table (from Zod schemas)
   - curl example using `POST /v1/actions/execute`

   5a. `register_agent` (high/APPROVAL) - name (required), description, services[], metadata
   5b. `set_agent_wallet` (high/APPROVAL) - agentId (required), deadline (optional, default 1h)
   5c. `unset_agent_wallet` (high/APPROVAL) - agentId (required)
   5d. `set_agent_uri` (medium/DELAY) - agentId (required), uri (required, URL)
   5e. `set_metadata` (low/NOTIFY) - agentId (required), key (1-64 chars), value
   5f. `give_feedback` (low/NOTIFY) - agentId, value (-100 to 100), valueDecimals, tag1, tag2, endpoint, feedbackURI
   5g. `revoke_feedback` (low/INSTANT) - agentId, feedbackIndex (0-based)
   5h. `request_validation` (medium/DELAY) - agentId, validatorAddress (0x EVM), requestDescription, tag

6. **SDK Methods** - Document 11 methods with TypeScript signatures:
   - 8 write: registerAgent, setAgentWallet, unsetAgentWallet, setAgentUri, setAgentMetadata, giveFeedback, revokeFeedback, requestValidation
   - 3 read: getAgentInfo, getAgentReputation, getValidationStatus
   - Include import example and basic usage

7. **MCP Tools** - Document 11 tools:
   - 8 write (auto-exposed via mcpExpose from action provider): action_erc8004_agent_{action_name}
   - 3 read-only (manual): erc8004_get_agent_info, erc8004_get_reputation, erc8004_get_validation_status

8. **Admin Settings** - Table of 9 settings with key, type, default, description:
   - erc8004_agent_enabled (boolean, false)
   - erc8004_identity_registry_address (string, mainnet address)
   - erc8004_reputation_registry_address (string, mainnet address)
   - erc8004_validation_registry_address (string, empty = feature-gated off)
   - erc8004_registration_file_base_url (string, empty)
   - erc8004_auto_publish_registration (boolean, true)
   - erc8004_reputation_cache_ttl_sec (number, 300)
   - erc8004_min_reputation_score (number, 0)
   - erc8004_reputation_rpc_timeout_ms (number, 3000)

9. **REPUTATION_THRESHOLD Policy** - Brief section explaining the policy type with cross-reference to policies.skill.md

10. **Common Workflows** - 3 examples:
    - Register an agent and set up registration file
    - Give feedback and query reputation
    - Set up REPUTATION_THRESHOLD policy

11. **Error Reference** - Table of error codes (CHAIN_ERROR, ADAPTER_NOT_AVAILABLE, WALLET_NOT_FOUND, etc.)

12. **Related Skill Files** - Cross-references to policies.skill.md, actions.skill.md, admin.skill.md, wallet.skill.md
  </action>
  <verify>
    <automated>test -f skills/erc8004.skill.md && grep -q 'erc8004_agent_enabled' skills/erc8004.skill.md && grep -q 'register_agent' skills/erc8004.skill.md && grep -q 'AI agents must NEVER request the master password' skills/erc8004.skill.md && echo PASS</automated>
  </verify>
  <done>skills/erc8004.skill.md exists with all 4 REST endpoints, 8 write actions, 11 SDK methods, 11 MCP tools, 9 admin settings, security notice, and cross-references</done>
</task>

<task type="auto">
  <name>Task 2: Update policies.skill.md + admin.skill.md + actions.skill.md</name>
  <files>skills/policies.skill.md, skills/admin.skill.md, skills/actions.skill.md</files>
  <action>
**policies.skill.md** changes:
1. Update frontmatter description to mention 13 policy types (currently says 12)
2. Add section `### m. REPUTATION_THRESHOLD (v30.8)` after the X402_ALLOWED_DOMAINS section:
   - Description: Evaluate counterparty agent's on-chain reputation. When reputation is below threshold, escalate transaction security tier.
   - Rules schema JSON: min_score (number, 0-100), below_threshold_tier (INSTANT/NOTIFY/DELAY/APPROVAL, default APPROVAL), unrated_tier (same, default APPROVAL), tag1 (string), tag2 (string), check_counterparty (boolean, default true)
   - Table of fields with types, required, descriptions
   - Curl example creating the policy
   - Note: "Requires ERC-8004 feature to be enabled (`actions.erc8004_agent_enabled=true` in Admin Settings)"
   - Note: "Tier only escalates (never downgrades). If SPENDING_LIMIT already assigned APPROVAL, reputation policy cannot lower it to NOTIFY."
3. Update "## 2. Policy Types (12 Types)" heading to "## 2. Policy Types (13 Types)"
4. Add REPUTATION_THRESHOLD to the policy evaluation flow description in section 3, at position 6 in Stage 3 (after APPROVED_SPENDERS, before SPENDING_LIMIT)
5. Add to Related Skill Files: `erc8004.skill.md` -- ERC-8004 trustless agent identity and reputation

**admin.skill.md** changes:
1. Add a new section for ERC-8004 settings (9 keys) in the Admin Settings section. Place it after the existing Actions settings:
   - Table format matching existing settings documentation pattern
   - All 9 keys with descriptions and defaults
   - Note: erc8004_agent_enabled is the master feature gate (default: false)
2. Add to Related Skill Files: `erc8004.skill.md`

**actions.skill.md** changes:
1. Add `erc8004_agent` to the BUILTIN_PROVIDERS list table (description: "ERC-8004 Trustless Agents — identity, reputation, validation", chains: ethereum, requires API key: No, feature gate: actions.erc8004_agent_enabled)
2. Add a brief section or note: "For ERC-8004 action details, see `erc8004.skill.md`"
  </action>
  <verify>
    <automated>grep -q 'REPUTATION_THRESHOLD' skills/policies.skill.md && grep -q 'erc8004_agent_enabled' skills/admin.skill.md && grep -q 'erc8004_agent' skills/actions.skill.md && echo PASS</automated>
  </verify>
  <done>policies.skill.md has REPUTATION_THRESHOLD as 13th policy type with full schema; admin.skill.md has 9 ERC-8004 settings; actions.skill.md lists erc8004_agent in BUILTIN_PROVIDERS</done>
</task>

</tasks>

<verification>
1. `skills/erc8004.skill.md` exists and contains all required sections
2. `skills/policies.skill.md` mentions REPUTATION_THRESHOLD with rules schema
3. `skills/admin.skill.md` mentions all 9 ERC-8004 settings keys
4. `skills/actions.skill.md` lists erc8004_agent provider
5. All skill files include the security notice
</verification>

<success_criteria>
- erc8004.skill.md is 200+ lines with all endpoints, actions, SDK methods, MCP tools, settings documented
- policies.skill.md updated from "12 Types" to "13 Types" with REPUTATION_THRESHOLD section
- admin.skill.md includes ERC-8004 settings table (9 keys)
- actions.skill.md lists erc8004_agent in BUILTIN_PROVIDERS
- All 4 files have security notice
</success_criteria>

<output>
After completion, create `.planning/phases/323-skills-tests/323-01-SUMMARY.md`
</output>
