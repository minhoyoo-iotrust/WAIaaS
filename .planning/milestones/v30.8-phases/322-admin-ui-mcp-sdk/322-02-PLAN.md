---
phase: 322-admin-ui-mcp-sdk
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/erc8004.tsx
  - packages/admin/src/components/layout.tsx
  - packages/admin/src/api/endpoints.ts
  - packages/admin/src/__tests__/erc8004.test.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "Admin UI /erc8004 페이지에서 등록된 에이전트 목록(walletId, agentId, status badge, registryAddress)을 확인할 수 있다"
    - "Admin UI에서 EVM 지갑 선택 + 이름/설명 입력으로 에이전트 등록을 시작할 수 있다"
    - "Admin UI에서 WalletConnect EIP-712 서명으로 agentWallet 링크/언링크를 수행할 수 있다"
    - "Admin UI에서 등록 파일 JSON 트리 뷰어 + URL 복사가 가능하다"
  artifacts:
    - path: "packages/admin/src/pages/erc8004.tsx"
      provides: "ERC-8004 Identity 관리 페이지 전체"
      min_lines: 200
    - path: "packages/admin/src/components/layout.tsx"
      provides: "ERC-8004 네비게이션 항목 추가"
      contains: "erc8004"
    - path: "packages/admin/src/api/endpoints.ts"
      provides: "ERC-8004 API 엔드포인트 상수"
      contains: "ERC8004"
  key_links:
    - from: "packages/admin/src/pages/erc8004.tsx"
      to: "/v1/erc8004/*"
      via: "apiGet/apiPost"
      pattern: "apiGet.*erc8004"
    - from: "packages/admin/src/components/layout.tsx"
      to: "packages/admin/src/pages/erc8004.tsx"
      via: "PageRouter import + route"
      pattern: "Erc8004Page|erc8004"
---

<objective>
Admin UI에 ERC-8004 Identity 관리 페이지(/erc8004) 생성. 에이전트 등록 테이블, 등록 폼, 월렛 링킹, 등록 파일 JSON 뷰어 포함.

Purpose: Owner가 Admin UI에서 ERC-8004 에이전트를 시각적으로 관리할 수 있는 인터페이스 제공
Output: erc8004.tsx 페이지, layout.tsx 네비게이션 추가, 엔드포인트 상수, 렌더 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Admin UI page pattern (from packages/admin/src/pages/wallets.tsx): -->
```typescript
import { useSignal } from '@preact/signals';
import { useEffect } from 'preact/hooks';
import { apiGet, apiPost, apiPut, apiDelete, ApiError } from '../api/client';
import { API } from '../api/endpoints';
import { Table } from '../components/table';
import type { Column } from '../components/table';
import { FormField, Button, Badge } from '../components/form';
import { Modal } from '../components/modal';
import { CopyButton } from '../components/copy-button';
import { EmptyState } from '../components/empty-state';
import { showToast } from '../components/toast';
import { getErrorMessage } from '../utils/error-messages';
import { TabNav } from '../components/tab-nav';

export default function WalletsPage() { ... }
```

<!-- Layout routing (from packages/admin/src/components/layout.tsx): -->
```typescript
// NAV_ITEMS array:
const NAV_ITEMS = [
  { path: '/dashboard', label: 'Dashboard' },
  { path: '/wallets', label: 'Wallets' },
  // ...
];

// PageRouter switch:
if (path === '/actions') return <ActionsPage />;
// ...
```

<!-- API endpoints (from packages/admin/src/api/endpoints.ts): -->
```typescript
export const API = {
  ACTIONS_PROVIDERS: '/v1/actions/providers',
  // ...
} as const;
```

<!-- REST API endpoints available: -->
```
GET  /v1/erc8004/agent/:agentId              -> agent info
GET  /v1/erc8004/agent/:agentId/reputation   -> reputation
GET  /v1/erc8004/registration-file/:walletId  -> registration file JSON
GET  /v1/erc8004/validation/:requestHash      -> validation status
POST /v1/actions/erc8004_agent/register_agent  -> via actions API
POST /v1/actions/erc8004_agent/set_agent_wallet -> via actions API
POST /v1/actions/erc8004_agent/unset_agent_wallet -> via actions API
```

<!-- DB에 저장된 agent_identities 테이블 (admin API는 masterAuth로 직접 쿼리): -->
<!-- Admin routes에서 agent_identities를 조회하는 엔드포인트가 아직 없으므로, -->
<!-- 클라이언트 사이드에서 wallets API + erc8004 API 조합으로 데이터를 구성한다. -->

<!-- WalletConnect 관련 API (wallet-specific): -->
```
POST /v1/wallets/:id/wc/pair   -> { uri: string }
GET  /v1/wallets/:id/wc/session -> { connected, peerName, ... }
DELETE /v1/wallets/:id/wc/session -> disconnect
GET  /v1/wallets/:id/wc/pair/status -> { connected, peerName }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ERC-8004 Identity 관리 페이지 + Layout/Endpoints 수정</name>
  <files>
    packages/admin/src/pages/erc8004.tsx
    packages/admin/src/components/layout.tsx
    packages/admin/src/api/endpoints.ts
  </files>
  <action>
    **endpoints.ts에 ERC-8004 상수 추가:**
    ```typescript
    ERC8004_AGENT: (agentId: string) => `/v1/erc8004/agent/${agentId}`,
    ERC8004_REPUTATION: (agentId: string) => `/v1/erc8004/agent/${agentId}/reputation`,
    ERC8004_REGISTRATION_FILE: (walletId: string) => `/v1/erc8004/registration-file/${walletId}`,
    ERC8004_VALIDATION: (hash: string) => `/v1/erc8004/validation/${hash}`,
    ```

    **layout.tsx 수정:**
    - `import Erc8004Page from '../pages/erc8004';` 추가
    - `NAV_ITEMS`에 `{ path: '/erc8004', label: 'ERC-8004' }` 추가 (`/wallet-apps` 뒤, `/security` 앞)
    - `PAGE_TITLES`에 `'/erc8004': 'ERC-8004 Agents'` 추가
    - `PAGE_SUBTITLES`에 `'/erc8004': 'On-chain agent identity, reputation, and wallet linking'` 추가
    - `PageRouter`에 `if (path === '/erc8004') return <Erc8004Page />;` 추가

    **erc8004.tsx 페이지 생성** (TabNav 2탭: Identity / Registration File):

    **Tab 1: Identity (기본 탭)**
    - 상단: agent_identities 목록 표시를 위해, 먼저 `GET /v1/wallets`로 EVM 지갑 목록을 가져오고, 각 EVM 지갑에 대해 agent_identities DB를 체크 (daemon admin API가 없으므로, 지갑별로 `GET /v1/erc8004/registration-file/:walletId` 호출하여 agentId 존재 여부로 등록 상태 추론). 실제로는 `GET /v1/admin/settings`에서 `actions.erc8004_agent_enabled` 체크 후 비활성이면 feature gate 안내 메시지 표시.
    - Table 구성: columns = [Wallet Name, Wallet ID (short), Status (Badge: REGISTERED/NOT_REGISTERED), Agent ID, Registry Address]. 데이터는 wallets API + erc8004 API 조합.
    - "Register Agent" 버튼 -> Modal 열기:
      - EVM 지갑 선택 (select, chain='ethereum'인 지갑만 필터)
      - Name 입력 (FormField, required)
      - Description 입력 (FormField, optional)
      - Submit -> `apiPost(API.ACTIONS_PROVIDERS + '/erc8004_agent/register_agent', { params: { name, description }, walletId })` 호출 (actions API 패턴)
      - 성공 시 showToast + 목록 새로고침

    **Wallet Linking 섹션 (Identity 탭 하단):**
    - 등록된 에이전트 옆에 "Link Wallet" / "Unlink Wallet" 버튼
    - Link Wallet 클릭 시:
      1. WC 페어링 시작: `apiPost(API.WALLET_WC_PAIR(walletId))` -> URI 표시 (QR placeholder + CopyButton)
      2. 페어링 상태 폴링: `apiGet(API.WALLET_WC_PAIR_STATUS(walletId))` 1초 간격
      3. 연결 확인 후 -> `apiPost('/v1/actions/erc8004_agent/set_agent_wallet', { params: { agentId }, walletId })` 호출
      4. EIP-712 서명은 daemon이 WC를 통해 자동 처리 (Admin UI는 트리거만)
    - Unlink: `apiPost('/v1/actions/erc8004_agent/unset_agent_wallet', { params: { agentId }, walletId })` 호출

    **Tab 2: Registration File**
    - 지갑 선택 (select)
    - `apiGet(API.ERC8004_REGISTRATION_FILE(walletId))` 호출
    - JSON 트리 뷰어: 재귀적 <details>/<summary> 구조로 JSON object 렌더링. 키-값 쌍, 배열 지원.
    - URL 복사 버튼: `${window.location.origin}/v1/erc8004/registration-file/${walletId}` CopyButton

    **Feature gate 체크:**
    - 페이지 로드 시 `apiGet(API.ADMIN_SETTINGS)` 호출하여 `actions.erc8004_agent_enabled` 확인
    - false이면 EmptyState 컴포넌트로 "ERC-8004 Agent feature is disabled. Enable it in Settings > Actions > erc8004_agent_enabled" 안내
    - Settings 페이지 링크 포함

    **스타일:**
    - 기존 CSS 변수 사용 (--color-*, --space-*, --font-* 등)
    - 인라인 스타일 객체 패턴 (기존 페이지와 동일)
    - Badge: REGISTERED=success, NOT_REGISTERED=default, WALLET_LINKED=info
    - JSON 트리 뷰어: monospace 폰트, 들여쓰기 var(--space-2), 키 이름은 bold
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run build --filter=@waiaas/admin && pnpm turbo run typecheck --filter=@waiaas/admin</automated>
  </verify>
  <done>Admin UI /erc8004 페이지가 생성되어 에이전트 목록, 등록 폼, 월렛 링킹, 등록 파일 뷰어가 동작하고 빌드 통과</done>
</task>

<task type="auto">
  <name>Task 2: ERC-8004 페이지 렌더 테스트</name>
  <files>packages/admin/src/__tests__/erc8004.test.tsx</files>
  <action>
    기존 Admin 테스트 패턴 따름 (vi.mock('../api/client'), render, screen assertions).

    테스트 케이스:
    1. Feature gate 비활성 시 EmptyState 렌더 확인 (settings에 erc8004_agent_enabled: false)
    2. Feature gate 활성 시 테이블 헤더 렌더 확인 (Wallet Name, Status, Agent ID 등)
    3. Register Agent 모달 열기/닫기
    4. Registration File 탭 전환 + JSON 뷰어 렌더 (mock registration file 응답)
    5. Link Wallet 버튼 클릭 시 WC 페어링 API 호출 확인
    6. 로딩 상태 표시 확인

    Mock 데이터:
    - wallets: `[{ id: 'w1', name: 'test', chain: 'ethereum', network: 'ethereum-mainnet', ... }]`
    - settings: `{ 'actions.erc8004_agent_enabled': { value: 'true', source: 'admin' } }`
    - registration-file: `{ name: 'test', endpoints: { mcp: '...', rest: '...' } }`
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/admin/src/__tests__/erc8004.test.tsx --reporter=verbose</automated>
  </verify>
  <done>6개 렌더 테스트 통과: feature gate, 테이블, 모달, JSON 뷰어, WC 페어링, 로딩 상태</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run build --filter=@waiaas/admin` 성공
2. `pnpm turbo run typecheck --filter=@waiaas/admin` 성공
3. Admin UI /erc8004 페이지 렌더 테스트 6개 통과
4. 기존 Admin UI 테스트 회귀 없음 (layout 변경으로 인한)
</verification>

<success_criteria>
- Admin UI에서 `#/erc8004` 해시로 ERC-8004 페이지에 접근 가능
- 사이드바 네비게이션에 "ERC-8004" 항목 표시
- 에이전트 등록 테이블에 지갑 이름, 상태 Badge, Agent ID 표시
- Register Agent 모달에서 EVM 지갑 선택 + 이름/설명 입력 가능
- Registration File 탭에서 JSON 트리 뷰어 + URL 복사 동작
- Feature gate 비활성 시 안내 메시지 표시
- 6개 이상 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/322-admin-ui-mcp-sdk/322-02-SUMMARY.md`
</output>
