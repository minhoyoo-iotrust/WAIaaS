---
phase: 322-admin-ui-mcp-sdk
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/erc8004-reputation.tsx
  - packages/admin/src/pages/erc8004.tsx
  - packages/admin/src/components/policy-forms/reputation-threshold-form.tsx
  - packages/admin/src/components/policy-forms/index.tsx
  - packages/admin/src/pages/policies.tsx
  - packages/admin/src/pages/actions.tsx
  - packages/admin/src/__tests__/erc8004-reputation.test.tsx
  - packages/admin/src/__tests__/reputation-threshold-form.test.tsx
autonomous: true
requirements: [UI-05, UI-06, UI-07]

must_haves:
  truths:
    - "Admin UI 평판 대시보드에서 에이전트 점수, 수신 피드백 목록(tag 필터), 외부 에이전트 평판 조회가 가능하다"
    - "REPUTATION_THRESHOLD 정책 폼이 Policies 페이지에 추가되어 min_score, tier 드롭다운, tag 필터를 설정할 수 있다"
    - "Actions 페이지의 BUILTIN_PROVIDERS에 erc8004_agent가 표시된다"
  artifacts:
    - path: "packages/admin/src/pages/erc8004.tsx"
      provides: "Reputation 탭이 추가된 ERC-8004 페이지"
      contains: "Reputation"
    - path: "packages/admin/src/components/policy-forms/reputation-threshold-form.tsx"
      provides: "REPUTATION_THRESHOLD 정책 전용 폼 컴포넌트"
      min_lines: 40
    - path: "packages/admin/src/components/policy-forms/index.tsx"
      provides: "PolicyFormRouter에 REPUTATION_THRESHOLD 케이스 추가"
      contains: "REPUTATION_THRESHOLD"
    - path: "packages/admin/src/pages/policies.tsx"
      provides: "POLICY_TYPES에 REPUTATION_THRESHOLD 추가"
      contains: "Reputation Threshold"
    - path: "packages/admin/src/pages/actions.tsx"
      provides: "BUILTIN_PROVIDERS에 erc8004_agent 항목"
      contains: "erc8004_agent"
  key_links:
    - from: "packages/admin/src/components/policy-forms/index.tsx"
      to: "packages/admin/src/components/policy-forms/reputation-threshold-form.tsx"
      via: "import + switch case"
      pattern: "ReputationThresholdForm"
    - from: "packages/admin/src/pages/erc8004.tsx"
      to: "/v1/erc8004/agent/:agentId/reputation"
      via: "apiGet"
      pattern: "apiGet.*reputation"
---

<objective>
Admin UI에 평판 대시보드(ERC-8004 페이지 Reputation 탭), REPUTATION_THRESHOLD 정책 폼, Actions 페이지 erc8004_agent 항목 추가.

Purpose: Owner가 Admin UI에서 에이전트 평판을 모니터링하고, 평판 기반 정책을 시각적으로 설정할 수 있는 인터페이스 완성
Output: Reputation 탭, 정책 폼, Actions 항목, 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- PolicyFormRouter pattern (from packages/admin/src/components/policy-forms/index.tsx): -->
```typescript
import { ReputationThresholdForm } from './reputation-threshold-form';

export interface PolicyFormProps {
  rules: Record<string, unknown>;
  onChange: (rules: Record<string, unknown>) => void;
  errors: Record<string, string>;
  network?: string;
}

export function PolicyFormRouter({ type, rules, onChange, errors, network }: { type: string } & PolicyFormProps) {
  switch (type) {
    case 'REPUTATION_THRESHOLD':
      return <ReputationThresholdForm rules={rules} onChange={onChange} errors={errors} />;
    // ...
  }
}
```

<!-- Policy form component pattern (e.g. approve-tier-override-form.tsx): -->
```typescript
import type { PolicyFormProps } from './index';
import { FormField } from '../form';

export function ApproveTierOverrideForm({ rules, onChange, errors }: PolicyFormProps) {
  return (
    <div>
      <FormField label="Tier" error={errors.tier}>
        <select value={rules.tier as string || 'DELAY'} onChange={(e) => onChange({ ...rules, tier: (e.target as HTMLSelectElement).value })}>
          <option value="INSTANT">Instant</option>
          <option value="NOTIFY">Notify</option>
          <option value="DELAY">Delay</option>
          <option value="APPROVAL">Approval</option>
        </select>
      </FormField>
    </div>
  );
}
```

<!-- POLICY_TYPES array in policies.tsx: -->
```typescript
const POLICY_TYPES = [
  { label: 'Spending Limit', value: 'SPENDING_LIMIT' },
  // ... 12 entries
  { label: 'x402 Allowed Domains', value: 'X402_ALLOWED_DOMAINS' },
];
```

<!-- BUILTIN_PROVIDERS array in actions.tsx: -->
```typescript
const BUILTIN_PROVIDERS: BuiltinProvider[] = [
  { key: 'jupiter_swap', name: 'Jupiter Swap', ... },
  // ... 9 entries
  { key: 'drift_perp', name: 'Drift Perp', ... },
];
```

<!-- ReputationThresholdRulesSchema (from @waiaas/core): -->
```typescript
export const ReputationThresholdRulesSchema = z.object({
  min_score: z.number().min(0).max(100),
  below_threshold_tier: PolicyTierEnum.default('APPROVAL'),
  unrated_tier: PolicyTierEnum.default('APPROVAL'),
  tag1: z.string().max(32).optional(),
  tag2: z.string().max(32).optional(),
  check_counterparty: z.boolean().default(false),
});
```

<!-- ERC-8004 reputation API: -->
```
GET /v1/erc8004/agent/:agentId/reputation?tag1=&tag2=
-> { agentId, count, score, decimals, tag1, tag2 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reputation 탭 + REPUTATION_THRESHOLD 폼 + Actions erc8004_agent</name>
  <files>
    packages/admin/src/pages/erc8004.tsx
    packages/admin/src/components/policy-forms/reputation-threshold-form.tsx
    packages/admin/src/components/policy-forms/index.tsx
    packages/admin/src/pages/policies.tsx
    packages/admin/src/pages/actions.tsx
  </files>
  <action>
    **IMPORTANT: 이 태스크는 Plan 322-02와 병렬 실행될 수 있다. erc8004.tsx가 아직 없을 경우 이 태스크에서 Reputation 탭만을 위한 최소한의 페이지를 먼저 생성하거나, 322-02가 먼저 완료된 경우 기존 파일에 Reputation 탭을 추가한다. 어느 경우든, 최종 erc8004.tsx에는 Identity + Registration File + Reputation 3개 탭이 모두 포함되어야 한다.**

    **erc8004.tsx에 Reputation 탭 추가** (Tab 3: Reputation):

    Reputation 대시보드 구성:
    1. **My Agent Score 섹션:**
       - 등록된 에이전트 목록에서 agentId가 있는 항목 표시
       - 각 에이전트에 대해 `apiGet(API.ERC8004_REPUTATION(agentId))` 호출
       - 점수 표시: 큰 숫자(score/10^decimals 변환) + count 건
       - Badge: score >= 50 -> success, score >= 20 -> warning, score < 20 -> danger

    2. **Tag 필터:**
       - tag1, tag2 입력 필드 (FormField, optional)
       - 입력 변경 시 reputation API 재호출
       - 기본값: tag1='', tag2='' (전체 조회)

    3. **External Agent Lookup 섹션:**
       - Agent ID 입력 필드 + "Query" 버튼
       - 입력한 agentId의 평판 조회: `apiGet(API.ERC8004_REPUTATION(inputAgentId))`
       - 결과 카드: agentId, score, count, tag1, tag2

    **reputation-threshold-form.tsx 생성:**

    ReputationThresholdRulesSchema의 6개 필드에 대응하는 폼:
    - `min_score`: range 슬라이더 (0-100) + 숫자 표시. FormField, label="Minimum Score".
    - `below_threshold_tier`: select 드롭다운 (INSTANT/NOTIFY/DELAY/APPROVAL). FormField, label="Below Threshold Tier".
    - `unrated_tier`: select 드롭다운 (INSTANT/NOTIFY/DELAY/APPROVAL). FormField, label="Unrated Agent Tier".
    - `tag1`: text input (optional, max 32). FormField, label="Tag1 Filter".
    - `tag2`: text input (optional, max 32). FormField, label="Tag2 Filter".
    - `check_counterparty`: checkbox. FormField, label="Check Counterparty". 설명: "Also evaluate the reputation of the transaction recipient".

    PolicyFormProps 인터페이스 구현. onChange로 부모에 rules 변경 전파.

    **policy-forms/index.tsx 수정:**
    - `import { ReputationThresholdForm } from './reputation-threshold-form';` 추가
    - `PolicyFormRouter` switch에 `case 'REPUTATION_THRESHOLD': return <ReputationThresholdForm ...>;` 추가
    - 주석의 지원 타입 수를 12 -> 13으로 수정

    **policies.tsx 수정:**
    - `POLICY_TYPES` 배열에 `{ label: 'Reputation Threshold', value: 'REPUTATION_THRESHOLD' }` 추가 (X402_ALLOWED_DOMAINS 뒤)
    - `POLICY_DESCRIPTIONS`에 `REPUTATION_THRESHOLD: 'Adjust security tier based on counterparty agent on-chain reputation score (ERC-8004).'` 추가
    - `DEFAULT_RULES`에 `REPUTATION_THRESHOLD: { min_score: 50, below_threshold_tier: 'APPROVAL', unrated_tier: 'APPROVAL', check_counterparty: false }` 추가

    **actions.tsx 수정:**
    - `BUILTIN_PROVIDERS` 배열에 추가 (drift_perp 뒤):
      ```typescript
      { key: 'erc8004_agent', name: 'ERC-8004 Agent', description: 'On-chain agent identity, reputation, and validation (ERC-8004 Trustless Agents)', chain: 'evm', requiresApiKey: false, docsUrl: 'https://eips.ethereum.org/EIPS/eip-8004' },
      ```
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run build --filter=@waiaas/admin && pnpm turbo run typecheck --filter=@waiaas/admin</automated>
  </verify>
  <done>Reputation 탭이 erc8004 페이지에 추가되고, REPUTATION_THRESHOLD 폼이 Policies에 등록되고, erc8004_agent가 Actions에 표시되며 빌드 통과</done>
</task>

<task type="auto">
  <name>Task 2: Reputation + Policy 폼 테스트</name>
  <files>
    packages/admin/src/__tests__/erc8004-reputation.test.tsx
    packages/admin/src/__tests__/reputation-threshold-form.test.tsx
  </files>
  <action>
    **erc8004-reputation.test.tsx** (Reputation 탭 테스트):
    기존 Admin 테스트 패턴 따름.

    1. Reputation 탭 전환 시 점수 표시 확인 (mock reputation 응답: count=5, score='75', decimals=0)
    2. Tag 필터 입력 시 API 재호출 확인 (tag1, tag2 query param)
    3. External agent lookup: agentId 입력 + Query 버튼 클릭 -> reputation API 호출 확인
    4. Score 0일 때 Badge 색상 확인 (danger)

    **reputation-threshold-form.test.tsx** (정책 폼 테스트):
    기존 policy-forms.test.tsx 패턴 따름.

    1. 초기 렌더: min_score 슬라이더 + tier 드롭다운 + check_counterparty 체크박스 표시
    2. min_score 변경 시 onChange 호출 확인
    3. below_threshold_tier 변경 시 onChange 호출 확인
    4. check_counterparty 토글 시 onChange 호출 확인
    5. policies.tsx에서 REPUTATION_THRESHOLD가 POLICY_TYPES에 포함되어 있는지 확인
    6. actions.tsx에서 erc8004_agent가 BUILTIN_PROVIDERS에 포함되어 있는지 확인

    Mock 데이터:
    - settings: `{ 'actions.erc8004_agent_enabled': { value: 'true', source: 'admin' } }`
    - wallets: `[{ id: 'w1', chain: 'ethereum', ... }]`
    - reputation: `{ agentId: '42', count: 5, score: '75', decimals: 0, tag1: '', tag2: '' }`
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/admin/src/__tests__/erc8004-reputation.test.tsx packages/admin/src/__tests__/reputation-threshold-form.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Reputation 탭 4개 + 정책 폼 6개 테스트 통과: 점수 표시, tag 필터, 외부 조회, 폼 필드 변경, POLICY_TYPES/BUILTIN_PROVIDERS 확인</done>
</task>

</tasks>

<verification>
1. `pnpm turbo run build --filter=@waiaas/admin` 성공
2. `pnpm turbo run typecheck --filter=@waiaas/admin` 성공
3. Reputation 탭 테스트 4개 통과
4. REPUTATION_THRESHOLD 폼 테스트 6개 통과
5. 기존 Admin 테스트 회귀 없음 (policies, actions 변경으로 인한)
</verification>

<success_criteria>
- ERC-8004 페이지에 Reputation 탭이 추가되어 점수, 피드백 수, tag 필터 표시
- External agent reputation 조회 가능
- REPUTATION_THRESHOLD가 Policies 페이지 정책 타입 목록에 표시
- REPUTATION_THRESHOLD 전용 폼에서 min_score 슬라이더, tier 드롭다운, tag 필터, check_counterparty 토글 동작
- Actions 페이지 BUILTIN_PROVIDERS에 erc8004_agent 표시
- 10개 이상 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/322-admin-ui-mcp-sdk/322-03-SUMMARY.md`
</output>
