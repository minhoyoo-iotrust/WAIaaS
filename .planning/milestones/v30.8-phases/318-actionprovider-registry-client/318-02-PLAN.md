---
phase: 318-actionprovider-registry-client
plan: 02
type: execute
wave: 2
depends_on: [318-01]
files_modified:
  - packages/actions/src/providers/erc8004/index.ts
  - packages/actions/src/providers/erc8004/registration-file.ts
  - packages/actions/src/index.ts
  - packages/actions/src/__tests__/erc8004-provider.test.ts
autonomous: true
requirements: [PKG-02, IDEN-01, IDEN-04, IDEN-05, VALD-01]

must_haves:
  truths:
    - "Erc8004ActionProvider가 IActionProvider를 구현하고, metadata.name이 'erc8004_agent'이며 mcpExpose: true이다"
    - "register_agent 액션이 APPROVAL 티어로 Identity Registry에 NFT 민팅 ContractCallRequest를 생성한다"
    - "set_agent_uri 액션이 DELAY 티어로 setAgentURI calldata를 인코딩한 ContractCallRequest를 생성한다"
    - "set_metadata 액션이 NOTIFY 티어로 setMetadata calldata를 인코딩한 ContractCallRequest를 생성한다"
    - "give_feedback 액션이 NOTIFY 티어로 giveFeedback calldata를 인코딩한 ContractCallRequest를 생성한다"
    - "revoke_feedback 액션이 INSTANT 티어로 revokeFeedback calldata를 인코딩한 ContractCallRequest를 생성한다"
    - "request_validation 액션이 DELAY 티어로 동작하며, validation_registry_address가 비어있으면 에러를 반환한다"
    - "set_agent_wallet 액션이 APPROVAL 티어로 정의되어 있다 (EIP-712 서명은 Phase 321에서 완성)"
    - "unset_agent_wallet 액션이 APPROVAL 티어로 unsetAgentWallet calldata를 인코딩한 ContractCallRequest를 생성한다"
    - "registerBuiltInProviders에 erc8004_agent가 등록되고 actions.erc8004_agent_enabled 설정으로 활성화/비활성화된다"
  artifacts:
    - path: "packages/actions/src/providers/erc8004/index.ts"
      provides: "Erc8004ActionProvider 클래스 (IActionProvider 구현)"
      exports: ["Erc8004ActionProvider"]
      min_lines: 150
    - path: "packages/actions/src/providers/erc8004/registration-file.ts"
      provides: "등록 파일 JSON 생성 유틸리티"
      exports: ["buildRegistrationFile"]
    - path: "packages/actions/src/index.ts"
      provides: "registerBuiltInProviders에 erc8004_agent 항목 추가"
      contains: "erc8004_agent"
    - path: "packages/actions/src/__tests__/erc8004-provider.test.ts"
      provides: "ActionProvider 단위 테스트 (8 액션 resolve + feature gate)"
      min_lines: 200
  key_links:
    - from: "packages/actions/src/providers/erc8004/index.ts"
      to: "packages/actions/src/providers/erc8004/erc8004-registry-client.ts"
      via: "RegistryClient 인스턴스를 사용하여 calldata 인코딩"
      pattern: "this\\.client\\.encode"
    - from: "packages/actions/src/providers/erc8004/index.ts"
      to: "packages/actions/src/providers/erc8004/schemas.ts"
      via: "각 액션의 inputSchema로 Zod 스키마 사용"
      pattern: "RegisterAgentInputSchema|GiveFeedbackInputSchema"
    - from: "packages/actions/src/index.ts"
      to: "packages/actions/src/providers/erc8004/index.ts"
      via: "registerBuiltInProviders factory"
      pattern: "Erc8004ActionProvider"
---

<objective>
Erc8004ActionProvider: 8개 write 액션을 IActionProvider로 구현하고, registerBuiltInProviders에 등록하여 AI 에이전트가 온체인 Identity/Reputation/Validation 레지스트리에 쓰기 작업을 수행할 수 있는 상태를 만든다.

Purpose: AI 에이전트가 POST /v1/actions/erc8004_agent/{action} 경로로 ERC-8004 레지스트리 트랜잭션을 생성하고, 기존 6-stage 파이프라인을 통해 정책 평가 + 서명 + 제출이 이뤄지도록 한다.
Output: 4개 파일 (ActionProvider, registration-file util, index.ts 업데이트, 테스트)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/318-actionprovider-registry-client/318-01-SUMMARY.md

@packages/actions/src/providers/zerox-swap/index.ts (ActionProvider resolve 패턴)
@packages/actions/src/index.ts (registerBuiltInProviders 등록 패턴)
@packages/core/src/interfaces/action-provider.types.ts (IActionProvider 인터페이스)
@internal/objectives/m30-08-erc8004-trustless-agents.md (설계 문서 -- 액션 정의 + resolve 플로우)

<interfaces>
<!-- 318-01에서 생성된 계약: Plan 01 SUMMARY에서 확인 -->

From packages/actions/src/providers/erc8004/erc8004-registry-client.ts:
```typescript
export class Erc8004RegistryClient {
  constructor(config: Erc8004Config);
  getRegistryAddress(registry: 'identity' | 'reputation' | 'validation'): `0x${string}`;
  encodeRegister(agentURI: string): `0x${string}`;
  encodeRegisterWithMetadata(agentURI: string, metadata: Array<{ key: string; value: string }>): `0x${string}`;
  encodeSetAgentWallet(agentId: bigint, newWallet: `0x${string}`, deadline: bigint, signature: `0x${string}`): `0x${string}`;
  encodeUnsetAgentWallet(agentId: bigint): `0x${string}`;
  encodeSetAgentURI(agentId: bigint, newURI: string): `0x${string}`;
  encodeSetMetadata(agentId: bigint, key: string, value: string): `0x${string}`;
  encodeGiveFeedback(agentId: bigint, value: bigint, valueDecimals: number, tag1: string, tag2: string, endpoint: string, feedbackURI: string, feedbackHash: `0x${string}`): `0x${string}`;
  encodeRevokeFeedback(agentId: bigint, feedbackIndex: bigint): `0x${string}`;
  encodeValidationRequest(validatorAddress: `0x${string}`, agentId: bigint, requestURI: string, requestHash: `0x${string}`): `0x${string}`;
}
```

From packages/actions/src/providers/erc8004/schemas.ts:
```typescript
export const RegisterAgentInputSchema: z.ZodObject<...>;
export const SetAgentWalletInputSchema: z.ZodObject<...>;
export const UnsetAgentWalletInputSchema: z.ZodObject<...>;
export const SetAgentUriInputSchema: z.ZodObject<...>;
export const SetMetadataInputSchema: z.ZodObject<...>;
export const GiveFeedbackInputSchema: z.ZodObject<...>;
export const RevokeFeedbackInputSchema: z.ZodObject<...>;
export const RequestValidationInputSchema: z.ZodObject<...>;
```

From packages/actions/src/providers/erc8004/config.ts:
```typescript
export interface Erc8004Config {
  enabled: boolean;
  identityRegistryAddress: string;
  reputationRegistryAddress: string;
  validationRegistryAddress: string;
  registrationFileBaseUrl: string;
  autoPublishRegistration: boolean;
  reputationCacheTtlSec: number;
}
export const ERC8004_DEFAULTS: Erc8004Config;
```

From packages/core/src/interfaces/action-provider.types.ts:
```typescript
export interface IActionProvider {
  readonly metadata: ActionProviderMetadata;
  readonly actions: readonly ActionDefinition[];
  resolve(actionName: string, params: Record<string, unknown>, context: ActionContext): Promise<ContractCallRequest | ContractCallRequest[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: registration-file.ts + Erc8004ActionProvider 구현</name>
  <files>
    packages/actions/src/providers/erc8004/registration-file.ts
    packages/actions/src/providers/erc8004/index.ts
  </files>
  <action>
    1. **registration-file.ts**: 등록 파일 JSON을 생성하는 유틸리티.

    ```typescript
    export interface RegistrationFileOptions {
      name: string;
      description?: string;
      services?: Array<{ name: string; endpoint: string; version?: string }>;
      agentId?: string;
      chainId?: number;
      identityRegistryAddress?: string;
      baseUrl?: string;
    }

    export function buildRegistrationFile(options: RegistrationFileOptions): Record<string, unknown>
    ```

    반환 JSON 형식 (설계 문서 IDEN-05 참조):
    ```json
    {
      "type": "https://eips.ethereum.org/EIPS/eip-8004#registration-v1",
      "name": "{options.name}",
      "description": "{options.description || 'WAIaaS-managed AI agent wallet'}",
      "services": [기본: mcp + rest-api endpoints from baseUrl, + options.services],
      "active": true,
      "registrations": [agentId/registry가 있으면 포함],
      "supportedTrust": ["reputation"]
    }
    ```

    baseUrl이 제공되면 기본 services에 `{baseUrl}/mcp` (MCP) + `{baseUrl}/v1` (REST API) 자동 추가.

    2. **index.ts (Erc8004ActionProvider)**:

    ZeroExSwapActionProvider 패턴을 따르는 IActionProvider 구현.

    metadata:
    ```typescript
    {
      name: 'erc8004_agent',
      description: 'ERC-8004 Trustless Agents — identity registration, reputation management, and on-chain validation',
      version: '1.0.0',
      chains: ['ethereum'],
      mcpExpose: true,
      requiresApiKey: false,
      requiredApis: [],
    }
    ```

    actions 배열 (8개):
    | name | description (20자 이상) | chain | inputSchema | riskLevel | defaultTier |
    |------|------------------------|-------|-------------|-----------|-------------|
    | register_agent | Register agent identity on ERC-8004 Identity Registry (NFT minting) | ethereum | RegisterAgentInputSchema | high | APPROVAL |
    | set_agent_wallet | Link agent wallet address via EIP-712 owner signature on Identity Registry | ethereum | SetAgentWalletInputSchema | high | APPROVAL |
    | unset_agent_wallet | Unlink agent wallet from Identity Registry entry | ethereum | UnsetAgentWalletInputSchema | high | APPROVAL |
    | set_agent_uri | Update agent registration file URI on Identity Registry | ethereum | SetAgentUriInputSchema | medium | DELAY |
    | set_metadata | Set or update agent metadata key-value pair on Identity Registry | ethereum | SetMetadataInputSchema | low | NOTIFY |
    | give_feedback | Post reputation feedback for another agent on Reputation Registry | ethereum | GiveFeedbackInputSchema | low | NOTIFY |
    | revoke_feedback | Revoke previously posted feedback on Reputation Registry | ethereum | RevokeFeedbackInputSchema | low | INSTANT |
    | request_validation | Request on-chain validation for an agent via Validation Registry | ethereum | RequestValidationInputSchema | medium | DELAY |

    resolve() 구현:
    - actionName으로 switch 분기
    - 각 case에서 해당 InputSchema.parse(params) 후 client.encode* 호출
    - ContractCallRequest 반환: { type: 'CONTRACT_CALL', to: registryAddress, calldata }
    - 알 수 없는 actionName → ChainError('INVALID_INSTRUCTION')

    **register_agent resolve 상세:**
    1. RegisterAgentInputSchema.parse(params)
    2. buildRegistrationFile({ name, description, services, baseUrl: config.registrationFileBaseUrl })로 등록 파일 JSON 생성
    3. agentURI 결정: config.registrationFileBaseUrl이 있으면 `{baseUrl}/v1/erc8004/registration-file/{context.walletId}`, 없으면 빈 문자열
    4. metadata가 있으면 encodeRegisterWithMetadata, 없으면 encodeRegister 사용
    5. to: client.getRegistryAddress('identity')

    **set_agent_wallet resolve 상세:**
    set_agent_wallet은 Phase 321에서 EIP-712 서명 흐름이 완성된다. 이 단계에서는 placeholder 구현:
    1. SetAgentWalletInputSchema.parse(params)
    2. agentId를 BigInt로 변환
    3. context.walletAddress를 newWallet로 사용
    4. deadline: params.deadline ? BigInt(params.deadline) : BigInt(Math.floor(Date.now() / 1000) + 3600)
    5. signature: '0x' (placeholder -- Phase 321에서 EIP-712 승인 흐름으로 교체)
    6. client.encodeSetAgentWallet(agentId, newWallet, deadline, signature)
    7. to: client.getRegistryAddress('identity')
    NOTE: resolve() 주석에 "Phase 321: EIP-712 signature will be provided via ApprovalWorkflow" 명시

    **request_validation resolve 상세:**
    1. RequestValidationInputSchema.parse(params)
    2. client.getRegistryAddress('validation') -- 빈 주소면 ChainError 발생 (feature gate)
    3. requestURI: JSON.stringify({ agentId, description: params.requestDescription, tag: params.tag, timestamp: Date.now() })
    4. requestHash: keccak256(toHex(requestURI)) (viem keccak256)
    5. to: client.getRegistryAddress('validation')

    **give_feedback resolve 상세:**
    1. GiveFeedbackInputSchema.parse(params)
    2. feedbackHash: params.feedbackURI ? keccak256(toHex(params.feedbackURI)) : '0x' + '0'.repeat(64) (빈 해시)
    3. client.encodeGiveFeedback with BigInt conversions
    4. to: client.getRegistryAddress('reputation')

    생성자에서 Erc8004RegistryClient를 config로 초기화.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions run build</automated>
  </verify>
  <done>Erc8004ActionProvider가 8개 액션을 정의하고, 각 resolve()가 올바른 ContractCallRequest를 반환한다. registration-file.ts가 등록 파일 JSON을 생성한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: registerBuiltInProviders 등록 + 전체 테스트</name>
  <files>
    packages/actions/src/index.ts
    packages/actions/src/__tests__/erc8004-provider.test.ts
  </files>
  <behavior>
    - registerBuiltInProviders가 actions.erc8004_agent_enabled='true'일 때 erc8004_agent를 loaded에 포함
    - registerBuiltInProviders가 actions.erc8004_agent_enabled='false'(기본)일 때 erc8004_agent를 skipped에 포함
    - Erc8004ActionProvider.metadata.name === 'erc8004_agent'
    - Erc8004ActionProvider.metadata.mcpExpose === true
    - Erc8004ActionProvider.actions.length === 8
    - resolve('register_agent', { name: 'Test Agent' }, context) returns ContractCallRequest with type 'CONTRACT_CALL' and to === identity registry address
    - resolve('set_agent_uri', { agentId: '42', uri: 'https://example.com/agent.json' }, context) returns ContractCallRequest with to === identity registry address
    - resolve('set_metadata', { agentId: '42', key: 'version', value: '1.0' }, context) returns ContractCallRequest
    - resolve('give_feedback', { agentId: '42', value: 80, ... }, context) returns ContractCallRequest with to === reputation registry address
    - resolve('revoke_feedback', { agentId: '42', feedbackIndex: 0 }, context) returns ContractCallRequest
    - resolve('request_validation', ..., context) throws when validation registry address is empty
    - resolve('request_validation', { agentId: '42', validatorAddress: '0x...', requestDescription: 'test' }, context) returns ContractCallRequest when validation address configured
    - resolve('set_agent_wallet', { agentId: '42' }, context) returns ContractCallRequest (placeholder signature)
    - resolve('unset_agent_wallet', { agentId: '42' }, context) returns ContractCallRequest
    - resolve('unknown_action', ...) throws ChainError INVALID_INSTRUCTION
    - Invalid input (missing required fields) throws ZodError
    - buildRegistrationFile produces valid JSON with type, name, services, active fields
  </behavior>
  <action>
    1. **packages/actions/src/index.ts 수정:**

    파일 상단에 import 추가:
    ```typescript
    import { Erc8004ActionProvider } from './providers/erc8004/index.js';
    import { type Erc8004Config, ERC8004_DEFAULTS } from './providers/erc8004/config.js';
    ```

    Re-export 섹션에 추가:
    ```typescript
    export { Erc8004ActionProvider } from './providers/erc8004/index.js';
    export { ERC8004_DEFAULTS, ERC8004_MAINNET_ADDRESSES } from './providers/erc8004/config.js';
    export type { Erc8004Config } from './providers/erc8004/config.js';
    export { Erc8004RegistryClient } from './providers/erc8004/erc8004-registry-client.js';
    export { buildRegistrationFile } from './providers/erc8004/registration-file.js';
    ```
    (ERC8004_MAINNET_ADDRESSES는 constants.ts에서 export하므로 config.ts가 아닌 constants.ts에서 가져옴 -- 정확한 import 경로는 318-01 구현에 따라 조정)

    providers 배열에 추가 (drift_perp 다음):
    ```typescript
    {
      key: 'erc8004_agent',
      enabledKey: 'actions.erc8004_agent_enabled',
      factory: () => {
        const config: Erc8004Config = {
          ...ERC8004_DEFAULTS,
          enabled: true,
          identityRegistryAddress: settingsReader.get('actions.erc8004_identity_registry_address'),
          reputationRegistryAddress: settingsReader.get('actions.erc8004_reputation_registry_address'),
          validationRegistryAddress: settingsReader.get('actions.erc8004_validation_registry_address'),
          registrationFileBaseUrl: settingsReader.get('actions.erc8004_registration_file_base_url'),
          autoPublishRegistration: settingsReader.get('actions.erc8004_auto_publish_registration') === 'true',
          reputationCacheTtlSec: Number(settingsReader.get('actions.erc8004_reputation_cache_ttl_sec')) || 300,
        };
        return new Erc8004ActionProvider(config);
      },
    },
    ```

    2. **erc8004-provider.test.ts**: 종합 테스트.

    zerox-swap.test.ts 패턴을 따르되, ERC-8004는 외부 API 호출이 없으므로 msw 불필요. viem의 decodeFunctionData로 calldata 역검증.

    테스트 구조:
    - describe('Erc8004ActionProvider')
      - describe('metadata') -- name, version, chains, mcpExpose, actions.length
      - describe('actions definitions') -- 각 액션의 riskLevel, defaultTier 확인
      - describe('resolve: register_agent') -- 유효 입력 → ContractCallRequest, 무효 입력 → ZodError
      - describe('resolve: set_agent_wallet') -- placeholder 동작 확인
      - describe('resolve: unset_agent_wallet') -- calldata 확인
      - describe('resolve: set_agent_uri') -- calldata 확인
      - describe('resolve: set_metadata') -- calldata 확인
      - describe('resolve: give_feedback') -- calldata 확인 (reputation registry)
      - describe('resolve: revoke_feedback') -- calldata 확인
      - describe('resolve: request_validation') -- feature gate 확인, 유효 설정 시 calldata 확인
      - describe('resolve: unknown action') -- ChainError
      - describe('registration-file') -- buildRegistrationFile JSON 형식 확인
    - describe('registerBuiltInProviders integration')
      - 'true' → loaded에 포함
      - 'false' → skipped에 포함

    mock SettingsReader: Map 기반 간단한 구현 (기존 테스트 패턴 참조).
    mock ActionContext: { walletAddress: '0x1234...', chain: 'ethereum', walletId: 'test-wallet-id' }.

    최소 20개 테스트 케이스.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions run test -- --run erc8004-provider && pnpm --filter @waiaas/actions run build</automated>
  </verify>
  <done>registerBuiltInProviders에 erc8004_agent가 등록되고, Erc8004ActionProvider의 8개 액션이 모두 올바른 ContractCallRequest를 생성하며, feature gate(erc8004_agent_enabled)가 동작하고, 20개 이상의 테스트가 통과한다.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/actions run build` -- 타입 에러 없이 빌드 성공
2. `pnpm --filter @waiaas/actions run test -- --run erc8004` -- registry-client + provider 테스트 모두 통과
3. `pnpm turbo run typecheck` -- 전체 프로젝트 타입 체크 통과
4. register_agent가 Identity Registry 주소로 ContractCallRequest 생성
5. give_feedback이 Reputation Registry 주소로 ContractCallRequest 생성
6. request_validation이 빈 주소 시 에러, 설정된 주소 시 Validation Registry로 ContractCallRequest 생성
7. registerBuiltInProviders에서 erc8004_agent_enabled='true'로 활성화 가능
</verification>

<success_criteria>
- Erc8004ActionProvider가 8개 write 액션을 정의하고 각각 올바른 ContractCallRequest를 반환
- register_agent = APPROVAL, set_agent_uri = DELAY, set_metadata = NOTIFY, give_feedback = NOTIFY, revoke_feedback = INSTANT, request_validation = DELAY, set_agent_wallet = APPROVAL, unset_agent_wallet = APPROVAL
- registerBuiltInProviders에서 actions.erc8004_agent_enabled 설정으로 토글 가능
- feature gate: validation_registry_address 비어있으면 request_validation 비활성
- 전체 빌드 + 35개 이상 테스트 통과 (318-01 15개 + 318-02 20개)
</success_criteria>

<output>
After completion, create `.planning/phases/318-actionprovider-registry-client/318-02-SUMMARY.md`
</output>
