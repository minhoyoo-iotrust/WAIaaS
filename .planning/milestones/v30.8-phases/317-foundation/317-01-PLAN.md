---
phase: 317-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/infrastructure/database/schema.ts
  - packages/daemon/src/__tests__/migration-v39.test.ts
autonomous: true
requirements: [DB-01, DB-02, DB-03, DB-04]

must_haves:
  truths:
    - "DB v39 마이그레이션이 실행되면 agent_identities 테이블이 wallet_id FK + 인덱스 2개와 함께 생성된다"
    - "DB v39 마이그레이션이 실행되면 reputation_cache 테이블이 composite PK(agent_id, registry_address, tag1, tag2)로 생성된다"
    - "DB v39 마이그레이션이 실행되면 pending_approvals에 approval_type TEXT 컬럼(기본 SIWE, CHECK IN SIWE/EIP712)이 추가된다"
    - "DB v39 마이그레이션이 실행되면 policies 테이블이 재생성되어 REPUTATION_THRESHOLD가 CHECK 제약에 포함된다"
    - "LATEST_SCHEMA_VERSION이 39이고, 기존 v38 DB에서 업그레이드가 성공한다"
  artifacts:
    - path: "packages/daemon/src/infrastructure/database/migrate.ts"
      provides: "v39 마이그레이션 함수 + LATEST_SCHEMA_VERSION=39 + DDL 업데이트"
      contains: "version: 39"
    - path: "packages/daemon/src/infrastructure/database/schema.ts"
      provides: "agentIdentities + reputationCache Drizzle 테이블 + pendingApprovals.approvalType"
      contains: "agentIdentities"
    - path: "packages/daemon/src/__tests__/migration-v39.test.ts"
      provides: "v39 마이그레이션 테스트 (테이블 생성, 데이터 보존, 업그레이드 경로)"
      min_lines: 80
  key_links:
    - from: "packages/daemon/src/infrastructure/database/migrate.ts"
      to: "packages/core/src/enums/policy.ts"
      via: "import { POLICY_TYPES } for inList() in policies CHECK"
      pattern: "inList\\(POLICY_TYPES\\)"
    - from: "packages/daemon/src/infrastructure/database/schema.ts"
      to: "packages/daemon/src/infrastructure/database/migrate.ts"
      via: "Drizzle schema matches DDL + migration output"
      pattern: "agentIdentities|reputationCache"
---

<objective>
DB v39 마이그레이션을 구현하여 ERC-8004 에이전트 신원 추적(agent_identities), 평판 캐시(reputation_cache) 테이블을 생성하고, pending_approvals에 approval_type 컬럼을 추가하며, policies 테이블을 재생성하여 REPUTATION_THRESHOLD CHECK 제약을 반영한다.

Purpose: Phase 318-323의 모든 작업이 의존하는 DB 기반을 확보한다.
Output: migrate.ts v39 마이그레이션 + schema.ts Drizzle 테이블 + DDL 업데이트 + 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/daemon/src/infrastructure/database/migrate.ts
@packages/daemon/src/infrastructure/database/schema.ts
@packages/core/src/enums/policy.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/core/src/enums/policy.ts:
```typescript
export const POLICY_TYPES = [
  'SPENDING_LIMIT', 'WHITELIST', 'TIME_RESTRICTION', 'RATE_LIMIT',
  'ALLOWED_TOKENS', 'CONTRACT_WHITELIST', 'METHOD_WHITELIST',
  'APPROVED_SPENDERS', 'APPROVE_AMOUNT_LIMIT', 'APPROVE_TIER_OVERRIDE',
  'ALLOWED_NETWORKS', 'X402_ALLOWED_DOMAINS',
  'LENDING_LTV_LIMIT', 'LENDING_ASSET_WHITELIST',
  'PERP_MAX_LEVERAGE', 'PERP_MAX_POSITION_USD', 'PERP_ALLOWED_MARKETS',
] as const;
// Will be extended to 18 types with REPUTATION_THRESHOLD in Plan 317-02
```

From packages/daemon/src/infrastructure/database/migrate.ts (v38, current latest):
```typescript
export const LATEST_SCHEMA_VERSION = 38;

// Migration pattern:
MIGRATIONS.push({
  version: 38,
  description: 'Add smart account columns to wallets table ...',
  up: (sqlite) => { ... },
});
```

From packages/daemon/src/infrastructure/database/schema.ts:
```typescript
export const pendingApprovals = sqliteTable('pending_approvals', {
  id: text('id').primaryKey(),
  txId: text('tx_id').notNull().references(() => transactions.id, { onDelete: 'cascade' }),
  requiredBy: integer('required_by', { mode: 'timestamp' }).notNull(),
  expiresAt: integer('expires_at', { mode: 'timestamp' }).notNull(),
  approvedAt: integer('approved_at', { mode: 'timestamp' }),
  rejectedAt: integer('rejected_at', { mode: 'timestamp' }),
  ownerSignature: text('owner_signature'),
  approvalChannel: text('approval_channel').default('rest_api'),
  createdAt: integer('created_at', { mode: 'timestamp' }).notNull(),
});

export const policies = sqliteTable('policies', {
  id: text('id').primaryKey(),
  walletId: text('wallet_id').references(() => wallets.id, { onDelete: 'cascade' }),
  type: text('type').notNull(),
  rules: text('rules').notNull(),
  priority: integer('priority').notNull().default(0),
  enabled: integer('enabled', { mode: 'boolean' }).notNull().default(true),
  network: text('network'),
  createdAt: integer('created_at', { mode: 'timestamp' }).notNull(),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).notNull(),
});
```

policies 테이블 재생성 패턴 (v11, v20, v27, v33 등에서 검증됨):
```sql
CREATE TABLE policies_new (...);
INSERT INTO policies_new SELECT * FROM policies;
DROP TABLE policies;
ALTER TABLE policies_new RENAME TO policies;
-- Recreate indexes
```

getCreateTableStatements() DDL에서 pending_approvals:
```sql
CREATE TABLE IF NOT EXISTS pending_approvals (
  id TEXT PRIMARY KEY,
  tx_id TEXT NOT NULL REFERENCES transactions(id) ON DELETE CASCADE,
  required_by INTEGER NOT NULL,
  expires_at INTEGER NOT NULL,
  approved_at INTEGER,
  rejected_at INTEGER,
  owner_signature TEXT,
  approval_channel TEXT DEFAULT 'rest_api',
  created_at INTEGER NOT NULL
)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB v39 마이그레이션 구현 (migrate.ts + schema.ts + DDL)</name>
  <files>
    packages/daemon/src/infrastructure/database/migrate.ts
    packages/daemon/src/infrastructure/database/schema.ts
  </files>
  <behavior>
    - Test 1: v38 DB에서 v39 마이그레이션 실행 후 agent_identities 테이블이 존재하고 올바른 컬럼(id, wallet_id, chain_agent_id, registry_address, chain_id, agent_uri, registration_file_url, status, created_at, updated_at)을 갖는다
    - Test 2: v38 DB에서 v39 마이그레이션 실행 후 reputation_cache 테이블이 존재하고 composite PK(agent_id, registry_address, tag1, tag2)를 갖는다
    - Test 3: v38 DB에서 v39 마이그레이션 실행 후 pending_approvals.approval_type 컬럼이 존재하고 기본값 'SIWE'이며 CHECK IN ('SIWE', 'EIP712')를 만족한다
    - Test 4: v38 DB에서 v39 마이그레이션 실행 후 policies 테이블의 CHECK 제약에 REPUTATION_THRESHOLD가 포함된다
    - Test 5: 기존 policies 데이터가 재생성 후에도 보존된다 (INSERT → DROP → RENAME 무결성)
    - Test 6: Fresh DB (pushSchema)에서 agent_identities, reputation_cache 테이블이 생성되고 LATEST_SCHEMA_VERSION=39이다
    - Test 7: agent_identities에 idx_agent_identities_wallet과 idx_agent_identities_chain 인덱스가 존재한다
    - Test 8: agent_identities.status CHECK('PENDING','REGISTERED','WALLET_LINKED','DEREGISTERED')가 동작한다
  </behavior>
  <action>
**1. packages/core/src/enums/policy.ts 변경은 Plan 317-02에서 수행한다.** 이 task는 POLICY_TYPES 배열에 이미 REPUTATION_THRESHOLD가 추가되어 있다고 가정하므로, **Plan 317-02가 먼저 실행되어야 한다 -- 또는 이 task 내에서 직접 추가해야 한다.** 실제로는 같은 wave 1이므로 이 task에서 먼저 `POLICY_TYPES`에 `'REPUTATION_THRESHOLD'`를 추가한다 (Plan 02에서 다시 확인).

단, **Plan 317-02와 파일 충돌을 방지하기 위해** policy.ts 수정은 이 task에서 하되, enum 테스트 업데이트는 Plan 02에서 수행한다.

**packages/core/src/enums/policy.ts:**
- POLICY_TYPES 배열 끝에 `'REPUTATION_THRESHOLD'`를 추가하여 18번째 타입으로 만든다.

**2. packages/daemon/src/infrastructure/database/migrate.ts:**

LATEST_SCHEMA_VERSION을 38 -> 39로 변경.

MIGRATIONS.push로 v39 마이그레이션 추가:

```typescript
MIGRATIONS.push({
  version: 39,
  description: 'ERC-8004: agent_identities + reputation_cache + pending_approvals.approval_type + policies CHECK update',
  up: (sqlite) => {
    // Step 1: Create agent_identities table
    sqlite.exec(`CREATE TABLE IF NOT EXISTS agent_identities (
  id TEXT PRIMARY KEY,
  wallet_id TEXT NOT NULL REFERENCES wallets(id) ON DELETE CASCADE,
  chain_agent_id TEXT NOT NULL,
  registry_address TEXT NOT NULL,
  chain_id INTEGER NOT NULL,
  agent_uri TEXT,
  registration_file_url TEXT,
  status TEXT NOT NULL DEFAULT 'PENDING'
    CHECK (status IN ('PENDING', 'REGISTERED', 'WALLET_LINKED', 'DEREGISTERED')),
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
)`);
    sqlite.exec('CREATE INDEX idx_agent_identities_wallet ON agent_identities(wallet_id)');
    sqlite.exec('CREATE UNIQUE INDEX idx_agent_identities_chain ON agent_identities(registry_address, chain_agent_id)');

    // Step 2: Create reputation_cache table
    sqlite.exec(`CREATE TABLE IF NOT EXISTS reputation_cache (
  agent_id TEXT NOT NULL,
  registry_address TEXT NOT NULL,
  tag1 TEXT NOT NULL DEFAULT '',
  tag2 TEXT NOT NULL DEFAULT '',
  score INTEGER NOT NULL,
  score_decimals INTEGER NOT NULL DEFAULT 0,
  feedback_count INTEGER NOT NULL DEFAULT 0,
  cached_at INTEGER NOT NULL,
  PRIMARY KEY (agent_id, registry_address, tag1, tag2)
)`);

    // Step 3: Add approval_type to pending_approvals
    sqlite.exec("ALTER TABLE pending_approvals ADD COLUMN approval_type TEXT NOT NULL DEFAULT 'SIWE' CHECK (approval_type IN ('SIWE', 'EIP712'))");

    // Step 4: Recreate policies with updated CHECK (includes REPUTATION_THRESHOLD)
    sqlite.exec(`CREATE TABLE policies_new (
  id TEXT PRIMARY KEY,
  wallet_id TEXT REFERENCES wallets(id) ON DELETE CASCADE,
  type TEXT NOT NULL CHECK (type IN (${inList(POLICY_TYPES)})),
  rules TEXT NOT NULL,
  priority INTEGER NOT NULL DEFAULT 0,
  enabled INTEGER NOT NULL DEFAULT 1,
  network TEXT CHECK (network IS NULL OR network IN (${inList(NETWORK_TYPES)})),
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
)`);
    sqlite.exec('INSERT INTO policies_new SELECT * FROM policies');
    sqlite.exec('DROP TABLE policies');
    sqlite.exec('ALTER TABLE policies_new RENAME TO policies');
    sqlite.exec('CREATE INDEX idx_policies_wallet_enabled ON policies(wallet_id, enabled)');
    sqlite.exec('CREATE INDEX idx_policies_type ON policies(type)');
    sqlite.exec('CREATE INDEX idx_policies_network ON policies(network)');
  },
});
```

**주의사항 (C3 from STATE.md):** policies 테이블 재생성 시 FK cascade 주의. `INSERT INTO policies_new SELECT * FROM policies` 패턴은 기존 v27, v33 마이그레이션에서 검증됨. policies가 다른 테이블을 참조하지 않으므로 안전.

**3. getCreateTableStatements() DDL 업데이트:**

기존 pending_approvals DDL에 `approval_type TEXT NOT NULL DEFAULT 'SIWE' CHECK (approval_type IN ('SIWE', 'EIP712'))` 컬럼을 추가한다.

agent_identities와 reputation_cache CREATE TABLE IF NOT EXISTS 문을 추가한다.

getCreateIndexStatements()에 agent_identities 인덱스 2개를 추가한다.

**4. packages/daemon/src/infrastructure/database/schema.ts:**

agentIdentities 테이블 추가:
```typescript
export const agentIdentities = sqliteTable('agent_identities', {
  id: text('id').primaryKey(),
  walletId: text('wallet_id').notNull().references(() => wallets.id, { onDelete: 'cascade' }),
  chainAgentId: text('chain_agent_id').notNull(),
  registryAddress: text('registry_address').notNull(),
  chainId: integer('chain_id').notNull(),
  agentUri: text('agent_uri'),
  registrationFileUrl: text('registration_file_url'),
  status: text('status').notNull().default('PENDING'),
  createdAt: integer('created_at', { mode: 'timestamp' }).notNull(),
  updatedAt: integer('updated_at', { mode: 'timestamp' }).notNull(),
});
```

reputationCache 테이블 추가:
```typescript
export const reputationCache = sqliteTable('reputation_cache', {
  agentId: text('agent_id').notNull(),
  registryAddress: text('registry_address').notNull(),
  tag1: text('tag1').notNull().default(''),
  tag2: text('tag2').notNull().default(''),
  score: integer('score').notNull(),
  scoreDecimals: integer('score_decimals').notNull().default(0),
  feedbackCount: integer('feedback_count').notNull().default(0),
  cachedAt: integer('cached_at', { mode: 'timestamp' }).notNull(),
}, (table) => [
  primaryKey({ columns: [table.agentId, table.registryAddress, table.tag1, table.tag2] }),
]);
```

pendingApprovals에 approvalType 추가:
```typescript
approvalType: text('approval_type').notNull().default('SIWE'),
```

**5. schema.ts의 index.ts에서 agentIdentities, reputationCache를 export에 추가.**
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon exec vitest run --reporter=verbose packages/daemon/src/__tests__/migration-v39.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - LATEST_SCHEMA_VERSION=39
    - v39 마이그레이션이 v38 DB를 agent_identities + reputation_cache + approval_type + policies REPUTATION_THRESHOLD CHECK로 업그레이드
    - Fresh DB에 모든 테이블과 인덱스가 생성
    - Drizzle schema에 agentIdentities, reputationCache 테이블 추가, pendingApprovals.approvalType 추가
    - 기존 마이그레이션 테스트(migration-chain, schema-compatibility 등) 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 기존 마이그레이션 테스트 LATEST_SCHEMA_VERSION 업데이트</name>
  <files>
    packages/daemon/src/__tests__/migration-v34-v35.test.ts
    packages/daemon/src/__tests__/migration-v14.test.ts
    packages/daemon/src/__tests__/migration-v33.test.ts
    packages/daemon/src/__tests__/signing-sdk-migration.test.ts
    packages/daemon/src/__tests__/schema-compatibility.test.ts
    packages/daemon/src/__tests__/migration-v6-v8.test.ts
    packages/daemon/src/__tests__/migration-chain.test.ts
    packages/daemon/src/__tests__/audit-helper.test.ts
    packages/daemon/src/__tests__/upgrade-flow-e2e.test.ts
    packages/daemon/src/__tests__/settings-schema-migration.test.ts
  </files>
  <action>
기존 마이그레이션 테스트 파일들에 `expect(LATEST_SCHEMA_VERSION).toBe(38)` assertion이 있다. 모두 `toBe(39)`로 업데이트한다.

대상 파일 (10개):
1. `migration-v34-v35.test.ts` (line ~149-150)
2. `migration-v14.test.ts` (line ~245-246)
3. `migration-v33.test.ts` (line ~127-128)
4. `signing-sdk-migration.test.ts` (line ~67)
5. `schema-compatibility.test.ts` (line ~453-454)
6. `migration-v6-v8.test.ts` (line ~522-523)
7. `migration-chain.test.ts` (line ~1007, ~1808-1813)
8. `audit-helper.test.ts` (line ~145-146)
9. `upgrade-flow-e2e.test.ts` (LATEST_SCHEMA_VERSION 비교)
10. `settings-schema-migration.test.ts` (line ~306-307)

각 파일에서 `toBe(38)` → `toBe(39)` 변경. 테스트 설명 문자열에 38이 포함된 경우도 39로 업데이트.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon exec vitest run --reporter=verbose packages/daemon/src/__tests__/schema-compatibility.test.ts packages/daemon/src/__tests__/migration-chain.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 기존 10개 테스트 파일의 LATEST_SCHEMA_VERSION assertion이 39를 기대
    - schema-compatibility, migration-chain 테스트가 통과
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/core run build` -- 코어 빌드 성공 (POLICY_TYPES 변경)
2. `pnpm --filter @waiaas/daemon run build` -- 데몬 빌드 성공 (migrate.ts + schema.ts 변경)
3. `pnpm --filter @waiaas/daemon exec vitest run --reporter=verbose packages/daemon/src/__tests__/migration-v39.test.ts` -- v39 전용 테스트 통과
4. `pnpm --filter @waiaas/daemon exec vitest run --reporter=verbose packages/daemon/src/__tests__/migration-chain.test.ts` -- 체인 마이그레이션 통과
5. `pnpm --filter @waiaas/daemon exec vitest run --reporter=verbose packages/daemon/src/__tests__/schema-compatibility.test.ts` -- 호환성 통과
</verification>

<success_criteria>
- LATEST_SCHEMA_VERSION=39
- agent_identities 테이블: 10 컬럼 + 2 인덱스 + FK(wallets) + status CHECK
- reputation_cache 테이블: 8 컬럼 + composite PK
- pending_approvals.approval_type: TEXT, default 'SIWE', CHECK IN ('SIWE', 'EIP712')
- policies 재생성: REPUTATION_THRESHOLD가 CHECK 제약에 포함
- 기존 마이그레이션 체인 테스트 모두 통과
- Drizzle 스키마가 DDL과 일치
</success_criteria>

<output>
After completion, create `.planning/phases/317-foundation/317-01-SUMMARY.md`
</output>
