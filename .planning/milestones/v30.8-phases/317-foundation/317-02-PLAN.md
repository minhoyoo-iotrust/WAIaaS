---
phase: 317-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/enums/notification.ts
  - packages/core/src/schemas/signing-protocol.ts
  - packages/core/src/schemas/policy.schema.ts
  - packages/core/src/__tests__/enums.test.ts
  - packages/core/src/__tests__/signing-protocol.test.ts
  - packages/core/src/i18n/en.ts
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/daemon/src/__tests__/settings-service.test.ts
autonomous: true
requirements: [PKG-03, NOTIF-01]

must_haves:
  truths:
    - "POLICY_TYPES 배열의 18번째 항목이 REPUTATION_THRESHOLD이고, PolicyTypeEnum이 이를 검증한다"
    - "NOTIFICATION_EVENT_TYPES에 5개 ERC-8004 이벤트가 추가되어 총 54개이고, EVENT_CATEGORY_MAP과 EVENT_DESCRIPTIONS에 매핑된다"
    - "Admin Settings에 actions.erc8004_* 9개 키가 정의되고, feature gate(erc8004_agent_enabled)의 기본값이 'false'이다"
    - "REPUTATION_THRESHOLD의 rules 스키마가 POLICY_RULES_SCHEMAS에 등록되어 CreatePolicyRequestSchema에서 검증된다"
    - "NOTIFICATION_CATEGORIES에 'identity' 카테고리가 추가되어 ERC-8004 이벤트가 분류된다"
  artifacts:
    - path: "packages/core/src/enums/notification.ts"
      provides: "5개 ERC-8004 NotificationEventType 추가"
      contains: "AGENT_REGISTERED"
    - path: "packages/core/src/schemas/signing-protocol.ts"
      provides: "EVENT_CATEGORY_MAP + EVENT_DESCRIPTIONS에 5개 이벤트 매핑"
      contains: "AGENT_REGISTERED.*identity"
    - path: "packages/core/src/schemas/policy.schema.ts"
      provides: "ReputationThresholdRulesSchema + POLICY_RULES_SCHEMAS 등록"
      contains: "REPUTATION_THRESHOLD"
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "9개 ERC-8004 Admin Settings 정의"
      contains: "actions.erc8004_agent_enabled"
  key_links:
    - from: "packages/core/src/schemas/policy.schema.ts"
      to: "packages/core/src/enums/policy.ts"
      via: "PolicyTierEnum import for below_threshold_tier/unrated_tier"
      pattern: "PolicyTierEnum"
    - from: "packages/core/src/schemas/signing-protocol.ts"
      to: "packages/core/src/enums/notification.ts"
      via: "EVENT_CATEGORY_MAP keys must match NOTIFICATION_EVENT_TYPES"
      pattern: "AGENT_REGISTERED|AGENT_WALLET_LINKED"
    - from: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      to: "packages/daemon/src/infrastructure/config/loader.ts"
      via: "configPath maps to DaemonConfigSchema"
      pattern: "actions\\.erc8004"
---

<objective>
ERC-8004 코어 타입(PolicyType REPUTATION_THRESHOLD, 5개 NotificationEventType, ReputationThresholdRulesSchema)을 정의하고, Admin Settings에 9개 설정 키를 등록한다.

Purpose: Phase 318-323의 ActionProvider, 정책 엔진, 알림 서비스가 참조할 코어 타입과 설정 인프라를 확보한다.
Output: @waiaas/core enum/schema 확장 + Admin Settings 9키 + 테스트 업데이트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/core/src/enums/policy.ts
@packages/core/src/enums/notification.ts
@packages/core/src/schemas/signing-protocol.ts
@packages/core/src/schemas/policy.schema.ts
@packages/core/src/__tests__/enums.test.ts
@packages/core/src/__tests__/signing-protocol.test.ts
@packages/daemon/src/infrastructure/settings/setting-keys.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/core/src/enums/policy.ts:
```typescript
export const POLICY_TYPES = [
  'SPENDING_LIMIT', 'WHITELIST', 'TIME_RESTRICTION', 'RATE_LIMIT',
  'ALLOWED_TOKENS', 'CONTRACT_WHITELIST', 'METHOD_WHITELIST',
  'APPROVED_SPENDERS', 'APPROVE_AMOUNT_LIMIT', 'APPROVE_TIER_OVERRIDE',
  'ALLOWED_NETWORKS', 'X402_ALLOWED_DOMAINS',
  'LENDING_LTV_LIMIT', 'LENDING_ASSET_WHITELIST',
  'PERP_MAX_LEVERAGE', 'PERP_MAX_POSITION_USD', 'PERP_ALLOWED_MARKETS',
] as const; // 17 types -> will be 18 with REPUTATION_THRESHOLD
export type PolicyType = (typeof POLICY_TYPES)[number];
export const PolicyTypeEnum = z.enum(POLICY_TYPES);

export const POLICY_TIERS = ['INSTANT', 'NOTIFY', 'DELAY', 'APPROVAL'] as const;
export const PolicyTierEnum = z.enum(POLICY_TIERS);
```

From packages/core/src/enums/notification.ts:
```typescript
export const NOTIFICATION_EVENT_TYPES = [
  'TX_REQUESTED', ... 'SESSION_IDLE',  // 49 events total
] as const;
```

From packages/core/src/schemas/signing-protocol.ts:
```typescript
export const NOTIFICATION_CATEGORIES = [
  'transaction', 'policy', 'security_alert', 'session', 'owner', 'system', 'defi_monitoring',
] as const; // 7 categories

export const EVENT_CATEGORY_MAP: Record<NotificationEventType, NotificationCategory> = {
  TX_REQUESTED: 'transaction', ...
};

export const EVENT_DESCRIPTIONS: Record<NotificationEventType, string> = {
  TX_REQUESTED: 'Transaction request received', ...
};
```

From packages/core/src/schemas/policy.schema.ts:
```typescript
const POLICY_RULES_SCHEMAS: Record<string, z.ZodTypeAny> = {
  ALLOWED_TOKENS: AllowedTokensRulesSchema,
  CONTRACT_WHITELIST: ContractWhitelistRulesSchema,
  // ... 12 entries
};
```

From packages/daemon/src/infrastructure/settings/setting-keys.ts:
```typescript
export interface SettingDefinition {
  key: string;
  category: string;
  configPath: string;
  defaultValue: string;
  isCredential: boolean;
}

export const SETTING_DEFINITIONS: readonly SettingDefinition[] = [
  // ... actions category entries at the end
];
```

From packages/core/src/__tests__/enums.test.ts:
```typescript
it('PolicyType has 17 values', () => { expect(POLICY_TYPES).toHaveLength(17); });
it('NotificationEventType has 49 values', () => { expect(NOTIFICATION_EVENT_TYPES).toHaveLength(49); });
```

From packages/core/src/__tests__/signing-protocol.test.ts:
```typescript
it('NOTIFICATION_CATEGORIES has 7 values including defi_monitoring', () => {
  expect(NOTIFICATION_CATEGORIES).toHaveLength(7);
});
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 코어 Enum 확장 + ReputationThreshold 정책 스키마 + 알림 이벤트 5종</name>
  <files>
    packages/core/src/enums/notification.ts
    packages/core/src/schemas/signing-protocol.ts
    packages/core/src/schemas/policy.schema.ts
    packages/core/src/__tests__/enums.test.ts
    packages/core/src/__tests__/signing-protocol.test.ts
    packages/core/src/i18n/en.ts
  </files>
  <behavior>
    - Test 1: POLICY_TYPES has 18 values, last is REPUTATION_THRESHOLD
    - Test 2: PolicyTypeEnum.parse('REPUTATION_THRESHOLD') succeeds
    - Test 3: NOTIFICATION_EVENT_TYPES has 54 values (49 + 5 new)
    - Test 4: 5 new events exist: AGENT_REGISTERED, AGENT_WALLET_LINKED, AGENT_WALLET_UNLINKED, REPUTATION_FEEDBACK_RECEIVED, REPUTATION_THRESHOLD_TRIGGERED
    - Test 5: EVENT_CATEGORY_MAP has entries for all 54 event types
    - Test 6: NOTIFICATION_CATEGORIES has 8 values (7 + 'identity')
    - Test 7: REPUTATION_THRESHOLD rules schema validates {min_score, below_threshold_tier, unrated_tier, tag1, tag2, check_counterparty}
    - Test 8: CreatePolicyRequestSchema with type REPUTATION_THRESHOLD + valid rules succeeds
    - Test 9: CreatePolicyRequestSchema with type REPUTATION_THRESHOLD + invalid rules (min_score > 100) fails
  </behavior>
  <action>
**1. packages/core/src/enums/policy.ts** (Plan 317-01에서 이미 REPUTATION_THRESHOLD 추가를 수행했을 수 있음 -- 확인 후 중복 방지):

POLICY_TYPES 배열에 `'REPUTATION_THRESHOLD'`가 없으면 추가. 이미 있으면 skip.

**2. packages/core/src/enums/notification.ts:**

NOTIFICATION_EVENT_TYPES 배열 끝에 5개 추가:
```typescript
'AGENT_REGISTERED',
'AGENT_WALLET_LINKED',
'AGENT_WALLET_UNLINKED',
'REPUTATION_FEEDBACK_RECEIVED',
'REPUTATION_THRESHOLD_TRIGGERED',
```
배열 총 54개.

**3. packages/core/src/schemas/signing-protocol.ts:**

NOTIFICATION_CATEGORIES에 `'identity'` 추가 (8번째):
```typescript
export const NOTIFICATION_CATEGORIES = [
  'transaction', 'policy', 'security_alert', 'session', 'owner', 'system', 'defi_monitoring', 'identity',
] as const;
```

EVENT_CATEGORY_MAP에 5개 추가:
```typescript
AGENT_REGISTERED: 'identity',
AGENT_WALLET_LINKED: 'identity',
AGENT_WALLET_UNLINKED: 'identity',
REPUTATION_FEEDBACK_RECEIVED: 'identity',
REPUTATION_THRESHOLD_TRIGGERED: 'policy',  // 정책 평가 결과이므로 policy 카테고리
```

EVENT_DESCRIPTIONS에 5개 추가:
```typescript
AGENT_REGISTERED: 'Agent registered on ERC-8004 Identity Registry',
AGENT_WALLET_LINKED: 'Agent wallet linked via EIP-712 signature',
AGENT_WALLET_UNLINKED: 'Agent wallet unlinked',
REPUTATION_FEEDBACK_RECEIVED: 'Reputation feedback received from another agent',
REPUTATION_THRESHOLD_TRIGGERED: 'Reputation threshold policy triggered tier escalation',
```

**4. packages/core/src/schemas/policy.schema.ts:**

ReputationThresholdRulesSchema 추가:
```typescript
/** REPUTATION_THRESHOLD: ERC-8004 평판 기반 보안 티어 오버라이드. */
export const ReputationThresholdRulesSchema = z.object({
  /** 최소 평판 점수 (0-100 정규화). 이 점수 미만이면 below_threshold_tier 적용 */
  min_score: z.number().min(0).max(100),
  /** 점수 미달 시 적용할 보안 티어 */
  below_threshold_tier: PolicyTierEnum.default('APPROVAL'),
  /** 평판 미확인(새 에이전트) 시 적용할 보안 티어 */
  unrated_tier: PolicyTierEnum.default('APPROVAL'),
  /** 평판 조회 시 필터링할 태그 (선택) */
  tag1: z.string().max(32).optional(),
  tag2: z.string().max(32).optional(),
  /** 상대방(to 주소) 에이전트의 평판도 확인할지 여부 */
  check_counterparty: z.boolean().default(false),
});
export type ReputationThresholdRules = z.infer<typeof ReputationThresholdRulesSchema>;
```

POLICY_RULES_SCHEMAS에 추가:
```typescript
REPUTATION_THRESHOLD: ReputationThresholdRulesSchema,
```

주석도 업데이트: "All 12 PolicyTypes" -> 숫자 정정 (현재 실제 등록 수에 맞게).

**5. packages/core/src/i18n/en.ts:**

NotificationEventType i18n 키가 있다면 5개 추가. 파일을 확인하고 패턴에 맞게 추가.

**6. 테스트 업데이트:**

packages/core/src/__tests__/enums.test.ts:
- `expect(POLICY_TYPES).toHaveLength(17)` -> `toHaveLength(18)`
- `expect(POLICY_TYPES).toContain('REPUTATION_THRESHOLD')` 추가
- `expect(NOTIFICATION_EVENT_TYPES).toHaveLength(49)` -> `toHaveLength(54)`
- 5개 신규 이벤트 toContain 추가

packages/core/src/__tests__/signing-protocol.test.ts:
- `expect(NOTIFICATION_CATEGORIES).toHaveLength(7)` -> `toHaveLength(8)`
- `expect(NOTIFICATION_CATEGORIES).toContain('identity')` 추가
- EVENT_CATEGORY_MAP 키 수 검증이 NOTIFICATION_EVENT_TYPES.length와 일치하는지 확인 (이미 동적 검증)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core exec vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - POLICY_TYPES에 REPUTATION_THRESHOLD가 18번째로 추가
    - NOTIFICATION_EVENT_TYPES에 5개 ERC-8004 이벤트 추가 (총 54개)
    - NOTIFICATION_CATEGORIES에 'identity' 추가 (총 8개)
    - EVENT_CATEGORY_MAP에 5개 이벤트 매핑 완료
    - EVENT_DESCRIPTIONS에 5개 영문 설명 추가
    - ReputationThresholdRulesSchema가 POLICY_RULES_SCHEMAS에 등록
    - 모든 @waiaas/core 테스트 통과
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin Settings 9키 정의</name>
  <files>
    packages/daemon/src/infrastructure/settings/setting-keys.ts
    packages/daemon/src/__tests__/settings-service.test.ts
  </files>
  <behavior>
    - Test 1: SETTING_DEFINITIONS에 actions.erc8004_agent_enabled 키가 존재하고 category='actions', defaultValue='false'
    - Test 2: SETTING_DEFINITIONS에 9개 erc8004 관련 키가 모두 존재
    - Test 3: actions.erc8004_identity_registry_address의 기본값이 메인넷 주소 '0x8004A169FB4a3325136EB29fA0ceB6D2e539a432'
    - Test 4: actions.erc8004_reputation_registry_address의 기본값이 메인넷 주소 '0x8004BAa17C55a88189AE136b182e5fdA19dE9b63'
    - Test 5: actions.erc8004_validation_registry_address의 기본값이 빈 문자열 (미배포)
    - Test 6: actions.erc8004_reputation_cache_ttl_sec의 기본값이 '300'
    - Test 7: actions.erc8004_reputation_rpc_timeout_ms의 기본값이 '3000'
    - Test 8: getSettingDefinition('actions.erc8004_agent_enabled') 반환값이 정확
  </behavior>
  <action>
**1. packages/daemon/src/infrastructure/settings/setting-keys.ts:**

SETTING_DEFINITIONS 배열의 `// --- Drift Perp ---` 섹션 다음(또는 기존 actions 섹션 마지막)에 ERC-8004 설정 9개를 추가:

```typescript
  // --- ERC-8004 Agent Identity ---
  { key: 'actions.erc8004_agent_enabled', category: 'actions', configPath: 'actions.erc8004_agent_enabled', defaultValue: 'false', isCredential: false },
  { key: 'actions.erc8004_identity_registry_address', category: 'actions', configPath: 'actions.erc8004_identity_registry_address', defaultValue: '0x8004A169FB4a3325136EB29fA0ceB6D2e539a432', isCredential: false },
  { key: 'actions.erc8004_reputation_registry_address', category: 'actions', configPath: 'actions.erc8004_reputation_registry_address', defaultValue: '0x8004BAa17C55a88189AE136b182e5fdA19dE9b63', isCredential: false },
  { key: 'actions.erc8004_validation_registry_address', category: 'actions', configPath: 'actions.erc8004_validation_registry_address', defaultValue: '', isCredential: false },
  { key: 'actions.erc8004_registration_file_base_url', category: 'actions', configPath: 'actions.erc8004_registration_file_base_url', defaultValue: '', isCredential: false },
  { key: 'actions.erc8004_auto_publish_registration', category: 'actions', configPath: 'actions.erc8004_auto_publish_registration', defaultValue: 'true', isCredential: false },
  { key: 'actions.erc8004_reputation_cache_ttl_sec', category: 'actions', configPath: 'actions.erc8004_reputation_cache_ttl_sec', defaultValue: '300', isCredential: false },
  { key: 'actions.erc8004_min_reputation_score', category: 'actions', configPath: 'actions.erc8004_min_reputation_score', defaultValue: '0', isCredential: false },
  { key: 'actions.erc8004_reputation_rpc_timeout_ms', category: 'actions', configPath: 'actions.erc8004_reputation_rpc_timeout_ms', defaultValue: '3000', isCredential: false },
```

설명:
- `erc8004_agent_enabled`: 프로바이더 활성화 (기본 false = feature gate)
- `erc8004_identity_registry_address`: Identity Registry 메인넷 주소
- `erc8004_reputation_registry_address`: Reputation Registry 메인넷 주소
- `erc8004_validation_registry_address`: Validation Registry (빈 문자열 = 미배포, feature gate)
- `erc8004_registration_file_base_url`: 등록 파일 호스팅 base URL (빈 = 데몬 자체 호스팅)
- `erc8004_auto_publish_registration`: 등록 파일 자동 생성/게시
- `erc8004_reputation_cache_ttl_sec`: 평판 점수 캐시 TTL (300초 = 5분)
- `erc8004_min_reputation_score`: 최소 평판 점수 기본값 (정책 연동)
- `erc8004_reputation_rpc_timeout_ms`: 평판 조회 RPC 타임아웃 (3000ms)

**2. 테스트 파일 업데이트:**

settings-service.test.ts에 기존 settings count assertion이 있다면 9 증가 업데이트. 또한 ERC-8004 설정 키 존재 검증 추가:

```typescript
it('includes 9 ERC-8004 agent identity settings', () => {
  const erc8004Keys = SETTING_DEFINITIONS.filter(d => d.key.startsWith('actions.erc8004_'));
  expect(erc8004Keys).toHaveLength(9);
});

it('erc8004_agent_enabled defaults to false', () => {
  const def = getSettingDefinition('actions.erc8004_agent_enabled');
  expect(def).toBeDefined();
  expect(def!.defaultValue).toBe('false');
  expect(def!.category).toBe('actions');
});
```
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon exec vitest run --reporter=verbose packages/daemon/src/__tests__/settings-service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SETTING_DEFINITIONS에 9개 ERC-8004 설정 키가 추가
    - feature gate (erc8004_agent_enabled) 기본값 'false'
    - Identity/Reputation Registry 메인넷 주소가 기본값으로 설정
    - Validation Registry 기본값 빈 문자열 (미배포)
    - getSettingDefinition으로 각 키 조회 가능
    - settings-service 테스트 통과
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/core run build` -- 코어 빌드 성공
2. `pnpm --filter @waiaas/core exec vitest run --reporter=verbose` -- 모든 코어 테스트 통과
3. `pnpm --filter @waiaas/daemon run build` -- 데몬 빌드 성공
4. `pnpm --filter @waiaas/daemon exec vitest run --reporter=verbose packages/daemon/src/__tests__/settings-service.test.ts` -- 설정 테스트 통과
5. `pnpm turbo run typecheck` -- 전체 타입체크 성공
</verification>

<success_criteria>
- POLICY_TYPES 배열 18개 (REPUTATION_THRESHOLD 포함)
- NOTIFICATION_EVENT_TYPES 54개 (5개 ERC-8004 이벤트 추가)
- NOTIFICATION_CATEGORIES 8개 ('identity' 추가)
- EVENT_CATEGORY_MAP 54개 전체 매핑 완료
- ReputationThresholdRulesSchema가 min_score, below_threshold_tier, unrated_tier, tag1, tag2, check_counterparty 필드 검증
- POLICY_RULES_SCHEMAS에 REPUTATION_THRESHOLD 등록
- Admin Settings 9키: 기본값, 카테고리, configPath 모두 정확
- 전체 @waiaas/core + settings 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/317-foundation/317-02-SUMMARY.md`
</output>
