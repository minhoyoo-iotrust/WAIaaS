---
phase: 250-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/actions.skill.md
autonomous: true
requirements: [INTG-03, INTG-04]

must_haves:
  truths:
    - "MCP action_0x_swap_swap tool is auto-registered by registerActionProviderTools when 0x-swap provider is enabled"
    - "actions.skill.md contains 0x Swap section with REST API, MCP, and SDK usage examples"
    - "actions.skill.md documents 0x Swap configuration via Admin Settings (not config.toml)"
    - "actions.skill.md documents 0x Swap safety features (slippage clamp, liquidity check, AllowanceHolder, same-token block)"
    - "actions.skill.md documents provider-trust policy bypass for enabled action providers"
  artifacts:
    - path: "skills/actions.skill.md"
      provides: "0x Swap documentation section with REST/MCP/SDK examples"
      contains: "0x Swap"
  key_links:
    - from: "packages/mcp/src/tools/action-provider.ts"
      to: "GET /v1/actions/providers"
      via: "apiClient.get fetches mcpExpose=true providers"
      pattern: "apiClient\\.get.*actions/providers"
---

<objective>
Verify MCP auto-exposure works for 0x Swap and update actions.skill.md with comprehensive 0x Swap documentation.

Purpose: Complete the DX surface for 0x Swap by ensuring MCP tool auto-registration works (INTG-03 -- already implemented, needs verification) and providing detailed skill documentation (INTG-04) for AI agents and developers.
Output: Updated actions.skill.md with 0x Swap section.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/249-0x-swap-provider/249-01-SUMMARY.md
@.planning/phases/249-0x-swap-provider/249-02-SUMMARY.md
@.planning/phases/248-provider-infrastructure/248-01-SUMMARY.md

# MCP auto-registration mechanism
@packages/mcp/src/tools/action-provider.ts
@packages/mcp/src/__tests__/action-provider.test.ts

# 0x Swap provider source
@packages/actions/src/providers/zerox-swap/index.ts

# Existing skill file (to extend)
@skills/actions.skill.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify MCP auto-exposure and update actions.skill.md</name>
  <files>
    skills/actions.skill.md
  </files>
  <action>
1. **Verify INTG-03 (MCP auto-exposure):** Read `packages/mcp/src/tools/action-provider.ts` to confirm it fetches `GET /v1/actions/providers`, filters `mcpExpose=true`, and registers tools as `action_{provider}_{action}`. Read the existing `action-provider.test.ts` to confirm tests cover this. Read `packages/actions/src/providers/zerox-swap/index.ts` to verify `mcpExpose: true` in metadata. The MCP auto-exposure for 0x-swap is already implemented -- `registerActionProviderTools` will register `action_0x-swap_swap` when the daemon returns it in the providers list. No code changes needed for INTG-03.

2. **Update actions.skill.md for INTG-04:** Rewrite the entire skill file to include both Jupiter Swap and 0x Swap, and modernize it to reflect the Admin Settings pattern (replacing old config.toml references).

Major sections to add/update:

**Section 3 (existing, update):** Rename from "Jupiter Swap -- Built-in Provider" to keep it but update Configuration subsection to reference Admin Settings instead of config.toml. Keep the existing Jupiter content mostly intact.

**Section 4 (new):** "0x Swap -- Built-in Provider" with:
  - **Configuration:** Admin UI > Settings > Actions > 0x Swap settings (enabled toggle, API key, slippage defaults). Mention that 0x API key is required (requiresApiKey=true). Environment variables: `WAIAAS_ACTIONS_ZEROX_SWAP_ENABLED`, etc.
  - **Swap Parameters:** Table with sellToken (hex address), buyToken (hex address), sellAmount (wei string), chainId (optional number), slippageBps (optional number). Note that these are EVM addresses, not Solana mints.
  - **Common EVM Tokens:** Table with ETH (0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE), USDC (0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48), WETH (0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2), etc.
  - **Safety Features:**
    - Slippage protection: Default 100bps (1%), max 500bps (5%), auto-clamped
    - Liquidity check: Rejects when 0x API returns liquidityAvailable=false
    - Same-token block: Rejects sellToken === buyToken before API call
    - AllowanceHolder validation: Verifies returned allowanceTarget matches known AllowanceHolder address
    - Zod runtime validation: All 0x API responses validated against schemas
    - Provider-trust: When 0x-swap is enabled in Admin Settings, CONTRACT_WHITELIST check is skipped for 0x-resolved transactions (AllowanceHolder contract is trusted)
  - **Multi-step execution:** ERC-20 sells generate two transactions: (1) ERC-20 approve to AllowanceHolder, (2) swap call. Native ETH sells generate one transaction. The pipeline array is visible in the API response.
  - **Example: Swap 0.1 ETH to USDC on Ethereum:**
    - REST API curl example with POST /v1/actions/0x-swap/swap
    - MCP tool example with action_0x-swap_swap
    - TS SDK example with client.executeAction('0x-swap', 'swap', {...})
    - Python SDK example with await client.execute_action('0x-swap', 'swap', {...})

**Section 5 (update):** Rename "Policy Integration" -- Add a "Provider-Trust Bypass" subsection explaining that enabled action providers skip CONTRACT_WHITELIST. AllowanceHolder is trusted automatically.

**Section 6 (update):** "Configuration via Admin Settings" (NEW) -- Explain that since v28.2, all action provider settings are managed via Admin UI > Settings > Actions (not config.toml). List key settings: enabled toggle, API key, base URL, slippage defaults, max slippage, max price impact (Jupiter), timeout.

**Section 7:** Error Reference -- Keep existing, add 0x-specific errors if any (PRICE_IMPACT_TOO_HIGH already there, add LIQUIDITY_UNAVAILABLE note).

**Section 8:** Related Skill Files -- Keep as is.

Update the version in the frontmatter to match the current release.
  </action>
  <verify>
1. Read the updated `skills/actions.skill.md` and verify it contains: "0x Swap" section, REST/MCP/SDK examples, safety features, config via Admin Settings.
2. Verify `packages/mcp/src/tools/action-provider.ts` registers tools with `action_{provider}_{action}` pattern -- confirm by reading the code that `action_0x-swap_swap` will be registered when provider is in list.
3. Verify `packages/actions/src/providers/zerox-swap/index.ts` has `mcpExpose: true` in metadata.
  </verify>
  <done>
actions.skill.md contains complete 0x Swap documentation with REST API, MCP, TS SDK, and Python SDK examples. Provider-trust bypass is documented. Admin Settings configuration is documented. MCP auto-exposure verified.
  </done>
</task>

</tasks>

<verification>
1. `skills/actions.skill.md` contains "0x Swap" section with all required subsections
2. REST API example shows `POST /v1/actions/0x-swap/swap` with correct params
3. MCP example shows `action_0x-swap_swap` tool name
4. TS SDK example shows `client.executeAction('0x-swap', 'swap', ...)`
5. Python SDK example shows `await client.execute_action('0x-swap', 'swap', ...)`
6. Safety features (slippage, liquidity, same-token, AllowanceHolder) documented
7. Admin Settings configuration documented (not config.toml only)
8. Provider-trust bypass documented
</verification>

<success_criteria>
- actions.skill.md has 0x Swap section with REST/MCP/SDK examples
- Configuration references Admin Settings (not just config.toml)
- Safety features are documented
- MCP auto-exposure mechanism verified (code review -- no changes needed)
</success_criteria>

<output>
After completion, create `.planning/phases/250-integration/250-02-SUMMARY.md`
</output>
