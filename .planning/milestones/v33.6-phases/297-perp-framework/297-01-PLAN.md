---
phase: 297-perp-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/interfaces/perp-provider.types.ts
  - packages/core/src/interfaces/index.ts
  - packages/core/src/events/event-types.ts
  - packages/core/src/enums/policy.ts
  - packages/daemon/src/pipeline/database-policy-engine.ts
  - packages/daemon/src/__tests__/perp-provider-types.test.ts
autonomous: true
requirements:
  - PERP-01
  - PERP-02

must_haves:
  truths:
    - "IPerpProvider 인터페이스가 IActionProvider를 확장하며 5 표준 액션(open/close/modify_position, add/withdraw_margin)과 getPosition/getMarginInfo/getMarkets 메서드를 정의한다"
    - "PerpPositionSummary, MarginInfo, PerpMarketInfo Zod 스키마가 SSoT로 정의되고 @waiaas/core에서 export된다"
    - "MarginWarningEvent가 WaiaasEventMap에 등록되어 EventBus로 타입-안전하게 emit 가능하다"
    - "POLICY_TYPES 배열에 PERP_MAX_LEVERAGE, PERP_MAX_POSITION_USD, PERP_ALLOWED_MARKETS가 추가되어 정책 DB INSERT가 CHECK 제약을 통과한다"
    - "TransactionParam에 perpLeverage/perpSizeUsd 필드가 추가되어 Perp 정책 평가에 사용 가능하다"
  artifacts:
    - path: "packages/core/src/interfaces/perp-provider.types.ts"
      provides: "IPerpProvider interface + Zod schemas (3)"
      exports: ["IPerpProvider", "PerpPositionSummarySchema", "MarginInfoSchema", "PerpMarketInfoSchema", "PerpPositionSummary", "MarginInfo", "PerpMarketInfo"]
    - path: "packages/core/src/interfaces/index.ts"
      provides: "Re-export of perp-provider types"
    - path: "packages/core/src/events/event-types.ts"
      provides: "MarginWarningEvent interface + WaiaasEventMap extension"
      contains: "MarginWarningEvent"
    - path: "packages/core/src/enums/policy.ts"
      provides: "3 new PERP policy types in POLICY_TYPES"
      contains: "PERP_MAX_LEVERAGE"
    - path: "packages/daemon/src/pipeline/database-policy-engine.ts"
      provides: "TransactionParam perpLeverage/perpSizeUsd fields + NON_SPENDING_ACTIONS perp additions"
    - path: "packages/daemon/src/__tests__/perp-provider-types.test.ts"
      provides: "Unit tests for Zod schema validation + type checks"
  key_links:
    - from: "packages/core/src/interfaces/perp-provider.types.ts"
      to: "packages/core/src/interfaces/action-provider.types.ts"
      via: "IPerpProvider extends IActionProvider"
      pattern: "extends IActionProvider"
    - from: "packages/core/src/interfaces/index.ts"
      to: "packages/core/src/interfaces/perp-provider.types.ts"
      via: "re-export"
      pattern: "perp-provider\\.types"
    - from: "packages/core/src/events/event-types.ts"
      to: "MarginWarningEvent"
      via: "WaiaasEventMap key"
      pattern: "perp:margin-warning"
---

<objective>
IPerpProvider 인터페이스 정의 + Perp 인프라 타입 확장

Purpose: Perp 프레임워크의 모든 타입 계약(IPerpProvider, Zod 스키마, 이벤트, 정책 타입)을 @waiaas/core에 정의하여 후속 Plan 02(MarginMonitor, PerpPolicyEvaluator)와 Phase 298(DriftPerpProvider)가 타입-안전하게 구현할 수 있는 기반을 마련한다.
Output: perp-provider.types.ts(새 파일), 4개 기존 파일 수정, 1개 테스트 파일
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/297-perp-framework/297-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/core/src/interfaces/action-provider.types.ts:
```typescript
export interface IActionProvider {
  readonly metadata: ActionProviderMetadata;
  readonly actions: readonly ActionDefinition[];
  resolve(actionName: string, params: Record<string, unknown>, context: ActionContext): Promise<ContractCallRequest | ContractCallRequest[]>;
}
export type ActionContext = z.infer<typeof ActionContextSchema>;
// ActionContextSchema = z.object({ walletAddress, chain, walletId, sessionId? })
```

From packages/core/src/interfaces/lending-provider.types.ts (pattern to follow):
```typescript
export const LendingPositionSummarySchema = z.object({ ... });
export const HealthFactorSchema = z.object({ ... });
export const MarketInfoSchema = z.object({ ... });
export type LendingPositionSummary = z.infer<typeof LendingPositionSummarySchema>;
export type HealthFactor = z.infer<typeof HealthFactorSchema>;
export type MarketInfo = z.infer<typeof MarketInfoSchema>;
export interface ILendingProvider extends IActionProvider {
  getPosition(walletId: string, context: ActionContext): Promise<LendingPositionSummary[]>;
  getHealthFactor(walletId: string, context: ActionContext): Promise<HealthFactor>;
  getMarkets(chain: string, network?: string): Promise<MarketInfo[]>;
}
```

From packages/core/src/interfaces/index.ts (re-export pattern):
```typescript
// v29.2 Lending Provider types (Zod SSoT)
export type { LendingPositionSummary, HealthFactor, MarketInfo, ILendingProvider } from './lending-provider.types.js';
export { LendingPositionSummarySchema, HealthFactorSchema, MarketInfoSchema } from './lending-provider.types.js';
// v29.6 Yield Provider types (Zod SSoT)
export type { YieldMarketInfo, YieldPositionSummary, YieldForecast, IYieldProvider } from './yield-provider.types.js';
export { YieldMarketInfoSchema, YieldPositionSummarySchema, YieldForecastSchema } from './yield-provider.types.js';
```

From packages/core/src/enums/policy.ts:
```typescript
export const POLICY_TYPES = [
  'SPENDING_LIMIT', 'WHITELIST', 'TIME_RESTRICTION', 'RATE_LIMIT',
  'ALLOWED_TOKENS', 'CONTRACT_WHITELIST', 'METHOD_WHITELIST',
  'APPROVED_SPENDERS', 'APPROVE_AMOUNT_LIMIT', 'APPROVE_TIER_OVERRIDE',
  'ALLOWED_NETWORKS', 'X402_ALLOWED_DOMAINS',
  'LENDING_LTV_LIMIT', 'LENDING_ASSET_WHITELIST',
] as const;
```

From packages/core/src/events/event-types.ts:
```typescript
export interface YieldMaturityWarningEvent {
  walletId: string; positionId: string; provider: string;
  marketId: string; daysUntilMaturity: number; maturityDate: number; timestamp: number;
}
export interface WaiaasEventMap {
  // ... existing entries ...
  'yield:maturity-warning': YieldMaturityWarningEvent;
}
```

From packages/daemon/src/pipeline/database-policy-engine.ts (TransactionParam):
```typescript
interface TransactionParam {
  type: string; amount: string; toAddress: string; chain: string;
  network?: string; tokenAddress?: string; assetId?: string;
  contractAddress?: string; selector?: string; spenderAddress?: string;
  approveAmount?: string; tokenDecimals?: number;
  actionProvider?: string; actionName?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IPerpProvider 인터페이스 + Zod 스키마 정의</name>
  <files>
    packages/core/src/interfaces/perp-provider.types.ts
    packages/core/src/interfaces/index.ts
  </files>
  <action>
1. **새 파일 `packages/core/src/interfaces/perp-provider.types.ts` 생성** -- lending-provider.types.ts 패턴을 1:1로 따른다.

JSDoc 헤더:
```
/**
 * Perp Provider types and interfaces (Zod SSoT).
 *
 * Defines IPerpProvider extending IActionProvider with
 * domain-specific query methods for DeFi perp positions.
 *
 * Design source: m29-00 design doc section 14.
 * @see PERP-01, PERP-02
 */
```

Zod SSoT 스키마 3개:

```typescript
import { z } from 'zod';
import type { IActionProvider, ActionContext } from './action-provider.types.js';

// PerpPositionSummarySchema
export const PerpPositionSummarySchema = z.object({
  market: z.string(),                        // e.g., "SOL-PERP"
  direction: z.enum(['LONG', 'SHORT']),
  size: z.string(),                          // string for bigint-safe
  entryPrice: z.number().nullable(),
  leverage: z.number(),
  unrealizedPnl: z.number().nullable(),
  margin: z.number().nullable(),
  liquidationPrice: z.number().nullable(),
});
export type PerpPositionSummary = z.infer<typeof PerpPositionSummarySchema>;

// MarginInfoSchema
export const MarginInfoSchema = z.object({
  totalMargin: z.number(),
  freeMargin: z.number(),
  maintenanceMarginRatio: z.number(),
  marginRatio: z.number(),     // current margin / maintenance margin. Lower = more dangerous
  status: z.enum(['safe', 'warning', 'danger', 'critical']),
});
export type MarginInfo = z.infer<typeof MarginInfoSchema>;

// PerpMarketInfoSchema
export const PerpMarketInfoSchema = z.object({
  market: z.string(),           // e.g., "SOL-PERP"
  baseAsset: z.string(),
  maxLeverage: z.number(),
  fundingRate: z.number().nullable(),
  openInterest: z.number().nullable(),
  oraclePrice: z.number().nullable(),
});
export type PerpMarketInfo = z.infer<typeof PerpMarketInfoSchema>;
```

IPerpProvider 인터페이스:

```typescript
/**
 * Perp Provider contract.
 *
 * Extends IActionProvider with perp-specific query methods.
 * Implementations (e.g., DriftPerpProvider) provide both
 * action resolution (open_position/close_position/modify_position/
 * add_margin/withdraw_margin) and position/margin/market queries.
 *
 * Standard actions: open_position, close_position, modify_position,
 *   add_margin, withdraw_margin
 */
export interface IPerpProvider extends IActionProvider {
  getPosition(walletId: string, context: ActionContext): Promise<PerpPositionSummary[]>;
  getMarginInfo(walletId: string, context: ActionContext): Promise<MarginInfo>;
  getMarkets(chain: string, network?: string): Promise<PerpMarketInfo[]>;
}
```

2. **`packages/core/src/interfaces/index.ts` 수정** -- 파일 끝에 Perp Provider re-export 추가:

```typescript
// v29.8 Perp Provider types (Zod SSoT)
export type {
  PerpPositionSummary,
  MarginInfo,
  PerpMarketInfo,
  IPerpProvider,
} from './perp-provider.types.js';
export {
  PerpPositionSummarySchema,
  MarginInfoSchema,
  PerpMarketInfoSchema,
} from './perp-provider.types.js';
```
  </action>
  <verify>
    cd packages/core && npx tsc --noEmit 2>&1 | head -20
    -- 타입 에러 없어야 함
  </verify>
  <done>
    - perp-provider.types.ts가 3개 Zod 스키마(PerpPositionSummary, MarginInfo, PerpMarketInfo)와 IPerpProvider 인터페이스를 export
    - index.ts에서 모든 타입과 스키마가 re-export
    - `import { IPerpProvider, PerpPositionSummarySchema } from '@waiaas/core'`로 접근 가능
  </done>
</task>

<task type="auto">
  <name>Task 2: 이벤트 타입 + 정책 타입 + TransactionParam 확장</name>
  <files>
    packages/core/src/events/event-types.ts
    packages/core/src/enums/policy.ts
    packages/daemon/src/pipeline/database-policy-engine.ts
  </files>
  <action>
1. **`packages/core/src/events/event-types.ts` 수정** -- MarginWarningEvent 추가.

YieldMaturityWarningEvent 뒤에 추가:

```typescript
// v29.8 perp margin warning event

export interface MarginWarningEvent {
  walletId: string;
  positionId: string;
  provider: string;
  market: string;
  marginRatio: number;
  threshold: number;
  severity: 'WARNING' | 'DANGER' | 'CRITICAL';
  timestamp: number;
}
```

WaiaasEventMap에 추가 (기존 `'yield:maturity-warning'` 뒤):
```typescript
'perp:margin-warning': MarginWarningEvent;
```

JSDoc 상단 주석에 `perp:margin-warning` 이벤트 설명 추가:
```
 * - perp:margin-warning -- fired when perp position margin ratio crosses threshold (v29.8)
```

2. **`packages/core/src/enums/policy.ts` 수정** -- POLICY_TYPES 배열 끝(LENDING_ASSET_WHITELIST 뒤)에 3개 추가:

```typescript
  'PERP_MAX_LEVERAGE',
  'PERP_MAX_POSITION_USD',
  'PERP_ALLOWED_MARKETS',
```

3. **`packages/daemon/src/pipeline/database-policy-engine.ts` 수정** -- 3개 변경:

(a) TransactionParam 인터페이스에 2개 필드 추가 (actionName 뒤):
```typescript
  /** Leverage for perp policy evaluation (open_position/modify_position). Set by ActionProviderRegistry. */
  perpLeverage?: number;
  /** Position size in USD for perp policy evaluation. Set by ActionProviderRegistry. */
  perpSizeUsd?: number;
```

(b) evaluate() 메서드의 NON_SPENDING_ACTIONS Set에 perp 액션 추가 (Step 5):
기존: `const NON_SPENDING_ACTIONS = new Set(['supply', 'repay', 'withdraw']);`
변경: `const NON_SPENDING_ACTIONS = new Set(['supply', 'repay', 'withdraw', 'close_position', 'add_margin']);`

(c) evaluateAndReserve() 메서드의 NON_SPENDING_ACTIONS_R Set에도 동일하게 추가:
기존: `const NON_SPENDING_ACTIONS_R = new Set(['supply', 'repay', 'withdraw']);`
변경: `const NON_SPENDING_ACTIONS_R = new Set(['supply', 'repay', 'withdraw', 'close_position', 'add_margin']);`

(d) 3개 Perp 정책 rules 인터페이스 추가 (LendingAssetWhitelistRules 뒤):
```typescript
/** Perp max leverage restriction rules (Phase 297). */
interface PerpMaxLeverageRules {
  maxLeverage: number;       // e.g., 10 (10x)
  warningLeverage?: number;  // e.g., 5 (5x) -- DELAY tier
}

/** Perp max position USD restriction rules (Phase 297). */
interface PerpMaxPositionUsdRules {
  maxPositionUsd: number;    // e.g., 10000 ($10,000)
  warningPositionUsd?: number; // e.g., 8000 -- DELAY tier
}

/** Perp allowed markets whitelist rules (Phase 297). */
interface PerpAllowedMarketsRules {
  markets: Array<{ market: string; name?: string }>;
}
```

주의: 이 태스크에서는 정책 **평가 로직**(evaluatePerpAllowedMarkets 등)은 추가하지 않는다. 그것은 Plan 297-02에서 구현한다. 여기서는 타입/인터페이스/NON_SPENDING만 변경한다.
  </action>
  <verify>
    cd packages/core && npx tsc --noEmit 2>&1 | head -20 && cd ../daemon && npx tsc --noEmit 2>&1 | head -20
    -- 타입 에러 없어야 함
  </verify>
  <done>
    - MarginWarningEvent가 event-types.ts에 정의되고 WaiaasEventMap에 'perp:margin-warning' 키로 매핑
    - POLICY_TYPES에 PERP_MAX_LEVERAGE, PERP_MAX_POSITION_USD, PERP_ALLOWED_MARKETS 3개 추가
    - TransactionParam에 perpLeverage, perpSizeUsd 옵셔널 필드 추가
    - NON_SPENDING_ACTIONS에 close_position, add_margin 추가 (evaluate + evaluateAndReserve 양쪽)
    - Perp 정책 rules 인터페이스 3개 정의
  </done>
</task>

<task type="auto">
  <name>Task 3: Perp 타입 유닛 테스트</name>
  <files>
    packages/daemon/src/__tests__/perp-provider-types.test.ts
  </files>
  <action>
lending-policy-evaluator.test.ts 패턴을 따라 유닛 테스트 작성:

```typescript
/**
 * Unit tests for Perp Provider types and Perp infrastructure extensions.
 *
 * Tests: Zod schema validation, IPerpProvider type contracts, policy type
 * registration, non-spending classification, TransactionParam extensions.
 * @see PERP-01, PERP-02
 */

import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import {
  PerpPositionSummarySchema,
  MarginInfoSchema,
  PerpMarketInfoSchema,
} from '@waiaas/core';
import { createDatabase, pushSchema } from '../infrastructure/database/index.js';
import type { DatabaseConnection } from '../infrastructure/database/index.js';
import { wallets, policies } from '../infrastructure/database/schema.js';
import { generateId } from '../infrastructure/database/id.js';
import { DatabasePolicyEngine } from '../pipeline/database-policy-engine.js';
import { SettingsService } from '../infrastructure/settings/settings-service.js';
import { DaemonConfigSchema } from '../infrastructure/config/loader.js';
```

테스트 그룹:

**1. Zod 스키마 검증 (3 describe)**

- `PerpPositionSummarySchema`: valid 데이터 parse 성공, direction이 'LONG'|'SHORT' 외의 값이면 실패, leverage가 음수여도 통과(Zod 수준에서는 제한 없음), entryPrice nullable 확인
- `MarginInfoSchema`: valid 데이터 parse 성공, status가 'safe'|'warning'|'danger'|'critical' enum 제한 확인
- `PerpMarketInfoSchema`: valid 데이터 parse 성공, fundingRate/openInterest/oraclePrice nullable 확인

**2. POLICY_TYPES에 Perp 정책 타입 포함 확인**

```typescript
import { POLICY_TYPES } from '@waiaas/core/enums/policy.js';
// 또는 import 경로에 맞게
```
- PERP_MAX_LEVERAGE, PERP_MAX_POSITION_USD, PERP_ALLOWED_MARKETS가 POLICY_TYPES 배열에 포함 확인

**3. DB CHECK 제약 통과 확인 (정책 INSERT)**

lending-policy-evaluator.test.ts와 동일한 setup 패턴:
- PERP_MAX_LEVERAGE 타입의 정책을 DB에 INSERT -- 성공 확인 (CHECK 제약 통과)
- PERP_MAX_POSITION_USD 타입의 정책을 DB에 INSERT -- 성공 확인
- PERP_ALLOWED_MARKETS 타입의 정책을 DB에 INSERT -- 성공 확인

**4. Non-spending 분류 확인**

DatabasePolicyEngine.evaluate()를 사용하여:
- close_position 액션이 SPENDING_LIMIT를 건너뛰는지 확인 (NON_SPENDING)
- add_margin 액션이 SPENDING_LIMIT를 건너뛰는지 확인 (NON_SPENDING)
- open_position 액션이 SPENDING_LIMIT에 의해 평가되는지 확인 (SPENDING)

Non-spending 테스트를 위해:
1. 테스트 월렛 생성
2. SPENDING_LIMIT 정책 추가 (tight limits: instant_max=100, notify_max=1000, delay_max=10000)
3. close_position/add_margin은 amount가 999999999여도 INSTANT 반환 (non-spending)
4. open_position은 amount가 999999999이면 APPROVAL 반환 (spending)

주의: Perp 액션은 CONTRACT_CALL 타입이므로, default_deny_contracts=false 설정 필요 (lending-policy-evaluator.test.ts 패턴과 동일). 또한 PERP_ALLOWED_MARKETS가 아직 평가 로직이 없으므로 (Plan 02에서 추가), 시장 화이트리스트 체크 없이 통과한다.
  </action>
  <verify>
    cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test --filter=@waiaas/daemon -- --run --reporter=verbose packages/daemon/src/__tests__/perp-provider-types.test.ts 2>&1 | tail -30
    -- 모든 테스트 통과
  </verify>
  <done>
    - Zod 스키마 3개(PerpPositionSummary, MarginInfo, PerpMarketInfo) 검증 테스트 통과
    - POLICY_TYPES에 3개 Perp 정책 타입 포함 확인
    - DB CHECK 제약이 3개 Perp 정책 타입 INSERT 허용 확인
    - close_position, add_margin이 non-spending으로 분류 확인
    - open_position이 spending으로 분류되어 SPENDING_LIMIT 평가 대상 확인
  </done>
</task>

</tasks>

<verification>
1. `cd packages/core && npx tsc --noEmit` -- 타입 에러 없음
2. `cd packages/daemon && npx tsc --noEmit` -- 타입 에러 없음
3. `pnpm turbo run test --filter=@waiaas/daemon -- --run packages/daemon/src/__tests__/perp-provider-types.test.ts` -- 전체 통과
4. `pnpm turbo run test --filter=@waiaas/daemon -- --run packages/daemon/src/__tests__/lending-policy-evaluator.test.ts` -- 기존 테스트 깨지지 않음 (regression)
5. `pnpm turbo run test --filter=@waiaas/daemon -- --run packages/daemon/src/__tests__/health-factor-monitor.test.ts` -- 기존 테스트 깨지지 않음 (regression)
</verification>

<success_criteria>
- IPerpProvider 인터페이스가 packages/core에서 export되고 IActionProvider를 확장
- 3개 Zod 스키마(PerpPositionSummary, MarginInfo, PerpMarketInfo)가 parse/infer 가능
- MarginWarningEvent가 EventBus에서 'perp:margin-warning' 키로 emit 가능
- POLICY_TYPES에 3개 Perp 타입이 포함되어 DB INSERT 성공
- TransactionParam에 perpLeverage, perpSizeUsd 필드 존재
- NON_SPENDING_ACTIONS에 close_position, add_margin 포함
- 모든 기존 테스트 regression 없음
</success_criteria>

<output>
After completion, create `.planning/phases/297-perp-framework/297-01-SUMMARY.md`
</output>
