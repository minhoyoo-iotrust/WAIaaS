---
phase: 470-ssot-extension-db-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/networks.ts
  - packages/shared/src/__tests__/networks.test.ts
  - packages/core/src/enums/chain.ts
  - packages/core/src/utils/chain-constants.ts
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-08]
must_haves:
  truths:
    - "'ripple'이 ChainType Zod 스키마 유효성 검사를 통과한다"
    - "xrpl-mainnet/xrpl-testnet/xrpl-devnet가 NetworkType으로 인식된다"
    - "ENVIRONMENT_NETWORK_MAP에서 ripple:mainnet -> xrpl-mainnet, ripple:testnet -> [xrpl-testnet, xrpl-devnet] 매핑이 동작한다"
    - "ENVIRONMENT_SINGLE_NETWORK에서 ripple이 Solana처럼 환경당 단일 네트워크로 동작한다"
    - "XRP 네이티브 토큰이 6 decimals로 처리된다"
  artifacts:
    - path: "packages/shared/src/networks.ts"
      provides: "CHAIN_TYPES에 'ripple' 추가, RIPPLE_NETWORK_TYPES, ENVIRONMENT_NETWORK_MAP ripple 항목"
      contains: "ripple"
    - path: "packages/core/src/enums/chain.ts"
      provides: "ENVIRONMENT_SINGLE_NETWORK ripple 항목, MAINNET_NETWORKS에 xrpl-mainnet, RippleNetworkType re-export"
      contains: "xrpl-mainnet"
    - path: "packages/core/src/utils/chain-constants.ts"
      provides: "NATIVE_DECIMALS ripple=6, NATIVE_SYMBOLS ripple=XRP"
      contains: "ripple"
  key_links:
    - from: "packages/core/src/enums/chain.ts"
      to: "packages/shared/src/networks.ts"
      via: "import from @waiaas/shared"
      pattern: "CHAIN_TYPES.*from.*@waiaas/shared"
---

<objective>
Ripple ChainType/NetworkType SSoT 등록 및 체인 상수 추가

Purpose: 'ripple'을 시스템 전체의 단일 진실 소스(SSoT)에 등록하여 Zod 스키마, TypeScript 타입, 환경 매핑이 모두 ripple 체인을 인식하도록 한다.
Output: @waiaas/shared와 @waiaas/core에서 ripple이 유효한 ChainType이고 xrpl-mainnet/testnet/devnet이 유효한 NetworkType으로 동작
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v33.6-ROADMAP.md

@packages/shared/src/networks.ts
@packages/shared/src/__tests__/networks.test.ts
@packages/core/src/enums/chain.ts
@packages/core/src/utils/chain-constants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: @waiaas/shared SSoT에 ripple ChainType 및 NetworkType 등록</name>
  <files>packages/shared/src/networks.ts, packages/shared/src/__tests__/networks.test.ts</files>
  <behavior>
    - CHAIN_TYPES에 'ripple'이 포함되어 length가 3이다
    - RIPPLE_NETWORK_TYPES = ['xrpl-mainnet', 'xrpl-testnet', 'xrpl-devnet'] 가 export된다
    - NETWORK_TYPES에 xrpl-mainnet, xrpl-testnet, xrpl-devnet 3개가 추가되어 length가 18이다
    - ENVIRONMENT_NETWORK_MAP['ripple:mainnet'] = ['xrpl-mainnet']
    - ENVIRONMENT_NETWORK_MAP['ripple:testnet'] = ['xrpl-testnet', 'xrpl-devnet']
    - validateChainNetwork('ripple', 'xrpl-mainnet') 통과, ('ripple', 'solana-mainnet') 에러
    - NETWORK_DISPLAY_NAMES['xrpl-mainnet'] = 'XRPL Mainnet' 등 3개
    - NETWORK_NATIVE_SYMBOL['xrpl-mainnet'] = 'XRP' 등 3개
    - RIPPLE_RPC_SETTING_KEYS = ['xrpl_mainnet', 'xrpl_testnet', 'xrpl_devnet']
    - RPC_KEY_LABELS에 xrpl 3개 키가 추가
  </behavior>
  <action>
1. packages/shared/src/networks.ts 수정:
   - CHAIN_TYPES에 'ripple' 추가: `['solana', 'ethereum', 'ripple'] as const`
   - RIPPLE_NETWORK_TYPES 배열 추가: `['xrpl-mainnet', 'xrpl-testnet', 'xrpl-devnet'] as const` (per INFRA-02)
   - RippleNetworkType 타입 export
   - NETWORK_TYPES에 xrpl-mainnet, xrpl-testnet, xrpl-devnet 3개 추가 (per INFRA-02)
   - ENVIRONMENT_NETWORK_MAP에 'ripple:mainnet': ['xrpl-mainnet']와 'ripple:testnet': ['xrpl-testnet', 'xrpl-devnet'] 추가 (per INFRA-03)
   - validateChainNetwork에 `else if (chain === 'ripple')` 분기 추가하여 RIPPLE_NETWORK_TYPES와 대조 (per INFRA-01)
   - NETWORK_DISPLAY_NAMES에 'xrpl-mainnet': 'XRPL Mainnet', 'xrpl-testnet': 'XRPL Testnet', 'xrpl-devnet': 'XRPL Devnet' 추가
   - NETWORK_NATIVE_SYMBOL에 'xrpl-mainnet': 'XRP', 'xrpl-testnet': 'XRP', 'xrpl-devnet': 'XRP' 추가
   - RIPPLE_RPC_SETTING_KEYS 배열 추가: RIPPLE_NETWORK_TYPES.map(n => n.replace(/-/g, '_').replace('xrpl_', 'xrpl_'))
     - 결과: ['xrpl_mainnet', 'xrpl_testnet', 'xrpl_devnet']
   - RPC_KEY_LABELS에 RIPPLE_NETWORK_TYPES 매핑 추가

2. packages/shared/src/__tests__/networks.test.ts 수정:
   - CHAIN_TYPES 테스트: length 3, 'ripple' 포함
   - NETWORK_TYPES 테스트: length 18 (15 + 3)
   - RIPPLE_NETWORK_TYPES 임포트 및 테스트 추가
   - ENVIRONMENT_NETWORK_MAP ripple 항목 검증
   - validateChainNetwork ripple 유효/무효 조합 검증
   - NETWORK_DISPLAY_NAMES, NETWORK_NATIVE_SYMBOL ripple 항목 검증
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/shared test -- --run</automated>
  </verify>
  <done>CHAIN_TYPES에 'ripple', NETWORK_TYPES에 xrpl 3종, ENVIRONMENT_NETWORK_MAP에 ripple 매핑, 모든 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: @waiaas/core chain enum 확장 및 체인 상수 등록</name>
  <files>packages/core/src/enums/chain.ts, packages/core/src/utils/chain-constants.ts</files>
  <action>
1. packages/core/src/enums/chain.ts 수정:
   - @waiaas/shared에서 RIPPLE_NETWORK_TYPES, RippleNetworkType, RIPPLE_RPC_SETTING_KEYS를 추가 import 및 re-export (per INFRA-01)
   - ENVIRONMENT_SINGLE_NETWORK에 ripple 항목 추가 (per INFRA-04):
     ```
     'ripple:mainnet': 'xrpl-mainnet',
     'ripple:testnet': null,  // testnet과 devnet 중 선택 필요
     ```
     참고: Solana 패턴과 동일하게 ripple:mainnet은 단일 네트워크, ripple:testnet은 null (2개 네트워크이므로 명시 필요)
   - MAINNET_NETWORKS에 'xrpl-mainnet' 추가 (deriveEnvironment 함수에서 사용)

2. packages/core/src/utils/chain-constants.ts 수정 (per INFRA-08):
   - NATIVE_DECIMALS에 ripple: 6 추가 (XRP는 drops 단위, 1 XRP = 1,000,000 drops = 6 decimals)
   - NATIVE_SYMBOLS에 ripple: 'XRP' 추가
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core run build && pnpm --filter @waiaas/core test -- --run</automated>
  </verify>
  <done>core 패키지에서 ChainType='ripple'이 타입 검사 통과, ENVIRONMENT_SINGLE_NETWORK에 ripple 항목 존재, NATIVE_DECIMALS ripple=6, 빌드 및 테스트 통과</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/shared test -- --run` 통과
2. `pnpm --filter @waiaas/core run build` 빌드 성공
3. `pnpm --filter @waiaas/core test -- --run` 테스트 통과
4. TypeScript 타입에서 ChainType = 'solana' | 'ethereum' | 'ripple' 확인
</verification>

<success_criteria>
- 'ripple'이 ChainType Zod enum에서 유효한 값
- xrpl-mainnet/xrpl-testnet/xrpl-devnet가 NetworkType Zod enum에서 유효한 값
- ENVIRONMENT_NETWORK_MAP에 ripple:mainnet/testnet 매핑 존재
- NATIVE_DECIMALS.ripple === 6
- shared + core 패키지 빌드 및 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/470-ssot-extension-db-migration/470-01-SUMMARY.md`
</output>
