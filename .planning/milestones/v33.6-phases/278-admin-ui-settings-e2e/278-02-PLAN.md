---
phase: 278-admin-ui-settings-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/api/routes/admin.ts
  - packages/admin/src/api/endpoints.ts
  - packages/admin/src/pages/dashboard.tsx
autonomous: true
requirements: [ADMN-01]

must_haves:
  truths:
    - "Admin 대시보드에 DeFi Positions 섹션이 표시되어 총 DeFi 가치, 최악 헬스 팩터, 활성 포지션 수가 확인된다"
    - "GET /v1/admin/defi/positions 엔드포인트가 masterAuth로 보호되고 전체 지갑의 DeFi 포지션을 집계하여 반환한다"
    - "DeFi 포지션 테이블에 각 포지션의 카테고리, 프로바이더, 체인, 금액, USD 환산이 표시된다"
  artifacts:
    - path: "packages/daemon/src/api/routes/admin.ts"
      provides: "GET /admin/defi/positions cross-wallet aggregation endpoint"
      contains: "admin/defi/positions"
    - path: "packages/admin/src/pages/dashboard.tsx"
      provides: "DeFi Positions summary section + positions table"
      contains: "DefiSection"
    - path: "packages/admin/src/api/endpoints.ts"
      provides: "ADMIN_DEFI_POSITIONS endpoint constant"
      contains: "ADMIN_DEFI_POSITIONS"
  key_links:
    - from: "packages/admin/src/pages/dashboard.tsx"
      to: "packages/admin/src/api/endpoints.ts"
      via: "apiGet(API.ADMIN_DEFI_POSITIONS)"
      pattern: "ADMIN_DEFI_POSITIONS"
    - from: "packages/admin/src/api/endpoints.ts"
      to: "packages/daemon/src/api/routes/admin.ts"
      via: "/v1/admin/defi/positions"
      pattern: "admin/defi/positions"
---

<objective>
Admin 대시보드에 DeFi Positions 섹션을 추가하여 운영자가 전체 지갑의 DeFi 포지션 현황(총 가치, 헬스 팩터, 개수)을 한눈에 파악하고 개별 포지션 목록을 확인할 수 있도록 한다.

Purpose: 운영자가 Admin 대시보드에서 DeFi 포트폴리오의 전반적 상태를 모니터링할 수 있어야 한다 (ADMN-01).
Output: Admin DeFi endpoint + Dashboard DeFi 섹션 (StatCards + Table)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/278-admin-ui-settings-e2e/278-RESEARCH.md

@packages/daemon/src/api/routes/admin.ts
@packages/daemon/src/api/routes/defi-positions.ts
@packages/admin/src/pages/dashboard.tsx
@packages/admin/src/api/endpoints.ts
@packages/admin/src/components/table.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/admin/src/pages/dashboard.tsx:
```typescript
function StatCard({ label, value, loading, badge, href }: {
  label: string; value: string; loading?: boolean; badge?: 'success' | 'danger' | 'warning'; href?: string;
}) { /* ... */ }

// Table component import:
import { Table } from '../components/table';
import type { Column } from '../components/table';
```

From packages/admin/src/api/endpoints.ts:
```typescript
export const API = {
  ADMIN_STATUS: '/v1/admin/status',
  ADMIN_WALLET_STAKING: (id: string) => `/v1/admin/wallets/${id}/staking`,
  // ...
} as const;
```

From packages/daemon/src/api/routes/admin.ts (staking route pattern):
```typescript
const adminWalletStakingRoute = createRoute({
  method: 'get',
  path: '/admin/wallets/{id}/staking',
  tags: ['Admin'],
  summary: 'Get wallet staking positions',
  request: { params: z.object({ id: z.string().uuid() }) },
  responses: { 200: { ... }, ...buildErrorResponses([...]) },
});
```

From packages/daemon/src/api/routes/defi-positions.ts (DB query pattern):
```typescript
interface DefiPositionRow {
  id: string; wallet_id: string; category: string; provider: string;
  chain: string; network: string | null; asset_id: string | null;
  amount: string; amount_usd: number | null; metadata: string | null;
  status: string; opened_at: number; last_synced_at: number;
}
// Query: sqlite.prepare(`SELECT ... FROM defi_positions WHERE wallet_id = ? AND status = 'ACTIVE'`)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin DeFi positions 엔드포인트 추가 + endpoints.ts 상수</name>
  <files>
    packages/daemon/src/api/routes/admin.ts
    packages/admin/src/api/endpoints.ts
  </files>
  <action>
1. **`packages/admin/src/api/endpoints.ts`**에 새 엔드포인트 상수 추가:

```typescript
ADMIN_DEFI_POSITIONS: '/v1/admin/defi/positions',
```

기존 `ADMIN_WALLET_STAKING` 항목 아래에 추가.

2. **`packages/daemon/src/api/routes/admin.ts`**에 새 라우트 정의 + 핸들러 추가:

라우트 정의 (기존 라우트 정의 블록 내, `adminWalletStakingRoute` 근처에):

```typescript
const adminDefiPositionsRoute = createRoute({
  method: 'get',
  path: '/admin/defi/positions',
  tags: ['Admin'],
  summary: 'Get all DeFi positions across wallets',
  description:
    'Returns all active DeFi positions across all wallets with aggregated totals. ' +
    'Optionally filter by wallet_id. masterAuth required.',
  request: {
    query: z.object({
      wallet_id: z.string().uuid().optional(),
    }),
  },
  responses: {
    200: {
      description: 'Active DeFi positions with aggregates',
      content: {
        'application/json': {
          schema: z.object({
            positions: z.array(z.object({
              id: z.string(),
              walletId: z.string(),
              category: z.string(),
              provider: z.string(),
              chain: z.string(),
              network: z.string().nullable(),
              assetId: z.string().nullable(),
              amount: z.string(),
              amountUsd: z.number().nullable(),
              status: z.string(),
              openedAt: z.number(),
              lastSyncedAt: z.number(),
            })),
            totalValueUsd: z.number().nullable(),
            worstHealthFactor: z.number().nullable(),
            activeCount: z.number(),
          }),
        },
      },
    },
  },
});
```

핸들러 구현 (기존 adminWalletStakingRoute 핸들러 근처에):

```typescript
router.openapi(adminDefiPositionsRoute, async (c) => {
  const { wallet_id } = c.req.valid('query');

  if (!deps.sqlite) {
    return c.json({ positions: [], totalValueUsd: null, worstHealthFactor: null, activeCount: 0 }, 200);
  }

  // Cross-wallet DeFi positions query
  let rows: Array<{
    id: string; wallet_id: string; category: string; provider: string;
    chain: string; network: string | null; asset_id: string | null;
    amount: string; amount_usd: number | null; status: string;
    opened_at: number; last_synced_at: number;
  }>;

  if (wallet_id) {
    rows = deps.sqlite.prepare(
      `SELECT id, wallet_id, category, provider, chain, network, asset_id,
              amount, amount_usd, status, opened_at, last_synced_at
       FROM defi_positions
       WHERE wallet_id = ? AND status = 'ACTIVE'
       ORDER BY category, provider`
    ).all(wallet_id) as typeof rows;
  } else {
    rows = deps.sqlite.prepare(
      `SELECT id, wallet_id, category, provider, chain, network, asset_id,
              amount, amount_usd, status, opened_at, last_synced_at
       FROM defi_positions
       WHERE status = 'ACTIVE'
       ORDER BY category, provider`
    ).all() as typeof rows;
  }

  const positions = rows.map((row) => ({
    id: row.id,
    walletId: row.wallet_id,
    category: row.category,
    provider: row.provider,
    chain: row.chain,
    network: row.network,
    assetId: row.asset_id,
    amount: row.amount,
    amountUsd: row.amount_usd,
    status: row.status,
    openedAt: row.opened_at,
    lastSyncedAt: row.last_synced_at,
  }));

  // Aggregate totalValueUsd
  const usdValues = positions.map((p) => p.amountUsd).filter((v): v is number => v !== null);
  const totalValueUsd = usdValues.length > 0 ? usdValues.reduce((a, b) => a + b, 0) : null;

  // Worst health factor: query from metadata if available, else null
  // Health factor is stored in metadata JSON { healthFactor: X } for LENDING positions
  let worstHealthFactor: number | null = null;
  for (const row of rows) {
    if (row.category === 'LENDING') {
      try {
        const rawMeta = deps.sqlite.prepare(
          'SELECT metadata FROM defi_positions WHERE id = ?'
        ).get(row.id) as { metadata: string | null } | undefined;
        if (rawMeta?.metadata) {
          const meta = JSON.parse(rawMeta.metadata);
          if (typeof meta.healthFactor === 'number' && meta.healthFactor > 0) {
            if (worstHealthFactor === null || meta.healthFactor < worstHealthFactor) {
              worstHealthFactor = meta.healthFactor;
            }
          }
        }
      } catch { /* skip */ }
    }
  }

  return c.json({
    positions,
    totalValueUsd,
    worstHealthFactor,
    activeCount: positions.length,
  }, 200);
});
```

주의사항:
- masterAuth는 기존 admin 라우트 그룹이 미들웨어로 적용 중이므로 별도 추가 불필요
- `deps.sqlite` null guard 필수 (sqlite 미제공 시 빈 응답)
- 메타데이터 내 healthFactor 파싱은 안전하게 try/catch
- worstHealthFactor는 metadata 조회를 위한 추가 쿼리 대신, 초기 쿼리에 metadata 컬럼도 포함하여 최적화 가능. 단, 코드 간결성을 위해 별도 쿼리도 허용.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c 'admin/defi/positions' packages/daemon/src/api/routes/admin.ts && grep -c 'ADMIN_DEFI_POSITIONS' packages/admin/src/api/endpoints.ts</automated>
  </verify>
  <done>
    - GET /v1/admin/defi/positions가 모든 활성 DeFi 포지션 + totalValueUsd + worstHealthFactor + activeCount 반환
    - wallet_id 파라미터로 특정 지갑 필터링 가능
    - `API.ADMIN_DEFI_POSITIONS` 상수가 endpoints.ts에 정의됨
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard에 DeFi Positions 섹션 추가 (StatCards + Table)</name>
  <files>
    packages/admin/src/pages/dashboard.tsx
  </files>
  <action>
1. **import 추가**: `API`에서 `ADMIN_DEFI_POSITIONS`를 사용하므로 추가 import 불필요 (이미 `API` 전체를 import 중).

2. **DeFi position 타입 정의** (파일 상단, `AgentPromptResult` 인터페이스 아래에):

```typescript
interface DefiPositionSummary {
  positions: Array<{
    id: string;
    walletId: string;
    category: string;
    provider: string;
    chain: string;
    network: string | null;
    assetId: string | null;
    amount: string;
    amountUsd: number | null;
    status: string;
    openedAt: number;
    lastSyncedAt: number;
  }>;
  totalValueUsd: number | null;
  worstHealthFactor: number | null;
  activeCount: number;
}
```

3. **DeFi state signals** (`DashboardPage` 컴포넌트 내부, 기존 signal 선언부에):

```typescript
const defiData = useSignal<DefiPositionSummary | null>(null);
const defiLoading = useSignal(true);
```

4. **fetchDefi 함수** (`fetchStatus` 함수 근처에):

```typescript
const fetchDefi = async () => {
  defiLoading.value = true;
  try {
    const result = await apiGet<DefiPositionSummary>(API.ADMIN_DEFI_POSITIONS);
    defiData.value = result;
  } catch {
    // DeFi positions not available or empty -- keep null
  } finally {
    defiLoading.value = false;
  }
};
```

5. **useEffect에 fetchDefi() 호출 추가**: 기존 `useEffect` 블록 내 `fetchStatus()` 호출 직후에 `fetchDefi();` 추가.

6. **DeFi positions columns 함수** (`buildTxColumns` 함수 아래에):

```typescript
function buildDefiColumns(
  displayCurrency: string,
  displayRate: number | null,
): Column<DefiPositionSummary['positions'][number]>[] {
  return [
    { key: 'provider', header: 'Provider' },
    { key: 'category', header: 'Category', render: (p) => <Badge variant="info">{p.category}</Badge> },
    { key: 'chain', header: 'Chain', render: (p) => <Badge variant="info">{p.chain}</Badge> },
    { key: 'amount', header: 'Amount' },
    {
      key: 'amountUsd',
      header: 'USD Value',
      render: (p) => {
        if (p.amountUsd === null) return '\u2014';
        const display = formatWithDisplay(p.amountUsd, displayCurrency, displayRate);
        return display ?? `$${p.amountUsd.toFixed(2)}`;
      },
    },
  ];
}
```

7. **Dashboard JSX에 DeFi 섹션 추가**: Agent Connection Prompt 카드(`prompt-card`)와 Recent Activity(`<h3>Recent Activity</h3>`) 사이에 DeFi 섹션 삽입:

```tsx
{/* DeFi Positions Section */}
{defiData.value && defiData.value.activeCount > 0 && (
  <div style={{ marginTop: 'var(--space-4)' }}>
    <h3 style={{ marginBottom: 'var(--space-3)' }}>DeFi Positions</h3>
    <div class="stat-grid">
      <StatCard
        label="Total DeFi Value"
        value={defiData.value.totalValueUsd !== null
          ? (formatWithDisplay(defiData.value.totalValueUsd, displayCurrency.value, displayRate.value) ?? `$${defiData.value.totalValueUsd.toFixed(2)}`)
          : '\u2014'}
        loading={defiLoading.value}
      />
      <StatCard
        label="Health Factor"
        value={defiData.value.worstHealthFactor !== null
          ? defiData.value.worstHealthFactor.toFixed(2)
          : 'N/A'}
        loading={defiLoading.value}
        badge={defiData.value.worstHealthFactor !== null
          ? (defiData.value.worstHealthFactor < 1.2 ? 'danger' : defiData.value.worstHealthFactor < 1.5 ? 'warning' : 'success')
          : undefined}
      />
      <StatCard
        label="Active Positions"
        value={defiData.value.activeCount.toString()}
        loading={defiLoading.value}
      />
    </div>
    <div style={{ marginTop: 'var(--space-3)' }}>
      <Table
        columns={buildDefiColumns(displayCurrency.value, displayRate.value)}
        data={defiData.value.positions}
        loading={defiLoading.value}
        emptyMessage="No active DeFi positions"
      />
    </div>
  </div>
)}
```

주의사항:
- DeFi 섹션은 `activeCount > 0` 일 때만 표시 (포지션 없으면 숨김)
- HF badge: < 1.2 → 'danger', < 1.5 → 'warning', >= 1.5 → 'success'
- `formatWithDisplay`는 이미 import 되어 있음 (기존 dashboard에서 사용)
- 30초 auto-refresh에 fetchDefi도 포함시키기: 기존 `setInterval(fetchStatus, 30_000)` → `setInterval(() => { fetchStatus(); fetchDefi(); }, 30_000)`
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c 'DeFi Positions' packages/admin/src/pages/dashboard.tsx && grep -c 'ADMIN_DEFI_POSITIONS' packages/admin/src/pages/dashboard.tsx</automated>
  </verify>
  <done>
    - Dashboard에 "DeFi Positions" 섹션이 렌더링됨 (StatCards 3개 + 포지션 테이블)
    - Total DeFi Value, Health Factor (color badge), Active Positions 통계 카드 표시
    - 포지션 테이블에 Provider, Category, Chain, Amount, USD Value 컬럼
    - 30초 자동 갱신에 DeFi 데이터 포함
    - 포지션 0개 시 섹션 미표시
  </done>
</task>

</tasks>

<verification>
1. `grep 'admin/defi/positions' packages/daemon/src/api/routes/admin.ts` → route 정의 + 핸들러 존재
2. `grep 'ADMIN_DEFI_POSITIONS' packages/admin/src/api/endpoints.ts` → 상수 존재
3. `grep 'DeFi Positions' packages/admin/src/pages/dashboard.tsx` → 섹션 제목 존재
4. `grep 'buildDefiColumns' packages/admin/src/pages/dashboard.tsx` → 컬럼 빌더 존재
5. `grep 'fetchDefi' packages/admin/src/pages/dashboard.tsx` → 데이터 fetch 함수 존재
</verification>

<success_criteria>
- GET /v1/admin/defi/positions가 masterAuth로 보호되고 cross-wallet DeFi 포지션을 반환
- Dashboard DeFi 섹션에 총 가치, HF, 포지션 수 StatCards + 포지션 테이블 표시
- HF badge가 1.2 미만 시 danger, 1.5 미만 시 warning, 이상 시 success
- 30초 자동 갱신에 DeFi 데이터 포함
</success_criteria>

<output>
After completion, create `.planning/phases/278-admin-ui-settings-e2e/278-02-SUMMARY.md`
</output>
