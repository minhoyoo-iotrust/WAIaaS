---
phase: 278-admin-ui-settings-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/admin/src/pages/actions.tsx
  - packages/admin/src/utils/settings-helpers.ts
  - packages/daemon/src/services/defi/position-tracker.ts
  - packages/daemon/src/services/monitoring/health-factor-monitor.ts
autonomous: true
requirements: [ADMN-02, ADMN-03, ADMN-04, ADMN-05]

must_haves:
  truths:
    - "4개 Aave V3 설정 키가 SETTING_DEFINITIONS에 존재하고 SettingsService.get()으로 조회 가능하다"
    - "Admin Actions 페이지에서 Aave V3 카드가 표시되고 enabled 토글이 동작한다"
    - "Actions 페이지에서 HF threshold, sync interval, max LTV 3개 고급 설정 필드가 Aave V3 카드 내에 표시된다"
    - "PositionTracker가 settingsService에서 position_sync_interval_sec 값을 읽어 LENDING 폴링 주기를 오버라이드한다"
    - "HealthFactorMonitor가 settingsService에서 health_factor_warning_threshold 값을 읽어 경고 임계값으로 사용한다"
  artifacts:
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "4 Aave V3 setting definitions"
      contains: "actions.aave_v3_enabled"
    - path: "packages/admin/src/pages/actions.tsx"
      provides: "Aave V3 BUILTIN_PROVIDERS entry + advanced settings section"
      contains: "aave_v3"
    - path: "packages/admin/src/utils/settings-helpers.ts"
      provides: "Human-readable labels for 4 Aave V3 keys"
      contains: "aave_v3_health_factor_warning_threshold"
  key_links:
    - from: "packages/admin/src/pages/actions.tsx"
      to: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      via: "BUILTIN_PROVIDERS key 'aave_v3' -> settings key 'actions.aave_v3_enabled'"
      pattern: "actions\\.aave_v3_enabled"
    - from: "packages/daemon/src/services/defi/position-tracker.ts"
      to: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      via: "settingsService.get('actions.aave_v3_position_sync_interval_sec')"
      pattern: "aave_v3_position_sync_interval_sec"
---

<objective>
Aave V3 런타임 설정 키 4개를 SSoT(`setting-keys.ts`)에 추가하고, Admin Actions 페이지에 Aave V3 카드(enabled 토글 + 고급 설정 필드)를 추가하며, PositionTracker와 HealthFactorMonitor에 settingsService 통합을 완성한다.

Purpose: 운영자가 Admin UI에서 Aave V3 프로바이더를 활성화/비활성화하고, HF 경고 임계값/폴링 주기/최대 LTV를 런타임에 조정할 수 있어야 한다.
Output: 4개 설정 키 + Actions 페이지 Aave V3 카드 + 서비스 settingsService 연동
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/278-admin-ui-settings-e2e/278-RESEARCH.md

@packages/daemon/src/infrastructure/settings/setting-keys.ts
@packages/admin/src/pages/actions.tsx
@packages/admin/src/utils/settings-helpers.ts
@packages/daemon/src/services/defi/position-tracker.ts
@packages/daemon/src/services/monitoring/health-factor-monitor.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/daemon/src/infrastructure/settings/setting-keys.ts:
```typescript
export interface SettingDefinition {
  key: string;
  category: string;
  configPath: string;
  defaultValue: string;
  isCredential: boolean;
}

export const SETTING_CATEGORIES = [
  'notifications', 'rpc', 'security', 'daemon', 'walletconnect',
  'oracle', 'display', 'autostop', 'monitoring', 'telegram',
  'signing_sdk', 'incoming', 'actions', 'policy', 'gas_condition', 'rpc_pool',
] as const;
```

From packages/admin/src/pages/actions.tsx:
```typescript
interface BuiltinProvider {
  key: string;
  name: string;
  description: string;
  chain: 'solana' | 'evm' | 'multi';
  requiresApiKey: boolean;
  docsUrl?: string;
}

const BUILTIN_PROVIDERS: BuiltinProvider[] = [
  { key: 'jupiter_swap', ... },
  { key: 'zerox_swap', ... },
  { key: 'lifi', ... },
  { key: 'lido_staking', ... },
  { key: 'jito_staking', ... },
];
```

From packages/admin/src/utils/settings-helpers.ts:
```typescript
export function keyToLabel(key: string): string { /* map: Record<string, string> */ }
```

From packages/daemon/src/services/defi/position-tracker.ts:
```typescript
export class PositionTracker {
  constructor(opts: { sqlite: Database; settingsService?: SettingsService }) {
    this.sqlite = opts.sqlite;
    // settingsService reserved for future runtime config overrides (Phase 278)
    this.writeQueue = new PositionWriteQueue();
  }
  start(): void {
    for (const category of POSITION_CATEGORIES) {
      const intervalMs = DEFAULT_INTERVALS[category];  // hardcoded
      const timer = setInterval(() => void this.syncCategory(category), intervalMs);
      // ...
    }
  }
}
```

From packages/daemon/src/services/monitoring/health-factor-monitor.ts:
```typescript
updateConfig(config: Record<string, unknown>): void {
  if (typeof config['health_factor_warning_threshold'] === 'number') {
    this.config.warningThreshold = config['health_factor_warning_threshold'];
  }
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aave V3 설정 키 4개를 setting-keys.ts SSoT에 추가 + keyToLabel 레이블 추가</name>
  <files>
    packages/daemon/src/infrastructure/settings/setting-keys.ts
    packages/admin/src/utils/settings-helpers.ts
  </files>
  <action>
1. `packages/daemon/src/infrastructure/settings/setting-keys.ts`의 SETTING_DEFINITIONS 배열에서 `// --- Jito Staking ---` 블록 다음(Jito 항목 3개 뒤), `// --- gas_condition category ---` 블록 전에 다음 4개 항목을 추가:

```typescript
  // --- Aave V3 Lending ---
  { key: 'actions.aave_v3_enabled', category: 'actions', configPath: 'actions.aave_v3_enabled', defaultValue: 'true', isCredential: false },
  { key: 'actions.aave_v3_health_factor_warning_threshold', category: 'actions', configPath: 'actions.aave_v3_health_factor_warning_threshold', defaultValue: '1.2', isCredential: false },
  { key: 'actions.aave_v3_position_sync_interval_sec', category: 'actions', configPath: 'actions.aave_v3_position_sync_interval_sec', defaultValue: '300', isCredential: false },
  { key: 'actions.aave_v3_max_ltv_pct', category: 'actions', configPath: 'actions.aave_v3_max_ltv_pct', defaultValue: '0.8', isCredential: false },
```

주의사항:
- `category`는 기존 `'actions'` 사용 (새 카테고리 추가 금지)
- `configPath`는 `actions.aave_v3_*` 패턴 (Lido/Jito 패턴과 동일)
- `defaultValue`는 문자열 (Zod SSoT에서 파생)
- `isCredential: false` (모두 공개 설정)

2. `packages/admin/src/utils/settings-helpers.ts`의 `keyToLabel()` 함수 내 `map` 객체에 다음 4개 항목 추가 (기존 `// gas_condition keys` 주석 직전에):

```typescript
    // aave_v3 keys
    aave_v3_enabled: 'Aave V3 Enabled',
    aave_v3_health_factor_warning_threshold: 'HF Warning Threshold',
    aave_v3_position_sync_interval_sec: 'Position Sync Interval (seconds)',
    aave_v3_max_ltv_pct: 'Max LTV Percentage',
```
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsx -e "
      const { SETTING_DEFINITIONS, getSettingDefinition } = require('./packages/daemon/src/infrastructure/settings/setting-keys');
      const keys = ['actions.aave_v3_enabled', 'actions.aave_v3_health_factor_warning_threshold', 'actions.aave_v3_position_sync_interval_sec', 'actions.aave_v3_max_ltv_pct'];
      for (const k of keys) {
        const def = getSettingDefinition(k);
        if (!def) throw new Error('Missing: ' + k);
        if (def.category !== 'actions') throw new Error('Wrong category: ' + k);
      }
      console.log('All 4 Aave V3 setting keys verified');
    " 2>/dev/null || echo "Verify via: grep 'aave_v3' packages/daemon/src/infrastructure/settings/setting-keys.ts | wc -l (expect 4)"</automated>
  </verify>
  <done>
    - `getSettingDefinition('actions.aave_v3_enabled')` 등 4개 키가 정상 반환
    - `keyToLabel('aave_v3_health_factor_warning_threshold')` → 'HF Warning Threshold' 반환
  </done>
</task>

<task type="auto">
  <name>Task 2: Actions 페이지에 Aave V3 카드 추가 (toggle + 고급 설정 필드)</name>
  <files>
    packages/admin/src/pages/actions.tsx
  </files>
  <action>
1. `BUILTIN_PROVIDERS` 배열 끝(Jito 다음)에 Aave V3 항목 추가:

```typescript
  { key: 'aave_v3', name: 'Aave V3 Lending', description: 'EVM lending protocol (supply, borrow, repay, withdraw)', chain: 'evm', requiresApiKey: false, docsUrl: 'https://docs.aave.com/developers' },
```

2. Aave V3 카드 내에 고급 설정(HF threshold, sync interval, max LTV)을 표시하는 인라인 섹션을 구현한다. 기존 `settings-category-body` div 안에, `requiresApiKey` 조건 블록과 actions list 사이에 "Aave V3 advanced settings" 블록을 추가한다.

구현 접근법:
- `bp.key === 'aave_v3'` 조건으로 고급 설정 섹션을 Aave V3 카드에만 렌더링
- 3개 설정 필드: `aave_v3_health_factor_warning_threshold` (number input), `aave_v3_position_sync_interval_sec` (number input), `aave_v3_max_ltv_pct` (number input)
- 각 필드 값은 `settings.value['actions']` 에서 읽기 (예: `cat?.['aave_v3_health_factor_warning_threshold'] ?? '1.2'`)
- 값 변경 시 `apiPut(API.ADMIN_SETTINGS, { settings: [{ key: 'actions.aave_v3_health_factor_warning_threshold', value: newValue }] })` 호출 (기존 `handleToggle` 패턴과 유사한 즉시 저장 패턴)
- 새 상태 시그널 `advancedSaving`을 추가하여 저장 중 상태 표시

고급 설정 섹션 구조:
```tsx
{bp.key === 'aave_v3' && enabled && (
  <div style={{ borderTop: '1px solid var(--border)', paddingTop: 'var(--space-3)', marginBottom: 'var(--space-3)' }}>
    <div style={{ fontWeight: 600, fontSize: 'var(--font-size-sm)', marginBottom: 'var(--space-2)', color: 'var(--text-secondary)' }}>
      Advanced Settings
    </div>
    {/* FormField for each of 3 settings, with onBlur save handler */}
  </div>
)}
```

각 FormField는 `type="text"` (숫자 입력이지만 문자열 저장), label은 `keyToLabel()` 함수 사용.

3. 고급 설정 저장 핸들러 `handleAdvancedSave` 함수를 컴포넌트 내부에 추가:

```typescript
const handleAdvancedSave = async (settingKey: string, value: string) => {
  const fullKey = `actions.${settingKey}`;
  try {
    const result = await apiPut<{ updated: number; settings: SettingsData }>(API.ADMIN_SETTINGS, {
      settings: [{ key: fullKey, value }],
    });
    settings.value = result.settings;
    showToast('success', `Setting updated`);
  } catch (err) {
    const e = err instanceof ApiError ? err : new ApiError(0, 'UNKNOWN', 'Unknown error');
    showToast('error', getErrorMessage(e.code));
  }
};
```

4. `import { keyToLabel } from '../utils/settings-helpers';` 를 추가.

주의: `handleAdvancedSave`를 위한 dirty/local state 관리가 필요하다. `advancedDirty` 시그널(`useSignal<Record<string, string>>({})``)을 선언하고, 입력 시 dirty에 쓰고, onBlur 시 저장.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c 'aave_v3' packages/admin/src/pages/actions.tsx</automated>
  </verify>
  <done>
    - BUILTIN_PROVIDERS에 Aave V3 항목이 포함되어 6개 프로바이더 카드 렌더링
    - Aave V3 카드 내 "Advanced Settings" 섹션에 3개 설정 필드 표시
    - 필드 값 변경 후 blur 시 즉시 Admin Settings API로 저장
  </done>
</task>

<task type="auto">
  <name>Task 3: PositionTracker + HealthFactorMonitor에 settingsService 연동</name>
  <files>
    packages/daemon/src/services/defi/position-tracker.ts
    packages/daemon/src/services/monitoring/health-factor-monitor.ts
  </files>
  <action>
1. **PositionTracker** (`packages/daemon/src/services/defi/position-tracker.ts`):
   - constructor에서 `settingsService`를 private 멤버로 저장:
     ```typescript
     private readonly settingsService?: SettingsService;

     constructor(opts: { sqlite: Database; settingsService?: SettingsService }) {
       this.sqlite = opts.sqlite;
       this.settingsService = opts.settingsService;
       this.writeQueue = new PositionWriteQueue();
     }
     ```
   - `start()` 메서드에서 LENDING 카테고리의 interval을 settingsService에서 읽도록 수정:
     ```typescript
     start(): void {
       for (const category of POSITION_CATEGORIES) {
         this.running.set(category, false);
         let intervalMs = DEFAULT_INTERVALS[category];
         // Runtime override from Admin Settings (Phase 278)
         if (category === 'LENDING' && this.settingsService) {
           try {
             const val = this.settingsService.get('actions.aave_v3_position_sync_interval_sec');
             const parsed = Number(val);
             if (!Number.isNaN(parsed) && parsed > 0) {
               intervalMs = parsed * 1000; // seconds -> milliseconds
             }
           } catch { /* fallback to default */ }
         }
         const timer = setInterval(() => void this.syncCategory(category), intervalMs);
         timer.unref();
         this.timers.set(category, timer);
       }
     }
     ```
   - 기존 "reserved for Phase 278" 주석을 제거

2. **HealthFactorMonitor** (`packages/daemon/src/services/monitoring/health-factor-monitor.ts`):
   - 이미 `updateConfig()` 메서드가 있으므로 새로운 메서드 추가 불필요
   - constructor 또는 초기화 시 settingsService에서 값을 읽는 `loadFromSettings()` 메서드를 추가:
     ```typescript
     loadFromSettings(settingsService: SettingsService): void {
       try {
         const threshold = settingsService.get('actions.aave_v3_health_factor_warning_threshold');
         const parsed = Number(threshold);
         if (!Number.isNaN(parsed) && parsed > 0) {
           this.updateConfig({ health_factor_warning_threshold: parsed });
         }
       } catch { /* fallback to default */ }
       try {
         const maxLtv = settingsService.get('actions.aave_v3_max_ltv_pct');
         const parsed = Number(maxLtv);
         if (!Number.isNaN(parsed) && parsed > 0 && parsed <= 1) {
           this.updateConfig({ max_ltv_pct: parsed });
         }
       } catch { /* fallback to default */ }
     }
     ```
   - `import type { SettingsService } from '../../infrastructure/settings/settings-service.js';` 추가 (이미 있으면 생략)
   - `updateConfig()`에 `max_ltv_pct` 핸들링 추가:
     ```typescript
     if (typeof config['max_ltv_pct'] === 'number') {
       // Store for LendingPolicyEvaluator reference
       this.config.maxLtvPct = config['max_ltv_pct'];
     }
     ```
   - config 인터페이스에 `maxLtvPct?: number` 필드 추가 (선택적이고 실제 사용은 LendingPolicyEvaluator에서)

주의: settingsService 통합은 "읽기만" 패턴. 다음 sync cycle에서 새 값을 자연스럽게 반영. change listener나 timer restart는 불필요(리서치 권고사항 준수).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c 'settingsService' packages/daemon/src/services/defi/position-tracker.ts && grep -c 'loadFromSettings' packages/daemon/src/services/monitoring/health-factor-monitor.ts</automated>
  </verify>
  <done>
    - PositionTracker.start()가 `actions.aave_v3_position_sync_interval_sec` 설정을 읽어 LENDING 폴링 주기를 오버라이드
    - HealthFactorMonitor.loadFromSettings()가 `actions.aave_v3_health_factor_warning_threshold` 설정을 읽어 경고 임계값을 업데이트
    - 설정이 없거나 파싱 실패 시 기본값(5분, 1.2) 유지
  </done>
</task>

</tasks>

<verification>
1. `grep 'aave_v3' packages/daemon/src/infrastructure/settings/setting-keys.ts` → 4줄
2. `grep 'aave_v3' packages/admin/src/pages/actions.tsx` → 1+ 줄 (BUILTIN_PROVIDERS + advanced settings)
3. `grep 'aave_v3' packages/admin/src/utils/settings-helpers.ts` → 4줄
4. PositionTracker constructor가 settingsService를 저장하고 start()에서 사용
5. HealthFactorMonitor에 loadFromSettings() 메서드 존재
</verification>

<success_criteria>
- SETTING_DEFINITIONS에 4개 Aave V3 키가 정의됨
- Admin Actions 페이지에 6개 프로바이더 카드 렌더링 (기존 5 + Aave V3)
- Aave V3 카드에 enabled 토글 + 3개 고급 설정 필드 표시
- keyToLabel()이 4개 키에 대해 사람이 읽을 수 있는 레이블 반환
- PositionTracker가 settings에서 LENDING 폴링 주기 오버라이드
- HealthFactorMonitor가 settings에서 HF 경고 임계값 로드
</success_criteria>

<output>
After completion, create `.planning/phases/278-admin-ui-settings-e2e/278-01-SUMMARY.md`
</output>
