---
phase: 278-admin-ui-settings-e2e
plan: 03
type: execute
wave: 2
depends_on: [278-01, 278-02]
files_modified:
  - packages/admin/src/__tests__/actions-aave-v3.test.tsx
  - packages/admin/src/__tests__/dashboard-defi.test.tsx
  - packages/daemon/src/__tests__/aave-lending-e2e.test.ts
autonomous: true
requirements: [ADMN-01, ADMN-02, ADMN-03, ADMN-04, ADMN-05]

must_haves:
  truths:
    - "Actions 페이지 Aave V3 카드 렌더링 + toggle + 고급 설정 저장이 테스트로 검증된다"
    - "Dashboard DeFi 섹션 렌더링 + StatCards + HF badge 색상이 테스트로 검증된다"
    - "supply -> position-sync -> health-check -> borrow -> HF-warning 전체 플로우가 E2E로 검증된다"
    - "모든 테스트가 기존 커버리지 임계값을 유지한다"
  artifacts:
    - path: "packages/admin/src/__tests__/actions-aave-v3.test.tsx"
      provides: "Actions Aave V3 card UI tests"
      min_lines: 50
    - path: "packages/admin/src/__tests__/dashboard-defi.test.tsx"
      provides: "Dashboard DeFi section UI tests"
      min_lines: 50
    - path: "packages/daemon/src/__tests__/aave-lending-e2e.test.ts"
      provides: "Full lending flow E2E integration test"
      min_lines: 80
  key_links:
    - from: "packages/admin/src/__tests__/actions-aave-v3.test.tsx"
      to: "packages/admin/src/pages/actions.tsx"
      via: "render(ActionsPage) + mock apiGet/apiPut"
      pattern: "Aave V3 Lending"
    - from: "packages/daemon/src/__tests__/aave-lending-e2e.test.ts"
      to: "packages/daemon/src/services/monitoring/health-factor-monitor.ts"
      via: "HealthFactorMonitor mock + LIQUIDATION_WARNING assertion"
      pattern: "LIQUIDATION_WARNING"
---

<objective>
Phase 278의 모든 구현(Settings, Actions 카드, Dashboard 섹션, 서비스 연동)을 테스트로 검증하고, supply -> position-sync -> health-check -> borrow -> HF-warning 전체 Lending 플로우를 E2E 통합 테스트로 확인한다.

Purpose: 모든 ADMN-* 요구사항이 자동화된 테스트로 검증되어야 하며, 전체 Lending 플로우의 정합성이 E2E로 확인되어야 한다.
Output: Admin UI 테스트 2개 + Daemon E2E 테스트 1개
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/278-admin-ui-settings-e2e/278-RESEARCH.md
@.planning/phases/278-admin-ui-settings-e2e/278-01-SUMMARY.md
@.planning/phases/278-admin-ui-settings-e2e/278-02-SUMMARY.md

@packages/admin/src/__tests__/actions.test.tsx
@packages/admin/src/pages/actions.tsx
@packages/admin/src/pages/dashboard.tsx
@packages/daemon/src/__tests__/health-factor-monitor.test.ts
@packages/daemon/src/__tests__/lido-staking-integration.test.ts

<interfaces>
<!-- Key test patterns the executor needs. -->

From packages/admin/src/__tests__/actions.test.tsx (mock pattern):
```typescript
vi.mock('../api/client', () => ({
  apiGet: vi.fn(), apiPost: vi.fn(), apiPut: vi.fn(), apiDelete: vi.fn(),
  ApiError: class ApiError extends Error { status: number; code: string; ... },
  apiCall: vi.fn(),
}));
vi.mock('../components/toast', () => ({ showToast: vi.fn(), ToastContainer: () => null }));
vi.mock('../auth/store', () => ({ masterPassword: { value: 'test-pw' }, isAuthenticated: { value: true }, ... }));
vi.mock('../utils/error-messages', () => ({ getErrorMessage: (code: string) => `Error: ${code}` }));
vi.mock('../components/settings-search', () => ({ pendingNavigation: { value: null }, highlightField: { value: '' } }));
vi.mock('../utils/dirty-guard', () => ({ registerDirty: vi.fn(), unregisterDirty: vi.fn(), hasDirty: { value: false } }));

function mockApiCalls(settingsData, apiKeysData, providersData) {
  vi.mocked(apiGet).mockImplementation(async (path: string) => {
    if (path === '/v1/admin/settings') return settingsData;
    if (path === '/v1/admin/api-keys') return apiKeysData;
    if (path === '/v1/actions/providers') return providersData;
    return {};
  });
}
```

From packages/daemon/src/__tests__/health-factor-monitor.test.ts (pattern):
```typescript
// Uses in-memory SQLite, mock NotificationService
// beforeEach: create DB, push schema, create monitor instance
// Tests check that notify() is called with correct event type
```

From packages/daemon/src/__tests__/lido-staking-integration.test.ts (pattern):
```typescript
// Uses ActionProviderRegistry, mock settingsReader
// registerBuiltInProviders(registry, settingsReader, { rpcCaller })
// Tests verify resolve returns correct ContractCallRequest[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actions 페이지 Aave V3 카드 + Dashboard DeFi 섹션 Admin UI 테스트</name>
  <files>
    packages/admin/src/__tests__/actions-aave-v3.test.tsx
    packages/admin/src/__tests__/dashboard-defi.test.tsx
  </files>
  <action>
**1. `packages/admin/src/__tests__/actions-aave-v3.test.tsx`** 생성:

기존 `actions.test.tsx`의 mock 패턴을 완전히 복제하여 Aave V3 카드에 집중하는 테스트 파일 생성.

```typescript
/**
 * actions-aave-v3.test.tsx
 *
 * Tests for Aave V3 card in Actions page:
 * - Aave V3 card renders with correct info
 * - Toggle enable/disable works
 * - Advanced settings section shows when enabled
 * - Advanced settings save calls apiPut with correct key
 */
import { describe, it, expect, vi, afterEach } from 'vitest';
import { render, screen, waitFor, fireEvent, cleanup } from '@testing-library/preact';

// ... same mock block as actions.test.tsx ...

import { apiGet, apiPut } from '../api/client';
import { showToast } from '../components/toast';
import ActionsPage from '../pages/actions';
```

테스트 케이스:

a) **rendering**: `'renders Aave V3 Lending card'` - mockApiCalls 후 렌더링, `screen.getByText('Aave V3 Lending')` 확인, description 확인

b) **toggle**: `'toggle Aave V3 calls apiPut with actions.aave_v3_enabled'` - checkbox change → apiPut 호출 확인

c) **advanced settings visible**: `'shows Advanced Settings section when Aave V3 is enabled'` - settings에 `aave_v3_enabled: 'true'` 설정 후, 'Advanced Settings' 텍스트 존재 확인, HF threshold / sync interval / max LTV 필드 확인

d) **advanced settings save**: `'saving advanced setting calls apiPut with correct key'` - 설정 필드 값 변경 후 blur → apiPut 호출 시 `{ settings: [{ key: 'actions.aave_v3_health_factor_warning_threshold', value: '...' }] }` 확인

e) **card shows Inactive when disabled**: mockSettings에서 aave_v3_enabled: 'false'일 때 해당 카드에 Inactive 뱃지 표시 확인 (전체 6개 카드 중 6개 Inactive).

Mock 데이터:
```typescript
const mockSettingsAaveEnabled = {
  actions: {
    jupiter_swap_enabled: 'false',
    zerox_swap_enabled: 'false',
    lifi_enabled: 'false',
    lido_staking_enabled: 'false',
    jito_staking_enabled: 'false',
    aave_v3_enabled: 'true',
    aave_v3_health_factor_warning_threshold: '1.2',
    aave_v3_position_sync_interval_sec: '300',
    aave_v3_max_ltv_pct: '0.8',
  },
};

const mockSettingsAllDisabled = {
  actions: {
    jupiter_swap_enabled: 'false',
    zerox_swap_enabled: 'false',
    lifi_enabled: 'false',
    lido_staking_enabled: 'false',
    jito_staking_enabled: 'false',
    aave_v3_enabled: 'false',
  },
};
```

**2. `packages/admin/src/__tests__/dashboard-defi.test.tsx`** 생성:

기존 dashboard 테스트 패턴(mock apiGet, render DashboardPage)을 사용하되 DeFi 섹션에 집중.

```typescript
/**
 * dashboard-defi.test.tsx
 *
 * Tests for DeFi Positions section in Dashboard:
 * - Shows DeFi Positions section when positions exist
 * - Hides DeFi Positions section when no positions
 * - StatCards show correct values (Total DeFi Value, HF, Active Positions)
 * - HF badge shows correct color (danger/warning/success)
 */
```

Mock 패턴: `apiGet` mock에 `/v1/admin/defi/positions` 응답 추가.

테스트 케이스:

a) `'renders DeFi Positions section when active positions exist'` - mock defi positions data 반환 → 'DeFi Positions' 텍스트 확인, 'Total DeFi Value' 확인, 'Health Factor' 확인, 'Active Positions' 확인

b) `'hides DeFi section when activeCount is 0'` - mock empty defi data → 'DeFi Positions' 텍스트 부재 확인

c) `'shows danger badge for HF < 1.2'` - worstHealthFactor: 1.1 → danger badge 확인

d) `'shows warning badge for 1.2 <= HF < 1.5'` - worstHealthFactor: 1.3 → warning badge 확인

e) `'shows success badge for HF >= 1.5'` - worstHealthFactor: 2.0 → success badge 확인

Mock defi data:
```typescript
const mockDefiPositions = {
  positions: [
    { id: 'pos-1', walletId: 'w-1', category: 'LENDING', provider: 'aave_v3', chain: 'ethereum', network: 'ethereum-mainnet', assetId: null, amount: '1000', amountUsd: 2500.00, status: 'ACTIVE', openedAt: 1700000000, lastSyncedAt: 1700001000 },
  ],
  totalValueUsd: 2500.00,
  worstHealthFactor: 1.8,
  activeCount: 1,
};
```

주의:
- Dashboard 테스트에서 `apiGet` mock은 기존 `/v1/admin/status` + `/v1/admin/transactions` + `/v1/admin/defi/positions` 모두 처리해야 함
- displayCurrency 관련 mock도 필요 (기존 패턴 복사)
- 기존 `packages/admin/vitest.config.ts` 커버리지 임계값(77% branches, 70% functions, 84% lines, 84% statements) 유지 확인
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin run test -- --run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - actions-aave-v3.test.tsx의 5개 테스트 모두 통과
    - dashboard-defi.test.tsx의 5개 테스트 모두 통과
    - Admin 패키지 커버리지 임계값 유지
  </done>
</task>

<task type="auto">
  <name>Task 2: Aave V3 Lending E2E 통합 테스트</name>
  <files>
    packages/daemon/src/__tests__/aave-lending-e2e.test.ts
  </files>
  <action>
`packages/daemon/src/__tests__/aave-lending-e2e.test.ts` 생성.

기존 `lido-staking-integration.test.ts` + `health-factor-monitor.test.ts` 하이브리드 패턴.

```typescript
/**
 * aave-lending-e2e.test.ts
 *
 * E2E integration test for the full Aave V3 lending flow:
 * supply -> position-sync -> health-check -> borrow -> HF-warning
 *
 * Uses in-memory SQLite, mock RPC responses, mock NotificationService.
 * Pattern: lido-staking-integration.test.ts + health-factor-monitor.test.ts
 */
```

**테스트 구조:**

```typescript
describe('Aave V3 Lending E2E flow', () => {
  let sqlite: Database;

  beforeEach(() => {
    // Create in-memory DB with full schema (createDatabase + pushSchema pattern)
    // Insert test wallet
  });

  afterEach(() => {
    sqlite.close();
  });

  it('resolves supply action to ContractCallRequest with approve step', async () => {
    // 1. Import AaveV3LendingProvider or registerBuiltInProviders
    // 2. Create settings reader with aave_v3_enabled: 'true'
    // 3. Call resolve for aave_supply action
    // 4. Verify result contains 2 ContractCallRequests (approve + supply)
    // 5. Verify approve calldata starts with ERC-20 approve selector (0x095ea7b3)
    // 6. Verify supply calldata starts with Pool.supply() selector (0x617ba037)
  });

  it('position sync writes positions to DB', async () => {
    // 1. Create PositionTracker with sqlite + mock settingsService
    // 2. Register a mock IPositionProvider that returns test positions
    // 3. Call syncCategory('LENDING')
    // 4. Query defi_positions table
    // 5. Verify positions exist in DB with correct wallet_id, category, provider
  });

  it('health factor monitor sends LIQUIDATION_WARNING when HF < threshold', async () => {
    // 1. Insert LENDING position in defi_positions with metadata.healthFactor = 1.15
    // 2. Create HealthFactorMonitor with mock NotificationService
    // 3. Call loadFromSettings() with HF threshold = 1.2
    // 4. Call checkAllPositions()
    // 5. Verify mockNotify was called with event type 'LIQUIDATION_WARNING'
    // 6. Verify notification includes wallet_id and healthFactor value
  });

  it('health factor monitor does NOT warn when HF >= threshold', async () => {
    // 1. Insert LENDING position with metadata.healthFactor = 2.5
    // 2. Create HealthFactorMonitor with threshold 1.2
    // 3. Call checkAllPositions()
    // 4. Verify mockNotify was NOT called
  });

  it('settings override changes position sync interval', () => {
    // 1. Create mock SettingsService that returns '120' for position_sync_interval_sec
    // 2. Create PositionTracker with that settingsService
    // 3. Verify that PositionTracker reads the setting (indirect: verify constructor stores settingsService)
    // This is a unit-level check since actually testing timers is flaky
  });

  it('full flow: supply resolve -> DB position -> HF check -> warning', async () => {
    // This is THE E2E test that ties everything together.
    // 1. Settings: aave_v3_enabled = true, HF threshold = 1.2
    // 2. Resolve supply action -> verify ContractCallRequest[]
    // 3. Simulate post-supply: insert position in DB (category=LENDING, provider=aave_v3)
    //    with metadata.healthFactor = 2.5 (safe after supply)
    // 4. HealthFactorMonitor check -> verify NO warning
    // 5. Simulate borrow impact: update position metadata.healthFactor to 1.15
    // 6. HealthFactorMonitor check -> verify LIQUIDATION_WARNING sent
    // 7. Verify notification includes correct wallet ID and HF value
  });
});
```

구현 지침:
- DB 초기화: 기존 `health-factor-monitor.test.ts`의 `createDatabase(':memory:')` + `pushSchema(sqlite)` 패턴 사용
- 테스트 지갑 삽입: `sqlite.prepare('INSERT INTO wallets ...').run(...)` 또는 기존 테스트 헬퍼
- Mock NotificationService: `{ notify: vi.fn() }` 패턴
- Mock SettingsService: `{ get: vi.fn((key) => { if (key === 'actions.aave_v3_enabled') return 'true'; ... }) }`
- HF check에서 `checkAllPositions()`가 없으면 `checkPositions()` 또는 해당 메서드명을 실제 코드에서 확인하여 사용
- 중요: `defi_positions` 테이블 삽입 시 모든 NOT NULL 컬럼 값 제공 (id, wallet_id, category, provider, chain, amount, status, opened_at, last_synced_at)
- metadata 컬럼에 `JSON.stringify({ healthFactor: 1.15 })` 저장

주의사항:
- 실제 RPC 호출 없음 (모든 체인 호출 모킹)
- registerBuiltInProviders는 rpcCaller 의존 → mock rpcCaller 제공
- E2E 테스트가 기존 daemon 커버리지 임계값을 낮추지 않도록 주의
- 테스트 실행 시간 < 30초 목표
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon run test -- --run --reporter=verbose -- aave-lending-e2e 2>&1 | tail -20</automated>
  </verify>
  <done>
    - aave-lending-e2e.test.ts의 6개 테스트 모두 통과
    - supply resolve -> ContractCallRequest[] 검증 완료
    - position sync -> DB 쓰기 검증 완료
    - HF < threshold -> LIQUIDATION_WARNING 발송 검증 완료
    - full flow E2E (supply -> sync -> HF check -> borrow -> warning) 검증 완료
    - Daemon 패키지 커버리지 임계값 유지
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @waiaas/admin run test -- --run` → 전체 통과, 커버리지 유지
2. `pnpm --filter @waiaas/daemon run test -- --run -- aave-lending-e2e` → 전체 통과
3. 총 ~11개 새 테스트 추가 (admin 10 + daemon 6 = ~16)
4. 기존 테스트 깨짐 없음
</verification>

<success_criteria>
- Actions Aave V3 카드 렌더링/토글/고급 설정 저장이 UI 테스트로 검증됨
- Dashboard DeFi 섹션 렌더링/HF badge 색상이 UI 테스트로 검증됨
- supply -> position-sync -> health-check -> borrow -> HF-warning E2E 플로우 검증됨
- 모든 커버리지 임계값 유지 (NEVER lower thresholds)
</success_criteria>

<output>
After completion, create `.planning/phases/278-admin-ui-settings-e2e/278-03-SUMMARY.md`
</output>
