---
phase: 448.1-coverage-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/__tests__/restore-wallet-branches.test.ts
  - packages/cli/src/__tests__/quickstart-update-branches.test.ts
  - packages/cli/vitest.config.ts
  - scripts/coverage-gate.sh
autonomous: true
requirements: [CLI-02, CLI-03]
gap_closure: true

must_haves:
  truths:
    - "cli Lines >= 90% (현재 88.31%, +30 lines 필요)"
    - "cli Branches >= 85% (현재 80.16%, +24 branches 필요)"
  artifacts:
    - path: "packages/cli/src/__tests__/restore-wallet-branches.test.ts"
      provides: "restore.ts + wallet.ts 미커버 라인/분기 테스트"
    - path: "packages/cli/src/__tests__/quickstart-update-branches.test.ts"
      provides: "quickstart.ts + update.ts + backup.ts + init.ts + stop.ts + status.ts 미커버 라인/분기 테스트"
  key_links:
    - from: "test files"
      to: "cli vitest.config.ts thresholds"
      via: "pnpm --filter cli test:unit"
      pattern: "thresholds.*lines.*90.*branches.*85"
    - from: "vitest.config.ts (daemon/admin/cli)"
      to: "scripts/coverage-gate.sh"
      via: "threshold sync"
      pattern: "coverage-gate.*daemon.*admin.*cli"
---

<objective>
cli 패키지의 Lines를 90% 이상, Branches를 85% 이상으로 끌어올리고, 전체 패키지 coverage-gate.sh를 동기화한다.

Purpose: CLI-02 (Lines 88.31% -> 90%) 와 CLI-03 (Branches 80.16% -> 85%) 미충족 요구사항을 해소한다.
Output: cli 테스트 파일 2개, vitest.config.ts 임계값 인상, coverage-gate.sh 동기화
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v32.8-MILESTONE-AUDIT.md

기존 cli 테스트 패턴 참조:
@packages/cli/src/__tests__/restore-cli.test.ts
@packages/cli/src/__tests__/wallet-coverage.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: cli restore + wallet 명령어 미커버 라인/분기 테스트</name>
  <files>
    packages/cli/src/__tests__/restore-wallet-branches.test.ts
  </files>
  <action>
cli Lines 90% 달성에 30 lines, Branches 85% 달성에 24 branches 추가 필요. 가장 미커버가 큰 파일 2개를 집중 공략한다.

**restore-wallet-branches.test.ts**

1. `src/commands/restore.ts` (38 uncovered lines, 10 uncovered branches):
   - 백업 파일 경로 없음 에러
   - 잘못된 백업 파일 형식 에러
   - 복원 대상 선택 분기 (전체 vs 부분)
   - 비밀번호 불일치 에러
   - 복원 성공 후 재시작 프롬프트 분기
   - 이미 존재하는 데이터 덮어쓰기 확인 분기
   - 네트워크 에러 시 재시도/실패 분기

2. `src/commands/wallet.ts` (34 uncovered lines, 18 uncovered branches):
   - 월렛 목록 빈 상태 분기
   - 월렛 생성 시 이름 중복 에러
   - 월렛 삭제 확인 취소 분기
   - 잘못된 체인 타입 에러
   - 잔액 조회 실패 에러
   - 월렛 상세 조회 시 존재하지 않는 ID 에러
   - export 명령어 에러 경로

기존 `restore-cli.test.ts`와 `wallet-coverage.test.ts` 패턴을 참조한다:
- `vi.mock`으로 SDK client, readline, fs 모킹
- 각 명령어 함수를 직접 호출하여 에러 경로 테스트
- `process.exit` mock으로 exit code 검증
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter cli test:unit -- --reporter=verbose --coverage 2>&1 | grep -E "(lines|branches|All files)" | tail -5</automated>
  </verify>
  <done>restore.ts + wallet.ts 미커버 라인 50+ 추가 커버, 미커버 분기 20+ 추가 커버</done>
</task>

<task type="auto">
  <name>Task 2: cli 나머지 명령어 분기 테스트 + 임계값 인상 + coverage-gate.sh 동기화</name>
  <files>
    packages/cli/src/__tests__/quickstart-update-branches.test.ts
    packages/cli/vitest.config.ts
    scripts/coverage-gate.sh
  </files>
  <action>
Task 1 후 남은 갭을 해소하고 임계값을 인상한다.

**quickstart-update-branches.test.ts** (남은 갭 보충)
1. `src/commands/quickstart.ts` (27 uncovered lines, 12 uncovered branches):
   - 이미 설정된 상태에서 quickstart 시도 분기
   - 포트 충돌 에러
   - config 파일 생성 실패
   - 데몬 시작 실패 후 정리
   - 각 설정 단계별 에러 분기

2. `src/commands/update.ts` (26 uncovered lines, 7 uncovered branches):
   - 최신 버전 이미 설치된 경우
   - 업데이트 다운로드 실패
   - Docker 업데이트 vs npm 업데이트 분기

3. `src/commands/backup.ts` (19 uncovered lines, 9 uncovered branches):
   - 백업 경로 생성 실패
   - 데이터베이스 락 에러
   - 암호화 실패 에러

4. `src/commands/init.ts` (10 uncovered lines, 4 uncovered branches):
   - 이미 초기화된 상태 분기
   - 설정 디렉토리 생성 실패

5. `src/commands/stop.ts` (8 uncovered lines, 2 uncovered branches):
   - 데몬이 실행 중이 아닌 경우
   - 정상 종료 vs 강제 종료

6. `src/commands/status.ts` (6 uncovered lines, 4 uncovered branches):
   - 데몬 미실행 상태
   - 연결 실패 에러

**vitest.config.ts 임계값 인상:**
현재: branches: 80, functions: 98, lines: 88, statements: 88
목표: branches: 85, functions: 98 (유지), lines: 90, statements: 90

**coverage-gate.sh 동기화:**
scripts/coverage-gate.sh에서 daemon, admin, cli 항목의 임계값을 vitest.config.ts와 일치시킨다:
- daemon: lines=90, branches=85 (현재 89, 81)
- admin: branches=85, functions=달성값 (현재 81, 75)
- cli: lines=90, branches=85 (현재 88, 80)

주의: coverage-gate.sh의 다른 패키지 임계값은 절대 변경하지 않는다. 해당 3개 패키지만 업데이트.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter cli test:unit 2>&1 | tail -5 && echo "---" && grep -E "daemon|admin|cli" scripts/coverage-gate.sh</automated>
  </verify>
  <done>cli Lines >= 90% (CLI-02 해소), cli Branches >= 85% (CLI-03 해소), vitest.config.ts 임계값 인상됨, coverage-gate.sh 동기화됨, 전체 테스트 0 failures</done>
</task>

</tasks>

<verification>
1. `pnpm --filter cli test:unit` 이 0 failures로 통과
2. cli vitest.config.ts thresholds: lines >= 90, branches >= 85
3. coverage-gate.sh가 daemon/admin/cli 패키지의 vitest.config.ts 임계값과 동기화됨
4. `pnpm turbo run test:unit` 전체 패키지 0 failures (최종 검증)
</verification>

<success_criteria>
- cli Lines >= 90% (CLI-02 해소)
- cli Branches >= 85% (CLI-03 해소)
- cli vitest.config.ts 임계값이 통일 기준 (90/85)으로 인상됨
- coverage-gate.sh가 daemon/admin/cli 모두 새 임계값으로 동기화됨
- 전체 패키지 테스트 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/448.1-coverage-gap-closure/448.1-03-SUMMARY.md`
</output>
