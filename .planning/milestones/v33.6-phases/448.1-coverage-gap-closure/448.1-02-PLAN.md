---
phase: 448.1-coverage-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/__tests__/page-functions-wallets.test.tsx
  - packages/admin/src/__tests__/page-functions-system-dashboard.test.tsx
  - packages/admin/src/__tests__/page-functions-remaining.test.tsx
  - packages/admin/vitest.config.ts
autonomous: true
requirements: [ADM-05, ADM-06]
gap_closure: true

must_haves:
  truths:
    - "admin Branches >= 85% (현재 81.71%, +111 branches 필요)"
    - "admin Functions가 최대 달성 가능 수준에 도달 (현재 75.30%, 목표 95% 중 최대 달성 가능)"
  artifacts:
    - path: "packages/admin/src/__tests__/page-functions-wallets.test.tsx"
      provides: "wallets.tsx 미커버 함수/분기 테스트 (65 functions, 153 branches)"
    - path: "packages/admin/src/__tests__/page-functions-system-dashboard.test.tsx"
      provides: "system.tsx + dashboard.tsx 미커버 함수/분기 테스트 (40 functions)"
    - path: "packages/admin/src/__tests__/page-functions-remaining.test.tsx"
      provides: "notifications/sessions/transactions/credentials/rpc-proxy/security/policies 미커버 함수/분기 테스트"
  key_links:
    - from: "test files"
      to: "admin vitest.config.ts thresholds"
      via: "pnpm --filter admin test:unit"
      pattern: "thresholds.*functions.*branches.*85"
---

<objective>
admin 패키지의 Functions와 Branches를 최대 달성 가능 수준으로 끌어올린다.

Purpose: ADM-05 (Branches 81.71% -> 85%) 와 ADM-06 (Functions 75.30% -> 95%) 미충족 요구사항을 해소한다.
Output: admin 테스트 파일 3개, vitest.config.ts 임계값 인상

**ADM-06 달성 가능성 분석:**
- 총 984 functions, 741 covered, 243 uncovered
- 95% 달성에 194개 추가 필요 (243개 중 194개 = 거의 전부)
- Preact 컴포넌트의 내부 이벤트 핸들러는 render+fireEvent 패턴으로 대부분 테스트 가능
- 단, app.tsx/main.tsx (entry points) 등 2개는 테스트 불가
- 현실적 목표: 90-93% (약 145-170개 추가 커버), 95% 도전
- wallets.tsx(65), system.tsx(23), notifications.tsx(20), dashboard.tsx(17) 4개 파일만 해도 125개 함수
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v32.8-MILESTONE-AUDIT.md

기존 admin 테스트 패턴 참조:
@packages/admin/src/__tests__/pages-functions-1.test.tsx
@packages/admin/src/__tests__/pages-functions-2.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: wallets.tsx 미커버 함수 + 분기 대량 테스트</name>
  <files>
    packages/admin/src/__tests__/page-functions-wallets.test.tsx
  </files>
  <action>
wallets.tsx가 단일 최대 미커버 파일 (65 uncovered functions, 153 uncovered branches). 이 파일 하나를 집중 공략한다.

기존 `pages-functions-1.test.tsx` 패턴을 참조하여 Preact render + fireEvent 기반 테스트를 작성한다:

1. **테스트 환경 셋업:**
   - `@testing-library/preact`의 `render`, `fireEvent`, `waitFor`, `screen` 사용
   - `fetch` mock으로 API 응답 시뮬레이션 (기존 테스트의 globalThis.fetch mock 패턴)
   - `@preact/signals` mock으로 인증 상태 시뮬레이션

2. **wallets.tsx 미커버 함수 테스트 대상** (65개 중 최대한 많이):
   - 월렛 목록 로딩/에러/빈 상태 렌더링 함수
   - 월렛 생성 폼 제출 핸들러 (이름 검증, 체인 선택, submit)
   - 월렛 상세 탭 전환 핸들러 (Overview/Activity/Assets/Setup)
   - 월렛 삭제 확인 모달 핸들러
   - 잔액 조회 / 토큰 잔액 조회 함수
   - 트랜잭션 필터링 핸들러
   - 주소 복사 핸들러
   - export/backup 관련 핸들러
   - 설정 업데이트 핸들러 (이름 변경 등)
   - 각 조건부 렌더링 분기 (로딩/에러/데이터/빈 상태)

3. **테스트 구조:**
   ```tsx
   import { render, fireEvent, waitFor, screen } from '@testing-library/preact';
   // wallets.tsx 페이지 컴포넌트 import
   // fetch mock, signal mock 셋업

   describe('WalletsPage functions coverage', () => {
     // 각 함수별 describe 블록
     // render -> user action -> assertion 패턴
   });
   ```

4. **branches 커버 전략:**
   - 각 함수 호출 시 성공/실패 양 경로를 테스트
   - 조건부 렌더링의 truthy/falsy 양쪽 테스트
   - optional chaining (?.) 의 null/undefined 경우 테스트

목표: wallets.tsx에서 ~50+ functions, ~100+ branches 추가 커버
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter admin test:unit -- --reporter=verbose --coverage 2>&1 | grep -E "(functions|branches|All files)" | tail -5</automated>
  </verify>
  <done>wallets.tsx 미커버 함수 50개 이상 추가 커버, admin 전체 Functions 80% 이상 도달</done>
</task>

<task type="auto">
  <name>Task 2: system/dashboard/notifications + 나머지 페이지 함수 테스트 + 임계값 인상</name>
  <files>
    packages/admin/src/__tests__/page-functions-system-dashboard.test.tsx
    packages/admin/src/__tests__/page-functions-remaining.test.tsx
    packages/admin/vitest.config.ts
  </files>
  <action>
Task 1에서 wallets.tsx를 처리한 후, 남은 큰 미커버 파일들을 공략한다.

**page-functions-system-dashboard.test.tsx** (목표: ~60 functions, ~50 branches)
1. `system.tsx` (23 uncovered functions) — 시스템 설정 폼 핸들러, 비밀번호 변경, API 키 재생성, 임계값 변경
2. `dashboard.tsx` (17 uncovered functions) — 대시보드 데이터 로딩, 새로고침, 통계 카드 클릭
3. `notifications.tsx` (20 uncovered functions) — 알림 채널 설정, 테스트 발송, 채널별 폼 핸들러

**page-functions-remaining.test.tsx** (목표: ~80 functions, ~60 branches)
1. `sessions.tsx` (13 uncovered functions) — 세션 목록, 세션 해제, 필터링
2. `transactions.tsx` (13 uncovered functions) — 트랜잭션 목록, 상세, 필터
3. `credentials.tsx` (12 uncovered functions) — 크리덴셜 CRUD 핸들러
4. `rpc-proxy.tsx` (7 uncovered functions) — RPC 프록시 설정 핸들러
5. `security.tsx` (7 uncovered functions) — 보안 설정 핸들러
6. `policies.tsx` (8 uncovered functions) — 정책 편집 핸들러
7. `erc8004.tsx` (5 uncovered functions) — ERC-8004 설정 핸들러
8. `human-wallet-apps.tsx` (5 uncovered functions) — 월렛 앱 연결 핸들러
9. `components/policy-forms/spending-limit-form.tsx` (5 uncovered)
10. `components/hyperliquid/SettingsPanel.tsx` (4 uncovered)
11. `components/policy-forms/approved-spenders-form.tsx` (4 uncovered)
12. `components/policy-forms/contract-whitelist-form.tsx` (4 uncovered)
13. `components/policy-forms/method-whitelist-form.tsx` (4 uncovered)
14. `components/layout.tsx` (3 uncovered)
15. `components/polymarket/PolymarketSettings.tsx` (3 uncovered)
16. `components/policy-forms/reputation-threshold-form.tsx` (3 uncovered)
17. `utils/settings-helpers.ts` (2 uncovered functions + 10 uncovered branches)

같은 render + fireEvent 패턴 사용. 기존 `pages-functions-2.test.tsx` 참조.

**vitest.config.ts 임계값 인상:**
현재: branches: 81, functions: 75, lines: 90, statements: 90
목표: branches: 85, functions: 달성값(최소 90, 가능하면 95), lines: 90, statements: 90

구체적으로:
- branches: 81 -> 85 (ADM-05 해소)
- functions: 75 -> 달성값 (ADM-06 최대 해소)
- lines, statements는 이미 90 이상이므로 유지 또는 소폭 인상

주의: 기존 임계값보다 절대 하향하지 않는다 (CLAUDE.md 규칙).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter admin test:unit 2>&1 | tail -5</automated>
  </verify>
  <done>admin Branches >= 85% (ADM-05 해소), admin Functions가 최대 달성 가능 수준 (90%+ 목표, vitest.config.ts 임계값에 반영), 전체 테스트 0 failures</done>
</task>

</tasks>

<verification>
1. `pnpm --filter admin test:unit` 이 0 failures로 통과
2. admin vitest.config.ts thresholds: branches >= 85
3. admin Functions가 달성 가능 최대값으로 임계값 설정됨
4. 기존 테스트가 깨지지 않음
</verification>

<success_criteria>
- admin Branches >= 85% (ADM-05 해소)
- admin Functions 최대 달성 가능 수준 (95% 도전, 최소 90%)
- admin vitest.config.ts 임계값이 달성값으로 인상됨
- 전체 admin 테스트 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/448.1-coverage-gap-closure/448.1-02-SUMMARY.md`
</output>
