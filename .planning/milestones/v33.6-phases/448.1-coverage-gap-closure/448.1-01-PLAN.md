---
phase: 448.1-coverage-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/__tests__/route-branches-coverage.test.ts
  - packages/daemon/src/__tests__/pipeline-branches-gap.test.ts
  - packages/daemon/src/__tests__/route-branches-coverage-2.test.ts
autonomous: true
requirements: [DCOV-01, DCOV-02]
gap_closure: true

must_haves:
  truths:
    - "daemon Lines >= 90% (현재 89.96%, +14 lines 필요)"
    - "daemon Branches >= 85% (현재 81.91%, +221 branches 필요)"
  artifacts:
    - path: "packages/daemon/src/__tests__/route-branches-coverage.test.ts"
      provides: "API route handler branch coverage tests"
    - path: "packages/daemon/src/__tests__/pipeline-branches-gap.test.ts"
      provides: "Pipeline branch coverage tests (stage5-execute, sign-only, dry-run, external-action)"
    - path: "packages/daemon/src/__tests__/route-branches-coverage-2.test.ts"
      provides: "Secondary route handler branch coverage (admin-settings, admin-notifications, nft, staking, mcp)"
  key_links:
    - from: "test files"
      to: "daemon vitest.config.ts thresholds"
      via: "pnpm --filter daemon test:unit"
      pattern: "thresholds.*lines.*90.*branches.*85"
---

<objective>
daemon 패키지의 Lines를 90% 이상, Branches를 85% 이상으로 끌어올린다.

Purpose: DCOV-01 (Lines 89.96% -> 90%) 과 DCOV-02 (Branches 81.91% -> 85%) 미충족 요구사항을 해소한다.
Output: daemon 테스트 파일 3개, vitest.config.ts 임계값 인상 (lines: 90, branches: 85)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v32.8-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API route handler + pipeline branch coverage 테스트 (가장 큰 갭 해소)</name>
  <files>
    packages/daemon/src/__tests__/route-branches-coverage.test.ts
    packages/daemon/src/__tests__/pipeline-branches-gap.test.ts
  </files>
  <action>
daemon Branches가 81.91% (5858/7151)이고 85% 달성에 221개 branch가 추가로 필요하다. Lines는 14개만 추가 커버하면 90% 달성.

가장 미커버 branch가 많은 파일 순으로 공략한다:

**route-branches-coverage.test.ts** (목표: ~120 branches 커버)
아래 파일의 미커버 branch를 타겟으로 mock 기반 라우트 테스트를 작성한다:
1. `src/api/routes/wallets.ts` (153 uncovered branches) — POST/PUT/DELETE 에러 경로, 파라미터 검증 실패, 중복 월렛 이름, 잘못된 체인 타입 분기
2. `src/api/routes/actions.ts` (30 uncovered branches) — 액션 실행 에러 경로, 미지원 액션 타입, 파라미터 검증 실패
3. `src/api/routes/transactions.ts` (27 uncovered branches) — 필터링 분기, 페이지네이션 엣지, 트랜잭션 없는 경우
4. `src/api/routes/admin-wallets.ts` (18 uncovered branches) — admin wallet CRUD 에러 경로

테스트 패턴: 기존 `admin-wallets-coverage.test.ts` 등 기존 route coverage 테스트 패턴을 따른다. Hono app을 직접 import하지 않고, 해당 route의 핸들러 함수를 mock context와 함께 호출하거나, `app.request()` 패턴을 사용한다.

**pipeline-branches-gap.test.ts** (목표: ~80 branches 커버)
1. `src/pipeline/stage5-execute.ts` (106 uncovered branches) — 각 트랜잭션 타입별 실행 분기 (TOKEN_TRANSFER, CONTRACT_CALL, APPROVE, BATCH, NFT_TRANSFER, CONTRACT_DEPLOY), 에러 경로, gasEstimate fallback
2. `src/pipeline/sign-only.ts` (27 uncovered branches) — 서명 전용 파이프라인 에러 경로, 타입별 분기
3. `src/pipeline/dry-run.ts` (15 uncovered branches) — dry-run 시뮬레이션 실패, 가스 추정 에러
4. `src/pipeline/external-action-pipeline.ts` (32 uncovered branches) — external action 실행 에러, credential 검증 실패

기존 `pipeline-branches-coverage.test.ts`를 참조하되, 신규 파일로 작성한다. mock chainAdapter, mock database, mock policyEngine 패턴을 사용한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon test:unit -- --reporter=verbose --coverage 2>&1 | grep -E "(branches|lines|All files)" | tail -5</automated>
  </verify>
  <done>daemon branches가 최소 83% 이상 도달 (중간 목표). Lines는 90% 이상 도달.</done>
</task>

<task type="auto">
  <name>Task 2: 나머지 route + infra branch coverage + 임계값 인상</name>
  <files>
    packages/daemon/src/__tests__/route-branches-coverage-2.test.ts
    packages/daemon/vitest.config.ts
  </files>
  <action>
Task 1에서 ~200 branches를 확보한 후 남은 갭을 해소한다.

**route-branches-coverage-2.test.ts** (목표: 남은 ~40-60 branches)
1. `src/api/routes/admin-settings.ts` (18 uncovered branches) — 설정 업데이트 검증 실패, 존재하지 않는 키, 타입 미스매치
2. `src/api/routes/admin-notifications.ts` (16 uncovered branches) — 알림 설정 검증, 채널 타입별 분기
3. `src/api/routes/x402.ts` (31 uncovered branches) — x402 결제 에러 경로, 잔액 부족, 서명 검증 실패
4. `src/api/routes/erc8128.ts` (20 uncovered branches) — 서명 요청 검증, 만료된 요청
5. `src/api/routes/nft-approvals.ts` (7 uncovered branches) — NFT 승인 에러 경로
6. `src/api/routes/staking.ts` (9 uncovered branches) — 스테이킹 에러 경로
7. `src/api/routes/mcp.ts` (8 uncovered branches) — MCP 라우트 분기
8. `src/infrastructure/nft/helius-nft-indexer.ts` (27 uncovered) — API 에러, 빈 응답, 페이지네이션
9. `src/infrastructure/nft/alchemy-nft-indexer.ts` (25 uncovered) — API 에러, 빈 응답
10. `src/api/routes/erc8004.ts` (14 uncovered branches) — erc8004 에러 경로
11. `src/infrastructure/settings/hot-reload.ts` (13 uncovered branches) — 설정 변경 감지 분기

기존 route coverage 테스트 패턴을 따른다.

**vitest.config.ts 임계값 인상:**
```
thresholds: {
  branches: 85,
  functions: 95,
  lines: 90,
  statements: 90,
}
```
현재 lines: 89, branches: 81에서 lines: 90, branches: 85로 인상한다.

주의: CLAUDE.md 규칙에 따라 functions/statements는 현재값(95/89) 이상으로만 설정. 절대 하향 금지.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon test:unit 2>&1 | tail -5</automated>
  </verify>
  <done>daemon Lines >= 90%, Branches >= 85%, vitest.config.ts 임계값이 lines: 90, branches: 85로 인상되어 테스트가 통과한다.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter daemon test:unit` 이 0 failures로 통과
2. daemon vitest.config.ts thresholds: lines >= 90, branches >= 85
3. 기존 테스트가 깨지지 않음
</verification>

<success_criteria>
- daemon Lines >= 90% (DCOV-01 해소)
- daemon Branches >= 85% (DCOV-02 해소)
- daemon vitest.config.ts 임계값이 통일 기준 (90/85/95)으로 인상됨
- 전체 daemon 테스트 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/448.1-coverage-gap-closure/448.1-01-SUMMARY.md`
</output>
