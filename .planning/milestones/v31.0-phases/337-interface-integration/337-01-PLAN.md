---
phase: 337-interface-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/tools/list-nfts.ts
  - packages/mcp/src/tools/get-nft-metadata.ts
  - packages/mcp/src/tools/transfer-nft.ts
  - packages/mcp/src/server.ts
  - packages/mcp/src/__tests__/nft-tools.test.ts
  - packages/sdk/src/client.ts
  - packages/sdk/src/types.ts
  - packages/sdk/src/__tests__/client-nft.test.ts
  - packages/daemon/src/api/routes/connect-info.ts
  - packages/daemon/src/__tests__/connect-info-nft.test.ts
autonomous: true
requirements: [MCPSK-01, MCPSK-02, MCPSK-03]

must_haves:
  truths:
    - "MCP tool list_nfts calls GET /v1/wallet/nfts with network param and returns NFT list"
    - "MCP tool get_nft_metadata calls GET /v1/wallet/nfts/{tokenIdentifier} and returns metadata"
    - "MCP tool transfer_nft calls POST /v1/transactions/send with type NFT_TRANSFER"
    - "SDK method listNfts() calls GET /v1/wallet/nfts with network/cursor/limit/groupBy params"
    - "SDK method getNftMetadata() calls GET /v1/wallet/nfts/{tokenIdentifier} with network param"
    - "SDK method transferNft() calls POST /v1/transactions/send with type NFT_TRANSFER"
    - "connect-info includes NFT summary per wallet when indexer is configured"
    - "connect-info gracefully omits NFT section when indexer is not configured"
  artifacts:
    - path: "packages/mcp/src/tools/list-nfts.ts"
      provides: "MCP list_nfts tool"
      exports: ["registerListNfts"]
    - path: "packages/mcp/src/tools/get-nft-metadata.ts"
      provides: "MCP get_nft_metadata tool"
      exports: ["registerGetNftMetadata"]
    - path: "packages/mcp/src/tools/transfer-nft.ts"
      provides: "MCP transfer_nft tool"
      exports: ["registerTransferNft"]
    - path: "packages/sdk/src/client.ts"
      provides: "SDK NFT methods on WAIaaSClient"
    - path: "packages/daemon/src/api/routes/connect-info.ts"
      provides: "NFT summary in connect-info response"
  key_links:
    - from: "packages/mcp/src/tools/list-nfts.ts"
      to: "/v1/wallet/nfts"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*wallet/nfts"
    - from: "packages/mcp/src/tools/transfer-nft.ts"
      to: "/v1/transactions/send"
      via: "apiClient.post with type NFT_TRANSFER"
      pattern: "NFT_TRANSFER"
    - from: "packages/sdk/src/client.ts"
      to: "/v1/wallet/nfts"
      via: "http.get"
      pattern: "wallet/nfts"
    - from: "packages/mcp/src/server.ts"
      to: "tools/list-nfts.ts"
      via: "registerListNfts import"
      pattern: "registerListNfts"
---

<objective>
MCP 3 NFT 도구(list_nfts, get_nft_metadata, transfer_nft)와 SDK 3 NFT 메서드(listNfts, getNftMetadata, transferNft)를 구현하고, connect-info에 NFT 보유 요약을 추가한다.

Purpose: AI 에이전트가 MCP/SDK를 통해 NFT 조회/전송을 수행할 수 있게 함
Output: MCP 3 tools + SDK 3 methods + connect-info NFT extension
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Existing MCP tool pattern (from packages/mcp/src/tools/get-tokens.ts): -->
```typescript
import { z } from 'zod';
import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { type ApiClient, toToolResult } from '../api-client.js';
import { type WalletContext, withWalletPrefix } from '../server.js';

export function registerGetTokens(server: McpServer, apiClient: ApiClient, walletContext?: WalletContext): void {
  server.tool('get_tokens', withWalletPrefix('...', walletContext?.walletName), { network: z.string().describe('...') },
    async (args) => { ... return toToolResult(result); });
}
```

<!-- Existing SDK client method pattern (from packages/sdk/src/client.ts): -->
```typescript
async getAssets(options?: AssetsOptions): Promise<AssetsResponse> {
  const query = new URLSearchParams();
  if (options?.network) query.set('network', options.network);
  const qs = query.toString();
  return withRetry(() => this.http.get<AssetsResponse>(`/v1/wallet/assets${qs ? `?${qs}` : ''}`, this.authHeaders()), this.retryOptions);
}
```

<!-- NFT REST API endpoints (from Phase 335): -->
- GET /v1/wallet/nfts?network=...&cursor=...&limit=...&groupBy=collection -- session auth
- GET /v1/wallet/nfts/{tokenIdentifier}?network=... -- session auth NFT metadata
- POST /v1/transactions/send with { type: "NFT_TRANSFER", to, token: { address, tokenId, standard }, network, amount? }

<!-- connect-info route deps (from packages/daemon/src/api/routes/connect-info.ts): -->
```typescript
export interface ConnectInfoRouteDeps {
  db: BetterSQLite3Database<typeof schema>;
  config: DaemonConfig;
  settingsService?: SettingsService;
  actionProviderRegistry?: ActionProviderRegistry;
  version: string;
}
```

<!-- MCP server registration (from packages/mcp/src/server.ts): -->
- Currently 32 tools registered via registerXxx(server, apiClient, walletContext?) pattern
- Import + register call in createMcpServer function
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP 3 NFT tools + SDK 3 NFT methods</name>
  <files>
    packages/mcp/src/tools/list-nfts.ts,
    packages/mcp/src/tools/get-nft-metadata.ts,
    packages/mcp/src/tools/transfer-nft.ts,
    packages/mcp/src/server.ts,
    packages/mcp/src/__tests__/nft-tools.test.ts,
    packages/sdk/src/client.ts,
    packages/sdk/src/types.ts,
    packages/sdk/src/__tests__/client-nft.test.ts
  </files>
  <behavior>
    MCP tools:
    - list_nfts: takes network (required), cursor (optional), limit (optional), group_by (optional "collection"), wallet_id (optional). Calls GET /v1/wallet/nfts with query params. Returns toToolResult.
    - get_nft_metadata: takes token_identifier (required string), network (required). Calls GET /v1/wallet/nfts/{token_identifier}?network=.... Returns toToolResult.
    - transfer_nft: takes to (required), token_address (required), token_id (required), standard (required enum erc721/erc1155/metaplex), network (required), amount (optional, default "1"), wallet_id (optional). Calls POST /v1/transactions/send with type=NFT_TRANSFER body. Returns toToolResult.

    SDK methods:
    - listNfts(options: ListNftsParams): GET /v1/wallet/nfts with network/cursor/limit/groupBy query params. Returns NftListResponse.
    - getNftMetadata(tokenIdentifier: string, options: NftMetadataParams): GET /v1/wallet/nfts/{tokenIdentifier} with network param. Returns NftMetadataResponse.
    - transferNft(params: TransferNftParams): POST /v1/transactions/send with type=NFT_TRANSFER. Returns SendTokenResponse.

    Tests:
    - MCP: mock apiClient.get/post, verify correct URL + params for each tool
    - SDK: mock http.get/post, verify correct URL + body construction
  </behavior>
  <action>
    1. Create 3 MCP tool files following the registerXxx pattern (see get-tokens.ts for reference):
       - list-nfts.ts: registerListNfts with z.string() for network, optional cursor/limit/group_by/wallet_id. Build URLSearchParams, call apiClient.get.
       - get-nft-metadata.ts: registerGetNftMetadata with z.string() for token_identifier and network. Call apiClient.get with path + query.
       - transfer-nft.ts: registerTransferNft with to/token_address/token_id/standard/network required, amount/wallet_id optional. Build body with type="NFT_TRANSFER" and token object { address: token_address, tokenId: token_id, standard }, call apiClient.post to /v1/transactions/send.

    2. Update server.ts: import 3 new register functions, add to createMcpServer (update comment from "32 tools" to "35 tools"), call after existing registerSignMessage line.

    3. Add SDK types to types.ts:
       - ListNftsParams: { network: string; cursor?: string; limit?: number; groupBy?: 'collection'; walletId?: string }
       - NftListResponse: { nfts: NftItemResponse[]; cursor?: string; hasMore: boolean }
       - NftItemResponse: { tokenId: string; contractAddress: string; standard: string; name?: string; image?: string; description?: string; amount?: string; collection?: { name?: string; address: string }; assetId?: string }
       - NftMetadataParams: { network: string; walletId?: string }
       - NftMetadataResponse: { tokenId: string; contractAddress: string; standard: string; name?: string; image?: string; description?: string; attributes?: Array<{ traitType: string; value: string }>; metadata?: Record<string, unknown>; assetId?: string }
       - TransferNftParams: { to: string; token: { address: string; tokenId: string; standard: string }; network: string; amount?: string; walletId?: string; gasCondition?: { maxGasPrice?: string; maxPriorityFee?: string; timeout?: number } }

    4. Add 3 methods to WAIaaSClient in client.ts:
       - listNfts(): build query params from ListNftsParams, GET /v1/wallet/nfts
       - getNftMetadata(): GET /v1/wallet/nfts/{tokenIdentifier}?network=...
       - transferNft(): POST /v1/transactions/send with type=NFT_TRANSFER body

    5. Write tests:
       - nft-tools.test.ts: mock ApiClient, verify each tool calls correct endpoint with correct params
       - client-nft.test.ts: mock HttpClient, verify each SDK method calls correct URL with correct body/query
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test --filter=@waiaas/mcp -- --run --testPathPattern="nft-tools" && pnpm turbo run test --filter=@waiaas/sdk -- --run --testPathPattern="client-nft"</automated>
  </verify>
  <done>
    - 3 MCP tools registered (list_nfts, get_nft_metadata, transfer_nft) and tests pass
    - 3 SDK methods added (listNfts, getNftMetadata, transferNft) with types and tests pass
    - server.ts updated to register 35 tools
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: connect-info NFT summary extension</name>
  <files>
    packages/daemon/src/api/routes/connect-info.ts,
    packages/daemon/src/__tests__/connect-info-nft.test.ts
  </files>
  <behavior>
    - When indexer is configured (settings has alchemy_api_key or helius_api_key), connect-info response includes per-wallet nftSummary: { count: number, collections: number } fetched via NftIndexerClient
    - When indexer is NOT configured, nftSummary field is omitted entirely (graceful degradation)
    - When indexer call fails (network error), nftSummary is omitted with no error thrown
    - The AI prompt text includes NFT info when available: "NFTs: {count} items in {collections} collections"
  </behavior>
  <action>
    1. Add optional nftIndexerClient to ConnectInfoRouteDeps: `nftIndexerClient?: NftIndexerClient`

    2. In the connect-info route handler, after building wallet info, for each wallet:
       - Check if nftIndexerClient exists (deps.nftIndexerClient)
       - If yes, try to call nftIndexerClient.listNfts() for each network of the wallet. Wrap in try/catch to handle failures gracefully.
       - Aggregate total NFT count and unique collection count
       - Add nftSummary: { count, collections } to the wallet info object
       - If nftIndexerClient is null or call fails, simply omit the nftSummary field

    3. Update buildConnectInfoPrompt to include NFT line when nftSummary is present:
       - Add after existing wallet info lines: `  - NFTs: {count} items in {collections} collections`
       - Skip this line when nftSummary is undefined

    4. Update ConnectInfoResponseSchema in openapi-schemas.ts to include optional nftSummary field in wallet objects (add z.object({ count: z.number(), collections: z.number() }).optional())

    5. Wire nftIndexerClient in server.ts where connectInfoRoutes is called (pass from app context, or undefined if not available)

    6. Write test connect-info-nft.test.ts:
       - Mock nftIndexerClient.listNfts to return NFT items -> verify nftSummary in response
       - No nftIndexerClient (undefined) -> verify no nftSummary field, no error
       - nftIndexerClient.listNfts throws -> verify no nftSummary field, no error
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run test --filter=@waiaas/daemon -- --run --testPathPattern="connect-info-nft"</automated>
  </verify>
  <done>
    - connect-info includes nftSummary per wallet when indexer is configured and working
    - connect-info gracefully omits nftSummary when indexer is not configured or fails
    - AI prompt includes NFT line when summary is available
    - Tests pass for all 3 scenarios (configured, not configured, error)
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run test --filter=@waiaas/mcp -- --run --testPathPattern="nft-tools"` passes
2. `pnpm turbo run test --filter=@waiaas/sdk -- --run --testPathPattern="client-nft"` passes
3. `pnpm turbo run test --filter=@waiaas/daemon -- --run --testPathPattern="connect-info-nft"` passes
4. `pnpm turbo run typecheck --filter=@waiaas/mcp --filter=@waiaas/sdk --filter=@waiaas/daemon` passes
</verification>

<success_criteria>
- MCP: list_nfts, get_nft_metadata, transfer_nft 도구가 올바른 REST API를 호출하며 테스트 통과
- SDK: listNfts(), getNftMetadata(), transferNft() 메서드가 올바른 REST API를 호출하며 테스트 통과
- connect-info: NFT 보유 요약이 인덱서 설정 시 포함되고, 미설정 시 graceful 생략
</success_criteria>

<output>
After completion, create `.planning/phases/337-interface-integration/337-01-SUMMARY.md`
</output>
