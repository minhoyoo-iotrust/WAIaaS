---
phase: 267-push-relay-payload-transform
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/push-relay/src/config.ts
  - packages/push-relay/src/transformer/payload-transformer.ts
  - packages/push-relay/src/__tests__/config.test.ts
  - packages/push-relay/src/__tests__/payload-transformer.test.ts
autonomous: true
requirements:
  - RLAY-01
  - RLAY-02

must_haves:
  truths:
    - "config.toml with [relay.push.payload] section parses without error and populates static_fields + category_map"
    - "config.toml without [relay.push.payload] section still parses successfully (backward compat)"
    - "ConfigurablePayloadTransformer.transform() injects static_fields into every PushPayload"
    - "ConfigurablePayloadTransformer.transform() applies category_map fields based on payload.category"
    - "category_map keys not matching current payload.category are ignored"
  artifacts:
    - path: "packages/push-relay/src/config.ts"
      provides: "PayloadConfigSchema Zod schema nested under PushConfigSchema"
      contains: "PayloadConfigSchema"
    - path: "packages/push-relay/src/transformer/payload-transformer.ts"
      provides: "IPayloadTransformer interface + ConfigurablePayloadTransformer class"
      exports: ["IPayloadTransformer", "ConfigurablePayloadTransformer"]
    - path: "packages/push-relay/src/__tests__/payload-transformer.test.ts"
      provides: "Unit tests for ConfigurablePayloadTransformer"
      min_lines: 60
    - path: "packages/push-relay/src/__tests__/config.test.ts"
      provides: "Extended tests for [relay.push.payload] parsing"
      min_lines: 180
  key_links:
    - from: "packages/push-relay/src/config.ts"
      to: "packages/push-relay/src/transformer/payload-transformer.ts"
      via: "PayloadConfig type exported and consumed by transformer constructor"
      pattern: "PayloadConfig"
---

<objective>
[relay.push.payload] Zod 스키마 확장 + ConfigurablePayloadTransformer TDD 구현

Purpose: Push Relay config.toml에 선언적 페이로드 변환 설정([relay.push.payload])을 추가하고, 이 설정을 기반으로 PushPayload를 변환하는 ConfigurablePayloadTransformer를 구현한다. 설정이 없으면 기존 동작을 유지한다.

Output: PayloadConfigSchema Zod 스키마, IPayloadTransformer 인터페이스, ConfigurablePayloadTransformer 구현체 + 단위 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/push-relay/src/config.ts
@packages/push-relay/src/subscriber/message-parser.ts
@packages/push-relay/src/providers/push-provider.ts
@packages/push-relay/src/__tests__/config.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PayloadConfigSchema Zod 스키마 확장 + config 테스트</name>
  <files>
    packages/push-relay/src/config.ts
    packages/push-relay/src/__tests__/config.test.ts
  </files>
  <action>
**config.ts 확장:**

1. `PushConfigSchema` 안에 `payload` 필드를 optional로 추가. `PayloadConfigSchema`를 별도로 정의:

```typescript
const CategoryFieldsSchema = z.record(z.string(), z.string());

const PayloadConfigSchema = z.object({
  static_fields: z.record(z.string(), z.string()).default({}),
  category_map: z.record(z.string(), CategoryFieldsSchema).default({}),
});
```

2. `PushConfigSchema`에 `payload: PayloadConfigSchema.optional()` 추가.

3. `PayloadConfig` 타입 export: `export type PayloadConfig = z.infer<typeof PayloadConfigSchema>;`

**핵심 설계:**
- `static_fields`: 모든 페이로드에 주입되는 key-value 쌍 (예: `{ "app_id" = "com.dcent.wallet" }`)
- `category_map`: 카테고리(`sign_request`, `notification`)별로 주입되는 추가 필드 맵
  - 예: `[relay.push.payload.category_map.sign_request]` 아래 `{ "sound" = "alert.caf", "badge" = "1" }`
- config.toml TOML 형식 예시:
  ```toml
  [relay.push.payload]
  [relay.push.payload.static_fields]
  app_id = "com.dcent.wallet"

  [relay.push.payload.category_map.sign_request]
  sound = "alert.caf"
  badge = "1"

  [relay.push.payload.category_map.notification]
  sound = "default"
  ```

**config.test.ts 확장:**

기존 테스트 유지 + 아래 추가:
1. `it('loads config with [relay.push.payload] section')`: static_fields + category_map 파싱 검증
2. `it('loads config without [relay.push.payload] section (backward compat)')`: payload가 undefined인지 확인
3. `it('loads config with empty [relay.push.payload] section')`: 빈 static_fields/category_map이 기본값 적용 확인
4. `it('loads config with only static_fields')`: category_map 없이도 동작 확인
5. `it('loads config with only category_map')`: static_fields 없이도 동작 확인

각 테스트는 tmpDir에 config.toml을 작성하고 loadConfig()으로 파싱하는 기존 패턴을 따른다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/push-relay exec vitest run src/__tests__/config.test.ts</automated>
  </verify>
  <done>
    - PayloadConfigSchema가 static_fields + category_map을 Zod로 검증
    - PushConfigSchema.payload가 optional (없으면 undefined)
    - PayloadConfig 타입이 export됨
    - 새 config 테스트 5개 통과
    - 기존 config 테스트 5개 그대로 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: IPayloadTransformer 인터페이스 + ConfigurablePayloadTransformer TDD 구현</name>
  <files>
    packages/push-relay/src/transformer/payload-transformer.ts
    packages/push-relay/src/__tests__/payload-transformer.test.ts
    packages/push-relay/src/index.ts
  </files>
  <action>
**TDD RED phase — 테스트 먼저 작성 (payload-transformer.test.ts):**

```typescript
import { describe, it, expect } from 'vitest';
import { ConfigurablePayloadTransformer } from '../transformer/payload-transformer.js';
import type { PushPayload } from '../subscriber/message-parser.js';
```

테스트 케이스:
1. `it('injects static_fields into payload data')`: static_fields `{ app_id: "com.dcent" }` → payload.data에 app_id 존재
2. `it('applies category_map for matching category')`: sign_request category_map `{ sound: "alert" }` → sign_request payload.data에 sound 존재
3. `it('ignores category_map for non-matching category')`: sign_request category_map 설정 → notification payload에 sound 없음
4. `it('merges static_fields and category_map')`: 둘 다 적용하고 기존 data 필드도 유지
5. `it('static_fields do not overwrite existing data keys')`: 기존 payload.data에 있는 키는 유지 (static_fields보다 원본 우선)
6. `it('category_map fields overwrite static_fields for same key')`: category_map이 static_fields보다 우선
7. `it('returns unchanged payload when no config matches')`: 빈 static_fields + 빈 category_map
8. `it('preserves title, body, category, priority fields unchanged')`: data만 변경, 나머지 보존

**TDD GREEN phase — payload-transformer.ts 구현:**

```typescript
import type { PushPayload } from '../subscriber/message-parser.js';
import type { PayloadConfig } from '../config.js';

export interface IPayloadTransformer {
  transform(payload: PushPayload): PushPayload;
}

export class ConfigurablePayloadTransformer implements IPayloadTransformer {
  private readonly staticFields: Record<string, string>;
  private readonly categoryMap: Record<string, Record<string, string>>;

  constructor(config: PayloadConfig) {
    this.staticFields = config.static_fields;
    this.categoryMap = config.category_map;
  }

  transform(payload: PushPayload): PushPayload {
    // Merge order: static_fields (lowest) → category_map → original data (highest)
    const categoryFields = this.categoryMap[payload.category] ?? {};
    const mergedData = {
      ...this.staticFields,
      ...categoryFields,
      ...payload.data,
    };

    return {
      ...payload,
      data: mergedData,
    };
  }
}
```

**Merge 우선순위:** original data > category_map > static_fields (원본 데이터가 최우선)

**index.ts 확장:**
- `export { ConfigurablePayloadTransformer } from './transformer/payload-transformer.js';`
- `export type { IPayloadTransformer } from './transformer/payload-transformer.js';`
- `export type { PayloadConfig } from './config.js';`
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/push-relay exec vitest run src/__tests__/payload-transformer.test.ts</automated>
  </verify>
  <done>
    - IPayloadTransformer 인터페이스 정의 (transform 메서드)
    - ConfigurablePayloadTransformer가 PayloadConfig를 받아 PushPayload.data를 변환
    - static_fields가 모든 페이로드에 주입됨
    - category_map이 해당 카테고리에만 적용됨
    - 원본 data 키가 static_fields/category_map보다 우선
    - 8개 단위 테스트 통과
    - index.ts에서 IPayloadTransformer, ConfigurablePayloadTransformer, PayloadConfig export
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/push-relay exec vitest run
pnpm --filter @waiaas/push-relay run typecheck
```

- 모든 기존 + 신규 테스트 통과
- TypeScript 타입 체크 통과
- PayloadConfig 타입이 config.ts에서 정상 export
- ConfigurablePayloadTransformer가 index.ts에서 정상 export
</verification>

<success_criteria>
1. config.toml에 [relay.push.payload] 섹션 추가 시 Zod 검증 통과하고 static_fields + category_map 파싱됨
2. [relay.push.payload] 미설정 시 기존 config 파싱 100% 호환
3. ConfigurablePayloadTransformer가 static_fields 주입 + category_map 카테고리별 매핑 수행
4. 원본 PushPayload.data 키가 변환 시 유지됨 (덮어쓰기 방지)
5. 모든 테스트 통과 + typecheck 통과
</success_criteria>

<output>
After completion, create `.planning/phases/267-push-relay-payload-transform/267-01-SUMMARY.md`
</output>
