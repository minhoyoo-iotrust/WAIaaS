---
phase: 265-wallet-preset-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/schemas/wallet-preset.ts
  - packages/core/src/index.ts
  - packages/daemon/src/infrastructure/database/schema.ts
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/__tests__/migration-chain.test.ts
autonomous: true
requirements:
  - PRST-01
  - PRST-03
  - DB-01

must_haves:
  truths:
    - "wallets 테이블에 wallet_type nullable TEXT 컬럼이 존재하고 기존 데이터가 NULL로 정상 조회된다"
    - "LATEST_SCHEMA_VERSION이 24이고 fresh DB DDL에 wallet_type 컬럼이 포함된다"
    - "WalletPreset 타입과 BUILTIN_PRESETS 레지스트리가 @waiaas/core에서 export된다"
    - "BUILTIN_PRESETS에 dcent 프리셋이 존재하고 approval_method, preferred_wallet 필드를 포함한다"
    - "wallet_type 미제공 시 기존 동작이 100% 유지된다 (하위 호환)"
  artifacts:
    - path: "packages/core/src/schemas/wallet-preset.ts"
      provides: "WalletPreset type + BUILTIN_PRESETS registry + WalletPresetTypeSchema Zod"
      exports: ["WalletPreset", "WalletPresetType", "BUILTIN_PRESETS", "WalletPresetTypeSchema", "WALLET_PRESET_TYPES"]
    - path: "packages/daemon/src/infrastructure/database/migrate.ts"
      provides: "v24 migration adding wallet_type column to wallets"
      contains: "version: 24"
    - path: "packages/daemon/src/infrastructure/database/schema.ts"
      provides: "Drizzle walletType column in wallets table"
      contains: "walletType"
  key_links:
    - from: "packages/daemon/src/infrastructure/database/migrate.ts"
      to: "wallets table"
      via: "ALTER TABLE wallets ADD COLUMN wallet_type TEXT"
      pattern: "ALTER TABLE wallets ADD COLUMN wallet_type"
    - from: "packages/core/src/schemas/wallet-preset.ts"
      to: "packages/core/src/index.ts"
      via: "re-export"
      pattern: "wallet-preset"
---

<objective>
DB v24 마이그레이션으로 wallets.wallet_type 컬럼을 추가하고, @waiaas/core에 WalletPreset 타입과 빌트인 프리셋 레지스트리(D'CENT 1종)를 정의한다.

Purpose: Phase 266의 자동 설정 파이프라인과 Plan 02의 API 확장이 의존하는 DB 컬럼과 타입 기반을 마련한다.
Output: wallet_type 컬럼이 있는 DB v24 + WalletPreset 타입 + BUILTIN_PRESETS 레지스트리 + 마이그레이션 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/index.ts
@packages/core/src/schemas/signing-protocol.ts
@packages/daemon/src/infrastructure/database/schema.ts
@packages/daemon/src/infrastructure/database/migrate.ts
@packages/daemon/src/__tests__/migration-chain.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WalletPreset 타입 정의 + 빌트인 프리셋 레지스트리</name>
  <files>
    packages/core/src/schemas/wallet-preset.ts
    packages/core/src/index.ts
  </files>
  <action>
1. `packages/core/src/schemas/wallet-preset.ts` 파일을 생성한다:

```typescript
// Zod SSoT for wallet preset types
import { z } from 'zod';

/** Wallet preset type identifiers (lowercase kebab-case). */
export const WALLET_PRESET_TYPES = ['dcent'] as const;

export const WalletPresetTypeSchema = z.enum(WALLET_PRESET_TYPES);
export type WalletPresetType = z.infer<typeof WalletPresetTypeSchema>;

/** Wallet preset configuration — defines auto-setup parameters for a wallet type. */
export interface WalletPreset {
  /** Display name (human-readable). */
  displayName: string;
  /** Preferred approval method for this wallet type. */
  approvalMethod: 'sdk_ntfy' | 'sdk_telegram' | 'walletconnect' | 'telegram_bot' | 'rest';
  /** Preferred wallet identifier for signing SDK registry. */
  preferredWallet: string;
  /** Whether signing SDK should be enabled for this preset. */
  signingEnabled: boolean;
  /** Optional description for Admin UI display. */
  description?: string;
}

/**
 * Builtin preset registry.
 * Key = WalletPresetType, Value = WalletPreset config.
 * Phase 266 auto-setup reads from this registry.
 */
export const BUILTIN_PRESETS: Record<WalletPresetType, WalletPreset> = {
  dcent: {
    displayName: "D'CENT Wallet",
    approvalMethod: 'walletconnect',
    preferredWallet: 'dcent',
    signingEnabled: true,
    description: "D'CENT hardware wallet with WalletConnect signing",
  },
};
```

2. `packages/core/src/index.ts`에 wallet-preset 모듈을 re-export한다:
   - `export { WALLET_PRESET_TYPES, WalletPresetTypeSchema, BUILTIN_PRESETS } from './schemas/wallet-preset.js';`
   - `export type { WalletPresetType, WalletPreset } from './schemas/wallet-preset.js';`
   - 기존 export 구조 패턴을 따라 적절한 위치(schemas 섹션 근처)에 추가한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/core 2>&1 | tail -5</automated>
    <manual>BUILTIN_PRESETS.dcent이 존재하고 WalletPresetTypeSchema.parse('dcent')가 성공하는지 확인</manual>
  </verify>
  <done>
    - WalletPresetTypeSchema가 'dcent'를 유효값으로 파싱한다
    - BUILTIN_PRESETS에 dcent 프리셋이 approvalMethod='walletconnect', preferredWallet='dcent', signingEnabled=true로 존재한다
    - @waiaas/core에서 WALLET_PRESET_TYPES, WalletPresetTypeSchema, WalletPresetType, WalletPreset, BUILTIN_PRESETS가 export된다
    - typecheck가 통과한다
  </done>
</task>

<task type="auto">
  <name>Task 2: DB v24 마이그레이션 + Drizzle 스키마 + 마이그레이션 테스트</name>
  <files>
    packages/daemon/src/infrastructure/database/schema.ts
    packages/daemon/src/infrastructure/database/migrate.ts
    packages/daemon/src/__tests__/migration-chain.test.ts
  </files>
  <action>
1. **Drizzle schema** (`packages/daemon/src/infrastructure/database/schema.ts`):
   - wallets 테이블 정의에 `walletType: text('wallet_type')` 컬럼을 추가한다.
   - `monitorIncoming` 바로 아래, 테이블 정의 마지막 컬럼으로 배치한다.
   - nullable이므로 `.notNull()` 없이 그냥 `text('wallet_type')`으로 선언.
   - 테이블 상단 주석에 `v28.8: wallet_type column added for wallet preset auto-setup.`를 추가한다.

2. **Fresh DB DDL** (`packages/daemon/src/infrastructure/database/migrate.ts`):
   - `LATEST_SCHEMA_VERSION`을 `23` → `24`로 증가시킨다.
   - `getCreateTableStatements()`의 wallets CREATE TABLE DDL에 `wallet_type TEXT` 컬럼을 추가한다. `monitor_incoming` 다음 줄, `owner_approval_method` 다음에 배치.
   - CHECK 제약은 추가하지 않는다 (wallet_type은 응용 레벨 Zod로 검증 — SQLite ALTER CHECK 불가하므로 향후 확장성 유지).

3. **v24 마이그레이션** (`packages/daemon/src/infrastructure/database/migrate.ts`):
   - MIGRATIONS 배열에 version 24를 추가:
   ```typescript
   MIGRATIONS.push({
     version: 24,
     description: 'Add wallet_type column to wallets table for preset auto-setup',
     up: (sqlite) => {
       sqlite.exec('ALTER TABLE wallets ADD COLUMN wallet_type TEXT');
     },
   });
   ```
   - Simple ALTER TABLE — `managesOwnTransaction: false` (기본값), CHECK 제약 변경 없으므로 12-step 불필요.

4. **마이그레이션 테스트** (`packages/daemon/src/__tests__/migration-chain.test.ts`):
   - 기존 테스트 파일에 v24 관련 테스트를 추가한다:
   - **T-v24-1**: v23 DB에서 v24 마이그레이션 적용 후 wallets 테이블에 wallet_type 컬럼이 존재하고 NULL 기본값인지 확인
   - **T-v24-2**: Fresh DB pushSchema 후 LATEST_SCHEMA_VERSION이 24인지 확인 (기존 T-4 업데이트 또는 별도 테스트)
   - **T-v24-3**: v24 마이그레이션 후 기존 wallet 데이터의 wallet_type이 NULL인지 확인 (하위 호환)
   - **T-v24-4**: Fresh DB에서 wallet_type 컬럼이 포함된 wallets 테이블이 생성되는지 확인
   - 기존 migration-chain.test.ts 패턴을 따른다: createDatabase(':memory:'), pushSchema, PRAGMA table_info 검증.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -5 && pnpm vitest run packages/daemon/src/__tests__/migration-chain.test.ts --reporter=verbose 2>&1 | tail -20</automated>
    <manual>LATEST_SCHEMA_VERSION === 24, v24 migration이 MIGRATIONS 배열에 존재, wallets DDL에 wallet_type TEXT 포함</manual>
  </verify>
  <done>
    - LATEST_SCHEMA_VERSION이 24이다
    - MIGRATIONS 배열에 version 24 (wallet_type 추가)가 존재한다
    - Fresh DB pushSchema() 후 wallets 테이블에 wallet_type TEXT nullable 컬럼이 존재한다
    - 기존 DB(v23)에서 v24 마이그레이션 적용 후 wallet_type 컬럼이 추가되고 기존 데이터는 NULL이다
    - Drizzle schema에 walletType 필드가 선언되어 있다
    - daemon typecheck가 통과한다
    - migration-chain 테스트가 모두 통과한다
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/core --filter=@waiaas/daemon` 통과
2. `pnpm vitest run packages/daemon/src/__tests__/migration-chain.test.ts` 전체 통과
3. LATEST_SCHEMA_VERSION === 24
4. `BUILTIN_PRESETS.dcent.approvalMethod === 'walletconnect'`
5. Drizzle schema wallets 테이블에 `walletType: text('wallet_type')` 존재
</verification>

<success_criteria>
- wallets.wallet_type nullable TEXT 컬럼이 DB v24 마이그레이션으로 추가된다
- Fresh DB DDL에 wallet_type 컬럼이 포함되고 LATEST_SCHEMA_VERSION이 24이다
- WalletPreset 타입과 BUILTIN_PRESETS 레지스트리가 @waiaas/core에서 export된다
- D'CENT 프리셋이 walletconnect + dcent + signingEnabled:true로 등록되어 있다
- 마이그레이션 체인 테스트가 모두 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/265-wallet-preset-foundation/265-01-SUMMARY.md`
</output>
