---
phase: 186-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/wallets.tsx
  - packages/admin/src/pages/sessions.tsx
  - packages/admin/src/pages/policies.tsx
  - packages/admin/src/pages/notifications.tsx
  - packages/admin/src/pages/security.tsx
  - packages/admin/src/pages/system.tsx
  - README.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "Every FormField in settings tabs has a description prop showing help text below the field"
    - "README.md Admin UI section lists 7 menus: Dashboard, Wallets, Sessions, Policies, Notifications, Security, System"
    - "Description text matches the search index entries for consistency"
  artifacts:
    - path: "packages/admin/src/pages/wallets.tsx"
      provides: "description props on RPC, Monitoring, WalletConnect FormFields"
      contains: "description="
    - path: "packages/admin/src/pages/sessions.tsx"
      provides: "description props on Session Settings FormFields"
      contains: "description="
    - path: "packages/admin/src/pages/policies.tsx"
      provides: "description props on Policy Defaults FormFields"
      contains: "description="
    - path: "packages/admin/src/pages/notifications.tsx"
      provides: "description props on Notification Settings FormFields"
      contains: "description="
    - path: "packages/admin/src/pages/security.tsx"
      provides: "description props on AutoStop FormFields"
      contains: "description="
    - path: "packages/admin/src/pages/system.tsx"
      provides: "description props on System FormFields"
      contains: "description="
    - path: "README.md"
      provides: "Updated Admin UI section with 7-menu structure"
      contains: "Security"
  key_links:
    - from: "settings-search-index.ts"
      to: "all settings tabs"
      via: "description text consistency"
      pattern: "description="
---

<objective>
Add description help text to every FormField in all settings tabs and update README.md Admin UI section to reflect the new 7-menu structure.

Purpose: Complete Phase 186 (finalization) -- fill in all description help text on settings fields and update documentation so the v2.3 milestone is ready for release.
Output: Updated 6 page files with description props + updated README.md
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/admin/src/utils/settings-search-index.ts
@packages/admin/src/components/form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add description props to all settings FormField components</name>
  <files>
    packages/admin/src/pages/wallets.tsx
    packages/admin/src/pages/sessions.tsx
    packages/admin/src/pages/policies.tsx
    packages/admin/src/pages/notifications.tsx
    packages/admin/src/pages/security.tsx
    packages/admin/src/pages/system.tsx
  </files>
  <action>
Add `description` prop to every FormField in settings tabs. Use descriptions from `settings-search-index.ts` for consistency.

**wallets.tsx -- RpcEndpointsTab:**
The RpcField component renders FormField for each RPC key. The RPC fields are rendered via a map with short keys, so descriptions should be looked up by key. Since there are 14 RPC URL fields that all have "RPC endpoint URL for {network}" pattern, add description to the FormField inside RpcField:
- For the `FormField` inside `RpcField`, derive a description from the shortKey. Use a map or helper:
  - `solana_mainnet` -> "RPC endpoint URL for Solana mainnet"
  - `solana_devnet` -> "RPC endpoint URL for Solana devnet"
  - `solana_testnet` -> "RPC endpoint URL for Solana testnet"
  - `evm_ethereum_mainnet` -> "RPC endpoint URL for Ethereum mainnet"
  - etc. (all 10 EVM + 3 Solana keys)
  Simplest approach: Create a `const RPC_DESCRIPTIONS: Record<string, string>` map at module scope. Then pass `description={RPC_DESCRIPTIONS[shortKey]}` to the FormField in RpcField.
- `evm_default_network` FormField: add `description="Default EVM network for new wallets"`

**wallets.tsx -- BalanceMonitoringTab:**
The fields array maps to FormFields. Add description lookup. Create a `const MONITORING_DESCRIPTIONS: Record<string, string>` map:
- `enabled` -> "Enable or disable balance monitoring"
- `check_interval_sec` -> "How often to check wallet balances"
- `low_balance_threshold_sol` -> "Alert when SOL balance drops below this amount"
- `low_balance_threshold_eth` -> "Alert when ETH balance drops below this amount"
- `cooldown_hours` -> "Suppress duplicate alerts for this many hours"
Pass `description={MONITORING_DESCRIPTIONS[f.key]}` to each FormField.

**wallets.tsx -- WalletConnectTab:**
- `project_id` FormField: add `description="WalletConnect Cloud project identifier"`
- `relay_url` FormField: add `description="WalletConnect relay server URL"`

**sessions.tsx -- SessionSettingsTab:**
Add `description` to each of the 7 FormFields:
- `session_ttl`: "How long a session token is valid before renewal"
- `session_absolute_lifetime`: "Maximum total session duration regardless of renewals"
- `session_max_renewals`: "Maximum number of times a session can be renewed"
- `max_sessions_per_wallet`: "Maximum concurrent sessions for a single wallet"
- `max_pending_tx`: "Maximum in-flight transactions per session"
- `rate_limit_session_rpm`: "Max requests per minute per session"
- `rate_limit_tx_rpm`: "Max transaction requests per minute per session"

**policies.tsx -- PolicyDefaultsTab:**
Add `description` to each of the 5 FormFields:
- `policy_defaults_delay_seconds`: "Default delay before executing delayed-tier transactions"
- `policy_defaults_approval_timeout`: "How long to wait for owner approval before timeout"
- `default_deny_tokens`: "Deny token transfers unless a matching whitelist policy exists"
- `default_deny_contracts`: "Deny contract calls unless a matching whitelist policy exists"
- `default_deny_spenders`: "Deny token approvals unless a matching whitelist policy exists"

**notifications.tsx -- NotificationSettingsTab:**
Add `description` to each FormField in the Telegram and Other Channels FieldGroups:
- `notifications.enabled`: "Enable or disable notifications globally"
- `notifications.telegram_bot_token`: "Bot token for Telegram notifications"
- `notifications.telegram_chat_id`: "Chat ID for Telegram notification delivery"
- `notifications.locale`: "Language for notification messages"
- `telegram.enabled`: "Enable or disable the Telegram bot"
- `telegram.bot_token`: "Dedicated bot token for Telegram bot (optional, uses notification token if empty)"
- `telegram.locale` (not in search index, use): "Language for Telegram bot messages"
- `notifications.discord_webhook_url`: "Webhook URL for Discord notifications"
- `notifications.ntfy_server`: "Server URL for ntfy notifications"
- `notifications.ntfy_topic`: "Topic name for ntfy notifications"
- `notifications.slack_webhook_url`: "Webhook URL for Slack notifications"
- `notifications.rate_limit_rpm`: "Max notifications per minute"

**security.tsx -- AutoStopTab:**
Add `description` to each of the 6 FormFields:
- `autostop.enabled`: "Enable or disable AutoStop protection rules"
- `autostop.consecutive_failures_threshold`: "Suspend wallet after this many consecutive failed transactions"
- `autostop.unusual_activity_threshold`: "Max transactions within window before triggering unusual activity alert"
- `autostop.unusual_activity_window_sec`: "Time window for unusual activity detection"
- `autostop.idle_timeout_sec`: "Revoke sessions with no activity for this duration"
- `autostop.idle_check_interval_sec`: "How often to check for idle sessions"

**system.tsx:**
Add `description` to FormFields in OracleSection, GlobalRateLimitSection, LogLevelSection:
- `oracle.cross_validation_threshold`: "Maximum allowed deviation between price oracle sources"
- `security.rate_limit_global_ip_rpm`: "Maximum API requests per minute from a single IP address"
- `daemon.log_level`: "Daemon logging verbosity level"
  </action>
  <verify>
Run `grep -c 'description=' packages/admin/src/pages/{wallets,sessions,policies,notifications,security,system}.tsx` and confirm each file has description props.
Run `pnpm turbo run typecheck --filter=@waiaas/admin` to verify no type errors.
  </verify>
  <done>
Every FormField in settings tabs has a `description` prop. The description text matches the search index entries for consistency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md Admin UI section to 7-menu structure</name>
  <files>
    README.md
  </files>
  <action>
Replace the current Admin UI section in README.md. The current section (lines 79-96) has 6 items including "Settings". Update to 7 items reflecting the new menu structure:

```
## Admin UI

After starting the daemon, access the admin panel at:

```
http://127.0.0.1:3100/admin
```

Requires masterAuth (master password) to log in. The Admin UI provides:

- **Dashboard** -- System overview, wallet balances, recent transactions
- **Wallets** -- Create, manage, and monitor wallets across chains; RPC endpoints, balance monitoring, and WalletConnect settings
- **Sessions** -- Issue and revoke agent session tokens; session lifetime and rate limit settings
- **Policies** -- Configure 12 policy types with visual form editors; default deny and tier settings
- **Notifications** -- Channel status and delivery logs; Telegram, Discord, ntfy, and Slack settings
- **Security** -- Kill Switch emergency controls, AutoStop protection rules, JWT rotation
- **System** -- API keys, display currency, price oracle, rate limits, log level, and daemon shutdown

Features include settings search (Ctrl+K / Cmd+K) and unsaved changes protection.

Enabled by default (`admin_ui = true` in config.toml).
```

Note: The "Settings" item is removed because settings are now distributed across each page's Settings tab. Security and System are new top-level pages.
  </action>
  <verify>
Read README.md and confirm the Admin UI section lists 7 menu items (Dashboard, Wallets, Sessions, Policies, Notifications, Security, System) and mentions settings search + unsaved changes protection.
  </verify>
  <done>
README.md Admin UI section reflects the new 7-menu structure with expanded descriptions for each page, and mentions key UX features (settings search, unsaved changes guard).
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'description=' packages/admin/src/pages/{wallets,sessions,policies,notifications,security,system}.tsx` -- all files have description counts matching expected FormField counts
2. `pnpm turbo run typecheck --filter=@waiaas/admin` passes
3. `pnpm turbo run lint --filter=@waiaas/admin` passes
4. README.md contains "Security" and "System" in Admin UI bullet list (no more "Settings" as standalone item)
5. README.md contains "Ctrl+K" and "unsaved" references
</verification>

<success_criteria>
- All FormField components in settings tabs have description props with help text
- README.md Admin UI section lists 7 menu items with accurate descriptions
- TypeScript typecheck passes
- Lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/186-finalization/186-01-SUMMARY.md`
</output>
