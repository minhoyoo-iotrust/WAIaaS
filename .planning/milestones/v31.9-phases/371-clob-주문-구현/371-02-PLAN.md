---
phase: 371-clob-주문-구현
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/infrastructure/database/schema.ts
  - packages/daemon/src/__tests__/migration-v53-v54.test.ts
autonomous: true
requirements:
  - INTG-06

must_haves:
  truths:
    - "DB migration v53 creates polymarket_orders table with correct columns, CHECK constraints, and indexes"
    - "DB migration v54 creates polymarket_positions and polymarket_api_keys tables"
    - "polymarket_api_keys stores encrypted secret and passphrase (api_secret_encrypted, api_passphrase_encrypted)"
    - "Existing data is preserved across migrations (incremental ALTER TABLE, no drop/recreate)"
    - "Schema version table records v53 and v54"
  artifacts:
    - path: "packages/daemon/src/infrastructure/database/migrate.ts"
      provides: "v53 + v54 migration functions"
      contains: "polymarket_orders|polymarket_positions|polymarket_api_keys"
    - path: "packages/daemon/src/infrastructure/database/schema.ts"
      provides: "Drizzle ORM table definitions for 3 new tables"
      exports: ["polymarketOrders", "polymarketPositions", "polymarketApiKeys"]
    - path: "packages/daemon/src/__tests__/migration-v53-v54.test.ts"
      provides: "Migration tests for v53-v54"
  key_links:
    - from: "schema.ts"
      to: "wallets table"
      via: "foreign key wallet_id"
      pattern: "REFERENCES wallets"
    - from: "migrate.ts"
      to: "schema_version table"
      via: "version tracking"
      pattern: "INSERT INTO schema_version"
---

<objective>
DB 마이그레이션 v53-v54: polymarket_orders, polymarket_positions, polymarket_api_keys 3개 테이블을 추가한다.

Purpose: Phase 371의 OrderProvider와 후속 Phase 372의 포지션 추적이 의존하는 데이터 저장소를 생성한다. hyperliquid_orders 테이블 구조(v51-v52)를 기반으로 Polymarket 고유 컬럼을 추가한다.
Output: migrate.ts에 v53/v54 함수 추가, schema.ts에 Drizzle 테이블 정의 3개, 마이그레이션 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@internal/design/80-polymarket-prediction-market.md (Section 8: DB Migration v53-v54)

Reference existing pattern:
@packages/daemon/src/infrastructure/database/migrate.ts
@packages/daemon/src/infrastructure/database/schema.ts
@packages/daemon/src/__tests__/migration-v52.test.ts

<interfaces>
From schema.ts (current tables):
- 26 tables defined, hyperliquid_orders (v51) and hyperliquid_sub_accounts (v52) are latest additions
- All tables use text('id').primaryKey() for UUID v7
- integer('created_at', { mode: 'timestamp' }) for timestamps
- Foreign keys: text('wallet_id').references(() => wallets.id)
- CHECK constraints from @waiaas/core enum arrays

From migrate.ts:
- LATEST_SCHEMA_VERSION constant (currently 52)
- runMigrations(db) function with version checks
- Each migration: check current version -> CREATE TABLE IF NOT EXISTS -> CREATE INDEX -> INSERT schema_version
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: v53-v54 migration + Drizzle schema + tests</name>
  <files>
    packages/daemon/src/infrastructure/database/migrate.ts,
    packages/daemon/src/infrastructure/database/schema.ts,
    packages/daemon/src/__tests__/migration-v53-v54.test.ts
  </files>
  <behavior>
    - v53 migration creates polymarket_orders table with 22 columns per design doc 8.1
    - polymarket_orders has CHECK constraints on side ('BUY','SELL'), order_type ('GTC','GTD','FOK','IOC'), status (6 values)
    - polymarket_orders has 5 indexes (wallet, order_id, condition, status, created_at)
    - v54 migration creates polymarket_positions (12 columns) and polymarket_api_keys (8 columns)
    - polymarket_positions has UNIQUE(wallet_id, token_id) and 3 indexes
    - polymarket_api_keys has UNIQUE(wallet_id) constraint
    - polymarket_api_keys has api_secret_encrypted, api_passphrase_encrypted for master password encryption
    - LATEST_SCHEMA_VERSION updated to 54
    - Schema.ts has Drizzle ORM definitions matching the SQL CREATE TABLE statements
    - Migrations are idempotent (CREATE TABLE IF NOT EXISTS)
    - Running on fresh DB creates all 3 tables
    - Running on v52 DB creates 3 new tables without affecting existing data
  </behavior>
  <action>
    1. Edit `schema.ts`: Add 3 new Drizzle table definitions after hyperliquid_sub_accounts:

       `polymarketOrders` (Table 27): All 22 columns per design doc section 8.1. Use text for all bigint-range values (salt, maker_amount, taker_amount, nonce). CHECK constraints: side IN ('BUY','SELL'), order_type IN ('GTC','GTD','FOK','IOC'), status IN ('PENDING','LIVE','MATCHED','PARTIALLY_FILLED','CANCELLED','EXPIRED'). Add 5 indexes. Foreign key to wallets(id) and transactions(id) (nullable).

       `polymarketPositions` (Table 28): 12 columns per design doc section 8.2. UNIQUE on (wallet_id, token_id). CHECK on outcome IN ('YES','NO'). 3 indexes (wallet, condition, resolved).

       `polymarketApiKeys` (Table 29): 8 columns per design doc section 8.2. UNIQUE on wallet_id. api_key is plaintext (non-secret identifier). api_secret_encrypted and api_passphrase_encrypted use master password encryption (same pattern as wallets.privateKey encryption in existing code). signature_type default 0. proxy_address nullable.

       Update table count comment at top of file from 26 to 29.

    2. Edit `migrate.ts`: Update LATEST_SCHEMA_VERSION from 52 to 54. Add `migrateV53(db)` function: CREATE TABLE IF NOT EXISTS polymarket_orders with all columns, CHECK constraints, and indexes. INSERT schema_version (53). Add `migrateV54(db)` function: CREATE TABLE IF NOT EXISTS polymarket_positions and polymarket_api_keys. INSERT schema_version (54). Wire both into runMigrations chain with version guards (only run if current version < target).

    3. Create `migration-v53-v54.test.ts`: Follow existing migration-v52.test.ts pattern.
       - Test fresh DB gets all 3 tables with correct schema
       - Test v52 DB successfully migrates to v54
       - Test polymarket_orders CRUD (insert, query by wallet_id, update status)
       - Test polymarket_positions UNIQUE constraint (wallet_id, token_id)
       - Test polymarket_api_keys UNIQUE constraint (wallet_id)
       - Test CHECK constraints reject invalid values (bad side, bad status)
       - Test indexes exist (pragma index_list)
       - Test schema_version records v53 and v54
       - Test idempotency (running migration twice is safe)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/migration-v53-v54.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - LATEST_SCHEMA_VERSION is 54
    - polymarket_orders table has 22 columns, 5 indexes, 3 CHECK constraints
    - polymarket_positions table has 12 columns, UNIQUE(wallet_id, token_id), 3 indexes
    - polymarket_api_keys table has 8 columns, UNIQUE(wallet_id), encrypted fields for secret/passphrase
    - Drizzle schema matches SQL DDL
    - Migration tests pass for fresh and incremental scenarios
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/migration-v53-v54.test.ts --reporter=verbose
```
Migration tests pass.

```bash
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon
```
TypeScript compiles without errors.
</verification>

<success_criteria>
- DB schema version is 54
- 3 new tables created with correct structure matching design doc section 8
- Encrypted fields for API credentials follow existing master password encryption pattern
- All migration tests pass
- Existing v52 data unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/371-clob-주문-구현/371-02-SUMMARY.md`
</output>
