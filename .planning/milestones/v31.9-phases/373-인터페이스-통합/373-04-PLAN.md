---
phase: 373-인터페이스-통합
plan: "04"
type: execute
wave: 2
depends_on: ["373-01"]
files_modified:
  - packages/daemon/src/api/routes/connect-info.ts
  - packages/daemon/src/__tests__/polymarket-policy.test.ts
  - skills/polymarket.skill.md
  - skills/actions.skill.md
autonomous: true
requirements: [INTG-05, INTG-07, INTG-10]

must_haves:
  truths:
    - "정책 엔진이 pm_buy/pm_split에 SPENDING_LIMIT을 적용하고 pm_cancel 등에는 적용하지 않는다"
    - "Polymarket action 실행 시 wallet이 Polygon 네트워크가 아니면 에러가 발생한다"
    - "connect-info에 polymarket capability가 포함된다"
    - "polymarket.skill.md 파일이 생성되어 AI 에이전트 가이드를 제공한다"
  artifacts:
    - path: "packages/daemon/src/api/routes/connect-info.ts"
      provides: "polymarket capability in connect-info"
      contains: "polymarket"
    - path: "skills/polymarket.skill.md"
      provides: "Polymarket skill file"
      contains: "AI agents must NEVER request the master password"
    - path: "packages/daemon/src/__tests__/polymarket-policy.test.ts"
      provides: "Policy engine integration tests"
  key_links:
    - from: "packages/daemon/src/api/routes/connect-info.ts"
      to: "actions.polymarket_enabled"
      via: "settings.get check"
      pattern: "polymarket_enabled.*polymarket"
    - from: "packages/actions/src/providers/polymarket/order-provider.ts"
      to: "getSpendingAmount"
      via: "pipeline Stage 3 policy evaluation"
      pattern: "getSpendingAmount"
---

<objective>
정책 엔진 연동(지출 한도 + 네트워크 검증) + connect-info polymarket capability + Skill 파일 생성/업데이트

Purpose: Polymarket 거래가 기존 보안 정책 프레임워크에 통합되고, 에이전트 자기 발견과 skill 가이드가 제공된다
Output: connect-info 확장, 정책 테스트, polymarket.skill.md, actions.skill.md 업데이트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/373-인터페이스-통합/373-01-PLAN.md

<interfaces>
<!-- connect-info capability pattern from Hyperliquid -->

From packages/daemon/src/api/routes/connect-info.ts (lines 379-386):
```typescript
// hyperliquid: check if enabled via settings
if (deps.settingsService.get('actions.hyperliquid_enabled') === 'true') {
  capabilities.push('hyperliquid');
}
```

From connect-info prompt builder (lines 162-165):
```typescript
if (capabilities.includes('hyperliquid')) {
  lines.push('Hyperliquid Perp Trading: Use hl_* action tools...');
}
```

<!-- Policy engine: getSpendingAmount already implemented in OrderProvider (design doc 80, Section 11) -->
From packages/actions/src/providers/polymarket/order-provider.ts:
```typescript
async getSpendingAmount(actionName: string, params: unknown): Promise<{ amount: bigint; asset: string }> {
  // pm_buy: size * price in USDC.e 6 decimals
  // pm_sell/cancel: 0n
  // pm_update: delta if increasing
}
```

<!-- Network validation already in provider resolve() -->
From order-provider.ts resolve():
```typescript
if (context.wallet.networkId !== 'polygon-mainnet') {
  throw new ChainError('INVALID_NETWORK', ...);
}
```

<!-- Skill file pattern from skills/actions.skill.md -->
```markdown
---
name: "..."
description: "..."
category: "api"
tags: [...]
version: "..."
dispatch:
  kind: "tool"
  allowedCommands: ["curl"]
---
# Title
> AI agents must NEVER request the master password. Use only your session token.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: connect-info polymarket capability + 정책 통합 테스트</name>
  <files>
    packages/daemon/src/api/routes/connect-info.ts
    packages/daemon/src/__tests__/polymarket-policy.test.ts
  </files>
  <action>
1. connect-info.ts 수정:
   a) capabilities 구성 부분에 Polymarket 조건 추가 (Hyperliquid 패턴 바로 아래):
      ```typescript
      if (deps.settingsService.get('actions.polymarket_enabled') === 'true') {
        capabilities.push('polymarket');
      }
      ```

   b) buildConnectInfoPrompt에 polymarket capability 문구 추가:
      ```typescript
      if (capabilities.includes('polymarket')) {
        lines.push('Polymarket Prediction Market: Use pm_buy/pm_sell/pm_cancel_order/pm_cancel_all/pm_update_order for CLOB trading. CTF operations: pm_split_position/pm_merge_positions/pm_redeem_positions/pm_approve_collateral/pm_approve_ctf. Query: GET /v1/wallets/{id}/polymarket/positions, /orders, /pnl, /balance. Markets: GET /v1/polymarket/markets, /events. Setup: POST /v1/wallets/{id}/polymarket/setup.');
      }
      ```

2. polymarket-policy.test.ts 생성 — 정책 엔진 통합 테스트:
   a) getSpendingAmount 검증:
      - pm_buy(tokenId, price='0.65', size='100') -> amount = 65_000000n (USDC.e 6d), asset = USDC_E_ADDRESS
      - pm_sell -> amount = 0n
      - pm_cancel_order -> amount = 0n
      - pm_cancel_all -> amount = 0n
      - pm_update_order (price increase) -> delta amount

   b) 네트워크 검증 테스트:
      - polygon-mainnet wallet -> 정상 실행
      - ethereum-mainnet wallet -> ChainError INVALID_NETWORK

   c) defaultTier 검증:
      - pm_buy -> APPROVAL
      - pm_sell -> DELAY
      - pm_cancel_order -> INSTANT
      - pm_redeem_positions -> INSTANT

   d) connect-info에 polymarket capability 포함 확인:
      - polymarket_enabled=true -> capabilities에 'polymarket' 포함
      - polymarket_enabled=false -> capabilities에 'polymarket' 미포함

   최소 12개 테스트 케이스.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/polymarket-policy.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - connect-info에 polymarket capability 조건부 포함
    - 정책 통합 테스트 12개 이상 통과
    - getSpendingAmount가 올바른 금액을 반환
    - 네트워크 검증이 polygon-mainnet만 허용
  </done>
</task>

<task type="auto">
  <name>Task 2: Skill 파일 생성 + actions.skill.md 업데이트</name>
  <files>
    skills/polymarket.skill.md
    skills/actions.skill.md
  </files>
  <action>
1. polymarket.skill.md 생성 — 새 skill 파일:
   - frontmatter: name "WAIaaS Polymarket", description "Polymarket prediction market trading...", category "api", tags [polymarket, prediction-market, clob, ctf, defi], version "2.9.0-rc", dispatch kind "tool"
   - 보안 공지: "AI agents must NEVER request the master password. Use only your session token."
   - Base URL / Authentication 섹션
   - Setup 섹션: POST /v1/wallets/{id}/polymarket/setup (API Key 생성)
   - Actions 섹션:
     - CLOB Trading: pm_buy, pm_sell, pm_cancel_order, pm_cancel_all, pm_update_order
     - CTF Operations: pm_split_position, pm_merge_positions, pm_redeem_positions, pm_approve_collateral, pm_approve_ctf
   - Query 섹션:
     - GET /v1/wallets/{id}/polymarket/positions
     - GET /v1/wallets/{id}/polymarket/orders
     - GET /v1/polymarket/markets, /markets/{conditionId}
     - GET /v1/polymarket/events
     - GET /v1/wallets/{id}/polymarket/balance, /pnl
   - Workflow Examples:
     - "마켓 검색 -> 매수 -> 포지션 확인 -> 매도" 워크플로우
     - "해결된 마켓 리딤" 워크플로우
   - Notes: Polygon 네트워크 전용, USDC.e 사용, Neg Risk 자동 라우팅

2. actions.skill.md 업데이트:
   - tags 배열에 polymarket, prediction-market, clob 추가
   - Provider 목록에 Polymarket 추가 (polymarket_order + polymarket_ctf)
   - 간단한 사용 예시 1개 추가
  </action>
  <verify>
    <automated>test -f /Users/minho.yoo/dev/wallet/WAIaaS/skills/polymarket.skill.md && grep -c "AI agents must NEVER" /Users/minho.yoo/dev/wallet/WAIaaS/skills/polymarket.skill.md</automated>
  </verify>
  <done>
    - polymarket.skill.md 파일이 생성되어 전체 Polymarket API를 문서화함
    - 보안 공지 포함됨
    - actions.skill.md에 Polymarket provider 추가됨
    - 워크플로우 예시 포함됨
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/daemon` 통과
2. `pnpm vitest run packages/daemon/src/__tests__/polymarket-policy.test.ts` 통과
3. `grep -l "polymarket" skills/*.skill.md` — 2개 파일 반환
4. `grep "AI agents must NEVER" skills/polymarket.skill.md` — 보안 공지 확인
</verification>

<success_criteria>
- connect-info에 polymarket capability 포함
- 정책 엔진이 pm_buy에 SPENDING_LIMIT 적용, pm_cancel에는 미적용
- Polygon 네트워크 검증 동작
- polymarket.skill.md 생성 (보안 공지 포함)
- actions.skill.md에 Polymarket 추가
- 테스트 12개 이상 통과
</success_criteria>

<output>
After completion, create `.planning/phases/373-인터페이스-통합/373-04-SUMMARY.md`
</output>
