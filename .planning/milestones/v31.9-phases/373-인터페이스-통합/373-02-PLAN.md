---
phase: 373-인터페이스-통합
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/tools/polymarket.ts
  - packages/mcp/src/server.ts
  - packages/sdk/src/client.ts
  - packages/sdk/src/__tests__/client-polymarket.test.ts
  - packages/mcp/src/__tests__/polymarket-tools.test.ts
autonomous: true
requirements: [INTG-02, INTG-03]

must_haves:
  truths:
    - "MCP 8개 쿼리 도구가 등록되어 AI 에이전트가 Polymarket 데이터를 조회할 수 있다"
    - "TypeScript SDK에서 Polymarket 주문/조회/설정 메서드를 호출할 수 있다"
    - "Action 10개 도구는 mcpExpose=true로 자동 등록된다"
  artifacts:
    - path: "packages/mcp/src/tools/polymarket.ts"
      provides: "8 manually registered MCP query tools"
      exports: ["registerPolymarketTools"]
    - path: "packages/sdk/src/client.ts"
      provides: "Polymarket client methods on WAIaaSClient"
      contains: "pmBuy"
    - path: "packages/sdk/src/__tests__/client-polymarket.test.ts"
      provides: "SDK Polymarket method tests"
  key_links:
    - from: "packages/mcp/src/server.ts"
      to: "packages/mcp/src/tools/polymarket.ts"
      via: "registerPolymarketTools import and call"
      pattern: "registerPolymarketTools"
    - from: "packages/sdk/src/client.ts"
      to: "/v1/actions/polymarket_order/:action"
      via: "this.executeAction('polymarket_order', ...)"
      pattern: "executeAction.*polymarket_order"
---

<objective>
MCP 쿼리 도구 8개 수동 등록 + TypeScript SDK PolymarketClient 메서드 추가

Purpose: AI 에이전트(MCP)와 개발자(SDK)가 Polymarket 전체 기능에 접근할 수 있도록 한다
Output: polymarket.ts MCP 도구 파일, client.ts에 Polymarket 메서드, 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- MCP tool registration pattern from Hyperliquid -->

From packages/mcp/src/tools/hyperliquid.ts:
```typescript
export function registerHyperliquidTools(
  server: McpServer, apiClient: ApiClient, walletContext?: WalletContext
): void {
  server.tool('waiaas_hl_get_positions', withWalletPrefix('...', walletContext?.walletName),
    { wallet_id: z.string().optional().describe('...') },
    async (args) => {
      const walletId = args.wallet_id || 'default';
      const result = await apiClient.get(`/v1/wallets/${walletId}/hyperliquid/positions`);
      return toToolResult(result);
    });
  // ... 7 more tools
}
```

From packages/mcp/src/server.ts (line 119):
```typescript
registerHyperliquidTools(server, apiClient, walletContext);
```

<!-- SDK pattern from client.ts -->

From packages/sdk/src/client.ts (lines 1016-1160):
```typescript
// Action methods:
async hlOpenPosition(walletId: string, params: Record<string, unknown>) {
  return this.executeAction('hyperliquid_perp', 'hl_open_position', { params, walletId });
}
// Query methods:
async hlGetPositions(walletId: string) {
  return this.withRetry(
    () => this.http.get<unknown>(`/v1/wallets/${walletId}/hyperliquid/positions`, this.authHeaders()),
  );
}
```

<!-- Design doc 80 Section 9.2: 8 query MCP tools -->
waiaas_pm_get_positions, waiaas_pm_get_orders, waiaas_pm_get_markets,
waiaas_pm_get_market_detail, waiaas_pm_get_events, waiaas_pm_get_balance,
waiaas_pm_get_pnl, waiaas_pm_setup

<!-- Design doc 80 Section 9.3: SDK PolymarketClient methods -->
buy, sell, cancelOrder, cancelAll, updateOrder, splitPosition, mergePositions,
redeemPositions, getPositions, getOrders, getMarkets, getMarketDetail, getBalance, getPnl, setup
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP 쿼리 도구 8개 등록 + 테스트</name>
  <files>
    packages/mcp/src/tools/polymarket.ts
    packages/mcp/src/server.ts
    packages/mcp/src/__tests__/polymarket-tools.test.ts
  </files>
  <action>
1. polymarket.ts 생성 — registerPolymarketTools(server, apiClient, walletContext) 함수:
   Hyperliquid tools 패턴 복제. 8개 수동 등록 쿼리 도구:

   a) `waiaas_pm_get_positions` — GET /v1/wallets/:id/polymarket/positions
      params: wallet_id (optional)
   b) `waiaas_pm_get_orders` — GET /v1/wallets/:id/polymarket/orders
      params: wallet_id (optional), status (optional, 'LIVE'|'MATCHED'|'CANCELLED')
   c) `waiaas_pm_get_markets` — GET /v1/polymarket/markets
      params: category (optional), status (optional), keyword (optional), limit (optional)
   d) `waiaas_pm_get_market_detail` — GET /v1/polymarket/markets/:conditionId
      params: condition_id (required)
   e) `waiaas_pm_get_events` — GET /v1/polymarket/events
      params: category (optional)
   f) `waiaas_pm_get_balance` — GET /v1/wallets/:id/polymarket/balance
      params: wallet_id (optional)
   g) `waiaas_pm_get_pnl` — GET /v1/wallets/:id/polymarket/pnl
      params: wallet_id (optional)
   h) `waiaas_pm_setup` — POST /v1/wallets/:id/polymarket/setup
      params: wallet_id (required), auto_approve (optional boolean)

   모든 도구에 withWalletPrefix 적용.
   Action 도구 10개는 mcpExpose=true로 action-provider.ts에서 자동 등록되므로 수동 등록 불필요.

2. server.ts 수정:
   - import { registerPolymarketTools } from './tools/polymarket.js'
   - registerHyperliquidTools 호출 바로 아래에 registerPolymarketTools(server, apiClient, walletContext) 추가

3. polymarket-tools.test.ts 생성:
   - mock McpServer, ApiClient
   - registerPolymarketTools 호출 후 8개 도구가 등록되었는지 확인
   - 각 도구의 handler가 올바른 REST 경로를 호출하는지 확인
   - wallet_id 파라미터 기본값 'default' 동작 확인
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/mcp/src/__tests__/polymarket-tools.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - polymarket.ts에 8개 MCP 쿼리 도구 등록 함수 완성
    - server.ts에서 호출됨
    - 테스트 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: SDK PolymarketClient 메서드 15개 + 테스트</name>
  <files>
    packages/sdk/src/client.ts
    packages/sdk/src/__tests__/client-polymarket.test.ts
  </files>
  <action>
1. client.ts의 WAIaaSClient에 Polymarket 메서드 추가 (Hyperliquid 패턴 복제):

   Action 메서드 (executeAction 패턴):
   - pmBuy(walletId, params) → executeAction('polymarket_order', 'pm_buy', ...)
   - pmSell(walletId, params) → executeAction('polymarket_order', 'pm_sell', ...)
   - pmCancelOrder(walletId, orderId) → executeAction('polymarket_order', 'pm_cancel_order', ...)
   - pmCancelAll(walletId, conditionId?) → executeAction('polymarket_order', 'pm_cancel_all', ...)
   - pmUpdateOrder(walletId, params) → executeAction('polymarket_order', 'pm_update_order', ...)
   - pmSplitPosition(walletId, params) → executeAction('polymarket_ctf', 'pm_split_position', ...)
   - pmMergePositions(walletId, params) → executeAction('polymarket_ctf', 'pm_merge_positions', ...)
   - pmRedeemPositions(walletId, params) → executeAction('polymarket_ctf', 'pm_redeem_positions', ...)

   Query 메서드 (http.get + withRetry 패턴):
   - pmGetPositions(walletId) → GET /v1/wallets/{id}/polymarket/positions
   - pmGetOrders(walletId) → GET /v1/wallets/{id}/polymarket/orders
   - pmGetMarkets(params?) → GET /v1/polymarket/markets?{querystring}
   - pmGetMarketDetail(conditionId) → GET /v1/polymarket/markets/{conditionId}
   - pmGetBalance(walletId) → GET /v1/wallets/{id}/polymarket/balance
   - pmGetPnl(walletId) → GET /v1/wallets/{id}/polymarket/pnl

   Setup 메서드:
   - pmSetup(walletId) → POST /v1/wallets/{id}/polymarket/setup

   JSDoc 주석 포함 (Hyperliquid 주석 패턴 따라서).
   섹션 주석: `// --- Polymarket convenience methods ---`

2. client-polymarket.test.ts 생성 (client-hyperliquid.test.ts 패턴):
   - mock HTTP client
   - 각 action 메서드가 올바른 provider/action으로 executeAction 호출하는지 확인
   - 각 query 메서드가 올바른 URL을 호출하는지 확인
   - 최소 15개 테스트 케이스
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/sdk/src/__tests__/client-polymarket.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - WAIaaSClient에 Polymarket 메서드 15개 추가
    - 각 action 메서드가 올바른 provider/action name으로 호출됨
    - 각 query 메서드가 올바른 REST 경로를 호출함
    - 테스트 15개 이상 통과
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/mcp --filter=@waiaas/sdk` 통과
2. MCP 도구 테스트 통과
3. SDK 클라이언트 테스트 통과
</verification>

<success_criteria>
- MCP 8개 쿼리 도구 등록됨 (10개 action 도구는 자동 등록)
- SDK 15개 Polymarket 메서드 추가됨
- 테스트 23개 이상 통과
</success_criteria>

<output>
After completion, create `.planning/phases/373-인터페이스-통합/373-02-SUMMARY.md`
</output>
