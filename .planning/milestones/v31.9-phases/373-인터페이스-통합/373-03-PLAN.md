---
phase: 373-인터페이스-통합
plan: "03"
type: execute
wave: 2
depends_on: ["373-01"]
files_modified:
  - packages/admin/src/pages/polymarket.tsx
  - packages/admin/src/components/polymarket/PolymarketOverview.tsx
  - packages/admin/src/components/polymarket/PolymarketMarkets.tsx
  - packages/admin/src/components/polymarket/PolymarketOrders.tsx
  - packages/admin/src/components/polymarket/PolymarketPositions.tsx
  - packages/admin/src/components/polymarket/PolymarketSettings.tsx
  - packages/admin/src/components/layout.tsx
  - packages/admin/src/__tests__/polymarket.test.tsx
autonomous: true
requirements: [INTG-04]

must_haves:
  truths:
    - "Admin UI에 Polymarket 메뉴 항목이 표시되고 클릭 시 5탭 페이지가 렌더링된다"
    - "Overview 탭에서 활성 포지션 수, 총 PnL, USDC.e 잔액이 표시된다"
    - "Markets 탭에서 마켓 목록을 검색/필터링할 수 있다"
    - "Orders 탭에서 활성 주문과 이력이 표시된다"
    - "Positions 탭에서 포지션과 미실현 PnL이 표시된다"
    - "Settings 탭에서 7개 Polymarket 설정을 확인할 수 있다"
  artifacts:
    - path: "packages/admin/src/pages/polymarket.tsx"
      provides: "5-tab Polymarket page"
      min_lines: 50
    - path: "packages/admin/src/components/polymarket/PolymarketOverview.tsx"
      provides: "Overview dashboard component"
    - path: "packages/admin/src/components/layout.tsx"
      provides: "Navigation with Polymarket menu entry"
      contains: "polymarket"
  key_links:
    - from: "packages/admin/src/components/layout.tsx"
      to: "packages/admin/src/pages/polymarket.tsx"
      via: "route definition and lazy import"
      pattern: "/polymarket.*PolymarketPage"
    - from: "packages/admin/src/pages/polymarket.tsx"
      to: "/v1/polymarket/markets"
      via: "apiGet fetch calls"
      pattern: "apiGet.*polymarket"
---

<objective>
Admin UI에 Polymarket 예측 시장 탭 추가 (5탭: Overview/Markets/Orders/Positions/Settings)

Purpose: 관리자가 Admin UI에서 Polymarket 포지션, 마켓, 주문, 설정을 시각적으로 관리할 수 있다
Output: polymarket.tsx 페이지 + 5개 탭 컴포넌트 + layout.tsx 네비게이션 추가
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/373-인터페이스-통합/373-01-PLAN.md

<interfaces>
<!-- Hyperliquid Admin UI pattern to replicate -->

From packages/admin/src/pages/hyperliquid.tsx:
```typescript
type Tab = 'overview' | 'orders' | 'spot' | 'subaccounts' | 'settings';
export default function HyperliquidPage() {
  const wallets = useSignal<Wallet[]>([]);
  const selectedWalletId = useSignal<string | null>(null);
  const activeTab = useSignal<Tab>('overview');
  // ... tab rendering with components
}
```

From packages/admin/src/components/layout.tsx:
```typescript
// Navigation entry pattern (line 82):
{ path: '/hyperliquid', label: 'Hyperliquid' },
// Route rendering (line 105):
if (path === '/hyperliquid') return <HyperliquidPage />;
// Page titles (line 41):
'/hyperliquid': 'Hyperliquid',
```

From packages/admin/src/components/hyperliquid/PositionsTable.tsx:
- Table component with position data display
- Uses apiGet for data fetching
- @preact/signals for state management

<!-- REST endpoints from Plan 01 (design doc 80, Section 9.1) -->
GET /v1/wallets/:walletId/polymarket/positions
GET /v1/wallets/:walletId/polymarket/orders
GET /v1/polymarket/markets
GET /v1/polymarket/markets/:conditionId
GET /v1/polymarket/events
GET /v1/wallets/:walletId/polymarket/balance
GET /v1/wallets/:walletId/polymarket/pnl
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polymarket 5탭 페이지 + 5개 컴포넌트</name>
  <files>
    packages/admin/src/pages/polymarket.tsx
    packages/admin/src/components/polymarket/PolymarketOverview.tsx
    packages/admin/src/components/polymarket/PolymarketMarkets.tsx
    packages/admin/src/components/polymarket/PolymarketOrders.tsx
    packages/admin/src/components/polymarket/PolymarketPositions.tsx
    packages/admin/src/components/polymarket/PolymarketSettings.tsx
  </files>
  <action>
1. polymarket.tsx 생성 (Hyperliquid 페이지 패턴 복제):
   - type Tab = 'overview' | 'markets' | 'orders' | 'positions' | 'settings'
   - wallets signal (filter to Polygon EVM wallets: chain === 'ethereum' AND network contains 'polygon')
   - selectedWalletId signal
   - activeTab signal (default 'overview')
   - 탭 버튼 UI (tabStyle, tabButton 패턴 동일)
   - 탭별 컴포넌트 렌더링

2. PolymarketOverview.tsx:
   - 선택된 지갑의 활성 포지션 수, 총 PnL (unrealized + realized), USDC.e 잔액 표시
   - apiGet(/v1/wallets/{id}/polymarket/pnl) + apiGet(/v1/wallets/{id}/polymarket/balance)
   - 최근 주문 5개 간략 표시
   - 빈 상태 메시지: "No Polymarket activity. Use pm_setup to create API keys."

3. PolymarketMarkets.tsx:
   - 마켓 목록 테이블 (question, volume, liquidity, endDate)
   - 검색 입력 필드 (keyword)
   - 카테고리 필터 (드롭다운)
   - apiGet(/v1/polymarket/markets?keyword=...) 호출
   - 빈 상태: "No markets found."

4. PolymarketOrders.tsx:
   - 활성 주문 테이블 (market, side, type, price, size, status, createdAt)
   - 상태 필터 (LIVE/MATCHED/CANCELLED/ALL)
   - apiGet(/v1/wallets/{id}/polymarket/orders?status=...) 호출
   - 빈 상태: "No orders found."

5. PolymarketPositions.tsx:
   - 포지션 테이블 (market, outcome, size, avgPrice, currentPrice, unrealizedPnl)
   - 해결된 마켓 별도 섹션 (Redeem 가능 표시)
   - apiGet(/v1/wallets/{id}/polymarket/positions) 호출
   - 빈 상태: "No positions."

6. PolymarketSettings.tsx:
   - 7개 Admin Settings 표시 (SettingsPanel 패턴 - Hyperliquid SettingsPanel.tsx 참조)
   - actions.polymarket_* 키들을 apiGet/apiPut으로 읽기/쓰기
   - API Key 상태 표시 (설정 여부)

Preact 10.x + @preact/signals. CSS는 인라인 스타일 (Hyperliquid 패턴). CSP 준수 (no external scripts).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/admin 2>&1 | tail -5</automated>
  </verify>
  <done>
    - polymarket.tsx가 5탭 페이지로 동작한다
    - 각 탭 컴포넌트가 REST API를 호출하여 데이터를 표시한다
    - 빈 상태 처리가 포함된다
  </done>
</task>

<task type="auto">
  <name>Task 2: layout.tsx 네비게이션 추가 + 컴포넌트 테스트</name>
  <files>
    packages/admin/src/components/layout.tsx
    packages/admin/src/__tests__/polymarket.test.tsx
  </files>
  <action>
1. layout.tsx 수정 (Hyperliquid 패턴 복제):
   - import PolymarketPage from '../pages/polymarket'
   - PAGE_TITLES에 '/polymarket': 'Polymarket' 추가
   - PAGE_DESCRIPTIONS에 '/polymarket': 'Polymarket prediction market positions, orders, and settings' 추가
   - NAV_ITEMS에 { path: '/polymarket', label: 'Polymarket' } 추가 (Hyperliquid 아래)
   - renderPage에 `if (path === '/polymarket') return <PolymarketPage />;` 추가

2. polymarket.test.tsx 생성 (hyperliquid.test.tsx 패턴):
   - PolymarketPage 렌더링 테스트
   - 5탭 전환 테스트
   - 각 탭 컴포넌트 기본 렌더링 테스트 (mock apiGet)
   - 빈 상태 표시 테스트
   - 최소 8개 테스트 케이스
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/admin/src/__tests__/polymarket.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>
    - layout.tsx에 Polymarket 메뉴가 추가됨
    - polymarket.test.tsx에서 페이지/탭/컴포넌트 테스트 8개 이상 통과
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/admin` 통과
2. `pnpm vitest run packages/admin/src/__tests__/polymarket.test.tsx` 통과
3. `pnpm turbo run build --filter=@waiaas/admin` 통과
</verification>

<success_criteria>
- Admin UI에 Polymarket 메뉴 항목 표시
- 5탭 페이지 (Overview/Markets/Orders/Positions/Settings) 렌더링
- 각 탭이 올바른 REST API를 호출
- 테스트 8개 이상 통과
</success_criteria>

<output>
After completion, create `.planning/phases/373-인터페이스-통합/373-03-SUMMARY.md`
</output>
