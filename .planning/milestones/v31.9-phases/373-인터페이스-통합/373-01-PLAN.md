---
phase: 373-인터페이스-통합
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
  - packages/daemon/src/api/routes/polymarket.ts
  - packages/daemon/src/api/routes/index.ts
  - packages/daemon/src/api/server.ts
  - packages/daemon/src/lifecycle/daemon.ts
  - packages/daemon/src/__tests__/polymarket-routes.test.ts
autonomous: true
requirements: [INTG-01]

must_haves:
  truths:
    - "Admin Settings에 Polymarket 7개 키가 등록되어 조회/변경 가능하다"
    - "REST API 9개 쿼리 라우트가 올바른 인증과 함께 응답한다"
    - "Polymarket 인프라가 daemon 시작 시 settings 기반으로 생성된다"
  artifacts:
    - path: "packages/daemon/src/infrastructure/settings/setting-keys.ts"
      provides: "7 Polymarket setting definitions"
      contains: "actions.polymarket_enabled"
    - path: "packages/daemon/src/api/routes/polymarket.ts"
      provides: "9 REST query routes"
      exports: ["createPolymarketRoutes"]
    - path: "packages/daemon/src/__tests__/polymarket-routes.test.ts"
      provides: "Route unit tests"
  key_links:
    - from: "packages/daemon/src/lifecycle/daemon.ts"
      to: "createPolymarketInfrastructure"
      via: "settings.get('actions.polymarket_enabled')"
      pattern: "polymarket_enabled.*createPolymarket"
    - from: "packages/daemon/src/api/server.ts"
      to: "packages/daemon/src/api/routes/polymarket.ts"
      via: "createPolymarketRoutes import and mount"
      pattern: "createPolymarketRoutes"
---

<objective>
Admin Settings 7개 키 등록 + REST API 9개 쿼리 라우트 생성 + daemon 시작 시 Polymarket 인프라 연결

Purpose: Polymarket 데이터를 REST API로 노출하고, 설정을 Admin Settings로 관리할 수 있는 기반을 만든다
Output: setting-keys.ts에 7개 키, polymarket.ts에 9개 라우트, daemon.ts에 인프라 부팅 로직
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/371-clob-주문-구현/371-04-SUMMARY.md
@.planning/phases/372-마켓-조회-포지션-정산/372-03-SUMMARY.md

<interfaces>
<!-- Existing Hyperliquid pattern to replicate for Polymarket -->

From packages/daemon/src/infrastructure/settings/setting-keys.ts:
```typescript
// Hyperliquid settings pattern (lines 301-309):
{ key: 'actions.hyperliquid_enabled', category: 'actions', configPath: 'actions.hyperliquid_enabled', defaultValue: 'false', isCredential: false },
// ... 8 more settings in same format
```

From packages/daemon/src/api/routes/hyperliquid.ts:
```typescript
export interface HyperliquidRouteDeps {
  db: BetterSQLite3Database<typeof schema>;
  marketData: HyperliquidMarketData | null;
}
export function createHyperliquidRoutes(deps: HyperliquidRouteDeps) { ... }
```

From packages/daemon/src/api/server.ts (line 567):
```typescript
createHyperliquidRoutes({ db: deps.db, marketData: deps.hyperliquidMarketData ?? null })
```

From packages/daemon/src/lifecycle/daemon.ts (lines 1199-1201):
```typescript
if (builtIn.hyperliquidMarketData) {
  this.hyperliquidMarketData = builtIn.hyperliquidMarketData;
}
```

From packages/actions/src/providers/polymarket/infrastructure.ts:
```typescript
export interface PolymarketInfrastructure {
  clobClient, rateLimiter, apiKeyService, orderProvider, orderbookService,
  approveHelper, ctfProvider, gammaClient, marketData,
  positionTracker: PolymarketPositionTracker | null,
  pnlCalculator: typeof PolymarketPnlCalculator,
  resolutionMonitor: PolymarketResolutionMonitor | null,
}
export function createPolymarketInfrastructure(
  config: PolymarketConfig, db: PolymarketDb,
  encryptFn: EncryptFn, decryptFn: DecryptFn,
  emitNotification?: (event: PolymarketNotificationEvent) => void,
): PolymarketInfrastructure
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Settings 7개 키 등록 + Polymarket REST 쿼리 라우트 9개</name>
  <files>
    packages/daemon/src/infrastructure/settings/setting-keys.ts
    packages/daemon/src/api/routes/polymarket.ts
    packages/daemon/src/api/routes/index.ts
  </files>
  <action>
1. setting-keys.ts에 Polymarket 7개 Admin Settings 추가 (Hyperliquid 패턴 동일, category='actions'):
   - `actions.polymarket_enabled` (boolean, default 'false')
   - `actions.polymarket_default_fee_bps` (number, default '0')
   - `actions.polymarket_order_expiry_seconds` (number, default '86400')
   - `actions.polymarket_max_position_usdc` (number, default '1000')
   - `actions.polymarket_proxy_wallet` (boolean, default 'false')
   - `actions.polymarket_neg_risk_enabled` (boolean, default 'true')
   - `actions.polymarket_auto_approve_ctf` (boolean, default 'true')

2. polymarket.ts 생성 — createPolymarketRoutes(deps) 함수, Hyperliquid routes 패턴 복제:
   - PolymarketRouteDeps 인터페이스: db, polymarketInfra (PolymarketInfrastructure | null)
   - GET /v1/wallets/:walletId/polymarket/positions — positionTracker.getPositions(walletId)
   - GET /v1/wallets/:walletId/polymarket/orders — DB query polymarket_orders WHERE wallet_id, 최근 50개
   - GET /v1/wallets/:walletId/polymarket/orders/:orderId — DB query single order
   - GET /v1/polymarket/markets — marketData.getMarkets(filter from query params)
   - GET /v1/polymarket/markets/:conditionId — marketData.getMarketDetail(conditionId)
   - GET /v1/polymarket/events — marketData.getEvents()
   - GET /v1/wallets/:walletId/polymarket/balance — USDC.e balance + CTF token positions
   - GET /v1/wallets/:walletId/polymarket/pnl — pnlCalculator.summarize(positions)
   - POST /v1/wallets/:walletId/polymarket/setup — apiKeyService.ensureKeys(walletId) + optional approve
   - 각 route에서 ensurePolymarketInfra(deps.polymarketInfra) null 체크 (Hyperliquid의 ensureMarketData 패턴)
   - wallet route에서 walletId param -> DB에서 wallet 존재 검증

3. index.ts에 export 추가: `export { createPolymarketRoutes, type PolymarketRouteDeps } from './polymarket.js';`
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -5</automated>
  </verify>
  <done>
    - setting-keys.ts에 7개 polymarket 키가 SETTING_DEFINITIONS 배열에 추가됨
    - polymarket.ts에 9개 REST route가 Hono app으로 생성됨
    - index.ts에서 re-export됨
  </done>
</task>

<task type="auto">
  <name>Task 2: daemon 시작 시 Polymarket 인프라 부팅 + server 라우트 마운트 + 테스트</name>
  <files>
    packages/daemon/src/lifecycle/daemon.ts
    packages/daemon/src/api/server.ts
    packages/daemon/src/__tests__/polymarket-routes.test.ts
  </files>
  <action>
1. daemon.ts 수정 — Hyperliquid 패턴 복제:
   - private polymarketInfra 필드 추가 (PolymarketInfrastructure | null = null)
   - settings.get('actions.polymarket_enabled') === 'true' 조건부로:
     - createPolymarketInfrastructure 호출 (config from settings, db 어댑터 구성, encrypt/decrypt 함수 전달)
     - registry.register(pm.orderProvider), registry.register(pm.ctfProvider)
     - this.polymarketInfra = pm 저장
   - API server deps에 polymarketInfra 전달

2. server.ts 수정:
   - import createPolymarketRoutes
   - ApiDeps에 polymarketInfra? 추가
   - createPolymarketRoutes({ db, polymarketInfra }) 호출하여 app에 mount

3. polymarket-routes.test.ts 생성 (Hyperliquid routes 테스트 패턴):
   - 각 9개 route에 대한 기본 테스트 (mock PolymarketInfrastructure)
   - polymarket disabled일 때 "not enabled" 에러 반환 테스트
   - wallet not found 시 404 테스트
   - 최소 10개 테스트 케이스
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/polymarket-routes.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - daemon이 polymarket_enabled=true 시 인프라를 생성하고 provider를 registry에 등록한다
    - server.ts에서 Polymarket REST 라우트가 마운트된다
    - polymarket-routes.test.ts에서 최소 10개 테스트가 통과한다
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck --filter=@waiaas/daemon` 통과
2. `pnpm vitest run packages/daemon/src/__tests__/polymarket-routes.test.ts` 통과
3. settings-completeness 테스트가 새 키를 인식: `pnpm vitest run packages/admin/src/__tests__/settings-completeness.test.ts`
</verification>

<success_criteria>
- Admin Settings에 Polymarket 7개 키 등록 완료
- REST API 9개 쿼리 라우트 생성 및 daemon에 마운트 완료
- Polymarket 인프라가 daemon 시작 시 조건부 생성됨
- 테스트 10개 이상 통과
</success_criteria>

<output>
After completion, create `.planning/phases/373-인터페이스-통합/373-01-SUMMARY.md`
</output>
