---
phase: 374-테스트-검증
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/e2e-tests/src/scenarios/onchain-polymarket.ts
  - packages/e2e-tests/src/__tests__/onchain-polymarket.e2e.test.ts
  - packages/e2e-tests/src/e2e-coverage-map.ts
autonomous: true
requirements: [INTG-08]

must_haves:
  truths:
    - "Polymarket E2E 스모크 시나리오가 ScenarioRegistry에 등록되어 있다"
    - "E2E 테스트가 CLOB mock 기반 오프체인 플로우를 검증한다"
    - "e2e-coverage-map에 polymarket provider와 route가 매핑되어 있다"
    - "verify-e2e-coverage 스크립트가 통과한다"
  artifacts:
    - path: "packages/e2e-tests/src/scenarios/onchain-polymarket.ts"
      provides: "Polymarket E2E scenario registration"
      exports: ["registry.register calls"]
    - path: "packages/e2e-tests/src/__tests__/onchain-polymarket.e2e.test.ts"
      provides: "Polymarket E2E test file"
      contains: "describe.*polymarket"
    - path: "packages/e2e-tests/src/e2e-coverage-map.ts"
      provides: "Updated coverage map with polymarket entries"
      contains: "polymarket"
  key_links:
    - from: "packages/e2e-tests/src/__tests__/onchain-polymarket.e2e.test.ts"
      to: "packages/e2e-tests/src/scenarios/onchain-polymarket.ts"
      via: "import side-effect"
      pattern: "import.*scenarios/onchain-polymarket"
    - from: "packages/e2e-tests/src/e2e-coverage-map.ts"
      to: "onchain-polymarket.ts"
      via: "providerCoverage + routeCoverage entries"
      pattern: "polymarket.*onchain-polymarket"
---

<objective>
Polymarket E2E 스모크 시나리오를 생성하고 CI 커버리지 맵에 등록한다.

Purpose: Polymarket CLOB 주문/마켓/포지션/리딤 플로우를 오프체인 mock 기반으로 E2E 검증하여 CI에서 회귀 방지
Output: E2E scenario file, test file, updated coverage map
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/e2e-tests/src/scenarios/onchain-hyperliquid.ts
@packages/e2e-tests/src/__tests__/onchain-hyperliquid.e2e.test.ts
@packages/e2e-tests/src/e2e-coverage-map.ts
@packages/e2e-tests/src/types.ts
@packages/daemon/src/api/routes/polymarket.ts

<interfaces>
<!-- Existing E2E patterns to follow -->

From packages/e2e-tests/src/types.ts:
```typescript
export type Track = 'offchain' | 'onchain';
export interface E2EScenario {
  id: string;
  name: string;
  track: Track;
  category: string;
  description: string;
  networks?: string[];
  protocols?: string[];
}
export const registry: ScenarioRegistry;
```

From packages/e2e-tests/src/helpers/http-client.ts:
```typescript
export class E2EHttpClient { get, post, put, delete, patch }
```

From packages/e2e-tests/src/helpers/session.ts:
```typescript
export class SessionManager { createSession, deleteSession, http }
```

Coverage map pattern (providerCoverage key = dir name under providers/):
```typescript
'polymarket': ['onchain-polymarket.ts'],
```

Coverage map pattern (routeCoverage key = route file name without .ts):
```typescript
'polymarket': ['onchain-polymarket.ts'],
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E 시나리오 등록 + 테스트 파일 생성</name>
  <files>packages/e2e-tests/src/scenarios/onchain-polymarket.ts, packages/e2e-tests/src/__tests__/onchain-polymarket.e2e.test.ts</files>
  <action>
1. **시나리오 등록 파일** `packages/e2e-tests/src/scenarios/onchain-polymarket.ts` 생성:
   - `onchain-hyperliquid.ts` 패턴을 따른다
   - 4개 시나리오를 `registry.register()`로 등록:
     - `polymarket-market-browse`: track=offchain, category=polymarket, "Browse markets via Gamma API, verify market list and detail"
     - `polymarket-order-place`: track=onchain, category=polymarket, networks=['polygon-mainnet'], protocols=['prediction'], "Place a limit buy order on Polymarket CLOB, verify ApiDirectResult"
     - `polymarket-position-pnl`: track=offchain, category=polymarket, "Query positions and PnL for a wallet with Polymarket orders"
     - `polymarket-settings-crud`: track=offchain, category=polymarket, "Set/read/update Polymarket Admin Settings (enabled, fee_bps, auto_approve_ctf)"

2. **테스트 파일** `packages/e2e-tests/src/__tests__/onchain-polymarket.e2e.test.ts` 생성:
   - `onchain-hyperliquid.e2e.test.ts` 패턴을 따른다
   - `WAIAAS_E2E_POLYMARKET_ENABLED` env var로 게이팅
   - `shouldSkipNetwork('polygon-mainnet')` 체크
   - Side-effect import: `import '../scenarios/onchain-polymarket.js'`
   - 4개 describe 블록:

   **polymarket-settings-crud** (offchain):
   - Admin Settings API로 `polymarket_enabled=true` 설정
   - GET `/v1/admin/settings` 로 polymarket 관련 설정 확인
   - PUT으로 `polymarket_fee_bps=50` 업데이트, 다시 GET으로 확인

   **polymarket-market-browse** (offchain, Polymarket enabled 필요):
   - GET `/v1/polymarket/markets?limit=5` 호출
   - 200 OK, items 배열 반환 확인
   - GET `/v1/polymarket/events?limit=3` 호출
   - 200 OK 확인

   **polymarket-order-place** (onchain, skipAll 게이팅):
   - Polygon wallet 찾기 (chain=ethereum, wallet list에서)
   - POST `/v1/actions/polymarket_order/buy` with `{ conditionId, side: 'buy', size: '1', price: '0.01' }`
   - 4xx면 skip (CLOB 미설정 or 잔액 없음), 201이면 `result` 필드 확인 (ApiDirectResult 패턴)

   **polymarket-position-pnl** (offchain, Polymarket enabled 필요):
   - GET `/v1/polymarket/positions?walletId={walletId}` 호출
   - 200 OK 확인 (빈 배열도 OK)
   - GET `/v1/polymarket/pnl?walletId={walletId}` 호출
   - 200 OK 확인

   각 테스트는 `it.skipIf(skipAll)` 패턴을 사용하고, 4xx 응답 시 gracefully skip한다.
   beforeAll에서 evmWallet 찾기 + SessionManager 생성, afterAll에서 세션 정리.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsx packages/e2e-tests/src/scenarios/onchain-polymarket.ts 2>&1 | head -5</automated>
  </verify>
  <done>4개 E2E 시나리오가 ScenarioRegistry에 등록되고, 테스트 파일이 컴파일 에러 없이 존재한다</done>
</task>

<task type="auto">
  <name>Task 2: E2E Coverage Map 업데이트 + CI 검증</name>
  <files>packages/e2e-tests/src/e2e-coverage-map.ts</files>
  <action>
1. `packages/e2e-tests/src/e2e-coverage-map.ts`의 `providerCoverage`에 추가:
   ```typescript
   'polymarket': ['onchain-polymarket.ts'],
   ```
   DeFi providers 그룹 (alphabetical) 에 배치한다. pendle 다음에 추가.

2. `routeCoverage`에 추가:
   ```typescript
   'polymarket': ['onchain-polymarket.ts'],
   ```
   Chain-specific 그룹의 hyperliquid 다음에 배치.

3. `pnpm verify:e2e-coverage` (또는 `npx tsx scripts/verify-e2e-coverage.ts`) 실행하여 통과 확인.
   - polymarket provider와 route가 커버리지 맵에 등록되어 있어야 한다
   - 시나리오 파일에 최소 1개 registry.register() 호출이 있어야 한다

4. `pnpm -F @waiaas/e2e-tests typecheck` 실행하여 타입 에러 없음 확인.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsx scripts/verify-e2e-coverage.ts</automated>
  </verify>
  <done>verify-e2e-coverage 스크립트가 exit 0으로 통과하고, polymarket provider/route가 커버리지 맵에 등록되어 있다</done>
</task>

</tasks>

<verification>
1. `npx tsx scripts/verify-e2e-coverage.ts` 통과 (exit 0)
2. `pnpm -F @waiaas/e2e-tests typecheck` 통과
3. Scenario registry에 4개 polymarket 시나리오 등록 확인
</verification>

<success_criteria>
- Polymarket E2E 스모크 시나리오 4개가 ScenarioRegistry에 등록됨
- E2E 테스트 파일이 CLOB mock 기반 오프체인 + 온체인 플로우를 검증함
- e2e-coverage-map에 polymarket provider/route 매핑 추가됨
- verify-e2e-coverage CI 스크립트가 통과함
</success_criteria>

<output>
After completion, create `.planning/phases/374-테스트-검증/374-01-SUMMARY.md`
</output>
