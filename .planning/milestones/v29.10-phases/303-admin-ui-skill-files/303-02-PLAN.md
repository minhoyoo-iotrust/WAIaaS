---
phase: 303-admin-ui-skill-files
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/admin.skill.md
autonomous: true
requirements: [SKIL-01]

must_haves:
  truths:
    - "admin.skill.md documents sign_topic and notify_topic fields on wallet-apps API responses"
    - "admin.skill.md documents sign_topic and notify_topic as optional fields on POST and PUT wallet-apps requests"
    - "admin.skill.md no longer shows notifications.ntfy_topic as a valid setting key"
  artifacts:
    - path: "skills/admin.skill.md"
      provides: "Updated API documentation for wallet-apps with topic fields"
      contains: "sign_topic"
  key_links:
    - from: "skills/admin.skill.md"
      to: "packages/daemon/src/api/routes/wallet-apps.ts"
      via: "API documentation reflects actual response schema"
      pattern: "sign_topic.*notify_topic"
---

<objective>
Sync admin.skill.md to reflect per-wallet topic API changes from Phase 302: add signTopic/notifyTopic fields to wallet-apps API documentation, remove notifications.ntfy_topic from settings keys list.

Purpose: Keep skill files accurate so AI agents and documentation consumers have correct API references after the per-wallet topic migration.
Output: Updated skills/admin.skill.md
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/302-per-wallet-topic-backend/302-01-SUMMARY.md

<interfaces>
<!-- Actual API response format from Phase 302 -->

From packages/daemon/src/api/routes/wallet-apps.ts (toApiResponse):
```typescript
{
  id: app.id,
  name: app.appName,
  display_name: app.displayName,
  signing_enabled: app.signingEnabled,
  alerts_enabled: app.alertsEnabled,
  sign_topic: app.signTopic,      // string | null
  notify_topic: app.notifyTopic,  // string | null
  used_by: [...],
  created_at: app.createdAt,
  updated_at: app.updatedAt,
}
```

POST /v1/admin/wallet-apps request body now accepts:
```typescript
{
  name: string,            // required
  display_name: string,    // required
  sign_topic?: string,     // optional — auto-generates waiaas-sign-{name} if omitted
  notify_topic?: string,   // optional — auto-generates waiaas-notify-{name} if omitted
}
```

PUT /v1/admin/wallet-apps/{id} request body now accepts:
```typescript
{
  signing_enabled?: boolean,
  alerts_enabled?: boolean,
  sign_topic?: string,     // NEW
  notify_topic?: string,   // NEW
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin.skill.md for per-wallet topic API changes</name>
  <files>skills/admin.skill.md</files>
  <action>
Update `skills/admin.skill.md` with the following changes:

**1. Section 8: Human Wallet Apps Management — Update the description paragraph**

Change the description to mention that each app has explicit sign_topic and notify_topic fields (not just "tied to ntfy notification topics"). The topics are stored per-app in the database and are editable.

Replace the current description text:
```
Manage Human Wallet Apps -- the wallet applications used by the human operator for signing requests and activity alerts. Apps are tied to ntfy notification topics (`waiaas-sign-{name}` for signing, `waiaas-notify-{name}` for alerts).
```

With:
```
Manage Human Wallet Apps -- the wallet applications used by the human operator for signing requests and activity alerts. Each app has explicit `sign_topic` and `notify_topic` fields for ntfy push notifications (defaults: `waiaas-sign-{name}`, `waiaas-notify-{name}`). Topics are editable per-app.
```

**2. GET /v1/admin/wallet-apps response — Add sign_topic/notify_topic**

In the JSON example response, add the two new fields after `alerts_enabled`:
```json
{
  "apps": [
    {
      "id": "<uuid>",
      "name": "dcent",
      "display_name": "D'CENT Wallet",
      "signing_enabled": true,
      "alerts_enabled": true,
      "sign_topic": "waiaas-sign-dcent",
      "notify_topic": "waiaas-notify-dcent",
      "used_by": [
        {"id": "<wallet-uuid>", "label": "my-wallet"}
      ],
      "created_at": 1707000000,
      "updated_at": 1707000000
    }
  ]
}
```

Update the response field table — add after `alerts_enabled`:

| Field | Type | Description |
| ----- | ---- | ----------- |
| `sign_topic` | string\|null | ntfy topic for signing requests (null = default `waiaas-sign-{name}`). |
| `notify_topic` | string\|null | ntfy topic for activity alerts (null = default `waiaas-notify-{name}`). |

**3. POST /v1/admin/wallet-apps request body — Add optional topic fields**

Add to the request body table after `display_name`:

| Field | Type | Required | Description |
| ----- | ---- | -------- | ----------- |
| `sign_topic` | string | No | ntfy topic for signing requests (auto-generates `waiaas-sign-{name}` if omitted). |
| `notify_topic` | string | No | ntfy topic for activity alerts (auto-generates `waiaas-notify-{name}` if omitted). |

Update the JSON example to show the optional fields:
```json
curl -s -X POST http://localhost:3100/v1/admin/wallet-apps \
  -H 'Content-Type: application/json' \
  -H 'X-Master-Password: <password>' \
  -d '{"name": "my-custom-wallet", "display_name": "My Custom Wallet", "sign_topic": "custom-sign", "notify_topic": "custom-notify"}'
```

Update the POST response example to include sign_topic/notify_topic:
```json
{
  "app": {
    "id": "<uuid>",
    "name": "my-custom-wallet",
    "display_name": "My Custom Wallet",
    "signing_enabled": true,
    "alerts_enabled": true,
    "sign_topic": "custom-sign",
    "notify_topic": "custom-notify",
    "used_by": [],
    "created_at": 1707000000,
    "updated_at": 1707000000
  }
}
```

**4. PUT /v1/admin/wallet-apps/{id} — Add topic fields to request body**

Update the PUT request body table to include:

| Field | Type | Required | Description |
| ----- | ---- | -------- | ----------- |
| `sign_topic` | string | No | Update ntfy signing request topic. |
| `notify_topic` | string | No | Update ntfy activity alert topic. |

Update the title from "Update App Toggles" to "Update Wallet App" since it now handles more than toggles.

Update the curl example to show topic update:
```bash
curl -s -X PUT http://localhost:3100/v1/admin/wallet-apps/<app-uuid> \
  -H 'Content-Type: application/json' \
  -H 'X-Master-Password: <password>' \
  -d '{"signing_enabled": true, "alerts_enabled": false, "sign_topic": "my-sign-topic"}'
```

**5. Section 4: Settings Management — Remove notifications.ntfy_topic**

In the "All valid setting keys" list under "Notifications:", remove the line:
```
- `notifications.ntfy_topic` -- ntfy topic name
```

Also in the GET /v1/admin/settings example JSON response, remove the `"notifications.ntfy_topic": ""` line from the notifications object.

In the Categories table, update the `notifications` row keys from `enabled, telegram_*, discord_*, ntfy_*, slack_*, locale, rate_limit_rpm` to `enabled, telegram_*, discord_*, ntfy_server, slack_*, locale, rate_limit_rpm` (replace `ntfy_*` with just `ntfy_server` since ntfy_topic is gone).

**6. Section 3: Notification Management — Update ntfy note**

Update the note after the test notification section:
```
> **ntfy channel:** Per-wallet ntfy topics (sign_topic, notify_topic) are managed in Human Wallet Apps (`POST/PUT /v1/admin/wallet-apps`). The ntfy server URL is a shared global setting (`notifications.ntfy_server`).
```

This replaces the existing note that references "ntfy has its own section in Notifications Settings".
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c 'sign_topic' skills/admin.skill.md && grep -c 'notify_topic' skills/admin.skill.md && ! grep -q 'notifications.ntfy_topic' skills/admin.skill.md && echo "PASS: topics present, ntfy_topic removed"</automated>
  </verify>
  <done>admin.skill.md reflects wallet-apps API sign_topic/notify_topic fields in GET/POST/PUT documentation, notifications.ntfy_topic removed from settings keys, ntfy note updated to reference per-wallet topics</done>
</task>

</tasks>

<verification>
1. `grep 'sign_topic' skills/admin.skill.md` returns multiple matches (GET response, POST body, PUT body)
2. `grep 'notify_topic' skills/admin.skill.md` returns multiple matches
3. `grep 'notifications.ntfy_topic' skills/admin.skill.md` returns zero matches
4. The skill file includes the security notice: "AI agents must NEVER request the master password"
</verification>

<success_criteria>
- admin.skill.md GET /v1/admin/wallet-apps response includes sign_topic and notify_topic fields with descriptions
- admin.skill.md POST /v1/admin/wallet-apps documents optional sign_topic and notify_topic in request body
- admin.skill.md PUT /v1/admin/wallet-apps/{id} documents sign_topic and notify_topic in request body
- admin.skill.md settings keys list no longer includes notifications.ntfy_topic
- admin.skill.md ntfy note references per-wallet topics in Human Wallet Apps
</success_criteria>

<output>
After completion, create `.planning/phases/303-admin-ui-skill-files/303-02-SUMMARY.md`
</output>
