---
phase: 453-skills-cleanup-admin-manual
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/admin.skill.md
  - skills/setup.skill.md
  - skills/wallet.skill.md
  - skills/transactions.skill.md
  - skills/policies.skill.md
  - skills/actions.skill.md
  - skills/external-actions.skill.md
  - skills/erc8004.skill.md
  - skills/erc8128.skill.md
  - packages/skills/src/registry.ts
  - packages/skills/scripts/sync-skills.mjs
  - packages/skills/src/openclaw.ts
autonomous: true
requirements:
  - SKL-01
  - SKL-02
  - SKL-03
  - SKL-04
  - SKL-05
  - SKL-06
  - SKL-07
  - SKL-08
  - SKL-09
  - SKL-10
  - SKL-11
  - SKL-12
must_haves:
  truths:
    - "skills/admin.skill.md 와 skills/setup.skill.md 가 삭제되어 존재하지 않는다"
    - "skills/ 내 모든 파일에서 masterAuth 엔드포인트 참조가 0건이다 (보안 안내 문구 제외)"
    - "SKILL_REGISTRY 에서 admin, setup 항목이 제거되었다"
    - "sync-skills.mjs 가 admin/setup 을 복사하지 않는다 (SKILL_REGISTRY 기반이므로 자동)"
    - "openclaw.ts 인스톨러에서 WAIAAS_MASTER_PASSWORD 출력이 없다"
  artifacts:
    - path: "skills/wallet.skill.md"
      provides: "sessionAuth 전용 지갑 스킬"
    - path: "skills/policies.skill.md"
      provides: "sessionAuth 전용 정책 조회 스킬"
    - path: "skills/actions.skill.md"
      provides: "sessionAuth 전용 액션 실행 스킬"
    - path: "skills/external-actions.skill.md"
      provides: "sessionAuth 전용 외부 액션 스킬"
    - path: "skills/erc8004.skill.md"
      provides: "sessionAuth 전용 ERC-8004 스킬"
    - path: "skills/erc8128.skill.md"
      provides: "sessionAuth 전용 ERC-8128 스킬"
    - path: "packages/skills/src/registry.ts"
      provides: "admin/setup 제거된 레지스트리"
    - path: "packages/skills/src/openclaw.ts"
      provides: "MASTER_PASSWORD 미출력 인스톨러"
  key_links:
    - from: "packages/skills/src/registry.ts"
      to: "packages/skills/scripts/sync-skills.mjs"
      via: "SKILL_REGISTRY drives sync"
      pattern: "SKILL_REGISTRY"
    - from: "packages/skills/scripts/sync-skills.mjs"
      to: "skills/*.skill.md"
      via: "copies from root skills/"
      pattern: "readdirSync.*skill\\.md"
    - from: "packages/skills/src/openclaw.ts"
      to: "console output"
      via: "WAIAAS_MASTER_PASSWORD removal"
      pattern: "WAIAAS_MASTER_PASSWORD"
---

<objective>
skills/ 에서 admin.skill.md 와 setup.skill.md 를 삭제하고, 나머지 7개 스킬 파일에서 masterAuth 콘텐츠를 제거하여 sessionAuth 전용으로 만든다. SKILL_REGISTRY 와 openclaw 인스톨러도 업데이트한다.

Purpose: AI 에이전트 스킬 파일이 에이전트가 실제로 사용할 수 있는 sessionAuth 엔드포인트만 안내하도록 정리한다. masterAuth 콘텐츠는 Plan 01 의 docs/admin-manual/ 로 이전되었다.
Output: 2 파일 삭제 + 7 스킬 파일 수정 + registry.ts + openclaw.ts 수정
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin.skill.md + setup.skill.md 삭제 및 레지스트리 업데이트</name>
  <files>
    skills/admin.skill.md
    skills/setup.skill.md
    packages/skills/src/registry.ts
    packages/skills/src/openclaw.ts
  </files>
  <read_first>
    skills/admin.skill.md
    skills/setup.skill.md
    packages/skills/src/registry.ts
    packages/skills/src/openclaw.ts
    packages/skills/scripts/sync-skills.mjs
  </read_first>
  <action>
1. **삭제**: `git rm skills/admin.skill.md skills/setup.skill.md` 로 두 파일을 삭제한다.

2. **registry.ts 수정**: `SKILL_REGISTRY` 배열에서 `admin` 과 `setup` 항목 2개를 제거한다. 결과적으로 배열에 남는 항목: quickstart, wallet, transactions, policies, actions, x402 (6개). 나머지 스킬(nft, polymarket, rpc-proxy, session-recovery, external-actions, erc8004, erc8128)은 레지스트리에 없었으므로 변동 없다.

수정 후 SKILL_REGISTRY:
```typescript
export const SKILL_REGISTRY: readonly SkillEntry[] = [
  {
    name: "quickstart",
    filename: "quickstart.skill.md",
    description: "End-to-end quickset: create wallet, session, check balance, send first transfer",
  },
  {
    name: "wallet",
    filename: "wallet.skill.md",
    description: "Wallet queries, asset balances, session info, token list",
  },
  {
    name: "transactions",
    filename: "transactions.skill.md",
    description: "All 5 transaction types (TRANSFER, TOKEN_TRANSFER, CONTRACT_CALL, APPROVE, BATCH) with lifecycle management",
  },
  {
    name: "policies",
    filename: "policies.skill.md",
    description: "Policy queries: view applied spending limits, whitelists, time restrictions",
  },
  {
    name: "actions",
    filename: "actions.skill.md",
    description: "Action Provider framework: list providers, execute DeFi actions through the 6-stage transaction pipeline",
  },
  {
    name: "x402",
    filename: "x402.skill.md",
    description: "x402 auto-payment protocol: fetch URLs with automatic cryptocurrency payments",
  },
] as const;
```

3. **openclaw.ts 수정**: `installOpenClawSkills` 함수 마지막의 console.log 에서 `WAIAAS_MASTER_PASSWORD` 줄을 제거한다. 수정 후 env 블록:
```
  {
    "skills": {
      "entries": {
        "waiaas-quickstart": {
          "env": {
            "WAIAAS_BASE_URL": "http://localhost:3100",
            "WAIAAS_SESSION_TOKEN": "<your-session-token>"
          }
        }
      }
    }
  }
```

4. **sync-skills.mjs 는 수정 불필요**: 이 스크립트는 `readdirSync(rootSkillsDir).filter(f => f.endsWith('.skill.md'))` 로 skills/ 디렉토리를 직접 읽으므로, admin.skill.md 와 setup.skill.md 가 삭제되면 자동으로 복사 대상에서 제외된다. SKILL_REGISTRY 는 인스톨러(agent-skills, claude-code, openclaw)만 사용한다.
  </action>
  <acceptance_criteria>
    - `test ! -f skills/admin.skill.md && echo "DELETED"` 출력이 "DELETED"
    - `test ! -f skills/setup.skill.md && echo "DELETED"` 출력이 "DELETED"
    - `grep -c '"admin"' packages/skills/src/registry.ts` 가 0
    - `grep -c '"setup"' packages/skills/src/registry.ts` 가 0
    - `grep -c "WAIAAS_MASTER_PASSWORD" packages/skills/src/openclaw.ts` 가 0
    - `grep -c "quickstart" packages/skills/src/registry.ts` 가 1 이상
    - `grep -c "WAIAAS_SESSION_TOKEN" packages/skills/src/openclaw.ts` 가 1 이상
  </acceptance_criteria>
  <verify>
    <automated>test ! -f skills/admin.skill.md && test ! -f skills/setup.skill.md && ! grep -q '"admin"' packages/skills/src/registry.ts && ! grep -q "WAIAAS_MASTER_PASSWORD" packages/skills/src/openclaw.ts && echo "PASS"</automated>
  </verify>
  <done>admin.skill.md 와 setup.skill.md 가 삭제되고, SKILL_REGISTRY 에서 admin/setup 항목이 제거되고, openclaw 인스톨러에서 WAIAAS_MASTER_PASSWORD 출력이 제거된다</done>
</task>

<task type="auto">
  <name>Task 2: 7개 스킬 파일에서 masterAuth 콘텐츠 제거</name>
  <files>
    skills/wallet.skill.md
    skills/transactions.skill.md
    skills/policies.skill.md
    skills/actions.skill.md
    skills/external-actions.skill.md
    skills/erc8004.skill.md
    skills/erc8128.skill.md
  </files>
  <read_first>
    skills/wallet.skill.md
    skills/transactions.skill.md
    skills/policies.skill.md
    skills/actions.skill.md
    skills/external-actions.skill.md
    skills/erc8004.skill.md
    skills/erc8128.skill.md
  </read_first>
  <action>
각 스킬 파일을 읽고 masterAuth(X-Master-Password) 엔드포인트/섹션을 제거한다. "AI agents must NEVER request the master password" 보안 안내 문구는 유지한다.

**wallet.skill.md** (SKL-03):
- 제거 대상: "Operator only (masterAuth)" 권한 섹션의 상세 목록, "1. Wallet CRUD" 섹션 전체 (POST/GET/PUT/DELETE /v1/wallets 중 masterAuth 부분), 세션 CRUD (POST/GET/DELETE /v1/sessions 중 masterAuth 부분), MCP 토큰 관리, 토큰 레지스트리 POST/DELETE, Owner 설정, WalletConnect 페어링 관리, Smart Account Provider 설정, 관리자용 NFT 조회
- 유지 대상: Agent(sessionAuth) 권한 목록, GET /v1/wallet/balance, GET /v1/wallet/address, GET /v1/wallet/nonce, GET /v1/connect-info, GET /v1/tokens (조회), Amount Units 설명
- 파일 상단 description 을 "Wallet queries, asset balances, session info, token list" 로 변경

**transactions.skill.md** (SKL-04):
- 제거 대상: masterAuth 관련 정책 사전 설정 안내 (예: "Admin 사전 설정" 문구, masterAuth 로 정책 생성하라는 안내)
- 유지 대상: 5개 트랜잭션 타입 전체 (TRANSFER, TOKEN_TRANSFER, CONTRACT_CALL, APPROVE, BATCH), amount/humanAmount 설명, 트랜잭션 조회, 라이프사이클

**policies.skill.md** (SKL-05):
- 제거 대상: POST /v1/policies (생성), PUT /v1/policies/:id (수정), DELETE /v1/policies/:id (삭제), "Admin (masterAuth)" 권한 섹션, 정책 CRUD curl 예시 (masterAuth 헤더 사용하는 것들)
- 유지 대상: GET /v1/policies (sessionAuth 조회), 16개 정책 타입 설명 (읽기 참조용), "Agent (sessionAuth)" 권한 목록
- 파일 상단 description 을 "Policy queries: view applied spending limits, whitelists, time restrictions, and access controls" 로 변경

**actions.skill.md** (SKL-06):
- 제거 대상: "Admin (masterAuth -- prerequisite)" 섹션 (Provider 설정, API 키 등록, CONTRACT_WHITELIST 정책 설정 안내)
- 유지 대상: 모든 sessionAuth 액션 실행/조회 엔드포인트 (POST /v1/actions/:provider/:action, GET /v1/actions, GET /v1/actions/:provider/actions), Amount Units, 각 Provider별 액션 파라미터

**external-actions.skill.md** (SKL-07):
- 제거 대상: "Admin (masterAuth)" 권한 섹션, Credential CRUD (POST/DELETE/PUT /v1/wallets/:id/credentials, /v1/admin/credentials), Venue/Category 정책 생성 안내
- 유지 대상: ResolvedAction 3-Kind, Signing Schemes 7 Types, 액션 실행 POST /v1/actions/:provider/:action, 액션 조회 GET, Credential 메타데이터 조회 GET /v1/wallets/:id/credentials (sessionAuth)

**erc8004.skill.md** (SKL-08):
- 제거 대상: "Admin (masterAuth -- prerequisite)" 섹션 (Provider 활성화, 레지스트리 주소 설정, REPUTATION_THRESHOLD 정책 생성)
- 유지 대상: 4개 GET 엔드포인트 (agent identity, reputation, validation), 7개 write actions (register, set wallet, set URI, set metadata, give/revoke feedback, request validation) — 이들은 sessionAuth 로 실행

**erc8128.skill.md** (SKL-09):
- 제거 대상: "Admin (masterAuth -- prerequisite)" 섹션 (ERC8128_ALLOWED_DOMAINS 정책, 기능 활성화 설정, 프리셋/TTL 설정)
- 유지 대상: POST /v1/erc8128/sign, POST /v1/erc8128/verify — sessionAuth 엔드포인트

각 파일에서 제거 후 남은 내용이 자연스럽게 이어지도록 섹션 번호를 재조정한다. "이 설정은 관리자가 Admin UI 또는 docs/admin-manual/ 을 참조하여 사전에 완료해야 합니다" 정도의 한 줄 참조만 남긴다.
  </action>
  <acceptance_criteria>
    - `grep -c "X-Master-Password" skills/wallet.skill.md` 가 0
    - `grep -c "X-Master-Password" skills/policies.skill.md` 가 0
    - `grep -c "X-Master-Password" skills/actions.skill.md` 가 0
    - `grep -c "X-Master-Password" skills/external-actions.skill.md` 가 0
    - `grep -c "X-Master-Password" skills/erc8004.skill.md` 가 0
    - `grep -c "X-Master-Password" skills/erc8128.skill.md` 가 0
    - `grep -c "X-Master-Password" skills/transactions.skill.md` 가 0
    - `grep -c "POST /v1/wallets" skills/wallet.skill.md` 가 0 (CRUD 제거됨)
    - `grep -c "POST /v1/policies" skills/policies.skill.md` 가 0 (CRUD 제거됨)
    - `grep "NEVER request the master password" skills/wallet.skill.md` 가 매치 (보안 안내 유지)
    - `grep "NEVER request the master password" skills/policies.skill.md` 가 매치 (보안 안내 유지)
    - `grep "GET /v1/wallet/balance\|GET /v1/connect-info" skills/wallet.skill.md` 가 매치 (sessionAuth 유지)
    - `grep "GET /v1/policies" skills/policies.skill.md` 가 매치 (조회 유지)
    - `grep "POST /v1/actions" skills/actions.skill.md` 가 매치 (액션 실행 유지)
    - `grep "POST /v1/erc8128/sign" skills/erc8128.skill.md` 가 매치 (서명 유지)
  </acceptance_criteria>
  <verify>
    <automated>grep -rl "X-Master-Password" skills/*.skill.md | wc -l | grep -q "^0$" && echo "PASS: no masterAuth refs in skills" || echo "FAIL: masterAuth refs remain"</automated>
  </verify>
  <done>skills/ 디렉토리 내 모든 파일에서 masterAuth 엔드포인트 참조가 0건이고, sessionAuth 엔드포인트만 남아있다</done>
</task>

</tasks>

<verification>
- `test ! -f skills/admin.skill.md && test ! -f skills/setup.skill.md` (두 파일 삭제됨)
- `grep -rl "X-Master-Password" skills/*.skill.md | wc -l` 가 0 (masterAuth 참조 없음)
- `grep -c '"admin"' packages/skills/src/registry.ts` 가 0
- `grep -c "WAIAAS_MASTER_PASSWORD" packages/skills/src/openclaw.ts` 가 0
- `grep "NEVER request the master password" skills/wallet.skill.md` 가 매치 (보안 안내 유지)
</verification>

<success_criteria>
1. skills/admin.skill.md 와 skills/setup.skill.md 가 삭제된다
2. skills/ 내 모든 파일에서 masterAuth 엔드포인트 참조가 0건이다 (보안 안내 문구 제외)
3. SKILL_REGISTRY 에서 admin/setup 항목이 제거된다
4. sync-skills.mjs 가 admin/setup 을 복사하지 않는다
5. openclaw.ts 에서 WAIAAS_MASTER_PASSWORD 출력이 제거된다
</success_criteria>

<output>
After completion, create `.planning/phases/453-skills-cleanup-admin-manual/453-02-SUMMARY.md`
</output>
