---
phase: 452-document-structure-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/agent-guides/agent-self-setup.md
  - docs/agent-guides/agent-skills-integration.md
  - docs/agent-guides/claude-code-integration.md
  - docs/agent-guides/docker-sidecar-install.md
  - docs/agent-guides/openclaw-integration.md
  - README.md
  - site/index.html
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "docs/agent-guides/ 디렉토리에 5개 가이드 파일이 존재한다"
    - "docs/guides/ 디렉토리가 존재하지 않는다"
    - "README.md 내 docs/guides/ 참조가 0건이다"
    - "site/index.html 내 docs/guides 링크가 docs/agent-guides/ 를 가리킨다"
    - "코드베이스 전체에서 docs/guides/ 참조가 0건이다 (아카이브/planning 제외)"
  artifacts:
    - path: "docs/agent-guides/agent-self-setup.md"
      provides: "Agent self-setup guide"
    - path: "docs/agent-guides/agent-skills-integration.md"
      provides: "Agent skills integration guide"
    - path: "docs/agent-guides/claude-code-integration.md"
      provides: "Claude Code integration guide"
    - path: "docs/agent-guides/docker-sidecar-install.md"
      provides: "Docker sidecar install guide"
    - path: "docs/agent-guides/openclaw-integration.md"
      provides: "OpenClaw integration guide"
  key_links:
    - from: "README.md"
      to: "docs/agent-guides/*.md"
      via: "relative markdown links"
      pattern: "docs/agent-guides/"
    - from: "site/index.html"
      to: "docs/agent-guides/"
      via: "GitHub tree URL"
      pattern: "github.com.*docs/agent-guides"
---

<objective>
docs/guides/ 디렉토리를 docs/agent-guides/ 로 이름 변경하고 코드베이스 전체의 참조를 업데이트한다.

Purpose: 에이전트 가이드와 관리자 매뉴얼의 디렉토리를 분리하기 위한 기반 작업. Phase 453에서 docs/admin-manual/ 을 신설할 때 docs/agent-guides/ 와 대칭을 이룬다.
Output: docs/agent-guides/ 디렉토리 (5개 파일), 업데이트된 README.md 및 site/index.html
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename docs/guides/ to docs/agent-guides/ and update all references</name>
  <files>docs/agent-guides/, README.md, site/index.html</files>
  <read_first>
    - README.md (current docs/guides/ references on lines 136, 218-221)
    - site/index.html (line 1238, GitHub tree link)
    - docs/guides/agent-self-setup.md (verify file exists before move)
  </read_first>
  <action>
1. Rename the directory using git mv:
   ```
   git mv docs/guides docs/agent-guides
   ```
   This moves all 5 files: agent-self-setup.md, agent-skills-integration.md, claude-code-integration.md, docker-sidecar-install.md, openclaw-integration.md

2. Update README.md -- 5 occurrences of `docs/guides/` to `docs/agent-guides/`:
   - Line 136: `(docs/guides/agent-self-setup.md)` -> `(docs/agent-guides/agent-self-setup.md)`
   - Line 218: `(docs/guides/agent-self-setup.md)` -> `(docs/agent-guides/agent-self-setup.md)`
   - Line 219: `(docs/guides/agent-skills-integration.md)` -> `(docs/agent-guides/agent-skills-integration.md)`
   - Line 220: `(docs/guides/claude-code-integration.md)` -> `(docs/agent-guides/claude-code-integration.md)`
   - Line 221: `(docs/guides/openclaw-integration.md)` -> `(docs/agent-guides/openclaw-integration.md)`

3. Update site/index.html -- 1 occurrence on line 1238:
   - `https://github.com/minhoyoo-iotrust/WAIaaS/tree/main/docs/guides` -> `https://github.com/minhoyoo-iotrust/WAIaaS/tree/main/docs/agent-guides`

4. Check docs/deployment.md line 617 for any docs/guides reference -- currently references `/blog/docker-sidecar-install/` (URL path, not filesystem), so no change needed.

Do NOT modify files under internal/objectives/archive/ or .planning/ -- these are historical records.
  </action>
  <verify>
    <automated>test -d docs/agent-guides && test ! -d docs/guides && echo "DIRS_OK" && ls docs/agent-guides/ | wc -l | grep -q 5 && echo "FILES_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/agent-guides/` directory exists with exactly 5 files
    - `docs/guides/` directory does NOT exist
    - `grep -c 'docs/guides/' README.md` returns 0
    - `grep -c 'docs/agent-guides/' README.md` returns 5
    - `grep -c 'docs/guides' site/index.html` returns 0
    - `grep -c 'docs/agent-guides' site/index.html` returns 1
  </acceptance_criteria>
  <done>5개 가이드 파일이 docs/agent-guides/ 로 이동되고 README.md, site/index.html 참조가 업데이트됨</done>
</task>

<task type="auto">
  <name>Task 2: Verify zero remaining docs/guides/ references in active codebase</name>
  <files>(verification only, no file modifications)</files>
  <read_first>
    - README.md (confirm updated)
    - site/index.html (confirm updated)
  </read_first>
  <action>
1. Run a comprehensive grep to find ALL remaining `docs/guides` references in the repo, excluding archive/planning directories:
   ```
   grep -rn 'docs/guides' . --include='*.md' --include='*.html' --include='*.mjs' --include='*.ts' --include='*.js' --include='*.json' --include='*.yml' --include='*.yaml' | grep -v 'internal/objectives/archive/' | grep -v '.planning/' | grep -v 'node_modules/' | grep -v 'internal/objectives/m32-10'
   ```

2. If any results found, fix them. Expected: 0 results.

3. Verify the site build still works (the build.mjs uses filesystem paths and frontmatter slugs, so the rename should be transparent):
   ```
   node site/build.mjs
   ```
   Verify the output includes the agent guides pages.

4. Verify git status shows the rename properly tracked.
  </action>
  <verify>
    <automated>grep -rn 'docs/guides' . --include='*.md' --include='*.html' --include='*.mjs' --include='*.ts' --include='*.js' --include='*.json' --include='*.yml' --include='*.yaml' | grep -v 'internal/objectives/archive/' | grep -v '.planning/' | grep -v 'node_modules/' | grep -v 'internal/objectives/m32-10' | grep -v '.git/' | wc -l | grep -q '^0$' && echo "ZERO_REFS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `docs/guides` in active codebase (excluding archive, .planning, node_modules, m32-10 objective) returns 0 results
    - `node site/build.mjs` completes without error
    - git status shows renamed files (docs/guides/ -> docs/agent-guides/)
  </acceptance_criteria>
  <done>코드베이스 전체에서 docs/guides/ 참조가 0건이고 (아카이브 제외) site 빌드가 정상 작동함</done>
</task>

</tasks>

<verification>
1. `ls docs/agent-guides/` -- 5개 파일 확인
2. `test ! -d docs/guides` -- 기존 디렉토리 삭제 확인
3. `grep -c 'docs/guides/' README.md` -- 0건
4. `grep -c 'docs/agent-guides/' README.md` -- 5건
5. `grep 'docs/guides' site/index.html` -- 0건
6. Comprehensive grep for docs/guides in active codebase -- 0건 (아카이브 제외)
7. `node site/build.mjs` -- 빌드 성공
</verification>

<success_criteria>
- docs/agent-guides/ 에 5개 가이드 파일 존재
- docs/guides/ 디렉토리 부재
- README.md 의 5개 링크가 docs/agent-guides/ 를 가리킴
- site/index.html 의 GitHub 링크가 docs/agent-guides/ 를 가리킴
- 아카이브 외 코드베이스에서 docs/guides/ 참조 0건
- site 빌드 정상 작동
</success_criteria>

<output>
After completion, create `.planning/phases/452-document-structure-rename/452-01-SUMMARY.md`
</output>
