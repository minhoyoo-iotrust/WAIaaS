---
phase: 455-ci-cd-documentation-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - release-please-config.json
  - .release-please-manifest.json
  - turbo.json
  - .github/workflows/release.yml
  - scripts/smoke-test-published.sh
autonomous: true
requirements: [CID-01, CID-02, CID-03, CID-04]

must_haves:
  truths:
    - "release-please-config.json packages 키에 packages/openclaw-plugin 항목이 있다"
    - ".release-please-manifest.json에 packages/openclaw-plugin 초기 버전이 있다"
    - "turbo.json에 openclaw-plugin 빌드 태스크가 정의된다"
    - "release.yml의 publish-check/deploy 잡 PACKAGES 배열에 packages/openclaw-plugin이 포함된다"
    - "smoke-test-published.sh PACKAGES 배열에 packages/openclaw-plugin이 포함된다"
  artifacts:
    - path: "release-please-config.json"
      provides: "release-please openclaw-plugin 패키지 등록"
      contains: "openclaw-plugin"
    - path: ".release-please-manifest.json"
      provides: "openclaw-plugin 초기 버전 앵커"
      contains: "packages/openclaw-plugin"
    - path: "turbo.json"
      provides: "openclaw-plugin 빌드/테스트/린트 태스크"
      contains: "openclaw-plugin"
    - path: ".github/workflows/release.yml"
      provides: "npm publish 파이프라인 포함"
      contains: "packages/openclaw-plugin"
  key_links:
    - from: "release-please-config.json"
      to: "packages/openclaw-plugin/package.json"
      via: "extra-files 배열에 추가"
      pattern: "openclaw-plugin"
    - from: ".github/workflows/release.yml deploy"
      to: "npm registry"
      via: "PACKAGES 배열 순회 → pnpm pack → npm publish"
      pattern: "packages/openclaw-plugin"
---

<objective>
@waiaas/openclaw-plugin 패키지를 release-please, turbo, npm publish 파이프라인에 통합한다.

Purpose: openclaw-plugin이 다른 12개 패키지와 동일한 자동화 릴리즈/퍼블리시 흐름을 따르도록 만든다.
Output: release-please-config.json, .release-please-manifest.json, turbo.json, release.yml, smoke-test-published.sh 수정 완료
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 현재 release-please-config.json extra-files 배열 -->
현재 14개 항목: packages/shared, core, daemon, cli, sdk, mcp, skills, admin, adapters/solana, adapters/evm, wallet-sdk, push-relay, actions + python-sdk/pyproject.toml

<!-- 현재 .release-please-manifest.json -->
{ ".": "2.11.0-rc.22" }

<!-- turbo.json 패턴 - @waiaas/skills#build 처럼 의존성 없는 빌드는 dependsOn: [] 로 선언 -->

<!-- release.yml PACKAGES 배열 - publish-check 잡과 deploy 잡에 동일한 12개 배열이 3군데에 반복됨 -->
구체적으로: publish-check.Copy root README, publish-check.pnpm dry-run, publish-check.workspace verify, deploy.Copy root README, deploy.Publish packages 의 5개 PACKAGES 배열을 모두 업데이트해야 함

<!-- openclaw-plugin package.json scripts -->
build: tsc -p tsconfig.build.json
test: vitest run
test:unit: vitest run
lint: eslint src/
typecheck: tsc --noEmit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: release-please 설정에 openclaw-plugin 등록</name>
  <files>release-please-config.json, .release-please-manifest.json</files>
  <action>
  release-please-config.json 수정:
  extra-files 배열에서 "packages/actions/package.json" 다음 줄에 "packages/openclaw-plugin/package.json" 을 추가한다. python-sdk/pyproject.toml 바로 앞에 위치한다.

  .release-please-manifest.json 수정:
  openclaw-plugin은 별도 컴포넌트가 아닌 루트 패키지의 extra-files로 관리되므로, 초기 앵커 버전을 추가한다:
  {
    ".": "2.11.0-rc.22",
    "packages/openclaw-plugin": "1.0.0"
  }
  openclaw-plugin의 package.json version이 1.0.0이므로 매니페스트도 동일하게 설정한다.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && node -e "const c=JSON.parse(require('fs').readFileSync('release-please-config.json','utf8')); const ef=c.packages['.']['extra-files']; const m=JSON.parse(require('fs').readFileSync('.release-please-manifest.json','utf8')); console.assert(ef.includes('packages/openclaw-plugin/package.json'), 'FAIL: openclaw-plugin not in extra-files'); console.assert('packages/openclaw-plugin' in m, 'FAIL: manifest missing openclaw-plugin'); console.log('OK: release-please config valid');"</automated>
  </verify>
  <done>release-please-config.json의 extra-files 배열에 packages/openclaw-plugin/package.json이 있고, .release-please-manifest.json에 "packages/openclaw-plugin": "1.0.0"이 있다</done>
</task>

<task type="auto">
  <name>Task 2: turbo.json + release.yml + smoke-test 파이프라인 통합</name>
  <files>turbo.json, .github/workflows/release.yml, scripts/smoke-test-published.sh</files>
  <action>
  **turbo.json 수정:**
  기존 @waiaas/skills#build 항목 바로 아래에 openclaw-plugin 전용 빌드 태스크를 추가한다.
  openclaw-plugin은 @waiaas/sdk에 workspace:* 의존성이 있으므로 sdk 빌드 완료 후 실행되어야 한다:
  ```json
  "@waiaas/openclaw-plugin#build": {
    "dependsOn": ["@waiaas/sdk#build"],
    "outputs": ["dist/**"]
  }
  ```

  **release.yml 수정 (5군데 PACKAGES 배열):**
  release.yml 파일 내 PACKAGES 배열이 등장하는 모든 위치(publish-check 잡 3군데 + deploy 잡 2군데 = 5군데)에서 "packages/actions" 다음 줄에 "packages/openclaw-plugin" 을 추가한다.

  또한 release.yml 상단 publish-check 잡의 "Copy root README to packages" 스텝에서 PACKAGES 배열에도 packages/openclaw-plugin을 추가한다.

  deploy 잡 "Deploy summary" 스텝의 npm 패키지 수 "12 packages" → "13 packages"로 업데이트한다.

  **scripts/smoke-test-published.sh 수정:**
  PACKAGES 배열에 "packages/openclaw-plugin" 을 추가한다. 기존 "packages/actions" 다음 줄에 삽입한다.

  smoke-test-published.sh에서 ESM import 검증 섹션이 있다면 @waiaas/openclaw-plugin도 검증 목록에 추가한다 (import { register } from '@waiaas/openclaw-plugin' 패턴).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && node -e "const t=JSON.parse(require('fs').readFileSync('turbo.json','utf8')); console.assert('@waiaas/openclaw-plugin#build' in t.tasks, 'FAIL: turbo missing openclaw-plugin'); const r=require('fs').readFileSync('.github/workflows/release.yml','utf8'); const count=(r.match(/packages\/openclaw-plugin/g)||[]).length; console.assert(count>=5,'FAIL: release.yml needs >=5 occurrences, got '+count); const s=require('fs').readFileSync('scripts/smoke-test-published.sh','utf8'); console.assert(s.includes('packages/openclaw-plugin'),'FAIL: smoke-test missing openclaw-plugin'); console.log('OK: all pipeline configs updated, release.yml count='+count);"</automated>
  </verify>
  <done>turbo.json에 @waiaas/openclaw-plugin#build 태스크가 있고, release.yml의 모든 PACKAGES 배열에 packages/openclaw-plugin이 포함되고, smoke-test-published.sh에 packages/openclaw-plugin이 있다</done>
</task>

</tasks>

<verification>
pnpm turbo run build --filter=@waiaas/openclaw-plugin が通ること (turbo 의존성 그래프 검증)
node -e "JSON.parse(require('fs').readFileSync('release-please-config.json','utf8'))" で JSON parse エラーがないこと
</verification>

<success_criteria>
1. release-please-config.json extra-files에 packages/openclaw-plugin/package.json이 있다
2. .release-please-manifest.json에 "packages/openclaw-plugin": "1.0.0"이 있다
3. turbo.json에 @waiaas/openclaw-plugin#build 태스크가 @waiaas/sdk#build 의존성과 함께 있다
4. release.yml의 모든 PACKAGES 배열(5군데)에 packages/openclaw-plugin이 있다
5. smoke-test-published.sh PACKAGES 배열에 packages/openclaw-plugin이 있다
</success_criteria>

<output>
After completion, create `.planning/phases/455-ci-cd-documentation-seo/455-01-SUMMARY.md`
</output>
