---
phase: 469-admin-ui-radio-group
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/pages/human-wallet-apps.tsx
  - packages/admin/src/__tests__/human-wallet-apps.test.tsx
autonomous: false
requirements: [ADM-01, ADM-02, ADM-03, ADM-04]

must_haves:
  truths:
    - "같은 wallet_type의 앱이 시각적 그룹으로 묶여 표시된다"
    - "서명 컨트롤이 라디오 버튼이며 그룹 내 하나만 선택 가능하다"
    - "'없음(None)' 라디오 옵션 선택으로 해당 wallet_type의 모든 앱을 signing_enabled=0으로 설정할 수 있다"
    - "wallet_type에 앱이 1개만 있으면 라디오가 자동 선택 상태로 표시된다"
  artifacts:
    - path: "packages/admin/src/pages/human-wallet-apps.tsx"
      provides: "wallet_type 그룹 레이아웃 + 서명 라디오 버튼"
      contains: "groupBy wallet_type"
    - path: "packages/admin/src/__tests__/human-wallet-apps.test.tsx"
      provides: "라디오 그룹 UI 테스트"
      contains: "radio"
  key_links:
    - from: "packages/admin/src/pages/human-wallet-apps.tsx"
      to: "/v1/admin/wallet-apps/{id}"
      via: "api.PUT with signing_enabled toggle"
      pattern: "api\\.PUT.*wallet-apps.*signing_enabled"
---

<objective>
Human Wallet Apps 페이지를 wallet_type 기반 그룹 레이아웃 + 서명 라디오 버튼으로 전환한다.

Purpose: 운영자가 wallet_type 그룹 내에서 서명 앱을 명시적으로 선택할 수 있게 한다. 기존 체크박스를 라디오 버튼으로 변경하여 한 그룹에서 하나만 선택 가능하게 하고, "None" 옵션으로 모든 앱의 서명을 비활성화할 수 있다.
Output: 수정된 human-wallet-apps.tsx + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/467-db-migration-backend-service/467-02-SUMMARY.md
@.planning/phases/468-signrequestbuilder-query-transition/468-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/admin/src/api/types.aliases.ts:
```typescript
export type WalletApp = components['schemas']['WalletApp'];
// WalletApp has: id, name, display_name, wallet_type, signing_enabled, alerts_enabled,
// sign_topic, notify_topic, subscription_token, push_relay_url, used_by, created_at, updated_at
```

From packages/admin/src/pages/human-wallet-apps.tsx (current):
```typescript
// handleToggle calls api.PUT('/v1/admin/wallet-apps/{id}', { body: { signing_enabled: !app.signing_enabled } })
// Backend WalletAppService.update() handles exclusive toggle atomically (Phase 467)
// When signing_enabled=true is sent, backend auto-disables same wallet_type peers
```

API contract (PUT /v1/admin/wallet-apps/{id}):
- Body: { signing_enabled?: boolean, alerts_enabled?: boolean, ... }
- When signing_enabled=true: backend atomically disables same wallet_type peers (SVC-01)
- When signing_enabled=false: just disables this app (no side effects on peers)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wallet_type 그룹 레이아웃 + 서명 라디오 버튼 구현</name>
  <files>packages/admin/src/pages/human-wallet-apps.tsx, packages/admin/src/__tests__/human-wallet-apps.test.tsx</files>
  <behavior>
    - T-ADM-01: 같은 wallet_type 앱이 하나의 그룹 섹션 안에 표시된다 (그룹 헤더에 wallet_type 표시)
    - T-ADM-02: 그룹 내 서명 컨트롤이 라디오 버튼이다 (type="radio", name=wallet_type)
    - T-ADM-03: "None" 라디오 옵션이 존재하고 선택 시 signing_enabled=false PUT 호출
    - T-ADM-04: wallet_type에 앱이 1개만 있으면 라디오가 자동 선택(checked) 상태
    - T-ADM-05: 라디오 선택 변경 시 api.PUT 호출로 signing_enabled 토글 (기존 handleToggle 재사용)
    - T-ADM-06: 다른 wallet_type 앱(dcent vs custom)은 별도 그룹으로 분리 표시
  </behavior>
  <action>
    1. **그룹핑 로직 추가** (per ADM-01): apps.value를 wallet_type으로 그룹핑하는 유틸 함수 작성. `Map<string, WalletAppApi[]>` 반환. wallet_type이 없는 앱은 name을 wallet_type으로 사용 (기존 DB 동작과 동일).

    2. **서명 라디오 핸들러** (per ADM-02, ADM-03): 기존 `handleToggle(app, 'signing_enabled')` 체크박스 대신 라디오 버튼 onChange 핸들러 작성.
       - 라디오 value = app.id (선택할 앱) 또는 "none" (모든 앱 비활성화)
       - "none" 선택 시: 현재 signing_enabled=true인 앱의 signing_enabled=false를 PUT
       - 앱 선택 시: 해당 앱의 signing_enabled=true를 PUT (backend가 peer 자동 비활성화 처리)

    3. **렌더링 변경** (per ADM-01, ADM-02): "Registered Apps" 섹션 내부를 그룹별 렌더링으로 변경.
       - 각 그룹: wallet_type 헤더(h4) + "Signing Primary" 라디오 그룹 + 앱 카드 목록
       - 라디오 그룹 구조:
         ```
         <div class="signing-radio-group">
           <label><input type="radio" name="signing-{walletType}" value="none" /> None</label>
           <label><input type="radio" name="signing-{walletType}" value="{app.id}" /> {app.display_name}</label>
         </div>
         ```
       - 라디오의 checked 상태: signing_enabled=true인 앱의 id, 없으면 "none"
       - 앱 카드 내의 기존 signing_enabled 체크박스 제거. alerts_enabled 체크박스는 유지.

    4. **자동 선택** (per ADM-04): 앱이 1개일 때 signing_enabled=true이면 라디오가 선택 상태, false이면 "None"이 선택 상태. 단일 앱 그룹에서도 "None" 선택이 가능해야 한다.

    5. **테스트 업데이트**: 기존 T-HWUI-04(signing toggle calls PUT) 테스트를 라디오 기반으로 변경. 새 테스트 추가:
       - T-ADM-01: mockApps에 같은 wallet_type 앱 2개 + 다른 wallet_type 앱 1개 -> 2개 그룹 렌더링 확인
       - T-ADM-02: 라디오 input[type="radio"] 존재 확인
       - T-ADM-03: "None" 라디오 선택 시 signing_enabled=false PUT 호출
       - T-ADM-04: 단일 앱 그룹에서 signing_enabled=true인 앱의 라디오가 checked
       - T-ADM-05: 라디오 변경 시 signing_enabled=true PUT 호출 확인
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/admin/src/__tests__/human-wallet-apps.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>
    - wallet_type별 그룹 헤더가 렌더링된다
    - 서명 컨트롤이 라디오 버튼이다 (체크박스 아님)
    - "None" 라디오 옵션이 존재한다
    - 라디오 변경 시 api.PUT이 올바른 signing_enabled 값으로 호출된다
    - 기존 alerts_enabled 토글, test notification, test sign, subscription token, push relay URL 기능이 그대로 동작한다
    - 모든 테스트 통과
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Admin UI 시각적 검증</name>
  <files>N/A</files>
  <action>
    운영자가 Human Wallet Apps 페이지에서 wallet_type 그룹 레이아웃과 서명 라디오 버튼이 올바르게 동작하는지 시각적으로 확인한다.
    1. 데몬 + Admin UI 실행 (`pnpm dev` 또는 기존 방법)
    2. Admin UI > Channels > Human Wallet Apps 페이지 접속
    3. 확인 항목:
       a. 같은 wallet_type의 앱이 하나의 그룹으로 묶여 표시되는가
       b. 서명 컨트롤이 라디오 버튼인가 (체크박스가 아닌지 확인)
       c. "None" 라디오 옵션이 보이는가
       d. 라디오 선택 변경 시 토스트 메시지가 표시되는가
       e. 다른 그룹의 라디오가 독립적으로 동작하는가
       f. alerts_enabled 토글, Test Notify/Sign 버튼이 기존대로 동작하는가
  </action>
  <verify>User confirms visual and functional correctness</verify>
  <done>운영자가 "approved" 응답</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run packages/admin/src/__tests__/human-wallet-apps.test.tsx` -- 모든 테스트 통과
2. `pnpm turbo run typecheck --filter=@waiaas/admin` -- 타입 에러 없음
3. Admin UI에서 Human Wallet Apps 페이지가 정상 렌더링
</verification>

<success_criteria>
1. Human Wallet Apps 페이지에서 같은 wallet_type의 앱이 시각적 그룹으로 묶여 표시된다 (ADM-01)
2. 서명 컨트롤이 라디오 버튼이며 그룹 내 하나만 선택 가능하다 (ADM-02)
3. "없음" 라디오 옵션 선택으로 해당 wallet_type의 모든 앱을 signing_enabled=0으로 설정할 수 있다 (ADM-03)
4. wallet_type에 앱이 1개만 있으면 라디오가 자동 선택 상태로 표시된다 (ADM-04)
</success_criteria>

<output>
After completion, create `.planning/phases/469-admin-ui-radio-group/469-01-SUMMARY.md`
</output>
