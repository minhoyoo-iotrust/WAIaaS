---
phase: 468-signrequestbuilder-query-transition
plan: 01
type: execute
wave: 1
depends_on: [467-02]
files_modified:
  - packages/daemon/src/services/signing-sdk/sign-request-builder.ts
  - packages/daemon/src/__tests__/sign-request-builder.test.ts
  - packages/daemon/src/__tests__/signing-sdk-e2e.test.ts
  - packages/daemon/src/infrastructure/settings/setting-keys.ts
autonomous: true
requirements: [SIG-01, SIG-02, TST-03]

must_haves:
  truths:
    - "SignRequestBuilder가 wallet_type + signing_enabled=1 조건으로 서명 대상 앱을 조회한다"
    - "signing_sdk.preferred_wallet 설정이 deprecated 처리되어 SignRequestBuilder에서 참조하지 않는다"
    - "APPROVAL 티어 TX 생성 시 signing_enabled=1인 앱의 Push Relay URL로 요청이 전송된다"
  artifacts:
    - path: "packages/daemon/src/services/signing-sdk/sign-request-builder.ts"
      provides: "wallet_type + signing_enabled=1 기반 서명 앱 조회"
      contains: "signing_enabled = 1"
    - path: "packages/daemon/src/__tests__/sign-request-builder.test.ts"
      provides: "SignRequestBuilder 단위 테스트 (wallet_type 기반)"
    - path: "packages/daemon/src/__tests__/signing-sdk-e2e.test.ts"
      provides: "통합 테스트 (signing_enabled=1 앱의 Push Relay URL)"
  key_links:
    - from: "packages/daemon/src/services/signing-sdk/sign-request-builder.ts"
      to: "wallet_apps table"
      via: "SELECT ... WHERE wallet_type = ? AND signing_enabled = 1"
      pattern: "wallet_type.*signing_enabled"
    - from: "packages/daemon/src/services/signing-sdk/approval-channel-router.ts"
      to: "packages/daemon/src/services/signing-sdk/sign-request-builder.ts"
      via: "enrichedParams.walletName = row.wallet_type"
      pattern: "walletName.*wallet_type"
---

<objective>
SignRequestBuilder를 name 기반 3-쿼리 패턴에서 wallet_type + signing_enabled=1 단일 쿼리 패턴으로 전환한다.

Purpose: 서명 대상 앱 조회가 preferred_wallet 설정에 의존하지 않고, DB의 signing_enabled 컬럼으로 직접 조회하여 정확한 signing primary를 사용하게 한다.
Output: 수정된 SignRequestBuilder + 업데이트된 테스트 + 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/467-db-migration-backend-service/467-02-SUMMARY.md

<interfaces>
<!-- ApprovalChannelRouter already enriches walletName with wallet_type from wallets table -->
<!-- Line 89: walletName: row.wallet_type || params.walletName -->

From packages/daemon/src/services/signing-sdk/sign-request-builder.ts:
```typescript
export interface BuildRequestParams {
  txId: string;
  chain: ChainType;
  network: string;
  type: string;
  from: string;
  to: string;
  amount?: string;
  symbol?: string;
  policyTier: 'APPROVAL' | 'DELAY';
  walletName?: string;
  approvalType?: 'SIWE' | 'EIP712';
  signerAddress?: string;
}
```

From packages/daemon/src/services/signing-sdk/wallet-app-service.ts:
```typescript
export interface WalletApp {
  id: string;
  name: string;
  displayName: string;
  walletType: string;
  signingEnabled: boolean;
  alertsEnabled: boolean;
  signTopic: string | null;
  notifyTopic: string | null;
  subscriptionToken: string | null;
  pushRelayUrl: string | null;
  createdAt: number;
  updatedAt: number;
}
```

DB wallet_apps table columns:
- id, name, display_name, wallet_type, signing_enabled, alerts_enabled, sign_topic, notify_topic, subscription_token, push_relay_url, created_at, updated_at
- Partial unique index: wallet_apps(wallet_type) WHERE signing_enabled = 1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SignRequestBuilder wallet_type 기반 쿼리 전환 + preferred_wallet deprecated</name>
  <files>
    packages/daemon/src/services/signing-sdk/sign-request-builder.ts
    packages/daemon/src/__tests__/sign-request-builder.test.ts
    packages/daemon/src/infrastructure/settings/setting-keys.ts
  </files>
  <behavior>
    - Test: walletName 없이 buildRequest 호출 시 wallet_apps에서 signing_enabled=1인 앱을 자동으로 찾아 사용한다
    - Test: walletName이 전달되면 해당 값을 wallet_type으로 취급하여 signing_enabled=1인 앱을 조회한다
    - Test: signing_enabled=1인 앱이 없으면 WALLET_NOT_REGISTERED 에러를 던진다
    - Test: push_relay_url이 signing_enabled=1인 앱의 DB 값에서 조회된다 (name 기반이 아님)
    - Test: subscription_token이 signing_enabled=1인 앱의 DB 값에서 requestTopic으로 사용된다
    - Test: preferred_wallet 설정이 더 이상 참조되지 않는다 (설정에 값이 있어도 무시)
  </behavior>
  <action>
    **sign-request-builder.ts 수정 (SIG-01, SIG-02):**

    1. `buildRequest()` 메서드의 "2. Determine wallet name" 섹션 전면 변경:
       - `this.settings.get('signing_sdk.preferred_wallet')` 호출을 완전히 제거한다
       - `walletName`이 없으면 (params.walletName 미전달) DB에서 `signing_enabled = 1`인 아무 앱을 찾는다:
         ```sql
         SELECT name, wallet_type, push_relay_url, subscription_token
         FROM wallet_apps WHERE signing_enabled = 1 LIMIT 1
         ```
       - `walletName`이 있으면 (ApprovalChannelRouter가 wallet_type으로 enriched) 해당 wallet_type의 signing primary를 찾는다:
         ```sql
         SELECT name, wallet_type, push_relay_url, subscription_token
         FROM wallet_apps WHERE wallet_type = ? AND signing_enabled = 1
         ```
       - 결과가 없으면 `throw new WAIaaSError('WALLET_NOT_REGISTERED', { message: 'No signing-enabled wallet app found' })`

    2. "3. Verify wallet exists" 부분: `this.walletLinkRegistry.getWallet(walletName)` 호출은 유지 — 앱 name으로 WalletLinkConfig 조회

    3. push_relay_url 조회 (기존 step 8): 별도 쿼리 제거 — step 2에서 이미 가져온 값 사용

    4. subscription_token 조회 (기존 step 11): 별도 쿼리 제거 — step 2에서 이미 가져온 값 사용

    5. 결과: 3개의 `WHERE name = ?` 쿼리가 1개의 `WHERE wallet_type = ? AND signing_enabled = 1` 쿼리로 통합

    **setting-keys.ts 수정 (SIG-02):**

    6. `signing_sdk.preferred_wallet` 설정의 description을 deprecated 표시로 변경:
       `description: '[DEPRECATED] No longer used by SignRequestBuilder. Use signing_enabled column in wallet_apps.'`

    **sign-request-builder.test.ts 수정:**

    7. 기존 mock 패턴 변경:
       - `createMockSettingsService`에서 `'signing_sdk.preferred_wallet': 'dcent'` 항목 제거
       - 대신 mock sqlite를 기본으로 사용하여 signing_enabled=1인 앱 행을 반환하도록 구성
       - mock sqlite prepare().get() 패턴:
         - `signing_enabled = 1` 쿼리 → `{ name: 'dcent', wallet_type: 'dcent', push_relay_url: 'https://relay.example.com', subscription_token: 'tok-123' }` 반환
         - `pending_approvals` 쿼리 → 기존 유지
         - `owner_address` 쿼리 → 기존 유지

    8. 테스트 5번 ("No preferred_wallet + no walletName -> error") 변경:
       - 제목: "throws error when no signing-enabled wallet app found"
       - sqlite mock이 signing_enabled=1 쿼리에 undefined 반환하도록 설정
       - 에러 코드 WALLET_NOT_REGISTERED 확인

    9. 기존 push_relay_url 테스트 (7번) 업데이트: signing_enabled=1 조회 결과에서 push_relay_url 가져오는지 확인

    10. preferred_wallet 무시 테스트 추가: settings에 preferred_wallet='some-wallet' 설정하고, signing_enabled=1인 앱이 'dcent'이면 'dcent'가 사용되는지 확인
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/sign-request-builder.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - SignRequestBuilder가 preferred_wallet 설정을 참조하지 않는다
    - wallet_type + signing_enabled=1 기반 단일 쿼리로 서명 앱을 조회한다
    - 3개 분리 쿼리(name 기반 push_relay_url, subscription_token)가 1개 쿼리로 통합되었다
    - 모든 단위 테스트 통과
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: APPROVAL 티어 TX 통합 테스트 (TST-03)</name>
  <files>
    packages/daemon/src/__tests__/signing-sdk-e2e.test.ts
  </files>
  <behavior>
    - Test: APPROVAL 티어 TX 생성 시 signing_enabled=1인 앱의 push_relay_url로 Push Relay HTTP POST가 전송된다
    - Test: signing_enabled=0인 앱의 push_relay_url은 사용되지 않는다
    - Test: preferred_wallet 설정이 있어도 signing_enabled=1 앱이 사용된다
  </behavior>
  <action>
    **signing-sdk-e2e.test.ts 수정 (TST-03):**

    1. 기존 `createMockSettingsService`에서 `preferred_wallet` 기본값을 빈 문자열로 변경 (deprecated 확인)

    2. 기존 mock sqlite를 signing_enabled 기반으로 업데이트:
       - `signing_enabled = 1` 쿼리 → `{ name: 'dcent', wallet_type: 'dcent', push_relay_url: 'https://relay.test.com', subscription_token: 'sub-tok' }` 반환

    3. 새로운 통합 테스트 추가 — "routes APPROVAL tier TX to signing_enabled=1 app push relay":
       - SignRequestBuilder에 sqlite mock 주입 (signing_enabled=1인 앱 데이터)
       - PushRelaySigningChannel.sendRequest() 호출
       - fetchMock에서 POST URL에 signing_enabled=1 앱의 push_relay_url이 사용되었는지 검증
       - requestTopic이 subscription_token 값인지 검증

    4. 새로운 통합 테스트 추가 — "ignores preferred_wallet setting, uses signing_enabled=1 app":
       - settings에 preferred_wallet='old-wallet' 설정
       - sqlite mock에 signing_enabled=1인 앱이 'dcent' (name='dcent')
       - buildRequest 호출 결과 walletLinkRegistry.getWallet이 'dcent'로 호출되는지 확인

    5. 기존 e2e 테스트의 mock 패턴도 signing_enabled 기반으로 일관되게 업데이트
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/signing-sdk-e2e.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - APPROVAL 티어 TX에서 signing_enabled=1인 앱의 Push Relay URL이 사용되는 통합 테스트 통과
    - preferred_wallet 설정 무시 통합 테스트 통과
    - 기존 e2e 테스트 전체 통과
  </done>
</task>

</tasks>

<verification>
모든 signing-sdk 관련 테스트 통과:
```bash
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/sign-request-builder.test.ts packages/daemon/src/__tests__/signing-sdk-e2e.test.ts packages/daemon/src/__tests__/approval-channel-router.test.ts --reporter=verbose 2>&1 | tail -50
```

SignRequestBuilder에 preferred_wallet 참조 없음:
```bash
grep -n "preferred_wallet" packages/daemon/src/services/signing-sdk/sign-request-builder.ts
# 출력 없어야 함
```

signing_enabled 기반 쿼리 존재:
```bash
grep -n "signing_enabled" packages/daemon/src/services/signing-sdk/sign-request-builder.ts
# signing_enabled = 1 쿼리가 있어야 함
```
</verification>

<success_criteria>
1. SignRequestBuilder가 wallet_type + signing_enabled=1 조건으로 서명 대상 앱을 조회한다 (SIG-01)
2. signing_sdk.preferred_wallet 설정이 deprecated 처리되어 SignRequestBuilder에서 참조하지 않는다 (SIG-02)
3. APPROVAL 티어 TX 생성 시 signing_enabled=1인 앱의 Push Relay URL로 요청이 전송되는 통합 테스트가 통과한다 (TST-03)
</success_criteria>

<output>
After completion, create `.planning/phases/468-signrequestbuilder-query-transition/468-01-SUMMARY.md`
</output>
