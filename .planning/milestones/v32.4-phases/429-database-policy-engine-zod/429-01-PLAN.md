---
phase: 429-database-policy-engine-zod
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/schemas/policy.schema.ts
  - packages/core/src/errors/error-codes.ts
  - packages/core/src/i18n/en.ts
  - packages/core/src/i18n/ko.ts
  - packages/core/src/__tests__/errors.test.ts
  - packages/core/src/__tests__/i18n.test.ts
  - packages/core/src/__tests__/package-exports.test.ts
autonomous: true
requirements: [ZOD-03, ZOD-04, ZOD-05, ZOD-06, ZOD-07, ZOD-08, ZOD-09]

must_haves:
  truths:
    - "POLICY_RULES_SCHEMAS에 20개 정책 타입 모두 매핑이 존재한다 (기존 13 + 신규 7)"
    - "POLICY_RULES_CORRUPT 에러 코드가 ERROR_CODES에 등록되어 있다"
    - "Lending/Perp/Venue/ActionCategory 정책에 대한 Zod 스키마가 @waiaas/core에 export된다"
  artifacts:
    - path: "packages/core/src/schemas/policy.schema.ts"
      provides: "7 new Zod rule schemas + POLICY_RULES_SCHEMAS 20-entry map"
      exports: ["LendingAssetWhitelistRulesSchema", "LendingLtvLimitRulesSchema", "PerpAllowedMarketsRulesSchema", "PerpMaxLeverageRulesSchema", "PerpMaxPositionUsdRulesSchema", "VenueWhitelistRulesSchema", "ActionCategoryLimitRulesSchema"]
    - path: "packages/core/src/errors/error-codes.ts"
      provides: "POLICY_RULES_CORRUPT error code"
      contains: "POLICY_RULES_CORRUPT"
  key_links:
    - from: "packages/core/src/schemas/policy.schema.ts"
      to: "POLICY_RULES_SCHEMAS"
      via: "20-entry Record mapping all PolicyType to schema"
      pattern: "POLICY_RULES_SCHEMAS.*LENDING_ASSET_WHITELIST"
---

<objective>
@waiaas/core에 Lending/Perp/Venue/ActionCategory 7개 정책 타입의 Zod 룰 스키마를 추가하고, POLICY_RULES_SCHEMAS 매핑을 13 -> 20개로 확장하며, POLICY_RULES_CORRUPT 에러 코드를 등록한다.

Purpose: Phase 429 Plan 02에서 DatabasePolicyEngine의 JSON.parse를 safeJsonParse로 교체할 때 필요한 스키마와 에러 코드를 먼저 준비한다.
Output: policy.schema.ts에 7개 신규 스키마, error-codes.ts에 POLICY_RULES_CORRUPT 에러 코드
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/427-core-exports-safejsonparse/427-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/core/src/schemas/policy.schema.ts (existing schemas to match pattern):
```typescript
export const AllowedTokensRulesSchema = z.object({ tokens: z.array(...) });
export const ContractWhitelistRulesSchema = z.object({ contracts: z.array(...) });
// ... 13 schemas total, mapped in POLICY_RULES_SCHEMAS
export const POLICY_RULES_SCHEMAS: Record<string, z.ZodTypeAny> = { ... };
```

From packages/core/src/enums/policy.ts (all 20 PolicyTypes):
```typescript
export const POLICY_TYPES = [
  'SPENDING_LIMIT', 'WHITELIST', 'RATE_LIMIT', 'TIME_RESTRICTION',
  'ALLOWED_TOKENS', 'CONTRACT_WHITELIST', 'METHOD_WHITELIST',
  'APPROVED_SPENDERS', 'APPROVE_AMOUNT_LIMIT', 'APPROVE_TIER_OVERRIDE',
  'ALLOWED_NETWORKS', 'X402_ALLOWED_DOMAINS', 'REPUTATION_THRESHOLD',
  'LENDING_LTV_LIMIT', 'LENDING_ASSET_WHITELIST',
  'PERP_MAX_LEVERAGE', 'PERP_MAX_POSITION_USD', 'PERP_ALLOWED_MARKETS',
  'VENUE_WHITELIST', 'ACTION_CATEGORY_LIMIT',
] as const;
```

From packages/daemon/src/pipeline/database-policy-engine.ts (local interfaces to mirror):
```typescript
interface LendingLtvLimitRules { maxLtv: number; warningLtv: number; }
interface LendingAssetWhitelistRules { assets: Array<{ address: string; symbol?: string }>; }
export interface PerpMaxLeverageRules { maxLeverage: number; warningLeverage?: number; }
export interface PerpMaxPositionUsdRules { maxPositionUsd: number; warningPositionUsd?: number; }
export interface PerpAllowedMarketsRules { markets: Array<{ market: string; name?: string }>; }
interface VenueWhitelistRules { venues: Array<{ id: string; name?: string }>; }
interface ActionCategoryLimitRules {
  category: string; daily_limit_usd?: number; monthly_limit_usd?: number;
  per_action_limit_usd?: number; tier_on_exceed?: string;
}
```

From packages/core/src/errors/error-codes.ts:
```typescript
export const ERROR_CODES = { ..., VALIDATION_FAILED: { domain: 'SYSTEM', ... } };
// Currently 146 error codes
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7 Zod rule schemas + POLICY_RULES_CORRUPT error code</name>
  <files>
    packages/core/src/schemas/policy.schema.ts
    packages/core/src/errors/error-codes.ts
    packages/core/src/i18n/en.ts
    packages/core/src/i18n/ko.ts
    packages/core/src/__tests__/errors.test.ts
    packages/core/src/__tests__/i18n.test.ts
    packages/core/src/__tests__/package-exports.test.ts
  </files>
  <action>
1. In `packages/core/src/schemas/policy.schema.ts`, add 7 Zod schemas matching the local interfaces in database-policy-engine.ts. Place them after the REPUTATION_THRESHOLD schema and before the POLICY_RULES_SCHEMAS map. Each schema mirrors the daemon's local interface exactly:

```typescript
/** LENDING_ASSET_WHITELIST: rules.assets array (permitted lending assets). */
export const LendingAssetWhitelistRulesSchema = z.object({
  assets: z.array(z.object({
    address: z.string().min(1),
    symbol: z.string().optional(),
  })).min(1, 'At least one asset required'),
});
export type LendingAssetWhitelistRules = z.infer<typeof LendingAssetWhitelistRulesSchema>;

/** LENDING_LTV_LIMIT: LTV-based borrow restriction. */
export const LendingLtvLimitRulesSchema = z.object({
  maxLtv: z.number().min(0).max(1),
  warningLtv: z.number().min(0).max(1),
});
export type LendingLtvLimitRules = z.infer<typeof LendingLtvLimitRulesSchema>;

/** PERP_MAX_LEVERAGE: Maximum leverage restriction. */
export const PerpMaxLeverageRulesSchema = z.object({
  maxLeverage: z.number().positive(),
  warningLeverage: z.number().positive().optional(),
});
export type PerpMaxLeverageRules = z.infer<typeof PerpMaxLeverageRulesSchema>;

/** PERP_MAX_POSITION_USD: Maximum position size restriction. */
export const PerpMaxPositionUsdRulesSchema = z.object({
  maxPositionUsd: z.number().positive(),
  warningPositionUsd: z.number().positive().optional(),
});
export type PerpMaxPositionUsdRules = z.infer<typeof PerpMaxPositionUsdRulesSchema>;

/** PERP_ALLOWED_MARKETS: Permitted perp markets. */
export const PerpAllowedMarketsRulesSchema = z.object({
  markets: z.array(z.object({
    market: z.string().min(1),
    name: z.string().optional(),
  })).min(1, 'At least one market required'),
});
export type PerpAllowedMarketsRules = z.infer<typeof PerpAllowedMarketsRulesSchema>;

/** VENUE_WHITELIST: Permitted external venues. */
export const VenueWhitelistRulesSchema = z.object({
  venues: z.array(z.object({
    id: z.string().min(1),
    name: z.string().optional(),
  })).min(1, 'At least one venue required'),
});
export type VenueWhitelistRules = z.infer<typeof VenueWhitelistRulesSchema>;

/** ACTION_CATEGORY_LIMIT: Per-category action spending limits. */
export const ActionCategoryLimitRulesSchema = z.object({
  category: z.string().min(1),
  daily_limit_usd: z.number().positive().optional(),
  monthly_limit_usd: z.number().positive().optional(),
  per_action_limit_usd: z.number().positive().optional(),
  tier_on_exceed: z.string().optional(),  // PolicyTier string, validated at runtime
});
export type ActionCategoryLimitRules = z.infer<typeof ActionCategoryLimitRulesSchema>;
```

2. Update `POLICY_RULES_SCHEMAS` map to include all 20 entries (add the 7 new ones).

3. In `packages/core/src/errors/error-codes.ts`, add POLICY_RULES_CORRUPT error code:
```typescript
POLICY_RULES_CORRUPT: { domain: 'POLICY' as ErrorDomain, httpStatus: 500, retryable: false },
```

4. In `packages/core/src/i18n/en.ts`, add:
```typescript
POLICY_RULES_CORRUPT: 'Policy rules data is corrupted or does not match expected schema',
```

5. In `packages/core/src/i18n/ko.ts`, add:
```typescript
POLICY_RULES_CORRUPT: '정책 규칙 데이터가 손상되었거나 예상 스키마와 일치하지 않습니다',
```

6. Update error code count in test files from 146 to 147:
   - `packages/core/src/__tests__/errors.test.ts`
   - `packages/core/src/__tests__/i18n.test.ts` (if applicable)
   - `packages/core/src/__tests__/package-exports.test.ts`

7. Ensure all 7 new schemas and their types are exported via `packages/core/src/schemas/index.ts` and `packages/core/src/index.ts` (if not already re-exported by the existing barrel).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/core && pnpm vitest run --project core 2>&1 | tail -20</automated>
  </verify>
  <done>
    - POLICY_RULES_SCHEMAS has 20 entries (all PolicyType values covered)
    - 7 new Zod schemas exported from @waiaas/core with inferred types
    - POLICY_RULES_CORRUPT error code registered (domain POLICY, httpStatus 500)
    - All core tests pass including error code count test (147)
    - typecheck passes
  </done>
</task>

</tasks>

<verification>
```bash
# Verify POLICY_RULES_SCHEMAS has 20 entries
cd /Users/minho.yoo/dev/wallet/WAIaaS && node -e "
const { POLICY_RULES_SCHEMAS } = require('./packages/core/dist/schemas/policy.schema.js');
console.log('POLICY_RULES_SCHEMAS entries:', Object.keys(POLICY_RULES_SCHEMAS).length);
console.assert(Object.keys(POLICY_RULES_SCHEMAS).length === 20, 'Expected 20 entries');
"

# Verify error code exists
node -e "
const { ERROR_CODES } = require('./packages/core/dist/errors/error-codes.js');
console.assert(ERROR_CODES.POLICY_RULES_CORRUPT, 'POLICY_RULES_CORRUPT must exist');
console.log('POLICY_RULES_CORRUPT:', ERROR_CODES.POLICY_RULES_CORRUPT);
"
```
</verification>

<success_criteria>
- POLICY_RULES_SCHEMAS map has 20 entries covering all PolicyType enum values
- ERROR_CODES.POLICY_RULES_CORRUPT exists with domain POLICY, httpStatus 500, retryable false
- pnpm turbo run typecheck --filter=@waiaas/core passes
- All core tests pass (938+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/429-database-policy-engine-zod/429-01-SUMMARY.md`
</output>
