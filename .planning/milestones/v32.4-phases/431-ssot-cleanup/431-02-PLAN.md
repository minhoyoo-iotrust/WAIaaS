---
phase: 431-ssot-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/utils/display-currency.ts
  - packages/daemon/src/api/routes/staking.ts
  - packages/daemon/src/api/routes/admin-wallets.ts
  - packages/daemon/src/pipeline/stages.ts
  - packages/daemon/src/pipeline/pipeline.ts
  - packages/daemon/src/pipeline/gas-condition-tracker.ts
  - packages/adapters/evm/src/adapter.ts
  - packages/adapters/solana/src/adapter.ts
  - packages/core/src/interfaces/IChainAdapter.ts
autonomous: true
requirements:
  - SSOT-03
  - SSOT-04
  - CLN-01
  - CLN-03
  - CLN-05
  - CLN-06

must_haves:
  truths:
    - "Admin UI의 formatDisplayCurrency 인라인 구현이 @waiaas/core import로 교체된다"
    - "aggregateStakingBalance가 공유 유틸로 추출되어 admin-wallets.ts의 인라인 복제가 제거된다"
    - "sweepAll stub가 인터페이스에서 제거된다"
    - "stage3_5GasCondition이 stageGasCondition으로 rename된다"
    - "hintedTokens export가 제거되고 모듈 내부로 캡슐화된다"
  artifacts:
    - path: "packages/daemon/src/services/staking/aggregate-staking-balance.ts"
      provides: "aggregateStakingBalance shared utility"
    - path: "packages/admin/src/utils/display-currency.ts"
      provides: "formatDisplayCurrency imported from core-compatible inline (CSP)"
  key_links:
    - from: "packages/daemon/src/api/routes/admin-wallets.ts"
      to: "packages/daemon/src/services/staking/aggregate-staking-balance.ts"
      via: "import { aggregateStakingBalance }"
      pattern: "aggregateStakingBalance"
---

<objective>
formatDisplayCurrency Admin UI 중복 제거, aggregateStakingBalance 공유 추출, phantom 설정 정리, sweepAll stub 제거, stage 네이밍 정리, hintedTokens 캡슐화를 수행한다.

Purpose: 중복 코드를 SSoT로 통합하고 stale API/stub을 제거하여 코드 품질을 높인다.
Output: 공유 유틸 추출, 불필요한 인터페이스/stub 정리, 일관된 네이밍
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/admin/src/utils/display-currency.ts
@packages/daemon/src/api/routes/staking.ts
@packages/daemon/src/api/routes/admin-wallets.ts
@packages/daemon/src/pipeline/stages.ts
@packages/core/src/interfaces/IChainAdapter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: formatDisplayCurrency + aggregateStakingBalance SSoT 통합</name>
  <files>packages/admin/src/utils/display-currency.ts, packages/daemon/src/api/routes/staking.ts, packages/daemon/src/api/routes/admin-wallets.ts, packages/daemon/src/services/staking/aggregate-staking-balance.ts</files>
  <action>
1. **Admin UI formatDisplayCurrency (SSOT-03):**
   - Admin UI는 CSP `default-src 'none'` 제약으로 @waiaas/core를 직접 import할 수 없다 (Admin은 Preact SPA, core는 Node.js 패키지).
   - 따라서 `packages/admin/src/utils/display-currency.ts`의 인라인 구현을 유지하되, 파일 상단 주석을 업데이트하여 canonical source 참조를 명확히 하고, 실제 로직이 core와 일치하는지 diff 검증한다.
   - `formatDisplayCurrency` 함수 시그니처와 ISO 4217 decimal override 목록이 `packages/core/src/utils/format-currency.ts`와 동일한지 확인한다. 불일치가 있으면 core 기준으로 맞춘다.
   - 주석에 `// SYNC: Must match packages/core/src/utils/format-currency.ts` 추가.

2. **aggregateStakingBalance 공유 추출 (SSOT-04):**
   - `packages/daemon/src/services/staking/` 디렉토리 생성 (없으면).
   - `packages/daemon/src/services/staking/aggregate-staking-balance.ts` 파일 생성:
     - staking.ts의 `aggregateStakingBalance` 함수를 export 함수로 추출.
     - 시그니처: `export function aggregateStakingBalance(sqlite: SQLiteDatabase, walletId: string, providerKey: string): { balanceWei: bigint; pendingUnstake: ... }`
     - staking.ts의 구현을 그대로 이동.
   - `packages/daemon/src/api/routes/staking.ts`에서 로컬 `aggregateStakingBalance` 제거하고 import로 교체:
     `import { aggregateStakingBalance } from '../../services/staking/aggregate-staking-balance.js';`
   - `packages/daemon/src/api/routes/admin-wallets.ts`에서 로컬 `aggregateProvider` 함수를 제거하고 공유 `aggregateStakingBalance`를 import하여 사용:
     `import { aggregateStakingBalance } from '../../services/staking/aggregate-staking-balance.js';`
     - 기존 `aggregateProvider(id, 'lido_staking')` 호출을 `aggregateStakingBalance(deps.sqlite!, id, 'lido_staking')`으로 교체.

3. Run `pnpm turbo run typecheck` to verify.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -rn "function aggregateProvider\|function aggregateStakingBalance" packages/daemon/src/api/ --include="*.ts" | wc -l | xargs test 0 -eq && test -f packages/daemon/src/services/staking/aggregate-staking-balance.ts && echo "PASS: shared utility extracted, inline copies removed"</automated>
  </verify>
  <done>aggregateStakingBalance가 공유 모듈로 추출되고 staking.ts/admin-wallets.ts의 인라인 복제가 제거됨. Admin display-currency.ts가 core와 동기화됨.</done>
</task>

<task type="auto">
  <name>Task 2: sweepAll stub 제거 + stage naming + hintedTokens 캡슐화 + CLN-01 phantom 설정</name>
  <files>packages/core/src/interfaces/IChainAdapter.ts, packages/adapters/evm/src/adapter.ts, packages/adapters/solana/src/adapter.ts, packages/daemon/src/pipeline/stages.ts, packages/daemon/src/pipeline/pipeline.ts, packages/daemon/src/pipeline/gas-condition-tracker.ts</files>
  <action>
1. **sweepAll stub 제거 (CLN-05):**
   - `packages/core/src/interfaces/IChainAdapter.ts`: `sweepAll` 메서드를 optional로 변경: `sweepAll?(from: string, to: string, privateKey: Uint8Array): Promise<SweepResult>;`
   - `packages/adapters/evm/src/adapter.ts`: 구현되지 않은 sweepAll stub 메서드(~line 838) 전체 제거. "Not implemented: sweepAll will be implemented in Phase 80" throw 코드 삭제.
   - `packages/adapters/solana/src/adapter.ts`: 동일하게 sweepAll stub 메서드(~line 1286) 전체 제거.
   - 해당 adapter의 클래스 문서 주석에서 sweepAll 언급도 제거 (evm adapter line ~80 "Utility: getTransactionFee, getCurrentNonce, sweepAll" -> "Utility: getTransactionFee, getCurrentNonce").
   - `packages/daemon/src/api/routes/wallets.ts`의 sweepAll 호출(~line 1379)은 옵셔널 체이닝으로: `adapter.sweepAll?.(...)`

2. **stage3_5GasCondition rename (CLN-06):**
   - `packages/daemon/src/pipeline/stages.ts`: `stage3_5GasCondition` 함수명을 `stageGasCondition`으로 rename. 기존 이름을 deprecated re-export로 유지하지 않는다 (내부 API).
   - `packages/daemon/src/pipeline/pipeline.ts`: import/호출을 `stageGasCondition`으로 변경.
   - `packages/daemon/src/pipeline/gas-condition-tracker.ts`: 주석에서 `stage3_5GasCondition` 참조를 `stageGasCondition`으로 변경.
   - 테스트 파일도 자동으로 업데이트: `packages/daemon/src/__tests__/pipeline-stage3-5-gas-condition.test.ts`에서 import/describe/호출을 모두 `stageGasCondition`으로 변경. `packages/daemon/src/__tests__/admin-actions-route.test.ts`에서 mock 변수명도 변경.

3. **hintedTokens 캡슐화 (CLN-03):**
   - `packages/daemon/src/pipeline/stages.ts`: `export { hintedTokens }` 제거. `hintedTokens`를 모듈 내부 private으로 유지.
   - 대신 `export function clearHintedTokens(): void { hintedTokens.clear(); }` 와 `export function hasHintedToken(key: string): boolean { return hintedTokens.has(key); }` 헬퍼를 export.
   - `packages/daemon/src/__tests__/pipeline-stage1-stage3.test.ts`: `import { hintedTokens }` 를 `import { clearHintedTokens, hasHintedToken }` 으로 교체. `hintedTokens.clear()` -> `clearHintedTokens()`, `hintedTokens.has(...)` -> `hasHintedToken(...)`.

4. **CLN-01: rpc.evm_default_network phantom 설정:**
   - grep으로 `evm_default_network` 사용처 확인. 사용처가 0건이므로 SETTING_DEFINITIONS에 등록 불필요. 대신 코드에 해당 설정 참조가 있으면 제거한다. 이미 0건이므로 추가 작업 없음 -- CLN-01은 "사용처 없음" 확인으로 충족.
   - REQUIREMENTS.md의 CLN-01 설명: "하드코딩이 명시적 파라미터로 교체된다" -- 이미 모든 RPC 호출이 명시적 network 파라미터를 사용하므로 만족.

5. Run `pnpm turbo run typecheck` and relevant tests.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -rn "stage3_5GasCondition" packages/daemon/src/ --include="*.ts" | wc -l | xargs test 0 -eq && grep -rn "export.*hintedTokens" packages/daemon/src/ --include="*.ts" | wc -l | xargs test 0 -eq && echo "PASS: naming + encapsulation done"</automated>
  </verify>
  <done>sweepAll이 optional 인터페이스로 변경되고 stub이 제거됨. stage3_5GasCondition이 stageGasCondition으로 rename됨. hintedTokens가 캡슐화됨. CLN-01 확인 완료.</done>
</task>

</tasks>

<verification>
- `grep -rn "function aggregateProvider" packages/daemon/src/ --include="*.ts"` returns 0 lines
- `grep -rn "stage3_5GasCondition" packages/daemon/src/ --include="*.ts"` returns 0 lines
- `grep -rn "export.*hintedTokens" packages/daemon/src/ --include="*.ts"` returns 0 lines
- IChainAdapter.sweepAll is marked optional (`?`)
- `pnpm turbo run typecheck` passes
</verification>

<success_criteria>
- Admin display-currency.ts가 core 구현과 동기화됨
- aggregateStakingBalance가 공유 모듈로 추출됨
- sweepAll이 optional interface이고 adapter stub이 제거됨
- stage3_5GasCondition -> stageGasCondition rename 완료
- hintedTokens가 모듈 내부로 캡슐화됨
- phantom 설정 CLN-01 확인 완료
</success_criteria>

<output>
After completion, create `.planning/phases/431-ssot-cleanup/431-02-SUMMARY.md`
</output>
