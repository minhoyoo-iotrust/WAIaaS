---
phase: 431-ssot-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/utils/chain-constants.ts
  - packages/core/src/utils/index.ts
  - packages/core/src/index.ts
  - packages/daemon/src/pipeline/database-policy-engine.ts
  - packages/daemon/src/pipeline/stages.ts
  - packages/daemon/src/pipeline/dry-run.ts
  - packages/daemon/src/pipeline/resolve-effective-amount-usd.ts
  - packages/daemon/src/api/routes/admin-wallets.ts
  - packages/daemon/src/services/incoming/incoming-tx-monitor-service.ts
  - packages/daemon/src/services/monitoring/balance-monitor-service.ts
  - packages/daemon/src/services/incoming/safety-rules.ts
  - packages/daemon/src/infrastructure/adapter-pool.ts
  - packages/daemon/src/api/routes/wallets.ts
  - packages/daemon/src/api/routes/transactions.ts
  - packages/daemon/src/lifecycle/daemon.ts
  - packages/daemon/src/infrastructure/config/loader.ts
autonomous: true
requirements:
  - SSOT-01
  - SSOT-05
  - SSOT-06
  - SSOT-07

must_haves:
  truths:
    - "NATIVE_DECIMALS와 NATIVE_SYMBOLS가 @waiaas/core에 단일 정의되고 5곳 로컬 정의가 0건이다"
    - "Number(balance) / 10 ** decimals 패턴이 formatAmount() 호출로 교체된다"
    - "resolveRpcUrl 호출 시 as unknown as Record 캐스팅이 불필요하다"
  artifacts:
    - path: "packages/core/src/utils/chain-constants.ts"
      provides: "NATIVE_DECIMALS, NATIVE_SYMBOLS, nativeDecimals(), nativeSymbol()"
    - path: "packages/daemon/src/infrastructure/adapter-pool.ts"
      provides: "resolveRpcUrl with typed DaemonConfig['rpc'] overload"
  key_links:
    - from: "packages/daemon/src/pipeline/stages.ts"
      to: "@waiaas/core"
      via: "import { NATIVE_DECIMALS, NATIVE_SYMBOLS }"
      pattern: "from '@waiaas/core'"
    - from: "packages/daemon/src/api/routes/wallets.ts"
      to: "packages/daemon/src/infrastructure/adapter-pool.ts"
      via: "resolveRpcUrl(config.rpc, ...)"
      pattern: "resolveRpcUrl\\(.*config\\.rpc"
---

<objective>
NATIVE_DECIMALS/NATIVE_SYMBOLS 상수를 @waiaas/core SSoT로 통합하고, Balance formatting을 formatAmount()로 통일하고, resolveRpcUrl의 as unknown as Record 캐스팅을 제거한다.

Purpose: 5곳에 중복 정의된 상수와 6곳의 인라인 Balance 포매팅, 8곳의 불필요한 타입 캐스팅을 제거하여 유지보수성을 높인다.
Output: chain-constants.ts (SSoT), resolveRpcUrl typed overload, 0건 로컬 중복
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/utils/index.ts
@packages/core/src/index.ts
@packages/daemon/src/infrastructure/adapter-pool.ts
@packages/daemon/src/infrastructure/config/loader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: NATIVE_DECIMALS/NATIVE_SYMBOLS SSoT + formatAmount 통합</name>
  <files>packages/core/src/utils/chain-constants.ts, packages/core/src/utils/index.ts, packages/core/src/index.ts, packages/daemon/src/pipeline/database-policy-engine.ts, packages/daemon/src/pipeline/stages.ts, packages/daemon/src/pipeline/dry-run.ts, packages/daemon/src/pipeline/resolve-effective-amount-usd.ts, packages/daemon/src/api/routes/admin-wallets.ts, packages/daemon/src/services/incoming/incoming-tx-monitor-service.ts, packages/daemon/src/services/monitoring/balance-monitor-service.ts, packages/daemon/src/services/incoming/safety-rules.ts</files>
  <action>
1. Create `packages/core/src/utils/chain-constants.ts`:
   - Export `NATIVE_DECIMALS: Record<string, number>` with `{ solana: 9, ethereum: 18 }` (matching existing local definitions)
   - Export `NATIVE_SYMBOLS: Record<string, string>` with `{ solana: 'SOL', ethereum: 'ETH' }`
   - Export helper `nativeDecimals(chain: string): number` returning `NATIVE_DECIMALS[chain] ?? 18`
   - Export helper `nativeSymbol(chain: string): string` returning `NATIVE_SYMBOLS[chain] ?? chain.toUpperCase()`

2. Add re-exports in `packages/core/src/utils/index.ts` and `packages/core/src/index.ts`:
   - `export { NATIVE_DECIMALS, NATIVE_SYMBOLS, nativeDecimals, nativeSymbol } from './chain-constants.js'`

3. Remove local NATIVE_DECIMALS/NATIVE_SYMBOLS definitions and import from @waiaas/core in these 5 files:
   - `packages/daemon/src/pipeline/database-policy-engine.ts` (lines ~80-87) -- replace `const NATIVE_DECIMALS` with `import { nativeDecimals } from '@waiaas/core'`
   - `packages/daemon/src/pipeline/stages.ts` (lines ~254-255) -- remove local consts, add import
   - `packages/daemon/src/pipeline/dry-run.ts` (lines ~44-45) -- remove local consts, add import
   - `packages/daemon/src/pipeline/resolve-effective-amount-usd.ts` (lines ~53-58) -- remove local consts, add import
   - `packages/daemon/src/api/routes/admin-wallets.ts` (lines ~27-28) -- remove local consts, add import
   - `packages/daemon/src/services/incoming/incoming-tx-monitor-service.ts` (line ~73) -- already imports from core, just remove local NATIVE_SYMBOLS const and use nativeSymbol()

4. Replace `Number(balance) / 10 ** decimals` patterns with `formatAmount()` (already exported from @waiaas/core) in:
   - `packages/daemon/src/services/monitoring/balance-monitor-service.ts:153` -- `Number(formatAmount(balanceInfo.balance, balanceInfo.decimals))`
   - `packages/daemon/src/services/incoming/safety-rules.ts:59,102` -- these compute USD value, use `Number(formatAmount(BigInt(tx.amount), ctx.decimals)) * ctx.usdPrice`
   - `packages/daemon/src/api/routes/admin-wallets.ts:392,408` -- use `formatAmount(balanceInfo.balance, balanceInfo.decimals)` (already imported in this file)

5. In each file, update existing `@waiaas/core` import to include the new symbols (NATIVE_DECIMALS, nativeDecimals, nativeSymbol, formatAmount as needed). Do NOT add duplicate import lines.

6. Run `pnpm turbo run typecheck` to verify no type errors. Run `pnpm turbo run test:unit` to verify no test regressions.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -rn "const NATIVE_DECIMALS" packages/daemon/src/ --include="*.ts" | grep -v __tests__ | grep -v node_modules | wc -l | xargs test 0 -eq && grep -rn "const NATIVE_SYMBOLS" packages/daemon/src/ --include="*.ts" | grep -v __tests__ | grep -v node_modules | wc -l | xargs test 0 -eq && echo "PASS: 0 local duplicates"</automated>
  </verify>
  <done>NATIVE_DECIMALS/NATIVE_SYMBOLS가 core에 SSoT로 정의되고 5곳 로컬 정의가 0건이며, Number(x)/10**d 패턴이 formatAmount()로 교체됨</done>
</task>

<task type="auto">
  <name>Task 2: resolveRpcUrl typed overload + as unknown as Record 캐스팅 제거</name>
  <files>packages/daemon/src/infrastructure/adapter-pool.ts, packages/daemon/src/infrastructure/config/loader.ts, packages/daemon/src/api/routes/wallets.ts, packages/daemon/src/api/routes/transactions.ts, packages/daemon/src/lifecycle/daemon.ts</files>
  <action>
1. In `packages/daemon/src/infrastructure/config/loader.ts`, export the rpc schema's inferred type:
   - Add near the DaemonConfig export: `export type RpcConfig = DaemonConfig['rpc'];`

2. In `packages/daemon/src/infrastructure/adapter-pool.ts`:
   - Import `RpcConfig` from config/loader
   - Change `resolveRpcUrl` signature to accept `Record<string, string> | RpcConfig` (union type):
     ```typescript
     export function resolveRpcUrl(
       rpcConfig: Record<string, string> | RpcConfig,
       chain: string,
       network: string,
     ): string {
       const key = rpcConfigKey(chain, network);
       return (rpcConfig as Record<string, string>)[key] || '';
     }
     ```
   - The internal cast is safe because RpcConfig is a z.object with all string properties. This eliminates the need for callers to cast.

3. Remove `as unknown as Record<string, string>` casts from all callers:
   - `packages/daemon/src/api/routes/wallets.ts` (lines ~434, ~555, ~1362): change `deps.config.rpc as unknown as Record<string, string>` to `deps.config.rpc`
   - `packages/daemon/src/api/routes/transactions.ts` (lines ~571, ~772, ~824): same
   - `packages/daemon/src/lifecycle/daemon.ts` (lines ~458, ~1732): same

4. Run `pnpm turbo run typecheck` to confirm all casts removed and type-safe.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -rn "as unknown as Record" packages/daemon/src/ --include="*.ts" | grep -v __tests__ | grep -v node_modules | grep "rpc" | wc -l | xargs test 0 -eq && echo "PASS: 0 rpc Record casts"</automated>
  </verify>
  <done>resolveRpcUrl가 typed RpcConfig를 직접 수용하여 8곳 as unknown as Record 캐스팅이 0건으로 제거됨</done>
</task>

</tasks>

<verification>
- `grep -rn "const NATIVE_DECIMALS\|const NATIVE_SYMBOLS" packages/daemon/src/ --include="*.ts" | grep -v __tests__` returns 0 lines
- `grep -rn "as unknown as Record" packages/daemon/src/ --include="*.ts" | grep -v __tests__ | grep rpc` returns 0 lines
- `grep -rn "Number(.*balance.*) / 10" packages/daemon/src/ --include="*.ts" | grep -v __tests__` returns 0 lines
- `pnpm turbo run typecheck` passes
</verification>

<success_criteria>
- NATIVE_DECIMALS/NATIVE_SYMBOLS가 @waiaas/core/utils/chain-constants.ts에 SSoT로 존재
- 5곳 daemon 로컬 중복 정의 0건
- Balance formatting Number(x)/10**d 패턴이 formatAmount() 사용으로 교체
- resolveRpcUrl의 as unknown as Record 캐스팅 8곳이 0건
- pnpm turbo run typecheck 통과
</success_criteria>

<output>
After completion, create `.planning/phases/431-ssot-cleanup/431-01-SUMMARY.md`
</output>
