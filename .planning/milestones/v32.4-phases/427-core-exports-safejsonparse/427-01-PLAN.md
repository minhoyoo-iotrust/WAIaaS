---
phase: 427-core-exports-safejsonparse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/utils/safe-json-parse.ts
  - packages/core/src/utils/sleep.ts
  - packages/core/src/utils/index.ts
  - packages/core/src/schemas/policy.schema.ts
  - packages/core/src/schemas/index.ts
  - packages/core/src/index.ts
  - packages/core/src/errors/error-codes.ts
  - packages/core/src/i18n/en.ts
  - packages/core/src/i18n/ko.ts
  - packages/core/src/__tests__/safe-json-parse.test.ts
  - packages/core/src/__tests__/sleep.test.ts
  - packages/daemon/src/pipeline/sleep.ts
  - packages/daemon/src/api/routes/x402.ts
  - packages/adapters/solana/src/adapter.ts
  - packages/cli/src/commands/stop.ts
  - packages/core/src/interfaces/connection-state.ts
autonomous: true
requirements:
  - ZOD-01
  - ZOD-02
  - SSOT-02
  - LAYER-06
  - LAYER-07

must_haves:
  truths:
    - "safeJsonParse<T>(json, schema) 호출 시 Zod 스키마 기반으로 JSON을 파싱+검증하며, 실패 시 구조화된 에러를 반환한다"
    - "다른 패키지(daemon 등)에서 import { POLICY_RULES_SCHEMAS } from '@waiaas/core'가 동작한다"
    - "sleep()이 @waiaas/core에서 export되고 기존 4곳 로컬 정의가 제거되어 중복이 0건이다"
    - "INTERNAL_ERROR와 VALIDATION_FAILED 에러 코드가 ERROR_CODES 레지스트리에 존재한다"
  artifacts:
    - path: "packages/core/src/utils/safe-json-parse.ts"
      provides: "safeJsonParse<T> generic helper"
      exports: ["safeJsonParse", "SafeJsonParseResult", "SafeJsonParseError"]
    - path: "packages/core/src/utils/sleep.ts"
      provides: "sleep utility SSoT"
      exports: ["sleep"]
    - path: "packages/core/src/errors/error-codes.ts"
      provides: "INTERNAL_ERROR + VALIDATION_FAILED error codes"
      contains: "INTERNAL_ERROR"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/utils/safe-json-parse.ts"
      via: "re-export"
      pattern: "export.*safeJsonParse"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/schemas/policy.schema.ts"
      via: "re-export POLICY_RULES_SCHEMAS"
      pattern: "POLICY_RULES_SCHEMAS"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/utils/sleep.ts"
      via: "re-export"
      pattern: "export.*sleep"
---

<objective>
@waiaas/core에 safeJsonParse 범용 헬퍼, POLICY_RULES_SCHEMAS export, sleep SSoT, INTERNAL_ERROR/VALIDATION_FAILED 에러 코드를 추가하여 하위 패키지가 사용할 공유 유틸리티와 스키마 export가 존재하도록 한다.

Purpose: Phase 429(DatabasePolicyEngine Zod 검증)과 Phase 430(as any 제거)의 기반이 되는 공유 유틸리티를 @waiaas/core에 구축한다.
Output: safeJsonParse.ts, sleep.ts, POLICY_RULES_SCHEMAS export, 2개 에러 코드 등록, 4곳 sleep 중복 제거.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/core/src/index.ts
@packages/core/src/utils/index.ts
@packages/core/src/schemas/index.ts
@packages/core/src/schemas/policy.schema.ts
@packages/core/src/errors/error-codes.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/core/src/errors/error-codes.ts:
```typescript
export type ErrorDomain = 'AUTH' | 'SESSION' | 'TX' | 'POLICY' | 'OWNER' | 'SYSTEM' | 'WALLET' | 'WITHDRAW' | 'ACTION' | 'ADMIN' | 'X402' | 'SIGNING' | 'CHAIN' | 'ERC8128' | 'NFT' | 'USEROP' | 'CREDENTIAL';

export interface ErrorCodeEntry {
  code: string;
  domain: ErrorDomain;
  httpStatus: number;
  retryable: boolean;
  message: string;
}

// Pattern: add entries to ERROR_CODES object (as const satisfies Record<string, ErrorCodeEntry>)
```

From packages/core/src/schemas/policy.schema.ts:
```typescript
// POLICY_RULES_SCHEMAS is currently `const` (not exported).
// It maps PolicyType string to ZodTypeAny for superRefine validation.
const POLICY_RULES_SCHEMAS: Record<string, z.ZodTypeAny> = { ... };
// Individual rule schemas: some already exported (SpendingLimitRulesSchema, WhitelistRulesSchema, etc.)
// Some NOT exported: AllowedTokensRulesSchema, ContractWhitelistRulesSchema,
//   MethodWhitelistRulesSchema, ApprovedSpendersRulesSchema, ApproveAmountLimitRulesSchema,
//   ApproveTierOverrideRulesSchema, AllowedNetworksRulesSchema
```

Existing sleep locations (to be replaced):
- packages/daemon/src/pipeline/sleep.ts (export function sleep)
- packages/daemon/src/api/routes/x402.ts (const sleep = inline)
- packages/adapters/solana/src/adapter.ts (function sleep)
- packages/cli/src/commands/stop.ts (function sleep)
- packages/core/src/interfaces/connection-state.ts (function sleep)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: safeJsonParse 헬퍼 + POLICY_RULES_SCHEMAS export + 에러 코드 등록</name>
  <files>
    packages/core/src/utils/safe-json-parse.ts
    packages/core/src/utils/index.ts
    packages/core/src/schemas/policy.schema.ts
    packages/core/src/schemas/index.ts
    packages/core/src/index.ts
    packages/core/src/errors/error-codes.ts
    packages/core/src/i18n/en.ts
    packages/core/src/i18n/ko.ts
    packages/core/src/__tests__/safe-json-parse.test.ts
  </files>
  <behavior>
    - safeJsonParse(validJson, zodSchema) returns { success: true, data: T }
    - safeJsonParse(invalidJson, zodSchema) returns { success: false, error: SafeJsonParseError } with JSON parse error details
    - safeJsonParse(validJson, wrongSchema) returns { success: false, error: SafeJsonParseError } with Zod validation issues
    - safeJsonParse(null/undefined, schema) returns { success: false, error } (does not throw)
    - POLICY_RULES_SCHEMAS['SPENDING_LIMIT'] equals SpendingLimitRulesSchema
    - POLICY_RULES_SCHEMAS covers all 13 policy types
    - ERROR_CODES.INTERNAL_ERROR exists with domain 'SYSTEM', httpStatus 500
    - ERROR_CODES.VALIDATION_FAILED exists with domain 'SYSTEM', httpStatus 400
  </behavior>
  <action>
    1. **Create packages/core/src/utils/safe-json-parse.ts:**
       - Define `SafeJsonParseError` type: `{ type: 'json_parse' | 'validation'; message: string; issues?: z.ZodIssue[] }`
       - Define `SafeJsonParseResult<T>`: discriminated union `{ success: true; data: T } | { success: false; error: SafeJsonParseError }`
       - Implement `safeJsonParse<T>(json: string, schema: z.ZodType<T>): SafeJsonParseResult<T>`
         - First try JSON.parse (catch -> json_parse error)
         - Then schema.safeParse (failure -> validation error with issues)
         - Success -> return { success: true, data }
       - Handle null/undefined input gracefully (return json_parse error, no throw)

    2. **Export POLICY_RULES_SCHEMAS from policy.schema.ts:**
       - Change `const POLICY_RULES_SCHEMAS` to `export const POLICY_RULES_SCHEMAS`
       - Also export the 7 currently-unexported individual rule schemas: add `export` to AllowedTokensRulesSchema, ContractWhitelistRulesSchema, MethodWhitelistRulesSchema, ApprovedSpendersRulesSchema, ApproveAmountLimitRulesSchema, ApproveTierOverrideRulesSchema, AllowedNetworksRulesSchema
       - Export corresponding types via z.infer for each newly-exported schema
       - Update packages/core/src/schemas/index.ts to re-export POLICY_RULES_SCHEMAS and all new schemas/types
       - Update packages/core/src/index.ts to re-export POLICY_RULES_SCHEMAS and all new schemas/types

    3. **Register INTERNAL_ERROR and VALIDATION_FAILED in error-codes.ts:**
       - Add to SYSTEM domain section:
         ```
         INTERNAL_ERROR: { code: 'INTERNAL_ERROR', domain: 'SYSTEM', httpStatus: 500, retryable: false, message: 'Internal server error' }
         VALIDATION_FAILED: { code: 'VALIDATION_FAILED', domain: 'SYSTEM', httpStatus: 400, retryable: false, message: 'Validation failed' }
         ```
       - Add i18n translations in en.ts and ko.ts:
         - en: INTERNAL_ERROR: 'Internal server error', VALIDATION_FAILED: 'Validation failed'
         - ko: INTERNAL_ERROR: '내부 서버 오류', VALIDATION_FAILED: '검증에 실패했습니다'

    4. **Update packages/core/src/utils/index.ts** to re-export safeJsonParse, SafeJsonParseResult, SafeJsonParseError.

    5. **Update packages/core/src/index.ts** to re-export safeJsonParse, SafeJsonParseResult, SafeJsonParseError from utils.

    6. **Write tests in packages/core/src/__tests__/safe-json-parse.test.ts:**
       - Valid JSON + matching schema -> success
       - Invalid JSON string -> json_parse error
       - Valid JSON + wrong schema -> validation error with issues array
       - Null/undefined input -> json_parse error (no throw)
       - Test with SpendingLimitRulesSchema from POLICY_RULES_SCHEMAS
       - Verify POLICY_RULES_SCHEMAS has 13 entries
       - Verify ERROR_CODES.INTERNAL_ERROR and ERROR_CODES.VALIDATION_FAILED exist
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core exec vitest run src/__tests__/safe-json-parse.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - safeJsonParse<T> 함수가 @waiaas/core에서 export되고 모든 테스트 케이스 통과
    - POLICY_RULES_SCHEMAS가 13개 정책 타입을 포함하고 @waiaas/core에서 export됨
    - INTERNAL_ERROR, VALIDATION_FAILED가 ERROR_CODES에 등록됨
    - pnpm --filter @waiaas/core exec vitest run이 통과
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sleep() SSoT를 @waiaas/core로 통합 + 4곳 로컬 중복 제거</name>
  <files>
    packages/core/src/utils/sleep.ts
    packages/core/src/utils/index.ts
    packages/core/src/index.ts
    packages/core/src/__tests__/sleep.test.ts
    packages/daemon/src/pipeline/sleep.ts
    packages/daemon/src/api/routes/x402.ts
    packages/adapters/solana/src/adapter.ts
    packages/cli/src/commands/stop.ts
    packages/core/src/interfaces/connection-state.ts
  </files>
  <behavior>
    - sleep(100) resolves after ~100ms
    - sleep(0) resolves immediately
    - grep -r "function sleep" packages/ finds only packages/core/src/utils/sleep.ts
    - grep -r "const sleep" packages/ --include="*.ts" finds 0 inline sleep definitions in production code
    - All existing imports of local sleep still work (via @waiaas/core import or daemon pipeline import redirect)
  </behavior>
  <action>
    1. **Create packages/core/src/utils/sleep.ts:**
       ```typescript
       /**
        * Sleep utility — single source of truth.
        * All packages import from @waiaas/core instead of defining locally.
        */
       export function sleep(ms: number): Promise<void> {
         return new Promise(resolve => setTimeout(resolve, ms));
       }
       ```

    2. **Update packages/core/src/utils/index.ts:** Add `export { sleep } from './sleep.js';`

    3. **Update packages/core/src/index.ts:** Add `export { sleep } from './utils/index.js';` in the utils section.

    4. **Replace 4 local sleep definitions with @waiaas/core import:**

       a. **packages/daemon/src/pipeline/sleep.ts**: Keep file but change to re-export from @waiaas/core to avoid breaking existing imports within daemon:
          ```typescript
          // Re-export from @waiaas/core SSoT (v32.4 SSOT-02)
          export { sleep } from '@waiaas/core';
          ```

       b. **packages/daemon/src/api/routes/x402.ts**: Remove `const sleep = (ms: number): Promise<void> => ...` inline definition. Add `import { sleep } from '@waiaas/core';` at top.

       c. **packages/adapters/solana/src/adapter.ts**: Remove local `function sleep(ms: number)` definition. Add `import { sleep } from '@waiaas/core';` at top. (Keep existing @waiaas/core import line -- just add sleep to the destructuring.)

       d. **packages/cli/src/commands/stop.ts**: Remove local `function sleep(ms: number)` definition. Add `import { sleep } from '@waiaas/core';` at top.

       e. **packages/core/src/interfaces/connection-state.ts**: Remove local `function sleep(ms: number)` definition. Import from `../utils/sleep.js` instead (internal module import, not from package root to avoid circular deps).

    5. **Write tests in packages/core/src/__tests__/sleep.test.ts:**
       - sleep(50) resolves (timing test with tolerance)
       - sleep(0) resolves immediately
       - Returns a Promise<void>

  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core exec vitest run src/__tests__/sleep.test.ts --reporter=verbose 2>&1 | tail -15 && echo "--- Checking no duplicate sleep defs ---" && grep -rn "function sleep\|const sleep.*=.*Promise" packages/*/src/ packages/adapters/*/src/ --include="*.ts" | grep -v node_modules | grep -v __tests__ | grep -v "\.test\." | grep -v "packages/core/src/utils/sleep.ts" | grep -v "re-export" || echo "OK: No duplicates found"</automated>
  </verify>
  <done>
    - sleep()이 packages/core/src/utils/sleep.ts에 SSoT로 존재
    - 4곳 로컬 정의(daemon/pipeline, daemon/x402, solana/adapter, cli/stop)가 @waiaas/core import로 교체
    - connection-state.ts의 로컬 sleep이 내부 import로 교체
    - pnpm turbo run typecheck이 전체 패키지에서 통과
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Core package tests pass
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/core exec vitest run

# 2. Full typecheck passes
pnpm turbo run typecheck

# 3. POLICY_RULES_SCHEMAS is importable
node -e "import('@waiaas/core').then(m => { console.log('POLICY_RULES_SCHEMAS keys:', Object.keys(m.POLICY_RULES_SCHEMAS).length); console.log('safeJsonParse:', typeof m.safeJsonParse); console.log('sleep:', typeof m.sleep); console.log('INTERNAL_ERROR:', m.ERROR_CODES.INTERNAL_ERROR?.code); console.log('VALIDATION_FAILED:', m.ERROR_CODES.VALIDATION_FAILED?.code); })"

# 4. No duplicate sleep definitions
grep -rn "function sleep\|const sleep.*=.*Promise" packages/*/src/ packages/adapters/*/src/ --include="*.ts" | grep -v node_modules | grep -v __tests__ | grep -v "packages/core/src/utils/sleep.ts" | grep -v "re-export"
```
</verification>

<success_criteria>
1. `import { safeJsonParse, POLICY_RULES_SCHEMAS, sleep, ERROR_CODES } from '@waiaas/core'`가 동작
2. safeJsonParse가 valid/invalid JSON과 schema mismatch를 안전하게 처리
3. POLICY_RULES_SCHEMAS가 13개 정책 타입 매핑을 포함
4. ERROR_CODES.INTERNAL_ERROR (500, SYSTEM)과 ERROR_CODES.VALIDATION_FAILED (400, SYSTEM)이 존재
5. sleep()의 로컬 중복이 0건 (core SSoT만 존재)
6. pnpm turbo run typecheck 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/427-core-exports-safejsonparse/427-01-SUMMARY.md`
</output>
