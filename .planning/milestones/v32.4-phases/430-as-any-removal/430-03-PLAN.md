---
phase: 430-as-any-removal
plan: 03
type: execute
wave: 2
depends_on: ["430-01", "430-02"]
files_modified:
  - packages/daemon/src/pipeline/external-action-pipeline.ts
  - packages/daemon/src/api/routes/actions.ts
  - packages/daemon/src/api/routes/admin-actions.ts
  - packages/daemon/src/api/routes/nfts.ts
  - packages/daemon/src/api/routes/staking.ts
  - packages/daemon/src/api/routes/defi-positions.ts
  - packages/daemon/src/api/routes/tokens.ts
  - packages/daemon/src/api/routes/wallet.ts
  - packages/daemon/src/infrastructure/action/action-provider-registry.ts
  - packages/daemon/src/infrastructure/nft/alchemy-nft-indexer.ts
  - packages/daemon/src/infrastructure/nft/helius-nft-indexer.ts
  - packages/daemon/src/infrastructure/nft/nft-indexer-client.ts
  - packages/daemon/src/api/routes/wc.ts
autonomous: true
requirements:
  - CAST-07
  - CAST-11
  - CAST-12
  - CAST-13

must_haves:
  truths:
    - "external-action-pipeline.ts에서 as any가 0건이다"
    - "actions.ts에서 as any가 0건이다 (item 타입 내로잉)"
    - "API 라우트의 network as any가 모두 NetworkType 단언으로 교체되었다"
    - "프로덕션 소스 전체에서 as any가 0건이다 (@ts-expect-error Solana 전환 제외)"
    - "pnpm turbo run typecheck가 전체 패키지에서 통과한다"
  artifacts:
    - path: "packages/daemon/src/pipeline/external-action-pipeline.ts"
      provides: "typed policy evaluate, typed execute result"
    - path: "packages/daemon/src/api/routes/actions.ts"
      provides: "typed ResolvedAction kind discrimination"
  key_links:
    - from: "packages/daemon/src/api/routes/actions.ts"
      to: "ResolvedAction type"
      via: "kind discriminator"
      pattern: "kind.*signedData|signedHttp"
    - from: "packages/daemon/src/pipeline/external-action-pipeline.ts"
      to: "IPolicyEngine"
      via: "evaluate with extended fields"
      pattern: "policyEngine.evaluate"
---

<objective>
나머지 모든 프로덕션 as any를 제거하고, 전체 프로덕션 소스에서 as any 0건을 달성한다.

Purpose: external-action-pipeline, API 라우트, NFT 인프라의 as any를 모두 제거하여 CAST-12 (as any 0건) 목표를 완성한다.
Output: 13개 파일에서 ~25건의 as any가 제거되어 전체 프로덕션 소스 as any 0건 달성.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/430-as-any-removal/430-01-SUMMARY.md
@.planning/phases/430-as-any-removal/430-02-SUMMARY.md

@packages/daemon/src/pipeline/external-action-pipeline.ts
@packages/daemon/src/api/routes/actions.ts
@packages/daemon/src/infrastructure/action/action-provider-registry.ts

<interfaces>
<!-- Key types the executor needs -->

From @waiaas/core IPolicyEngine:
```typescript
evaluate(walletId: string, transaction: {
  type: string; amount: string; toAddress: string; chain: string; network?: string;
}): Promise<PolicyEvaluation>;
```

networkToCaip2 signature:
```typescript
function networkToCaip2(network: NetworkType): string;
// NetworkType is a string union from ChainTypeEnum
```

ResolvedAction types (from @waiaas/actions or daemon):
```typescript
// ResolvedAction has kind: 'onChain' | 'signedData' | 'signedHttp'
// ApiDirectResult has externalId, status fields
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: external-action-pipeline + actions.ts + admin-actions.ts as any 제거</name>
  <files>
    packages/daemon/src/pipeline/external-action-pipeline.ts
    packages/daemon/src/api/routes/actions.ts
    packages/daemon/src/api/routes/admin-actions.ts
    packages/daemon/src/infrastructure/action/action-provider-registry.ts
  </files>
  <action>
    **external-action-pipeline.ts (4건):**
    - L88, L244 `policyEngine.evaluate(walletId, { ... } as any)`: IPolicyEngine.evaluate는 `{ type, amount, toAddress, chain, network? }`를 받는다. 추가 필드(venue, actionCategory, notionalUsd)는 인터페이스에 없다.
    - 해결 방법 1 (권장): IPolicyEngine 인터페이스의 transaction 파라미터에 `[key: string]: unknown` index signature를 추가하거나, 별도 `& Record<string, unknown>` intersection을 사용.
    - 해결 방법 2: 필요한 필드만 전달. `{ type: 'TRANSFER', amount: '0', toAddress: \`external:\${action.venue}\`, chain: deps.wallet.chain }` -- venue/actionCategory/notionalUsd가 정책 평가에 실제로 사용되지 않을 수 있으므로 확인. 만약 DatabasePolicyEngine이 이 추가 필드를 사용하면, IPolicyEngine 인터페이스를 확장한다.
    - IPolicyEngine 인터페이스 변경이 scope 초과 시, Omit/Pick으로 필요 필드만 전달하고 추가 필드는 별도 채널로 전달하거나, `as { type: string; amount: string; toAddress: string; chain: string; venue?: string; actionCategory?: string; notionalUsd?: string }`으로 단언.
    - L310 `chain: deps.wallet.chain as any`: wallet.chain은 string 타입이고 execute 콜백이 ChainType을 기대. `as ChainType`으로 교체.
    - L316 `(executeResult as any).externalId`: 이미 `'externalId' in executeResult` 체크 후이므로, `(executeResult as { externalId: string }).externalId`로 좁힌다.

    **actions.ts (~7건):**
    - L394-395 `(item as any).kind`: ResolvedAction 타입에 kind 필드가 있는지 확인. 있으면 직접 접근, 없으면 타입 가드 `function isKindedAction(item: unknown): item is { kind: string }` 사용.
    - L407 `contractCallsForLegacy: any[]`: 적절한 타입으로 교체. contractCalls의 아이템 타입 또는 `unknown[]` 사용.
    - L412 `(item as any).externalId`, `(item as any).status`: isApiDirectResult 체크 후이므로 ApiDirectResult 타입으로 단언. `const direct = item as ApiDirectResult; pipelineResults.push({ id: direct.externalId, status: direct.status });`
    - L418 `(item as any).kind`: 위의 타입 가드 사용.
    - L574 `(contractCall as any).eip712`: ContractCallRequest 타입에 eip712 필드가 없는 경우, `'eip712' in contractCall`로 체크 후 접근하거나 타입 확장.

    **admin-actions.ts (1건):**
    - L257 `(contractCall as any).eip712`: actions.ts와 동일 패턴.

    **action-provider-registry.ts (3건):**
    - L218-220 `(item as any).actionProvider = ...` / `(item as any).actionName = ...` / `validated.push(item as any)`: item에 동적 속성을 추가하는 패턴.
    - 해결: validated 배열에 넣기 전에 새 객체를 만든다. `validated.push({ ...item, actionProvider: entry.provider.metadata.name, actionName: entry.action.name } as ValidatedAction)`. ValidatedAction 타입을 정의하거나 기존 타입에 optional 필드 추가.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -n 'as any' packages/daemon/src/pipeline/external-action-pipeline.ts packages/daemon/src/api/routes/actions.ts packages/daemon/src/api/routes/admin-actions.ts packages/daemon/src/infrastructure/action/action-provider-registry.ts | grep -v test | grep -v '@ts-expect-error' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>external-action-pipeline.ts 4건, actions.ts 7건, admin-actions.ts 1건, action-provider-registry.ts 3건 총 15건의 as any가 제거되었다</done>
</task>

<task type="auto">
  <name>Task 2: API 라우트 network as any + NFT indexer as any + 최종 sweep + typecheck</name>
  <files>
    packages/daemon/src/api/routes/nfts.ts
    packages/daemon/src/api/routes/staking.ts
    packages/daemon/src/api/routes/defi-positions.ts
    packages/daemon/src/api/routes/tokens.ts
    packages/daemon/src/api/routes/wallet.ts
    packages/daemon/src/infrastructure/nft/alchemy-nft-indexer.ts
    packages/daemon/src/infrastructure/nft/helius-nft-indexer.ts
    packages/daemon/src/infrastructure/nft/nft-indexer-client.ts
  </files>
  <action>
    **API 라우트 network as any (9건):**
    모든 라우트에서 `networkToCaip2(network as any)` 또는 `validateNetworkEnvironment(chain, environment, network as any)` 패턴이 반복된다. network 변수가 DB에서 읽은 string이라 NetworkType이 아닌 것이 원인.

    공통 해결 패턴:
    1. `NetworkTypeEnum.safeParse(network)`로 런타임 검증 후 사용: `const parsed = NetworkTypeEnum.safeParse(network); if (parsed.success) { networkToCaip2(parsed.data); }`
    2. 또는 단순히 `as NetworkType`으로 단언 (DB에서 온 값은 이미 유효한 NetworkType).
    3. 각 라우트에서 일관된 패턴 사용. `as NetworkType` 단언이 가장 간결하고, DB에 저장된 network 값이 유효함이 보장되므로 안전하다.

    - nfts.ts L265, L277, L349, L364: `network as any` → `network as NetworkType`
    - staking.ts L243, L281: `'ethereum-mainnet' as any` → 리터럴이 NetworkType에 포함되므로 `as any` 제거만으로 충분. 타입 에러 시 `'ethereum-mainnet' as const satisfies NetworkType` 사용.
    - defi-positions.ts L174: `row.network as any` → `row.network as NetworkType`
    - tokens.ts L177: `network as any` → `network as NetworkType`
    - wallet.ts L473: `targetNetwork as any` → `targetNetwork as NetworkType`

    **NFT indexer (4건):**
    - alchemy-nft-indexer.ts L215 `response.json() as any`: `response.json() as Promise<AlchemyNftResponse>` 등 실제 응답 타입으로 교체. 없으면 `as unknown`으로 바꾸고 이후 타입 가드.
    - helius-nft-indexer.ts L182: 동일 패턴.
    - nft-indexer-client.ts L111, L131: `network as any` → `network as NetworkType` (위와 동일 패턴).

    **최종 sweep:**
    - 모든 파일 교체 후 `grep -rn 'as any' packages/daemon/src/ --include='*.ts' | grep -v '__tests__' | grep -v '.test.ts' | grep -v '.spec.ts' | grep -v node_modules | grep -v '@ts-expect-error'`로 0건 확인.
    - 누락된 as any가 있으면 동일 패턴으로 처리.

    **전체 typecheck:**
    - `pnpm turbo run typecheck`로 전체 패키지 typecheck 통과 확인.
    - 실패 시 타입 에러를 분석하여 수정. `as unknown as X` 패턴 허용 (as any보다 안전).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -rn 'as any' packages/daemon/src/ --include='*.ts' | grep -v '__tests__' | grep -v '.test.ts' | grep -v '.spec.ts' | grep -v '@ts-expect-error' | wc -l | xargs test 0 -eq && pnpm turbo run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>프로덕션 소스 전체에서 as any가 0건이다 (Solana @ts-expect-error 전환 제외). pnpm turbo run typecheck가 전체 통과한다.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'as any' packages/daemon/src/ --include='*.ts' | grep -v '__tests__' | grep -v '.test.ts' | grep -v '.spec.ts' | grep -v '@ts-expect-error'` → 0건
2. `pnpm turbo run typecheck` → 전체 패키지 통과
3. `pnpm turbo run test:unit --filter=@waiaas/daemon` → 통과 (기존 테스트 깨지지 않음)
</verification>

<success_criteria>
- external-action-pipeline.ts의 as any 4건이 제거됨
- actions.ts/admin-actions.ts의 as any 8건이 제거됨
- API 라우트 9곳의 network as any가 NetworkType 단언으로 교체됨
- NFT indexer 4건의 as any가 제거됨
- 프로덕션 소스 전체 as any 0건 (CAST-12 달성)
- pnpm turbo run typecheck 전체 통과 (CAST-13 달성)
</success_criteria>

<output>
After completion, create `.planning/phases/430-as-any-removal/430-03-SUMMARY.md`
</output>
