---
phase: 271-yield-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/objectives/m29-00-defi-advanced-protocol-design.md
autonomous: true
requirements: [YIELD-01, YIELD-02, YIELD-03, YIELD-04]

must_haves:
  truths:
    - "IYieldProvider interface extends IActionProvider with 5 yield-specific actions and 3 query methods"
    - "YieldPositionSummary Zod schema provides API-response-level yield position data (distinct from DB YieldMetadataSchema)"
    - "MaturityInfo type captures maturity date, days remaining, redeemability, and expiry status"
    - "YieldMarketInfo and YieldForecast types provide market listing and APY data for AI agent consumption"
    - "ActionDefinition entries for buyPT/buyYT/redeemPT/addLiquidity/removeLiquidity are fully specified with input schemas"
  artifacts:
    - path: "internal/objectives/m29-00-defi-advanced-protocol-design.md"
      provides: "Sections 18-19 (IYieldProvider interface + Yield types)"
      contains: "## 18. IYieldProvider 인터페이스"
  key_links:
    - from: "IYieldProvider interface (section 18)"
      to: "IActionProvider (existing)"
      via: "extends relationship"
      pattern: "IYieldProvider extends IActionProvider"
    - from: "YieldPositionSummary (section 19)"
      to: "YieldMetadataSchema (section 5.3)"
      via: "API response extends DB metadata"
      pattern: "YieldPositionSummary.*token_type.*market_id.*maturity"
    - from: "IYieldProvider.getPosition"
      to: "YieldPositionSummary"
      via: "return type"
      pattern: "getPosition.*YieldPositionSummary"
---

<objective>
IYieldProvider 인터페이스(extends IActionProvider, 5 액션, 3 조회)와 YieldPosition/MaturityInfo/YieldMarketInfo/YieldForecast Zod 타입을 m29-00 설계 문서 섹션 18-19에 정의한다.

Purpose: Pendle V2 구현(m29-06)이 프레임워크 수정 없이 IYieldProvider만 구현하면 동작하도록, 인터페이스와 타입 계약을 확정한다. Phase 270의 ILendingProvider 패턴을 정확히 미러링하되, yield 도메인 특화(만기 관리, PT/YT/LP 토큰 타입, 수익률 예측)를 반영한다.
Output: m29-00 설계 문서에 섹션 18 (IYieldProvider 인터페이스) + 섹션 19 (Yield 타입 시스템) 추가
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/271-yield-framework/271-RESEARCH.md
@.planning/phases/270-lending-framework/270-01-SUMMARY.md
@.planning/phases/268-position-infra-design/268-01-SUMMARY.md
@internal/objectives/m29-00-defi-advanced-protocol-design.md (sections 5.3, 13-15 for pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: IYieldProvider 인터페이스 + ActionDefinition 5개 + IPositionProvider 이중 구현 패턴 (섹션 18)</name>
  <files>internal/objectives/m29-00-defi-advanced-protocol-design.md</files>
  <action>
m29-00 설계 문서 끝(섹션 17 이후)에 **섹션 18: IYieldProvider 인터페이스**를 추가한다. Phase 270 섹션 13 (ILendingProvider) 패턴을 정확히 미러링한다.

**18.1: 인터페이스 정의 (extends IActionProvider)**
- IYieldProvider extends IActionProvider with 3 query methods:
  - `getMarkets(chain: string, network?: string): Promise<YieldMarketInfo[]>` -- 사용 가능한 yield 시장 목록
  - `getPosition(walletId: string, context: ActionContext): Promise<YieldPositionSummary[]>` -- 지갑의 현재 yield 포지션 조회
  - `getYieldForecast(marketId: string, context: ActionContext): Promise<YieldForecast>` -- 시장의 수익률 예측 (현재 implied/underlying/fixed APY, 만기까지 기간)
- TypeScript interface 코드 블록으로 작성

**18.2: ActionDefinition 5개 상세**
- 각 액션마다: name, description, chain ('ethereum' -- Pendle은 EVM only), inputSchema (Zod), riskLevel, defaultTier
  1. `buy_pt` -- Principal Token 매수 (고정 수익률). Input: marketId, amount, slippage. riskLevel: medium, defaultTier: DELAY
  2. `buy_yt` -- Yield Token 매수 (변동 수익률 레버리지). Input: marketId, amount, slippage. riskLevel: high (YT는 만기 시 가치 0), defaultTier: DELAY
  3. `redeem_pt` -- PT 상환 (만기 후 원금 회수). Input: marketId, amount. riskLevel: low, defaultTier: AUTO. 자동으로 pre/post-maturity 감지하여 라우팅 (post-maturity: PT only, pre-maturity: PT+YT 필요)
  4. `add_liquidity` -- LP 추가 (AMM 유동성 공급). Input: marketId, amount, slippage. riskLevel: medium, defaultTier: DELAY
  5. `remove_liquidity` -- LP 제거. Input: marketId, amount, slippage. riskLevel: low, defaultTier: AUTO
- 각 inputSchema는 Zod 코드 블록으로 작성 (BuyPTInputSchema, BuyYTInputSchema, RedeemPTInputSchema, AddLiquidityInputSchema, RemoveLiquidityInputSchema)

**18.3: IPositionProvider 이중 구현 패턴**
- Phase 270 섹션 13.3 패턴 미러링: 동일 클래스가 IYieldProvider + IPositionProvider 동시 구현
- PendleProvider 예시 코드 (skeleton): metadata, actions, resolve(), getMarkets(), getPosition(), getYieldForecast() + IPositionProvider의 getPositions(), getProviderName(), getSupportedCategories()

**18.4: 설계 결정 4개**
- DEC-YIELD-01: IYieldProvider extends IActionProvider (ILendingProvider 동일 패턴)
- DEC-YIELD-02: 3 query methods on IYieldProvider directly (서비스 간접 참조 없음, DEC-LEND-02 미러링)
- DEC-YIELD-03: redeemPT single action handles both pre/post-maturity (AI 에이전트 인터페이스 단순화, 6개가 아닌 5개 액션 유지)
- DEC-YIELD-04: buy_yt riskLevel: high (YT는 만기 시 가치 0으로 수렴, 레버리지 특성)

모든 Zod 스키마에서 amount 필드는 string 타입 사용 (DEC-LEND-06 미러링). asset 식별자는 CAIP-19 형식 사용 (DEC-LEND-07 미러링).
  </action>
  <verify>
    <automated>grep -c "## 18\." internal/objectives/m29-00-defi-advanced-protocol-design.md | grep -q "1" && grep -c "DEC-YIELD-0[1-4]" internal/objectives/m29-00-defi-advanced-protocol-design.md | grep -q "4" && echo "PASS" || echo "FAIL"</automated>
    <manual>섹션 18이 IYieldProvider interface, 5 ActionDefinition, IPositionProvider dual pattern, 4 설계 결정을 포함하는지 확인</manual>
  </verify>
  <done>섹션 18이 m29-00에 추가되어 IYieldProvider interface(extends IActionProvider, 3 query methods), 5개 ActionDefinition(buyPT/buyYT/redeemPT/addLiquidity/removeLiquidity with input schemas), IPositionProvider dual implementation pattern, 4개 설계 결정(DEC-YIELD-01~04)을 포함한다</done>
</task>

<task type="auto">
  <name>Task 2: YieldPositionSummary + MaturityInfo + YieldMarketInfo + YieldForecast 타입 (섹션 19)</name>
  <files>internal/objectives/m29-00-defi-advanced-protocol-design.md</files>
  <action>
섹션 18 이후에 **섹션 19: YieldPosition + MaturityInfo 타입**을 추가한다. Phase 270 섹션 14 (LendingPosition + HealthFactor 타입) 패턴을 미러링한다.

**19.1: YieldPositionSummary Zod 스키마 (API 응답용)**
- Phase 268 섹션 5.3의 YieldMetadataSchema (DB 저장용)와 구별되는 API 응답 스키마
- LendingPositionSummarySchema (섹션 14.1) 패턴 미러링:
  ```
  YieldPositionSummarySchema = z.object({
    positionId: z.string(),
    provider: z.string(),       // e.g., 'pendle'
    chain: z.string(),
    network: z.string(),
    tokenType: z.enum(['PT', 'YT', 'LP']),
    marketId: z.string(),
    marketName: z.string(),     // human-readable, e.g., "PT stETH 27MAR2025"
    underlyingAsset: z.string(), // CAIP-19
    amount: z.string(),
    amountUsd: z.number().nullable(),
    maturity: z.number().int(), // epoch seconds
    maturityInfo: MaturityInfoSchema,
    apy: z.number().nullable(), // current implied APY
    entryPrice: z.number().nullable(),
    status: z.enum(['ACTIVE', 'CLOSED', 'MATURED']),
    updatedAt: z.number(),
  })
  ```
- DB YieldMetadataSchema와의 매핑 관계 문서화 (어떤 필드가 DB에서 오고 어떤 필드가 조회 시 추가되는지)

**19.2: MaturityInfo 타입**
- MaturityInfoSchema Zod 정의:
  ```
  MaturityInfoSchema = z.object({
    maturityEpoch: z.number().int(),     // 만기 타임스탬프 (epoch seconds)
    daysRemaining: z.number().int(),     // 만기까지 남은 일수 (음수 = 만기 지남)
    isRedeemable: z.boolean(),           // true = 만기 후 PT only redemption 가능
    isExpired: z.boolean(),              // true = 만기 지남
    warningLevel: z.enum(['NONE', 'WARNING_7D', 'WARNING_1D', 'EXPIRED_UNREDEEMED']).nullable(),
  })
  ```
- Phase 269 섹션 10.2 MaturityMonitor의 3-tier severity (WARNING 7d, DANGER 1d, CRITICAL post-maturity)와의 매핑 관계
- pre-maturity vs post-maturity 상환 차이 문서화 (pre: PT+YT, post: PT only)

**19.3: YieldMarketInfo 타입**
- YieldMarketInfoSchema Zod 정의 (IYieldProvider.getMarkets() 반환 타입):
  ```
  YieldMarketInfoSchema = z.object({
    marketId: z.string(),
    name: z.string(),           // e.g., "PT stETH 27MAR2025"
    chain: z.string(),
    network: z.string(),
    underlyingAsset: z.string(), // CAIP-19
    ptAddress: z.string(),
    ytAddress: z.string(),
    syAddress: z.string(),
    maturity: z.number().int(),
    impliedApy: z.number(),     // PT 매수 시 고정 수익률
    underlyingApy: z.number(),  // underlying asset의 기본 수익률
    liquidity: z.string(),      // TVL in underlying asset
    liquidityUsd: z.number().nullable(),
  })
  ```

**19.4: YieldForecast 타입**
- YieldForecastSchema Zod 정의 (IYieldProvider.getYieldForecast() 반환 타입):
  ```
  YieldForecastSchema = z.object({
    marketId: z.string(),
    impliedApy: z.number(),     // PT 매수 시 고정 수익률
    underlyingApy: z.number(),  // underlying의 현재 변동 수익률
    fixedApy: z.number(),       // PT 보유 시 만기까지 고정 수익률 (= impliedApy)
    longYieldApy: z.number().nullable(), // YT 보유 시 기대 수익률 (레버리지)
    daysToMaturity: z.number().int(),
    ptPriceInUnderlying: z.number(), // 1 PT = X underlying
    ytPriceInUnderlying: z.number(), // 1 YT = X underlying
  })
  ```
- getYieldForecast은 예측이 아닌 현재 시장 데이터 기반 조회임을 명시 (Pitfall 4 방지)

**19.5: 설계 결정 4개**
- DEC-YIELD-05: YieldPositionSummary는 DB YieldMetadataSchema의 superset (API 응답에 human-readable 필드 추가, DEC-LEND-06 amount string 미러링)
- DEC-YIELD-06: MaturityInfo.warningLevel은 MaturityMonitor severity와 1:1 매핑 (NONE=SAFE, WARNING_7D=WARNING, WARNING_1D=DANGER, EXPIRED_UNREDEEMED=CRITICAL)
- DEC-YIELD-07: YieldForecast는 현재 시장 데이터 기반 단순 조회 (예측 모델 아님, Pendle API market data 직접 반환)
- DEC-YIELD-08: 모든 asset 식별자는 CAIP-19 형식 (DEC-LEND-07 미러링)
  </action>
  <verify>
    <automated>grep -c "## 19\." internal/objectives/m29-00-defi-advanced-protocol-design.md | grep -q "1" && grep -c "DEC-YIELD-0[5-8]" internal/objectives/m29-00-defi-advanced-protocol-design.md | grep -q "4" && echo "PASS" || echo "FAIL"</automated>
    <manual>섹션 19가 YieldPositionSummary, MaturityInfo, YieldMarketInfo, YieldForecast 4개 스키마와 4개 설계 결정을 포함하는지 확인</manual>
  </verify>
  <done>섹션 19가 m29-00에 추가되어 YieldPositionSummary(API 응답용), MaturityInfo(만기 상태), YieldMarketInfo(시장 정보), YieldForecast(수익률 데이터) 4개 Zod 스키마와 4개 설계 결정(DEC-YIELD-05~08)을 포함한다</done>
</task>

</tasks>

<verification>
1. 섹션 18이 m29-00에 존재하고 IYieldProvider interface, 5 ActionDefinition, dual implementation pattern, 4 설계 결정을 포함
2. 섹션 19가 m29-00에 존재하고 4개 Zod 스키마(YieldPositionSummary, MaturityInfo, YieldMarketInfo, YieldForecast)와 4 설계 결정을 포함
3. IYieldProvider가 IActionProvider를 extends하고 3개 query methods를 정의
4. 5개 ActionDefinition의 inputSchema가 Zod 코드 블록으로 작성됨
5. YieldPositionSummary가 Phase 268 YieldMetadataSchema와의 매핑 관계가 문서화됨
6. MaturityInfo.warningLevel이 Phase 269 MaturityMonitor severity와 매핑 관계가 명시됨
</verification>

<success_criteria>
- m29-00 설계 문서에 섹션 18-19가 추가됨
- IYieldProvider interface가 IActionProvider를 extends하며 5 actions + 3 queries 정의
- 4개 Zod 스키마가 TypeScript 코드 블록으로 완전히 명세됨
- 8개 설계 결정(DEC-YIELD-01 through DEC-YIELD-08)이 기록됨
- Phase 268/269/270과의 교차 참조가 명확함
</success_criteria>

<output>
After completion, create `.planning/phases/271-yield-framework/271-01-SUMMARY.md`
</output>
