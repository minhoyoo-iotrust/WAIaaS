---
phase: 269-defi-monitoring-framework
plan: 02
type: execute
wave: 2
depends_on:
  - 269-01
files_modified:
  - internal/objectives/m29-00-defi-advanced-protocol-design.md
autonomous: true
requirements:
  - MON-05
  - MON-06
  - MON-07

must_haves:
  truths:
    - "4개 알림 이벤트(LIQUIDATION_WARNING, MATURITY_WARNING, MARGIN_WARNING, LIQUIDATION_IMMINENT)가 NOTIFICATION_EVENT_TYPES SSoT 배열 추가 위치, EVENT_CATEGORY_MAP 매핑, i18n 메시지 템플릿(en/ko)과 함께 명세된다"
    - "새로운 'defi_monitoring' 알림 카테고리가 NOTIFICATION_CATEGORIES 배열에 추가되고, LIQUIDATION_IMMINENT는 security_alert + BROADCAST_EVENTS에 매핑되는 이유가 명시된다"
    - "DeFiMonitorService 데몬 라이프사이클 통합이 Step 4c-11(시작)과 shutdown 시퀀스(정지)로 명세되며, fail-soft 패턴을 따른다"
    - "config.toml [monitoring] 섹션이 17개 플랫 키(enabled + health_factor 8개 + maturity 4개 + margin 3개 + cooldown 1개)로 정의되고, KNOWN_SECTIONS 추가 + DaemonConfigSchema Zod 확장이 명세된다"
    - "Admin Settings 5개 이상 핫 리로드 키와 HotReloadOrchestrator.reloadDeFiMonitors() 확장이 명세된다"
  artifacts:
    - path: "internal/objectives/m29-00-defi-advanced-protocol-design.md"
      provides: "섹션 11-12: 알림 이벤트 통합 + 설정 구조 + 라이프사이클"
      contains: "LIQUIDATION_WARNING"
  key_links:
    - from: "NOTIFICATION_EVENT_TYPES array"
      to: "EVENT_CATEGORY_MAP + i18n templates"
      via: "SSoT chain (5 files)"
      pattern: "LIQUIDATION_WARNING.*defi_monitoring"
    - from: "DeFiMonitorService"
      to: "DaemonLifecycle Step 4c-11"
      via: "fail-soft start/stop"
      pattern: "defiMonitorService.*start"
    - from: "config.toml [monitoring]"
      to: "DaemonConfigSchema.monitoring"
      via: "Zod validation + KNOWN_SECTIONS"
      pattern: "monitoring.*z\\.object"
    - from: "HotReloadOrchestrator"
      to: "DeFiMonitorService.updateConfig"
      via: "reloadDeFiMonitors method"
      pattern: "reloadDeFiMonitors"
---

<objective>
4개 알림 이벤트의 SSoT 체인 통합, DeFiMonitorService 데몬 라이프사이클 연동, config.toml [monitoring] 섹션과 Admin Settings 핫 리로드를 m29-00 설계 문서 섹션 11-12에 작성한다.

Purpose: 모니터가 발생시키는 알림이 기존 NotificationService에 정확히 통합되고, 데몬 시작/정지에 올바르게 등록/해제되며, 관리자가 Admin UI에서 임계값을 실시간 변경할 수 있도록 설계를 확정한다.
Output: m29-00 설계 문서 섹션 11 (알림 이벤트 통합) + 섹션 12 (설정 구조 + 라이프사이클)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/269-defi-monitoring-framework/269-RESEARCH.md
@.planning/phases/269-defi-monitoring-framework/269-01-SUMMARY.md

Key reference files for SSoT chain:
@packages/core/src/enums/notification.ts (NOTIFICATION_EVENT_TYPES array -- SSoT)
@packages/core/src/schemas/signing-protocol.ts (EVENT_CATEGORY_MAP, NOTIFICATION_CATEGORIES, EVENT_DESCRIPTIONS)
@packages/core/src/i18n/en.ts (notification message templates)
@packages/daemon/src/notifications/notification-service.ts (BROADCAST_EVENTS, notify method)
@packages/daemon/src/infrastructure/config/loader.ts (DaemonConfigSchema, KNOWN_SECTIONS)
@packages/daemon/src/infrastructure/settings/hot-reload.ts (HotReloadOrchestrator)
@packages/daemon/src/lifecycle/daemon.ts (DaemonLifecycle startup/shutdown sequence)

Phase 268+269 Plan 01 output:
@internal/objectives/m29-00-defi-advanced-protocol-design.md (sections 5-10)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4개 알림 이벤트 SSoT 통합 설계 (섹션 11)</name>
  <files>internal/objectives/m29-00-defi-advanced-protocol-design.md</files>
  <action>
m29-00 설계 문서에 섹션 11을 추가한다. 섹션 10 뒤에 배치한다.

**섹션 11: 알림 이벤트 통합**

11.1 4개 신규 NotificationEventType:
- `LIQUIDATION_WARNING`: Lending health factor가 warning/danger 임계값 이하. HealthFactorMonitor에서 발생
- `MATURITY_WARNING`: Yield 포지션 만기 접근 또는 만기 후 미상환. MaturityMonitor에서 발생
- `MARGIN_WARNING`: Perp 마진 비율이 warning 임계값 이하. MarginMonitor에서 발생
- `LIQUIDATION_IMMINENT`: Lending health factor CRITICAL 또는 Perp 마진 비율 CRITICAL. HealthFactorMonitor/MarginMonitor에서 발생
- 추가 위치: `packages/core/src/enums/notification.ts` NOTIFICATION_EVENT_TYPES 배열 끝부분 (기존 39개 → 43개)
- 이 배열이 Zod enum, DB CHECK constraint, i18n 키의 SSoT

11.2 EVENT_CATEGORY_MAP 확장:
- 파일: `packages/core/src/schemas/signing-protocol.ts`
- 새로운 카테고리 'defi_monitoring' 추가:
  * NOTIFICATION_CATEGORIES 배열에 `'defi_monitoring'` 추가 (기존 6개 → 7개)
  * NotificationCategory 타입이 자동으로 7개 값을 포함
- 매핑:
  * `LIQUIDATION_WARNING: 'defi_monitoring'`
  * `MATURITY_WARNING: 'defi_monitoring'`
  * `MARGIN_WARNING: 'defi_monitoring'`
  * `LIQUIDATION_IMMINENT: 'security_alert'` ← 의도적: 임박한 자금 손실은 보안 경고로 분류하여 BROADCAST 배송
- 근거: LIQUIDATION_IMMINENT는 KILL_SWITCH_ACTIVATED와 동일 수준의 긴급도. security_alert 카테고리는 BROADCAST_EVENTS와 결합하여 모든 알림 채널에 동시 배송

11.3 EVENT_DESCRIPTIONS 확장:
- 파일: `packages/core/src/schemas/signing-protocol.ts`
- 4개 이벤트 설명 추가:
  * `LIQUIDATION_WARNING: 'DeFi lending position health factor below warning threshold'`
  * `MATURITY_WARNING: 'DeFi yield position approaching or past maturity'`
  * `MARGIN_WARNING: 'DeFi perpetual position margin below warning threshold'`
  * `LIQUIDATION_IMMINENT: 'DeFi position at immediate liquidation risk'`

11.4 i18n 메시지 템플릿:
- 영문 (packages/core/src/i18n/en.ts):
  * LIQUIDATION_WARNING: title='Liquidation Risk Warning', body='{walletName} lending position on {provider} has health factor {healthFactor} (threshold: {threshold}). Consider adding collateral or repaying debt.'
  * MATURITY_WARNING: title='Maturity Warning', body='{walletName} yield position on {provider} matures in {daysRemaining} days (maturity: {maturityDate}). Redeem before maturity to avoid penalties.'
  * MARGIN_WARNING: title='Margin Warning', body='{walletName} perp position on {provider} margin ratio at {marginRatio}% (maintenance: {maintenanceMargin}%). Add margin to avoid liquidation.'
  * LIQUIDATION_IMMINENT: title='LIQUIDATION IMMINENT', body='{walletName} position on {provider} at immediate liquidation risk. Health factor: {healthFactor}, Liquidation price: {liquidationPrice}. Take action NOW.'
- 한글 (packages/core/src/i18n/ko.ts):
  * LIQUIDATION_WARNING: title='청산 위험 경고', body='{walletName} {provider} 대출 포지션 헬스 팩터 {healthFactor} (임계값: {threshold}). 담보 추가 또는 부채 상환을 고려하세요.'
  * MATURITY_WARNING: title='만기 경고', body='{walletName} {provider} 수익률 포지션이 {daysRemaining}일 후 만기입니다 (만기일: {maturityDate}). 만기 전 상환하세요.'
  * MARGIN_WARNING: title='마진 경고', body='{walletName} {provider} 무기한 선물 포지션 마진 비율 {marginRatio}% (유지 마진: {maintenanceMargin}%). 마진 추가로 청산을 방지하세요.'
  * LIQUIDATION_IMMINENT: title='청산 임박', body='{walletName} {provider} 포지션 즉시 청산 위험. 헬스 팩터: {healthFactor}, 청산 가격: {liquidationPrice}. 즉시 조치하세요.'

11.5 BROADCAST_EVENTS 확장:
- 파일: `packages/daemon/src/notifications/notification-service.ts`
- `LIQUIDATION_IMMINENT` 추가 (기존 5개 → 6개)
- 근거: KILL_SWITCH_ACTIVATED, TX_INCOMING_SUSPICIOUS와 동급의 긴급도. 모든 알림 채널(ntfy, Telegram, push relay, wallet app)에 동시 배송

11.6 알림 쿨다운 전략:
- 쿨다운 키: `${walletId}:${positionId}` 복합 키 (per-wallet:position)
- BalanceMonitorService는 per-wallet 쿨다운이지만, DeFi는 같은 지갑에 여러 포지션이 있으므로 per-position 쿨다운 필요
- WARNING 수준: cooldown_hours(기본 4시간) 적용
- CRITICAL 수준: 쿨다운 미적용 (LIQUIDATION_IMMINENT는 매 평가마다 발생)
- recovery 감지: severity가 SAFE로 복귀하면 쿨다운 맵에서 해당 키 삭제 → 재악화 시 즉시 알림

11.7 SSoT 체인 업데이트 체크리스트:
- [ ] `packages/core/src/enums/notification.ts` — NOTIFICATION_EVENT_TYPES 배열에 4개 추가
- [ ] `packages/core/src/schemas/signing-protocol.ts` — NOTIFICATION_CATEGORIES에 'defi_monitoring' 추가, EVENT_CATEGORY_MAP에 4개 매핑 추가, EVENT_DESCRIPTIONS에 4개 추가
- [ ] `packages/core/src/i18n/en.ts` — 4개 영문 메시지 템플릿
- [ ] `packages/core/src/i18n/ko.ts` — 4개 한글 메시지 템플릿
- [ ] `packages/daemon/src/notifications/notification-service.ts` — BROADCAST_EVENTS에 LIQUIDATION_IMMINENT 추가
- [ ] 기존 테스트: `signing-protocol.test.ts`가 모든 이벤트에 카테고리 매핑이 있는지 검증 → 43개로 자동 통과

11.8 설계 결정 기록:
- DEC-MON-09: "LIQUIDATION_IMMINENT는 security_alert 카테고리 + BROADCAST_EVENTS — 즉시 자금 손실 가능성은 보안 사건과 동급"
- DEC-MON-10: "defi_monitoring 신규 카테고리 추가 — DeFi 알림을 기존 6개 카테고리에 혼합하면 필터링 불가"
- DEC-MON-11: "쿨다운 키는 walletId:positionId 복합 키 — 같은 지갑의 다른 포지션 알림이 차단되면 안됨"
- DEC-MON-12: "CRITICAL 알림은 쿨다운 미적용 — 반복 알림이 사용자 행동을 유도하는 것이 알림 피로보다 중요"
  </action>
  <verify>
    <automated>grep -c "LIQUIDATION_WARNING\|MATURITY_WARNING\|MARGIN_WARNING\|LIQUIDATION_IMMINENT" internal/objectives/m29-00-defi-advanced-protocol-design.md | awk '{if ($1 >= 20) print "PASS: 4 notification events defined"; else print "FAIL: notification events insufficient"}'</automated>
    <manual>섹션 11이 11.1-11.8 구조를 갖추고, 4개 이벤트의 SSoT 체인 5파일 업데이트가 모두 명세되어 있으며, LIQUIDATION_IMMINENT가 security_alert + BROADCAST에 매핑된 근거가 있는지 확인</manual>
  </verify>
  <done>섹션 11이 4개 알림 이벤트(SSoT 5파일 체인), defi_monitoring 카테고리, BROADCAST_EVENTS 확장, 쿨다운 전략(per-wallet:position + CRITICAL 미적용), i18n 템플릿(en/ko), 4개 설계 결정을 포함하여 작성 완료</done>
</task>

<task type="auto">
  <name>Task 2: 설정 구조 + 데몬 라이프사이클 설계 (섹션 12)</name>
  <files>internal/objectives/m29-00-defi-advanced-protocol-design.md</files>
  <action>
m29-00 설계 문서에 섹션 12를 추가한다. 섹션 11 뒤에 배치한다.

**섹션 12: 설정 구조 + 라이프사이클**

12.1 config.toml [monitoring] 섹션:
- 플랫 키 규칙 준수 (CLAUDE.md: No nesting in config.toml)
- 17개 키 정의 (전체 명세, Zod 기본값 포함):
  * `enabled` (boolean, default true)
  * `health_factor_safe_threshold` (number, default 2.0, min 1.0, max 10.0)
  * `health_factor_warning_threshold` (number, default 1.5, min 1.0, max 10.0)
  * `health_factor_danger_threshold` (number, default 1.2, min 1.0, max 10.0)
  * `health_factor_safe_interval` (integer seconds, default 300, min 10, max 3600)
  * `health_factor_warning_interval` (integer seconds, default 60, min 5, max 300)
  * `health_factor_danger_interval` (integer seconds, default 15, min 3, max 60)
  * `health_factor_critical_interval` (integer seconds, default 5, min 1, max 30)
  * `maturity_check_interval` (integer seconds, default 86400, min 3600, max 604800)
  * `maturity_warning_days_first` (integer, default 7, min 1, max 30)
  * `maturity_warning_days_final` (integer, default 1, min 1, max 7)
  * `maturity_unredeemed_alert` (boolean, default true)
  * `margin_check_interval` (integer seconds, default 60, min 10, max 3600)
  * `margin_warning_ratio` (number, default 0.3, min 0.05, max 0.5)
  * `margin_critical_ratio` (number, default 0.15, min 0.01, max 0.3)
  * `cooldown_hours` (integer, default 4, min 1, max 48)
- config.toml 예시 블록 (모든 키 + 주석)
- 제약 조건: health_factor_safe_threshold > warning > danger (런타임 검증)

12.2 DaemonConfigSchema Zod 확장:
- 파일: `packages/daemon/src/infrastructure/config/loader.ts`
- `monitoring` 섹션을 z.object()로 추가 (17개 키, 전부 .default() 가짐)
- KNOWN_SECTIONS에 `'monitoring'` 추가 (기존 12개 → 13개)
- 환경 변수 패턴: `WAIAAS_MONITORING_ENABLED`, `WAIAAS_MONITORING_HEALTH_FACTOR_SAFE_THRESHOLD` 등

12.3 Admin Settings 키 등록:
- SettingsService에 등록할 핫 리로드 가능 키 7개:
  * `monitoring.enabled` (boolean)
  * `monitoring.health_factor_safe_threshold` (number)
  * `monitoring.health_factor_warning_threshold` (number)
  * `monitoring.health_factor_danger_threshold` (number)
  * `monitoring.margin_warning_ratio` (number)
  * `monitoring.margin_critical_ratio` (number)
  * `monitoring.cooldown_hours` (number)
- 핫 리로드 불가 키 (데몬 재시작 필요): 폴링 간격 변경은 타이머 재생성 필요하므로 핫 리로드로 처리하되, interval 변경은 다음 폴링 사이클부터 적용 (BalanceMonitorService updateConfig 패턴)
- 실제로는 모든 17개 키를 핫 리로드 지원. updateConfig()가 호출되면 각 모니터가 자체 interval/threshold를 갱신

12.4 HotReloadOrchestrator 확장:
- 파일: `packages/daemon/src/infrastructure/settings/hot-reload.ts`
- 신규 메서드: `reloadDeFiMonitors()` — SettingsService에서 monitoring.* 키 읽어서 DeFiMonitorService.updateConfig() 호출
- 기존 패턴: `reloadBalanceMonitor()` (duck-typed service deps, config 변환 후 service.updateConfig 호출)
- 변경 감지: settings 변경 이벤트 시 monitoring.* 프리픽스 키가 포함되면 reloadDeFiMonitors() 트리거

12.5 DaemonLifecycle 연동:
- 시작: Step 4c-11 (PositionTracker 이후, fail-soft)
  * 조건: sqlite && settingsService && config.monitoring.enabled !== false
  * DeFiMonitorService 생성 → 3개 모니터 register → start()
  * 에러 시: console.warn + defiMonitorService = null (다른 서비스 영향 없음)
  * PositionTracker 의존: 모니터가 defi_positions 테이블에서 데이터를 읽으므로 PositionTracker가 먼저 시작되어야 함
- 정지: EventBus cleanup 이전
  * defiMonitorService?.stop() → defiMonitorService = null
  * 각 모니터 내부에서 clearInterval/clearTimeout 처리
- 코드 스니펫 (시작/정지 모두)

12.6 설계 결정 기록:
- DEC-MON-13: "config.toml [monitoring] 섹션 17개 플랫 키 — CLAUDE.md 규칙 준수, 네스팅 금지"
- DEC-MON-14: "KNOWN_SECTIONS에 'monitoring' 추가 필수 — detectNestedSections() 검증 통과"
- DEC-MON-15: "Admin Settings 핫 리로드: 임계값 + 폴링 간격 모두 지원 — updateConfig()가 다음 사이클부터 적용"
- DEC-MON-16: "Step 4c-11 배치: PositionTracker 이후 — 데이터 소스가 준비된 후 모니터 시작"
  </action>
  <verify>
    <automated>grep -c "config.toml\|KNOWN_SECTIONS\|DaemonLifecycle\|HotReloadOrchestrator\|DaemonConfigSchema" internal/objectives/m29-00-defi-advanced-protocol-design.md | awk '{if ($1 >= 10) print "PASS: config and lifecycle designed"; else print "FAIL: config/lifecycle insufficient"}'</automated>
    <manual>섹션 12가 12.1-12.6 구조를 갖추고, config.toml 17개 플랫 키 + KNOWN_SECTIONS + DaemonConfigSchema Zod + Admin Settings 7개 키 + HotReloadOrchestrator + DaemonLifecycle Step 4c-11이 명세되어 있는지 확인</manual>
  </verify>
  <done>섹션 12가 config.toml [monitoring] 17개 플랫 키, DaemonConfigSchema Zod 확장, Admin Settings 핫 리로드 키, HotReloadOrchestrator.reloadDeFiMonitors(), DaemonLifecycle Step 4c-11(시작/정지), 4개 설계 결정을 포함하여 작성 완료</done>
</task>

</tasks>

<verification>
1. 섹션 11이 4개 알림 이벤트의 SSoT 체인(5파일)을 완전하게 명세한다
2. 섹션 12가 config.toml [monitoring] 17개 플랫 키 + Zod 스키마 + KNOWN_SECTIONS + 환경 변수를 포함한다
3. DaemonLifecycle Step 4c-11(시작)과 shutdown(정지)이 fail-soft 패턴으로 명세된다
4. HotReloadOrchestrator.reloadDeFiMonitors()가 기존 패턴을 따른다
5. SSoT 체인 업데이트 체크리스트(11.7)가 구현자가 빠뜨리기 쉬운 파일을 모두 나열한다
</verification>

<success_criteria>
- 4개 알림 이벤트가 SSoT 5파일 체인으로 완전히 명세됨 (notification.ts, signing-protocol.ts, en.ts, ko.ts, notification-service.ts)
- config.toml [monitoring] 17개 키가 Zod 기본값/검증과 함께 정의됨
- DaemonLifecycle Step 4c-11 시작/정지 코드 스니펫이 있음
- 8개 이상의 설계 결정(DEC-MON-09 ~ DEC-MON-16)이 기록됨
</success_criteria>

<output>
After completion, create `.planning/phases/269-defi-monitoring-framework/269-02-SUMMARY.md`
</output>
