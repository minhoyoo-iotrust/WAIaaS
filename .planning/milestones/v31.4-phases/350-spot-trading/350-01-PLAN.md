---
phase: 350-spot-trading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/hyperliquid/schemas.ts
  - packages/actions/src/providers/hyperliquid/market-data.ts
  - packages/actions/src/providers/hyperliquid/spot-provider.ts
  - packages/actions/src/providers/hyperliquid/index.ts
  - packages/actions/src/providers/hyperliquid/__tests__/spot-provider.test.ts
autonomous: true
requirements: [HSPOT-01, HSPOT-02, HSPOT-03, HSPOT-04, HSPOT-05, HSPOT-07]

must_haves:
  truths:
    - "HyperliquidSpotProvider can resolve hl_spot_buy with market/limit orders"
    - "HyperliquidSpotProvider can resolve hl_spot_sell with market/limit orders"
    - "HyperliquidSpotProvider can resolve hl_spot_cancel (single/all)"
    - "MarketData.getSpotMarkets() returns typed SpotMarketInfo[]"
    - "MarketData.getSpotBalances() returns typed SpotBalance[]"
    - "getSpendingAmount returns correct amounts: buy=size*price, sell=$0, cancel=$0"
  artifacts:
    - path: "packages/actions/src/providers/hyperliquid/spot-provider.ts"
      provides: "HyperliquidSpotProvider implementing IActionProvider"
      exports: ["HyperliquidSpotProvider"]
    - path: "packages/actions/src/providers/hyperliquid/schemas.ts"
      provides: "Spot input schemas + SpotMeta/SpotBalance response schemas"
      contains: "HlSpotBuyInputSchema"
    - path: "packages/actions/src/providers/hyperliquid/market-data.ts"
      provides: "Typed getSpotMarkets + getSpotBalances methods"
      contains: "getSpotMarkets"
    - path: "packages/actions/src/providers/hyperliquid/__tests__/spot-provider.test.ts"
      provides: "Tests for spot-provider resolve methods and getSpendingAmount"
      min_lines: 80
  key_links:
    - from: "packages/actions/src/providers/hyperliquid/spot-provider.ts"
      to: "packages/actions/src/providers/hyperliquid/signer.ts"
      via: "HyperliquidSigner.signL1Action for order/cancel signing"
      pattern: "HyperliquidSigner\\.signL1Action"
    - from: "packages/actions/src/providers/hyperliquid/spot-provider.ts"
      to: "packages/actions/src/providers/hyperliquid/exchange-client.ts"
      via: "client.exchange() for POST /exchange"
      pattern: "this\\.client\\.exchange"
    - from: "packages/actions/src/providers/hyperliquid/spot-provider.ts"
      to: "packages/actions/src/providers/hyperliquid/market-data.ts"
      via: "getSpotMarkets for asset index resolution, getAllMidPrices for market orders"
      pattern: "this\\.marketData\\.getSpotMarkets"
---

<objective>
Implement HyperliquidSpotProvider with 3 actions (hl_spot_buy, hl_spot_sell, hl_spot_cancel), add Spot Zod schemas, and extend MarketData with typed Spot methods.

Purpose: Core Spot trading logic that MCP/SDK/Admin will consume. Follows the exact same ApiDirectResult pattern as HyperliquidPerpProvider.
Output: spot-provider.ts, extended schemas.ts, extended market-data.ts, tests
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/actions/src/providers/hyperliquid/perp-provider.ts
@packages/actions/src/providers/hyperliquid/schemas.ts
@packages/actions/src/providers/hyperliquid/market-data.ts
@packages/actions/src/providers/hyperliquid/signer.ts
@packages/actions/src/providers/hyperliquid/exchange-client.ts
@packages/actions/src/providers/hyperliquid/config.ts
@packages/actions/src/providers/hyperliquid/index.ts

<interfaces>
<!-- Key types and contracts the executor needs from existing code. -->

From packages/actions/src/providers/hyperliquid/signer.ts:
```typescript
export class HyperliquidSigner {
  static async signL1Action(action, nonce, isMainnet, privateKey, vaultAddress?): Promise<{r,s,v}>
  static async signUserSignedAction(actionType, action, isMainnet, privateKey): Promise<{r,s,v}>
}
export function orderToWire(asset, isBuy, price, size, reduceOnly, orderType, cloid?): OrderWire
```

From packages/actions/src/providers/hyperliquid/exchange-client.ts:
```typescript
export class HyperliquidExchangeClient {
  async exchange(request: ExchangeRequest): Promise<ExchangeResponse>
  async info<T>(request: InfoRequest, schema: z.ZodType<T>): Promise<T>
}
```

From @waiaas/core action-provider.types.ts:
```typescript
interface IActionProvider {
  metadata: ActionProviderMetadata;
  actions: readonly ActionDefinition[];
  resolve(actionName, params, context): Promise<ContractCallRequest | ContractCallRequest[] | ApiDirectResult>;
  getSpendingAmount?(actionName, params): Promise<{amount: bigint; asset: string}>;
}
```

Key pattern: Spot asset indices are 10000 + token index (Perp uses raw index). Spot uses same L1 Action signing (phantom agent) for orders and cancels. Spot markets come from Info API type='spotMeta'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spot Zod schemas + MarketData typed Spot methods</name>
  <files>packages/actions/src/providers/hyperliquid/schemas.ts, packages/actions/src/providers/hyperliquid/market-data.ts</files>
  <behavior>
    - HlSpotBuyInputSchema validates: market(string), side='BUY' implicit, size(string), price(string optional), orderType(MARKET|LIMIT), tif(GTC|IOC|ALO optional), cloid(string optional), subAccount(string optional); price required for LIMIT
    - HlSpotSellInputSchema validates: same as buy but for sells
    - HlSpotCancelInputSchema validates: market(string), oid(number optional), cloid(string optional), subAccount(string optional)
    - SpotMetaSchema parses Hyperliquid spotMeta response: { universe: [{name, tokens, index, isCanonical, evmContract?}], tokens: [{name, szDecimals, weiDecimals, index, tokenId, isCanonical, evmContract?}] }
    - SpotBalanceSchema parses spotClearinghouseState: { balances: [{coin, hold, token, total, entryNtl?}] }
    - MarketData.getSpotMarkets() returns SpotMarketInfo[] from spotMeta
    - MarketData.getSpotBalances(walletAddress) returns SpotBalance[] from spotClearinghouseState
  </behavior>
  <action>
    1. Add to schemas.ts:
       - HlSpotBuyInputSchema: z.object({market, size, price?, orderType: z.enum(['MARKET','LIMIT']), tif?, cloid?, subAccount?}).refine(price required for LIMIT)
       - HlSpotSellInputSchema: same structure as buy
       - HlSpotCancelInputSchema: z.object({market, oid?, cloid?, subAccount?})
       - SpotMetaSchema: z.object({universe: z.array(z.object({name, tokens: z.array(z.number()), index: z.number(), isCanonical: z.boolean().optional()}).passthrough()), tokens: z.array(z.object({name, szDecimals: z.number(), weiDecimals: z.number(), index: z.number(), tokenId: z.string(), isCanonical: z.boolean().optional()}).passthrough())}).passthrough()
       - SpotBalanceSchema: z.object({coin: z.string(), hold: z.string(), token: z.number(), total: z.string(), entryNtl: z.string().optional()}).passthrough()
       - SpotClearinghouseStateSchema: z.object({balances: z.array(SpotBalanceSchema)}).passthrough()
       - Export types: SpotMeta, SpotBalance, SpotClearinghouseState, SpotMarketInfo

    2. Extend market-data.ts:
       - Import new schemas (SpotMetaSchema, SpotClearinghouseStateSchema, SpotBalanceSchema)
       - Add SpotMarketInfo interface: {name, tokens: number[], index: number, isCanonical?: boolean}
       - Replace getSpotState() (currently returns unknown) with getSpotBalances(walletAddress: Hex): Promise<SpotBalance[]> that uses info({type:'spotClearinghouseState', user}) parsed with SpotClearinghouseStateSchema, returning .balances
       - Add getSpotMarkets(): Promise<SpotMarketInfo[]> that uses info({type:'spotMeta'}) parsed with SpotMetaSchema, returning .universe
       - Add getSpotMeta(): Promise<SpotMeta> raw accessor for full metadata including tokens array
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx vitest run packages/actions/src/providers/hyperliquid/__tests__/market-data.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>HlSpotBuyInputSchema, HlSpotSellInputSchema, HlSpotCancelInputSchema exist with refine rules. SpotMeta/SpotBalance schemas parse correctly. MarketData has typed getSpotMarkets() and getSpotBalances() methods replacing the old untyped getSpotState().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HyperliquidSpotProvider + tests + index re-export</name>
  <files>packages/actions/src/providers/hyperliquid/spot-provider.ts, packages/actions/src/providers/hyperliquid/__tests__/spot-provider.test.ts, packages/actions/src/providers/hyperliquid/index.ts</files>
  <behavior>
    - hl_spot_buy with MARKET order: resolves spot asset index (10000+tokenIndex), gets mid price with 3% slippage, builds order wire, signs via signL1Action, calls client.exchange, returns ApiDirectResult
    - hl_spot_buy with LIMIT order: uses provided price, GTC tif default
    - hl_spot_sell: same flow as buy but opposite side
    - hl_spot_cancel with oid: sends cancel action for specific order
    - hl_spot_cancel without oid/cloid: gets open orders, filters by market, cancels all matching
    - getSpendingAmount('hl_spot_buy'): returns size*price in USDC (6 decimals)
    - getSpendingAmount('hl_spot_sell'): returns 0n (selling existing asset, no spending)
    - getSpendingAmount('hl_spot_cancel'): returns 0n
  </behavior>
  <action>
    1. Create spot-provider.ts following perp-provider.ts pattern exactly:
       - Constructor takes (client, marketData, isMainnet) same as PerpProvider
       - metadata: name='hyperliquid_spot', mcpExpose=true, requiresSigningKey=true, chains=['ethereum']
       - actions array: 3 ActionDefinitions:
         - hl_spot_buy: inputSchema=HlSpotBuyInputSchema, riskLevel='medium', defaultTier='DELAY'
         - hl_spot_sell: inputSchema=HlSpotSellInputSchema, riskLevel='low', defaultTier='INSTANT'
         - hl_spot_cancel: inputSchema=HlSpotCancelInputSchema, riskLevel='low', defaultTier='INSTANT'

    2. resolve() method dispatches to private methods:
       - resolveSpotBuy: get spot meta, find market by name, compute asset index = 10000 + token[1].index (quote token index for the pair). CRITICAL: Hyperliquid Spot asset index is the pair's index from spotMeta universe, NOT 10000+tokenIndex. Use getSpotMeta().universe.findIndex() to get the index, then the actual wire uses the universe entry directly. For market orders, get mid price and apply 3% slippage (buy: *1.03, sell: *0.97). Build orderToWire with the asset index. Sign with signL1Action. Call client.exchange.
       - resolveSpotSell: same logic as buy but side=SELL
       - resolveSpotCancel: same pattern as PerpProvider.resolveCancelOrder but uses spot asset index

    3. getSpendingAmount():
       - hl_spot_buy: size * price (or mid price for market orders). Use parseUsdcAmount helper (copy from perp-provider or extract to shared util).
       - hl_spot_sell: 0n
       - hl_spot_cancel: 0n

    4. Update index.ts:
       - Export HyperliquidSpotProvider from './spot-provider.js'
       - Export new Spot schemas (HlSpotBuyInputSchema, etc.)
       - Export new Spot types (SpotMarketInfo, SpotBalance, SpotMeta, SpotClearinghouseState)

    5. Create __tests__/spot-provider.test.ts:
       - Mock HyperliquidExchangeClient and HyperliquidMarketData
       - Test hl_spot_buy MARKET: verifies signL1Action called, client.exchange called, returns ApiDirectResult with provider='hyperliquid_spot'
       - Test hl_spot_buy LIMIT: verifies price used directly
       - Test hl_spot_sell: verifies side=SELL in wire
       - Test hl_spot_cancel with oid: verifies cancel action wire
       - Test hl_spot_cancel all: verifies open orders fetched and all cancelled
       - Test getSpendingAmount for buy/sell/cancel
       - Test unknown market throws ChainError
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx vitest run packages/actions/src/providers/hyperliquid/__tests__/spot-provider.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>HyperliquidSpotProvider resolves hl_spot_buy/sell/cancel with correct ApiDirectResult. getSpendingAmount returns correct values. Exported from index.ts. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/actions/src/providers/hyperliquid/ --reporter=verbose` -- all Hyperliquid tests pass (existing + new)
- SpotProvider imports compile: `npx tsc --noEmit -p packages/actions/tsconfig.json`
</verification>

<success_criteria>
- HyperliquidSpotProvider implements IActionProvider with 3 actions
- Spot schemas validate input correctly (price required for LIMIT)
- MarketData has typed getSpotMarkets() and getSpotBalances()
- getSpendingAmount returns correct policy amounts
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/350-spot-trading/350-01-SUMMARY.md`
</output>
