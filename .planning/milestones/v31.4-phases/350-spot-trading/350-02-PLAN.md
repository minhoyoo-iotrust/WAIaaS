---
phase: 350-spot-trading
plan: 02
type: execute
wave: 2
depends_on: ["350-01"]
files_modified:
  - packages/daemon/src/api/routes/hyperliquid.ts
  - packages/mcp/src/tools/hyperliquid.ts
  - packages/sdk/src/client.ts
  - packages/admin/src/pages/hyperliquid.tsx
  - packages/admin/src/components/hyperliquid/SpotBalancesTable.tsx
  - packages/admin/src/components/hyperliquid/SpotOrdersTable.tsx
  - packages/daemon/src/api/server.ts
  - packages/daemon/src/infrastructure/action-provider-registry.ts
  - packages/admin/src/pages/actions.tsx
  - packages/daemon/src/api/routes/connect-info.ts
  - skills/transactions.skill.md
autonomous: true
requirements: [HSPOT-01, HSPOT-02, HSPOT-03, HSPOT-04, HSPOT-05, HSPOT-06, HSPOT-07]

must_haves:
  truths:
    - "User can call REST GET /v1/wallets/:id/hyperliquid/spot/balances to see Spot token balances"
    - "User can call REST GET /v1/hyperliquid/spot/markets to see Spot market info"
    - "MCP tools waiaas_hl_get_spot_balances and waiaas_hl_get_spot_markets are registered and callable"
    - "SDK has hlSpotBuy, hlSpotSell, hlSpotCancel, hlGetSpotBalances, hlGetSpotMarkets methods"
    - "Admin UI has Spot tab showing SpotBalancesTable and SpotOrdersTable"
    - "HyperliquidSpotProvider is registered in action-provider-registry"
    - "connect-info shows spot: true in hyperliquid capability"
  artifacts:
    - path: "packages/daemon/src/api/routes/hyperliquid.ts"
      provides: "2 new GET endpoints for Spot balances and markets"
      contains: "spot/balances"
    - path: "packages/mcp/src/tools/hyperliquid.ts"
      provides: "2 new MCP query tools for Spot"
      contains: "waiaas_hl_get_spot_balances"
    - path: "packages/sdk/src/client.ts"
      provides: "5 new SDK methods for Spot"
      contains: "hlSpotBuy"
    - path: "packages/admin/src/components/hyperliquid/SpotBalancesTable.tsx"
      provides: "Spot balances display component"
      min_lines: 30
    - path: "packages/admin/src/pages/hyperliquid.tsx"
      provides: "Spot tab added to Hyperliquid page"
      contains: "spot"
  key_links:
    - from: "packages/mcp/src/tools/hyperliquid.ts"
      to: "packages/daemon/src/api/routes/hyperliquid.ts"
      via: "apiClient.get('/v1/.../spot/balances')"
      pattern: "spot/balances"
    - from: "packages/sdk/src/client.ts"
      to: "packages/daemon/src/api/routes/hyperliquid.ts"
      via: "this.executeAction('hyperliquid_spot', 'hl_spot_buy', ...)"
      pattern: "hyperliquid_spot"
    - from: "packages/daemon/src/infrastructure/action-provider-registry.ts"
      to: "packages/actions/src/providers/hyperliquid/spot-provider.ts"
      via: "registry.register(new HyperliquidSpotProvider(...))"
      pattern: "HyperliquidSpotProvider"
---

<objective>
Wire HyperliquidSpotProvider into all interfaces: REST API query endpoints, MCP tools, SDK methods, Admin UI Spot tab, provider registry, connect-info, and skill files.

Purpose: Make Spot trading accessible to users through every WAIaaS interface.
Output: Full Spot integration across REST/MCP/SDK/Admin/Skills
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/350-spot-trading/350-01-SUMMARY.md

@packages/daemon/src/api/routes/hyperliquid.ts
@packages/mcp/src/tools/hyperliquid.ts
@packages/sdk/src/client.ts
@packages/admin/src/pages/hyperliquid.tsx
@packages/daemon/src/api/server.ts
@packages/daemon/src/infrastructure/action-provider-registry.ts
@packages/admin/src/pages/actions.tsx
@packages/daemon/src/api/routes/connect-info.ts
@skills/transactions.skill.md

<interfaces>
<!-- From Plan 01 output (spot-provider.ts) -->
```typescript
export class HyperliquidSpotProvider implements IActionProvider {
  metadata: { name: 'hyperliquid_spot', mcpExpose: true, requiresSigningKey: true }
  actions: [hl_spot_buy, hl_spot_sell, hl_spot_cancel]
  resolve(actionName, params, context): Promise<ApiDirectResult>
  getSpendingAmount(actionName, params): Promise<{amount: bigint; asset: string}>
}
```

<!-- From Plan 01 output (market-data.ts extensions) -->
```typescript
// New methods on HyperliquidMarketData
async getSpotMarkets(): Promise<SpotMarketInfo[]>
async getSpotBalances(walletAddress: Hex): Promise<SpotBalance[]>
async getSpotMeta(): Promise<SpotMeta>
```

<!-- Existing patterns from Perp integration (349-04) -->
```typescript
// REST: plain Hono routes (not OpenAPIHono)
// MCP: query tools manually registered, action tools auto via mcpExpose
// SDK: action methods use this.executeAction(), query methods use this.get()
// Admin: Preact signals, apiGet() for data fetching
// Registry: registerActionProvider() in server.ts setup
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REST API + MCP tools + SDK methods + Provider registration</name>
  <files>packages/daemon/src/api/routes/hyperliquid.ts, packages/mcp/src/tools/hyperliquid.ts, packages/sdk/src/client.ts, packages/daemon/src/api/server.ts, packages/daemon/src/infrastructure/action-provider-registry.ts, packages/daemon/src/api/routes/connect-info.ts, skills/transactions.skill.md</files>
  <action>
    1. Add 2 REST GET endpoints to hyperliquid.ts (following existing pattern):
       - GET /v1/wallets/:walletId/hyperliquid/spot/balances: calls md.getSpotBalances(address), returns {balances}
       - GET /v1/hyperliquid/spot/markets: calls md.getSpotMarkets(), returns {markets}

    2. Add 2 MCP query tools to hyperliquid.ts (following existing tools pattern):
       - waiaas_hl_get_spot_balances: wallet_id param, calls GET /v1/wallets/:id/hyperliquid/spot/balances
       - waiaas_hl_get_spot_markets: no params, calls GET /v1/hyperliquid/spot/markets

    3. Add 5 SDK methods to client.ts (following existing HL methods pattern):
       - hlSpotBuy(walletId, params): this.executeAction(walletId, 'hyperliquid_spot', 'hl_spot_buy', params)
       - hlSpotSell(walletId, params): this.executeAction(walletId, 'hyperliquid_spot', 'hl_spot_sell', params)
       - hlSpotCancel(walletId, params): this.executeAction(walletId, 'hyperliquid_spot', 'hl_spot_cancel', params)
       - hlGetSpotBalances(walletId): this.get(`/v1/wallets/${walletId}/hyperliquid/spot/balances`)
       - hlGetSpotMarkets(): this.get('/v1/hyperliquid/spot/markets')

    4. Register HyperliquidSpotProvider in action-provider-registry.ts or server.ts setup:
       - Import HyperliquidSpotProvider from @waiaas/actions
       - Create instance with same (client, marketData, isMainnet) as PerpProvider
       - Register via registry.register(spotProvider)

    5. Update connect-info.ts:
       - Add spot: true to hyperliquid capability object

    6. Update skills/transactions.skill.md:
       - Add Spot trading section with examples for hl_spot_buy, hl_spot_sell, hl_spot_cancel
       - Add query examples for hl_get_spot_balances, hl_get_spot_markets
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit -p packages/daemon/tsconfig.json 2>&1 | tail -10 && npx tsc --noEmit -p packages/mcp/tsconfig.json 2>&1 | tail -10 && npx tsc --noEmit -p packages/sdk/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <done>2 REST endpoints serve Spot data. 2 MCP tools registered. 5 SDK methods added. SpotProvider registered in action-provider-registry. connect-info shows spot:true. Skill file updated.</done>
</task>

<task type="auto">
  <name>Task 2: Admin UI Spot tab with SpotBalancesTable and SpotOrdersTable</name>
  <files>packages/admin/src/pages/hyperliquid.tsx, packages/admin/src/components/hyperliquid/SpotBalancesTable.tsx, packages/admin/src/components/hyperliquid/SpotOrdersTable.tsx, packages/admin/src/pages/actions.tsx</files>
  <action>
    1. Create SpotBalancesTable.tsx (Preact component, follow PositionsTable pattern):
       - Props: { walletId: string | null }
       - Fetch from /v1/wallets/${walletId}/hyperliquid/spot/balances using apiGet
       - Display table: Token | Total | Hold | Available (total-hold) | Mid Price | Value
       - Get mid prices from /v1/hyperliquid/markets (allMids) to show USD value
       - Filter out zero-balance tokens
       - Show "No spot balances" empty state
       - Auto-refresh every 10 seconds (same as PositionsTable)

    2. Create SpotOrdersTable.tsx (Preact component, follow OpenOrdersTable pattern):
       - Props: { walletId: string | null }
       - Fetch open orders from /v1/wallets/${walletId}/hyperliquid/orders
       - Filter to show only spot orders (where coin matches spot market names from spotMeta)
       - NOTE: Hyperliquid openOrders endpoint returns both perp and spot orders. The OpenOrdersTable in the Orders tab already shows all. SpotOrdersTable should filter to spot-only by checking if the order's coin matches a spot market pair name (e.g., "HYPE/USDC" format vs perp "ETH" format -- spot pairs contain "/" in their name, or use a spotMarkets lookup).
       - Display: Market | Side | Type | Size | Price | Status | Time
       - Show "No spot orders" empty state

    3. Update hyperliquid.tsx:
       - Add 'spot' to Tab type: type Tab = 'overview' | 'orders' | 'spot' | 'settings'
       - Add Spot tab button to tab bar
       - Add tab content rendering: SpotBalancesTable + SpotOrdersTable when activeTab==='spot'
       - Import SpotBalancesTable and SpotOrdersTable

    4. Update actions.tsx:
       - Add 'hyperliquid_spot' to BUILTIN_PROVIDERS list
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit -p packages/admin/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <done>Admin UI Hyperliquid page has 4 tabs (Overview, Orders, Spot, Settings). Spot tab shows SpotBalancesTable with token balances and SpotOrdersTable with spot-only orders. hyperliquid_spot in BUILTIN_PROVIDERS.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for daemon, mcp, sdk, admin packages
- `npx vitest run packages/actions/src/providers/hyperliquid/` -- all tests pass
- Admin UI builds: `cd packages/admin && npx vite build --mode test`
</verification>

<success_criteria>
- REST API: /v1/wallets/:id/hyperliquid/spot/balances and /v1/hyperliquid/spot/markets return data
- MCP: waiaas_hl_get_spot_balances and waiaas_hl_get_spot_markets tools registered
- SDK: 5 new Spot methods exist (hlSpotBuy/Sell/Cancel/GetSpotBalances/GetSpotMarkets)
- Admin UI: Spot tab with balances table and spot orders table
- connect-info: spot: true in hyperliquid capability
- hyperliquid_spot registered as action provider
- Skill file has Spot examples
</success_criteria>

<output>
After completion, create `.planning/phases/350-spot-trading/350-02-SUMMARY.md`
</output>
