---
phase: 349-core-infra-perp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/hyperliquid/config.ts
  - packages/actions/src/providers/hyperliquid/schemas.ts
  - packages/actions/src/providers/hyperliquid/signer.ts
  - packages/actions/src/providers/hyperliquid/exchange-client.ts
  - packages/actions/src/providers/hyperliquid/market-data.ts
  - packages/actions/src/providers/hyperliquid/index.ts
  - packages/actions/package.json
  - packages/daemon/src/infrastructure/database/migrate.ts
  - packages/daemon/src/infrastructure/database/schema.ts
autonomous: true
requirements: [HPERP-05, HPERP-08, HPERP-09, HPERP-10, HPERP-11, HACCT-01, HACCT-02, HACCT-03]
must_haves:
  truths:
    - "HyperliquidSigner can produce valid EIP-712 signatures for L1 Actions (phantom agent)"
    - "HyperliquidSigner can produce valid EIP-712 signatures for User-Signed Actions"
    - "HyperliquidExchangeClient wraps /exchange and /info endpoints with Zod validation"
    - "HyperliquidRateLimiter enforces weight-based rate limiting"
    - "HyperliquidMarketData provides read-only queries for positions, orders, markets, funding rates"
    - "DB v51 hyperliquid_orders table exists with proper schema"
    - "@msgpack/msgpack dependency is added to packages/actions"
  artifacts:
    - path: "packages/actions/src/providers/hyperliquid/signer.ts"
      provides: "EIP-712 signing for L1 + User-Signed actions"
      exports: ["HyperliquidSigner"]
    - path: "packages/actions/src/providers/hyperliquid/exchange-client.ts"
      provides: "REST client for Exchange + Info API"
      exports: ["HyperliquidExchangeClient"]
    - path: "packages/actions/src/providers/hyperliquid/market-data.ts"
      provides: "Read-only Info API wrapper"
      exports: ["HyperliquidMarketData"]
    - path: "packages/actions/src/providers/hyperliquid/schemas.ts"
      provides: "Zod schemas for all API requests and responses"
    - path: "packages/actions/src/providers/hyperliquid/config.ts"
      provides: "Config constants, Admin Settings keys, defaults"
    - path: "packages/daemon/src/infrastructure/database/migrate.ts"
      provides: "v51 migration for hyperliquid_orders table"
      contains: "hyperliquid_orders"
  key_links:
    - from: "packages/actions/src/providers/hyperliquid/exchange-client.ts"
      to: "packages/actions/src/providers/hyperliquid/schemas.ts"
      via: "Zod schema validation on API responses"
      pattern: "schema\\.parse"
    - from: "packages/actions/src/providers/hyperliquid/signer.ts"
      to: "viem"
      via: "signTypedData for EIP-712"
      pattern: "signTypedData"
    - from: "packages/actions/src/providers/hyperliquid/market-data.ts"
      to: "packages/actions/src/providers/hyperliquid/exchange-client.ts"
      via: "client.info() calls"
      pattern: "this\\.client\\.info"
---

<objective>
Hyperliquid 공유 인프라 (Signer, ExchangeClient, MarketData, Schemas, Config)와 DB v51 마이그레이션을 구현한다.

Purpose: PerpProvider와 SpotProvider가 공유하는 기반 컴포넌트를 먼저 구축하여 이후 Plan 03-05의 기반을 마련한다. 설계 문서 HDESIGN-02/03의 코드 수준 확정을 그대로 구현한다.
Output: HyperliquidSigner, HyperliquidExchangeClient, HyperliquidRateLimiter, HyperliquidMarketData, Zod schemas, DB v51
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/objectives/m31-04-hyperliquid-ecosystem.md

<interfaces>
<!-- Design doc specifies exact code for these components. See HDESIGN-02 and HDESIGN-03 sections. -->

From design doc - HyperliquidSigner:
```typescript
// signL1Action: phantom agent signing with msgpack + keccak256
// signUserSignedAction: direct EIP-712 signing
// Domain for L1: { name: 'Exchange', version: '1', chainId: 1337 }
// Domain for User: { name: 'HyperliquidSignTransaction', version: '1', chainId: 42161/421614 }
```

From design doc - HyperliquidExchangeClient:
```typescript
// POST /exchange: signed trading actions
// POST /info: read-only queries with weight-based rate limiting
// INFO_WEIGHTS: clearinghouseState=20, meta=2, allMids=2, etc.
```

From design doc - DB v51:
```sql
CREATE TABLE hyperliquid_orders (
  id TEXT PRIMARY KEY,
  wallet_id TEXT NOT NULL REFERENCES wallets(id),
  -- 21 columns total, 5 indexes
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HyperliquidSigner + Schemas + Config</name>
  <files>packages/actions/src/providers/hyperliquid/signer.ts, packages/actions/src/providers/hyperliquid/schemas.ts, packages/actions/src/providers/hyperliquid/config.ts, packages/actions/src/providers/hyperliquid/index.ts, packages/actions/package.json, packages/actions/src/providers/hyperliquid/__tests__/signer.test.ts</files>
  <behavior>
    - signL1Action produces {r, s, v} signature for phantom agent with correct domain (Exchange, 1337)
    - signL1Action applies removeTrailingZeros to decimal string fields before msgpack encoding
    - signL1Action msgpack encodes with canonical field order (a, b, p, s, r, t, c for orders)
    - signL1Action correctly handles vaultAddress presence/absence (0x01+20bytes vs 0x00)
    - signUserSignedAction produces signature with correct domain (HyperliquidSignTransaction, 42161 mainnet / 421614 testnet)
    - removeTrailingZeros: "100.00" -> "100", "1.50" -> "1.5", "0.0010" -> "0.001", "10" -> "10"
    - Zod schemas validate correct order inputs and reject invalid ones
    - Config exports correct API URLs, rate limit defaults, Admin Settings keys
  </behavior>
  <action>
    1. Add `@msgpack/msgpack` ^3.0.0 to `packages/actions/package.json` dependencies
    2. Create `packages/actions/src/providers/hyperliquid/config.ts`:
       - Export API URLs (mainnet: https://api.hyperliquid.xyz, testnet: https://api.hyperliquid-testnet.xyz)
       - Export Admin Settings key constants (HYPERLIQUID_ENABLED, HYPERLIQUID_RATE_LIMIT_WEIGHT_PER_MIN, etc.)
       - Export defaults (leverage: 1, margin_mode: 'CROSS', rate_limit: 600)
       - Export INFO_WEIGHTS map (clearinghouseState: 20, meta: 2, allMids: 2, etc.)
       - Export error codes (HL_API_ERROR, HL_RATE_LIMITED, HL_INSUFFICIENT_MARGIN, etc.)
    3. Create `packages/actions/src/providers/hyperliquid/schemas.ts`:
       - Zod schemas for order input: HlOpenPositionInputSchema (market, side, size, price?, orderType, leverage?, reduceOnly?, tif?, triggerPrice?, cloid?, subAccount?)
       - Zod schemas for cancel: HlCancelOrderInputSchema (market, oid? or cloid?, subAccount?)
       - Zod schemas for leverage: HlSetLeverageInputSchema (market/asset, leverage, isCross)
       - Zod schemas for margin mode: HlSetMarginModeInputSchema (market/asset, mode: CROSS|ISOLATED)
       - Zod schemas for USDC transfer: HlTransferUsdcInputSchema (amount, toPerp: boolean)
       - Zod response schemas: ExchangeResponseSchema, ClearinghouseStateSchema, OpenOrderSchema, FillSchema, MarketMetaSchema, FundingRateSchema, AllMidsSchema
       - All schemas follow Zod SSoT rule (Zod -> TypeScript types)
    4. Create `packages/actions/src/providers/hyperliquid/signer.ts`:
       - Class `HyperliquidSigner` with static methods (no state)
       - `signL1Action(action, nonce, isMainnet, privateKey, vaultAddress?)`: phantom agent signing per HDESIGN-02 spec
       - `signUserSignedAction(actionType, action, isMainnet, privateKey)`: user-signed action per HDESIGN-02 spec
       - Helper `removeTrailingZeros(s: string): string`
       - Helper `orderToWire(order)`: canonical field order for msgpack (CRITICAL: must match Python SDK)
       - Uses viem `privateKeyToAccount`, `signTypedData`, `keccak256`, `concat`, `hexToBytes`
       - Uses `@msgpack/msgpack` `encode()` for canonical serialization
    5. Create `packages/actions/src/providers/hyperliquid/index.ts`: re-export all components
    6. Write comprehensive signer tests with known test vectors (generate from the signing spec)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm install && pnpm vitest run packages/actions/src/providers/hyperliquid/__tests__/signer.test.ts</automated>
  </verify>
  <done>HyperliquidSigner produces valid EIP-712 signatures for both L1 and User-Signed actions, schemas validate inputs/responses, config exports all constants</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExchangeClient + RateLimiter + MarketData + DB v51</name>
  <files>packages/actions/src/providers/hyperliquid/exchange-client.ts, packages/actions/src/providers/hyperliquid/market-data.ts, packages/daemon/src/infrastructure/database/migrate.ts, packages/daemon/src/infrastructure/database/schema.ts, packages/actions/src/providers/hyperliquid/__tests__/exchange-client.test.ts, packages/actions/src/providers/hyperliquid/__tests__/market-data.test.ts, packages/daemon/src/__tests__/migration-v51.test.ts</files>
  <behavior>
    - ExchangeClient.exchange() posts to /exchange with signature and validates response via Zod
    - ExchangeClient.info() posts to /info with weight-based rate limiting
    - RateLimiter blocks when weight exceeds maxWeightPerMin, resets after 60s window
    - RateLimiter uses configurable maxWeightPerMin (default 600)
    - MarketData.getPositions() returns parsed position data
    - MarketData.getOpenOrders() returns parsed order data
    - MarketData.getMarkets() returns perp market metadata
    - MarketData.getFundingHistory() returns funding rate data
    - MarketData.getAllMidPrices() returns mid prices
    - MarketData.getAccountState() returns clearinghouse state
    - MarketData.getUserFills() returns trade history
    - DB v51 migration creates hyperliquid_orders table with all columns and indexes
    - DB v51 migration is idempotent (re-run safe)
  </behavior>
  <action>
    1. Create `packages/actions/src/providers/hyperliquid/exchange-client.ts`:
       - Class `HyperliquidRateLimiter` (sliding window, weight-based, configurable maxWeightPerMin)
       - Class `HyperliquidExchangeClient(apiUrl, rateLimiter)`
       - Method `exchange(request)`: POST /exchange, validate with ExchangeResponseSchema, acquire weight=1
       - Method `info<T>(request, schema)`: POST /info, validate with provided Zod schema, acquire weight from INFO_WEIGHTS
       - Private `post<T>(path, body, schema)`: native fetch + AbortController (10s timeout), Zod parse, ChainError on non-200 with HL_API_ERROR/HL_RATE_LIMITED codes
       - Export factory function `createHyperliquidClient(isMainnet, settingsService?)` that reads Admin Settings for URL and rate limit
    2. Create `packages/actions/src/providers/hyperliquid/market-data.ts`:
       - Class `HyperliquidMarketData(client: HyperliquidExchangeClient)`
       - Methods: getPositions(address, subAccount?), getOpenOrders(address, subAccount?), getMarkets(), getFundingHistory(market, startTime), getAllMidPrices(), getAccountState(address), getSpotState(address), getUserFills(address, limit?), getSubAccounts(address)
       - Each method calls `client.info()` with correct `type` string and Zod schema
       - Parses Hyperliquid API responses into clean domain types (Position, OpenOrder, MarketMeta, etc.)
    3. In `packages/daemon/src/infrastructure/database/migrate.ts`:
       - Add v51 migration: CREATE TABLE hyperliquid_orders with all 21 columns per HDESIGN-06
       - Add 5 indexes: idx_hl_orders_wallet, idx_hl_orders_oid, idx_hl_orders_market, idx_hl_orders_status, idx_hl_orders_created
       - Register migration in migrations array
    4. In `packages/daemon/src/infrastructure/database/schema.ts`:
       - Add Drizzle table definition for hyperliquid_orders
    5. Write tests: ExchangeClient with mocked fetch (msw or manual mock), RateLimiter unit tests, MarketData with mocked client, migration v51 test
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/actions/src/providers/hyperliquid/__tests__/exchange-client.test.ts packages/actions/src/providers/hyperliquid/__tests__/market-data.test.ts packages/daemon/src/__tests__/migration-v51.test.ts && pnpm turbo run typecheck --filter=@waiaas/daemon</automated>
  </verify>
  <done>ExchangeClient wraps /exchange and /info with rate limiting and Zod validation, MarketData provides all read-only query methods, DB v51 creates hyperliquid_orders table</done>
</task>

</tasks>

<verification>
1. `pnpm install` succeeds (new @msgpack/msgpack dependency)
2. All signer tests pass with correct EIP-712 signatures
3. ExchangeClient + RateLimiter tests pass
4. MarketData tests pass
5. DB v51 migration test passes
6. `pnpm turbo run typecheck` passes for @waiaas/actions and @waiaas/daemon
</verification>

<success_criteria>
- HyperliquidSigner produces correct L1 Action and User-Signed Action signatures
- HyperliquidExchangeClient wraps /exchange and /info with rate limiting
- HyperliquidMarketData provides all query methods for positions, orders, markets, funding, fills
- DB v51 hyperliquid_orders table created with all 21 columns and 5 indexes
- @msgpack/msgpack added as dependency
- All Zod schemas defined for inputs and API responses
</success_criteria>

<output>
After completion, create `.planning/phases/349-core-infra-perp/349-02-SUMMARY.md`
</output>
