---
phase: 349-core-infra-perp
plan: 04
type: execute
wave: 3
depends_on: [349-03]
files_modified:
  - packages/daemon/src/api/routes/hyperliquid.ts
  - packages/daemon/src/api/routes/index.ts
  - packages/mcp/src/tools/hyperliquid.ts
  - packages/mcp/src/tools/index.ts
  - packages/sdk/src/client.ts
  - packages/sdk/src/types.ts
autonomous: true
requirements: [HPERP-12, HACCT-01, HACCT-02, HACCT-03, HACCT-04, HINT-02]
must_haves:
  truths:
    - "REST API exposes Perp action endpoints and query endpoints"
    - "MCP tools expose 13 Hyperliquid tools (7 action + 6 query)"
    - "SDK client has 13 Hyperliquid methods matching MCP tools 1:1"
    - "connect-info includes hyperliquid capability with perp=true"
    - "Query endpoints bypass pipeline and call MarketData directly"
    - "Action endpoints go through pipeline for policy evaluation"
  artifacts:
    - path: "packages/daemon/src/api/routes/hyperliquid.ts"
      provides: "REST API routes for Hyperliquid Perp actions and queries"
      exports: ["hyperliquidRoutes"]
    - path: "packages/mcp/src/tools/hyperliquid.ts"
      provides: "MCP tool definitions for 13 Hyperliquid operations"
    - path: "packages/sdk/src/client.ts"
      provides: "SDK methods for 13 Hyperliquid operations"
  key_links:
    - from: "packages/daemon/src/api/routes/hyperliquid.ts"
      to: "packages/daemon/src/pipeline/pipeline.ts"
      via: "pipeline.execute() for action endpoints"
      pattern: "pipeline\\.execute"
    - from: "packages/daemon/src/api/routes/hyperliquid.ts"
      to: "packages/actions/src/providers/hyperliquid/market-data.ts"
      via: "MarketData direct call for query endpoints"
      pattern: "marketData\\.get"
    - from: "packages/mcp/src/tools/hyperliquid.ts"
      to: "packages/sdk/src/client.ts"
      via: "SDK client calls from MCP tool handlers"
      pattern: "client\\.hl"
---

<objective>
Hyperliquid Perp 기능을 REST API, MCP 도구, SDK 메서드로 노출하고 connect-info에 hyperliquid capability를 추가한다.

Purpose: Provider만으로는 User가 기능을 사용할 수 없다. 3개 인터페이스(REST/MCP/SDK)로 노출해야 실제 사용이 가능하다.
Output: REST API 라우트, 13 MCP 도구, 13 SDK 메서드, connect-info capability
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/objectives/m31-04-hyperliquid-ecosystem.md
@.planning/phases/349-core-infra-perp/349-03-SUMMARY.md

<interfaces>
<!-- MCP tool list from HDESIGN-05 (Phase 349 scope: 13 tools) -->
Action tools (through pipeline):
- hl_open_position, hl_close_position, hl_place_order, hl_cancel_order
- hl_set_leverage, hl_set_margin_mode, hl_transfer_usdc

Query tools (direct MarketData, no pipeline):
- hl_get_positions, hl_get_open_orders, hl_get_markets
- hl_get_funding_rates, hl_get_account_state, hl_get_trade_history

<!-- SDK method signatures from HDESIGN-05 -->
```typescript
// Perp actions
hlOpenPosition(walletId, params): Promise<ActionResult>
hlClosePosition(walletId, params): Promise<ActionResult>
hlPlaceOrder(walletId, params): Promise<ActionResult>
hlCancelOrder(walletId, params): Promise<ActionResult>
hlSetLeverage(walletId, params): Promise<ActionResult>
hlSetMarginMode(walletId, params): Promise<ActionResult>
hlTransferUsdc(walletId, params): Promise<ActionResult>

// Perp queries
hlGetPositions(walletId): Promise<Position[]>
hlGetOpenOrders(walletId): Promise<OpenOrder[]>
hlGetMarkets(): Promise<Market[]>
hlGetFundingRates(market): Promise<FundingRate[]>
hlGetAccountState(walletId): Promise<AccountState>
hlGetTradeHistory(walletId, limit?): Promise<Fill[]>
```

<!-- connect-info capability -->
```typescript
capabilities.hyperliquid = { enabled: true, perp: true, spot: false, subAccounts: false, networks: ['hyperevm-mainnet'] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REST API routes + connect-info capability</name>
  <files>packages/daemon/src/api/routes/hyperliquid.ts, packages/daemon/src/api/routes/index.ts, packages/daemon/src/api/routes/connect-info.ts, packages/daemon/src/__tests__/hyperliquid-routes.test.ts, packages/daemon/src/__tests__/connect-info-hyperliquid.test.ts</files>
  <action>
    1. Create `packages/daemon/src/api/routes/hyperliquid.ts`:
       - Use existing OpenAPIHono route pattern (check Drift or other DeFi route files for pattern)
       - **Action routes** (POST, through pipeline):
         - POST /v1/wallets/:walletId/hyperliquid/perp/open — hl_open_position
         - POST /v1/wallets/:walletId/hyperliquid/perp/close — hl_close_position
         - POST /v1/wallets/:walletId/hyperliquid/perp/order — hl_place_order
         - DELETE /v1/wallets/:walletId/hyperliquid/perp/order — hl_cancel_order
         - PUT /v1/wallets/:walletId/hyperliquid/perp/leverage — hl_set_leverage
         - PUT /v1/wallets/:walletId/hyperliquid/perp/margin-mode — hl_set_margin_mode
         - POST /v1/wallets/:walletId/hyperliquid/transfer — hl_transfer_usdc
       - **Query routes** (GET, direct MarketData call, no pipeline):
         - GET /v1/wallets/:walletId/hyperliquid/positions — positions
         - GET /v1/wallets/:walletId/hyperliquid/orders — open orders
         - GET /v1/hyperliquid/markets — perp market list (no walletId needed)
         - GET /v1/hyperliquid/funding-rates?market=ETH — funding rates
         - GET /v1/wallets/:walletId/hyperliquid/account — account state (balances + margins)
         - GET /v1/wallets/:walletId/hyperliquid/fills — trade history
       - All routes require sessionAuth middleware
       - Action routes pass through pipeline.execute() with action provider name + action name
       - Query routes call MarketData methods directly using wallet's public address
    2. Register routes in `packages/daemon/src/api/routes/index.ts`
    3. In `packages/daemon/src/api/routes/connect-info.ts`:
       - Add `hyperliquid` capability to connect-info response: `{ enabled: true/false (from HYPERLIQUID_ENABLED setting), perp: true, spot: false, subAccounts: false, networks: ['hyperevm-mainnet'] }`
    4. Write route tests (mock pipeline + mock MarketData) and connect-info test
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/hyperliquid-routes.test.ts packages/daemon/src/__tests__/connect-info-hyperliquid.test.ts</automated>
  </verify>
  <done>REST API routes serve 7 action + 6 query endpoints, connect-info includes hyperliquid capability</done>
</task>

<task type="auto">
  <name>Task 2: MCP tools + SDK methods</name>
  <files>packages/mcp/src/tools/hyperliquid.ts, packages/mcp/src/tools/index.ts, packages/sdk/src/client.ts, packages/sdk/src/types.ts, packages/mcp/src/__tests__/hyperliquid-tools.test.ts, packages/sdk/src/__tests__/hyperliquid-methods.test.ts</files>
  <action>
    1. Create `packages/mcp/src/tools/hyperliquid.ts`:
       - 13 MCP tool definitions following existing pattern (check Jupiter/Drift tools for convention)
       - Action tools (7): hl_open_position, hl_close_position, hl_place_order, hl_cancel_order, hl_set_leverage, hl_set_margin_mode, hl_transfer_usdc
         - Each calls SDK client method, which calls REST API, which goes through pipeline
       - Query tools (6): hl_get_positions, hl_get_open_orders, hl_get_markets, hl_get_funding_rates, hl_get_account_state, hl_get_trade_history
         - Each calls SDK client method, which calls REST API query endpoint directly
       - Each tool has proper inputSchema (matching Zod schemas from Plan 02), description
    2. Register tools in `packages/mcp/src/tools/index.ts`
    3. In `packages/sdk/src/client.ts` add 13 methods:
       - hlOpenPosition(walletId, params), hlClosePosition(walletId, params), hlPlaceOrder(walletId, params)
       - hlCancelOrder(walletId, params), hlSetLeverage(walletId, params), hlSetMarginMode(walletId, params)
       - hlTransferUsdc(walletId, params)
       - hlGetPositions(walletId), hlGetOpenOrders(walletId), hlGetMarkets()
       - hlGetFundingRates(market), hlGetAccountState(walletId), hlGetTradeHistory(walletId, limit?)
       - Each method calls the corresponding REST API endpoint
    4. In `packages/sdk/src/types.ts`: add TypeScript types for params/responses (HlOpenPositionParams, Position, OpenOrder, etc.)
    5. Write MCP tool tests and SDK method tests
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/mcp/src/__tests__/hyperliquid-tools.test.ts packages/sdk/src/__tests__/hyperliquid-methods.test.ts && pnpm turbo run typecheck --filter=@waiaas/mcp --filter=@waiaas/sdk</automated>
  </verify>
  <done>13 MCP tools registered and functional, 13 SDK methods added matching MCP 1:1, all properly typed</done>
</task>

</tasks>

<verification>
1. All route tests pass
2. All MCP tool tests pass
3. All SDK method tests pass
4. connect-info includes hyperliquid capability
5. `pnpm turbo run typecheck` passes for daemon, mcp, sdk
</verification>

<success_criteria>
- REST API: 7 action + 6 query endpoints under /v1/wallets/:walletId/hyperliquid/ and /v1/hyperliquid/
- MCP: 13 tools registered (7 action + 6 query)
- SDK: 13 methods on WAIaaSClient
- connect-info: hyperliquid capability with perp=true
- Action endpoints go through pipeline, query endpoints bypass pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/349-core-infra-perp/349-04-SUMMARY.md`
</output>
