---
phase: 349-core-infra-perp
plan: 05
type: execute
wave: 3
depends_on: [349-03]
files_modified:
  - packages/admin/src/pages/HyperliquidPage.tsx
  - packages/admin/src/pages/HyperliquidPage.test.tsx
  - packages/admin/src/components/hyperliquid/PositionsTable.tsx
  - packages/admin/src/components/hyperliquid/OpenOrdersTable.tsx
  - packages/admin/src/components/hyperliquid/AccountSummary.tsx
  - packages/admin/src/components/hyperliquid/SettingsPanel.tsx
  - packages/admin/src/App.tsx
  - packages/daemon/src/infrastructure/settings/defaults.ts
  - skills/transactions.skill.md
  - skills/admin.skill.md
autonomous: true
requirements: [HPERP-13, HINT-01, HINT-03]
must_haves:
  truths:
    - "Admin UI shows Hyperliquid page under DeFi section with Overview and Orders tabs"
    - "Positions table shows market, side, size, entry, markPrice, PnL, leverage, liquidation"
    - "Open orders table shows market, side, type, size, price, status, created"
    - "Account summary shows Perp equity, margin used, available margin"
    - "Settings panel allows editing HYPERLIQUID_* Admin Settings keys"
    - "Admin Settings defaults include all 9 HYPERLIQUID_* keys"
    - "Skill files updated with Hyperliquid examples"
  artifacts:
    - path: "packages/admin/src/pages/HyperliquidPage.tsx"
      provides: "Main Hyperliquid Admin page with tabs"
      min_lines: 50
    - path: "packages/admin/src/components/hyperliquid/PositionsTable.tsx"
      provides: "Perp positions table component"
    - path: "packages/admin/src/components/hyperliquid/OpenOrdersTable.tsx"
      provides: "Open orders table component"
    - path: "packages/admin/src/components/hyperliquid/AccountSummary.tsx"
      provides: "Account equity and margin summary"
    - path: "packages/admin/src/components/hyperliquid/SettingsPanel.tsx"
      provides: "Hyperliquid Admin Settings editor"
    - path: "packages/daemon/src/infrastructure/settings/defaults.ts"
      provides: "HYPERLIQUID_* default settings registered"
  key_links:
    - from: "packages/admin/src/pages/HyperliquidPage.tsx"
      to: "packages/daemon/src/api/routes/hyperliquid.ts"
      via: "fetch /v1/wallets/:walletId/hyperliquid/* query endpoints"
      pattern: "fetch.*hyperliquid"
    - from: "packages/admin/src/components/hyperliquid/SettingsPanel.tsx"
      to: "packages/daemon/src/api/routes/admin-settings.ts"
      via: "PUT /v1/admin/settings for HYPERLIQUID_* keys"
      pattern: "HYPERLIQUID_"
---

<objective>
Admin UI에 Hyperliquid 포지션/주문 현황 페이지를 추가하고, Admin Settings에 Hyperliquid 설정 키를 등록하고, Skill 파일을 업데이트한다.

Purpose: Admin이 Hyperliquid 포지션과 주문 상태를 모니터링하고 설정을 관리할 수 있어야 한다. Skill 파일 업데이트로 AI 에이전트가 새 기능을 인식할 수 있다.
Output: Admin UI Hyperliquid 페이지, Admin Settings defaults, Skill 파일 업데이트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/objectives/m31-04-hyperliquid-ecosystem.md

<interfaces>
<!-- Admin UI tech stack: Preact 10.x + @preact/signals + Vite 6.x -->
<!-- CSP: default-src 'none', inline styles via style-src 'unsafe-inline' -->
<!-- masterAuth only for Admin UI access -->

<!-- REST API query endpoints (from Plan 04) -->
GET /v1/wallets/:walletId/hyperliquid/positions -> Position[]
GET /v1/wallets/:walletId/hyperliquid/orders -> OpenOrder[]
GET /v1/wallets/:walletId/hyperliquid/account -> AccountState
GET /v1/hyperliquid/markets -> MarketMeta[]

<!-- Admin Settings API -->
GET /v1/admin/settings -> all settings
PUT /v1/admin/settings -> update settings

<!-- HDESIGN-05 Admin Settings keys -->
HYPERLIQUID_ENABLED (boolean, config.toml)
HYPERLIQUID_API_URL (string, config.toml)
HYPERLIQUID_TESTNET_API_URL (string, config.toml)
HYPERLIQUID_RATE_LIMIT_WEIGHT_PER_MIN (number, Admin Settings, default: 600)
HYPERLIQUID_DEFAULT_LEVERAGE (number, Admin Settings, default: 1)
HYPERLIQUID_DEFAULT_MARGIN_MODE (string, Admin Settings, default: 'CROSS')
HYPERLIQUID_BUILDER_ADDRESS (string, Admin Settings, default: '')
HYPERLIQUID_BUILDER_FEE (number, Admin Settings, default: 0)
HYPERLIQUID_ORDER_STATUS_POLL_INTERVAL_MS (number, Admin Settings, default: 2000)

<!-- Admin UI design from HDESIGN-05 -->
DeFi -> Hyperliquid
  Overview Tab: AccountSummary + PositionsTable
  Orders Tab: OpenOrdersTable + OrderHistoryTable
  Settings Tab: Admin Settings editor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Settings defaults + Skill files update</name>
  <files>packages/daemon/src/infrastructure/settings/defaults.ts, skills/transactions.skill.md, skills/admin.skill.md, skills/wallet.skill.md</files>
  <action>
    1. In `packages/daemon/src/infrastructure/settings/defaults.ts`:
       - Add 6 runtime Admin Settings defaults (the 3 config.toml-only keys are handled separately):
         - HYPERLIQUID_RATE_LIMIT_WEIGHT_PER_MIN: 600
         - HYPERLIQUID_DEFAULT_LEVERAGE: 1
         - HYPERLIQUID_DEFAULT_MARGIN_MODE: 'CROSS'
         - HYPERLIQUID_BUILDER_ADDRESS: ''
         - HYPERLIQUID_BUILDER_FEE: 0
         - HYPERLIQUID_ORDER_STATUS_POLL_INTERVAL_MS: 2000
       - Follow existing pattern for registering setting definitions (check existing defaults.ts for convention)
    2. Update `skills/transactions.skill.md`:
       - Add Hyperliquid Perp section with examples:
         - Open a perp position (market order)
         - Place a limit order
         - Set stop-loss
         - Close a position
         - Check positions and PnL
         - Set leverage
       - Include security notice: "AI agents must NEVER request the master password. Use only your session token."
    3. Update `skills/admin.skill.md`:
       - Add Hyperliquid Settings section explaining HYPERLIQUID_* keys
       - Show example of updating rate limit and default leverage via Admin Settings API
    4. Update `skills/wallet.skill.md`:
       - Add note about HyperEVM network support and Hyperliquid DEX trading capability
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon && grep -c "HYPERLIQUID" packages/daemon/src/infrastructure/settings/defaults.ts && grep -c "Hyperliquid" skills/transactions.skill.md</automated>
  </verify>
  <done>6 HYPERLIQUID_* Admin Settings defaults registered, 3 skill files updated with Hyperliquid examples and security notice</done>
</task>

<task type="auto">
  <name>Task 2: Admin UI Hyperliquid page</name>
  <files>packages/admin/src/pages/HyperliquidPage.tsx, packages/admin/src/components/hyperliquid/PositionsTable.tsx, packages/admin/src/components/hyperliquid/OpenOrdersTable.tsx, packages/admin/src/components/hyperliquid/AccountSummary.tsx, packages/admin/src/components/hyperliquid/SettingsPanel.tsx, packages/admin/src/App.tsx, packages/admin/src/__tests__/HyperliquidPage.test.tsx</files>
  <action>
    1. Create `packages/admin/src/components/hyperliquid/AccountSummary.tsx`:
       - Displays Perp equity, total margin used, available margin, margin ratio
       - Fetches from GET /v1/wallets/:walletId/hyperliquid/account
       - Requires wallet selection (use existing wallet selector pattern from Admin UI)
       - Shows margin status with color coding (safe=green, warning=yellow, danger=red)
    2. Create `packages/admin/src/components/hyperliquid/PositionsTable.tsx`:
       - Table columns: Market, Side (LONG/SHORT with color), Size, Entry Price, Mark Price, PnL (green/red), Leverage, Liquidation Price
       - Fetches from GET /v1/wallets/:walletId/hyperliquid/positions
       - Empty state: "No open positions"
       - Auto-refresh every 10 seconds (setInterval)
    3. Create `packages/admin/src/components/hyperliquid/OpenOrdersTable.tsx`:
       - Table columns: Market, Side, Type (MARKET/LIMIT/STOP/TP), Size, Price, Trigger Price, Status, TIF, Created
       - Fetches from GET /v1/wallets/:walletId/hyperliquid/orders
       - Empty state: "No open orders"
    4. Create `packages/admin/src/components/hyperliquid/SettingsPanel.tsx`:
       - Form for editing HYPERLIQUID_* Admin Settings (6 runtime keys)
       - Uses existing Admin Settings API pattern (GET/PUT /v1/admin/settings)
       - Input types: number for rate limit/leverage/fee/poll interval, string for builder address, select for margin mode (CROSS/ISOLATED)
       - Save button with success/error feedback
    5. Create `packages/admin/src/pages/HyperliquidPage.tsx`:
       - Tab layout: Overview (AccountSummary + PositionsTable), Orders (OpenOrdersTable), Settings (SettingsPanel)
       - Wallet selector at top (from existing Admin UI component)
       - Follow existing Admin UI page patterns (Preact + signals)
    6. In `packages/admin/src/App.tsx`:
       - Add route for Hyperliquid page under DeFi section menu
       - Menu item: "Hyperliquid" under DeFi group
    7. Write component tests: render with mock data, verify table rendering, verify empty states
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/admin/src/__tests__/HyperliquidPage.test.tsx && pnpm turbo run typecheck --filter=@waiaas/admin</automated>
  </verify>
  <done>Admin UI shows Hyperliquid page with Overview (positions + account summary), Orders, and Settings tabs, properly fetching from REST API endpoints</done>
</task>

</tasks>

<verification>
1. Admin Settings defaults include all HYPERLIQUID_* keys
2. Skill files include Hyperliquid examples with security notice
3. Admin UI HyperliquidPage renders with tabs
4. PositionsTable shows position data with PnL coloring
5. OpenOrdersTable shows order data
6. SettingsPanel allows editing HYPERLIQUID_* settings
7. `pnpm turbo run typecheck` passes for admin package
</verification>

<success_criteria>
- 6 HYPERLIQUID_* Admin Settings defaults registered
- Skill files updated (transactions, admin, wallet)
- Admin UI Hyperliquid page with 3 tabs (Overview, Orders, Settings)
- Positions table shows market/side/size/entry/mark/PnL/leverage/liquidation
- Open orders table shows market/side/type/size/price/status
- Settings panel edits runtime HYPERLIQUID_* keys
</success_criteria>

<output>
After completion, create `.planning/phases/349-core-infra-perp/349-05-SUMMARY.md`
</output>
