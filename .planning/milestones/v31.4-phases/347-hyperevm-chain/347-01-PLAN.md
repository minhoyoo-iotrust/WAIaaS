---
phase: 347-hyperevm-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/enums/chain.ts
  - packages/core/src/caip/network-map.ts
  - packages/core/src/caip/asset-helpers.ts
  - packages/adapters/evm/src/evm-chain-map.ts
  - packages/core/src/__tests__/enums.test.ts
  - packages/core/src/__tests__/caip.test.ts
  - packages/adapters/evm/src/__tests__/evm-chain-map.test.ts
autonomous: true
requirements:
  - HCHAIN-01
  - HCHAIN-02
  - HCHAIN-03

must_haves:
  truths:
    - "HyperEVM Mainnet (Chain ID 999)이 EVM_CHAIN_MAP에 등록되어 있다"
    - "HyperEVM Testnet (Chain ID 998)이 EVM_CHAIN_MAP에 등록되어 있다"
    - "기존 EVM 지갑이 HyperEVM 네트워크에서 ETH 전송/토큰 전송/컨트랙트 호출이 동작한다"
    - "HyperEVM이 ALLOWED_NETWORKS 정책과 connect-info 네트워크 목록에 표시된다"
  artifacts:
    - path: "packages/core/src/enums/chain.ts"
      provides: "hyperevm-mainnet, hyperevm-testnet in NETWORK_TYPES and EVM_NETWORK_TYPES"
      contains: "hyperevm-mainnet"
    - path: "packages/adapters/evm/src/evm-chain-map.ts"
      provides: "HyperEVM chain entries with viem chain objects"
      contains: "hyperEvm"
    - path: "packages/core/src/caip/network-map.ts"
      provides: "CAIP-2 bidirectional mapping for eip155:999 and eip155:998"
      contains: "eip155:999"
    - path: "packages/core/src/caip/asset-helpers.ts"
      provides: "SLIP-44 coin type for HyperEVM native asset (HYPE)"
      contains: "eip155:999"
  key_links:
    - from: "packages/core/src/enums/chain.ts"
      to: "packages/adapters/evm/src/evm-chain-map.ts"
      via: "EvmNetworkType union includes hyperevm-mainnet/testnet"
      pattern: "hyperevm-mainnet"
    - from: "packages/core/src/caip/network-map.ts"
      to: "packages/core/src/caip/asset-helpers.ts"
      via: "CAIP-2 chain ID used in NATIVE_SLIP44 lookup"
      pattern: "eip155:999"
---

<objective>
HyperEVM Mainnet (Chain ID 999)과 Testnet (Chain ID 998)을 WAIaaS EVM 체인 레지스트리에 등록하여 기존 EVM 지갑이 HyperEVM 네트워크에서 즉시 동작하도록 한다.

Purpose: Phase 347의 유일한 목표 -- 기존 EVM 인프라(전송/토큰/컨트랙트/정책/connect-info)가 HyperEVM에서 자동으로 동작하게 하는 체인 등록 작업.
Output: 7개 파일 수정 (4 source + 3 test), HyperEVM 2개 네트워크 등록 완료.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- viem에서 제공하는 HyperEVM 체인 정의 -->

From node_modules/viem/chains/definitions/hyperEvm.ts:
```typescript
// id: 999, name: 'HyperEVM', nativeCurrency: { name: 'HYPE', symbol: 'HYPE', decimals: 18 }
// rpcUrls.default.http: ['https://rpc.hyperliquid.xyz/evm']
import { hyperEvm } from 'viem/chains';
```

From node_modules/viem/chains/definitions/hyperliquidEvmTestnet.ts:
```typescript
// id: 998, name: 'Hyperliquid EVM Testnet', nativeCurrency: { name: 'HYPE', symbol: 'HYPE', decimals: 18 }
// rpcUrls.default.http: ['https://rpc.hyperliquid-testnet.xyz/evm']
import { hyperliquidEvmTestnet } from 'viem/chains';
```

From packages/core/src/enums/chain.ts:
```typescript
export const NETWORK_TYPES = [
  'solana-mainnet', 'solana-devnet', 'solana-testnet',
  'ethereum-mainnet', 'ethereum-sepolia',
  'polygon-mainnet', 'polygon-amoy',
  'arbitrum-mainnet', 'arbitrum-sepolia',
  'optimism-mainnet', 'optimism-sepolia',
  'base-mainnet', 'base-sepolia',
] as const;

export const EVM_NETWORK_TYPES = [
  'ethereum-mainnet', 'ethereum-sepolia',
  'polygon-mainnet', 'polygon-amoy',
  'arbitrum-mainnet', 'arbitrum-sepolia',
  'optimism-mainnet', 'optimism-sepolia',
  'base-mainnet', 'base-sepolia',
] as const;
```

From packages/adapters/evm/src/evm-chain-map.ts:
```typescript
export interface EvmChainEntry {
  viemChain: Chain;
  chainId: number;
  nativeSymbol: string;
  nativeName: string;
}
export const EVM_CHAIN_MAP: Record<EvmNetworkType, EvmChainEntry> = { ... };
```

From packages/core/src/caip/network-map.ts:
```typescript
export const CAIP2_TO_NETWORK: Record<string, { chain: ChainType; network: NetworkType }> = { ... };
```

From packages/core/src/caip/asset-helpers.ts:
```typescript
const NATIVE_SLIP44: Record<string, number> = { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HyperEVM 네트워크 타입 및 매핑 등록</name>
  <files>
    packages/core/src/enums/chain.ts,
    packages/core/src/caip/network-map.ts,
    packages/core/src/caip/asset-helpers.ts,
    packages/adapters/evm/src/evm-chain-map.ts
  </files>
  <behavior>
    - NETWORK_TYPES에 'hyperevm-mainnet', 'hyperevm-testnet' 포함 (총 15개)
    - EVM_NETWORK_TYPES에 'hyperevm-mainnet', 'hyperevm-testnet' 포함 (총 12개)
    - EvmNetworkTypeEnum.parse('hyperevm-mainnet') 성공
    - ENVIRONMENT_NETWORK_MAP['ethereum:mainnet']에 'hyperevm-mainnet' 포함
    - ENVIRONMENT_NETWORK_MAP['ethereum:testnet']에 'hyperevm-testnet' 포함
    - MAINNET_NETWORKS에 'hyperevm-mainnet' 포함
    - deriveEnvironment('hyperevm-mainnet') === 'mainnet'
    - deriveEnvironment('hyperevm-testnet') === 'testnet'
    - EVM_CHAIN_MAP['hyperevm-mainnet'] = { viemChain: hyperEvm, chainId: 999, nativeSymbol: 'HYPE', nativeName: 'HYPE' }
    - EVM_CHAIN_MAP['hyperevm-testnet'] = { viemChain: hyperliquidEvmTestnet, chainId: 998, nativeSymbol: 'HYPE', nativeName: 'HYPE' }
    - CAIP2_TO_NETWORK['eip155:999'] = { chain: 'ethereum', network: 'hyperevm-mainnet' }
    - CAIP2_TO_NETWORK['eip155:998'] = { chain: 'ethereum', network: 'hyperevm-testnet' }
    - networkToCaip2('hyperevm-mainnet') === 'eip155:999'
    - networkToCaip2('hyperevm-testnet') === 'eip155:998'
    - nativeAssetId('hyperevm-mainnet') === 'eip155:999/slip44:999'
    - nativeAssetId('hyperevm-testnet') === 'eip155:998/slip44:999'
  </behavior>
  <action>
    1. **packages/core/src/enums/chain.ts:**
       - NETWORK_TYPES 배열에 `'hyperevm-mainnet', 'hyperevm-testnet'` 추가 (EVM 섹션 끝에, base-sepolia 다음)
       - EVM_NETWORK_TYPES 배열에 `'hyperevm-mainnet', 'hyperevm-testnet'` 추가 (base-sepolia 다음)
       - ENVIRONMENT_NETWORK_MAP['ethereum:mainnet'] 배열에 `'hyperevm-mainnet'` 추가
       - ENVIRONMENT_NETWORK_MAP['ethereum:testnet'] 배열에 `'hyperevm-testnet'` 추가
       - MAINNET_NETWORKS 배열에 `'hyperevm-mainnet'` 추가

    2. **packages/core/src/caip/network-map.ts:**
       - CAIP2_TO_NETWORK에 두 엔트리 추가:
         - `'eip155:999': { chain: 'ethereum', network: 'hyperevm-mainnet' }`
         - `'eip155:998': { chain: 'ethereum', network: 'hyperevm-testnet' }`

    3. **packages/core/src/caip/asset-helpers.ts:**
       - NATIVE_SLIP44에 두 엔트리 추가:
         - `'eip155:999': 999` (HYPE는 공식 SLIP-44 미등록, chain ID를 프로젝트 내부 coin type으로 사용)
         - `'eip155:998': 999` (testnet도 동일한 coin type)

    4. **packages/adapters/evm/src/evm-chain-map.ts:**
       - import 문에 `hyperEvm, hyperliquidEvmTestnet` 추가 (from 'viem/chains')
       - EVM_CHAIN_MAP에 두 엔트리 추가:
         - `'hyperevm-mainnet': { viemChain: hyperEvm, chainId: 999, nativeSymbol: 'HYPE', nativeName: 'HYPE' }`
         - `'hyperevm-testnet': { viemChain: hyperliquidEvmTestnet, chainId: 998, nativeSymbol: 'HYPE', nativeName: 'HYPE' }`
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/core --filter=@waiaas/evm-adapter 2>&1 | tail -5</automated>
  </verify>
  <done>
    - NETWORK_TYPES 15개, EVM_NETWORK_TYPES 12개
    - EVM_CHAIN_MAP에 hyperevm-mainnet(999), hyperevm-testnet(998) 등록
    - CAIP-2 양방향 매핑 (eip155:999, eip155:998)
    - SLIP-44 native asset 매핑 완료
    - ENVIRONMENT_NETWORK_MAP에 HyperEVM 포함
    - typecheck 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: 테스트 업데이트 및 검증</name>
  <files>
    packages/core/src/__tests__/enums.test.ts,
    packages/core/src/__tests__/caip.test.ts,
    packages/adapters/evm/src/__tests__/evm-chain-map.test.ts
  </files>
  <action>
    1. **packages/core/src/__tests__/enums.test.ts:**
       - `EVM_NETWORK_TYPES has 10 values` -> `12 values`로 수정
       - `NETWORK_TYPES` length assertion도 13 -> 15로 수정 (있다면)
       - `hyperevm-mainnet`, `hyperevm-testnet` toContain 추가
       - EvmNetworkTypeEnum validates `'hyperevm-mainnet'` 성공 테스트 추가

    2. **packages/core/src/__tests__/caip.test.ts:**
       - `nativeAssetId('hyperevm-mainnet')` === `'eip155:999/slip44:999'` 테스트 추가
       - `nativeAssetId('hyperevm-testnet')` === `'eip155:998/slip44:999'` 테스트 추가
       - networkToCaip2/caip2ToNetwork 양방향 테스트 추가 (eip155:999, eip155:998)

    3. **packages/adapters/evm/src/__tests__/evm-chain-map.test.ts:**
       - `has exactly 10 entries` -> `12 entries`로 수정
       - `hyperevm-mainnet: chainId=999, HYPE` 개별 테스트 추가
       - `hyperevm-testnet: chainId=998, HYPE` 개별 테스트 추가
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/core/src/__tests__/enums.test.ts packages/core/src/__tests__/caip.test.ts packages/adapters/evm/src/__tests__/evm-chain-map.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - enums 테스트: NETWORK_TYPES 15개, EVM_NETWORK_TYPES 12개 검증 통과
    - caip 테스트: HyperEVM nativeAssetId 및 CAIP-2 양방향 변환 통과
    - evm-chain-map 테스트: 12개 엔트리, hyperevm-mainnet/testnet 개별 검증 통과
    - 전체 관련 테스트 green
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo run typecheck` -- 전체 타입체크 통과
2. `pnpm vitest run packages/core/src/__tests__/enums.test.ts packages/core/src/__tests__/caip.test.ts packages/adapters/evm/src/__tests__/evm-chain-map.test.ts` -- HyperEVM 관련 테스트 전부 통과
3. ALLOWED_NETWORKS 정책에 'hyperevm-mainnet'/'hyperevm-testnet' 사용 가능 (NetworkTypeEnum 파생)
4. connect-info 네트워크 목록에 HyperEVM 표시 (ENVIRONMENT_NETWORK_MAP 기반 자동 노출)
</verification>

<success_criteria>
- HyperEVM Mainnet (Chain ID 999, HYPE)과 Testnet (Chain ID 998, HYPE)이 등록되어 기존 EVM 파이프라인에서 자동으로 동작
- typecheck 통과, 관련 테스트 전부 green
- CAIP-19 native asset: eip155:999/slip44:999, eip155:998/slip44:999
- ALLOWED_NETWORKS 정책 및 connect-info에서 HyperEVM 네트워크 노출
</success_criteria>

<output>
After completion, create `.planning/phases/347-hyperevm-chain/347-01-SUMMARY.md`
</output>
