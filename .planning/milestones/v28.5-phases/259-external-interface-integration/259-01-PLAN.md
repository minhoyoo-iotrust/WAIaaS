# Plan 259-01: REST API gasCondition OpenAPI + Admin Settings 5 keys + Admin UI Gas Condition Section

---
wave: 1
depends_on: []
requirements: [INTF-01, INTF-02, INTF-03]
files_modified:
  - packages/daemon/src/api/routes/openapi-schemas.ts
  - packages/daemon/src/api/routes/transactions.ts
  - packages/admin/src/pages/system.tsx
  - packages/admin/src/utils/settings-helpers.ts
  - packages/daemon/src/__tests__/rest-api-gas-condition.test.ts
  - packages/admin/src/__tests__/system.test.tsx
autonomous: true

must_haves:
  truths:
    - "OpenAPI /doc 문서에서 TransactionRequest 5-type 모두에 gasCondition 필드가 표시됨"
    - "POST /v1/transactions/send에 gasCondition을 포함한 요청이 정상 통과"
    - "Admin UI System > Settings에 Gas Condition 섹션이 5개 필드를 편집/저장할 수 있음"
    - "isSystemSetting() 함수가 gas_condition. prefix를 인식"
  artifacts:
    - path: "packages/daemon/src/api/routes/openapi-schemas.ts"
      provides: "GasCondition OpenAPI schema + 5-type request schemas에 gasCondition 필드"
      contains: "GasConditionOpenAPI"
    - path: "packages/admin/src/pages/system.tsx"
      provides: "GasConditionSection 컴포넌트"
      contains: "GasConditionSection"
    - path: "packages/admin/src/utils/settings-helpers.ts"
      provides: "gas_condition. prefix 인식"
      contains: "gas_condition."
  key_links:
    - from: "packages/daemon/src/api/routes/openapi-schemas.ts"
      to: "@waiaas/core GasConditionSchema"
      via: "import + OpenAPI mapping"
      pattern: "GasCondition"
    - from: "packages/admin/src/pages/system.tsx"
      to: "packages/admin/src/utils/settings-helpers.ts"
      via: "isSystemSetting() filter"
      pattern: "gas_condition\\."
---

<objective>
REST API `/v1/transactions/send` 엔드포인트에서 `gasCondition` 옵션 필드가 OpenAPI 스키마로 노출되고, Admin Settings에서 `gas_condition` 5개 키를 런타임 변경할 수 있으며, Admin UI System > Settings에 Gas Condition 설정 섹션이 표시된다.

Purpose: 에이전트와 운영자에게 가스 조건부 실행 인터페이스 제공
Output: OpenAPI 스키마 반영, Admin UI Gas Condition 섹션, REST API/Admin 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/daemon/src/api/routes/openapi-schemas.ts
@packages/admin/src/pages/system.tsx
@packages/admin/src/utils/settings-helpers.ts
@packages/daemon/src/api/routes/transactions.ts
</context>

<tasks>

<task id="259-01-T1" type="auto">
  <name>Task 1: Add gasCondition to OpenAPI request schemas</name>
  <files>packages/daemon/src/api/routes/openapi-schemas.ts</files>
  <action>
1. Import GasConditionSchema from `@waiaas/core`
2. Define GasConditionOpenAPI z.object with `.openapi('GasCondition')`:
   - maxGasPrice: z.string().optional() — Max gas price in wei (EVM baseFee+priorityFee)
   - maxPriorityFee: z.string().optional() — Max priority fee in wei (EVM) or micro-lamports (Solana)
   - timeout: z.number().int().min(60).max(86400).optional() — Max wait time in seconds
3. Add `gasCondition: GasConditionOpenAPI.optional()` to all 5 Request OpenAPI schemas:
   - TransferRequestOpenAPI
   - TokenTransferRequestOpenAPI
   - ContractCallRequestOpenAPI
   - ApproveRequestOpenAPI
   - BatchRequestOpenAPI
4. Add to SendTransactionRequestOpenAPI discriminatedUnion members as well (all 5 types)
Note: Actual Zod validation is already handled by `@waiaas/core` TransferRequestSchema etc. with gasConditionField. This is documentation-only.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon exec tsc --noEmit 2>&1 | head -20</automated>
    <manual>GET /doc response includes GasCondition in components/schemas and gasCondition field in all 5 request types</manual>
  </verify>
  <done>OpenAPI /doc JSON contains GasCondition schema definition and all 5 transaction request types include optional gasCondition field</done>
</task>

<task id="259-01-T2" type="auto">
  <name>Task 2: Add Gas Condition section to Admin UI System page</name>
  <files>
    packages/admin/src/pages/system.tsx
    packages/admin/src/utils/settings-helpers.ts
  </files>
  <action>
1. In `settings-helpers.ts`:
   - Add `'gas_condition.'` to the SYSTEM_PREFIXES array in `isSystemSetting()`
   - Add gas_condition keys to `keyToLabel` map: poll_interval_sec -> "Poll Interval (sec)", default_timeout_sec -> "Default Timeout (sec)", max_timeout_sec -> "Max Timeout (sec)", max_pending_count -> "Max Pending Count"

2. In `system.tsx`:
   - Create `GasConditionSection` component following the same pattern as existing sections (OracleSection, SigningSdkSection etc.)
   - Fields:
     a. gas_condition.enabled — select (Yes/No) — "Gas Condition Enabled"
     b. gas_condition.poll_interval_sec — number (min: 10, max: 300) — "Poll Interval (seconds)"
     c. gas_condition.default_timeout_sec — number (min: 60, max: 86400) — "Default Timeout (seconds)"
     d. gas_condition.max_timeout_sec — number (min: 60, max: 86400) — "Max Timeout (seconds)"
     e. gas_condition.max_pending_count — number (min: 1, max: 10000) — "Max Pending Count"
   - Add info-box: "Gas conditional execution allows agents to specify gas price conditions. Transactions wait until gas prices fall below the specified threshold before executing."
   - Place after SigningSDK section, before Danger Zone
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin exec tsc --noEmit 2>&1 | head -20</automated>
    <manual>Admin UI System Settings page shows Gas Condition section with 5 editable fields</manual>
  </verify>
  <done>Admin UI System > Settings renders GasConditionSection with 5 fields; isSystemSetting('gas_condition.enabled') returns true; Save includes gas_condition.* keys</done>
</task>

<task id="259-01-T3" type="auto">
  <name>Task 3: Write REST API gasCondition integration tests</name>
  <files>packages/daemon/src/__tests__/rest-api-gas-condition.test.ts</files>
  <action>
Create new test file with the following test cases:
1. POST /v1/transactions/send with gasCondition in TRANSFER request returns 201 and status=PENDING
2. POST /v1/transactions/send with gasCondition in TOKEN_TRANSFER request returns 201
3. POST /v1/transactions/send without gasCondition works as before (backward compat)
4. gasCondition with only maxGasPrice (no maxPriorityFee) is valid
5. gasCondition with only maxPriorityFee (no maxGasPrice) is valid
6. gasCondition with neither maxGasPrice nor maxPriorityFee fails Zod validation (400)
7. gasCondition.timeout outside range (< 60 or > 86400) fails validation (400)

Use mock chain adapter and mock pipeline stages. Pattern: import createApp from server.ts, use app.request() for HTTP testing. Mock stage functions to avoid actual on-chain execution.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/daemon run test -- --run rest-api-gas-condition 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 REST API gasCondition test cases pass, covering valid/invalid/backward-compat scenarios</done>
</task>

<task id="259-01-T4" type="auto">
  <name>Task 4: Update Admin UI system page tests for Gas Condition section</name>
  <files>packages/admin/src/__tests__/system.test.tsx</files>
  <action>
Add test cases to existing system.test.tsx:
1. Gas Condition section title renders
2. All 5 setting fields are displayed (enabled, poll_interval_sec, default_timeout_sec, max_timeout_sec, max_pending_count)
3. Changing gas_condition.enabled triggers dirty state
4. Save action includes gas_condition.* keys in API request body
5. isSystemSetting('gas_condition.enabled') === true
6. isSystemSetting('gas_condition.poll_interval_sec') === true

Follow existing test patterns in the file (render SystemPage, query by text/role, simulate user interaction with fireEvent).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin run test -- --run system 2>&1 | tail -20</automated>
  </verify>
  <done>All Gas Condition Admin UI tests pass, covering rendering, interaction, and isSystemSetting filter</done>
</task>

</tasks>

<verification>
```yaml
success_criteria:
  - OpenAPI GET /doc에 GasCondition 스키마가 존재하고 5개 요청 타입에 gasCondition 필드가 포함됨
  - Admin UI에 Gas Condition 섹션이 표시되고 5개 필드를 편집/저장할 수 있음
  - REST API gasCondition 통합 테스트 7개 통과
  - Admin UI Gas Condition 테스트 6개 통과
```
</verification>

<success_criteria>
- `pnpm --filter @waiaas/daemon run test -- --run rest-api-gas-condition` passes
- `pnpm --filter @waiaas/admin run test -- --run system` passes
- `pnpm --filter @waiaas/daemon exec tsc --noEmit` passes
- `pnpm --filter @waiaas/admin exec tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/259-external-interface-integration/259-01-SUMMARY.md`
</output>
