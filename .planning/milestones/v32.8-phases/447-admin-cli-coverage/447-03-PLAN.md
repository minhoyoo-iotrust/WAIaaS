---
phase: 447-admin-cli-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - 447-01
  - 447-02
files_modified:
  - packages/cli/src/__tests__/session-coverage.test.ts
  - packages/cli/src/__tests__/set-master-coverage.test.ts
  - packages/cli/src/__tests__/prompt-coverage.test.ts
  - packages/cli/src/__tests__/cli-lines-coverage.test.ts
  - packages/admin/vitest.config.ts
  - packages/cli/vitest.config.ts
autonomous: true
requirements:
  - ADM-04
  - ADM-05
  - ADM-06
  - CLI-01
  - CLI-02
  - CLI-03

must_haves:
  truths:
    - "CLI 인자 파싱 실패, config 미설정, 네트워크 에러 엣지 케이스가 테스트로 검증된다"
    - "admin Lines >= 90%, Branches >= 85%, Functions >= 95% 임계값을 통과한다"
    - "cli Lines >= 90%, Branches >= 85% 임계값을 통과한다"
  artifacts:
    - path: "packages/cli/src/__tests__/session-coverage.test.ts"
      provides: "CLI session.ts 커버리지 테스트"
      min_lines: 50
    - path: "packages/cli/src/__tests__/set-master-coverage.test.ts"
      provides: "CLI set-master.ts 커버리지 테스트"
      min_lines: 50
    - path: "packages/cli/src/__tests__/cli-lines-coverage.test.ts"
      provides: "CLI 나머지 라인/분기 커버리지 테스트"
      min_lines: 100
    - path: "packages/admin/vitest.config.ts"
      provides: "Admin 임계값 인상 (90/85/95)"
      contains: "functions: 95"
    - path: "packages/cli/vitest.config.ts"
      provides: "CLI 임계값 인상 (90/85)"
      contains: "lines: 90"
  key_links:
    - from: "packages/admin/vitest.config.ts"
      to: "packages/admin/src/**/*.tsx"
      via: "coverage thresholds"
      pattern: "thresholds"
    - from: "packages/cli/vitest.config.ts"
      to: "packages/cli/src/**/*.ts"
      via: "coverage thresholds"
      pattern: "thresholds"
---

<objective>
CLI 패키지의 0% 커버리지 파일(session.ts, set-master.ts, prompt.ts)과 낮은 커버리지 파일의 테스트를 추가하고, admin/cli 양쪽 vitest.config.ts 임계값을 목표치로 인상한다.

Purpose: Phase 447 완료 — admin 90/85/95, cli 90/85 임계값 달성 및 적용
Output: CLI 테스트 파일들 + vitest.config.ts 임계값 인상
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/447-admin-cli-coverage/447-01-SUMMARY.md
@.planning/phases/447-admin-cli-coverage/447-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI 0% 커버리지 파일 테스트 + 나머지 라인/분기 커버리지 테스트</name>
  <files>
    packages/cli/src/__tests__/session-coverage.test.ts
    packages/cli/src/__tests__/set-master-coverage.test.ts
    packages/cli/src/__tests__/prompt-coverage.test.ts
    packages/cli/src/__tests__/cli-lines-coverage.test.ts
  </files>
  <read_first>
    - packages/cli/src/commands/session.ts
    - packages/cli/src/commands/set-master.ts
    - packages/cli/src/utils/prompt.ts
    - packages/cli/src/commands/restore.ts
    - packages/cli/src/commands/wallet.ts
    - packages/cli/src/commands/init.ts
    - packages/cli/src/commands/update.ts
    - packages/cli/src/commands/stop.ts
    - packages/cli/src/__tests__/cli-commands.test.ts
    - packages/cli/src/__tests__/restore-cli.test.ts
    - packages/cli/src/__tests__/wallet-coverage.test.ts
    - packages/cli/src/__tests__/update.test.ts
    - packages/cli/vitest.config.ts
  </read_first>
  <action>
**session-coverage.test.ts** — session.ts (현재 0% Lines):
- session.ts의 모든 exported 함수를 확인한다
- 세션 생성(createSession), 조회(listSessions/getSession), 삭제(revokeSession) 명령어 테스트
- fetch/apiCall을 모킹하여 성공/실패 경로 커버
- 인자 파싱 실패(필수 인자 누락) 시 에러 메시지 검증
- config 미설정 시 에러 경로 검증

**set-master-coverage.test.ts** — set-master.ts (현재 0% Lines):
- set-master.ts의 모든 exported 함수를 확인한다
- 마스터 비밀번호 설정/변경 성공 경로 테스트
- 비밀번호 불일치, 빈 비밀번호 등 검증 실패 경로 테스트
- fetch를 모킹하여 API 호출 성공/실패 검증

**prompt-coverage.test.ts** — prompt.ts (현재 11.76% Lines):
- prompt.ts의 유틸리티 함수(promptPassword, confirm 등) 테스트
- readline 모킹으로 사용자 입력 시뮬레이션
- 빈 입력, 취소 등 엣지 케이스

**cli-lines-coverage.test.ts** — restore.ts(76%), wallet.ts(80%), init.ts(81%), update.ts(82%), stop.ts(82%):
- 각 파일의 uncovered lines를 coverage report에서 확인
- restore.ts: 복원 실패 경로(파일 없음, 비밀번호 오류, 무결성 검증 실패)
- wallet.ts: 지갑 생성/삭제/목록 엣지 케이스(네트워크 에러, 잘못된 체인, 중복 이름)
- init.ts: 초기화 실패(디렉토리 권한, config 파일 생성 실패)
- update.ts: 업데이트 확인 실패, 최신 버전 이미 설치됨
- stop.ts: 데몬 중지 실패(PID 파일 없음, 프로세스 미존재)

기존 테스트 패턴을 따라 fetch/fs/readline을 vi.mock으로 모킹한다.
  </action>
  <verify>
    <automated>cd packages/cli && npx vitest run src/__tests__/session-coverage.test.ts src/__tests__/set-master-coverage.test.ts src/__tests__/prompt-coverage.test.ts src/__tests__/cli-lines-coverage.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - session.ts Lines >= 80% (현재 0%)
    - set-master.ts Lines >= 80% (현재 0%)
    - prompt.ts Lines >= 80% (현재 11%)
    - restore.ts Lines >= 85% (현재 76%)
    - wallet.ts Lines >= 85% (현재 80%)
    - cli 전체 Lines >= 90%, Branches >= 85%
    - 테스트가 0 failures로 통과한다
  </acceptance_criteria>
  <done>CLI 0% 파일과 낮은 커버리지 파일의 테스트가 추가되어 전체 Lines >= 90%, Branches >= 85% 달성</done>
</task>

<task type="auto">
  <name>Task 2: admin + cli vitest.config.ts 임계값 인상 + 전체 통과 확인</name>
  <files>
    packages/admin/vitest.config.ts
    packages/cli/vitest.config.ts
  </files>
  <read_first>
    - packages/admin/vitest.config.ts
    - packages/cli/vitest.config.ts
  </read_first>
  <action>
**admin vitest.config.ts 임계값 인상:**
현재: branches: 80, functions: 71, lines: 87, statements: 87
변경: branches: 85, functions: 95, lines: 90, statements: 90

**cli vitest.config.ts 임계값 인상:**
현재: branches: 78, functions: 92, lines: 77, statements: 77
변경: branches: 85, functions: 95, lines: 90, statements: 90

임계값 인상 후 전체 테스트 실행하여 통과 확인:
1. `cd packages/admin && npx vitest run --coverage` — 0 failures, 임계값 통과
2. `cd packages/cli && npx vitest run --coverage` — 0 failures, 임계값 통과

만약 임계값 통과에 실패하면:
- 실패한 메트릭의 현재 값을 확인한다
- 추가 테스트가 필요하면 해당 파일에 테스트를 추가한다
- 임계값은 절대 하향하지 않는다 (CLAUDE.md 규칙)
- 현재 달성치보다 1%p 낮은 값으로 임계값을 설정하되, 최소한 목표치(90/85/95) 이상이어야 한다
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && cd packages/admin && npx vitest run --coverage 2>&1 | grep -E "(All files|FAIL|thresholds)" | head -5 && cd ../cli && npx vitest run --coverage 2>&1 | grep -E "(All files|FAIL|thresholds)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/admin/vitest.config.ts에 functions: 95, lines: 90, branches: 85가 설정됨
    - packages/cli/vitest.config.ts에 lines: 90, branches: 85가 설정됨
    - `cd packages/admin && npx vitest run --coverage` 0 failures + 임계값 통과
    - `cd packages/cli && npx vitest run --coverage` 0 failures + 임계값 통과
    - 기존 임계값보다 낮은 값이 하나도 없음
  </acceptance_criteria>
  <done>admin 90/85/95, cli 90/85/95 임계값이 적용되고 전체 테스트가 통과</done>
</task>

</tasks>

<verification>
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin run test:unit 2>&1 | tail -5 && pnpm --filter @waiaas/cli run test:unit 2>&1 | tail -5
</verification>

<success_criteria>
- CLI session.ts/set-master.ts/prompt.ts의 Lines가 80% 이상 달성
- CLI 전체 Lines >= 90%, Branches >= 85% 달성
- admin vitest.config.ts: lines 90, branches 85, functions 95 설정 및 통과
- cli vitest.config.ts: lines 90, branches 85 설정 및 통과
- 기존 임계값 하향 없음
</success_criteria>

<output>
After completion, create `.planning/phases/447-admin-cli-coverage/447-03-SUMMARY.md`
</output>
