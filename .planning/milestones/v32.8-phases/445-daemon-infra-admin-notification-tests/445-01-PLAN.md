---
phase: 445-daemon-infra-admin-notification-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/__tests__/incoming-tx-monitor-coverage.test.ts
  - packages/daemon/src/__tests__/incoming-tx-workers-coverage.test.ts
  - packages/daemon/src/__tests__/notification-service-coverage.test.ts
  - packages/daemon/src/__tests__/notification-templates-coverage.test.ts
  - packages/daemon/src/__tests__/rpc-proxy-route-coverage.test.ts
autonomous: true
requirements: [DINF-01, DINF-02, DINF-03, DINF-05]

must_haves:
  truths:
    - "IncomingTxMonitor syncSubscriptions, updateConfig, cooldown 로직이 테스트로 검증된다"
    - "IncomingTx workers confirmation/gap-recovery 핸들러가 테스트로 검증된다"
    - "NotificationService broadcast, rate-limit, category filter, locale fallback이 테스트로 검증된다"
    - "Notification 템플릿 금액 포맷팅, 익스플로러 링크 생성이 테스트로 검증된다"
    - "RPC Proxy 라우트 핸들러 인증, 체인 검증, 에러 응답이 테스트로 검증된다"
  artifacts:
    - path: "packages/daemon/src/__tests__/incoming-tx-monitor-coverage.test.ts"
      provides: "IncomingTxMonitor 미커버 경로 테스트"
    - path: "packages/daemon/src/__tests__/notification-service-coverage.test.ts"
      provides: "NotificationService 미커버 분기 테스트"
    - path: "packages/daemon/src/__tests__/rpc-proxy-route-coverage.test.ts"
      provides: "RPC Proxy 라우트 핸들러 테스트"
  key_links:
    - from: "packages/daemon/src/__tests__/incoming-tx-monitor-coverage.test.ts"
      to: "packages/daemon/src/services/incoming/incoming-tx-monitor-service.ts"
      via: "import + mock"
      pattern: "IncomingTxMonitorService"
    - from: "packages/daemon/src/__tests__/notification-service-coverage.test.ts"
      to: "packages/daemon/src/notifications/notification-service.ts"
      via: "import + mock"
      pattern: "NotificationService"
---

<objective>
IncomingTx 모니터, Notification 서비스, RPC Proxy 라우트의 미커버 경로를 테스트로 커버한다.

Purpose: daemon 패키지 90/85/95 달성을 위해 IncomingTx(56줄), Notification(54줄), RPC Proxy route(182줄) 미커버 경로 해소
Output: 5개 테스트 파일, ~292 uncovered lines + ~56 branches 커버
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/444-daemon-defi-pipeline-tests/444-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: IncomingTx Monitor + Workers + Notification 미커버 경로 테스트</name>
  <files>
    packages/daemon/src/__tests__/incoming-tx-monitor-coverage.test.ts
    packages/daemon/src/__tests__/incoming-tx-workers-coverage.test.ts
    packages/daemon/src/__tests__/notification-service-coverage.test.ts
    packages/daemon/src/__tests__/notification-templates-coverage.test.ts
  </files>
  <action>
1. **IncomingTxMonitorService 미커버 경로** (`incoming-tx-monitor-coverage.test.ts`):
   - 기존 테스트: `services/incoming/__tests__/incoming-tx-monitor-service.test.ts` 참조하여 패턴 파악
   - 현재 미커버: syncSubscriptions() 중 DB에서 지갑 로딩 실패 경로, updateConfig() partial merge, cooldown 만료 후 재알림 허용, stop() 중 queue drain 타임아웃
   - IncomingTxMonitorService를 import하고 DB(in-memory SQLite), EventBus, KillSwitchService, NotificationService를 목킹
   - syncSubscriptions()에서 새 지갑 추가/기존 지갑 제거 시 multiplexer subscribe/unsubscribe 호출 검증
   - updateConfig()에서 enabled toggle, pollIntervalSec 변경이 내부 상태에 반영되는지 검증
   - 알림 cooldown: 같은 txHash에 대해 cooldown 이내 중복 알림 억제, cooldown 경과 후 재알림 허용

2. **IncomingTx Workers 미커버 경로** (`incoming-tx-workers-coverage.test.ts`):
   - createConfirmationWorkerHandler: confirmed tx 업데이트 + CONFIRMED 이벤트 발행
   - createGapRecoveryHandler: cursor 이전 블록부터 재스캔, 누락 tx 큐에 추가
   - updateCursor: 정상 업데이트 + DB 에러 시 로깅만 (throw 안함)

3. **NotificationService 미커버 경로** (`notification-service-coverage.test.ts`):
   - 기존 테스트: `__tests__/notification-service.test.ts` 참조
   - 미커버 분기: broadcast 이벤트(KILL_SWITCH_ACTIVATED 등) 모든 채널 동시 전송, rate-limit 초과 시 다음 채널로 fallback
   - category filter: settingsService에서 disabled 카테고리 조회 → 해당 이벤트 무시
   - locale fallback: 지원하지 않는 locale → 'en' 기본값
   - walletNotificationChannel null일 때 side channel 스킵
   - 전체 채널 실패 시 audit_log 기록

4. **Notification 템플릿/포맷 유틸** (`notification-templates-coverage.test.ts`):
   - `notifications/templates/message-templates.ts`: 각 이벤트 타입별 메시지 생성 (TX_CONFIRMED, TX_FAILED, BALANCE_LOW 등)
   - `notifications/channels/format-utils.ts`: 금액 포맷팅, 익스플로러 링크 생성
   - 금액 0, 매우 큰 금액, undefined amount 엣지 케이스
   - 알 수 없는 네트워크에 대한 익스플로러 링크 null 반환
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon exec vitest run src/__tests__/incoming-tx-monitor-coverage.test.ts src/__tests__/incoming-tx-workers-coverage.test.ts src/__tests__/notification-service-coverage.test.ts src/__tests__/notification-templates-coverage.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>IncomingTxMonitor sync/config/cooldown, Workers confirmation/gap-recovery, NotificationService broadcast/rate-limit/category/locale, 템플릿 금액 포맷 테스트 전체 통과</done>
</task>

<task type="auto">
  <name>Task 2: RPC Proxy 라우트 핸들러 미커버 경로 테스트</name>
  <files>
    packages/daemon/src/__tests__/rpc-proxy-route-coverage.test.ts
  </files>
  <action>
1. **RPC Proxy 라우트** (`rpc-proxy-route-coverage.test.ts`):
   - 소스: `api/routes/rpc-proxy.ts` (405줄, 29.7% 커버 → 182줄 미커버)
   - 기존 테스트: `__tests__/rpc-proxy/rpc-proxy-route.test.ts` 참조하여 패턴 파악
   - 미커버 경로 (가장 큰 단일 파일 갭):
     a. POST /v1/rpc-evm/:walletId/:chainId 인증 실패 (세션 토큰 없음 → 401)
     b. walletId 존재하지 않는 경우 → 404
     c. chainId 미지원 → 400 에러
     d. JSON-RPC 2.0 파싱 실패 (invalid JSON body) → 에러 응답
     e. 배치 요청 (JSON array) 처리 → 배치 응답
     f. eth_sendTransaction 인터셉트 → pipeline 경유 → 결과 반환
     g. eth_signTransaction 인터셉트 → sign-only pipeline
     h. passthrough 메서드 (eth_blockNumber 등) → 원본 RPC로 전달
     i. CompletionWaiter 타임아웃 → 에러 응답
     j. RPC upstream 에러 전파
   - Hono app을 직접 생성하거나 createApiServer 활용, AdapterPool/SessionManager/PipelineRunner를 목킹
   - 각 경로에 대해 실제 HTTP 요청 없이 Hono의 app.request() 패턴으로 테스트
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon exec vitest run src/__tests__/rpc-proxy-route-coverage.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>RPC Proxy 라우트 인증/체인검증/JSON-RPC파싱/배치/인터셉트/패스스루/타임아웃 경로 테스트 전체 통과, rpc-proxy.ts 커버리지 70%+ 달성</done>
</task>

</tasks>

<verification>
전체 테스트 통과:
```bash
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter daemon exec vitest run src/__tests__/incoming-tx-monitor-coverage.test.ts src/__tests__/incoming-tx-workers-coverage.test.ts src/__tests__/notification-service-coverage.test.ts src/__tests__/notification-templates-coverage.test.ts src/__tests__/rpc-proxy-route-coverage.test.ts --reporter=verbose
```
</verification>

<success_criteria>
1. IncomingTxMonitor syncSubscriptions, updateConfig, cooldown 로직 테스트 통과
2. IncomingTx workers confirmation/gap-recovery 핸들러 테스트 통과
3. NotificationService broadcast/rate-limit/category-filter/locale 분기 테스트 통과
4. Notification 템플릿 금액 포맷/익스플로러 링크 엣지 케이스 테스트 통과
5. RPC Proxy 라우트 인증/체인검증/배치/인터셉트/패스스루 경로 테스트 통과
6. 신규 테스트 전체 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/445-daemon-infra-admin-notification-tests/445-01-SUMMARY.md`
</output>
