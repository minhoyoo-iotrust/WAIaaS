---
phase: 444-daemon-defi-pipeline-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/lido-staking/__tests__/lido-staking-provider.test.ts
  - packages/actions/src/providers/jito-staking/__tests__/jito-staking-provider.test.ts
  - packages/actions/src/providers/aave-v3/__tests__/aave-v3-provider.test.ts
autonomous: true
requirements:
  - DDEFI-04
  - DDEFI-05

must_haves:
  truths:
    - "Lido stake 성공 시 stETH 컨트랙트를 target으로 하는 ContractCallRequest가 반환된다"
    - "Lido unstake 성공 시 approve + requestWithdrawals 2개 ContractCallRequest가 반환된다"
    - "Jito stake 성공 시 SOL → JitoSOL 변환 ContractCallRequest가 반환된다"
    - "Jito 최소 입금액 미만 시 ChainError가 throw된다"
    - "Aave supply/borrow/repay/withdraw 각 액션이 올바른 ContractCallRequest를 반환한다"
    - "Aave health factor 위험 수준에서 borrow 시 경고가 포함된다"
  artifacts:
    - path: "packages/actions/src/providers/lido-staking/__tests__/lido-staking-provider.test.ts"
      provides: "Lido Staking Provider deposit/withdraw 테스트"
      min_lines: 120
    - path: "packages/actions/src/providers/jito-staking/__tests__/jito-staking-provider.test.ts"
      provides: "Jito Staking Provider deposit/withdraw 테스트"
      min_lines: 100
    - path: "packages/actions/src/providers/aave-v3/__tests__/aave-v3-provider.test.ts"
      provides: "Aave V3 Lending Provider supply/borrow/repay/withdraw 테스트"
      min_lines: 200
  key_links:
    - from: "lido-staking-provider.test.ts"
      to: "packages/actions/src/providers/lido-staking/index.ts"
      via: "LidoStakingActionProvider 인스턴스 직접 테스트"
      pattern: "new LidoStakingActionProvider"
    - from: "aave-v3-provider.test.ts"
      to: "packages/actions/src/providers/aave-v3/index.ts"
      via: "AaveV3LendingProvider 인스턴스 직접 테스트"
      pattern: "new AaveV3LendingProvider"
---

<objective>
Lido+Jito Staking, Aave V3 Lending 3개 DeFi Provider의 resolve 성공/실패 경로를 목킹 기반 단위 테스트로 커버한다.

Purpose: Staking/Lending 프로바이더는 컨트랙트 인코딩과 다단계 트랜잭션(approve + 실행)이 복잡하여 단위 테스트 부재 시 리그레션 위험이 높다.
Output: 3개 테스트 파일 (lido-staking, jito-staking, aave-v3 프로바이더)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/actions/src/providers/lido-staking/index.ts
@packages/actions/src/providers/lido-staking/lido-contract.ts
@packages/actions/src/providers/lido-staking/config.ts
@packages/actions/src/providers/lido-staking/withdrawal-tracker.ts
@packages/actions/src/providers/jito-staking/index.ts
@packages/actions/src/providers/jito-staking/jito-stake-pool.ts
@packages/actions/src/providers/jito-staking/config.ts
@packages/actions/src/providers/aave-v3/index.ts
@packages/actions/src/providers/aave-v3/aave-contracts.ts
@packages/actions/src/providers/aave-v3/aave-rpc.ts
@packages/actions/src/providers/aave-v3/config.ts
@packages/actions/src/providers/aave-v3/schemas.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lido + Jito Staking Provider 단위 테스트</name>
  <files>
    packages/actions/src/providers/lido-staking/__tests__/lido-staking-provider.test.ts
    packages/actions/src/providers/jito-staking/__tests__/jito-staking-provider.test.ts
  </files>
  <behavior>
    Lido Staking Provider:
    - resolve('stake', {amount}) 성공 시 stETH 컨트랙트 target의 ContractCallRequest 반환, value에 stake 금액 포함
    - resolve('stake', {humanAmount, decimals}) humanAmount 변환 후 정상 resolve
    - resolve('unstake', {amount}) 성공 시 [approve, requestWithdrawals] 2개 ContractCallRequest 반환
    - amount=0 또는 빈 문자열 시 입력 검증 에러
    - 메인넷/테스트넷(Holesky) 주소 분기 검증 (config 기반)
    - metadata.name === 'lido_staking', chains === ['ethereum']
    - getPositions 호출 시 PositionUpdate[] 반환 (stETH 잔액 기반)

    Jito Staking Provider:
    - resolve('stake', {amount}) 성공 시 depositSol ContractCallRequest 반환
    - resolve('stake', {amount}) 최소 입금액(JITO_MIN_DEPOSIT_LAMPORTS) 미만 시 ChainError
    - resolve('unstake', {amount}) 성공 시 withdrawSol ContractCallRequest 반환
    - resolve('stake', {humanAmount, decimals}) humanAmount 변환 정상 동작
    - metadata.name === 'jito_staking', chains === ['solana']
    - getPositions 호출 시 PositionUpdate[] 반환
  </behavior>
  <action>
    1. `packages/actions/src/providers/lido-staking/__tests__/` 디렉토리 생성 후 `lido-staking-provider.test.ts` 작성.
       - lido-contract.ts의 buildSubmitCalldata, buildApproveCalldata, buildRequestWithdrawalsCalldata를 vi.mock()으로 목킹.
       - stake 성공: ContractCallRequest.target이 config의 stETH 주소, value가 stake 금액 문자열.
       - unstake 성공: 2개 요소 배열 — [0]이 approve (target=stETH), [1]이 requestWithdrawals (target=withdrawalQueue).
       - humanAmount: decimals=18, humanAmount="1.0" → amount="1000000000000000000".
       - 주소 분기: mainnet config vs holesky config에서 다른 컨트랙트 주소 사용 확인.

    2. `packages/actions/src/providers/jito-staking/__tests__/` 디렉토리 생성 후 `jito-staking-provider.test.ts` 작성.
       - jito-stake-pool.ts의 buildDepositSolRequest, buildWithdrawSolRequest, getJitoSolBalance, getStakePoolExchangeRate를 vi.mock()으로 목킹.
       - stake 성공: ContractCallRequest 반환, programId가 config의 stakePoolProgramId.
       - stake 최소 금액 미만: amount < JITO_MIN_DEPOSIT_LAMPORTS → ChainError (메시지에 "minimum" 포함).
       - unstake 성공: withdrawSol ContractCallRequest 반환.
       - humanAmount: decimals=9, humanAmount="2.0" → amount="2000000000".
  </action>
  <verify>
    <automated>cd packages/actions && npx vitest run src/providers/lido-staking/__tests__/lido-staking-provider.test.ts src/providers/jito-staking/__tests__/jito-staking-provider.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Lido stake/unstake/humanAmount/주소분기 6+ 테스트 통과, Jito stake/unstake/최소금액/humanAmount 5+ 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aave V3 Lending Provider 단위 테스트</name>
  <files>
    packages/actions/src/providers/aave-v3/__tests__/aave-v3-provider.test.ts
  </files>
  <behavior>
    - resolve('supply', {asset, amount}) 성공 시 approve + supply 2개 ContractCallRequest 반환
    - resolve('borrow', {asset, amount, interestRateMode}) 성공 시 borrow ContractCallRequest 반환
    - resolve('repay', {asset, amount}) 성공 시 approve + repay 2개 ContractCallRequest 반환
    - resolve('repay', {asset, amount: 'max'}) 시 MAX_UINT256 사용
    - resolve('withdraw', {asset, amount}) 성공 시 withdraw ContractCallRequest 반환
    - resolve('withdraw', {asset, amount: 'max'}) 시 MAX_UINT256 사용
    - 미지원 체인(예: 'solana') 시 ChainError throw
    - humanAmount + decimals 변환 정상 동작
    - health factor 관련: getPositions 시 PositionUpdate에 healthFactor 포함
    - metadata.name === 'aave_v3', chains === ['ethereum']
  </behavior>
  <action>
    1. `packages/actions/src/providers/aave-v3/__tests__/` 디렉토리 생성 후 `aave-v3-provider.test.ts` 작성.
       - aave-contracts.ts의 ABI 인코딩 함수들과 aave-rpc.ts의 RPC 호출 함수들을 vi.mock()으로 목킹.
       - 기존 `aave-v3.test.ts`는 migration 전용이므로 별도 파일로 분리. 기존 파일 수정하지 않음.

    2. supply 테스트:
       - 성공: [approve(asset → Pool), supply(Pool)] 2개 반환. approve.target이 asset, supply.target이 Pool 주소.
       - humanAmount: decimals=18, humanAmount="100" → amount 변환 후 approve+supply.

    3. borrow 테스트:
       - 성공: interestRateMode=2(variable) → borrow ContractCallRequest 1개 반환.
       - 미지정 interestRateMode → 기본값 2(variable) 사용.

    4. repay 테스트:
       - 일반: approve + repay 2개 반환.
       - max: amount='max' → repay의 amount가 MAX_UINT256.

    5. withdraw 테스트:
       - 일반: withdraw 1개 반환.
       - max: amount='max' → withdraw의 amount가 MAX_UINT256.

    6. 에러 경로:
       - context.chain='solana' → ChainError (EVM 전용).
       - 미지원 asset → 적절한 에러.

    7. getPositions 테스트:
       - aave-rpc.ts getUserAccountData 목킹 → PositionUpdate[] 반환 확인.
       - healthFactor 필드 포함 확인.
  </action>
  <verify>
    <automated>cd packages/actions && npx vitest run src/providers/aave-v3/__tests__/aave-v3-provider.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Aave supply/borrow/repay/withdraw 각 성공 경로 + max 키워드 + 에러 경로 + positions 총 10+ 테스트 통과</done>
</task>

</tasks>

<verification>
cd packages/actions && npx vitest run src/providers/lido-staking/__tests__/ src/providers/jito-staking/__tests__/ src/providers/aave-v3/__tests__/ --reporter=verbose
</verification>

<success_criteria>
- Lido/Jito Staking Provider의 deposit/withdraw 성공/실패 경로가 테스트로 검증된다
- Aave V3 Lending Provider의 supply/borrow/repay/withdraw + max 키워드가 테스트로 검증된다
- 총 20+ 개 테스트 케이스가 모두 통과한다
- 기존 테스트가 깨지지 않는다 (`cd packages/actions && pnpm test:unit` 통과)
</success_criteria>

<output>
After completion, create `.planning/phases/444-daemon-defi-pipeline-tests/444-02-SUMMARY.md`
</output>
