---
phase: 444-daemon-defi-pipeline-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/actions/src/providers/jupiter-swap/__tests__/jupiter-swap-provider.test.ts
  - packages/actions/src/providers/zerox-swap/__tests__/zerox-swap-provider.test.ts
  - packages/actions/src/providers/lifi/__tests__/lifi-provider.test.ts
  - packages/actions/src/providers/lifi/__tests__/lifi-api-client.test.ts
autonomous: true
requirements:
  - DDEFI-01
  - DDEFI-02
  - DDEFI-03

must_haves:
  truths:
    - "Jupiter resolve 성공 시 JUPITER_PROGRAM_ID를 target으로 하는 ContractCallRequest가 반환된다"
    - "Jupiter resolve 실패 시 ChainError가 throw되고 에러 메시지가 유의미하다"
    - "0x resolve 성공 시 ERC-20은 [approve, swap] 2개, native ETH는 [swap] 1개 ContractCallRequest를 반환한다"
    - "0x chainId 매핑이 올바르고 미지원 체인에서 에러가 발생한다"
    - "LiFi cross_swap/bridge resolve 성공 시 올바른 ContractCallRequest가 반환된다"
    - "LiFi API 타임아웃 시 ChainError가 throw된다"
  artifacts:
    - path: "packages/actions/src/providers/jupiter-swap/__tests__/jupiter-swap-provider.test.ts"
      provides: "Jupiter Swap Provider resolve/execute 성공/실패 경로 테스트"
      min_lines: 120
    - path: "packages/actions/src/providers/zerox-swap/__tests__/zerox-swap-provider.test.ts"
      provides: "0x Swap Provider 견적 조회, 체인 매핑, hex→decimal 변환 테스트"
      min_lines: 120
    - path: "packages/actions/src/providers/lifi/__tests__/lifi-provider.test.ts"
      provides: "LiFi Bridge Provider 크로스체인 라우트, 토큰 주소 매핑 테스트"
      min_lines: 100
  key_links:
    - from: "jupiter-swap-provider.test.ts"
      to: "packages/actions/src/providers/jupiter-swap/index.ts"
      via: "JupiterSwapActionProvider 인스턴스 직접 테스트"
      pattern: "new JupiterSwapActionProvider"
    - from: "zerox-swap-provider.test.ts"
      to: "packages/actions/src/providers/zerox-swap/index.ts"
      via: "ZeroExSwapActionProvider 인스턴스 직접 테스트"
      pattern: "new ZeroExSwapActionProvider"
    - from: "lifi-provider.test.ts"
      to: "packages/actions/src/providers/lifi/index.ts"
      via: "LiFiActionProvider 인스턴스 직접 테스트"
      pattern: "new LiFiActionProvider"
---

<objective>
Jupiter, 0x, LiFi 3개 DeFi Provider의 resolve 성공/실패 경로를 목킹 기반 단위 테스트로 커버한다.

Purpose: 현재 이 3개 프로바이더에 단위 테스트가 없어 커버리지 갭이 존재한다. API 클라이언트를 목킹하고 Provider의 resolve 로직을 검증한다.
Output: 3개 테스트 파일 (jupiter-swap, zerox-swap, lifi 프로바이더)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/actions/src/providers/jupiter-swap/index.ts
@packages/actions/src/providers/jupiter-swap/jupiter-api-client.ts
@packages/actions/src/providers/jupiter-swap/schemas.ts
@packages/actions/src/providers/jupiter-swap/config.ts
@packages/actions/src/providers/zerox-swap/index.ts
@packages/actions/src/providers/zerox-swap/zerox-api-client.ts
@packages/actions/src/providers/zerox-swap/config.ts
@packages/actions/src/providers/zerox-swap/schemas.ts
@packages/actions/src/providers/lifi/index.ts
@packages/actions/src/providers/lifi/lifi-api-client.ts
@packages/actions/src/providers/lifi/config.ts
@packages/actions/src/providers/lifi/schemas.ts
@packages/actions/src/providers/lifi/bridge-status-tracker.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Jupiter + 0x Swap Provider 단위 테스트</name>
  <files>
    packages/actions/src/providers/jupiter-swap/__tests__/jupiter-swap-provider.test.ts
    packages/actions/src/providers/zerox-swap/__tests__/zerox-swap-provider.test.ts
  </files>
  <behavior>
    Jupiter Swap Provider:
    - resolve('swap', {inputMint, outputMint, amount}) 호출 시 JupiterApiClient.getQuote + getSwapInstructions 목킹 → ContractCallRequest 반환, target이 JUPITER_PROGRAM_ID
    - resolve('swap', {inputMint, outputMint, humanAmount, decimals}) 호출 시 humanAmount → smallest unit 변환 후 정상 resolve
    - API getQuote 실패 시 ChainError throw (에러 메시지에 "quote" 포함)
    - API getSwapInstructions 실패 시 ChainError throw
    - slippageBps 미지정 시 기본값(50) 사용, 지정 시 clamp 적용 (0-1000)
    - metadata.name === 'jupiter_swap', chains === ['solana']

    0x Swap Provider:
    - resolve('swap', {sellToken, buyToken, sellAmount}) ERC-20 매도 시 [approve, swap] 2개 반환
    - resolve('swap', {sellToken: NATIVE_ETH, buyToken, sellAmount}) native ETH 매도 시 [swap] 1개 반환
    - resolve('swap', {..., humanSellAmount, decimals}) humanAmount 변환 정상 동작
    - CHAIN_ID_MAP에 없는 체인에서 ChainError throw (에러 메시지에 "unsupported chain" 포함)
    - API 견적 조회 실패 시 ChainError throw
    - hex amount(0x prefix) → decimal 변환 정상 동작 (0x 응답 파싱)
    - metadata.name === 'zerox_swap', chains === ['ethereum']
  </behavior>
  <action>
    1. `packages/actions/src/providers/jupiter-swap/__tests__/` 디렉토리 생성 후 `jupiter-swap-provider.test.ts` 작성.
       - JupiterApiClient를 vi.mock()으로 전체 목킹. getQuote/getSwapInstructions가 미리 정의된 응답 반환.
       - QuoteResponse/SwapInstructionsResponse는 schemas.ts의 실제 Zod 스키마에 맞는 fixture 사용.
       - 성공 경로: resolve 반환값이 ContractCallRequest[] 타입, target이 JUPITER_PROGRAM_ID, data가 base64 디코딩 가능.
       - 실패 경로: getQuote throw → ChainError 전파 확인. getSwapInstructions throw → ChainError 전파 확인.
       - humanAmount 경로: decimals=9, humanAmount="1.5" → amount="1500000000" 변환 확인.
       - slippage 경로: slippageBps=2000 → clamp 1000, slippageBps=undefined → default 50.

    2. `packages/actions/src/providers/zerox-swap/__tests__/` 디렉토리 생성 후 `zerox-swap-provider.test.ts` 작성.
       - ZeroExApiClient를 vi.mock()으로 전체 목킹.
       - ERC-20 매도: approve + swap 2개 ContractCallRequest 반환. approve의 target이 sellToken, swap의 target이 API 응답의 to 필드.
       - Native ETH: NATIVE_ETH_ADDRESS(0xeee...eee) 사용 시 approve 없이 swap 1개만 반환.
       - 체인 매핑: context.chain='base' → chainId 8453, context.chain='unsupported' → ChainError.
       - hex→decimal: API 응답의 value 필드가 hex인 경우 BigInt 변환 검증.
       - humanSellAmount: decimals=18, humanSellAmount="1.0" → sellAmount="1000000000000000000".
  </action>
  <verify>
    <automated>cd packages/actions && npx vitest run src/providers/jupiter-swap/__tests__/jupiter-swap-provider.test.ts src/providers/zerox-swap/__tests__/zerox-swap-provider.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Jupiter resolve 성공/실패/humanAmount/slippage 경로 6+ 테스트 통과, 0x ERC-20/native/체인매핑/hex변환 경로 7+ 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LiFi Bridge Provider + API Client 단위 테스트</name>
  <files>
    packages/actions/src/providers/lifi/__tests__/lifi-provider.test.ts
    packages/actions/src/providers/lifi/__tests__/lifi-api-client.test.ts
  </files>
  <behavior>
    LiFi Provider:
    - resolve('cross_swap', {fromChain, toChain, fromToken, toToken, fromAmount}) 성공 시 ContractCallRequest 반환
    - resolve('bridge', {fromChain, toChain, fromToken, toToken, fromAmount}) 성공 시 ContractCallRequest 반환 (cross_swap과 동일 로직)
    - resolve 시 humanFromAmount + decimals → smallest unit 변환 정상 동작
    - 미지원 체인 fromChain → ChainError (getLiFiChainId 실패)
    - API 타임아웃/에러 → ChainError 전파
    - slippage 기본값(0.03) 사용, 범위 초과 시 clamp
    - metadata.name === 'lifi', chains === ['ethereum', 'solana']

    LiFi API Client:
    - getQuote 성공 시 Zod 검증 통과한 응답 반환
    - getQuote 네트워크 에러 시 에러 전파
  </behavior>
  <action>
    1. `packages/actions/src/providers/lifi/__tests__/` 디렉토리 생성 후 `lifi-provider.test.ts` 작성.
       - LiFiApiClient를 vi.mock()으로 전체 목킹. getQuote가 fixture 응답 반환.
       - cross_swap 성공: fromChain='ethereum', toChain='base' → ContractCallRequest with transactionRequest.
       - bridge 성공: 동일 로직이므로 다른 체인 조합(solana → ethereum)으로 테스트.
       - humanFromAmount: decimals=6, humanFromAmount="100" → fromAmount="100000000" 변환.
       - 미지원 체인: fromChain='unsupported' → ChainError (메시지에 "chain" 포함).
       - API 에러: getQuote throw → ChainError 전파.
       - slippage: 미지정 시 기본값 0.03 사용 확인 (getQuote 호출 인자 검증).
       - toAddress: 미지정 시 context.walletAddress 사용, 지정 시 override.

    2. `lifi-api-client.test.ts` 작성 (선택적, bridge-status-tracker.ts도 커버 가능하면 추가).
       - global fetch 목킹으로 getQuote 성공/실패 테스트.
       - Zod 스키마 검증 실패 시 에러 전파 확인.
  </action>
  <verify>
    <automated>cd packages/actions && npx vitest run src/providers/lifi/__tests__/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>LiFi cross_swap/bridge resolve 성공/humanAmount/미지원체인/API에러/slippage 경로 7+ 테스트 통과, API client 2+ 테스트 통과</done>
</task>

</tasks>

<verification>
cd packages/actions && npx vitest run src/providers/jupiter-swap/__tests__/ src/providers/zerox-swap/__tests__/ src/providers/lifi/__tests__/ --reporter=verbose
</verification>

<success_criteria>
- Jupiter/0x/LiFi 각 Provider의 성공 및 실패 경로가 목킹 기반 테스트로 검증된다
- 총 20+ 개 테스트 케이스가 모두 통과한다
- 기존 테스트가 깨지지 않는다 (`cd packages/actions && pnpm test:unit` 통과)
</success_criteria>

<output>
After completion, create `.planning/phases/444-daemon-defi-pipeline-tests/444-01-SUMMARY.md`
</output>
