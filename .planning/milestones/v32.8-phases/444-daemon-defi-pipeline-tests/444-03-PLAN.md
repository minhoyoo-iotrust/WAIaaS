---
phase: 444-daemon-defi-pipeline-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/__tests__/pipeline-delay-reentry.test.ts
  - packages/daemon/src/__tests__/pipeline-gas-waiting-reentry.test.ts
  - packages/daemon/src/__tests__/pipeline-sign-timeout.test.ts
  - packages/daemon/src/__tests__/pipeline-gas-estimation-error.test.ts
  - packages/daemon/src/__tests__/gas-condition-executor.test.ts
autonomous: true
requirements:
  - DPIPE-01
  - DPIPE-02
  - DPIPE-03
  - DPIPE-04
  - DPIPE-05

must_haves:
  truths:
    - "DELAY 재진입 시 stage1에서 직렬화된 원본 request가 stage5에서 복원되어 올바른 트랜잭션 타입으로 빌드된다"
    - "GAS_WAITING 재진입 시 원본 request가 보존되고 가스 조건 충족 후 stage5가 정상 실행된다"
    - "서명 타임아웃 발생 시 트랜잭션이 FAILED 상태로 전이되고 TX_FAILED 알림이 발송된다"
    - "가스 추정(simulate) 실패 시 ChainError에 유의미한 에러 메시지가 포함되어 API 응답에 전달된다"
    - "Gas Conditional Executor의 조건 평가가 maxGasPrice/maxPriorityFee 기준으로 정확히 동작한다"
    - "Gas Conditional 폴링 만료 시 트랜잭션이 CANCELLED 상태로 전이된다"
  artifacts:
    - path: "packages/daemon/src/__tests__/pipeline-delay-reentry.test.ts"
      provides: "DELAY 재진입 시 원본 request 보존 검증"
      min_lines: 80
    - path: "packages/daemon/src/__tests__/pipeline-gas-waiting-reentry.test.ts"
      provides: "GAS_WAITING 재진입 시 request 보존 + 실행 검증"
      min_lines: 80
    - path: "packages/daemon/src/__tests__/pipeline-sign-timeout.test.ts"
      provides: "서명 타임아웃 → FAILED 상태 전이 + 알림 검증"
      min_lines: 80
    - path: "packages/daemon/src/__tests__/pipeline-gas-estimation-error.test.ts"
      provides: "가스 추정 실패 시 에러 메시지 검증"
      min_lines: 60
    - path: "packages/daemon/src/__tests__/gas-condition-executor.test.ts"
      provides: "Gas Conditional 조건 평가 + 폴링 + 만료 검증"
      min_lines: 100
  key_links:
    - from: "pipeline-delay-reentry.test.ts"
      to: "packages/daemon/src/pipeline/stage1-validate.ts"
      via: "stage1에서 직렬화된 original_request가 metadata에 저장됨"
      pattern: "original_request"
    - from: "pipeline-gas-waiting-reentry.test.ts"
      to: "packages/daemon/src/pipeline/pipeline.ts"
      via: "GAS_WAITING → PENDING 전이 후 stage5 재진입"
      pattern: "GAS_WAITING"
    - from: "gas-condition-executor.test.ts"
      to: "packages/daemon/src/pipeline/gas-condition-tracker.ts"
      via: "GasConditionTracker.checkStatus로 가스 조건 평가"
      pattern: "GasConditionTracker"
---

<objective>
Pipeline 상태 머신의 엣지 케이스 5종(DELAY 재진입, GAS_WAITING 재진입, 서명 타임아웃, 가스 추정 실패, Gas Conditional 실행)을 테스트로 커버한다.

Purpose: Pipeline은 8-state 상태 머신으로 엣지 케이스에서의 상태 전이 오류가 자금 손실로 이어질 수 있다. 재진입 시 원본 request 보존과 타임아웃/실패 처리를 검증한다.
Output: 5개 테스트 파일 (pipeline 엣지 케이스)
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/daemon/src/pipeline/pipeline.ts
@packages/daemon/src/pipeline/pipeline-helpers.ts
@packages/daemon/src/pipeline/stage1-validate.ts
@packages/daemon/src/pipeline/stage4-wait.ts
@packages/daemon/src/pipeline/stage5-execute.ts
@packages/daemon/src/pipeline/stage6-confirm.ts
@packages/daemon/src/pipeline/gas-condition-tracker.ts
@packages/daemon/src/services/async-polling-service.ts
@packages/daemon/src/__tests__/pipeline-reentry.test.ts
@packages/daemon/src/__tests__/gas-condition-tracker.test.ts
@packages/daemon/src/__tests__/gas-condition-pipeline.test.ts
@packages/daemon/src/__tests__/pipeline-stage4.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DELAY/GAS_WAITING 재진입 + 서명 타임아웃 테스트</name>
  <files>
    packages/daemon/src/__tests__/pipeline-delay-reentry.test.ts
    packages/daemon/src/__tests__/pipeline-gas-waiting-reentry.test.ts
    packages/daemon/src/__tests__/pipeline-sign-timeout.test.ts
  </files>
  <behavior>
    DELAY 재진입:
    - stage1에서 original_request가 트랜잭션 metadata에 JSON 직렬화되어 저장된다
    - DELAY 만료 후 재진입 시 metadata에서 original_request가 파싱되어 stage5에 전달된다
    - 재진입된 request가 원본과 동일한 type/to/amount를 가진다
    - TOKEN_TRANSFER 타입 재진입 시 contractAddress/tokenAmount가 보존된다

    GAS_WAITING 재진입:
    - GAS_WAITING 상태에서 가스 조건 충족 시 PENDING으로 전이된다
    - 전이 후 stage5가 호출되고 원본 request로 트랜잭션이 빌드된다
    - GAS_WAITING → PENDING 전이 시 TX_GAS_CONDITION_MET 알림이 발송된다
    - 가스 조건 미충족 시 GAS_WAITING 상태 유지

    서명 타임아웃:
    - APPROVAL 상태에서 approval_timeout 초과 시 FAILED로 전이
    - FAILED 전이 시 TX_FAILED 알림이 발송된다
    - FAILED 전이 시 트랜잭션 status가 'failed'로 DB에 업데이트된다
    - 타임아웃 에러 메시지에 "timeout" 또는 "expired" 포함
  </behavior>
  <action>
    1. `pipeline-delay-reentry.test.ts` 작성:
       - 기존 pipeline-reentry.test.ts 패턴 참조 (in-memory SQLite + mock adapter).
       - stage1Validate 호출 후 DB에서 트랜잭션 metadata 조회 → original_request 필드 존재 확인.
       - TRANSFER 타입: {type:'TRANSFER', to, amount} 직렬화 → 역직렬화 일치.
       - TOKEN_TRANSFER 타입: {type:'TOKEN_TRANSFER', contractAddress, tokenAmount} 보존 확인.
       - CONTRACT_CALL 타입: {type:'CONTRACT_CALL', contractAddress, data, value} 보존 확인.
       - NOTE: 기존 pipeline-reentry.test.ts와 중복 최소화. 기존 테스트가 커버하지 않는 "다양한 request type별 직렬화 보존"에 집중.

    2. `pipeline-gas-waiting-reentry.test.ts` 작성:
       - 기존 gas-condition-pipeline.test.ts 패턴 참조 (in-memory SQLite + AsyncPollingService).
       - GAS_WAITING 트랜잭션 생성 → AsyncPollingService.checkPending 호출 → tracker가 COMPLETED 반환 → 상태가 PENDING으로 전이 확인.
       - COMPLETED 시 resumePipeline 콜백 호출 확인 (vi.fn() spy).
       - TX_GAS_CONDITION_MET 알림 발송 확인 (notificationService.notify spy).
       - tracker가 PENDING 반환 시 상태 변화 없음 확인.
       - NOTE: 기존 gas-condition-pipeline.test.ts와 중복 최소화. 기존이 커버하지 않는 "원본 request 보존 후 stage5 실행"에 집중.

    3. `pipeline-sign-timeout.test.ts` 작성:
       - APPROVAL 상태 트랜잭션 DB 삽입 → approval_timeout 시간 경과 시뮬레이션.
       - 타임아웃 처리 로직 호출 → DB status 'failed' 확인.
       - notificationService.notify('TX_FAILED', ...) 호출 확인.
       - 에러 메시지 검증.
       - NOTE: 기존 pipeline-stage4.test.ts의 APPROVAL 테스트와 중복 최소화. "타임아웃 경과 후 FAILED 전이"에 집중.
  </action>
  <verify>
    <automated>cd packages/daemon && npx vitest run src/__tests__/pipeline-delay-reentry.test.ts src/__tests__/pipeline-gas-waiting-reentry.test.ts src/__tests__/pipeline-sign-timeout.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>DELAY 재진입 request 타입별 보존 4+ 테스트, GAS_WAITING 재진입 + 알림 4+ 테스트, 서명 타임아웃 FAILED 전이 3+ 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 가스 추정 실패 + Gas Conditional Executor 테스트</name>
  <files>
    packages/daemon/src/__tests__/pipeline-gas-estimation-error.test.ts
    packages/daemon/src/__tests__/gas-condition-executor.test.ts
  </files>
  <behavior>
    가스 추정 실패:
    - adapter.simulate() 실패 시 stage5에서 ChainError throw
    - ChainError 메시지에 "gas" 또는 "simulate" 키워드 포함
    - 에러가 WAIaaSError로 변환되어 API 응답에 포함됨
    - EVM의 경우 revert reason이 있으면 에러 메시지에 포함
    - Solana의 경우 InstructionError가 있으면 에러 메시지에 포함

    Gas Conditional Executor:
    - maxGasPrice 조건: 현재 가스 < maxGasPrice → COMPLETED
    - maxGasPrice 조건: 현재 가스 > maxGasPrice → PENDING (대기)
    - maxPriorityFee 조건: 현재 priority fee < maxPriorityFee → COMPLETED
    - 타임아웃 경과 시 TIMEOUT 반환 → 트랜잭션 CANCELLED
    - RPC 에러 시 PENDING 반환 (에러 격리, 재시도)
    - 캐시 TTL 내 동일 결과 반환 (가스 가격 캐시)
  </behavior>
  <action>
    1. `pipeline-gas-estimation-error.test.ts` 작성:
       - Mock adapter의 simulate()가 ChainError throw하도록 설정.
       - stage5Execute 호출 → catch한 에러가 WAIaaSError 타입.
       - 에러 메시지에 원본 ChainError의 정보 포함 확인.
       - EVM revert reason 시나리오: ChainError({ code: 'EXECUTION_REVERTED', reason: 'insufficient balance' }).
       - Solana InstructionError 시나리오: ChainError({ code: 'INSTRUCTION_ERROR', ... }).
       - NOTE: 기존 pipeline-stage5-execute.test.ts 패턴 참조. 가스 추정 "실패" 경로에만 집중.

    2. `gas-condition-executor.test.ts` 작성:
       - GasConditionTracker 직접 인스턴스화 + global fetch 목킹.
       - 기존 gas-condition-tracker.test.ts가 커버하는 것과 중복 최소화.
       - 추가 커버: 복합 조건(maxGasPrice + maxPriorityFee 동시), Solana prioritization fee 조건.
       - 폴링 루프 시뮬레이션: checkStatus 연속 호출 시 PENDING → PENDING → COMPLETED 시퀀스.
       - 만료 처리: gasConditionCreatedAt이 timeout 이전인 경우 TIMEOUT 반환 → handleTimeout에서 CANCELLED.
       - 캐시 검증: 동일 rpcUrl + 짧은 간격 호출 시 fetch 1회만 호출 (gasPriceCache 활용).
       - NOTE: 기존 gas-condition-tracker.test.ts의 11개 테스트를 보완하는 추가 엣지 케이스에 집중.
  </action>
  <verify>
    <automated>cd packages/daemon && npx vitest run src/__tests__/pipeline-gas-estimation-error.test.ts src/__tests__/gas-condition-executor.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>가스 추정 실패 EVM/Solana 에러 경로 4+ 테스트, Gas Conditional 복합 조건/폴링/만료/캐시 6+ 테스트 통과</done>
</task>

</tasks>

<verification>
cd packages/daemon && npx vitest run src/__tests__/pipeline-delay-reentry.test.ts src/__tests__/pipeline-gas-waiting-reentry.test.ts src/__tests__/pipeline-sign-timeout.test.ts src/__tests__/pipeline-gas-estimation-error.test.ts src/__tests__/gas-condition-executor.test.ts --reporter=verbose
</verification>

<success_criteria>
- DELAY 재진입 시 원본 request가 보존되고 다양한 type에서 검증된다
- GAS_WAITING 재진입 시 가스 조건 충족 후 정상 실행이 검증된다
- 서명 타임아웃이 FAILED 상태 전이와 알림을 트리거함이 검증된다
- 가스 추정 실패 시 유의미한 에러 메시지가 검증된다
- Gas Conditional의 조건 평가, 폴링, 만료가 검증된다
- 총 20+ 개 테스트 케이스가 모두 통과한다
- 기존 테스트가 깨지지 않는다 (`cd packages/daemon && pnpm test:unit` 통과)
</success_criteria>

<output>
After completion, create `.planning/phases/444-daemon-defi-pipeline-tests/444-03-SUMMARY.md`
</output>
