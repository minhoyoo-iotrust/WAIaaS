---
phase: 448-sdk-shared-final-thresholds
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/vitest.config.ts
  - packages/shared/src/__tests__/networks.test.ts
  - packages/core/src/__tests__/core-functions-coverage.test.ts
  - packages/core/vitest.config.ts
  - packages/actions/src/__tests__/actions-branches-coverage.test.ts
  - packages/actions/vitest.config.ts
  - packages/mcp/src/__tests__/mcp-branches-coverage.test.ts
  - packages/mcp/vitest.config.ts
autonomous: true
requirements: [SHR-01, SHR-02, SHR-03, GAP-01, GAP-02, GAP-03]

must_haves:
  truths:
    - "shared 패키지에 vitest.config.ts가 설정되고 networks.ts 함수/분기가 테스트로 검증된다"
    - "core Functions >= 95% 달성된다"
    - "actions Branches >= 85% 달성된다"
    - "mcp Branches >= 85% 달성된다"
  artifacts:
    - path: "packages/shared/vitest.config.ts"
      provides: "shared 패키지 vitest 설정"
    - path: "packages/shared/src/__tests__/networks.test.ts"
      provides: "networks.ts validateChainNetwork + 헬퍼 테스트"
    - path: "packages/core/src/__tests__/core-functions-coverage.test.ts"
      provides: "core 미커버 함수 테스트"
    - path: "packages/actions/src/__tests__/actions-branches-coverage.test.ts"
      provides: "actions 미커버 분기 테스트"
    - path: "packages/mcp/src/__tests__/mcp-branches-coverage.test.ts"
      provides: "mcp 미커버 분기 테스트"
  key_links:
    - from: "packages/shared/src/__tests__/networks.test.ts"
      to: "packages/shared/src/networks.ts"
      via: "import validateChainNetwork"
      pattern: "validateChainNetwork"
---

<objective>
shared 패키지 vitest 설정 + 네트워크 테스트, core/actions/mcp 소량 갭 해소를 통해 전 패키지가 통일 기준을 달성한다.

Purpose: shared는 vitest.config.ts가 없어 커버리지 게이트가 부재하고, core/actions/mcp는 각각 1-2%p 미달하는 소량 갭만 남아있다. 이 plan에서 모두 해소한다.
Output: shared vitest 설정 + 4개 패키지 테스트 + 임계값 인상
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/shared/src/networks.ts
@packages/shared/src/constants.ts
@packages/shared/src/__tests__/constants.test.ts
@packages/shared/package.json
@packages/core/vitest.config.ts
@packages/actions/vitest.config.ts
@packages/mcp/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: shared vitest.config.ts 설정 + networks.ts 테스트</name>
  <files>packages/shared/vitest.config.ts, packages/shared/src/__tests__/networks.test.ts</files>
  <action>
1. packages/shared/vitest.config.ts 생성. 다른 패키지(예: packages/core/vitest.config.ts) 구조를 따르되:
   - provider: 'v8', reporter: ['text', 'json-summary', 'json']
   - include: ['src/**/*.ts'], exclude: ['src/**/__tests__/**', 'src/**/*.test.ts', 'src/**/index.ts']
   - thresholds: { lines: 90, branches: 85, functions: 95, statements: 90 }

2. networks.test.ts 작성:
   - CHAIN_TYPES: 2개 포함(solana, ethereum) 검증
   - NETWORK_TYPES: 15개 네트워크 포함 검증
   - SOLANA_NETWORK_TYPES: 3개, 모두 solana- 접두사
   - EVM_NETWORK_TYPES: 12개, 모두 NETWORK_TYPES에 포함
   - ENVIRONMENT_TYPES: testnet/mainnet 2개
   - ENVIRONMENT_NETWORK_MAP: 4개 키(solana:mainnet, solana:testnet, ethereum:mainnet, ethereum:testnet), 각 값이 해당 네트워크 타입에 포함되는지
   - validateChainNetwork: solana+solana-mainnet 성공, solana+ethereum-mainnet 에러, ethereum+base-sepolia 성공, ethereum+solana-devnet 에러
   - NETWORK_DISPLAY_NAMES: 모든 NETWORK_TYPES에 대해 레이블 존재 + 문자열
   - NETWORK_NATIVE_SYMBOL: 모든 NETWORK_TYPES에 대해 심볼 존재
   - EVM_NETWORK_OPTIONS: length === EVM_NETWORK_TYPES.length, 각 항목에 label/value
   - EVM_RPC_SETTING_KEYS: length === EVM_NETWORK_TYPES.length, evm_ 접두사
   - SOLANA_RPC_SETTING_KEYS: length === SOLANA_NETWORK_TYPES.length
   - RPC_KEY_LABELS: 모든 EVM+Solana RPC 키에 대해 레이블 존재

shared는 순수 상수+유틸 모듈이므로 높은 커버리지 달성이 용이. 테스트 실행 후 임계값 조정.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter shared exec vitest run --coverage 2>&1 | tail -20</automated>
  </verify>
  <done>shared 패키지 vitest run --coverage 통과, Lines >= 90%, Branches >= 85%, Functions >= 95%</done>
</task>

<task type="auto">
  <name>Task 2: core/actions/mcp 소량 갭 해소 + 임계값 인상</name>
  <files>packages/core/src/__tests__/core-functions-coverage.test.ts, packages/core/vitest.config.ts, packages/actions/src/__tests__/actions-branches-coverage.test.ts, packages/actions/vitest.config.ts, packages/mcp/src/__tests__/mcp-branches-coverage.test.ts, packages/mcp/vitest.config.ts</files>
  <action>
각 패키지별로 `pnpm --filter {pkg} exec vitest run --coverage` 실행하여 현재 실제 수치를 확인하고 부족한 부분만 타겟 테스트 추가.

**core** (Functions 93.75% -> 95% 필요, +1.25%p):
- `pnpm --filter core exec vitest run --coverage` 실행 후 uncovered functions 확인
- 미커버 함수 2-3개에 대한 단위 테스트 추가
- vitest.config.ts functions: 93 -> 95 인상

**actions** (Branches 84.59% -> 85% 필요, +0.41%p):
- `pnpm --filter actions exec vitest run --coverage` 실행 후 uncovered branches 확인
- 미커버 분기 1-2개에 대한 단위 테스트 추가
- vitest.config.ts branches: 79 -> 85 인상 (현재 임계값 79는 실제 84.59% 대비 매우 낮음)

**mcp** (Branches 84.85% -> 85% 필요, +0.15%p):
- `pnpm --filter mcp exec vitest run --coverage` 실행 후 uncovered branches 확인
- 미커버 분기 1개에 대한 단위 테스트 추가
- vitest.config.ts branches: 83 -> 85 인상

각 패키지의 다른 메트릭도 통일 기준 대비 확인:
- core: lines(95, OK), branches(91, OK), statements(95, OK) - functions만 인상
- actions: lines(95, OK), functions(95, OK), statements(95, OK) - branches만 인상
- mcp: lines(87 -> 90 가능?), functions(93 -> 95 가능?), statements(87 -> 90 가능?) - 실제 수치 확인 후 달성 가능한 만큼 인상

주의: 임계값은 절대 하향 금지. 현재 임계값보다 높은 값으로만 변경.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter core exec vitest run --coverage 2>&1 | tail -5 && pnpm --filter actions exec vitest run --coverage 2>&1 | tail -5 && pnpm --filter mcp exec vitest run --coverage 2>&1 | tail -5</automated>
  </verify>
  <done>core Functions >= 95%, actions Branches >= 85%, mcp Branches >= 85% 달성하고 각 vitest.config.ts 통과</done>
</task>

</tasks>

<verification>
cd /Users/minho.yoo/dev/wallet/WAIaaS
pnpm --filter shared exec vitest run --coverage
pnpm --filter core exec vitest run --coverage
pnpm --filter actions exec vitest run --coverage
pnpm --filter mcp exec vitest run --coverage
- 전부 0 failures, 임계값 통과
</verification>

<success_criteria>
1. shared 패키지에 vitest.config.ts가 설정되고 networks.ts 전 함수/상수가 테스트로 검증된다
2. core Functions >= 95% 임계값을 통과한다
3. actions Branches >= 85% 임계값을 통과한다
4. mcp Branches >= 85% 임계값을 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/448-sdk-shared-final-thresholds/448-02-SUMMARY.md`
</output>
