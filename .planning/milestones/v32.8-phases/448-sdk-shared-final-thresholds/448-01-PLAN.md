---
phase: 448-sdk-shared-final-thresholds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sdk/src/__tests__/client-coverage.test.ts
  - packages/sdk/src/__tests__/validation-coverage.test.ts
  - packages/sdk/src/__tests__/internal-http-coverage.test.ts
  - packages/sdk/vitest.config.ts
autonomous: true
requirements: [SDK-01, SDK-02, SDK-03, SDK-04]

must_haves:
  truths:
    - "SDK client.ts HTTP 에러 경로(4xx/5xx), DeFi 메서드, ERC-8004 메서드가 테스트로 검증된다"
    - "SDK validation.ts 모든 타입별 검증 분기 + 옵셔널 파라미터 조합이 테스트로 검증된다"
    - "SDK internal/http.ts 에러 응답 파싱, 타임아웃 처리가 테스트로 검증된다"
    - "SDK 패키지 Lines >= 90%, Functions >= 95% 임계값을 통과한다"
  artifacts:
    - path: "packages/sdk/src/__tests__/client-coverage.test.ts"
      provides: "client.ts 미커버 메서드 테스트"
    - path: "packages/sdk/src/__tests__/validation-coverage.test.ts"
      provides: "validation.ts 전 분기 테스트"
    - path: "packages/sdk/src/__tests__/internal-http-coverage.test.ts"
      provides: "internal/http.ts 에러 경로 테스트"
    - path: "packages/sdk/vitest.config.ts"
      provides: "SDK 임계값 90/85/95 인상"
  key_links:
    - from: "packages/sdk/src/__tests__/client-coverage.test.ts"
      to: "packages/sdk/src/client.ts"
      via: "import WAIaaSClient"
      pattern: "new WAIaaSClient"
    - from: "packages/sdk/src/__tests__/validation-coverage.test.ts"
      to: "packages/sdk/src/validation.ts"
      via: "import validateSendToken"
      pattern: "validateSendToken"
---

<objective>
SDK 패키지의 미커버 메서드/분기를 테스트로 커버하여 Lines 90% / Branches 85% / Functions 95% 통일 기준을 달성한다.

Purpose: SDK는 현재 Lines 80%, Functions 80%으로 통일 기준 대비 큰 갭이 있다. client.ts(1510줄)의 DeFi/ERC-8004/bridge 메서드와 validation.ts 분기를 집중 커버한다.
Output: SDK 테스트 3파일 + vitest.config.ts 임계값 인상
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/sdk/src/client.ts
@packages/sdk/src/validation.ts
@packages/sdk/src/internal/http.ts
@packages/sdk/src/__tests__/client.test.ts
@packages/sdk/src/__tests__/validation.test.ts
@packages/sdk/vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SDK client.ts + validation.ts + http.ts 미커버 경로 테스트</name>
  <files>packages/sdk/src/__tests__/client-coverage.test.ts, packages/sdk/src/__tests__/validation-coverage.test.ts, packages/sdk/src/__tests__/internal-http-coverage.test.ts</files>
  <behavior>
    - client-coverage: DeFi 메서드(executeAction 성공/에러, listActions, getActionSchema), ERC-8004 메서드(registerAgent/getAgentInfo/getAgentReputation/requestValidation), Across bridge 메서드(quote/execute/status/routes), WalletConnect(wcConnect/wcStatus/wcDisconnect), buildUserOp/signUserOp, listSessions/listPolicies, getMultiNetworkBalance/getMultiNetworkAssets, signHttpRequest/verifyHttpSignature/fetchWithErc8128 각 성공+에러 경로
    - validation-coverage: validateSendToken 모든 txType(TRANSFER/TOKEN_TRANSFER/CONTRACT_CALL/APPROVE/BATCH) + 유효하지 않은 type, token.assetId 단독 모드, amount vs humanAmount XOR, memo 256자 초과, instructions 1개 이하, null params
    - internal-http: HttpClient request 메서드 4xx/5xx 에러 파싱, JSON 파싱 실패, 타임아웃 에러 변환
  </behavior>
  <action>
기존 client.test.ts 패턴을 따라 HttpClient를 모킹하고 각 메서드 호출 시 올바른 HTTP 메서드/경로/바디가 전달되는지 검증.
에러 경로는 HttpClient가 WAIaaSError를 throw하는 시나리오를 테스트.

validation-coverage.test.ts: validateSendToken에 각 txType별 유효/무효 파라미터를 전달하여 성공/에러 분기 커버. token 오브젝트의 assetId-only 모드, full mode(address+decimals+symbol), 각 필드 누락 케이스 모두 커버.

internal-http-coverage.test.ts: HttpClient 생성자 옵션(baseUrl, token, timeout), request 메서드의 fetch 모킹을 통해 200/4xx/5xx 응답 처리 + JSON이 아닌 응답 body + AbortController 타임아웃 시나리오 커버.

기존 테스트와 중복되지 않도록 기존 test 파일을 먼저 확인하고, 미커버 메서드/분기만 타겟. `pnpm --filter sdk exec vitest run --coverage` 로 커버리지 확인 후 부족한 부분 추가 작성.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter sdk exec vitest run --coverage 2>&1 | tail -30</automated>
  </verify>
  <done>SDK Lines >= 90%, Branches >= 85%, Functions >= 95% 실제 달성 확인</done>
</task>

<task type="auto">
  <name>Task 2: SDK vitest.config.ts 임계값 인상</name>
  <files>packages/sdk/vitest.config.ts</files>
  <action>
SDK vitest.config.ts thresholds를 통일 기준으로 인상:
- lines: 80 -> 90
- branches: 89 -> (유지 또는 인상, 현재 이미 89이므로 실제 달성 수치에 맞춰 max(89, 85) = 89 유지)
- functions: 80 -> 95
- statements: 80 -> 90

주의: 기존 임계값보다 낮아지면 안 됨 (CLAUDE.md 규칙). branches는 현재 89로 이미 85 이상이므로 89 유지.
Task 1의 커버리지 결과를 확인한 후, 실제 달성 수치 기반으로 안전한 임계값 설정. 달성 수치보다 1-2%p 낮게 설정하여 향후 소규모 코드 추가 시 깨지지 않도록.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter sdk exec vitest run --coverage 2>&1 | tail -5</automated>
  </verify>
  <done>SDK vitest.config.ts 임계값이 통일 기준 이상으로 인상되고, `pnpm --filter sdk exec vitest run --coverage` 통과</done>
</task>

</tasks>

<verification>
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter sdk exec vitest run --coverage
- Lines >= 90%, Branches >= 85% (또는 89%), Functions >= 95% 확인
- 0 failures
</verification>

<success_criteria>
1. SDK client.ts HTTP 에러/DeFi/ERC-8004/bridge 메서드가 테스트로 검증된다
2. SDK validation.ts 전 txType 분기와 옵셔널 파라미터 조합이 테스트로 검증된다
3. SDK internal/http.ts 에러 파싱과 타임아웃이 테스트로 검증된다
4. SDK Lines >= 90%, Functions >= 95% 임계값을 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/448-sdk-shared-final-thresholds/448-01-SUMMARY.md`
</output>
