---
phase: 446-evm-wallet-sdk-branches
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/wallet-sdk/src/__tests__/channels.test.ts
  - packages/wallet-sdk/src/__tests__/parse-request.test.ts
  - packages/wallet-sdk/vitest.config.ts
autonomous: true
requirements:
  - WSDK-01
  - WSDK-02

must_haves:
  truths:
    - "wallet-sdk 서명 채널별 에러, 타임아웃, 재시도 분기가 테스트로 검증된다"
    - "wallet-sdk Branches >= 85% 임계값을 통과한다"
  artifacts:
    - path: "packages/wallet-sdk/src/__tests__/channels.test.ts"
      provides: "ntfy.ts SSE 재연결, attachment fallback, 만료 요청 스킵 분기 테스트"
    - path: "packages/wallet-sdk/src/__tests__/parse-request.test.ts"
      provides: "parse-request.ts remote fetch 에러 경로 분기 테스트"
  key_links:
    - from: "channels.test.ts"
      to: "channels/ntfy.ts subscribeToRequests/subscribeToNotifications"
      via: "mock fetch SSE stream with reconnection scenarios"
      pattern: "reconnectAttempts|resolveMessage|attachment"
---

<objective>
wallet-sdk 패키지의 Branches 커버리지를 79.09%에서 85%로 끌어올린다.

Purpose: ntfy.ts(70.8%, 14개 미커버 브랜치)와 parse-request.ts(75.8%, 8개 미커버 브랜치)의 에러 경로, 재연결 분기, remote fetch 경로를 테스트로 커버한다.
Output: 기존 테스트 보강, vitest.config.ts 임계값 인상
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ntfy.ts SSE 채널 에러/재연결/첨부 분기 테스트 보강</name>
  <files>
    packages/wallet-sdk/src/__tests__/channels.test.ts
  </files>
  <read_first>
    packages/wallet-sdk/src/channels/ntfy.ts
    packages/wallet-sdk/src/channels/relay.ts
    packages/wallet-sdk/src/__tests__/channels.test.ts
  </read_first>
  <action>
ntfy.ts Branches 70.8% (34/48). 14개 미커버 브랜치. channels.test.ts에 추가 테스트:

**1. resolveMessage() 분기 (lines 34-42):**
- event.attachment?.url 존재 → fetch로 파일 다운로드
- attachment fetch 실패 (res.ok === false) → null 반환
- attachment fetch 성공하지만 body.message가 string이 아닌 경우 → null 반환
- event.attachment이 없고 event.message가 없는 경우 → null 반환 (message ?? null)

**2. subscribeToRequests() SSE 재연결 분기 (lines 93-163):**
- abortController.signal.aborted가 true일 때 connect() 즉시 return (line 94)
- res.ok === false → throw → catch → reconnectAttempts++ → 재연결 시도
- res.body가 null → throw
- 정상 연결 후 reconnectAttempts가 0으로 리셋 (line 107)
- reader.read() done === true → break → 연결 종료 → 재연결 시도
- SSE data line이 'data: '로 시작하지 않음 → continue
- dataStr이 빈 문자열 → continue
- JSON.parse 실패 → catch 무시 (continue)
- base64url decode 실패 → catch 무시
- SignRequestSchema.parse 실패 → catch 무시
- expiresAt < Date.now() → continue (만료된 요청 스킵)
- reconnectAttempts > MAX_RECONNECT_ATTEMPTS (3) → 재연결 중단
- catch에서 abortController.signal.aborted === true → return (재연결 안 함)

**3. subscribeToNotifications() 동일 패턴 분기 (lines 210-265):**
- subscribeToRequests와 동일한 재연결, 에러 처리, 파싱 분기

**4. sendViaNtfy() 에러 분기 (lines 53-72):**
- res.ok === false → throw Error with HTTP status

**5. parseNotification() 분기 (lines 184-188):**
- 잘못된 base64url → JSON.parse 에러
- 유효한 JSON이지만 스키마 검증 실패

**relay.ts 분기 (현재 92.3%, 1개 미커버):**
- sendViaRelay: response.signature가 undefined → 객체에 signature 필드 미포함
- registerDevice/unregisterDevice/getSubscriptionToken: res.ok === false

Mock 전략:
- global.fetch를 vi.fn()으로 mock
- SSE 스트림은 ReadableStream으로 mock: `new ReadableStream({ start(controller) { controller.enqueue(new TextEncoder().encode('data: {...}\n\n')); controller.close(); } })`
- 재연결 테스트: 첫 번째 fetch reject → setTimeout mock (vi.useFakeTimers) → 두 번째 fetch resolve
- vi.useFakeTimers()로 RECONNECT_DELAY_MS 건너뛰기
- 만료 테스트: Date.now() mock으로 expiresAt 이전/이후 조절

주의:
- 기존 30개 테스트 깨뜨리지 않기
- afterEach에서 fetch mock 복원 (vi.restoreAllMocks)
- fakeTimers 사용 시 afterEach에서 vi.useRealTimers() 호출
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/wallet-sdk test:unit -- --coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - channels.test.ts에 resolveMessage attachment fetch 실패/성공 분기 테스트가 존재한다
    - channels.test.ts에 SSE 재연결 시도 분기 (reconnectAttempts <= 3) 테스트가 존재한다
    - channels.test.ts에 SSE 재연결 초과 시 중단 테스트가 존재한다
    - channels.test.ts에 만료된 SignRequest 스킵 분기 테스트가 존재한다
    - channels.test.ts에 malformed SSE data (non-data lines, empty data, invalid JSON/base64) 무시 테스트가 존재한다
    - ntfy.ts Branches가 85% 이상으로 증가한다
    - 기존 30개 테스트 전부 통과 (regression 없음)
  </acceptance_criteria>
  <done>ntfy.ts Branches >= 85%, relay.ts Branches >= 92%, 기존 + 신규 테스트 전부 통과</done>
</task>

<task type="auto">
  <name>Task 2: parse-request.ts remote fetch 분기 테스트 + vitest 임계값 인상</name>
  <files>
    packages/wallet-sdk/src/__tests__/parse-request.test.ts
    packages/wallet-sdk/vitest.config.ts
  </files>
  <read_first>
    packages/wallet-sdk/src/parse-request.ts
    packages/wallet-sdk/src/errors.ts
    packages/wallet-sdk/src/__tests__/parse-request.test.ts
    packages/wallet-sdk/vitest.config.ts
  </read_first>
  <action>
parse-request.ts Branches 75.8% (25/33). 8개 미커버 브랜치:

**parse-request.test.ts 보강:**

1. **fetchSignRequestFromNtfy 전체 경로 (lines 67-126):**
   - requestId 파라미터로 parseSignRequest 호출 → Promise 반환
   - ntfy fetch 실패 (res.ok === false) → InvalidSignRequestUrlError 'Failed to fetch from ntfy: HTTP 500'
   - ntfy 응답에 여러 줄 JSON → 올바른 requestId 매칭
   - ntfy 응답 줄 중 JSON parse 실패 → continue (스킵)
   - ntfy 응답 줄 중 msg.message가 없는 경우 → continue
   - ntfy 응답 줄 중 base64url decode 실패 → continue
   - ntfy 응답 줄 중 JSON.parse(decoded) 실패 → continue
   - ntfy 응답 줄 중 SignRequestSchema.parse 실패 → continue
   - requestId가 매칭되지 않음 → InvalidSignRequestUrlError 'not found in ntfy topic'
   - requestId 매칭되었으나 만료 → SignRequestExpiredError

2. **parseSignRequest 분기 (lines 137-164):**
   - URL 파싱 실패 → InvalidSignRequestUrlError 'Invalid URL'
   - data 파라미터 있음 → decodeInlineData (이미 테스트됨)
   - requestId 파라미터 있음 → fetchSignRequestFromNtfy 호출
   - topic 파라미터 없음 → 기본값 'waiaas-sign-requests'
   - serverUrl 파라미터 없음 → 기본값 'https://ntfy.sh'
   - data도 requestId도 없음 → InvalidSignRequestUrlError 'URL must contain either'

3. **decodeInlineData 에러 분기 (lines 35-62):**
   - base64url decode 실패 → InvalidSignRequestUrlError 'Invalid base64url'
   - JSON.parse 실패 → InvalidSignRequestUrlError 'Invalid JSON'
   - ZodError → SignRequestValidationError
   - 일반 에러 rethrow (line 57: throw err)
   - 만료된 요청 → SignRequestExpiredError

Mock 전략:
- global.fetch를 vi.fn()으로 mock하여 ntfy 응답 시뮬레이션
- 유효한 SignRequest JSON을 base64url 인코딩하여 ntfy 응답 본문 구성
- Date.now()를 vi.spyOn으로 mock하여 만료 테스트

**vitest.config.ts 임계값 인상:**
branches를 현재 76 → 85로 인상.

```typescript
thresholds: {
  branches: 85,
  functions: 98,   // 유지 (현재 100%)
  lines: 87,       // 유지 (현재 90.04%)
  statements: 87,  // 유지
},
```
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/wallet-sdk test:unit -- --coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - parse-request.test.ts에 fetchSignRequestFromNtfy 경로 테스트가 존재한다 (requestId 파라미터 URL)
    - parse-request.test.ts에 ntfy fetch 실패 시 InvalidSignRequestUrlError 테스트가 존재한다
    - parse-request.test.ts에 ntfy 응답 줄 중 malformed 줄 스킵 테스트가 존재한다
    - parse-request.test.ts에 requestId 미매칭 시 'not found' 에러 테스트가 존재한다
    - parse-request.test.ts에 topic/serverUrl 기본값 사용 분기 테스트가 존재한다
    - vitest.config.ts의 branches 임계값이 85이다
    - `pnpm --filter @waiaas/wallet-sdk test:unit -- --coverage` 실행 시 0 failures이고 coverage thresholds를 통과한다
  </acceptance_criteria>
  <done>wallet-sdk 패키지 Branches >= 85% 달성, vitest.config.ts 임계값 인상 완료, 전체 테스트 통과</done>
</task>

</tasks>

<verification>
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/wallet-sdk test:unit -- --coverage
- Branches >= 85%
- 0 failures
- Coverage thresholds pass
</verification>

<success_criteria>
1. ntfy.ts Branches가 70.8% → 85% 이상으로 증가
2. parse-request.ts Branches가 75.8% → 85% 이상으로 증가
3. relay.ts Branches 유지 또는 향상
4. vitest.config.ts branches 임계값이 85로 인상
5. 전체 wallet-sdk Branches >= 85% 통과
6. 기존 테스트 regression 없음 (0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/446-evm-wallet-sdk-branches/446-02-SUMMARY.md`
</output>
