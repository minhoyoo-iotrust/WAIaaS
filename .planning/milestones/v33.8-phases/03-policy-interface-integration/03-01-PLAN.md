---
phase: 03-policy-interface-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/pipeline/resolve-effective-amount-usd.ts
  - packages/daemon/src/__tests__/resolve-effective-amount-usd.test.ts
autonomous: true
requirements: [POL-01, POL-02]

must_haves:
  truths:
    - "XRPL DEX 스왑에서 TakerGets가 XRP일 때 USD 지출이 올바르게 계산된다"
    - "XRPL DEX 스왑에서 TakerGets가 IOU일 때 notListed가 반환되어 안전한 폴백이 작동한다"
    - "기존 CONTRACT_CALL USD 계산이 영향받지 않는다"
    - "SPENDING_LIMIT 정책이 XRPL DEX CONTRACT_CALL에 적용된다"
  artifacts:
    - path: "packages/daemon/src/pipeline/resolve-effective-amount-usd.ts"
      provides: "XRPL DEX calldata parsing for USD amount resolution"
      contains: "xrplTxType"
    - path: "packages/daemon/src/__tests__/resolve-effective-amount-usd.test.ts"
      provides: "XRPL DEX USD resolution tests"
      contains: "xrpl_dex"
  key_links:
    - from: "packages/daemon/src/pipeline/resolve-effective-amount-usd.ts"
      to: "packages/daemon/src/pipeline/stage3-policy.ts"
      via: "resolveEffectiveAmountUsd called before policy evaluation"
      pattern: "resolveEffectiveAmountUsd"
---

<objective>
XRPL DEX 거래의 TakerGets 금액이 USD 지출 한도 정책에 올바르게 반영되도록 resolveEffectiveAmountUsd를 확장한다.

Purpose: XRPL DEX CONTRACT_CALL 트랜잭션의 value 필드만으로는 IOU 지출을 추적할 수 없으므로, calldata JSON에서 TakerGets를 파싱하여 정책 엔진이 정확한 USD 금액으로 평가하도록 한다. 이를 통해 SPENDING_LIMIT 정책 우회를 방지한다.
Output: 수정된 resolve-effective-amount-usd.ts + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/PITFALLS.md (Pitfall 6: USD 지출 한도 누락)

@packages/daemon/src/pipeline/resolve-effective-amount-usd.ts
@packages/daemon/src/__tests__/resolve-effective-amount-usd.test.ts
@packages/actions/src/providers/xrpl-dex/index.ts (resolveSwap, resolveLimitOrder -- value field설정 확인)

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/daemon/src/pipeline/resolve-effective-amount-usd.ts:
```typescript
export type PriceResult =
  | PriceResultSuccess    // { type: 'success', usdAmount: number, isStale: boolean }
  | PriceResultOracleDown // { type: 'oracleDown' }
  | PriceResultNotListed; // { type: 'notListed', tokenAddress: string, chain: string }

export async function resolveEffectiveAmountUsd(
  request: Record<string, unknown>,
  txType: string,
  chain: string,
  priceOracle: IPriceOracle,
  network?: string,
): Promise<PriceResult>;
```

From packages/actions/src/providers/xrpl-dex/index.ts:
```typescript
// ContractCallRequest created by XrplDexProvider:
// - value: TakerGets drops string (when TakerGets is XRP), undefined (when TakerGets is IOU)
// - calldata: JSON.stringify({ xrplTxType: 'OfferCreate', TakerGets: ..., TakerPays: ..., Flags: ... })
// - actionProvider: 'xrpl_dex'
// - actionName: 'swap' | 'limit_order'
```

Security principle: "unknown price != price of 0" (see resolve-effective-amount-usd.ts header)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveEffectiveAmountUsd XRPL DEX calldata 파싱</name>
  <files>packages/daemon/src/pipeline/resolve-effective-amount-usd.ts, packages/daemon/src/__tests__/resolve-effective-amount-usd.test.ts</files>
  <behavior>
    - Test: CONTRACT_CALL with actionProvider='xrpl_dex' + calldata containing xrplTxType='OfferCreate' + TakerGets as XRP drops string -> success with correct USD (XRP drops / 10^6 * nativePrice)
    - Test: CONTRACT_CALL with actionProvider='xrpl_dex' + calldata containing TakerGets as IOU object -> notListed (IOU pricing out of scope)
    - Test: CONTRACT_CALL with actionProvider='xrpl_dex' + xrplTxType='OfferCancel' -> success with $0 (no spending)
    - Test: CONTRACT_CALL without actionProvider (standard EVM) -> unchanged behavior (value-based)
    - Test: CONTRACT_CALL with value='0' and no calldata -> unchanged behavior ($0)
    - Test: CONTRACT_CALL with malformed calldata JSON -> graceful fallback to existing value-based logic
  </behavior>
  <action>
    1. Add tests first in resolve-effective-amount-usd.test.ts:
       - New describe block 'XRPL DEX CONTRACT_CALL (calldata parsing)'
       - Test XRP TakerGets: request = { type: 'CONTRACT_CALL', value: '1000000', calldata: JSON.stringify({ xrplTxType: 'OfferCreate', TakerGets: '1000000', TakerPays: { currency: 'USD', issuer: 'rIssuer', value: '100' }, Flags: 0x00080000 }), actionProvider: 'xrpl_dex' }, expected USD = 1.0 * mockNativePrice
       - Test IOU TakerGets: request = { value: undefined, calldata: JSON.stringify({ xrplTxType: 'OfferCreate', TakerGets: { currency: 'USD', issuer: 'rIssuer', value: '100' }, TakerPays: '5000000' }), actionProvider: 'xrpl_dex' }, expected = notListed
       - Test OfferCancel: calldata with xrplTxType='OfferCancel' -> success, $0
       - Test standard CONTRACT_CALL without actionProvider: unchanged
       - Test malformed calldata: calldata='not-json' -> fallback to value-based

    2. Modify the CONTRACT_CALL case in resolveEffectiveAmountUsd:
       - Before the existing value-based logic, check if request has actionProvider === 'xrpl_dex' AND calldata is a string
       - If so, try JSON.parse(calldata) and check xrplTxType
       - For 'OfferCreate': extract TakerGets
         - If TakerGets is string (XRP drops): use existing native price logic with XRP decimals (6)
         - If TakerGets is object (IOU): return { type: 'notListed', tokenAddress: `${TakerGets.currency}.${TakerGets.issuer}`, chain }
       - For 'OfferCancel': return { type: 'success', usdAmount: 0, isStale: false }
       - For any parse error or unknown xrplTxType: fall through to existing value-based logic
       - Wrap XRPL-specific logic in try/catch so any error falls through to existing behavior

    3. Important: Do NOT change the function signature. The actionProvider field is already present on the request object (ContractCallRequest includes it).

    Security: When TakerGets is IOU, returning 'notListed' triggers the safe policy fallback (REQUIREMENTS out of scope says IOU pricing is separate milestone). The policy engine treats 'notListed' as "unknown price != $0" which may block or escalate the transaction -- this is the desired secure behavior.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/resolve-effective-amount-usd.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - XRP TakerGets: correct USD amount calculated from drops
    - IOU TakerGets: returns notListed (safe fallback)
    - OfferCancel: $0 (no spending)
    - Standard CONTRACT_CALL: unchanged
    - All existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: SPENDING_LIMIT + ALLOWED_TOKENS 정책 적용 검증 테스트</name>
  <files>packages/daemon/src/__tests__/resolve-effective-amount-usd.test.ts</files>
  <action>
    Add integration-style tests in the existing test file to verify the policy flow:

    1. Test 'XRPL DEX swap with XRP TakerGets applies spending limit':
       - Create CONTRACT_CALL request with actionProvider='xrpl_dex', value='50000000' (50 XRP), calldata with OfferCreate
       - Mock oracle returns XRP price $2.50
       - Expect: success, usdAmount = 125.0
       - This proves SPENDING_LIMIT can evaluate the $125 against daily limit

    2. Test 'XRPL DEX swap with IOU TakerGets triggers notListed for safe policy handling':
       - Create CONTRACT_CALL request with actionProvider='xrpl_dex', no value, calldata with IOU TakerGets
       - Expect: notListed with tokenAddress containing currency.issuer
       - This proves the policy engine will treat it as unknown (not $0), preventing bypass

    3. Test 'XRPL DEX does not interfere with non-XRPL CONTRACT_CALL':
       - Create standard CONTRACT_CALL with value='1000000000000000000' (1 ETH), no actionProvider
       - Expect: unchanged behavior (ETH price based)

    4. Test edge case: 'XRPL DEX with stale oracle price':
       - Mock getNativePrice returns isStale: true
       - Verify isStale propagates in result

    Note: ALLOWED_TOKENS policy is already applied by stage3-policy.ts for CONTRACT_CALL type. The token filtering checks token addresses in the request. For XRPL DEX, the 'to' field contains the issuer address, which is the contract address equivalent. No changes needed in ALLOWED_TOKENS evaluator -- it already works for CONTRACT_CALL type.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/resolve-effective-amount-usd.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All XRPL DEX policy integration tests pass
    - POL-01 verified: TakerGets USD correctly resolved
    - POL-02 verified: SPENDING_LIMIT applies via resolveEffectiveAmountUsd, ALLOWED_TOKENS applies via existing CONTRACT_CALL path
  </done>
</task>

</tasks>

<verification>
```bash
# All resolve-effective-amount-usd tests pass
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/resolve-effective-amount-usd.test.ts

# No regressions in pipeline tests
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/pipeline --reporter=verbose 2>&1 | tail -20

# Type check passes
cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm turbo run typecheck --filter=@waiaas/daemon 2>&1 | tail -10
```
</verification>

<success_criteria>
1. resolveEffectiveAmountUsd가 XRPL DEX OfferCreate의 XRP TakerGets를 정확한 USD로 변환한다
2. IOU TakerGets는 notListed로 반환되어 "unknown price != $0" 보안 원칙이 적용된다
3. OfferCancel은 $0으로 처리된다 (금전적 가치 없음)
4. 기존 EVM/Solana CONTRACT_CALL 동작에 영향 없다
5. 모든 테스트 통과, 타입 체크 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-policy-interface-integration/03-01-SUMMARY.md`
</output>
