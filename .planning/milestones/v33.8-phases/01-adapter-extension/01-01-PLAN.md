---
phase: 01-adapter-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/adapters/ripple/src/adapter.ts
  - packages/adapters/ripple/src/__tests__/ripple-adapter.test.ts
autonomous: true
requirements: [ADPT-01, ADPT-02]

must_haves:
  truths:
    - "buildContractCall()에 OfferCreate calldata JSON을 전달하면 autofill된 UnsignedTransaction이 반환된다"
    - "buildContractCall()에 OfferCancel calldata JSON을 전달하면 autofill된 UnsignedTransaction이 반환된다"
    - "calldata가 없는 buildContractCall() 호출은 기존과 동일하게 INVALID_INSTRUCTION 에러를 던진다"
    - "지원하지 않는 xrplTxType은 INVALID_INSTRUCTION 에러를 던진다"
  artifacts:
    - path: "packages/adapters/ripple/src/adapter.ts"
      provides: "buildContractCall() with OfferCreate/OfferCancel routing via calldata JSON"
      contains: "OfferCreate"
    - path: "packages/adapters/ripple/src/__tests__/ripple-adapter.test.ts"
      provides: "buildContractCall OfferCreate/OfferCancel tests"
      contains: "OfferCreate"
  key_links:
    - from: "packages/adapters/ripple/src/adapter.ts"
      to: "xrpl client.autofill()"
      via: "buildContractCall -> buildXrplNativeTx -> autofill"
      pattern: "client\\.autofill"
---

<objective>
RippleAdapter.buildContractCall()을 확장하여 calldata JSON에서 OfferCreate/OfferCancel 네이티브 트랜잭션을 빌드하도록 한다.

Purpose: XRPL DEX 액션이 기존 6-stage pipeline의 CONTRACT_CALL 경로를 그대로 사용할 수 있도록 adapter 레벨에서 XRPL 네이티브 트랜잭션 빌드를 지원한다.
Output: buildContractCall()이 calldata JSON의 xrplTxType 필드로 OfferCreate/OfferCancel을 분기하여 autofill된 UnsignedTransaction을 반환한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

@packages/adapters/ripple/src/adapter.ts
@packages/adapters/ripple/src/__tests__/ripple-adapter.test.ts
@packages/core/src/interfaces/chain-adapter.types.ts

<interfaces>
<!-- ContractCallParams interface from @waiaas/core -->
From packages/core/src/interfaces/chain-adapter.types.ts:
```typescript
export interface ContractCallParams {
  from: string;
  to: string; // contract address
  calldata?: string; // hex-encoded (will be JSON for XRPL)
  abi?: Record<string, unknown>[];
  value?: bigint;
  programId?: string;
  instructionData?: Uint8Array;
  accounts?: Array<{ pubkey: string; isSigner: boolean; isWritable: boolean }>;
}
```

From packages/adapters/ripple/src/adapter.ts (buildTransaction pattern):
```typescript
// autofill populates Sequence, Fee, LastLedgerSequence
const autofilled = await client.autofill(payment);
const baseFee = BigInt(autofilled.Fee ?? '12');
const safeFee = (baseFee * FEE_SAFETY_NUMERATOR) / FEE_SAFETY_DENOMINATOR;
autofilled.Fee = safeFee.toString();
const txJson = JSON.stringify(autofilled);
const serialized = new TextEncoder().encode(txJson);
// ... return UnsignedTransaction
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend buildContractCall() with OfferCreate/OfferCancel routing</name>
  <files>packages/adapters/ripple/src/adapter.ts, packages/adapters/ripple/src/__tests__/ripple-adapter.test.ts</files>
  <behavior>
    - Test: buildContractCall with OfferCreate calldata returns UnsignedTransaction with chain='ripple', serialized containing OfferCreate, metadata.originalTx containing TakerPays/TakerGets
    - Test: buildContractCall with OfferCancel calldata returns UnsignedTransaction with chain='ripple', serialized containing OfferCancel, metadata.originalTx containing OfferSequence
    - Test: buildContractCall with OfferCreate including Flags and Expiration fields preserves them in the autofilled tx
    - Test: buildContractCall without calldata still throws ChainError with code INVALID_INSTRUCTION
    - Test: buildContractCall with calldata containing unknown xrplTxType throws ChainError with code INVALID_INSTRUCTION
    - Test: buildContractCall with non-JSON calldata (e.g., EVM hex) throws ChainError with code INVALID_INSTRUCTION
  </behavior>
  <action>
1. Add `import type { OfferCreate, OfferCancel } from 'xrpl';` to adapter.ts imports (line 13).

2. Extract a private `buildXrplNativeTx()` helper method that encapsulates the shared autofill/fee-margin/serialize pattern already used in `buildTransaction()`. This helper accepts a `Transaction` object and returns `UnsignedTransaction`. The pattern is:
   - `const client = this.getClient();`
   - `const autofilled = await client.autofill(tx);`
   - Apply fee safety margin: `(BigInt(autofilled.Fee ?? '12') * 120n) / 100n`
   - Serialize to JSON bytes via `new TextEncoder().encode(JSON.stringify(autofilled))`
   - Calculate expiresAt from LastLedgerSequence
   - Return `{ chain: 'ripple', serialized, estimatedFee, expiresAt, metadata: { Sequence, LastLedgerSequence, Fee, originalTx: autofilled }, nonce: autofilled.Sequence }`

3. Replace the current `buildContractCall()` implementation (lines 509-513) with:
   ```typescript
   async buildContractCall(request: ContractCallParams): Promise<UnsignedTransaction> {
     if (request.calldata) {
       let parsed: Record<string, unknown>;
       try {
         parsed = JSON.parse(request.calldata) as Record<string, unknown>;
       } catch {
         throw new ChainError('INVALID_INSTRUCTION', 'ripple', {
           message: 'XRPL does not support smart contracts. Invalid calldata JSON.',
         });
       }

       const xrplTxType = parsed['xrplTxType'] as string | undefined;
       switch (xrplTxType) {
         case 'OfferCreate': {
           const offer: OfferCreate = {
             TransactionType: 'OfferCreate',
             Account: request.from,
             TakerPays: parsed['TakerPays'] as Amount,
             TakerGets: parsed['TakerGets'] as Amount,
             ...(parsed['Flags'] !== undefined && { Flags: parsed['Flags'] as number }),
             ...(parsed['Expiration'] !== undefined && { Expiration: parsed['Expiration'] as number }),
             ...(parsed['OfferSequence'] !== undefined && { OfferSequence: parsed['OfferSequence'] as number }),
           };
           return this.buildXrplNativeTx(offer);
         }
         case 'OfferCancel': {
           const cancel: OfferCancel = {
             TransactionType: 'OfferCancel',
             Account: request.from,
             OfferSequence: parsed['OfferSequence'] as number,
           };
           return this.buildXrplNativeTx(cancel);
         }
         default:
           throw new ChainError('INVALID_INSTRUCTION', 'ripple', {
             message: `Unsupported XRPL transaction type: ${xrplTxType ?? 'none'}. Use calldata with xrplTxType: OfferCreate | OfferCancel.`,
           });
       }
     }

     throw new ChainError('INVALID_INSTRUCTION', 'ripple', {
       message: 'XRPL does not support smart contracts. Use calldata with xrplTxType for native DEX operations.',
     });
   }
   ```

4. Import `Amount` type from xrpl: `import type { OfferCreate, OfferCancel, Amount } from 'xrpl';` -- but Amount may already be imported via Payment. Check and add if needed. If `Amount` is not directly importable, use the inline type (xrpl Amount = string | IssuedCurrencyAmount).

5. Update comment above buildContractCall from `// -- Contract operations (2) -- Unsupported` to `// -- Contract operations (2) -- XRPL native tx via calldata JSON`.

6. Update existing test `buildContractCall throws INVALID_INSTRUCTION` in the unsupported methods section to verify it still throws when no calldata is provided (keep the test, update description to clarify: "buildContractCall without calldata throws INVALID_INSTRUCTION").

7. Add new describe block `describe('buildContractCall -- XRPL native tx routing')` with tests for:
   - OfferCreate: mock `client.autofill` to return an autofilled tx, verify UnsignedTransaction shape
   - OfferCancel: same pattern
   - OfferCreate with Flags/Expiration: verify fields are passed through
   - Unknown xrplTxType: verify INVALID_INSTRUCTION
   - Non-JSON calldata (e.g., "0xabcdef"): verify INVALID_INSTRUCTION
   - Use the same mock pattern as existing buildTransaction tests: mock `this.getClient().autofill()` to return the tx with added Sequence/Fee/LastLedgerSequence fields.
  </action>
  <verify>
    <automated>cd packages/adapters/ripple && npx vitest run src/__tests__/ripple-adapter.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - buildContractCall with OfferCreate calldata returns valid UnsignedTransaction with autofilled fields
    - buildContractCall with OfferCancel calldata returns valid UnsignedTransaction
    - buildContractCall without calldata throws INVALID_INSTRUCTION (backward compatible)
    - buildContractCall with unknown xrplTxType throws INVALID_INSTRUCTION
    - All existing ripple-adapter tests still pass
  </done>
</task>

</tasks>

<verification>
```bash
# All ripple adapter tests pass
cd packages/adapters/ripple && npx vitest run --reporter=verbose 2>&1 | tail -20

# Type check passes
npx tsc --noEmit -p packages/adapters/ripple/tsconfig.json
```
</verification>

<success_criteria>
1. buildContractCall()에 `{xrplTxType: 'OfferCreate', TakerPays: ..., TakerGets: ...}` calldata를 전달하면 autofill된 UnsignedTransaction이 반환된다
2. buildContractCall()에 `{xrplTxType: 'OfferCancel', OfferSequence: N}` calldata를 전달하면 autofill된 UnsignedTransaction이 반환된다
3. calldata 없는 호출은 기존과 동일하게 INVALID_INSTRUCTION을 던진다
4. 모든 기존 테스트가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/01-adapter-extension/01-01-SUMMARY.md`
</output>
