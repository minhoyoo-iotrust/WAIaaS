---
phase: 02-xrpldexprovider-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/actions/src/providers/xrpl-dex/index.ts
  - packages/actions/src/providers/xrpl-dex/__tests__/xrpl-dex-provider.test.ts
autonomous: true
requirements: [DEX-01, DEX-02, DEX-03, DEX-04, DEX-05, DEX-07]

must_haves:
  truths:
    - "에이전트가 XRP/IOU 즉시 스왑을 실행하면 tfImmediateOrCancel OfferCreate ContractCallRequest가 반환된다"
    - "에이전트가 IOU/IOU 즉시 스왑을 실행하면 동일한 tfImmediateOrCancel 패턴으로 처리된다"
    - "슬리피지 초과 시 TakerPays가 슬리피지 적용된 최소 수량으로 설정된다"
    - "지정가 주문 생성 시 tfImmediateOrCancel 없이 Expiration이 설정된 OfferCreate가 반환된다"
    - "주문 취소 시 OfferCancel ContractCallRequest가 반환된다"
    - "IOU 수신 시 Trust Line이 없으면 TrustSet ContractCallRequest가 선행 반환된다"
  artifacts:
    - path: "packages/actions/src/providers/xrpl-dex/index.ts"
      provides: "XrplDexProvider class implementing IActionProvider with 5 actions"
      exports: ["XrplDexProvider"]
      min_lines: 150
    - path: "packages/actions/src/providers/xrpl-dex/__tests__/xrpl-dex-provider.test.ts"
      provides: "Provider resolve() tests for swap/limit/cancel with trust line auto-setup"
      min_lines: 100
  key_links:
    - from: "packages/actions/src/providers/xrpl-dex/index.ts"
      to: "packages/actions/src/providers/xrpl-dex/offer-builder.ts"
      via: "import buildSwapParams, buildLimitOrderParams, buildCancelParams, formatXrplAmount"
      pattern: "buildSwapParams|buildLimitOrderParams|buildCancelParams"
    - from: "packages/actions/src/providers/xrpl-dex/index.ts"
      to: "packages/actions/src/providers/xrpl-dex/orderbook-client.ts"
      via: "this.orderbookClient.checkTrustLine, getAccountReserve"
      pattern: "orderbookClient\\."
    - from: "packages/actions/src/providers/xrpl-dex/index.ts"
      to: "@waiaas/core ContractCallRequest"
      via: "resolve() returns ContractCallRequest for on-chain actions"
      pattern: "ContractCallRequest"
---

<objective>
Implement XrplDexProvider with 3 on-chain actions (swap, limit_order, cancel_order) that return ContractCallRequest objects for the 6-stage pipeline. Includes Trust Line auto-setup (DEX-07) and reserve pre-validation (DEX-08 via offer-builder).

Purpose: This is the core provider that enables agents to execute XRPL DEX trades through the existing pipeline.
Output: XrplDexProvider class + comprehensive tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/02-xrpldexprovider-core/02-01-SUMMARY.md

@packages/actions/src/providers/jupiter-swap/index.ts (reference: IActionProvider pattern)
@packages/actions/src/providers/xrpl-dex/schemas.ts (from Plan 01)
@packages/actions/src/providers/xrpl-dex/offer-builder.ts (from Plan 01)
@packages/actions/src/providers/xrpl-dex/orderbook-client.ts (from Plan 01)

<interfaces>
<!-- From Plan 01 schemas.ts -->
export const SwapInputSchema: z.ZodObject<{...}>;
export const LimitOrderInputSchema: z.ZodObject<{...}>;
export const CancelOrderInputSchema: z.ZodObject<{...}>;
export const GetOrderbookInputSchema: z.ZodObject<{...}>;
export const GetOffersInputSchema: z.ZodObject<{...}>;

<!-- From Plan 01 offer-builder.ts -->
export function formatXrplAmount(token: string, amount: string): string | IssuedCurrencyAmount;
export function buildSwapParams(input: SwapInput): { xrplTxType: 'OfferCreate'; TakerGets: Amount; TakerPays: Amount; Flags: number };
export function buildLimitOrderParams(input: LimitOrderInput): { xrplTxType: 'OfferCreate'; TakerGets: Amount; TakerPays: Amount; Expiration?: number };
export function buildCancelParams(offerSequence: number): { xrplTxType: 'OfferCancel'; OfferSequence: number };
export function validateReserve(availableXrpDrops: string, existingOfferCount: number): void;

<!-- From Plan 01 orderbook-client.ts -->
export class XrplOrderbookClient {
  constructor(rpcUrl: string);
  async ensureConnected(): Promise<void>;
  async disconnect(): Promise<void>;
  async getOrderbook(base, counter, limit): Promise<OrderbookResult>;
  async getAccountOffers(account: string, limit: number): Promise<AccountOffer[]>;
  async checkTrustLine(account: string, currency: string, issuer: string): Promise<boolean>;
  async getAccountReserve(account: string): Promise<ReserveInfo>;
}

<!-- From @waiaas/core -->
export interface IActionProvider {
  readonly metadata: ActionProviderMetadata;
  readonly actions: readonly ActionDefinition[];
  resolve(actionName: string, params: Record<string, unknown>, context: ActionContext): Promise<ContractCallRequest | ApiDirectResult | ContractCallRequest[]>;
}

export interface ContractCallRequest {
  type: 'CONTRACT_CALL';
  to: string;
  value?: string;
  calldata?: string;
  actionProvider?: string;
  actionName?: string;
}

<!-- From @waiaas/adapter-ripple -->
export function parseTrustLineToken(tokenAddress: string): { currency: string; issuer: string };

<!-- Phase 1 established: calldata JSON format -->
calldata: JSON.stringify({
  xrplTxType: 'OfferCreate' | 'OfferCancel',
  TakerGets: Amount,
  TakerPays: Amount,
  Flags?: number,
  Expiration?: number,
  OfferSequence?: number, // for cancel
})
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: XrplDexProvider on-chain actions (swap + limit_order + cancel_order)</name>
  <files>packages/actions/src/providers/xrpl-dex/index.ts, packages/actions/src/providers/xrpl-dex/__tests__/xrpl-dex-provider.test.ts</files>
  <behavior>
    - resolve("swap", {takerGets:"XRP", takerGetsAmount:"1000000", takerPays:"USD.rIssuer", takerPaysAmount:"0.5", slippageBps:50}) returns ContractCallRequest with calldata containing tfImmediateOrCancel OfferCreate
    - resolve("swap", ...) for IOU-IOU pair returns ContractCallRequest with both sides as IOU objects
    - resolve("swap", ...) when TakerPays is IOU and no trust line exists -> returns [TrustSetRequest, SwapRequest] array (2-step)
    - resolve("limit_order", ...) returns ContractCallRequest without tfImmediateOrCancel, with Expiration
    - resolve("limit_order", ...) validates reserve before returning (calls validateReserve)
    - resolve("cancel_order", {offerSequence: 123}) returns ContractCallRequest with OfferCancel calldata
    - metadata.name = "xrpl_dex", chains = ["ripple"], mcpExpose = true, requiresSigningKey = false
    - 5 actions defined: swap (medium/INSTANT), limit_order (medium/DELAY), cancel_order (low/INSTANT), get_orderbook (low/INSTANT), get_offers (low/INSTANT)
    - Unknown action name throws ChainError
  </behavior>
  <action>
    Create `packages/actions/src/providers/xrpl-dex/index.ts`:

    Class `XrplDexProvider implements IActionProvider`:

    **Constructor:** `constructor(orderbookClient: XrplOrderbookClient)` -- inject client for RPC queries.

    **metadata:**
    ```
    name: 'xrpl_dex', displayName: 'XRPL DEX', description: 'XRPL native orderbook DEX for token swaps and limit orders',
    version: '1.0.0', chains: ['ripple'], mcpExpose: true, requiresApiKey: false, requiresSigningKey: false
    ```

    **actions:** 5 ActionDefinitions:
    - swap: riskLevel 'medium', defaultTier 'INSTANT', inputSchema SwapInputSchema
    - limit_order: riskLevel 'medium', defaultTier 'DELAY', inputSchema LimitOrderInputSchema
    - cancel_order: riskLevel 'low', defaultTier 'INSTANT', inputSchema CancelOrderInputSchema
    - get_orderbook: riskLevel 'low', defaultTier 'INSTANT', inputSchema GetOrderbookInputSchema
    - get_offers: riskLevel 'low', defaultTier 'INSTANT', inputSchema GetOffersInputSchema

    **resolve() method:**

    For "swap":
    1. Parse input with SwapInputSchema
    2. Check if TakerPays is IOU -> check trust line via orderbookClient.checkTrustLine
    3. If no trust line, build TrustSet ContractCallRequest first:
       ```
       { type: 'CONTRACT_CALL', to: issuer, calldata: JSON.stringify({ xrplTxType: 'TrustSet', LimitAmount: { currency, issuer, value: '1000000000000000' } }), actionProvider: 'xrpl_dex', actionName: 'swap' }
       ```
       Wait -- TrustSet is already handled by RippleAdapter.buildApprove(). Instead, return a 2-element array where first element is an APPROVE request:
       ```
       { type: 'APPROVE', to: issuer, token: `${currency}.${issuer}`, value: '1000000000000000', actionProvider: 'xrpl_dex', actionName: 'trust_set' }
       ```
       Actually, checking the pipeline: ContractCallRequest[] is the supported multi-step return. For trust line auto-setup, use calldata with xrplTxType: 'TrustSet' since buildContractCall already handles TrustSet in Phase 1.

       Wait -- Phase 1 only added OfferCreate/OfferCancel to buildContractCall. TrustSet is handled by buildApprove() via the APPROVE pipeline path. So for Trust Line auto-setup, return the first element as:
       ```
       { type: 'APPROVE', to: `${currency}.${issuer}`, actionProvider: 'xrpl_dex', actionName: 'trust_set_auto' }
       ```
       Hmm, the return type is `ContractCallRequest | ApiDirectResult | ContractCallRequest[]`. An APPROVE type doesn't fit ContractCallRequest. Let me reconsider.

       The simplest approach: use calldata JSON with xrplTxType: 'TrustSet' and add TrustSet routing to buildContractCall. BUT Phase 1 only added OfferCreate/OfferCancel. We need to also add TrustSet routing.

       BETTER: Since RippleAdapter.buildContractCall() already handles xrplTxType routing, add 'TrustSet' to the xrplTxType discriminator. The TrustSet calldata JSON: `{ xrplTxType: 'TrustSet', LimitAmount: { currency, issuer, value } }`. This is a small adapter extension within this plan.

       ACTUALLY: We should NOT modify the adapter in this plan (file ownership conflict with Phase 1). Instead, throw a clear error with actionable message: "Trust line for {currency}.{issuer} is required. Create one first via the trust_set endpoint." This is simpler and avoids multi-step complexity. The AI agent can call the existing trust_set/approve endpoint first.

       FINAL DECISION: For DEX-07, the provider checks trust line and throws a descriptive ChainError('PRECONDITION_FAILED') with message indicating which trust line is needed. The error message includes actionable guidance. This is consistent with how other providers handle prerequisites (e.g., Aave checks health factor before borrow). Auto TrustSet via 2-step array is too complex for this plan and requires adapter changes.

       UPDATE: Re-reading DEX-07 requirement: "IOU 수신 시 Trust Line이 없으면 자동으로 TrustSet을 선행 실행한다". This requires auto-execution, not just an error. So we DO need 2-step. Since buildContractCall already routes by xrplTxType, we can add TrustSet routing in this plan as a small extension to the adapter (1 additional case in the switch). The file is also modified by Plan 01 but Plan 01 is already completed.

    For trust line auto-setup (DEX-07):
    1. Check trust line via orderbookClient.checkTrustLine()
    2. If missing, prepend a TrustSet ContractCallRequest to the result array:
       ```typescript
       const trustSetCalldata = JSON.stringify({
         xrplTxType: 'TrustSet',
         LimitAmount: { currency, issuer, value: '1000000000000000' }
       });
       ```
       Return [trustSetRequest, swapRequest] as ContractCallRequest[]
    3. This requires adding 'TrustSet' case to buildContractCall() switch in adapter.ts

    For "swap" core logic:
    1. Call buildSwapParams() from offer-builder (applies slippage, sets tfImmediateOrCancel)
    2. Construct ContractCallRequest:
       - type: 'CONTRACT_CALL'
       - to: TakerPays issuer address (or 'native' if XRP)
       - value: TakerGets drops string if XRP (for pipeline spending tracking)
       - calldata: JSON.stringify(buildSwapParams result)
       - actionProvider: 'xrpl_dex', actionName: 'swap'

    For "limit_order":
    1. Validate reserve via orderbookClient.getAccountReserve() + validateReserve()
    2. Check trust line for TakerPays IOU (same as swap)
    3. Call buildLimitOrderParams() from offer-builder
    4. Return ContractCallRequest (with Expiration in calldata)

    For "cancel_order":
    1. Parse CancelOrderInputSchema
    2. Call buildCancelParams(offerSequence)
    3. Return ContractCallRequest with OfferCancel calldata

    For "get_orderbook" and "get_offers": delegate to Plan 03 (return stub/throw for now, Plan 03 will implement). Actually, Plan 03 will implement these in the same file. So include the full resolve() switch with all 5 cases here, but get_orderbook/get_offers return ApiDirectResult using orderbookClient directly.

    WAIT -- Plan 03 is supposed to handle BOOK-01, BOOK-02, BOOK-03. But they share the same index.ts file. This creates a file ownership conflict. Let me handle all 5 actions in this plan instead, since they all live in the same Provider class. Plan 03 can be eliminated or repurposed.

    REVISED: Include get_orderbook and get_offers in this plan as well. They are simple ApiDirectResult returns using orderbookClient methods already built in Plan 01.

    For "get_orderbook":
    1. Parse GetOrderbookInputSchema
    2. Parse base/counter token strings into BookOfferCurrency using parseTokenToBookOfferCurrency()
    3. Call orderbookClient.getOrderbook(base, counter, limit)
    4. Return ApiDirectResult with { bids, asks, spread }

    For "get_offers":
    1. Parse GetOffersInputSchema
    2. Call orderbookClient.getAccountOffers(context.walletAddress, limit)
    3. Return ApiDirectResult with offers array (each with seq for cancel reference)

    Also extend RippleAdapter.buildContractCall() to handle TrustSet xrplTxType (for DEX-07 auto trust line). Add one case:
    ```
    case 'TrustSet': {
      const trustSet = { TransactionType: 'TrustSet', Account: request.from, LimitAmount: xrplParams.LimitAmount };
      return this.buildXrplNativeTx(trustSet);
    }
    ```

    Create test file mocking XrplOrderbookClient methods. Test:
    - swap XRP->IOU returns correct ContractCallRequest with tfImmediateOrCancel
    - swap IOU->IOU returns correct ContractCallRequest
    - swap with missing trust line returns 2-element array [TrustSet, OfferCreate]
    - limit_order returns ContractCallRequest with Expiration, no IoC flag
    - limit_order with insufficient reserve throws
    - cancel_order returns OfferCancel ContractCallRequest
    - get_orderbook returns ApiDirectResult
    - get_offers returns ApiDirectResult
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx vitest run packages/actions/src/providers/xrpl-dex/__tests__/xrpl-dex-provider.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>XrplDexProvider.resolve() handles all 5 actions correctly: swap with tfImmediateOrCancel + slippage + auto trust line, limit_order with Expiration + reserve check, cancel_order with OfferSequence, get_orderbook with bids/asks/spread ApiDirectResult, get_offers with active orders ApiDirectResult. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend buildContractCall for TrustSet + register provider exports</name>
  <files>packages/adapters/ripple/src/adapter.ts, packages/actions/src/index.ts</files>
  <action>
    1. In `packages/adapters/ripple/src/adapter.ts`, add 'TrustSet' case to the buildContractCall() switch statement (alongside existing OfferCreate/OfferCancel):
    ```typescript
    case 'TrustSet': {
      const trustSet: TrustSet = {
        TransactionType: 'TrustSet' as const,
        Account: request.from,
        LimitAmount: xrplParams.LimitAmount,
        Flags: xrplParams.Flags ?? 0x00020000, // tfSetNoRipple
      };
      return this.buildXrplNativeTx(trustSet as OfferCreate | OfferCancel | TrustSet);
    }
    ```
    Update the buildXrplNativeTx parameter type from `OfferCreate | OfferCancel` to include `TrustSet`:
    ```typescript
    import type { OfferCreate, OfferCancel, TrustSet } from 'xrpl';
    private async buildXrplNativeTx(tx: OfferCreate | OfferCancel | TrustSet): Promise<UnsignedTransaction>
    ```

    2. In `packages/actions/src/index.ts`, add XrplDexProvider export:
    ```typescript
    export { XrplDexProvider } from './providers/xrpl-dex/index.js';
    export { XrplOrderbookClient } from './providers/xrpl-dex/orderbook-client.js';
    ```

    3. Add XrplDexProvider to registerBuiltInProviders() factory array in `packages/actions/src/index.ts`:
    ```typescript
    {
      key: 'xrpl_dex',
      enabledKey: 'actions.xrpl_dex_enabled',
      factory: () => {
        const rpcUrl = settingsReader.get('actions.xrpl_dex_rpc_url') || 'wss://xrplcluster.com';
        const client = new XrplOrderbookClient(rpcUrl);
        return new XrplDexProvider(client);
      },
    },
    ```
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && npx tsc --noEmit --project packages/actions/tsconfig.json && npx tsc --noEmit --project packages/adapters/ripple/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>buildContractCall handles TrustSet xrplTxType, XrplDexProvider exported and registered in registerBuiltInProviders with actions.xrpl_dex_enabled setting gate, type-check passes for both packages</done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/actions/src/providers/xrpl-dex/ --reporter=verbose` -- all provider tests pass
2. `npx tsc --noEmit --project packages/actions/tsconfig.json` -- no type errors
3. `npx tsc --noEmit --project packages/adapters/ripple/tsconfig.json` -- no type errors
4. XrplDexProvider.resolve("swap") returns ContractCallRequest with tfImmediateOrCancel
5. XrplDexProvider.resolve("swap") with missing trust line returns [TrustSet, OfferCreate] array
6. XrplDexProvider.resolve("limit_order") includes Expiration, validates reserve
7. XrplDexProvider.resolve("cancel_order") returns OfferCancel ContractCallRequest
8. XrplDexProvider.resolve("get_orderbook") returns ApiDirectResult with bids/asks
9. XrplDexProvider.resolve("get_offers") returns ApiDirectResult with active orders
</verification>

<success_criteria>
1. XrplDexProvider implements IActionProvider with 5 actions, registered in factory
2. swap returns tfImmediateOrCancel ContractCallRequest with slippage-adjusted TakerPays
3. Trust Line auto-setup returns 2-step [TrustSet, OfferCreate] when needed
4. limit_order validates reserve and sets Ripple epoch Expiration
5. cancel_order returns OfferCancel with OfferSequence
6. get_orderbook returns ApiDirectResult with normalized bids/asks/spread
7. get_offers returns ApiDirectResult with active orders including seq field
8. All tests and type checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-xrpldexprovider-core/02-02-SUMMARY.md`
</output>
