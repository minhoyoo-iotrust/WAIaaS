---
phase: 296-skill-docs-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/admin.skill.md
  - skills/wallet.skill.md
autonomous: true
requirements: [DOC-04, DOC-05]

must_haves:
  truths:
    - "skills/admin.skill.md documents the Human Wallet Apps REST API (GET/POST/PUT/DELETE /v1/admin/wallet-apps) with request/response schemas and curl examples"
    - "skills/admin.skill.md mentions the Human Wallet Apps top-level menu in the Admin UI sidebar (between Security and System)"
    - "skills/admin.skill.md documents the ntfy independent FieldGroup section in Notifications Settings (separate from Other Channels)"
    - "skills/admin.skill.md includes the security notice: AI agents must NEVER request the master password"
    - "skills/wallet.skill.md documents the D'CENT preset with sdk_ntfy approval method in PUT /v1/wallets/{id}/owner examples"
    - "skills/wallet.skill.md shows that applying a D'CENT preset sets approval_method to sdk_ntfy and auto-registers the app in wallet_apps"
    - "skills/wallet.skill.md includes the security notice: AI agents must NEVER request the master password"
  artifacts:
    - path: "skills/admin.skill.md"
      provides: "Human Wallet Apps API docs + Admin UI menu + ntfy section docs"
      contains: "wallet-apps"
    - path: "skills/wallet.skill.md"
      provides: "D'CENT sdk_ntfy owner setup example"
      contains: "sdk_ntfy"
  key_links:
    - from: "skills/admin.skill.md"
      to: "packages/daemon/src/api/routes/wallet-apps.ts"
      via: "REST API documentation"
      pattern: "/v1/admin/wallet-apps"
    - from: "skills/wallet.skill.md"
      to: "packages/core/src/schemas/wallet-preset.ts"
      via: "D'CENT preset documentation"
      pattern: "dcent.*sdk_ntfy"
---

<objective>
Update skills/admin.skill.md and skills/wallet.skill.md to reflect the v29.7 milestone changes:
1. Human Wallet Apps menu and REST API (GET/POST/PUT/DELETE /v1/admin/wallet-apps)
2. D'CENT preset now uses sdk_ntfy approval method (not walletconnect)
3. Notifications Settings tab has ntfy as independent FieldGroup (separated from Other Channels)
4. Wallet app auto-registration on preset application

Purpose: AI agents and operators using skill files need accurate, up-to-date API and Admin UI documentation.
Output: Updated admin.skill.md and wallet.skill.md files.
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/objectives/m29-07-dcent-owner-signing.md

@skills/admin.skill.md
@skills/wallet.skill.md
@packages/daemon/src/api/routes/wallet-apps.ts
@packages/daemon/src/services/signing-sdk/wallet-app-service.ts
@packages/core/src/schemas/wallet-preset.ts
@packages/admin/src/components/layout.tsx (sidebar menu)
@packages/admin/src/pages/notifications.tsx (ntfy FieldGroup)

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/daemon/src/api/routes/wallet-apps.ts:
REST API endpoints (all require masterAuth):
- GET    /v1/admin/wallet-apps       — List all wallet apps with used_by
- POST   /v1/admin/wallet-apps       — Register a new wallet app
- PUT    /v1/admin/wallet-apps/{id}  — Update app toggles (signing_enabled, alerts_enabled)
- DELETE /v1/admin/wallet-apps/{id}  — Remove a wallet app

From packages/daemon/src/services/signing-sdk/wallet-app-service.ts:
```typescript
interface WalletApp {
  id: string;
  name: string;
  displayName: string;
  signingEnabled: boolean;
  alertsEnabled: boolean;
  createdAt: number;
  updatedAt: number;
}

interface WalletAppWithUsedBy extends WalletApp {
  usedBy: Array<{ id: string; label: string }>;
}
```

POST request body (WalletAppCreateRequestSchema):
```json
{ "name": "my-wallet", "display_name": "My Wallet" }
```

PUT request body (WalletAppUpdateRequestSchema):
```json
{ "signing_enabled": true, "alerts_enabled": false }
```

GET response:
```json
{
  "apps": [{
    "id": "uuid",
    "name": "dcent",
    "display_name": "D'CENT Wallet",
    "signing_enabled": true,
    "alerts_enabled": true,
    "used_by": [{"id": "wallet-uuid", "label": "my-wallet"}],
    "created_at": 1707000000,
    "updated_at": 1707000000
  }]
}
```

POST response (201): `{ "app": { ...app fields } }`
PUT response (200): `{ "app": { ...app fields with used_by } }`
DELETE response (200): `{ "ok": true }`
Error: WALLET_APP_DUPLICATE (409), WALLET_APP_NOT_FOUND (404)

From packages/core/src/schemas/wallet-preset.ts:
D'CENT preset uses approvalMethod: 'sdk_ntfy' (changed from 'walletconnect')
Description: "D'CENT hardware wallet with push notification signing"

From packages/admin/src/components/layout.tsx:
Sidebar menu items (in order): Dashboard, Wallets, Actions, Policies, Notifications, Security, Human Wallet Apps, System

From packages/admin/src/pages/notifications.tsx:
- ntfy FieldGroup (independent, with description: "Push notification channel for Human Wallet Apps")
- Other Channels FieldGroup (Discord + Slack only, ntfy removed)
- ntfy section includes link to Human Wallet Apps page (#/wallet-apps)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin.skill.md with Human Wallet Apps API, menu, and ntfy section</name>
  <files>skills/admin.skill.md</files>
  <action>
Update `skills/admin.skill.md` with the following changes. Read the existing file first, then apply edits:

**1. Add Human Wallet Apps API section (new Section 8, before current "8. Error Reference")**

Insert a new section "8. Human Wallet Apps Management (v29.7)" after "7. API Key Management (v1.5)" and before the Error Reference. Renumber "8. Error Reference" → "9. Error Reference" and "9. Related Skill Files" → "10. Related Skill Files".

New section content:

```markdown
## 8. Human Wallet Apps Management (v29.7)

Manage Human Wallet Apps — the wallet applications used by the human operator for signing requests and activity alerts. Apps are tied to ntfy notification topics (`waiaas-sign-{name}` for signing, `waiaas-notify-{name}` for alerts).

When a wallet preset (e.g., D'CENT) is applied to a wallet via `PUT /v1/wallets/{id}/owner`, the corresponding app is automatically registered in the wallet_apps registry.

### GET /v1/admin/wallet-apps -- List Wallet Apps

Returns all registered wallet apps with the wallets using each app.

```bash
curl -s http://localhost:3100/v1/admin/wallet-apps \
  -H 'X-Master-Password: <password>'
```

**Response (200):**
```json
{
  "apps": [
    {
      "id": "<uuid>",
      "name": "dcent",
      "display_name": "D'CENT Wallet",
      "signing_enabled": true,
      "alerts_enabled": true,
      "used_by": [
        {"id": "<wallet-uuid>", "label": "my-wallet"}
      ],
      "created_at": 1707000000,
      "updated_at": 1707000000
    }
  ]
}
```

| Field | Type | Description |
| ----- | ---- | ----------- |
| `id` | string | Wallet app UUID. |
| `name` | string | Unique app identifier (matches `wallets.wallet_type`). |
| `display_name` | string | Human-readable display name. |
| `signing_enabled` | boolean | Whether signing requests are routed to this app. |
| `alerts_enabled` | boolean | Whether activity alerts are sent to this app. |
| `used_by` | array | Wallets using this app (`wallet_type` = app name). |
| `created_at` | integer | Unix timestamp (seconds). |
| `updated_at` | integer | Unix timestamp (seconds). |

### POST /v1/admin/wallet-apps -- Register Wallet App

Register a new wallet app manually.

```bash
curl -s -X POST http://localhost:3100/v1/admin/wallet-apps \
  -H 'Content-Type: application/json' \
  -H 'X-Master-Password: <password>' \
  -d '{"name": "my-custom-wallet", "display_name": "My Custom Wallet"}'
```

**Request body:**

| Field | Type | Required | Description |
| ----- | ---- | -------- | ----------- |
| `name` | string | Yes | Unique app identifier (lowercase, kebab-case). |
| `display_name` | string | Yes | Human-readable display name. |

**Response (201):**
```json
{
  "app": {
    "id": "<uuid>",
    "name": "my-custom-wallet",
    "display_name": "My Custom Wallet",
    "signing_enabled": true,
    "alerts_enabled": true,
    "used_by": [],
    "created_at": 1707000000,
    "updated_at": 1707000000
  }
}
```

Error: `WALLET_APP_DUPLICATE` (409) if an app with the same name already exists.

### PUT /v1/admin/wallet-apps/{id} -- Update App Toggles

Toggle signing and/or alerts for a wallet app.

```bash
curl -s -X PUT http://localhost:3100/v1/admin/wallet-apps/<app-uuid> \
  -H 'Content-Type: application/json' \
  -H 'X-Master-Password: <password>' \
  -d '{"signing_enabled": true, "alerts_enabled": false}'
```

**Request body:**

| Field | Type | Required | Description |
| ----- | ---- | -------- | ----------- |
| `signing_enabled` | boolean | No | Enable/disable signing request routing. |
| `alerts_enabled` | boolean | No | Enable/disable activity alert notifications. |

**Response (200):** Same schema as POST response.

Error: `WALLET_APP_NOT_FOUND` (404) if the app ID does not exist.

### DELETE /v1/admin/wallet-apps/{id} -- Remove Wallet App

Remove a registered wallet app.

```bash
curl -s -X DELETE http://localhost:3100/v1/admin/wallet-apps/<app-uuid> \
  -H 'X-Master-Password: <password>'
```

**Response (200):**
```json
{"ok": true}
```

Error: `WALLET_APP_NOT_FOUND` (404) if the app ID does not exist.
```

**2. Add wallet-apps error codes to the Error Reference section**

In the error reference table (renumbered to section 9), add these two new rows:

| `WALLET_APP_DUPLICATE`   | 409  | Wallet app with the same name already exists.   |
| `WALLET_APP_NOT_FOUND`   | 404  | Wallet app not found by ID.                      |

**3. Update the Settings Management section (section 4)**

In the `signing_sdk` category description table row (around line 477), update the description from:
`Wallet Signing SDK for owner approval.`
to:
`Human Wallet Apps (signing + alerts) configuration. Internal key prefix is signing_sdk for backward compatibility.`

**4. Update the Notification Management section (section 3)**

After the existing notification test endpoint documentation (around line 324), add a brief note:

```markdown
> **ntfy channel:** In Admin UI, ntfy has its own section in Notifications Settings (separate from Other Channels). Per-app notification settings (Signing/Alerts toggles) are managed in Human Wallet Apps.
```

**5. Ensure the security notice is present**

Verify that the existing line `> AI agents must NEVER request the master password. Use only your session token.` is still present near the top of the file. Do not add a duplicate.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c "wallet-apps" skills/admin.skill.md && grep -c "WALLET_APP_DUPLICATE" skills/admin.skill.md && grep -c "Human Wallet Apps" skills/admin.skill.md && grep -c "AI agents must NEVER request the master password" skills/admin.skill.md</automated>
  </verify>
  <done>
    - Section 8 "Human Wallet Apps Management (v29.7)" exists with GET/POST/PUT/DELETE /v1/admin/wallet-apps docs
    - Each endpoint has curl example, request/response schema, and error codes
    - Error Reference section includes WALLET_APP_DUPLICATE (409) and WALLET_APP_NOT_FOUND (404)
    - Settings Management section describes signing_sdk as "Human Wallet Apps" config
    - Notification Management section notes ntfy independent section and Human Wallet Apps link
    - Security notice is present
  </done>
</task>

<task type="auto">
  <name>Task 2: Update wallet.skill.md with D'CENT sdk_ntfy owner setup examples</name>
  <files>skills/wallet.skill.md</files>
  <action>
Update `skills/wallet.skill.md` with the following changes. Read the existing file first, then apply edits:

**1. Update the PUT /v1/wallets/{id}/owner section**

In the existing owner setup section (section 1, around line 178), update the D'CENT preset example. Find the curl example that shows `wallet_type: "dcent"` and update the surrounding context to clarify the D'CENT behavior:

After the existing curl example with `wallet_type: "dcent"`, add an explanation block:

```markdown
When `wallet_type: "dcent"` is specified:
- `approval_method` is automatically set to `sdk_ntfy` (push notification signing via ntfy)
- The D'CENT app is auto-registered in the Human Wallet Apps registry (`wallet_apps` table)
- Signing requests are routed to the `waiaas-sign-dcent` ntfy topic
- Activity alerts are sent to the `waiaas-notify-dcent` ntfy topic (if Alerts toggle is enabled)
- WalletConnect is NOT required — D'CENT uses direct push notification signing
```

**2. Update the Approval methods list**

In the section that documents approval methods (around line 131), find the existing description for `sdk_ntfy`. Update or verify it reads:

```
Approval methods: `sdk_ntfy` (Human Wallet App via ntfy push notifications), `sdk_telegram` (Wallet SDK via Telegram), `walletconnect` (WalletConnect), `telegram_bot` (Telegram Bot), `rest` (REST API polling), or `null` (auto-detect based on infrastructure).
```

Note: The change is from "Wallet SDK via ntfy" to "Human Wallet App via ntfy push notifications" to align with the v29.7 naming.

**3. Update the WalletConnect section note (section 13)**

In the WalletConnect Session Management section, add a note at the beginning:

```markdown
> **Note:** Wallets using `sdk_ntfy` approval method (e.g., D'CENT preset) do not require WalletConnect. WalletConnect is only needed when `approval_method` is set to `walletconnect`. In Admin UI, the WalletConnect section is hidden when `sdk_ntfy` is active.
```

**4. Ensure the security notice is present**

Verify that the existing line `> AI agents must NEVER request the master password. Use only your session token.` is still present in the file. Do not add a duplicate.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && grep -c "sdk_ntfy" skills/wallet.skill.md && grep -c "Human Wallet App" skills/wallet.skill.md && grep -c "waiaas-sign-dcent" skills/wallet.skill.md && grep -c "AI agents must NEVER request the master password" skills/wallet.skill.md</automated>
  </verify>
  <done>
    - D'CENT preset example shows sdk_ntfy behavior (not walletconnect)
    - Explanation block lists: auto-registered in wallet_apps, signing topic waiaas-sign-dcent, alerts topic waiaas-notify-dcent
    - Approval methods list describes sdk_ntfy as "Human Wallet App via ntfy push notifications"
    - WalletConnect section has note about sdk_ntfy wallets not requiring WC
    - Security notice is present
  </done>
</task>

</tasks>

<verification>
1. `grep -c "wallet-apps" skills/admin.skill.md` — at least 10 occurrences (API paths + examples)
2. `grep -c "WALLET_APP_DUPLICATE" skills/admin.skill.md` — at least 2 (error table + POST docs)
3. `grep -c "Human Wallet Apps" skills/admin.skill.md` — at least 5 (section title + references)
4. `grep -c "sdk_ntfy" skills/wallet.skill.md` — at least 3 (approval method + D'CENT explanation)
5. `grep -c "waiaas-sign-dcent" skills/wallet.skill.md` — at least 1 (topic routing example)
6. `grep -c "AI agents must NEVER request the master password" skills/admin.skill.md` — exactly 1
7. `grep -c "AI agents must NEVER request the master password" skills/wallet.skill.md` — exactly 1
8. Both files are valid markdown (no broken links, no unclosed code blocks)
</verification>

<success_criteria>
- DOC-04: skills/admin.skill.md documents Human Wallet Apps REST API (4 endpoints with curl examples + response schemas), mentions the top-level Admin UI menu item, and describes the ntfy independent FieldGroup in Notifications Settings
- DOC-05: skills/wallet.skill.md documents D'CENT sdk_ntfy owner setup (approval_method=sdk_ntfy, auto-registration in wallet_apps, ntfy topic routing, no WalletConnect required)
- Both files retain the security notice: "AI agents must NEVER request the master password. Use only your session token."
- No regressions to existing documentation content
</success_criteria>

<output>
After completion, create `.planning/phases/296-skill-docs-update/296-01-SUMMARY.md`
</output>
