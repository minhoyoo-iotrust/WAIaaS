---
phase: 292-admin-ui-owner-settings
plan: 02
type: execute
wave: 2
depends_on: [292-01]
files_modified:
  - packages/admin/src/__tests__/wallets-preset-dropdown.test.tsx
autonomous: true
requirements: [OWN-01, OWN-02, OWN-03, OWN-04, OWN-05, OWN-06, OWN-07]

must_haves:
  truths:
    - "Test verifies NONE state dropdown shows approval method preview text when preset selected"
    - "Test verifies GRACE state Change button opens dropdown, and saving triggers API call with wallet_type:'dcent' and shows updated approval method"
    - "Test verifies LOCKED state has no edit controls and approval method radios are disabled"
    - "Test verifies WalletConnect section hidden when approvalMethod is sdk_ntfy"
    - "Test verifies WalletConnect section visible when approvalMethod is walletconnect"
  artifacts:
    - path: "packages/admin/src/__tests__/wallets-preset-dropdown.test.tsx"
      provides: "Tests for OWN-01 through OWN-07 requirements"
      contains: "T-OWN-01|T-OWN-02|T-OWN-03|T-OWN-04|T-OWN-05|T-OWN-06|T-OWN-07"
  key_links:
    - from: "packages/admin/src/__tests__/wallets-preset-dropdown.test.tsx"
      to: "packages/admin/src/pages/wallets.tsx"
      via: "imports WalletsPage component"
      pattern: "import.*wallets"
---

<objective>
Add comprehensive tests for all Phase 292 Owner tab UI changes: Wallet Type selection (NONE), Wallet Type change (GRACE), read-only display (LOCKED), approval method preview, and WalletConnect section conditional visibility.

Purpose: Verify all 7 OWN requirements with automated tests to prevent regression.

Output: Updated wallets-preset-dropdown.test.tsx with 7 new test cases (T-OWN-01 through T-OWN-07).
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/292-admin-ui-owner-settings/292-01-PLAN.md

<interfaces>
<!-- Key patterns from the existing test file that executors must follow. -->

From packages/admin/src/__tests__/wallets-preset-dropdown.test.tsx:
```typescript
// Mock data patterns:
const noneWallet = {
  id: 'test-id', name: 'test-wallet', chain: 'solana', network: 'devnet',
  environment: 'testnet', defaultNetwork: 'devnet', publicKey: 'abc123def456',
  status: 'ACTIVE', ownerAddress: null, ownerVerified: null,
  ownerState: 'NONE' as const, approvalMethod: null, walletType: null,
  suspendedAt: null, suspensionReason: null,
  createdAt: 1707609600, updatedAt: null,
};

const graceWallet = {
  ...noneWallet,
  ownerAddress: '0x1234567890abcdef1234567890abcdef12345678',
  ownerState: 'GRACE' as const,
  walletType: 'dcent',
};

// Helper patterns:
function setupApiMocks(walletData: Record<string, unknown>) { ... }
async function renderAndWaitForDetail() { ... }
async function switchToOwnerTab() { ... }

// All mocks already set up: apiGet/apiPut/apiDelete, toast, auth store, layout, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OWN-01 through OWN-07 tests to wallets-preset-dropdown.test.tsx</name>
  <files>packages/admin/src/__tests__/wallets-preset-dropdown.test.tsx</files>
  <action>
Add a new describe block inside the existing test file, after the existing `describe('Admin UI wallet preset dropdown', ...)` block. Add the following mock data constants at the top of the file (after the existing `graceWallet` definition ~line 126):

```typescript
const graceWalletSdkNtfy = {
  ...noneWallet,
  ownerAddress: '0x1234567890abcdef1234567890abcdef12345678',
  ownerState: 'GRACE' as const,
  walletType: 'dcent',
  approvalMethod: 'sdk_ntfy',
};

const graceWalletWc = {
  ...noneWallet,
  ownerAddress: '0x1234567890abcdef1234567890abcdef12345678',
  ownerState: 'GRACE' as const,
  walletType: null,
  approvalMethod: 'walletconnect',
};

const lockedWallet = {
  ...noneWallet,
  ownerAddress: '0x1234567890abcdef1234567890abcdef12345678',
  ownerVerified: true,
  ownerState: 'LOCKED' as const,
  walletType: 'dcent',
  approvalMethod: 'sdk_ntfy',
};
```

Then add the new describe block at the end of the file:

```typescript
describe('Admin UI Owner tab — Phase 292', () => {
  beforeEach(() => {
    currentPath.value = '/wallets/test-id';
  });

  afterEach(() => {
    cleanup();
    vi.clearAllMocks();
  });

  it('T-OWN-01: NONE state — preset selection shows approval method preview', async () => {
    setupApiMocks(noneWallet);
    await renderAndWaitForDetail();
    await switchToOwnerTab();

    // Open editing mode
    fireEvent.click(screen.getByText('Set Owner Address'));
    await waitFor(() => {
      expect(screen.getByText('Wallet Type')).toBeTruthy();
    });

    // Select D'CENT preset
    const select = screen.getByDisplayValue('Custom (manual setup)') as HTMLSelectElement;
    fireEvent.change(select, { target: { value: 'dcent' } });

    // Approval method preview should appear
    await waitFor(() => {
      expect(screen.getByText(/Approval:.*Wallet App \(ntfy\)/)).toBeTruthy();
    });
  });

  it('T-OWN-02: GRACE state — Change button opens Wallet Type dropdown', async () => {
    setupApiMocks(graceWalletSdkNtfy);
    await renderAndWaitForDetail();
    await switchToOwnerTab();

    // Should show current wallet type with change button
    await waitFor(() => {
      expect(screen.getByText("D'CENT Wallet")).toBeTruthy();
    });

    // Click the change pencil button for wallet type
    const changeBtn = screen.getByTitle('Change wallet type');
    expect(changeBtn).toBeTruthy();
    fireEvent.click(changeBtn);

    // Dropdown should appear
    await waitFor(() => {
      const select = screen.getByDisplayValue("D'CENT Wallet") as HTMLSelectElement;
      expect(select).toBeTruthy();
    });
  });

  it('T-OWN-03: LOCKED state — no edit controls, read-only display', async () => {
    setupApiMocks(lockedWallet);
    await renderAndWaitForDetail();
    await switchToOwnerTab();

    // Wallet Type should be displayed
    await waitFor(() => {
      expect(screen.getByText("D'CENT Wallet")).toBeTruthy();
    });

    // No change button for wallet type
    expect(screen.queryByTitle('Change wallet type')).toBeNull();

    // No "Set owner address" edit pencil button
    expect(screen.queryByTitle('Set owner address')).toBeNull();

    // Approval method radios should be disabled
    const radios = document.querySelectorAll('input[name="approval_method"]');
    radios.forEach((radio) => {
      expect((radio as HTMLInputElement).disabled).toBe(true);
    });
  });

  it('T-OWN-04: preset selection preview shows correct approval method text', async () => {
    setupApiMocks(noneWallet);
    await renderAndWaitForDetail();
    await switchToOwnerTab();

    fireEvent.click(screen.getByText('Set Owner Address'));
    await waitFor(() => {
      expect(screen.getByText('Wallet Type')).toBeTruthy();
    });

    // Custom selection (default) — no preview
    expect(screen.queryByText(/Approval:/)).toBeNull();

    // Select D'CENT
    const select = screen.getByDisplayValue('Custom (manual setup)') as HTMLSelectElement;
    fireEvent.change(select, { target: { value: 'dcent' } });

    // Preview appears
    await waitFor(() => {
      expect(screen.getByText(/Approval:.*Wallet App \(ntfy\)/)).toBeTruthy();
    });
  });

  it('T-OWN-05: GRACE state Wallet Type change triggers API call with wallet_type and shows updated approval method', async () => {
    // Start with a GRACE wallet that has no preset (Custom)
    const graceCustomWallet = {
      ...noneWallet,
      ownerAddress: '0x1234567890abcdef1234567890abcdef12345678',
      ownerState: 'GRACE' as const,
      walletType: null,
      approvalMethod: null,
    };
    // After save, API returns wallet with dcent preset applied
    const updatedDcentWallet = {
      ...graceCustomWallet,
      walletType: 'dcent',
      approvalMethod: 'sdk_ntfy',
    };

    setupApiMocks(graceCustomWallet);
    vi.mocked(apiPut).mockResolvedValue({ ...updatedDcentWallet });
    // After save, fetchWallet re-fetches — mock apiGet to return updated wallet
    vi.mocked(apiGet).mockResolvedValue({ ...updatedDcentWallet });
    await renderAndWaitForDetail();
    await switchToOwnerTab();

    // Open wallet type change
    await waitFor(() => {
      expect(screen.getByTitle('Change wallet type')).toBeTruthy();
    });
    fireEvent.click(screen.getByTitle('Change wallet type'));

    // Change to D'CENT (non-empty value to test wallet_type branching)
    await waitFor(() => {
      const select = screen.getByDisplayValue('Custom') as HTMLSelectElement;
      fireEvent.change(select, { target: { value: 'dcent' } });
    });

    // Save
    fireEvent.click(screen.getByText('Save'));

    // Assert API call includes wallet_type: 'dcent'
    await waitFor(() => {
      expect(apiPut).toHaveBeenCalledWith(
        '/v1/wallets/test-id/owner',
        expect.objectContaining({
          owner_address: graceCustomWallet.ownerAddress,
          wallet_type: 'dcent',
        }),
      );
    });

    // Assert the updated approval method is displayed after save
    await waitFor(() => {
      expect(screen.getByText("D'CENT Wallet")).toBeTruthy();
    });
  });

  it('T-OWN-06: WalletConnect section hidden when approvalMethod is sdk_ntfy', async () => {
    setupApiMocks(graceWalletSdkNtfy);
    await renderAndWaitForDetail();
    await switchToOwnerTab();

    // WalletConnect section header should NOT be present
    await waitFor(() => {
      expect(screen.getByText('Approval Method')).toBeTruthy();
    });

    // The h4 "WalletConnect" section should be absent
    const wcHeaders = screen.queryAllByText('WalletConnect');
    // Only the radio option label "WalletConnect" should be present, not the section header
    const sectionHeaders = wcHeaders.filter((el) => el.tagName === 'H4');
    expect(sectionHeaders.length).toBe(0);
  });

  it('T-OWN-07: WalletConnect section visible when approvalMethod is walletconnect', async () => {
    setupApiMocks(graceWalletWc);
    await renderAndWaitForDetail();
    await switchToOwnerTab();

    // WalletConnect section h4 should be present
    await waitFor(() => {
      const wcHeaders = screen.queryAllByText('WalletConnect');
      const sectionHeaders = wcHeaders.filter((el) => el.tagName === 'H4');
      expect(sectionHeaders.length).toBe(1);
    });
  });
});
```

**Notes:**
- Reuse the existing mock setup (vi.mock for api/client, toast, auth/store, layout, settings-search, dirty-guard, display-currency, copy-button, empty-state, error-messages) — these are file-level mocks that apply to all describe blocks.
- Reuse the existing `setupApiMocks`, `renderAndWaitForDetail`, and `switchToOwnerTab` helpers.
- Import `apiPut` is already imported at line 93.
- The test for T-OWN-06 checks that no H4 element with text "WalletConnect" exists (the radio option label has text "WalletConnect" but is not an H4).
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin run test:unit -- --run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>
    - 7 new tests (T-OWN-01 through T-OWN-07) pass
    - All existing tests (T-ADUI-01 through T-ADUI-05) continue to pass
    - Tests cover all 7 OWN requirements: NONE selection (OWN-01), GRACE change (OWN-02), LOCKED read-only (OWN-03), approval preview (OWN-04), auto-update (OWN-05), WC hidden (OWN-06), WC visible (OWN-07)
  </done>
</task>

</tasks>

<verification>
1. All admin tests pass: `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin run test:unit -- --run`
2. TypeScript compiles: `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin exec tsc --noEmit`
3. Lint passes: `cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/admin run lint`
</verification>

<success_criteria>
- All 12 tests pass (5 existing T-ADUI + 7 new T-OWN)
- T-OWN-01: NONE state preset selection shows approval method preview
- T-OWN-02: GRACE state Change button opens dropdown
- T-OWN-03: LOCKED state read-only with disabled radios
- T-OWN-04: Approval preview text is correct for each preset
- T-OWN-05: GRACE wallet_type change triggers API call
- T-OWN-06: WC section hidden when sdk_ntfy
- T-OWN-07: WC section visible when walletconnect
</success_criteria>

<output>
After completion, create `.planning/phases/292-admin-ui-owner-settings/292-02-SUMMARY.md`
</output>
