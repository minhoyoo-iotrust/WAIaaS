---
phase: 397-admin-dashboard-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/daemon/src/api/routes/admin-wallets.ts
  - packages/daemon/src/api/routes/openapi-schemas.ts
  - packages/admin/src/pages/dashboard.tsx
  - packages/admin/src/api/endpoints.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-04
  - DASH-05
  - DASH-06
must_haves:
  truths:
    - "Admin API returns metadata field in defi position response for category-specific rendering"
    - "Admin API supports category query parameter for server-side filtering"
    - "Dashboard shows STAKING/LENDING/YIELD/PERP/ALL category filter tabs"
    - "Dashboard shows wallet selector dropdown to filter by specific wallet"
    - "Dashboard auto-refreshes DeFi data every 30 seconds"
    - "Dashboard shows warning banner when worst Health Factor is below 1.5"
  artifacts:
    - path: "packages/daemon/src/api/routes/admin-wallets.ts"
      provides: "Enhanced admin defi/positions endpoint with metadata and category filter"
    - path: "packages/admin/src/pages/dashboard.tsx"
      provides: "Enhanced DeFi section with category tabs, wallet filter, HF banner"
  key_links:
    - from: "packages/admin/src/pages/dashboard.tsx"
      to: "/v1/admin/defi/positions"
      via: "apiGet with category and wallet_id query params"
      pattern: "apiGet.*admin/defi/positions"
    - from: "packages/admin/src/pages/dashboard.tsx"
      to: "/v1/wallets"
      via: "apiGet for wallet list dropdown"
      pattern: "apiGet.*WALLETS"
---

<objective>
Admin API에 metadata/category 필터를 추가하고, Dashboard에 카테고리 필터 탭 + 지갑 필터 + HF 경고 배너 + 30초 자동 새로고침을 구현한다.

Purpose: DeFi 포지션을 카테고리별/지갑별로 필터링하고 위험 상태를 즉시 인지할 수 있는 UX 기반 구축
Output: 향상된 Admin API + Dashboard DeFi 섹션 UI
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key source files:
@packages/daemon/src/api/routes/admin-wallets.ts (lines 173-214: adminDefiPositionsRoute, lines 619-693: handler)
@packages/admin/src/pages/dashboard.tsx (existing DeFi section lines 454-491)
@packages/admin/src/components/tab-nav.tsx (TabNav component)
@packages/admin/src/components/filter-bar.tsx (FilterBar component)
@packages/admin/src/api/endpoints.ts (API constants)
@packages/admin/src/constants.ts (DASHBOARD_POLL_INTERVAL_MS = 30_000)

<interfaces>
<!-- Admin API current response shape (GET /v1/admin/defi/positions) -->
Response: {
  positions: Array<{
    id: string; walletId: string; category: string; provider: string;
    chain: string; network: string | null; assetId: string | null;
    amount: string; amountUsd: number | null; status: string;
    openedAt: number; lastSyncedAt: number;
    // MISSING: metadata -- need to add
  }>;
  totalValueUsd: number | null;
  worstHealthFactor: number | null;
  activeCount: number;
}

<!-- Existing reusable components -->
TabNav: { tabs: TabItem[], activeTab: string, onTabChange: (key: string) => void }
FilterBar: { fields: FilterField[], values: Record<string,string>, onChange: (values) => void }
Badge: { variant: 'success' | 'danger' | 'warning' | 'info', children }
StatCard: { label, value, loading?, badge?, href? }
Table: { columns, data, loading?, emptyMessage? }
DASHBOARD_POLL_INTERVAL_MS = 30_000

<!-- Wallet list API response -->
GET /v1/wallets -> { items: Array<{ id: string; name: string; chain: string; status: string }> }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin API 확장 (metadata 포함 + category 필터)</name>
  <files>packages/daemon/src/api/routes/admin-wallets.ts, packages/daemon/src/api/routes/openapi-schemas.ts</files>
  <action>
1. In `admin-wallets.ts` adminDefiPositionsRoute (line ~173), add `category` query parameter:
   ```
   query: z.object({
     wallet_id: z.string().uuid().optional(),
     category: z.enum(['STAKING', 'LENDING', 'YIELD', 'PERP']).optional(),
   }),
   ```

2. In the response schema (line ~191), add `metadata` field to positions array:
   ```
   metadata: z.unknown().nullable(),
   ```

3. In the handler (line ~619), add category filter to SQL queries:
   - If `category` is provided, add `AND category = ?` to both wallet_id and all-wallets SQL paths
   - Extract `category` from `c.req.valid('query')` alongside `wallet_id`

4. In the position mapping (line ~653), add parsed metadata to the response:
   ```
   metadata: row.metadata ? (() => { try { return JSON.parse(row.metadata); } catch { return null; } })() : null,
   ```
   Use a cleaner pattern: parse metadata in a separate helper function to avoid inline IIFE.

5. In `openapi-schemas.ts`, if `AdminDefiPositionsResponseSchema` is referenced, update it to include `metadata: z.unknown().nullable()`. If not referenced (inline schema), the route definition change above is sufficient.

Do NOT change the worstHealthFactor extraction logic -- it already works correctly by parsing metadata from raw rows.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/daemon/src/__tests__/admin-defi-positions.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Admin API returns metadata in position objects and supports optional category query parameter filter. Existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard DeFi UX (카테고리 탭 + 지갑 필터 + HF 경고 배너)</name>
  <files>packages/admin/src/pages/dashboard.tsx, packages/admin/src/api/endpoints.ts</files>
  <action>
1. In `dashboard.tsx`, add new signals at the top of DashboardPage:
   - `categoryFilter = useSignal<string>('ALL')` -- active category tab
   - `walletFilter = useSignal<string>('')` -- selected wallet ID
   - `walletList = useSignal<Array<{id: string; name: string}>>([])` -- wallet options

2. Add wallet list fetch in useEffect (alongside existing fetchStatus/fetchDefi/fetchStats):
   ```
   apiGet<{items: Array<{id: string; name: string}>}>(API.WALLETS)
     .then(res => { walletList.value = (Array.isArray(res) ? res : res.items).map(w => ({id: w.id, name: w.name ?? w.id.slice(0,8)})); })
     .catch(() => {});
   ```

3. Modify `fetchDefi` to pass query params based on filters:
   ```
   const params = new URLSearchParams();
   if (walletFilter.value) params.set('wallet_id', walletFilter.value);
   if (categoryFilter.value !== 'ALL') params.set('category', categoryFilter.value);
   const qs = params.toString();
   const url = qs ? `${API.ADMIN_DEFI_POSITIONS}?${qs}` : API.ADMIN_DEFI_POSITIONS;
   const result = await apiGet<DefiPositionSummary>(url);
   ```

4. Re-fetch defi data when filters change. Add useEffect watching categoryFilter and walletFilter:
   ```
   useEffect(() => { fetchDefi(); }, [categoryFilter.value, walletFilter.value]);
   ```

5. Add HF Warning Banner ABOVE the DeFi section (after the auto-provision banner, before stat-grids or at the start of DeFi section):
   ```
   {defiData.value && defiData.value.worstHealthFactor !== null && defiData.value.worstHealthFactor < 1.5 && (
     <div class="hf-warning-banner" role="alert">
       <span class="hf-warning-icon">{'\u26A0'}</span>
       <span>
         <strong>Health Factor Warning:</strong>{' '}
         Worst Health Factor is <strong>{defiData.value.worstHealthFactor.toFixed(2)}</strong>
         {defiData.value.worstHealthFactor < 1.2 ? ' — Liquidation risk is HIGH!' : ' — Monitor closely.'}
       </span>
     </div>
   )}
   ```
   Style using existing banner pattern (similar to `.update-banner` and `.auto-provision-banner` in global.css). Add `.hf-warning-banner` styles to global.css with `background: var(--color-danger-light, #fef2f2); border-left: 4px solid var(--color-danger, #dc2626);`.

6. Replace the current simple DeFi Positions section (lines ~454-491) with enhanced version:
   a. Category filter tabs using inline button group (not TabNav -- Dashboard has no dirty state):
      ```
      const DEFI_CATEGORIES = ['ALL', 'STAKING', 'LENDING', 'YIELD', 'PERP'];
      ```
      Render as a row of buttons with active state styling.

   b. Wallet filter dropdown next to category tabs:
      ```
      <select value={walletFilter.value} onChange={...}>
        <option value="">All Wallets</option>
        {walletList.value.map(w => <option value={w.id}>{w.name}</option>)}
      </select>
      ```

   c. Keep existing StatCards (Total DeFi Value, Health Factor, Active Positions).

   d. Keep existing Table but update DefiPositionSummary interface to include `metadata: unknown`.

7. Update `DefiPositionSummary` interface to include `metadata`:
   ```
   metadata: unknown;
   ```
   in the positions array type.

8. The 30s auto-refresh is already handled by `DASHBOARD_POLL_INTERVAL_MS` in the existing `setInterval`. Ensure `fetchDefi()` in the interval also uses the current filter values (it will, since it reads from signals).

Do NOT add a separate DeFi page -- all changes go in the existing Dashboard page's DeFi section. Do NOT use TabNav component (it has dirty-state integration not needed here). Use simple button group instead.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm vitest run packages/admin/src/__tests__/dashboard-defi.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Dashboard shows category filter tabs (ALL/STAKING/LENDING/YIELD/PERP), wallet selector dropdown, HF warning banner when worstHealthFactor < 1.5, and auto-refreshes with filters applied.</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run packages/daemon/src/__tests__/admin-defi-positions.test.ts` -- existing API tests pass
2. `pnpm vitest run packages/admin/src/__tests__/dashboard-defi.test.tsx` -- existing dashboard DeFi tests pass
3. `pnpm turbo run typecheck --filter=@waiaas/daemon --filter=@waiaas/admin` -- no type errors
</verification>

<success_criteria>
- Admin API `/v1/admin/defi/positions` returns metadata field and supports ?category= filter
- Dashboard DeFi section shows 5 category tabs (ALL, STAKING, LENDING, YIELD, PERP)
- Dashboard shows wallet dropdown filter that filters positions by wallet_id
- HF warning banner appears when worstHealthFactor < 1.5
- 30s auto-refresh works with filter state preserved
</success_criteria>

<output>
After completion, create `.planning/phases/397-admin-dashboard-ux/397-01-SUMMARY.md`
</output>
