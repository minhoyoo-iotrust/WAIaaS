---
phase: 432-interface-extension
plan: 02
type: execute
wave: 2
depends_on: [432-01]
files_modified:
  - packages/actions/src/providers/lido-staking/index.ts
  - packages/actions/src/providers/jito-staking/index.ts
  - packages/actions/src/providers/aave-v3/index.ts
  - packages/actions/src/providers/kamino/index.ts
  - packages/actions/src/providers/pendle/index.ts
  - packages/actions/src/providers/drift/index.ts
  - packages/actions/src/providers/hyperliquid/perp-provider.ts
  - packages/actions/src/providers/hyperliquid/spot-provider.ts
  - packages/actions/src/__tests__/lido-staking.test.ts
  - packages/actions/src/__tests__/jito-staking.test.ts
  - packages/actions/src/__tests__/aave-v3-positions.test.ts
  - packages/actions/src/__tests__/kamino-provider.test.ts
  - packages/actions/src/providers/pendle/__tests__/pendle-provider.test.ts
  - packages/actions/src/__tests__/drift-provider.test.ts
  - packages/actions/src/providers/hyperliquid/__tests__/perp-provider.test.ts
  - packages/actions/src/providers/hyperliquid/__tests__/spot-provider.test.ts
autonomous: true
requirements: [INTF-04, INTF-05, INTF-06, INTF-07, INTF-08, INTF-09, INTF-10, INTF-11, INTF-12]

must_haves:
  truths:
    - "8개 프로바이더가 getPositions(ctx: PositionQueryContext) 시그니처로 동작한다"
    - "Solana 지갑으로 Lido(EVM) 포지션 조회 시 빈 배열을 반환한다"
    - "EVM 지갑으로 Jito(Solana) 포지션 조회 시 빈 배열을 반환한다"
    - "지원하는 체인의 지갑에서는 기존과 동일한 포지션이 반환된다"
  artifacts:
    - path: "packages/actions/src/providers/lido-staking/index.ts"
      provides: "Lido provider with PositionQueryContext signature + chain guard"
      contains: "getPositions(ctx: PositionQueryContext)"
    - path: "packages/actions/src/providers/jito-staking/index.ts"
      provides: "Jito provider with PositionQueryContext signature + chain guard"
      contains: "getPositions(ctx: PositionQueryContext)"
    - path: "packages/actions/src/providers/aave-v3/index.ts"
      provides: "Aave V3 provider with PositionQueryContext signature + chain guard"
      contains: "getPositions(ctx: PositionQueryContext)"
    - path: "packages/actions/src/providers/kamino/index.ts"
      provides: "Kamino provider with PositionQueryContext signature + chain guard"
      contains: "getPositions(ctx: PositionQueryContext)"
    - path: "packages/actions/src/providers/pendle/index.ts"
      provides: "Pendle provider with PositionQueryContext signature + chain guard"
      contains: "getPositions(ctx: PositionQueryContext)"
    - path: "packages/actions/src/providers/drift/index.ts"
      provides: "Drift provider with PositionQueryContext signature + chain guard"
      contains: "getPositions(ctx: PositionQueryContext)"
    - path: "packages/actions/src/providers/hyperliquid/perp-provider.ts"
      provides: "Hyperliquid Perp provider with PositionQueryContext signature + chain guard"
      contains: "getPositions(ctx: PositionQueryContext)"
    - path: "packages/actions/src/providers/hyperliquid/spot-provider.ts"
      provides: "Hyperliquid Spot provider with PositionQueryContext signature + chain guard"
      contains: "getPositions(ctx: PositionQueryContext)"
  key_links:
    - from: "각 프로바이더"
      to: "@waiaas/core PositionQueryContext"
      via: "import type"
      pattern: "import.*PositionQueryContext"
    - from: "각 프로바이더 getPositions"
      to: "ctx.chain 체인 가드"
      via: "early return []"
      pattern: "ctx\\.chain.*return \\[\\]"
---

<objective>
8개 포지션 프로바이더의 getPositions 시그니처를 PositionQueryContext로 마이그레이션하고, 미지원 체인에서 빈 배열을 반환하는 체인 가드를 추가한다.

Purpose: 모든 프로바이더가 통일된 인터페이스로 동작하며, 미지원 체인 호출 시 안전하게 빈 배열을 반환한다.
Output: 8개 프로바이더 마이그레이션 완료 + 체인 가드 테스트
</objective>

<execution_context>
@/Users/minho.yoo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/minho.yoo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/432-interface-extension/432-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: PositionQueryContext type -->

From packages/core/src/interfaces/position-provider.types.ts (after Plan 01):
```typescript
export interface PositionQueryContext {
  walletId: string;
  chain: ChainType;
  networks: readonly NetworkType[];
  environment: EnvironmentType;
  rpcUrls: Record<string, string>;
}

export interface IPositionProvider {
  getPositions(ctx: PositionQueryContext): Promise<PositionUpdate[]>;
  getProviderName(): string;
  getSupportedCategories(): PositionCategory[];
}
```

Provider chain mappings:
- Lido: ethereum only (stETH/wstETH)
- Jito: solana only (JitoSOL)
- Aave V3: ethereum only
- Kamino: solana only
- Pendle: ethereum only (PT/YT)
- Drift: solana only (perp DEX)
- Hyperliquid Perp: ethereum only (Arbitrum/HyperEVM)
- Hyperliquid Spot: ethereum only (Arbitrum/HyperEVM)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EVM 프로바이더 4개 마이그레이션 (Lido, Aave, Pendle, Hyperliquid Perp/Spot)</name>
  <files>
    packages/actions/src/providers/lido-staking/index.ts,
    packages/actions/src/providers/aave-v3/index.ts,
    packages/actions/src/providers/pendle/index.ts,
    packages/actions/src/providers/hyperliquid/perp-provider.ts,
    packages/actions/src/providers/hyperliquid/spot-provider.ts,
    packages/actions/src/__tests__/lido-staking.test.ts,
    packages/actions/src/__tests__/aave-v3-positions.test.ts,
    packages/actions/src/providers/pendle/__tests__/pendle-provider.test.ts,
    packages/actions/src/providers/hyperliquid/__tests__/perp-provider.test.ts,
    packages/actions/src/providers/hyperliquid/__tests__/spot-provider.test.ts
  </files>
  <behavior>
    - Lido getPositions(ctx): ctx.chain !== 'ethereum' -> return []
    - Aave V3 getPositions(ctx): ctx.chain !== 'ethereum' -> return []
    - Pendle getPositions(ctx): ctx.chain !== 'ethereum' -> return []
    - Hyperliquid Perp getPositions(ctx): ctx.chain !== 'ethereum' -> return []
    - Hyperliquid Spot getPositions(ctx): ctx.chain !== 'ethereum' -> return []
    - 각 프로바이더에서 ctx.walletId로 기존 walletId 참조를 대체
    - Solana 지갑(ctx.chain='solana')으로 호출 시 빈 배열 반환
  </behavior>
  <action>
    각 EVM 프로바이더에 대해:

    1. `import type { PositionQueryContext } from '@waiaas/core'` 추가
    2. getPositions 시그니처 변경: `async getPositions(walletId: string)` -> `async getPositions(ctx: PositionQueryContext)`
    3. 메서드 첫 줄에 체인 가드 추가:
       ```typescript
       if (ctx.chain !== 'ethereum') return [];
       ```
    4. 메서드 내부의 `walletId` 참조를 `ctx.walletId`로 변경
    5. Lido: `this.rpcUrl` 대신 `ctx.rpcUrls` 에서도 RPC URL을 가져올 수 있게 하되, 기존 constructor rpcUrl 우선 유지 (Phase 433에서 멀티네트워크 전환 시 완전 전환)

    테스트 파일에서:
    - getPositions 호출부를 PositionQueryContext 객체로 변경
    - 체인 가드 테스트 추가: `it('returns [] for solana wallet', ...)`
    - 기존 정상 동작 테스트가 ctx 객체 사용으로 업데이트

    **Lido 특이사항**: 현재 constructor의 rpcUrl을 사용한다. ctx.rpcUrls는 Phase 433에서 멀티네트워크 포지션 조회 시 활용된다. 이번 Phase에서는 기존 rpcUrl을 유지하되 시그니처만 변경한다.

    **Hyperliquid 특이사항**: getPositions 내부에서 this.marketData를 사용한다. walletId -> ctx.walletId 변경만 수행.
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions run test -- --run packages/actions/src/__tests__/lido-staking.test.ts packages/actions/src/__tests__/aave-v3-positions.test.ts packages/actions/src/providers/pendle/__tests__/pendle-provider.test.ts packages/actions/src/providers/hyperliquid/__tests__/perp-provider.test.ts packages/actions/src/providers/hyperliquid/__tests__/spot-provider.test.ts</automated>
  </verify>
  <done>5개 EVM 프로바이더가 PositionQueryContext 시그니처로 동작하고 체인 가드가 활성화되며 모든 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Solana 프로바이더 3개 마이그레이션 (Jito, Kamino, Drift) + 전체 빌드 검증</name>
  <files>
    packages/actions/src/providers/jito-staking/index.ts,
    packages/actions/src/providers/kamino/index.ts,
    packages/actions/src/providers/drift/index.ts,
    packages/actions/src/__tests__/jito-staking.test.ts,
    packages/actions/src/__tests__/kamino-provider.test.ts,
    packages/actions/src/__tests__/drift-provider.test.ts
  </files>
  <behavior>
    - Jito getPositions(ctx): ctx.chain !== 'solana' -> return []
    - Kamino getPositions(ctx): ctx.chain !== 'solana' -> return []
    - Drift getPositions(ctx): ctx.chain !== 'solana' -> return []
    - 각 프로바이더에서 ctx.walletId로 기존 walletId 참조를 대체
    - EVM 지갑(ctx.chain='ethereum')으로 호출 시 빈 배열 반환
    - 전체 typecheck 통과
  </behavior>
  <action>
    각 Solana 프로바이더에 대해:

    1. `import type { PositionQueryContext } from '@waiaas/core'` 추가
    2. getPositions 시그니처 변경: `async getPositions(walletId: string)` -> `async getPositions(ctx: PositionQueryContext)`
    3. 메서드 첫 줄에 체인 가드 추가:
       ```typescript
       if (ctx.chain !== 'solana') return [];
       ```
    4. 메서드 내부의 `walletId` 참조를 `ctx.walletId`로 변경
    5. Jito: `this.rpcUrl` 체크 유지, ctx.walletId 사용
    6. Kamino: `this.config.market` 등 기존 로직 유지, walletId -> ctx.walletId
    7. Drift: `this.sdkWrapper` 메서드 호출에서 walletId -> ctx.walletId

    테스트 파일에서:
    - getPositions 호출부를 PositionQueryContext 객체로 변경
    - 체인 가드 테스트 추가: `it('returns [] for ethereum wallet', ...)`

    전체 빌드 검증:
    - `pnpm turbo run typecheck` 로 모든 패키지 타입 체크 통과 확인
    - 다른 패키지(daemon 등)에서 IPositionProvider 참조가 있으면 함께 수정
      (daemon의 position-tracker.ts는 Plan 01에서 이미 수정됨)
  </action>
  <verify>
    <automated>cd /Users/minho.yoo/dev/wallet/WAIaaS && pnpm --filter @waiaas/actions run test -- --run packages/actions/src/__tests__/jito-staking.test.ts packages/actions/src/__tests__/kamino-provider.test.ts packages/actions/src/__tests__/drift-provider.test.ts && pnpm turbo run typecheck</automated>
  </verify>
  <done>3개 Solana 프로바이더가 PositionQueryContext 시그니처로 동작하고 체인 가드가 활성화되며, 전체 typecheck 통과</done>
</task>

</tasks>

<verification>
1. 8개 프로바이더 모두 `getPositions(ctx: PositionQueryContext)` 시그니처를 사용한다
2. EVM 프로바이더에 solana ctx 전달 시 빈 배열 반환
3. Solana 프로바이더에 ethereum ctx 전달 시 빈 배열 반환
4. `pnpm turbo run typecheck` 전체 통과
5. 관련 테스트 파일 전체 통과
</verification>

<success_criteria>
- 8개 프로바이더가 PositionQueryContext 시그니처로 동작한다
- 미지원 체인 프로바이더가 에러 없이 빈 배열을 반환한다
- 전체 typecheck 통과한다
- 기존 포지션 조회 동작이 변경되지 않았다 (지원 체인에서의 동작 유지)
</success_criteria>

<output>
After completion, create `.planning/phases/432-interface-extension/432-02-SUMMARY.md`
</output>
